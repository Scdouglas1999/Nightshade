# Nightshade 5.0.0

Version 5.0.0 (build 23)

This is a big one — everything since 4.3.0, 80 commits in all. The headline is
**Living Sky**: a personal, growing atlas of everything you've imaged, a "First
Light" path that turns a night's captures into real scientific contributions, and
a Constellation mode that lets more than one rig work the same target together.
But there's a lot more underneath: **live master/slave mirroring** so you can run
your whole rig from another machine, a unified **automation core**, a reorganized
set of screens, **S3-compatible cloud backup**, and a deep layer of security,
safety, and reliability hardening. The notes below walk through the major changes,
and a complete categorized list of every commit follows at the end.

## Living Sky

Living Sky is built on a new native imaging engine and bridge, with new database
storage for the atlas and your transient/discovery history. It comes in three
pillars.

### Your Sky — an atlas that grows as you image

Your Sky is a personal atlas. Every target you capture is folded into it
automatically when a frame solves, and the map fills in over time as a record of
where you've actually pointed and what you collected. You can name regions, watch
coverage grow, browse real pixel crops of what you've shot, and see your whole sky
at a glance on an all-sky map.

### First Light — from a capture to a real contribution

First Light scans solved frames for transients, variable stars, and minor planets,
and turns a detection into something you can publish. Nightshade packages the
evidence and submits it to the appropriate catalog through a real submission path —
TNS for transients, AAVSO for variable-star photometry, the Minor Planet Center for
astrometry. A "Chase" flow helps you re-observe a target, and there's a live light
curve for photometry runs. The emphasis throughout is honesty: the app only
presents a measurement as science when it actually is one, and shows its working
rather than a confident-looking number it can't stand behind.

### Constellation — more than one rig on the same sky

Constellation lets a set of rigs cooperate. One machine coordinates while others
join in, advertise their reachability, claim and cleanly retract regions, and share
the raw sub-frames each rig captures so the group's data lands in one place rather
than siloed per machine. Master and slave roles stay in parity, and the system was
hardened for scale and retention so a long-running swarm stays healthy.

A dedicated ship-readiness pass (116 reviewed findings across several waves) went
into making these pillars actually trustworthy before release — stopping atlas and
Constellation data corruption, tightening the science-honesty story, and proving
master/slave parity with tests.

## Run your rig from another machine — live master/slave mirroring

Nightshade can now run as a **live master/slave pair**. One machine acts as the
master running the session; another runs in **desktop slave mode** and mirrors it —
unified equipment profiles, full state sync, and even the host's autopilot
decisions reflected on the slave. A focused triage campaign made 29 separate
surfaces remote-aware and closed the sync gaps found in a coverage audit, and a
round of "de-jank" work stopped the slave from throwing bogus critical errors,
thrashing profiles, or burning idle CPU on animation loops. The result is that you
can watch and drive a running rig from a second computer and see a faithful copy of
what's happening.

## A more coherent automation core

The automatic-imaging systems were unified behind a single, consistent core. There
is now one "Plan Tonight" front door instead of several overlapping entry points;
safety configuration, local-sidereal-time math, and mosaic geometry are
single-sourced rather than reimplemented in different places; the flat-calibration
and focus-temperature models were merged behind a parity gate; and autopilot now
shares one DECIDE scoring contract and owns its own scheduling slot. This is mostly
invisible, but it makes planning, scheduling, and autopilot behave consistently no
matter where you start from.

## Screens and navigation, reorganized

The app's navigation was reworked: a new **Discover** tab in the planner, a cleaner
shell navigation, and reorganized science and analytics areas. Sub-tabbed screens
now carry a consistent title bar, and the "Plan Tonight" Recommendation tab no
longer flashes as you move around. The heavy live-sky tabs (Framing, Planetarium,
Discover) build lazily and preserve their state when you switch away, so navigation
stays responsive. Two adversarial UI review passes cleaned up objective defects
plus accessibility, overflow, and design-token issues.

## Cloud backup: now any S3-compatible store

Cloud backup previously spoke only WebDAV (Nextcloud, ownCloud, and similar). It
now also speaks **S3**, so you can point your backups at Amazon S3, a self-hosted
MinIO server, or Backblaze B2.

In **Settings → Backup & Sync**, pick your provider, enter the endpoint, region,
bucket, access key, and secret key. There's a path-style toggle for MinIO and other
self-hosted gateways. Use **Test Connection** to confirm your credentials and bucket
before saving, then backup push/pull, auto-push, and restore all work exactly as
they did with WebDAV. Your secret key is stored in the operating system's keyring,
never in app settings or logs. Existing WebDAV setups are untouched.

## Security, safety, and reliability

A large share of this release went into hardening the parts of Nightshade you don't
see but rely on:

- **Remote control is harder to abuse.** The headless server ties rate-limiting and
  lockout to the real network connection rather than a forgeable header, checks
  authentication before it buffers a request body, throttles pairing and websocket
  attempts, and tightens file-path validation (including symlink-ancestor
  resolution) so the file endpoints can't be coaxed into probing paths outside the
  folders you've allowed.
- **Updates fail closed.** The auto-updater requires a valid Ed25519 signature and
  re-verifies it at the moment it installs — not just an easily-recomputed hash. An
  unsigned or tampered update is refused. (Operators: update packages must be signed
  and the signing key provisioned, or auto-update stays safely disabled.)
- **Your gear won't be double-commanded.** Non-idempotent hardware commands — a
  relative focuser/rotator move, a mount slew, an exposure — are sent exactly once
  even over a slow or flaky remote link.
- **Sessions clean up after themselves.** The sequencer reliably reaps timed-out
  child processes and their process groups; loaded sequences no longer self-cancel
  at start from an edge case in tracking-loss handling; and a number of leaked
  images, controllers, and subscriptions were fixed.
- **Better error reporting.** Errors over the remote API now carry a stable
  machine-readable code alongside the human message, in a backward-compatible
  envelope.

## Imaging correctness and code quality

- **Imaging math.** Master-frame accumulation got overflow-checked geometry,
  corrected cross-fold frame weighting, and a more robust online clipping-variance
  path — improvements to the math behind your stacked masters, with the on-disk
  format unchanged.
- **Device hot-plug** events were promoted to first-class events end to end, so the
  app reacts more cleanly when gear is connected or removed mid-session.
- **A quieter, tighter codebase.** Broad cleanup removed proven-dead code and
  duplicate paths, consolidated logic that had drifted apart, replaced weak or
  self-referential tests with ones that assert real behavior, and added new
  consistency and boundary gates to keep it that way. A correctness/quality audit
  pass fixed nine confirmed findings.

## Platform and packaging

- Windows builds now bundle the vendor device SDKs next to the executable.
- The Linux release artifact matches the runnable bundle layout.
- A `--serve` startup hang (blank window) was fixed, and a Windows CI runner was
  pinned to fix a flaky symlink-extraction failure.
- Versions were reconciled across the workspace to 5.0.0 and are guarded by a
  consistency check.

## Upgrade notes and compatibility

- **Your configuration is preserved.** Saved profiles, sequences, cloud-sync
  setups, and layouts carry forward.
- **Database migration.** Living Sky adds new storage for the atlas and your
  transient/discovery history; your existing database migrates automatically on
  first launch.
- **Auto-update requires signed packages.** Because updates now fail closed on an
  invalid signature, an unsigned build (without the signing key provisioned) will
  not auto-update. This is deliberate.
- **Known limitations** are tracked in `docs/known-limitations.md`. In particular,
  the new S3 cloud-backup path is verified in automated tests against an in-process
  mock object store; validate it against your specific live S3, MinIO, or Backblaze
  B2 endpoint before relying on it.

---

## Complete change list (every commit since 4.3.0)

### Living Sky
- Living Sky design + interface contracts
- Native imaging engine + bridge for Living Sky
- DB schema v52 for atlas + transient persistence
- Your Sky personal growing atlas (Pillar A)
- First Light transient discovery (Pillar B)
- Constellation community swarm (Pillar C)
- Wire Living Sky pillars into app shell + headless
- Trigger atlas fold + First Light scan on solved frames
- Ship-readiness evaluation (116 confirmed findings, 3-wave plan)
- Wave 0 — stop Constellation/atlas data corruption
- Wave 1 — First Light science honesty + real catalog submission
- Wave 2 — slave reachability + real region UX, retract, raw-subframe sharing
- Wave 3 — scale/retention hardening + master/slave parity tests
- Final sweep — surface dead APIs, Chase/push/light-curve, fix id collision
- Deferred backlog — real pixel crops, all-sky map, payoff/release/export

### Remote master/slave mirroring & remote control
- Live master/slave mirroring — unified profiles + full state sync + desktop slave mode
- Mirror host autopilot decision to slave (Option 1) + triage report
- Make 29 slave surfaces remote-aware (full triage campaign)
- Close the 7 master/slave sync gaps from the coverage audit
- De-jank the slave — stop bogus critical errors, profile thrash, idle-CPU loops
- Don't auto-create a sequence on a slave / when not editable
- Dedup remote poll streams; consolidate planner title into tab row
- Stop settings-mutation spam from thrashing Plan Tonight Recommendation

### Automation core unification
- Phase 0 — remove dead automation code + fix latitudeSign
- Phase 1 — single "Plan Tonight" front door + fix mosaic export
- Phase 2 — single-source safety config, shared LST math, unified mosaic geometry
- Phase 3 — merge flat-calibration + focus-temp models (parity-gated)
- Phase 4 — shared DECIDE scoring contract + autopilot own-slot
- Fix 4 pre-existing automation test failures (stale/incomplete tests)

### UI / navigation
- Navigation + screen reorg (planner Discover tab, shell nav, science, analytics)
- Title-bar on all sub-tabbed screens + never-flash Recommendation tab
- Consolidate title into sub-tab row on science + flat-wizard screens
- Lazy-build heavy live-sky tabs (Framing/Planetarium/Discover)
- Preserve heavy-tab state via TickerMode instead of lazy rebuild
- Adversarial UI review — objective defects + a11y/overflow/token fixes
- Execute the 3 reported subjective UI items
- Consolidate theme color sources (ARCH-003)

### Cloud backup
- Implement S3SyncTarget (AWS SigV4 + S3 REST) for cloud backup
- Surface S3-compatible cloud backup as a selectable provider

### Security / safety / reliability hardening
- Headless rate-limit/lockout key from TCP socket peer, not spoofable X-Forwarded-For (HTTP-001)
- Headless security hardening — auth-before-body buffering, pairing/WS throttling, file-validate containment, handler dedup, trust-proxy doc (HTTP-001..005)
- Resolve in-root symlink ancestors in /api/files/validate to close existence oracle (HTTP-005)
- Unify error envelope with backward-compatible machine code field (NAME-001)
- Persist LAN-push staging manifest and fail closed on unsigned/insecure OTA (PERSIST-001..003, SEC-001)
- Require Ed25519 signature re-verification at apply time, not just a recomputable hash (PERSIST-001)
- ASCOM VariantClear owning Invoke-result VARIANTs to stop SAFEARRAY frame leak (DEV-001)
- Backend/profile/reconnect/auth hardening (CORE-001, DEFENSE-001, MOBILE-001, SEC-003)
- Pin non-idempotent hardware command POSTs to a single attempt (MOBILE-001)
- Dispose leaked ui.Image/controllers, single-source version compat, honest relay-connect (UI-001..003, MOBILE-002/003)
- Reap timed-out child process and drop dead wrapper (CONC-001, DEAD-004)
- Edge-trigger MountTrackingLost so loaded sequences don't self-cancel at start (B19)
- Map benign sequencer lifecycle events instead of defaulting to error

### Imaging correctness & code quality
- Overflow-checked master-accumulation geometry (IMG-003)
- Cross-fold accumulation weighting, online-clip variance, kill child process group on timeout (IMG-001, IMG-003, SEQ-001)
- Promote hot-plug device events to first-class FRB events
- Statically-typed PHD2 scale selector + correct algo-param-names decode key (W25)
- Dedup PHD2 accessors, fail-closed online-catalog gate, coalesce alert fetches, rebind providers on backend swap, mosaic export, honest cloud-motion errors (SLOP-DUP/DEFENSIVE/PERF/COMMENT, CONC-001/002)
- Allocation cleanups and target-scoring dedup (ALLOC-002/003, DUP-004)
- Remove dead providers and unreachable code paths (DEAD-001..005)
- Remove proven-unused derived providers (SLOP-DEAD-001..004) + continuation
- Replace tautological/self-referential tests with real-contract assertions (TEST-001..003, W18..W22)
- Harden migration regression audit fixtures (PERSIST-005)
- Realign platform-capability and headless contract/route audit matchers
- Correctness/quality/completeness audit pass — 9 confirmed findings fixed
- Retire the v2.7 NightshadeException TODO as a permanent invariant
- Correct misleading VNG gradient-type and min_max_clip comments (W24)
- Reword incidental XXX placeholders flagged by the marker audit

### Platform / build / docs
- Bundle Windows vendor device SDKs next to the exe
- --serve no longer hangs before runApp (blank window)
- Pin windows-2022 to fix flutter_webrtc symlink-extraction failure
- Reconcile versions to 5.0.0 + add version-consistency + bridge-boundary gates (BUILD-001/003/005, ARCH-001)
- Linux release-artifact runnable-bundle parity + developer-quality rollup (BUILD-002, DOC-001)
- Clear clippy errors (needless_range_loop, manual_checked_ops) and fmt drift
- dart format / cargo fmt campaigns
- Prep + draft 5.0.0 "Living Sky" release notes
