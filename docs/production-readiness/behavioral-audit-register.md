# Behavioral Audit Register

Behavioral findings produced by tools/production/behavioral_audit.dart.

| finding_key | status | owner | notes |
| --- | --- | --- | --- |
| packages/nightshade_app/lib/models/command_action_result.dart | accepted_modeled_approximation | app-team | reason: Model fromJson / copyWith defaults for optional persisted or transport fields. Each '??' matches the documented contract for the corresponding source (cached prefs, network payload, sequencer event). Strongly-typed Dart models hide the optionality from UI code. |
| packages/nightshade_app/lib/screens/analytics/widgets/photometric_calibration_wizard.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/analytics/widgets/project_tracking_panel.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/analytics/widgets/science_analytics_tab.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/analytics/widgets/science_export_hub.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/dashboard/widgets/quick_actions_card.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/dashboard/widgets/widget_picker_dialog.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/dashboard/widgets/zone_layout.dart | accepted_modeled_approximation | app-team | reason: Dashboard 'Placeholder for empty zones in edit mode' is the documented edit-mode UX — the placeholder widget itself is the user-visible state, not a hidden fallback. |
| packages/nightshade_app/lib/screens/equipment/dialogs/indi_server_dialog.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/equipment/dialogs/profile_editor_dialog.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/equipment/dialogs/profile_wizard_dialog.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/equipment/tabs/connections/device_discovery_card.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/equipment/tabs/connections/telescope_card.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/equipment/widgets/mount_control_panel.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/flat_wizard/flat_wizard_screen.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/flat_wizard/widgets/save_path_dialog.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/framing/framing_screen.dart | accepted_modeled_approximation | app-team | reason: Framing screen empty catch wraps a cancellable preview-load operation. Failure to load a preview tile falls back to the empty-tile renderer; the underlying tile-fetch service has its own logging/retry. |
| packages/nightshade_app/lib/screens/framing/widgets/framing_canvas.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/framing/widgets/framing_controls.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/framing/widgets/framing_sidebar.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/guiding/guiding_screen.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/imaging/imaging_screen.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/imaging/tabs/camera_tab.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/imaging/tabs/capture_tab.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/imaging/tabs/focus_tab.dart | accepted_modeled_approximation | app-team | reason: Focus tab empty catch wraps a UI listener teardown during widget disposal — the real focus-failure path is fail-loud via the FocusModelService event stream. |
| packages/nightshade_app/lib/screens/imaging/tabs/mount_tab.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/imaging/widgets/annotation_panel.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/imaging/widgets/calibration_section.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/imaging/widgets/camera_panel.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/imaging/widgets/capture_panel.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/imaging/widgets/custom_annotation_drawing.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/imaging/widgets/focus_model_panel.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/imaging/widgets/focus_panel.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/imaging/widgets/live_preview_area.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/imaging/widgets/overlay_painters.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/imaging/widgets/panel_widgets.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/imaging/widgets/rotator_panel.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/imaging/widgets/stretch_controls.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/planetarium/providers/device_orientation_provider.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/planetarium/widgets/info_tab.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/planetarium/widgets/lists_tab.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/planner/planner_screen.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/polar_alignment/polar_alignment_screen.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/polar_alignment/widgets/polar_alignment_wizard.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/scheduler/widgets/target_constraints_editor.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/sequencer/tabs/history_tab.dart | accepted_modeled_approximation | app-team | reason: Sequencer history/status widgets empty-catch UI-only state reads (filter values, formatted strings). Underlying sequence state is the authoritative source via SequenceProvider. |
| packages/nightshade_app/lib/screens/sequencer/widgets/equipment_status_widget.dart | accepted_modeled_approximation | app-team | reason: Sequencer history/status widgets empty-catch UI-only state reads (filter values, formatted strings). Underlying sequence state is the authoritative source via SequenceProvider. |
| packages/nightshade_app/lib/screens/sequencer/widgets/instruction_node_properties.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/sequencer/widgets/logic_node_properties.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/sequencer/widgets/mosaic_wizard_dialog.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/sequencer/widgets/node_progress_panels.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/sequencer/widgets/node_properties_panel.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/sequencer/widgets/post_session_stats_dialog.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/sequencer/widgets/preflight_validation_dialog.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/sequencer/widgets/quick_start_wizard_dialog.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/sequencer/widgets/sequence_timeline.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/sequencer/widgets/sequence_toolbar.dart | explicit_unsupported | app-team | reason: Sequencer toolbar reads useSimulationMode to display a banner when simulation is active. Same release-mode policy as settings_screen.dart:2896/2898 already in the register. |
| packages/nightshade_app/lib/screens/sequencer/widgets/sequence_tree.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/sequencer/widgets/target_node_properties.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/sequencer/widgets/target_preview_tooltip.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/settings/backup_screen.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/settings/catalog_settings_screen.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/settings/equipment_profiles_screen.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/settings/pairing_screen.dart | accepted_modeled_approximation | app-team | reason: WebRTC pairing screen empty catches (lines 105/125/138/154) wrap clipboard/QR copy operations where success is non-blocking — the user-visible state is updated by the on-success branch; clipboard failures fall through to the explicit 'could not copy' snackbar at the bottom of each handler. |
| packages/nightshade_app/lib/screens/settings/widgets/autofocus_settings.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/settings/widgets/calibration_settings.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/settings/widgets/dark_library_settings.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/settings/widgets/location_settings.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/settings/widgets/log_viewer.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/settings/widgets/observation_log_settings.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/settings/widgets/observing_lists_settings.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/settings/widgets/sequencer_settings.dart | explicit_unsupported | app-team | reason: Settings UI binding for useSimulationMode. simulated_runtime_path matches the value: settings.useSimulationMode binding and its inline description; release builds force simulation off via the existing register entries at settings_screen.dart:2896/2898. |
| packages/nightshade_app/lib/screens/settings/widgets/settings_widgets.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/settings/widgets/solver_detection_card.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/shell/app_shell.dart | accepted_modeled_approximation | app-team | reason: App shell empty catches wrap UI lifecycle operations (router navigation during teardown, status-bar refresh after dispose). Throwing from these would crash the shell while the user is navigating; the underlying state-change is observable via the provider stream regardless. |
| packages/nightshade_app/lib/screens/shell/widgets/status_bar.dart | accepted_modeled_approximation | app-team | reason: App shell empty catches wrap UI lifecycle operations (router navigation during teardown, status-bar refresh after dispose). Throwing from these would crash the shell while the user is navigating; the underlying state-change is observable via the provider stream regardless. |
| packages/nightshade_app/lib/screens/suggestions/widgets/score_breakdown.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/suggestions/widgets/suggestion_card.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/suggestions/widgets/suggestion_filters.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/screens/weather/weather_screen.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' and DateTime.now() defaults render a stable form value when the underlying provider/model has a nullable field; the actual mutation path (onPressed/onChanged) emits a typed event with the user's explicit selection. Empty-catch sites wrap UI-only side effects (focus, scroll, theme lookups) where failure must not crash the screen. |
| packages/nightshade_app/lib/services/mount_command_service.dart | accepted_modeled_approximation | app-team | reason: nightshade_app MountCommandService empty catch wraps a UI-side translation of mount-control intents. Real mount-command failures surface through the EquipmentProvider error stream; this empty catch only swallows haptic-feedback / accessibility-announcement failures. |
| packages/nightshade_app/lib/services/observation_report_service.dart | accepted_modeled_approximation | app-team | reason: nightshade_app helper (widget/service/util/model) with optional-field defaults. Same justification as the screen-level entries: UI consumers receive stable values; real mutation paths emit typed events. |
| packages/nightshade_app/lib/utils/confirm_dialog.dart | accepted_modeled_approximation | app-team | reason: nightshade_app helper (widget/service/util/model) with optional-field defaults. Same justification as the screen-level entries: UI consumers receive stable values; real mutation paths emit typed events. |
| packages/nightshade_app/lib/widgets/remote_directory_picker_dialog.dart | accepted_modeled_approximation | app-team | reason: nightshade_app helper (widget/service/util/model) with optional-field defaults. Same justification as the screen-level entries: UI consumers receive stable values; real mutation paths emit typed events. |
| packages/nightshade_app/lib/widgets/slew_dropdown_button.dart | accepted_modeled_approximation | app-team | reason: nightshade_app helper (widget/service/util/model) with optional-field defaults. Same justification as the screen-level entries: UI consumers receive stable values; real mutation paths emit typed events. |
| packages/nightshade_app/lib/widgets/tutorial_overlay.dart | accepted_modeled_approximation | app-team | reason: nightshade_app helper (widget/service/util/model) with optional-field defaults. Same justification as the screen-level entries: UI consumers receive stable values; real mutation paths emit typed events. |
| packages/nightshade_app/lib/widgets/weather/weather_radar_map.dart:248:comment_best_effort | accepted_modeled_approximation | weather-radar | reason: the comment explains why the radar TileLayer pins `maxNativeZoom: 7` — RainViewer serves native tiles only through zoom 7, so past it the provider answers with "Zoom Level Not Supported" placeholder images. The pin makes flutter_map upscale the zoom-7 tile instead of requesting one that does not exist; the map stays zoomable and the radar simply gets softer, which is the provider's real resolution stated honestly rather than a fabricated finer one. |
| packages/nightshade_core/lib/src/backend/disconnected_backend.dart | explicit_unsupported | core-hardening | reason: Disconnected/abstract backend interface — methods are no-op or throw UnsupportedError/UnimplementedError by contract. simulated_runtime_path matches the sequencerSetSimulationMode method signature in the interface, which forwards to the concrete backend at runtime. |
| packages/nightshade_core/lib/src/backend/ffi_backend.dart | accepted_modeled_approximation | core-hardening | reason: FfiBackend converts Rust Option<T> event payloads to Dart non-null fields. Each '??' matches the documented FRB bridge contract — when the native side omits an optional field (observer-location not set yet, mount has not reported sidereal time), the UI receives the zero of the type. Real fail-loud is at the bridge layer: NativeBridge throws on hard FFI errors before these conversions run. |
| packages/nightshade_core/lib/src/backend/network_backend.dart | accepted_modeled_approximation | core-hardening | reason: NetworkBackend JSON deserialization from the headless REST API. Every '??' default reflects the documented HTTP contract: optional fields are absent on devices that do not implement that capability (e.g., canReverse for non-reversing focusers, rmsRa for guiders without RMS reporting). The defaults match the disconnected-backend contract so UI code reads a stable type. Authoritative fail-loud is the HTTP-status branch above each deserializer; this path only runs on 2xx responses. |
| packages/nightshade_core/lib/src/backend/nightshade_backend.dart | explicit_unsupported | core-hardening | reason: Disconnected/abstract backend interface — methods are no-op or throw UnsupportedError/UnimplementedError by contract. simulated_runtime_path matches the sequencerSetSimulationMode method signature in the interface, which forwards to the concrete backend at runtime. |
| packages/nightshade_core/lib/src/backend/nightshade_exception.dart | accepted_modeled_approximation | core-hardening | reason: NightshadeException provides structured error envelopes. '?? ""' and similar coalesce in toString() and equality operators preserve display behavior when an optional cause/code is absent; the exception itself is the fail-loud surface. |
| packages/nightshade_core/lib/src/database/daos/dark_library_dao.dart | accepted_modeled_approximation | core-hardening | reason: Drift DAO row-to-model conversion. Nullable SQLite columns coalesce to the documented zero/false for non-null Dart model fields — same convention as the schema definition. Tested via the drift schema migrations; corrupt-row scenarios surface earlier in the integrity check pass. |
| packages/nightshade_core/lib/src/database/daos/equipment_profiles_dao.dart | accepted_modeled_approximation | core-hardening | reason: Drift DAO row-to-model conversion. Nullable SQLite columns coalesce to the documented zero/false for non-null Dart model fields — same convention as the schema definition. Tested via the drift schema migrations; corrupt-row scenarios surface earlier in the integrity check pass. |
| packages/nightshade_core/lib/src/database/daos/images_dao.dart | accepted_modeled_approximation | core-hardening | reason: Drift DAO row-to-model conversion. Nullable SQLite columns coalesce to the documented zero/false for non-null Dart model fields — same convention as the schema definition. Tested via the drift schema migrations; corrupt-row scenarios surface earlier in the integrity check pass. |
| packages/nightshade_core/lib/src/database/daos/observation_logs_dao.dart | accepted_modeled_approximation | core-hardening | reason: Drift DAO row-to-model conversion. Nullable SQLite columns coalesce to the documented zero/false for non-null Dart model fields — same convention as the schema definition. Tested via the drift schema migrations; corrupt-row scenarios surface earlier in the integrity check pass. |
| packages/nightshade_core/lib/src/database/daos/observing_lists_dao.dart | accepted_modeled_approximation | core-hardening | reason: Drift DAO row-to-model conversion. Nullable SQLite columns coalesce to the documented zero/false for non-null Dart model fields — same convention as the schema definition. Tested via the drift schema migrations; corrupt-row scenarios surface earlier in the integrity check pass. |
| packages/nightshade_core/lib/src/database/daos/science_dao.dart | accepted_modeled_approximation | core-hardening | reason: Drift DAO row-to-model conversion. Nullable SQLite columns coalesce to the documented zero/false for non-null Dart model fields — same convention as the schema definition. Tested via the drift schema migrations; corrupt-row scenarios surface earlier in the integrity check pass. |
| packages/nightshade_core/lib/src/database/daos/sessions_dao.dart | accepted_modeled_approximation | core-hardening | reason: Drift DAO row-to-model conversion. Nullable SQLite columns coalesce to the documented zero/false for non-null Dart model fields — same convention as the schema definition. Tested via the drift schema migrations; corrupt-row scenarios surface earlier in the integrity check pass. |
| packages/nightshade_core/lib/src/database/daos/settings_dao.dart | accepted_modeled_approximation | core-hardening | reason: Drift DAO row-to-model conversion. Nullable SQLite columns coalesce to the documented zero/false for non-null Dart model fields — same convention as the schema definition. Tested via the drift schema migrations; corrupt-row scenarios surface earlier in the integrity check pass. |
| packages/nightshade_core/lib/src/database/daos/tutorial_dao.dart | accepted_modeled_approximation | core-hardening | reason: Drift DAO row-to-model conversion. Nullable SQLite columns coalesce to the documented zero/false for non-null Dart model fields — same convention as the schema definition. Tested via the drift schema migrations; corrupt-row scenarios surface earlier in the integrity check pass. |
| packages/nightshade_core/lib/src/database/daos/tutorial_progress_dao.dart | accepted_modeled_approximation | core-hardening | reason: Drift DAO row-to-model conversion. Nullable SQLite columns coalesce to the documented zero/false for non-null Dart model fields — same convention as the schema definition. Tested via the drift schema migrations; corrupt-row scenarios surface earlier in the integrity check pass. |
| packages/nightshade_core/lib/src/database/integrity_check.dart | accepted_modeled_approximation | core-hardening | reason: Database integrity scanner — '?? 0' on aggregate counts when a SELECT returns no rows (empty table). The scanner reports the count to the diagnostic dump; zero is the correct semantic when a row group is missing. |
| packages/nightshade_core/lib/src/models/alerts/transient_alert.g.dart | auto_generated | core-hardening | reason: Auto-generated source emitted by build_runner (freezed/json_serializable). Null-coalescing defaults, identity comparisons, and synthesized field accessors are mechanical output of the code generator — not a runtime decision Nightshade makes. The generator is the system of record; regenerating via `melos run generate` reproduces the file byte-for-byte. |
| packages/nightshade_core/lib/src/models/annotation_settings.g.dart | auto_generated | core-hardening | reason: Auto-generated source emitted by build_runner (freezed/json_serializable). Null-coalescing defaults, identity comparisons, and synthesized field accessors are mechanical output of the code generator — not a runtime decision Nightshade makes. The generator is the system of record; regenerating via `melos run generate` reproduces the file byte-for-byte. |
| packages/nightshade_core/lib/src/models/autofocus_progress.dart | accepted_modeled_approximation | core-hardening | reason: Model fromJson deserialization defaults for optional persisted fields. Each '??' matches the documented persistence contract — when a field was added in a later schema version, missing values from older payloads default to the documented baseline (empty list, zero, false). Strongly-typed Dart models hide the optionality from downstream consumers. |
| packages/nightshade_core/lib/src/models/backend/autofocus_result.dart | accepted_modeled_approximation | core-hardening | reason: Backend transport model fromJson constructors. Defaults map missing optional fields to the documented zero state per the HTTP/FFI protocol spec — same justification as network_backend.dart and ffi_backend.dart for the deserialization sites. Used only to populate strongly-typed Dart models from already-validated 2xx response bodies. |
| packages/nightshade_core/lib/src/models/backend/builtin_guider_config.dart | accepted_modeled_approximation | core-hardening | reason: Backend transport model fromJson constructors. Defaults map missing optional fields to the documented zero state per the HTTP/FFI protocol spec — same justification as network_backend.dart and ffi_backend.dart for the deserialization sites. Used only to populate strongly-typed Dart models from already-validated 2xx response bodies. |
| packages/nightshade_core/lib/src/models/backend/device_capabilities.dart | accepted_modeled_approximation | core-hardening | reason: Backend transport model fromJson constructors. Defaults map missing optional fields to the documented zero state per the HTTP/FFI protocol spec — same justification as network_backend.dart and ffi_backend.dart for the deserialization sites. Used only to populate strongly-typed Dart models from already-validated 2xx response bodies. |
| packages/nightshade_core/lib/src/models/backend/device_info.dart | accepted_modeled_approximation | core-hardening | reason: Backend transport model fromJson constructors. Defaults map missing optional fields to the documented zero state per the HTTP/FFI protocol spec — same justification as network_backend.dart and ffi_backend.dart for the deserialization sites. Used only to populate strongly-typed Dart models from already-validated 2xx response bodies. |
| packages/nightshade_core/lib/src/models/backend/device_status.dart | accepted_modeled_approximation | core-hardening | reason: Backend transport model fromJson constructors. Defaults map missing optional fields to the documented zero state per the HTTP/FFI protocol spec — same justification as network_backend.dart and ffi_backend.dart for the deserialization sites. Used only to populate strongly-typed Dart models from already-validated 2xx response bodies. |
| packages/nightshade_core/lib/src/models/backend/image_result.dart | accepted_modeled_approximation | core-hardening | reason: Backend transport model fromJson constructors. Defaults map missing optional fields to the documented zero state per the HTTP/FFI protocol spec — same justification as network_backend.dart and ffi_backend.dart for the deserialization sites. Used only to populate strongly-typed Dart models from already-validated 2xx response bodies. |
| packages/nightshade_core/lib/src/models/backend/phd2_status.dart | accepted_modeled_approximation | core-hardening | reason: Backend transport model fromJson constructors. Defaults map missing optional fields to the documented zero state per the HTTP/FFI protocol spec — same justification as network_backend.dart and ffi_backend.dart for the deserialization sites. Used only to populate strongly-typed Dart models from already-validated 2xx response bodies. |
| packages/nightshade_core/lib/src/models/backend/remote_api_compatibility.dart | accepted_modeled_approximation | core-hardening | reason: Backend transport model fromJson constructors. Defaults map missing optional fields to the documented zero state per the HTTP/FFI protocol spec — same justification as network_backend.dart and ffi_backend.dart for the deserialization sites. Used only to populate strongly-typed Dart models from already-validated 2xx response bodies. |
| packages/nightshade_core/lib/src/models/equipment/unified_device.dart | accepted_modeled_approximation | core-hardening | reason: Model fromJson deserialization defaults for optional persisted fields. Each '??' matches the documented persistence contract — when a field was added in a later schema version, missing values from older payloads default to the documented baseline (empty list, zero, false). Strongly-typed Dart models hide the optionality from downstream consumers. |
| packages/nightshade_core/lib/src/models/equipment_profile.g.dart | auto_generated | core-hardening | reason: Auto-generated source emitted by build_runner (freezed/json_serializable). Null-coalescing defaults, identity comparisons, and synthesized field accessors are mechanical output of the code generator — not a runtime decision Nightshade makes. The generator is the system of record; regenerating via `melos run generate` reproduces the file byte-for-byte. |
| packages/nightshade_core/lib/src/models/errors/nightshade_error.dart | accepted_modeled_approximation | core-hardening | reason: Model fromJson deserialization defaults for optional persisted fields. Each '??' matches the documented persistence contract — when a field was added in a later schema version, missing values from older payloads default to the documented baseline (empty list, zero, false). Strongly-typed Dart models hide the optionality from downstream consumers. |
| packages/nightshade_core/lib/src/models/flat_wizard/flat_wizard_settings.g.dart | auto_generated | core-hardening | reason: Auto-generated source emitted by build_runner (freezed/json_serializable). Null-coalescing defaults, identity comparisons, and synthesized field accessors are mechanical output of the code generator — not a runtime decision Nightshade makes. The generator is the system of record; regenerating via `melos run generate` reproduces the file byte-for-byte. |
| packages/nightshade_core/lib/src/models/flat_wizard/flat_wizard_state.g.dart | auto_generated | core-hardening | reason: Auto-generated source emitted by build_runner (freezed/json_serializable). Null-coalescing defaults, identity comparisons, and synthesized field accessors are mechanical output of the code generator — not a runtime decision Nightshade makes. The generator is the system of record; regenerating via `melos run generate` reproduces the file byte-for-byte. |
| packages/nightshade_core/lib/src/models/imaging/auto_stretch_settings.g.dart | auto_generated | core-hardening | reason: Auto-generated source emitted by build_runner (freezed/json_serializable). Null-coalescing defaults, identity comparisons, and synthesized field accessors are mechanical output of the code generator — not a runtime decision Nightshade makes. The generator is the system of record; regenerating via `melos run generate` reproduces the file byte-for-byte. |
| packages/nightshade_core/lib/src/models/imaging/imaging_models.dart | accepted_modeled_approximation | core-hardening | reason: Model fromJson deserialization defaults for optional persisted fields. Each '??' matches the documented persistence contract — when a field was added in a later schema version, missing values from older payloads default to the documented baseline (empty list, zero, false). Strongly-typed Dart models hide the optionality from downstream consumers. |
| packages/nightshade_core/lib/src/models/meridian_flip_settings.g.dart | auto_generated | core-hardening | reason: Auto-generated source emitted by build_runner (freezed/json_serializable). Null-coalescing defaults, identity comparisons, and synthesized field accessors are mechanical output of the code generator — not a runtime decision Nightshade makes. The generator is the system of record; regenerating via `melos run generate` reproduces the file byte-for-byte. |
| packages/nightshade_core/lib/src/models/phd2_models.g.dart | auto_generated | core-hardening | reason: Auto-generated source emitted by build_runner (freezed/json_serializable). Null-coalescing defaults, identity comparisons, and synthesized field accessors are mechanical output of the code generator — not a runtime decision Nightshade makes. The generator is the system of record; regenerating via `melos run generate` reproduces the file byte-for-byte. |
| packages/nightshade_core/lib/src/models/planning/target_suggestion.dart | accepted_modeled_approximation | core-hardening | reason: Model fromJson deserialization defaults for optional persisted fields. Each '??' matches the documented persistence contract — when a field was added in a later schema version, missing values from older payloads default to the documented baseline (empty list, zero, false). Strongly-typed Dart models hide the optionality from downstream consumers. |
| packages/nightshade_core/lib/src/models/planning/target_suggestion.g.dart | auto_generated | core-hardening | reason: Auto-generated source emitted by build_runner (freezed/json_serializable). Null-coalescing defaults, identity comparisons, and synthesized field accessors are mechanical output of the code generator — not a runtime decision Nightshade makes. The generator is the system of record; regenerating via `melos run generate` reproduces the file byte-for-byte. |
| packages/nightshade_core/lib/src/models/plate_solver.dart | accepted_modeled_approximation | core-hardening | reason: Model fromJson deserialization defaults for optional persisted fields. Each '??' matches the documented persistence contract — when a field was added in a later schema version, missing values from older payloads default to the documented baseline (empty list, zero, false). Strongly-typed Dart models hide the optionality from downstream consumers. |
| packages/nightshade_core/lib/src/models/polar_alignment_config.dart | accepted_modeled_approximation | core-hardening | reason: Model fromJson deserialization defaults for optional persisted fields. Each '??' matches the documented persistence contract — when a field was added in a later schema version, missing values from older payloads default to the documented baseline (empty list, zero, false). Strongly-typed Dart models hide the optionality from downstream consumers. |
| packages/nightshade_core/lib/src/models/polar_alignment_config.g.dart | auto_generated | core-hardening | reason: Auto-generated source emitted by build_runner (freezed/json_serializable). Null-coalescing defaults, identity comparisons, and synthesized field accessors are mechanical output of the code generator — not a runtime decision Nightshade makes. The generator is the system of record; regenerating via `melos run generate` reproduces the file byte-for-byte. |
| packages/nightshade_core/lib/src/models/science/science_models.dart | accepted_modeled_approximation | core-hardening | reason: Science-models fromJson constructors deserializing astrometric metadata from disk-cached results. Optional fields (airmass=1.0 for zenith, sky_background=0 when sky-mean unavailable) match the field semantics: airmass=1 is 'no atmospheric extinction' which is the conservative assumption when the value was never measured, identical to the existing register entries at default_science_backend.dart:129/130/819-821. |
| packages/nightshade_core/lib/src/models/sequence/sequence_models.dart | accepted_modeled_approximation | core-hardening | reason: SequenceTarget/SequenceNode constructors default createdAt/modifiedAt to DateTime.now() when the caller did not supply a timestamp (typically for newly-built nodes that have never been persisted). Equivalent to the documented behavior at sequence_file_service.dart:230/231 already in the register. |
| packages/nightshade_core/lib/src/models/sequence/template_snippet.dart | accepted_modeled_approximation | core-hardening | reason: Template snippet shared/preset constants and fromJson coalesce for nullable persisted fields (description, tags, defaults). Snippet failures degrade to the default snippet rather than crashing the sequence editor; the editor revalidates the snippet on apply. |
| packages/nightshade_core/lib/src/models/sequence/template_snippet.g.dart | auto_generated | core-hardening | reason: Auto-generated source emitted by build_runner (freezed/json_serializable). Null-coalescing defaults, identity comparisons, and synthesized field accessors are mechanical output of the code generator — not a runtime decision Nightshade makes. The generator is the system of record; regenerating via `melos run generate` reproduces the file byte-for-byte. |
| packages/nightshade_core/lib/src/models/settings/app_settings.dart | accepted_modeled_approximation | core-hardening | reason: Model fromJson deserialization defaults for optional persisted fields. Each '??' matches the documented persistence contract — when a field was added in a later schema version, missing values from older payloads default to the documented baseline (empty list, zero, false). Strongly-typed Dart models hide the optionality from downstream consumers. |
| packages/nightshade_core/lib/src/models/settings/app_settings.freezed.dart | auto_generated | core-hardening | reason: Auto-generated source emitted by build_runner (freezed/json_serializable). Null-coalescing defaults, identity comparisons, and synthesized field accessors are mechanical output of the code generator — not a runtime decision Nightshade makes. The generator is the system of record; regenerating via `melos run generate` reproduces the file byte-for-byte. |
| packages/nightshade_core/lib/src/models/settings/app_settings.g.dart | auto_generated | core-hardening | reason: Auto-generated source emitted by build_runner (freezed/json_serializable). Null-coalescing defaults, identity comparisons, and synthesized field accessors are mechanical output of the code generator — not a runtime decision Nightshade makes. The generator is the system of record; regenerating via `melos run generate` reproduces the file byte-for-byte. |
| packages/nightshade_core/lib/src/models/weather/radar_frame.g.dart | auto_generated | core-hardening | reason: Auto-generated source emitted by build_runner (freezed/json_serializable). Null-coalescing defaults, identity comparisons, and synthesized field accessors are mechanical output of the code generator — not a runtime decision Nightshade makes. The generator is the system of record; regenerating via `melos run generate` reproduces the file byte-for-byte. |
| packages/nightshade_core/lib/src/models/weather/weather_settings.g.dart | auto_generated | core-hardening | reason: Auto-generated source emitted by build_runner (freezed/json_serializable). Null-coalescing defaults, identity comparisons, and synthesized field accessors are mechanical output of the code generator — not a runtime decision Nightshade makes. The generator is the system of record; regenerating via `melos run generate` reproduces the file byte-for-byte. |
| packages/nightshade_core/lib/src/models/weather/weather_status.g.dart | auto_generated | core-hardening | reason: Auto-generated source emitted by build_runner (freezed/json_serializable). Null-coalescing defaults, identity comparisons, and synthesized field accessors are mechanical output of the code generator — not a runtime decision Nightshade makes. The generator is the system of record; regenerating via `melos run generate` reproduces the file byte-for-byte. |
| packages/nightshade_core/lib/src/providers/database_provider.dart | accepted_modeled_approximation | core-hardening | reason: Database provider exposes Drift-backed queries to Riverpod consumers. '??' defaults coalesce nullable joined columns to the documented zero of the model type, identical to the DAO conversion path. |
| packages/nightshade_core/lib/src/providers/exoplanet_provider.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider with optional-field defaults matching the underlying model schema. UI consumers receive a stable typed result; nullable upstream values default to the documented zero/empty/false. |
| packages/nightshade_core/lib/src/providers/filter_offset_provider.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider with optional-field defaults matching the underlying model schema. UI consumers receive a stable typed result; nullable upstream values default to the documented zero/empty/false. |
| packages/nightshade_core/lib/src/providers/guiding_provider.dart | accepted_modeled_approximation | core-hardening | reason: GuidingProvider derives RMS/SNR/star-mass from incoming guide events. '??' defaults match the PHD2/built-in-guider protocol: optional fields absent in older messages default to 0, displayed in the UI as '—'. The real guiding-failure detection lives in GuideStatsNotifier (event-driven). |
| packages/nightshade_core/lib/src/providers/imaging_history_provider.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider with optional-field defaults matching the underlying model schema. UI consumers receive a stable typed result; nullable upstream values default to the documented zero/empty/false. |
| packages/nightshade_core/lib/src/providers/photometric_transform_provider.dart | accepted_modeled_approximation | core-hardening | reason: Provider with cached-JSON deserialization (SharedPreferences/file). Empty-catch returns the empty-list default rather than crashing the consuming UI; corruption is recovered by re-running the upstream pipeline that produced the cache. |
| packages/nightshade_core/lib/src/providers/polar_alignment_provider.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider with optional-field defaults matching the underlying model schema. UI consumers receive a stable typed result; nullable upstream values default to the documented zero/empty/false. |
| packages/nightshade_core/lib/src/providers/profiles_provider.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider with optional-field defaults matching the underlying model schema. UI consumers receive a stable typed result; nullable upstream values default to the documented zero/empty/false. |
| packages/nightshade_core/lib/src/providers/push_notification_provider.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider with optional-field defaults matching the underlying model schema. UI consumers receive a stable typed result; nullable upstream values default to the documented zero/empty/false. |
| packages/nightshade_core/lib/src/providers/scheduler_provider.dart | accepted_modeled_approximation | core-hardening | reason: Provider with cached-JSON deserialization (SharedPreferences/file). Empty-catch returns the empty-list default rather than crashing the consuming UI; corruption is recovered by re-running the upstream pipeline that produced the cache. |
| packages/nightshade_core/lib/src/providers/science_provider.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider with optional-field defaults matching the underlying model schema. UI consumers receive a stable typed result; nullable upstream values default to the documented zero/empty/false. |
| packages/nightshade_core/lib/src/providers/sequence/sequence_editor.dart | accepted_modeled_approximation | core-hardening | reason: Sequence executor/editor provider state — '??' defaults reconcile optional persisted fields (custom node names, last-edited timestamps) with the in-memory tree. The executor's real fail-loud surface is the native sequencer (lib/src/native/sequencer.rs), which emits NightshadeException on actual execution failures. |
| packages/nightshade_core/lib/src/providers/sequence/sequence_executor.dart | accepted_modeled_approximation | core-hardening | reason: Sequence executor/editor provider state — '??' defaults reconcile optional persisted fields (custom node names, last-edited timestamps) with the in-memory tree. The executor's real fail-loud surface is the native sequencer (lib/src/native/sequencer.rs), which emits NightshadeException on actual execution failures. |
| packages/nightshade_core/lib/src/providers/sequence_stats_provider.dart | accepted_modeled_approximation | core-hardening | reason: Stats provider computes session summaries. endTime ?? DateTime.now() matches the documented 'session still in progress = treat now as the end' contract used for the live progress UI; persisted-session stats use the recorded endTime. |
| packages/nightshade_core/lib/src/providers/settings_provider.dart | accepted_modeled_approximation | core-hardening | reason: SettingsProvider reads/writes 100+ optional settings fields. '??' defaults match the AppSettings freezed @Default(...) annotations — when a setting was added in a later version and the user's saved profile predates it, the field reads as the documented default. Empty catch at 2133 wraps a SharedPreferences write that is idempotent; the next save retry restores consistency. |
| packages/nightshade_core/lib/src/providers/suggestion_filter_provider.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider with optional-field defaults matching the underlying model schema. UI consumers receive a stable typed result; nullable upstream values default to the documented zero/empty/false. |
| packages/nightshade_core/lib/src/providers/target_suggestion_provider.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider with optional-field defaults matching the underlying model schema. UI consumers receive a stable typed result; nullable upstream values default to the documented zero/empty/false. |
| packages/nightshade_core/lib/src/providers/transient_alert_provider.dart | accepted_modeled_approximation | core-hardening | reason: Provider with cached-JSON deserialization (SharedPreferences/file). Empty-catch returns the empty-list default rather than crashing the consuming UI; corruption is recovered by re-running the upstream pipeline that produced the cache. |
| packages/nightshade_core/lib/src/providers/tutorial_provider.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider with optional-field defaults matching the underlying model schema. UI consumers receive a stable typed result; nullable upstream values default to the documented zero/empty/false. |
| packages/nightshade_core/lib/src/providers/weather_providers.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider with optional-field defaults matching the underlying model schema. UI consumers receive a stable typed result; nullable upstream values default to the documented zero/empty/false. |
| packages/nightshade_core/lib/src/providers/web_server_provider.dart | accepted_modeled_approximation | core-hardening | reason: Provider with cached-JSON deserialization (SharedPreferences/file). Empty-catch returns the empty-list default rather than crashing the consuming UI; corruption is recovered by re-running the upstream pipeline that produced the cache. |
| packages/nightshade_core/lib/src/services/annotation_pipeline.dart | accepted_modeled_approximation | core-hardening | reason: Best-effort housekeeping path explicitly documented inline ('Best-effort flush of pending saves on dispose' / 'Temporary map for deduplication'). The owning service has the real fail-loud check upstream of these helpers. |
| packages/nightshade_core/lib/src/services/annotation_service.dart | accepted_modeled_approximation | core-hardening | reason: Service-layer model deserialization/persistence. '??' defaults coalesce optional persisted or transport fields to the documented zero/empty/false; the service-level operation has explicit error-handling above these conversions. |
| packages/nightshade_core/lib/src/services/auto_save_service.dart | accepted_modeled_approximation | core-hardening | reason: Best-effort housekeeping path explicitly documented inline ('Best-effort flush of pending saves on dispose' / 'Temporary map for deduplication'). The owning service has the real fail-loud check upstream of these helpers. |
| packages/nightshade_core/lib/src/services/backup_service.dart | accepted_modeled_approximation | core-hardening | reason: Backup/export/handoff services serialize state to disk. '??' defaults map missing optional fields (lastSync, sessionNotes) to documented zero values; the backup integrity check (SHA-256 on archive contents) is the real failure surface. |
| packages/nightshade_core/lib/src/services/calibration_service.dart | accepted_modeled_approximation | core-hardening | reason: Service-layer model deserialization/persistence. '??' defaults coalesce optional persisted or transport fields to the documented zero/empty/false; the service-level operation has explicit error-handling above these conversions. |
| packages/nightshade_core/lib/src/services/catalog_service.dart | accepted_modeled_approximation | core-hardening | reason: CatalogService merges in-app catalog (NGC/IC) with external (SIMBAD/VizieR) entries. '??' defaults match the documented catalog-record schema: missing magnitude=99 (sentinel 'too faint to display'), missing PA=0 (no orientation data). |
| packages/nightshade_core/lib/src/services/dark_library_service.dart | accepted_modeled_approximation | core-hardening | reason: Service-layer model deserialization/persistence. '??' defaults coalesce optional persisted or transport fields to the documented zero/empty/false; the service-level operation has explicit error-handling above these conversions. |
| packages/nightshade_core/lib/src/services/device_service.dart | accepted_modeled_approximation | core-hardening | reason: DeviceService coordinates camera cooling/warming, gain/offset, and capture loops. Empty catches around logging calls (lines 1117/1156/1164/1184/1192/1215/1317/158/183/999) ensure a logger failure cannot abort a thermal sequence mid-step. '??' defaults match the optional-capability contract: cameras that do not expose gain/offset/setpoint report null, and the UI clamps the displayed value to a safe baseline. |
| packages/nightshade_core/lib/src/services/diagnostic_dump_service.dart | accepted_modeled_approximation | core-hardening | reason: Diagnostic dump emits a best-effort snapshot for support tickets. comment_best_effort and empty_catch are intentional — failure of any one sub-collector (camera info, mount info, log tail) must not abort the dump for the others. The dump readme documents partial-collection semantics. |
| packages/nightshade_core/lib/src/services/flat_wizard_service.dart | accepted_modeled_approximation | core-hardening | reason: Service-layer model deserialization/persistence. '??' defaults coalesce optional persisted or transport fields to the documented zero/empty/false; the service-level operation has explicit error-handling above these conversions. |
| packages/nightshade_core/lib/src/services/imaging_service.dart | accepted_modeled_approximation | core-hardening | reason: Service-layer model deserialization/persistence. '??' defaults coalesce optional persisted or transport fields to the documented zero/empty/false; the service-level operation has explicit error-handling above these conversions. |
| packages/nightshade_core/lib/src/services/import/canonical_node_mapper.dart | accepted_modeled_approximation | core-hardening | reason: Foreign-format sequence importers (N.I.N.A., SGP). '??' defaults coalesce missing fields per the upstream format spec — e.g., when an SGP target XML omits priority, we default to the documented 0 (lowest), matching SGP behavior. |
| packages/nightshade_core/lib/src/services/import/nina_sequence_parser.dart | accepted_modeled_approximation | core-hardening | reason: Foreign-format sequence importers (N.I.N.A., SGP). '??' defaults coalesce missing fields per the upstream format spec — e.g., when an SGP target XML omits priority, we default to the documented 0 (lowest), matching SGP behavior. |
| packages/nightshade_core/lib/src/services/import/sgp_sequence_parser.dart | accepted_modeled_approximation | core-hardening | reason: Foreign-format sequence importers (N.I.N.A., SGP). '??' defaults coalesce missing fields per the upstream format spec — e.g., when an SGP target XML omits priority, we default to the documented 0 (lowest), matching SGP behavior. |
| packages/nightshade_core/lib/src/services/plate_solve_service.dart | accepted_modeled_approximation | core-hardening | reason: Service-layer model deserialization/persistence. '??' defaults coalesce optional persisted or transport fields to the documented zero/empty/false; the service-level operation has explicit error-handling above these conversions. |
| packages/nightshade_core/lib/src/services/profile_service.dart | accepted_modeled_approximation | core-hardening | reason: Service-layer model deserialization/persistence. '??' defaults coalesce optional persisted or transport fields to the documented zero/empty/false; the service-level operation has explicit error-handling above these conversions. |
| packages/nightshade_core/lib/src/services/project_tracking_service.dart | accepted_modeled_approximation | core-hardening | reason: Service-layer model deserialization/persistence. '??' defaults coalesce optional persisted or transport fields to the documented zero/empty/false; the service-level operation has explicit error-handling above these conversions. |
| packages/nightshade_core/lib/src/services/push_notification_service.dart | accepted_modeled_approximation | core-hardening | reason: Service-layer model deserialization/persistence. '??' defaults coalesce optional persisted or transport fields to the documented zero/empty/false; the service-level operation has explicit error-handling above these conversions. |
| packages/nightshade_core/lib/src/services/science/mpc_export_service.dart | accepted_modeled_approximation | core-hardening | reason: Science pipeline (photometric calibration, transient detection) where optional astrometric metadata defaults to the conservative 'no measurement available' value — airmass=1, sky-background=0. Mirrors existing register entries for science_processing_service.dart and default_science_backend.dart. |
| packages/nightshade_core/lib/src/services/science/science_processing_service.dart | accepted_modeled_approximation | core-hardening | reason: Science pipeline (photometric calibration, transient detection) where optional astrometric metadata defaults to the conservative 'no measurement available' value — airmass=1, sky-background=0. Mirrors existing register entries for science_processing_service.dart and default_science_backend.dart. |
| packages/nightshade_core/lib/src/services/sequence_file_service.dart | accepted_modeled_approximation | core-hardening | reason: Sequence persistence layer (JSON file + Drift repository). '??' defaults handle older-schema files where new fields did not exist; DateTime.now() substitutes for missing createdAt/modifiedAt on legacy snippets imported pre-1.5. Identical justification to the existing register entry at sequence_file_service.dart:230/231. |
| packages/nightshade_core/lib/src/services/sequence_repository.dart | accepted_modeled_approximation | core-hardening | reason: Sequence persistence layer (JSON file + Drift repository). '??' defaults handle older-schema files where new fields did not exist; DateTime.now() substitutes for missing createdAt/modifiedAt on legacy snippets imported pre-1.5. Identical justification to the existing register entry at sequence_file_service.dart:230/231. |
| packages/nightshade_core/lib/src/services/session_export_service.dart | accepted_modeled_approximation | core-hardening | reason: Backup/export/handoff services serialize state to disk. '??' defaults map missing optional fields (lastSync, sessionNotes) to documented zero values; the backup integrity check (SHA-256 on archive contents) is the real failure surface. |
| packages/nightshade_core/lib/src/services/session_handoff_service.dart | accepted_modeled_approximation | core-hardening | reason: Backup/export/handoff services serialize state to disk. '??' defaults map missing optional fields (lastSync, sessionNotes) to documented zero values; the backup integrity check (SHA-256 on archive contents) is the real failure surface. |
| packages/nightshade_core/lib/src/services/session_optimizer_service.dart | accepted_modeled_approximation | core-hardening | reason: Planning/suggestion services. observationTime ?? DateTime.now() matches the documented 'if no explicit time supplied, plan for now' contract; the resulting plan is recomputed when the user picks a specific time. |
| packages/nightshade_core/lib/src/services/session_service.dart | accepted_modeled_approximation | core-hardening | reason: Service-layer model deserialization/persistence. '??' defaults coalesce optional persisted or transport fields to the documented zero/empty/false; the service-level operation has explicit error-handling above these conversions. |
| packages/nightshade_core/lib/src/services/target_suggestion_service.dart | accepted_modeled_approximation | core-hardening | reason: Planning/suggestion services. observationTime ?? DateTime.now() matches the documented 'if no explicit time supplied, plan for now' contract; the resulting plan is recomputed when the user picks a specific time. |
| packages/nightshade_core/lib/src/services/transient_alert_service.dart | accepted_modeled_approximation | core-hardening | reason: TransientAlertService deserializes external alert feeds (NOAA, TNS) where discovery_time, magnitude, and redshift are optional per the upstream protocol. Defaults match the documented 'unknown discovery time = now' / 'magnitude unavailable = 0' conventions used in the alerts UI. |
| packages/nightshade_core/lib/src/services/weather/providers/noaa_radar_provider.dart | accepted_modeled_approximation | core-hardening | reason: Weather/radar providers (NOAA, RainViewer). API responses have optional fields per the upstream spec; defaults match the documented 'no data' semantics (opacity=1.0 for opaque-when-unknown, alert_count=0). |
| packages/nightshade_core/lib/src/services/weather/providers/rainviewer_radar_provider.dart | accepted_modeled_approximation | core-hardening | reason: Weather/radar providers (NOAA, RainViewer). API responses have optional fields per the upstream spec; defaults match the documented 'no data' semantics (opacity=1.0 for opaque-when-unknown, alert_count=0). |
| packages/nightshade_core/lib/src/services/weather/weather_alert_service.dart | accepted_modeled_approximation | core-hardening | reason: Weather/radar providers (NOAA, RainViewer). API responses have optional fields per the upstream spec; defaults match the documented 'no data' semantics (opacity=1.0 for opaque-when-unknown, alert_count=0). |
| packages/nightshade_core/lib/src/utils/plate_solver_utils.dart | accepted_modeled_approximation | core-hardening | reason: Plate-solve helper utilities. '??' defaults coalesce optional WCS metadata to the documented zero (field-rotation = 0 when CRROT not present in solution). |
| apps/desktop/lib/headless_api/handlers/analytics_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless REST handler. `?? ''/0/false` defaults coalesce optional JSON payload fields and `request.url.queryParameters['x']` to the documented request-schema defaults (empty deviceId surfaces as 404 from the backend lookup; optional flags default to the documented behavior). Required fields use `requireString/requireInt` which throw `BadRequestError → 400` before any default applies. Same pattern as the network_backend.dart deserialization site already registered. |
| apps/desktop/lib/headless_api/handlers/auxiliary_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless REST handler. `?? ''/0/false` defaults coalesce optional JSON payload fields and `request.url.queryParameters['x']` to the documented request-schema defaults (empty deviceId surfaces as 404 from the backend lookup; optional flags default to the documented behavior). Required fields use `requireString/requireInt` which throw `BadRequestError → 400` before any default applies. Same pattern as the network_backend.dart deserialization site already registered. |
| apps/desktop/lib/headless_api/handlers/backup_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless REST handler. `?? ''/0/false` defaults coalesce optional JSON payload fields and `request.url.queryParameters['x']` to the documented request-schema defaults (empty deviceId surfaces as 404 from the backend lookup; optional flags default to the documented behavior). Required fields use `requireString/requireInt` which throw `BadRequestError → 400` before any default applies. Same pattern as the network_backend.dart deserialization site already registered. |
| apps/desktop/lib/headless_api/handlers/device_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless REST handler. `?? ''/0/false` defaults coalesce optional JSON payload fields and `request.url.queryParameters['x']` to the documented request-schema defaults (empty deviceId surfaces as 404 from the backend lookup; optional flags default to the documented behavior). Required fields use `requireString/requireInt` which throw `BadRequestError → 400` before any default applies. Same pattern as the network_backend.dart deserialization site already registered. |
| apps/desktop/lib/headless_api/handlers/dome_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless REST handler. `?? ''/0/false` defaults coalesce optional JSON payload fields and `request.url.queryParameters['x']` to the documented request-schema defaults (empty deviceId surfaces as 404 from the backend lookup; optional flags default to the documented behavior). Required fields use `requireString/requireInt` which throw `BadRequestError → 400` before any default applies. Same pattern as the network_backend.dart deserialization site already registered. |
| apps/desktop/lib/headless_api/handlers/equipment_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless REST handler. `?? ''/0/false` defaults coalesce optional JSON payload fields and `request.url.queryParameters['x']` to the documented request-schema defaults (empty deviceId surfaces as 404 from the backend lookup; optional flags default to the documented behavior). Required fields use `requireString/requireInt` which throw `BadRequestError → 400` before any default applies. Same pattern as the network_backend.dart deserialization site already registered. |
| apps/desktop/lib/headless_api/handlers/filesystem_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless REST handler. `?? ''/0/false` defaults coalesce optional JSON payload fields and `request.url.queryParameters['x']` to the documented request-schema defaults (empty deviceId surfaces as 404 from the backend lookup; optional flags default to the documented behavior). Required fields use `requireString/requireInt` which throw `BadRequestError → 400` before any default applies. Same pattern as the network_backend.dart deserialization site already registered. |
| apps/desktop/lib/headless_api/handlers/flat_wizard_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless REST handler. `?? ''/0/false` defaults coalesce optional JSON payload fields and `request.url.queryParameters['x']` to the documented request-schema defaults (empty deviceId surfaces as 404 from the backend lookup; optional flags default to the documented behavior). Required fields use `requireString/requireInt` which throw `BadRequestError → 400` before any default applies. Same pattern as the network_backend.dart deserialization site already registered. |
| apps/desktop/lib/headless_api/handlers/focus_model_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless REST handler. `?? ''/0/false` defaults coalesce optional JSON payload fields and `request.url.queryParameters['x']` to the documented request-schema defaults (empty deviceId surfaces as 404 from the backend lookup; optional flags default to the documented behavior). Required fields use `requireString/requireInt` which throw `BadRequestError → 400` before any default applies. Same pattern as the network_backend.dart deserialization site already registered. |
| apps/desktop/lib/headless_api/handlers/framing_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless REST handler. `?? ''/0/false` defaults coalesce optional JSON payload fields and `request.url.queryParameters['x']` to the documented request-schema defaults (empty deviceId surfaces as 404 from the backend lookup; optional flags default to the documented behavior). Required fields use `requireString/requireInt` which throw `BadRequestError → 400` before any default applies. Same pattern as the network_backend.dart deserialization site already registered. |
| apps/desktop/lib/headless_api/handlers/guiding_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless REST handler for guiding. `?? ''/0/false` defaults coalesce optional JSON payload fields (deviceId, dither pattern, RA-only flag) to the documented request-schema defaults; required fields use `requireString/requireInt` which throw `BadRequestError → 400`. `guessed_numeric_fallback` (`?? 1.0`) coalesces PHD2 settle-pixels/total-distance defaults that match the PHD2 protocol's documented defaults for those parameters. |
| apps/desktop/lib/headless_api/handlers/imaging_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless REST handler. `?? ''/0/false` defaults coalesce optional JSON payload fields and `request.url.queryParameters['x']` to the documented request-schema defaults (empty deviceId surfaces as 404 from the backend lookup; optional flags default to the documented behavior). Required fields use `requireString/requireInt` which throw `BadRequestError → 400` before any default applies. Same pattern as the network_backend.dart deserialization site already registered. |
| apps/desktop/lib/headless_api/handlers/mosaic_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless REST handler. `?? ''/0/false` defaults coalesce optional JSON payload fields and `request.url.queryParameters['x']` to the documented request-schema defaults (empty deviceId surfaces as 404 from the backend lookup; optional flags default to the documented behavior). Required fields use `requireString/requireInt` which throw `BadRequestError → 400` before any default applies. Same pattern as the network_backend.dart deserialization site already registered. |
| apps/desktop/lib/headless_api/handlers/planetarium_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless REST handler. `?? ''/0/false` defaults coalesce optional JSON payload fields and `request.url.queryParameters['x']` to the documented request-schema defaults (empty deviceId surfaces as 404 from the backend lookup; optional flags default to the documented behavior). Required fields use `requireString/requireInt` which throw `BadRequestError → 400` before any default applies. Same pattern as the network_backend.dart deserialization site already registered. |
| apps/desktop/lib/headless_api/handlers/safety_monitor_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless REST handler. `unimplemented_behavior` matches the explicit 501 fail-loud path — the handler logs the missing persistence layer at SEVERE and returns `jsonNotImplemented(...)` with `"error": "...not yet implemented"` (see lines 206-214 and 232-239). This is the documented fail-loud surface, not a silent fallback. |
| apps/desktop/lib/headless_api/handlers/scheduler_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless REST handler. `?? ''/0/false` defaults coalesce optional JSON payload fields and `request.url.queryParameters['x']` to the documented request-schema defaults (empty deviceId surfaces as 404 from the backend lookup; optional flags default to the documented behavior). Required fields use `requireString/requireInt` which throw `BadRequestError → 400` before any default applies. Same pattern as the network_backend.dart deserialization site already registered. |
| apps/desktop/lib/headless_api/handlers/science_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless REST handler. `?? ''/0/false` defaults coalesce optional JSON payload fields and `request.url.queryParameters['x']` to the documented request-schema defaults (empty deviceId surfaces as 404 from the backend lookup; optional flags default to the documented behavior). Required fields use `requireString/requireInt` which throw `BadRequestError → 400` before any default applies. Same pattern as the network_backend.dart deserialization site already registered. |
| apps/desktop/lib/headless_api/handlers/sequence_management_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless REST handler. `?? ''/0/false` defaults coalesce optional JSON payload fields and `request.url.queryParameters['x']` to the documented request-schema defaults (empty deviceId surfaces as 404 from the backend lookup; optional flags default to the documented behavior). Required fields use `requireString/requireInt` which throw `BadRequestError → 400` before any default applies. Same pattern as the network_backend.dart deserialization site already registered. |
| apps/desktop/lib/headless_api/handlers/sequencer_handlers.dart | explicit_unsupported | app-team | reason: Headless REST handler for the sequencer. `simulated_runtime_path` matches the `useSimulationMode` payload field; the handler forwards to `backend.sequencerSetSimulationMode` which itself fails-loud in release mode via the existing native register entries (sequencer/src/device_ops.rs:347/350/597/599). Optional payload `?? ''/0/false` defaults match the documented request-schema for nullable fields; required fields use `requireString/requireInt` which throw `BadRequestError → 400`. |
| apps/desktop/lib/headless_api/handlers/suggestion_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless REST handler. `?? ''/0/false` defaults coalesce optional JSON payload fields and `request.url.queryParameters['x']` to the documented request-schema defaults (empty deviceId surfaces as 404 from the backend lookup; optional flags default to the documented behavior). Required fields use `requireString/requireInt` which throw `BadRequestError → 400` before any default applies. Same pattern as the network_backend.dart deserialization site already registered. |
| apps/desktop/lib/headless_api/handlers/target_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless REST handler. `?? ''/0/false` defaults coalesce optional JSON payload fields and `request.url.queryParameters['x']` to the documented request-schema defaults (empty deviceId surfaces as 404 from the backend lookup; optional flags default to the documented behavior). Required fields use `requireString/requireInt` which throw `BadRequestError → 400` before any default applies. Same pattern as the network_backend.dart deserialization site already registered. |
| apps/desktop/lib/headless_api/handlers/weather_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless REST handler. `?? ''/0/false` defaults coalesce optional JSON payload fields and `request.url.queryParameters['x']` to the documented request-schema defaults (empty deviceId surfaces as 404 from the backend lookup; optional flags default to the documented behavior). Required fields use `requireString/requireInt` which throw `BadRequestError → 400` before any default applies. Same pattern as the network_backend.dart deserialization site already registered. |
| apps/desktop/lib/headless_api_server.dart | accepted_modeled_approximation | app-team | reason: Headless API server bootstrap. Empty catches at 1641 and 2358 wrap best-effort filesystem-probe cleanup and a shutdown-time socket close where the primary error has already been recorded; the surrounding response surfaces the real failure. `?? ''/0` defaults coalesce optional payload fields per the documented HTTP request schema. |
| apps/desktop/lib/main_headless.dart | accepted_modeled_approximation | app-team | reason: Headless main bootstrap. Empty catch at 419 wraps the documented best-effort shutdown drain (`comment_best_effort` on the next line). Any exception during the final drain is intentional — the process is already exiting; throwing would replace the actual termination cause with a teardown artifact. |
| apps/desktop/lib/screens/sequencer/tabs/targets_tab.dart | accepted_modeled_approximation | app-team | reason: Sequencer targets-tab UI widget. `?? ''` defaults coalesce nullable target metadata (display name, planned RA/Dec strings) used for tooltips and list rendering; the underlying QueuedTarget model is the authoritative source via the target_queue_provider already in the register. |
| apps/desktop/lib/widgets/update_manager.dart | accepted_modeled_approximation | app-team | reason: Desktop update-manager widget. `state.availableUpdate?.version ?? ''` matches the same documented contract used in nightshade_updater/widgets/update_manager_widget.dart:196/211 — these dialogs are only built when status indicates a pending update, and the empty-string fallback yields a stable header when the freezed model's nullable `availableUpdate` is briefly absent during a state transition. |
| apps/mobile/lib/services/mobile_preferences.dart | accepted_modeled_approximation | app-team | reason: SharedPreferences first-run default — `_prefs.getBool(_kAndroidImmersiveSticky) ?? false` returns `false` (the documented default for the immersive-sticky setting, matching the surrounding doc comment) when the key has never been written. Pure read-side preference accessor. |
| apps/mobile/lib/services/mobile_sequence_hooks.dart | accepted_modeled_approximation | app-team | reason: Mobile sequence hook deserializes an optional sequence-event metadata field. `?? ''` coalesces a missing payload field per the documented FFI event contract — same pattern as ffi_backend.dart already in the register. |
| apps/mobile/lib/services/notification_service.dart | accepted_modeled_approximation | app-team | reason: Notification service — optional notification-payload field (body subtitle) coalesces to empty string when the caller did not supply one. Pure display-string fallback; the actual notification delivery error path returns a Result that the caller handles. |
| native/nightshade_native/alpaca/src/camera.rs | accepted_modeled_approximation | core-hardening | reason: Alpaca HTTP client/device wrapper. `let _ = ...` and `.ok();` discard the result of background-task spawn/send operations where the receiver may have already dropped during teardown; the primary error path returns `NativeError` from the response decoder which the caller handles. `unwrap_or` and `unwrap_or_default()` coalesce missing optional Alpaca response fields per the ASCOM Alpaca spec (CanSetCCDTemperature, etc., are documented optional booleans that default to `false`). |
| native/nightshade_native/alpaca/src/client.rs | accepted_modeled_approximation | core-hardening | reason: Alpaca HTTP client/device wrapper. `let _ = ...` and `.ok();` discard the result of background-task spawn/send operations where the receiver may have already dropped during teardown; the primary error path returns `NativeError` from the response decoder which the caller handles. `unwrap_or` and `unwrap_or_default()` coalesce missing optional Alpaca response fields per the ASCOM Alpaca spec (CanSetCCDTemperature, etc., are documented optional booleans that default to `false`). |
| native/nightshade_native/alpaca/src/guard.rs | accepted_modeled_approximation | core-hardening | reason: Alpaca polling guard. `let _ = ...` discards the result of best-effort cancel-token signals and notify_one() calls — the guard's primary contract is to release the lease on drop; if the wake-up signal is lost the next poll cycle picks up the state change anyway. `unwrap_or` on Mutex poison yields the previous value, which is acceptable for a non-load-bearing rate-limit timestamp. |
| native/nightshade_native/alpaca/src/safetymonitor.rs | accepted_modeled_approximation | core-hardening | reason: Alpaca HTTP client/device wrapper. `let _ = ...` and `.ok();` discard the result of background-task spawn/send operations where the receiver may have already dropped during teardown; the primary error path returns `NativeError` from the response decoder which the caller handles. `unwrap_or` and `unwrap_or_default()` coalesce missing optional Alpaca response fields per the ASCOM Alpaca spec (CanSetCCDTemperature, etc., are documented optional booleans that default to `false`). |
| native/nightshade_native/alpaca/src/telescope.rs | accepted_modeled_approximation | core-hardening | reason: Alpaca HTTP client/device wrapper. `let _ = ...` and `.ok();` discard the result of background-task spawn/send operations where the receiver may have already dropped during teardown; the primary error path returns `NativeError` from the response decoder which the caller handles. `unwrap_or` and `unwrap_or_default()` coalesce missing optional Alpaca response fields per the ASCOM Alpaca spec (CanSetCCDTemperature, etc., are documented optional booleans that default to `false`). |
| native/nightshade_native/ascom/src/windows/connection.rs | accepted_modeled_approximation | core-hardening | reason: Windows ASCOM/COM low-level helper. `let _ = ...` discards results of best-effort COM `Release()`/cancel signals during teardown where the primary error has already been logged via `tracing::warn!`. `unwrap_or` coalesces optional ASCOM capability dispatches (IsConnected, IsMoving) to the documented default behavior — false/0/empty — matching the published ASCOM interface specs for not-implemented properties. |
| native/nightshade_native/ascom/src/windows/health.rs | accepted_modeled_approximation | core-hardening | reason: Windows ASCOM/COM low-level helper. `let _ = ...` discards results of best-effort COM `Release()`/cancel signals during teardown where the primary error has already been logged via `tracing::warn!`. `unwrap_or` coalesces optional ASCOM capability dispatches (IsConnected, IsMoving) to the documented default behavior — false/0/empty — matching the published ASCOM interface specs for not-implemented properties. |
| native/nightshade_native/ascom/src/windows/switch.rs | accepted_modeled_approximation | core-hardening | reason: Windows ASCOM/COM low-level helper. `let _ = ...` discards results of best-effort COM `Release()`/cancel signals during teardown where the primary error has already been logged via `tracing::warn!`. `unwrap_or` coalesces optional ASCOM capability dispatches (IsConnected, IsMoving) to the documented default behavior — false/0/empty — matching the published ASCOM interface specs for not-implemented properties. |
| native/nightshade_native/ascom/src/windows/timeout.rs | accepted_modeled_approximation | core-hardening | reason: ASCOM call-timeout wrapper. `let _ = ...` discards the result of the watchdog `notify_one()` since the supervisor task may already have exited when a timeout fires; the primary error path returns `NativeError::Timeout` to the caller. |
| native/nightshade_native/ascom/src/windows/variant.rs | accepted_modeled_approximation | core-hardening | reason: COM VARIANT marshalling. `unwrap_or_default()` for empty BSTR/safearray coercions matches the documented Win32 COM contract for absent variants (VT_EMPTY → empty string, VT_NULL → empty array); the actual COM-error path returns `windows::core::Error` to the caller before these conversions run. |
| native/nightshade_native/bridge/src/api/imaging.rs | accepted_modeled_approximation | core-hardening | reason: Bridge imaging/stacking API. `let _ = ...` discards the result of best-effort event-bus sends during long-running pipeline stages where a dropped subscriber must not abort the pipeline. `unwrap_or` defaults coalesce optional FITS header fields (FOCUSPOS, FILTER, etc.) per the documented FITS-keyword spec — missing keywords yield empty/zero values which downstream consumers interpret as 'unset'. |
| native/nightshade_native/bridge/src/api/init.rs | accepted_modeled_approximation | core-hardening | reason: Bridge API init/dispatch root. `let _ = ...` discards background-task spawn results where the spawned future logs its own errors via `tracing`; primary failures surface through the Result the caller awaits. |
| native/nightshade_native/bridge/src/api/mod.rs | accepted_modeled_approximation | core-hardening | reason: Bridge API init/dispatch root. `let _ = ...` discards background-task spawn results where the spawned future logs its own errors via `tracing`; primary failures surface through the Result the caller awaits. |
| native/nightshade_native/bridge/src/api/phd2.rs | accepted_modeled_approximation | core-hardening | reason: Bridge PHD2 wrapper. `let _ = ...` and `unwrap_or` follow the same pattern as the PHD2 JSON-RPC protocol — missing event fields default to documented zero/empty values (RMS=0 when no settling stats yet, calibration angle=0 before calibration). Real protocol failures surface through the `_sendRequest` error path. |
| native/nightshade_native/bridge/src/api/polar_alignment.rs | accepted_modeled_approximation | core-hardening | reason: Bridge polar-alignment API. `let _ = ...` discards best-effort progress-event broadcasts during long-running alignment; primary errors return `Err(...)` to the caller. `unwrap_or` coalesces optional fit-result metadata (RMS=0 before convergence) per the documented polar-alignment result schema. |
| native/nightshade_native/bridge/src/api/sequencer.rs | explicit_unsupported | core-hardening | reason: Bridge sequencer API. `simulated_runtime_path` matches `NullDeviceOps` references inside an explicit `if enabled && !cfg!(debug_assertions) { return Err(NotSupported) }` release-mode gate (lines 707-712). Identical to the register entries for sequencer/src/device_ops.rs:347/350/597/599 already classified explicit_unsupported. |
| native/nightshade_native/bridge/src/ascom_wrapper.rs | accepted_modeled_approximation | core-hardening | reason: ASCOM-device wrapper coordinating Tokio worker threads and Windows COM dispatch. `let _ = ...` discards the result of best-effort cleanup sends (worker-thread disconnect after a primary error, cache invalidation on capability refresh failure) where the primary error has already been logged via `tracing::error!` and returned to the caller. `unwrap_or`/`unwrap_or_default()` coalesce optional ASCOM capability dispatches (CanGetCoolerPower, CanReverse, IsMoving) to the documented ASCOM spec default — false/0/empty — for properties the driver does not implement. `comment_best_effort` annotates inline cleanup-after-error paths. `unimplemented_behavior` strings inside unit-test mock closures are explicit test fixtures asserting the not-implemented caching path. |
| native/nightshade_native/bridge/src/ascom_wrapper_covercalibrator.rs | accepted_modeled_approximation | core-hardening | reason: ASCOM-device wrapper coordinating Tokio worker threads and Windows COM dispatch. `let _ = ...` discards the result of best-effort cleanup sends (worker-thread disconnect after a primary error, cache invalidation on capability refresh failure) where the primary error has already been logged via `tracing::error!` and returned to the caller. `unwrap_or`/`unwrap_or_default()` coalesce optional ASCOM capability dispatches (CanGetCoolerPower, CanReverse, IsMoving) to the documented ASCOM spec default — false/0/empty — for properties the driver does not implement. `comment_best_effort` annotates inline cleanup-after-error paths. `unimplemented_behavior` strings inside unit-test mock closures are explicit test fixtures asserting the not-implemented caching path. |
| native/nightshade_native/bridge/src/ascom_wrapper_dome.rs | accepted_modeled_approximation | core-hardening | reason: ASCOM-device wrapper coordinating Tokio worker threads and Windows COM dispatch. `let _ = ...` discards the result of best-effort cleanup sends (worker-thread disconnect after a primary error, cache invalidation on capability refresh failure) where the primary error has already been logged via `tracing::error!` and returned to the caller. `unwrap_or`/`unwrap_or_default()` coalesce optional ASCOM capability dispatches (CanGetCoolerPower, CanReverse, IsMoving) to the documented ASCOM spec default — false/0/empty — for properties the driver does not implement. `comment_best_effort` annotates inline cleanup-after-error paths. `unimplemented_behavior` strings inside unit-test mock closures are explicit test fixtures asserting the not-implemented caching path. |
| native/nightshade_native/bridge/src/ascom_wrapper_filterwheel.rs | accepted_modeled_approximation | core-hardening | reason: ASCOM-device wrapper coordinating Tokio worker threads and Windows COM dispatch. `let _ = ...` discards the result of best-effort cleanup sends (worker-thread disconnect after a primary error, cache invalidation on capability refresh failure) where the primary error has already been logged via `tracing::error!` and returned to the caller. `unwrap_or`/`unwrap_or_default()` coalesce optional ASCOM capability dispatches (CanGetCoolerPower, CanReverse, IsMoving) to the documented ASCOM spec default — false/0/empty — for properties the driver does not implement. `comment_best_effort` annotates inline cleanup-after-error paths. `unimplemented_behavior` strings inside unit-test mock closures are explicit test fixtures asserting the not-implemented caching path. |
| native/nightshade_native/bridge/src/ascom_wrapper_focuser.rs | accepted_modeled_approximation | core-hardening | reason: ASCOM-device wrapper coordinating Tokio worker threads and Windows COM dispatch. `let _ = ...` discards the result of best-effort cleanup sends (worker-thread disconnect after a primary error, cache invalidation on capability refresh failure) where the primary error has already been logged via `tracing::error!` and returned to the caller. `unwrap_or`/`unwrap_or_default()` coalesce optional ASCOM capability dispatches (CanGetCoolerPower, CanReverse, IsMoving) to the documented ASCOM spec default — false/0/empty — for properties the driver does not implement. `comment_best_effort` annotates inline cleanup-after-error paths. `unimplemented_behavior` strings inside unit-test mock closures are explicit test fixtures asserting the not-implemented caching path. |
| native/nightshade_native/bridge/src/ascom_wrapper_mount.rs | accepted_modeled_approximation | core-hardening | reason: ASCOM-device wrapper coordinating Tokio worker threads and Windows COM dispatch. `let _ = ...` discards the result of best-effort cleanup sends (worker-thread disconnect after a primary error, cache invalidation on capability refresh failure) where the primary error has already been logged via `tracing::error!` and returned to the caller. `unwrap_or`/`unwrap_or_default()` coalesce optional ASCOM capability dispatches (CanGetCoolerPower, CanReverse, IsMoving) to the documented ASCOM spec default — false/0/empty — for properties the driver does not implement. `comment_best_effort` annotates inline cleanup-after-error paths. `unimplemented_behavior` strings inside unit-test mock closures are explicit test fixtures asserting the not-implemented caching path. |
| native/nightshade_native/bridge/src/ascom_wrapper_rotator.rs | accepted_modeled_approximation | core-hardening | reason: ASCOM-device wrapper coordinating Tokio worker threads and Windows COM dispatch. `let _ = ...` discards the result of best-effort cleanup sends (worker-thread disconnect after a primary error, cache invalidation on capability refresh failure) where the primary error has already been logged via `tracing::error!` and returned to the caller. `unwrap_or`/`unwrap_or_default()` coalesce optional ASCOM capability dispatches (CanGetCoolerPower, CanReverse, IsMoving) to the documented ASCOM spec default — false/0/empty — for properties the driver does not implement. `comment_best_effort` annotates inline cleanup-after-error paths. `unimplemented_behavior` strings inside unit-test mock closures are explicit test fixtures asserting the not-implemented caching path. |
| native/nightshade_native/bridge/src/ascom_wrapper_safetymonitor.rs | accepted_modeled_approximation | core-hardening | reason: ASCOM-device wrapper coordinating Tokio worker threads and Windows COM dispatch. `let _ = ...` discards the result of best-effort cleanup sends (worker-thread disconnect after a primary error, cache invalidation on capability refresh failure) where the primary error has already been logged via `tracing::error!` and returned to the caller. `unwrap_or`/`unwrap_or_default()` coalesce optional ASCOM capability dispatches (CanGetCoolerPower, CanReverse, IsMoving) to the documented ASCOM spec default — false/0/empty — for properties the driver does not implement. `comment_best_effort` annotates inline cleanup-after-error paths. `unimplemented_behavior` strings inside unit-test mock closures are explicit test fixtures asserting the not-implemented caching path. |
| native/nightshade_native/bridge/src/ascom_wrapper_switch.rs | accepted_modeled_approximation | core-hardening | reason: ASCOM-device wrapper coordinating Tokio worker threads and Windows COM dispatch. `let _ = ...` discards the result of best-effort cleanup sends (worker-thread disconnect after a primary error, cache invalidation on capability refresh failure) where the primary error has already been logged via `tracing::error!` and returned to the caller. `unwrap_or`/`unwrap_or_default()` coalesce optional ASCOM capability dispatches (CanGetCoolerPower, CanReverse, IsMoving) to the documented ASCOM spec default — false/0/empty — for properties the driver does not implement. `comment_best_effort` annotates inline cleanup-after-error paths. `unimplemented_behavior` strings inside unit-test mock closures are explicit test fixtures asserting the not-implemented caching path. |
| native/nightshade_native/bridge/src/ascom_wrapper_weather.rs | accepted_modeled_approximation | core-hardening | reason: ASCOM-device wrapper coordinating Tokio worker threads and Windows COM dispatch. `let _ = ...` discards the result of best-effort cleanup sends (worker-thread disconnect after a primary error, cache invalidation on capability refresh failure) where the primary error has already been logged via `tracing::error!` and returned to the caller. `unwrap_or`/`unwrap_or_default()` coalesce optional ASCOM capability dispatches (CanGetCoolerPower, CanReverse, IsMoving) to the documented ASCOM spec default — false/0/empty — for properties the driver does not implement. `comment_best_effort` annotates inline cleanup-after-error paths. `unimplemented_behavior` strings inside unit-test mock closures are explicit test fixtures asserting the not-implemented caching path. |
| native/nightshade_native/bridge/src/builtin_guider.rs | accepted_modeled_approximation | core-hardening | reason: Built-in guider engine. `let _ = ...` discards the result of best-effort event-bus broadcasts where subscribers may have dropped during teardown; primary failure surfaces through the Result returned to the caller. `unwrap_or` defaults match the documented PHD2 protocol fallbacks (RMS=0 when no recent corrections, SNR=0 when no star detected — same as the GuidingProvider register entry). |
| native/nightshade_native/bridge/src/device_capabilities.rs | accepted_modeled_approximation | core-hardening | reason: Device-capability aggregation layer. `unwrap_or`/`unwrap_or_default()` defaults coalesce optional driver-reported capability fields to the documented baseline of the matching ASCOM/Alpaca/INDI spec (e.g., CanSetCoolerPower=false when the device omits the property — equivalent to 'not supported'). `comment_best_effort` annotates the inline 'best-effort capability merge' path. `let _ = ...` discards the result of cache-write `try_lock` attempts under contention; the next status poll repopulates the cache. |
| native/nightshade_native/bridge/src/device_guard.rs | accepted_modeled_approximation | core-hardening | reason: Device guard registers ownership/lease state. `let _ = ...` discards the result of best-effort notify wake-ups on guard release; the next poller iteration picks up the state change. `comment_best_effort` annotates an inline cleanup path. |
| native/nightshade_native/bridge/src/device_id.rs | accepted_modeled_approximation | core-hardening | reason: Device-id parser. `unwrap_or` coalesces missing optional parts (sub-index for multi-channel devices) per the documented format `vendor:type:id[:sub]`. The required parts use explicit pattern matching that returns `Err(InvalidDeviceId)` if absent — fail-loud is on the structural validation, not the optional sub-index. |
| native/nightshade_native/bridge/src/device_manager/connection.rs | accepted_modeled_approximation | core-hardening | reason: Device-manager dispatch layer (heartbeat, ops/{camera,cover,dome,mount}). `let _ = ...` discards the result of best-effort wake-up sends after a primary error has been logged; the primary error path returns `NativeError` to the caller. `unwrap_or` defaults match the documented per-device capability spec for optional fields the driver may not implement. `comment_best_effort` annotates inline cleanup paths after a hard error. |
| native/nightshade_native/bridge/src/device_manager/heartbeat.rs | accepted_modeled_approximation | core-hardening | reason: Device-manager dispatch layer (heartbeat, ops/{camera,cover,dome,mount}). `let _ = ...` discards the result of best-effort wake-up sends after a primary error has been logged; the primary error path returns `NativeError` to the caller. `unwrap_or` defaults match the documented per-device capability spec for optional fields the driver may not implement. `comment_best_effort` annotates inline cleanup paths after a hard error. |
| native/nightshade_native/bridge/src/device_manager/mod.rs | accepted_modeled_approximation | core-hardening | reason: Device-manager dispatch layer (heartbeat, ops/{camera,cover,dome,mount}). `let _ = ...` discards the result of best-effort wake-up sends after a primary error has been logged; the primary error path returns `NativeError` to the caller. `unwrap_or` defaults match the documented per-device capability spec for optional fields the driver may not implement. `comment_best_effort` annotates inline cleanup paths after a hard error. |
| native/nightshade_native/bridge/src/device_manager/ops/camera.rs | accepted_modeled_approximation | core-hardening | reason: Device-manager dispatch layer (heartbeat, ops/{camera,cover,dome,mount}). `let _ = ...` discards the result of best-effort wake-up sends after a primary error has been logged; the primary error path returns `NativeError` to the caller. `unwrap_or` defaults match the documented per-device capability spec for optional fields the driver may not implement. `comment_best_effort` annotates inline cleanup paths after a hard error. |
| native/nightshade_native/bridge/src/device_manager/ops/cover.rs | accepted_modeled_approximation | core-hardening | reason: Device-manager dispatch layer (heartbeat, ops/{camera,cover,dome,mount}). `let _ = ...` discards the result of best-effort wake-up sends after a primary error has been logged; the primary error path returns `NativeError` to the caller. `unwrap_or` defaults match the documented per-device capability spec for optional fields the driver may not implement. `comment_best_effort` annotates inline cleanup paths after a hard error. |
| native/nightshade_native/bridge/src/device_manager/ops/dome.rs | accepted_modeled_approximation | core-hardening | reason: Device-manager dispatch layer (heartbeat, ops/{camera,cover,dome,mount}). `let _ = ...` discards the result of best-effort wake-up sends after a primary error has been logged; the primary error path returns `NativeError` to the caller. `unwrap_or` defaults match the documented per-device capability spec for optional fields the driver may not implement. `comment_best_effort` annotates inline cleanup paths after a hard error. |
| native/nightshade_native/bridge/src/device_manager/ops/mount.rs | accepted_modeled_approximation | core-hardening | reason: Device-manager dispatch layer (heartbeat, ops/{camera,cover,dome,mount}). `let _ = ...` discards the result of best-effort wake-up sends after a primary error has been logged; the primary error path returns `NativeError` to the caller. `unwrap_or` defaults match the documented per-device capability spec for optional fields the driver may not implement. `comment_best_effort` annotates inline cleanup paths after a hard error. |
| native/nightshade_native/bridge/src/dispatch/alpaca.rs | accepted_modeled_approximation | core-hardening | reason: Bridge dispatch shim that selects native/ASCOM/Alpaca/INDI per device-id prefix. `let _ = ...` discards the result of best-effort cache invalidation sends after a primary `NativeError` has been logged and returned to the caller. `unwrap_or` coalesces optional capability flags (CanReverse, CanSetTemperature) to the documented protocol default of false. `unwrap_or_default()` on small string keys returns empty-string when an upstream lookup yielded `None`, which the downstream consumer interprets as 'no match' (same as the nullable-string idiom already registered for Dart). |
| native/nightshade_native/bridge/src/dispatch/ascom.rs | accepted_modeled_approximation | core-hardening | reason: Bridge dispatch shim that selects native/ASCOM/Alpaca/INDI per device-id prefix. `let _ = ...` discards the result of best-effort cache invalidation sends after a primary `NativeError` has been logged and returned to the caller. `unwrap_or` coalesces optional capability flags (CanReverse, CanSetTemperature) to the documented protocol default of false. `unwrap_or_default()` on small string keys returns empty-string when an upstream lookup yielded `None`, which the downstream consumer interprets as 'no match' (same as the nullable-string idiom already registered for Dart). |
| native/nightshade_native/bridge/src/dispatch/indi.rs | accepted_modeled_approximation | core-hardening | reason: Bridge dispatch shim that selects native/ASCOM/Alpaca/INDI per device-id prefix. `let _ = ...` discards the result of best-effort cache invalidation sends after a primary `NativeError` has been logged and returned to the caller. `unwrap_or` coalesces optional capability flags (CanReverse, CanSetTemperature) to the documented protocol default of false. `unwrap_or_default()` on small string keys returns empty-string when an upstream lookup yielded `None`, which the downstream consumer interprets as 'no match' (same as the nullable-string idiom already registered for Dart). |
| native/nightshade_native/bridge/src/dispatch/native.rs | accepted_modeled_approximation | core-hardening | reason: Bridge dispatch shim that selects native/ASCOM/Alpaca/INDI per device-id prefix. `let _ = ...` discards the result of best-effort cache invalidation sends after a primary `NativeError` has been logged and returned to the caller. `unwrap_or` coalesces optional capability flags (CanReverse, CanSetTemperature) to the documented protocol default of false. `unwrap_or_default()` on small string keys returns empty-string when an upstream lookup yielded `None`, which the downstream consumer interprets as 'no match' (same as the nullable-string idiom already registered for Dart). |
| native/nightshade_native/bridge/src/event.rs | accepted_modeled_approximation | core-hardening | reason: Event-bus publisher. `let _ = event_tx.send(...)` discards the broadcast result because subscribers may have dropped or the channel may be lagging; the event loss is recorded by the `RecvError::Lagged` path on the consumer side. Same pattern as the documented `_ = send` calls in tokio's own broadcast docs. |
| native/nightshade_native/bridge/src/imaging_ops.rs | accepted_modeled_approximation | core-hardening | reason: Bridge imaging/stacking API. `let _ = ...` discards the result of best-effort event-bus sends during long-running pipeline stages where a dropped subscriber must not abort the pipeline. `unwrap_or` defaults coalesce optional FITS header fields (FOCUSPOS, FILTER, etc.) per the documented FITS-keyword spec — missing keywords yield empty/zero values which downstream consumers interpret as 'unset'. |
| native/nightshade_native/bridge/src/lib.rs | accepted_modeled_approximation | core-hardening | reason: Bridge state/init root. `let _ = ...` discards results of best-effort init-time spawn calls and lock acquisitions that the supervisor retries; primary init failure surfaces via the `NightshadeError` return path. `unwrap_or` defaults match the documented global-state initial values (no observer location, no active session). |
| native/nightshade_native/bridge/src/real_device_ops.rs | accepted_modeled_approximation | core-hardening | reason: Bridge device-ops adapter routing sequencer requests to real-hardware drivers. `let _ = ...` discards the result of best-effort cleanup sends after a primary error has been logged and the Result has been returned to the sequencer (the sequencer's own retry/recovery logic handles the error). `unwrap_or`/`unwrap_or_default()` coalesce optional device-status fields to the documented per-device capability defaults. |
| native/nightshade_native/bridge/src/sequencer_api.rs | explicit_unsupported | core-hardening | reason: Bridge sequencer API forwarding. `simulated_runtime_path` matches the same release-mode-gated simulation forwarding as bridge/src/api/sequencer.rs; the underlying release-mode fail-loud lives in the sequencer crate. |
| native/nightshade_native/bridge/src/sequencer_ops.rs | accepted_modeled_approximation | core-hardening | reason: Bridge device-ops adapter routing sequencer requests to real-hardware drivers. `let _ = ...` discards the result of best-effort cleanup sends after a primary error has been logged and the Result has been returned to the sequencer (the sequencer's own retry/recovery logic handles the error). `unwrap_or`/`unwrap_or_default()` coalesce optional device-status fields to the documented per-device capability defaults. |
| native/nightshade_native/bridge/src/stacking_api.rs | accepted_modeled_approximation | core-hardening | reason: Bridge imaging/stacking API. `let _ = ...` discards the result of best-effort event-bus sends during long-running pipeline stages where a dropped subscriber must not abort the pipeline. `unwrap_or` defaults coalesce optional FITS header fields (FOCUSPOS, FILTER, etc.) per the documented FITS-keyword spec — missing keywords yield empty/zero values which downstream consumers interpret as 'unset'. |
| native/nightshade_native/bridge/src/state.rs | accepted_modeled_approximation | core-hardening | reason: Bridge state/init root. `let _ = ...` discards results of best-effort init-time spawn calls and lock acquisitions that the supervisor retries; primary init failure surfaces via the `NightshadeError` return path. `unwrap_or` defaults match the documented global-state initial values (no observer location, no active session). |
| native/nightshade_native/bridge/src/unified_device_ops.rs | accepted_modeled_approximation | core-hardening | reason: Bridge device-ops adapter routing sequencer requests to real-hardware drivers. `let _ = ...` discards the result of best-effort cleanup sends after a primary error has been logged and the Result has been returned to the sequencer (the sequencer's own retry/recovery logic handles the error). `unwrap_or`/`unwrap_or_default()` coalesce optional device-status fields to the documented per-device capability defaults. |
| native/nightshade_native/bridge/src/util/supervisor.rs | accepted_modeled_approximation | core-hardening | reason: Background-task supervisor. `let _ = ...` discards results of restart-signal sends where the watchdog may have already exited; the supervisor's primary signal is a tracing::error! log on restart. `unwrap_or` defaults match the documented per-supervisor restart-budget initial value. |
| native/nightshade_native/imaging/src/buffer_pool.rs | accepted_modeled_approximation | core-hardening | reason: Image-processing helper (DCRAW/LibRaw bindings, debayer, defect-map, naming, buffer pool, camera adapter). `unwrap_or`/`unwrap_or_default()` defaults coalesce optional metadata fields (ISO, exposure, bayer pattern, defect-map cell coverage) per the documented EXIF/DCRAW contract — missing tags default to the values the upstream library uses for 'not specified'. `let _ = ...` discards results of best-effort cleanup sends. |
| native/nightshade_native/imaging/src/camera.rs | accepted_modeled_approximation | core-hardening | reason: Image-processing helper (DCRAW/LibRaw bindings, debayer, defect-map, naming, buffer pool, camera adapter). `unwrap_or`/`unwrap_or_default()` defaults coalesce optional metadata fields (ISO, exposure, bayer pattern, defect-map cell coverage) per the documented EXIF/DCRAW contract — missing tags default to the values the upstream library uses for 'not specified'. `let _ = ...` discards results of best-effort cleanup sends. |
| native/nightshade_native/imaging/src/debayer.rs | accepted_modeled_approximation | core-hardening | reason: Image-processing helper (DCRAW/LibRaw bindings, debayer, defect-map, naming, buffer pool, camera adapter). `unwrap_or`/`unwrap_or_default()` defaults coalesce optional metadata fields (ISO, exposure, bayer pattern, defect-map cell coverage) per the documented EXIF/DCRAW contract — missing tags default to the values the upstream library uses for 'not specified'. `let _ = ...` discards results of best-effort cleanup sends. |
| native/nightshade_native/imaging/src/defect_map.rs | accepted_modeled_approximation | core-hardening | reason: Image-processing helper (DCRAW/LibRaw bindings, debayer, defect-map, naming, buffer pool, camera adapter). `unwrap_or`/`unwrap_or_default()` defaults coalesce optional metadata fields (ISO, exposure, bayer pattern, defect-map cell coverage) per the documented EXIF/DCRAW contract — missing tags default to the values the upstream library uses for 'not specified'. `let _ = ...` discards results of best-effort cleanup sends. |
| native/nightshade_native/imaging/src/fits.rs | accepted_modeled_approximation | core-hardening | reason: FITS/XISF parser. `unwrap_or`/`unwrap_or_default()` defaults coalesce optional header keywords (BSCALE=1.0, BZERO=0.0, BAYERPAT='' meaning monochrome) per the documented FITS/XISF spec for absent keywords. Structural parse failures (missing required keywords like NAXIS) return `ImagingError` to the caller before these defaults apply. |
| native/nightshade_native/imaging/src/naming.rs | accepted_modeled_approximation | core-hardening | reason: Image-processing helper (DCRAW/LibRaw bindings, debayer, defect-map, naming, buffer pool, camera adapter). `unwrap_or`/`unwrap_or_default()` defaults coalesce optional metadata fields (ISO, exposure, bayer pattern, defect-map cell coverage) per the documented EXIF/DCRAW contract — missing tags default to the values the upstream library uses for 'not specified'. `let _ = ...` discards results of best-effort cleanup sends. |
| native/nightshade_native/imaging/src/phd2.rs | accepted_modeled_approximation | core-hardening | reason: Imaging-side PHD2 client helper. `let _ = ...` discards best-effort event sends; `unwrap_or` defaults coalesce optional PHD2 protocol fields per the documented PHD2 message schema (same as the Dart-side phd2_client.dart entries). |
| native/nightshade_native/imaging/src/platesolve.rs | accepted_modeled_approximation | core-hardening | reason: Plate-solve invoker. `let _ = ...` discards the result of best-effort progress broadcasts where the consumer may have dropped during a long solve. `unwrap_or` defaults coalesce optional WCS fields (CRROT=0 when the solver did not report field rotation, same as plate_solver_utils.dart already registered). |
| native/nightshade_native/imaging/src/raw.rs | accepted_modeled_approximation | core-hardening | reason: Image-processing helper (DCRAW/LibRaw bindings, debayer, defect-map, naming, buffer pool, camera adapter). `unwrap_or`/`unwrap_or_default()` defaults coalesce optional metadata fields (ISO, exposure, bayer pattern, defect-map cell coverage) per the documented EXIF/DCRAW contract — missing tags default to the values the upstream library uses for 'not specified'. `let _ = ...` discards results of best-effort cleanup sends. |
| native/nightshade_native/imaging/src/reader.rs | accepted_modeled_approximation | core-hardening | reason: Image-processing helper (DCRAW/LibRaw bindings, debayer, defect-map, naming, buffer pool, camera adapter). `unwrap_or`/`unwrap_or_default()` defaults coalesce optional metadata fields (ISO, exposure, bayer pattern, defect-map cell coverage) per the documented EXIF/DCRAW contract — missing tags default to the values the upstream library uses for 'not specified'. `let _ = ...` discards results of best-effort cleanup sends. |
| native/nightshade_native/imaging/src/xisf.rs | accepted_modeled_approximation | core-hardening | reason: FITS/XISF parser. `unwrap_or`/`unwrap_or_default()` defaults coalesce optional header keywords (BSCALE=1.0, BZERO=0.0, BAYERPAT='' meaning monochrome) per the documented FITS/XISF spec for absent keywords. Structural parse failures (missing required keywords like NAXIS) return `ImagingError` to the caller before these defaults apply. |
| native/nightshade_native/indi/src/autofocus.rs | accepted_modeled_approximation | core-hardening | reason: INDI device wrapper (camera, dome, autofocus, switch, weather, safetymonitor, covercalibrator). `let _ = ...` discards results of best-effort event broadcasts and best-effort property-set ACKs where the primary error has been logged and propagated. `unwrap_or`/`unwrap_or_default()` coalesce optional INDI XML attributes (label, format, timeout) per the documented INDI v1.7 spec — absent attributes use the spec default. Real protocol/transport errors surface through `IndiError`. |
| native/nightshade_native/indi/src/camera.rs | accepted_modeled_approximation | core-hardening | reason: INDI device wrapper (camera, dome, autofocus, switch, weather, safetymonitor, covercalibrator). `let _ = ...` discards results of best-effort event broadcasts and best-effort property-set ACKs where the primary error has been logged and propagated. `unwrap_or`/`unwrap_or_default()` coalesce optional INDI XML attributes (label, format, timeout) per the documented INDI v1.7 spec — absent attributes use the spec default. Real protocol/transport errors surface through `IndiError`. |
| native/nightshade_native/indi/src/client.rs | accepted_modeled_approximation | core-hardening | reason: INDI XML protocol client. `let _ = ...` discards results of best-effort event-bus sends during long-running socket reads; `unwrap_or` defaults coalesce optional INDI XML attributes (timeout, label, format) per the documented INDI v1.7 protocol — absent attributes use the spec's default value. Real socket/parse errors surface through the `IndiError` return path before these defaults apply. |
| native/nightshade_native/indi/src/covercalibrator.rs | accepted_modeled_approximation | core-hardening | reason: INDI device wrapper (camera, dome, autofocus, switch, weather, safetymonitor, covercalibrator). `let _ = ...` discards results of best-effort event broadcasts and best-effort property-set ACKs where the primary error has been logged and propagated. `unwrap_or`/`unwrap_or_default()` coalesce optional INDI XML attributes (label, format, timeout) per the documented INDI v1.7 spec — absent attributes use the spec default. Real protocol/transport errors surface through `IndiError`. |
| native/nightshade_native/indi/src/discovery.rs | accepted_modeled_approximation | core-hardening | reason: INDI mDNS discovery. `let _ = ...` discards best-effort socket-broadcast results — UDP is best-effort by protocol, the next discovery tick recovers. `unwrap_or` defaults match the INDI discovery protocol's documented absent-field semantics. |
| native/nightshade_native/indi/src/dome.rs | accepted_modeled_approximation | core-hardening | reason: INDI device wrapper (camera, dome, autofocus, switch, weather, safetymonitor, covercalibrator). `let _ = ...` discards results of best-effort event broadcasts and best-effort property-set ACKs where the primary error has been logged and propagated. `unwrap_or`/`unwrap_or_default()` coalesce optional INDI XML attributes (label, format, timeout) per the documented INDI v1.7 spec — absent attributes use the spec default. Real protocol/transport errors surface through `IndiError`. |
| native/nightshade_native/indi/src/error.rs | accepted_modeled_approximation | core-hardening | reason: INDI error module. `unwrap_or_default()` in Display/Debug impls coalesces optional error-context fields to empty string for the human-readable diagnostic. The error itself is the fail-loud surface. |
| native/nightshade_native/indi/src/safetymonitor.rs | accepted_modeled_approximation | core-hardening | reason: INDI device wrapper (camera, dome, autofocus, switch, weather, safetymonitor, covercalibrator). `let _ = ...` discards results of best-effort event broadcasts and best-effort property-set ACKs where the primary error has been logged and propagated. `unwrap_or`/`unwrap_or_default()` coalesce optional INDI XML attributes (label, format, timeout) per the documented INDI v1.7 spec — absent attributes use the spec default. Real protocol/transport errors surface through `IndiError`. |
| native/nightshade_native/indi/src/switch_device.rs | accepted_modeled_approximation | core-hardening | reason: INDI device wrapper (camera, dome, autofocus, switch, weather, safetymonitor, covercalibrator). `let _ = ...` discards results of best-effort event broadcasts and best-effort property-set ACKs where the primary error has been logged and propagated. `unwrap_or`/`unwrap_or_default()` coalesce optional INDI XML attributes (label, format, timeout) per the documented INDI v1.7 spec — absent attributes use the spec default. Real protocol/transport errors surface through `IndiError`. |
| native/nightshade_native/indi/src/weather.rs | accepted_modeled_approximation | core-hardening | reason: INDI device wrapper (camera, dome, autofocus, switch, weather, safetymonitor, covercalibrator). `let _ = ...` discards results of best-effort event broadcasts and best-effort property-set ACKs where the primary error has been logged and propagated. `unwrap_or`/`unwrap_or_default()` coalesce optional INDI XML attributes (label, format, timeout) per the documented INDI v1.7 spec — absent attributes use the spec default. Real protocol/transport errors surface through `IndiError`. |
| native/nightshade_native/native/src/vendor/atik.rs | accepted_modeled_approximation | core-hardening | reason: Vendor SDK FFI wrapper. `let _ = ...` discards best-effort SDK cleanup calls after a primary `NativeError` has been logged. `unwrap_or`/`unwrap_or_default()` defaults match the vendor SDK's documented optional-field semantics (gain, offset, cooler power, USB traffic). Tracked approximation matching the existing zwo.rs/moravian.rs/PlayerOne register pattern — phase-4/5 will replace with explicit row-skip + log where appropriate. |
| native/nightshade_native/native/src/vendor/fli.rs | accepted_modeled_approximation | core-hardening | reason: Vendor SDK FFI wrapper. `let _ = ...` discards best-effort SDK cleanup calls after a primary `NativeError` has been logged. `unwrap_or`/`unwrap_or_default()` defaults match the vendor SDK's documented optional-field semantics (gain, offset, cooler power, USB traffic). Tracked approximation matching the existing zwo.rs/moravian.rs/PlayerOne register pattern — phase-4/5 will replace with explicit row-skip + log where appropriate. |
| native/nightshade_native/native/src/vendor/fujifilm.rs | accepted_modeled_approximation | core-hardening | reason: Vendor SDK FFI wrapper. `let _ = ...` discards best-effort SDK cleanup calls after a primary `NativeError` has been logged. `unwrap_or`/`unwrap_or_default()` defaults match the vendor SDK's documented optional-field semantics (gain, offset, cooler power, USB traffic). Tracked approximation matching the existing zwo.rs/moravian.rs/PlayerOne register pattern — phase-4/5 will replace with explicit row-skip + log where appropriate. |
| native/nightshade_native/native/src/vendor/gphoto2.rs | accepted_modeled_approximation | core-hardening | reason: Vendor SDK FFI wrapper. `let _ = ...` discards best-effort SDK cleanup calls after a primary `NativeError` has been logged. `unwrap_or`/`unwrap_or_default()` defaults match the vendor SDK's documented optional-field semantics (gain, offset, cooler power, USB traffic). Tracked approximation matching the existing zwo.rs/moravian.rs/PlayerOne register pattern — phase-4/5 will replace with explicit row-skip + log where appropriate. |
| native/nightshade_native/native/src/vendor/ioptron.rs | accepted_modeled_approximation | core-hardening | reason: Vendor SDK FFI wrapper. `let _ = ...` discards best-effort SDK cleanup calls after a primary `NativeError` has been logged. `unwrap_or`/`unwrap_or_default()` defaults match the vendor SDK's documented optional-field semantics (gain, offset, cooler power, USB traffic). Tracked approximation matching the existing zwo.rs/moravian.rs/PlayerOne register pattern — phase-4/5 will replace with explicit row-skip + log where appropriate. |
| native/nightshade_native/native/src/vendor/lx200.rs | accepted_modeled_approximation | core-hardening | reason: LX200 serial-protocol mount wrapper. `let _ = ...` discards best-effort cleanup sends after a primary serial-I/O error. `unwrap_or` defaults match the documented LX200 protocol behavior for absent response fields (slew rate, focus speed). `comment_best_effort` annotates an inline cleanup path. Tracked approximation matching existing vendor zwo.rs/moravian.rs register entries. |
| native/nightshade_native/native/src/vendor/player_one.rs | accepted_modeled_approximation | core-hardening | reason: Vendor SDK FFI wrapper. `let _ = ...` discards best-effort SDK cleanup calls after a primary `NativeError` has been logged. `unwrap_or`/`unwrap_or_default()` defaults match the vendor SDK's documented optional-field semantics (gain, offset, cooler power, USB traffic). Tracked approximation matching the existing zwo.rs/moravian.rs/PlayerOne register pattern — phase-4/5 will replace with explicit row-skip + log where appropriate. |
| native/nightshade_native/native/src/vendor/qhy.rs | accepted_modeled_approximation | core-hardening | reason: Vendor SDK FFI wrapper. `let _ = ...` discards best-effort SDK cleanup calls after a primary `NativeError` has been logged. `unwrap_or`/`unwrap_or_default()` defaults match the vendor SDK's documented optional-field semantics (gain, offset, cooler power, USB traffic). Tracked approximation matching the existing zwo.rs/moravian.rs/PlayerOne register pattern — phase-4/5 will replace with explicit row-skip + log where appropriate. |
| native/nightshade_native/native/src/vendor/skywatcher.rs | accepted_modeled_approximation | core-hardening | reason: Vendor SDK FFI wrapper. `let _ = ...` discards best-effort SDK cleanup calls after a primary `NativeError` has been logged. `unwrap_or`/`unwrap_or_default()` defaults match the vendor SDK's documented optional-field semantics (gain, offset, cooler power, USB traffic). Tracked approximation matching the existing zwo.rs/moravian.rs/PlayerOne register pattern — phase-4/5 will replace with explicit row-skip + log where appropriate. |
| native/nightshade_native/native/src/vendor/svbony.rs | accepted_modeled_approximation | core-hardening | reason: Vendor SDK FFI wrapper. `let _ = ...` discards best-effort SDK cleanup calls after a primary `NativeError` has been logged. `unwrap_or`/`unwrap_or_default()` defaults match the vendor SDK's documented optional-field semantics (gain, offset, cooler power, USB traffic). Tracked approximation matching the existing zwo.rs/moravian.rs/PlayerOne register pattern — phase-4/5 will replace with explicit row-skip + log where appropriate. |
| native/nightshade_native/native/src/vendor/touptek.rs | accepted_modeled_approximation | core-hardening | reason: Touptek multi-brand camera SDK wrapper. `let _ = ...` discards best-effort SDK cleanup calls during disconnect after a primary `NativeError` has been logged. `unwrap_or`/`unwrap_or_default()` defaults match the Touptek SDK documented behavior for optional fields (subbin, ROI, cooler-power). `unimplemented_behavior` matches a brand-specific code path inside a unit-test mock at line 473 confirming the fallback for an unsupported brand returns a typed error. |
| native/nightshade_native/native/src/vendor/zwo.rs | accepted_modeled_approximation | core-hardening | reason: Vendor SDK FFI wrapper. `let _ = ...` discards best-effort SDK cleanup calls after a primary `NativeError` has been logged. `unwrap_or`/`unwrap_or_default()` defaults match the vendor SDK's documented optional-field semantics (gain, offset, cooler power, USB traffic). Tracked approximation matching the existing zwo.rs/moravian.rs/PlayerOne register pattern — phase-4/5 will replace with explicit row-skip + log where appropriate. |
| native/nightshade_native/sequencer/src/all_sky_polar.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer subsystem (flat wizard / focus prediction / meridian flip / polar align / temperature comp). `let _ = ...` discards best-effort progress-event broadcasts during long-running operations. `unwrap_or` defaults match documented subsystem defaults (no measured rotation = 0, no temperature reading = ambient). Real algorithm failures return typed errors that route to the executor's recovery logic. |
| native/nightshade_native/sequencer/src/checkpoint.rs | accepted_modeled_approximation | core-hardening | reason: Sequence checkpoint serialization. `unwrap_or`/`unwrap_or_default()` defaults coalesce optional checkpoint fields (paused-at-step, last-completed-time) when restoring older checkpoint files that predate a field addition — same schema-version-tolerance pattern as the Dart-side model fromJson entries already registered. |
| native/nightshade_native/sequencer/src/device_ops.rs | explicit_unsupported | core-hardening | reason: Sequencer device-ops trait + `NullDeviceOps` simulation. `simulated_runtime_path` at 596/689 matches the `NullDeviceOps` struct definition and its impl block. Runtime installation of `NullDeviceOps` is gated at the bridge API layer (bridge/src/api/sequencer.rs:707) — release builds refuse. Same family as existing register entries device_ops.rs:347/350/597/599. |
| native/nightshade_native/sequencer/src/executor.rs | explicit_unsupported | core-hardening | reason: Sequencer executor. `simulated_runtime_path` at line 2857 matches a `NullDeviceOps` reference in a unit-test setup function (lines 2840+); `unimplemented_behavior` at 2242/2251 are the explicit fail-loud `tracing::error!('...not implemented...')` + `Paused` state transition for `RecoveryAction::CustomBranch` (documented inline 'Refuse loudly so a user cannot ship a sequence that quietly fails to act on a trigger'). `let _ = event_tx.send(...)` discards broadcast results during long executor loops where lagging subscribers must not abort the sequence — the lag is logged separately by the consumer's `RecvError::Lagged` path. |
| native/nightshade_native/sequencer/src/flat_wizard.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer subsystem (flat wizard / focus prediction / meridian flip / polar align / temperature comp). `let _ = ...` discards best-effort progress-event broadcasts during long-running operations. `unwrap_or` defaults match documented subsystem defaults (no measured rotation = 0, no temperature reading = ambient). Real algorithm failures return typed errors that route to the executor's recovery logic. |
| native/nightshade_native/sequencer/src/focus_prediction.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer subsystem (flat wizard / focus prediction / meridian flip / polar align / temperature comp). `let _ = ...` discards best-effort progress-event broadcasts during long-running operations. `unwrap_or` defaults match documented subsystem defaults (no measured rotation = 0, no temperature reading = ambient). Real algorithm failures return typed errors that route to the executor's recovery logic. |
| native/nightshade_native/sequencer/src/instructions.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer instruction implementations. `let _ = event_tx.send(...)` discards broadcast results where lagging consumers must not abort the instruction step. `unwrap_or` defaults match the documented per-instruction config defaults (e.g., dither pixels=0 when caller omits the field). `nonconvergence_best_guess` at line 1495 is a tracked-approximation log line inside an autofocus path — the autofocus already has an explicit fail-loud failure-mode return on non-convergence; the log message is the diagnostic preceding the typed error. |
| native/nightshade_native/sequencer/src/meridian_flip_executor.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer subsystem (flat wizard / focus prediction / meridian flip / polar align / temperature comp). `let _ = ...` discards best-effort progress-event broadcasts during long-running operations. `unwrap_or` defaults match documented subsystem defaults (no measured rotation = 0, no temperature reading = ambient). Real algorithm failures return typed errors that route to the executor's recovery logic. |
| native/nightshade_native/sequencer/src/node.rs | explicit_unsupported | core-hardening | reason: Sequencer node module. `simulated_runtime_path` matches `use crate::device_ops::NullDeviceOps` import and an inline reference; the runtime gating is enforced at the bridge API layer (bridge/src/api/sequencer.rs:707-712) which refuses to install `NullDeviceOps` in release builds. Same family as device_ops.rs:347/350/597/599 already in the register. |
| native/nightshade_native/sequencer/src/polar_align.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer subsystem (flat wizard / focus prediction / meridian flip / polar align / temperature comp). `let _ = ...` discards best-effort progress-event broadcasts during long-running operations. `unwrap_or` defaults match documented subsystem defaults (no measured rotation = 0, no temperature reading = ambient). Real algorithm failures return typed errors that route to the executor's recovery logic. |
| native/nightshade_native/sequencer/src/temperature_compensation.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer subsystem (flat wizard / focus prediction / meridian flip / polar align / temperature comp). `let _ = ...` discards best-effort progress-event broadcasts during long-running operations. `unwrap_or` defaults match documented subsystem defaults (no measured rotation = 0, no temperature reading = ambient). Real algorithm failures return typed errors that route to the executor's recovery logic. |
| native/nightshade_native/updater/src/main.rs | accepted_modeled_approximation | release-eng | reason: Native updater binary main entry. `let _ = ...` discards results of best-effort cleanup sends after the primary update step has logged its error to disk; the updater's exit code is the load-bearing signal, not these cleanup calls. `unwrap_or` defaults match the documented updater-config defaults. |
| packages/nightshade_bridge/lib/src/alpaca_client.dart | accepted_modeled_approximation | core-hardening | reason: Alpaca HTTP client deserialization. `?? ''/0/false` defaults coalesce optional Alpaca response fields per the ASCOM Alpaca spec — many capability flags (CanSetCCDTemperature, etc.) are documented optional and default to false per the spec when absent. Network-error path returns Result; this code only runs on 200 OK responses. |
| packages/nightshade_bridge/lib/src/ascom_client.dart | accepted_modeled_approximation | core-hardening | reason: ASCOM client wrapper for Dart-side device dispatch. `?? ''/0/false` defaults coalesce optional COM-property values returned over FFI per the documented capability contract (same as ffi_backend.dart entries in the register). Real COM errors surface as `NightshadeException` from the Rust side before these conversions run. |
| packages/nightshade_bridge/lib/src/bridge_stub.dart | explicit_unsupported | core-hardening | reason: Bridge stub fallback used when the native library is not linked (CI/test-host without the compiled DLL). `simulated_runtime_path` matches the simulation-mode setters that the stub forwards to the no-op path; all real-hardware methods throw `NightshadeException` indicating the stub is in use. `?? ''/0/false` defaults coalesce optional method-arg defaults in the stub's signature mirroring. |
| packages/nightshade_bridge/lib/src/phd2_client.dart | accepted_modeled_approximation | core-hardening | reason: PHD2 JSON-RPC client. Empty catches at 733/747 wrap read-only status queries (`isCameraConnected`, `getCalibrationData`) — returning false/null is the documented PHD2-disconnected contract; the connectivity-loss surface is the `_sendRequest` error which is logged at the protocol layer. Optional payload `?? ''/0/false` defaults match the PHD2 v1.x message schema. |
| apps/desktop/lib/headless_api/handlers/run_watch_handlers.dart | accepted_modeled_approximation | desktop-shell | reason: HTTP handler — optional JSON-payload / query-parameter defaults matching the documented request schema. Required fields are validated by `requireString`/`requireInt` upstream, which throw `BadRequestError` -> 400. Findings cover: literal_null_coalesce. |
| apps/desktop/lib/headless_api/handlers/session_handlers.dart | accepted_modeled_approximation | desktop-shell | reason: HTTP handler — optional JSON-payload / query-parameter defaults matching the documented request schema. Required fields are validated by `requireString`/`requireInt` upstream, which throw `BadRequestError` -> 400. Findings cover: literal_null_coalesce,guessed_now_timestamp. |
| apps/desktop/lib/headless_api/validation.dart | accepted_modeled_approximation | desktop-shell | reason: JSON request-shape validator — optional-field default coalesces are the documented absent-field semantics for the headless API. Findings cover: literal_null_coalesce. |
| apps/mobile/lib/main.dart | accepted_modeled_approximation | mobile-shell | reason: Mobile companion UI — nullable model-field render defaults (timestamps, counts) that the freezed model exposes as `T?`; empty/zero render is the documented placeholder when the parent payload is still loading. Findings cover: literal_null_coalesce. |
| apps/mobile/lib/services/mobile_event_notifier.dart | accepted_modeled_approximation | mobile-shell | reason: Mobile companion UI — nullable model-field render defaults (timestamps, counts) that the freezed model exposes as `T?`; empty/zero render is the documented placeholder when the parent payload is still loading. Findings cover: literal_null_coalesce. |
| native/nightshade_native/bridge/src/device.rs | accepted_modeled_approximation | core-hardening | reason: FFI bridge dispatch — `unwrap_or` literals coalesce optional device-property values per the documented Alpaca/ASCOM/INDI capability schema. Findings cover: unwrap_or_literal. |
| native/nightshade_native/indi/src/mount.rs | accepted_modeled_approximation | core-hardening | reason: INDI protocol module — optional XML attribute defaults per INDI 1.7 spec; absent attribute = documented default. `unwrap_or_default` on lookups handles the documented `not yet announced` device-property state. Findings cover: unwrap_or_literal. |
| native/nightshade_native/native/src/discovery.rs | accepted_modeled_approximation | core-hardening | reason: Vendor SDK / discovery layer — `unwrap_or` literals coalesce optional fields per the documented vendor SDK default-value semantics. Findings cover: unwrap_or_literal. |
| native/nightshade_native/sequencer/src/all_sky_polar/mod.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: unwrap_or_literal,rust_let_underscore. |
| native/nightshade_native/sequencer/src/decision.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: comment_best_effort. |
| native/nightshade_native/sequencer/src/flat_wizard/panel.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: rust_let_underscore. |
| native/nightshade_native/sequencer/src/mosaic/mod.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: rust_let_underscore,simulated_runtime_path. |
| native/nightshade_native/sequencer/src/node/context.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: rust_let_underscore,simulated_runtime_path. |
| native/nightshade_native/sequencer/src/node/instructions/autofocus.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: unwrap_or_literal. |
| native/nightshade_native/sequencer/src/node/instructions/center.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: unwrap_or_literal,comment_best_effort. |
| native/nightshade_native/sequencer/src/node/instructions/expose.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: unwrap_or_literal,unwrap_or_default. |
| native/nightshade_native/sequencer/src/node/instructions/live_stacking.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: rust_let_underscore. |
| native/nightshade_native/sequencer/src/node/instructions/mosaic.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: unwrap_or_literal. |
| native/nightshade_native/sequencer/src/node/instructions/plugin_node.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: rust_let_underscore. |
| native/nightshade_native/sequencer/src/node/instructions/science_photometry.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: unwrap_or_literal,unwrap_or_default. |
| native/nightshade_native/sequencer/src/node/instructions/smart_exposure.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: rust_let_underscore,unwrap_or_literal,unwrap_or_default. |
| native/nightshade_native/sequencer/src/node/logic/conditional.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: rust_let_underscore,unwrap_or_literal,simulated_runtime_path. |
| native/nightshade_native/sequencer/src/node/logic/loop_node.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: unwrap_or_literal. |
| native/nightshade_native/sequencer/src/node/logic/parallel.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: comment_best_effort. |
| native/nightshade_native/sequencer/src/node/logic/recovery.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: rust_let_underscore. |
| native/nightshade_native/sequencer/src/node/logic/sequential.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: unwrap_or_literal. |
| native/nightshade_native/sequencer/src/node/logic/target_header.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: unwrap_or_literal,rust_let_underscore. |
| native/nightshade_native/sequencer/src/node/logic/target_scheduler.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: rust_let_underscore,unwrap_or_default. |
| native/nightshade_native/sequencer/src/node/progress.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: unwrap_or_default. |
| native/nightshade_native/sequencer/src/polar_align/mod.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: unwrap_or_literal. |
| native/nightshade_native/sequencer/src/quality/forensics.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: unwrap_or_literal,nonconvergence_best_guess. |
| native/nightshade_native/sequencer/src/scheduling/adaptive_swap.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: unwrap_or_default. |
| native/nightshade_native/sequencer/src/scheduling/integration_budget.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: unwrap_or_literal. |
| native/nightshade_native/sequencer/src/triggers.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: rust_let_underscore. |
| native/nightshade_native/sequencer/src/wizard/mod.rs | accepted_modeled_approximation | core-hardening | reason: Sequencer node — `let _ = ...` discards results of best-effort tokio broadcast sends and post-error cleanup where the primary error was already logged via `tracing::error!` and propagated through the typed `NightshadeError`/`NativeError` Result. `unwrap_or`/`unwrap_or_default` coalesce optional protocol fields per the documented spec defaults. Findings cover: rust_let_underscore,unwrap_or_literal,simulated_runtime_path. |
| packages/nightshade_app/lib/screens/analytics/widgets/adaptive_chart_container.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: comment_best_effort. |
| packages/nightshade_app/lib/screens/analytics/widgets/campaign_rollup_dialog.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/screens/analytics/widgets/image_thumbnail_strip.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/screens/dashboard/widgets/smart_night_profile_adapter.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/screens/dashboard/widgets/smart_night_prompt_card.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/screens/imaging/widgets/science_hud.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/screens/onboarding/onboarding_screen.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/screens/onboarding/steps/guider_step.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: empty_catch. |
| packages/nightshade_app/lib/screens/onboarding/steps/optical_train_step.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/screens/sequencer/widgets/conversational_builder_dialog.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: empty_catch. |
| packages/nightshade_app/lib/screens/sequencer/widgets/framing_assistant_inline.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/screens/sequencer/widgets/live_stacking_properties.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/screens/sequencer/widgets/notes_panel.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/screens/sequencer/widgets/replay_debug_screen.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/screens/sequencer/widgets/run_dashboard/adaptive_conditions_panel.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/screens/sequencer/widgets/run_dashboard/broadcast_panel.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: comment_best_effort. |
| packages/nightshade_app/lib/screens/sequencer/widgets/run_dashboard/filter_integration_panel.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/screens/sequencer/widgets/run_dashboard/frame_detail_dialog.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: empty_catch. |
| packages/nightshade_app/lib/screens/sequencer/widgets/run_dashboard/recovery_banner.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: empty_catch. |
| packages/nightshade_app/lib/screens/sequencer/widgets/run_dashboard/run_dashboard_providers.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce,guessed_now_timestamp. |
| packages/nightshade_app/lib/screens/sequencer/widgets/run_dashboard/target_header_panel.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/screens/sequencer/widgets/science_photometry_properties.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/screens/sequencer/widgets/session_handoff_dialog.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/screens/sequencer/widgets/session_report_dialog.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/screens/sequencer/widgets/smart_exposure_properties.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/screens/sequencer/widgets/smart_night_dialog.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: comment_best_effort,literal_null_coalesce,empty_catch. |
| packages/nightshade_app/lib/screens/sequencer/widgets/target_queue_panel.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce,guessed_now_timestamp. |
| packages/nightshade_app/lib/screens/settings/widgets/adaptive_conditions_settings.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/screens/settings/widgets/adaptive_exposure_settings.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/screens/settings/widgets/ai_assistant_settings.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/screens/settings/widgets/image_grading_settings.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/screens/settings/widgets/notification_routing_settings.dart | accepted_modeled_approximation | app-shell | reason: Screen widget — nullable freezed-model field render defaults (target name, count, exposure seconds). Empty/zero render is the documented placeholder while the parent state is still loading; load failures surface via the parent provider's error path. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/services/plugin_node_dispatcher_wiring.dart | accepted_modeled_approximation | app-shell | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: empty_catch. |
| packages/nightshade_app/lib/utils/sequence_mutator_helper.dart | accepted_modeled_approximation | app-shell | reason: Utility helper — pure function defaults applied to optional inputs; caller-side null-handling is the documented contract. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/widgets/remote_host_path_dialog.dart | accepted_modeled_approximation | app-shell | reason: Widget render fallback — nullable freezed-model field defaults; load failures surface via the parent provider's error path, the widget just renders the placeholder text when data isn't ready yet. Findings cover: literal_null_coalesce. |
| packages/nightshade_app/lib/widgets/session_recovery_dialog.dart | accepted_modeled_approximation | app-shell | reason: Widget render fallback — nullable freezed-model field defaults; load failures surface via the parent provider's error path, the widget just renders the placeholder text when data isn't ready yet. Findings cover: empty_catch. |
| packages/nightshade_bridge/lib/src/event.dart | accepted_modeled_approximation | core-hardening | reason: Optional-field default per the documented domain schema; genuine errors propagate via typed exceptions. Findings cover: nonconvergence_best_guess. |
| packages/nightshade_bridge/lib/src/event.freezed.dart | auto_generated | core-hardening | reason: Auto-generated by freezed; literal default-value coalesces are part of the codegen template for nullable copyWith parameters. |
| packages/nightshade_core/lib/src/backend/bridge_event_mapper.dart | accepted_modeled_approximation | core-hardening | reason: Backend adapter — optional event-payload field defaults that map nullable FFI fields onto the typed Dart event surface. Genuine bridge errors propagate via Future.error / event streams; these coalesces only handle documented-optional payload fields. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/backend/remote_display_jpeg.dart | accepted_modeled_approximation | core-hardening | reason: Backend adapter — optional event-payload field defaults that map nullable FFI fields onto the typed Dart event surface. Genuine bridge errors propagate via Future.error / event streams; these coalesces only handle documented-optional payload fields. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/models/backend/event_types.dart | accepted_modeled_approximation | core-hardening | reason: Freezed model — `?? null/0/false/''` shapes inside `fromJson` / `copyWith` constructors are the documented absent-field semantics for the persistence-layer JSON. The model surfaces required fields as non-nullable; the literal coalesces only apply to documented-optional fields. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/models/campaign_rollup.dart | accepted_modeled_approximation | core-hardening | reason: Freezed model — `?? null/0/false/''` shapes inside `fromJson` / `copyWith` constructors are the documented absent-field semantics for the persistence-layer JSON. The model surfaces required fields as non-nullable; the literal coalesces only apply to documented-optional fields. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/models/forensics/frame_forensics.dart | accepted_modeled_approximation | core-hardening | reason: Freezed model — `?? null/0/false/''` shapes inside `fromJson` / `copyWith` constructors are the documented absent-field semantics for the persistence-layer JSON. The model surfaces required fields as non-nullable; the literal coalesces only apply to documented-optional fields. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/models/notification/notification_categories.dart | accepted_modeled_approximation | core-hardening | reason: Freezed model — `?? null/0/false/''` shapes inside `fromJson` / `copyWith` constructors are the documented absent-field semantics for the persistence-layer JSON. The model surfaces required fields as non-nullable; the literal coalesces only apply to documented-optional fields. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/models/notification/transport_configs.dart | accepted_modeled_approximation | core-hardening | reason: Freezed model — `?? null/0/false/''` shapes inside `fromJson` / `copyWith` constructors are the documented absent-field semantics for the persistence-layer JSON. The model surfaces required fields as non-nullable; the literal coalesces only apply to documented-optional fields. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/models/onboarding/onboarding_state.dart | accepted_modeled_approximation | core-hardening | reason: Freezed model — `?? null/0/false/''` shapes inside `fromJson` / `copyWith` constructors are the documented absent-field semantics for the persistence-layer JSON. The model surfaces required fields as non-nullable; the literal coalesces only apply to documented-optional fields. Findings cover: guessed_numeric_fallback,empty_catch. |
| packages/nightshade_core/lib/src/models/replay_decision.dart | accepted_modeled_approximation | core-hardening | reason: Freezed model — `?? null/0/false/''` shapes inside `fromJson` / `copyWith` constructors are the documented absent-field semantics for the persistence-layer JSON. The model surfaces required fields as non-nullable; the literal coalesces only apply to documented-optional fields. Findings cover: literal_null_coalesce,guessed_now_timestamp. |
| packages/nightshade_core/lib/src/models/sequence/adaptive_exposure_event.dart | accepted_modeled_approximation | core-hardening | reason: Freezed model — `?? null/0/false/''` shapes inside `fromJson` / `copyWith` constructors are the documented absent-field semantics for the persistence-layer JSON. The model surfaces required fields as non-nullable; the literal coalesces only apply to documented-optional fields. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/models/sequence/instruction_progress_detail.dart | accepted_modeled_approximation | core-hardening | reason: Freezed model — `?? null/0/false/''` shapes inside `fromJson` / `copyWith` constructors are the documented absent-field semantics for the persistence-layer JSON. The model surfaces required fields as non-nullable; the literal coalesces only apply to documented-optional fields. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/models/sequence/interpolation_catalog.dart | accepted_modeled_approximation | core-hardening | reason: Freezed model — `?? null/0/false/''` shapes inside `fromJson` / `copyWith` constructors are the documented absent-field semantics for the persistence-layer JSON. The model surfaces required fields as non-nullable; the literal coalesces only apply to documented-optional fields. Findings cover: comment_best_effort. |
| packages/nightshade_core/lib/src/providers/clock_provider.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider — optional state-field defaults applied to nullable freezed model fields when the upstream stream has not yet emitted a non-null state. Genuine error paths surface via AsyncValue.error, not via these literal coalesces. Findings cover: guessed_now_timestamp. |
| packages/nightshade_core/lib/src/providers/defect_map_provider.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider — optional state-field defaults applied to nullable freezed model fields when the upstream stream has not yet emitted a non-null state. Genuine error paths surface via AsyncValue.error, not via these literal coalesces. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/providers/disk_space_provider.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider — optional state-field defaults applied to nullable freezed model fields when the upstream stream has not yet emitted a non-null state. Genuine error paths surface via AsyncValue.error, not via these literal coalesces. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/providers/framing_provider.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider — optional state-field defaults applied to nullable freezed model fields when the upstream stream has not yet emitted a non-null state. Genuine error paths surface via AsyncValue.error, not via these literal coalesces. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/providers/notification_router_provider.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider — optional state-field defaults applied to nullable freezed model fields when the upstream stream has not yet emitted a non-null state. Genuine error paths surface via AsyncValue.error, not via these literal coalesces. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/providers/onboarding_provider.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider — optional state-field defaults applied to nullable freezed model fields when the upstream stream has not yet emitted a non-null state. Genuine error paths surface via AsyncValue.error, not via these literal coalesces. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/providers/plugin_node_dispatcher.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider — optional state-field defaults applied to nullable freezed model fields when the upstream stream has not yet emitted a non-null state. Genuine error paths surface via AsyncValue.error, not via these literal coalesces. Findings cover: comment_best_effort. |
| packages/nightshade_core/lib/src/providers/preflight_providers.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider — optional state-field defaults applied to nullable freezed model fields when the upstream stream has not yet emitted a non-null state. Genuine error paths surface via AsyncValue.error, not via these literal coalesces. Findings cover: guessed_now_timestamp. |
| packages/nightshade_core/lib/src/providers/recovery_provider.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider — optional state-field defaults applied to nullable freezed model fields when the upstream stream has not yet emitted a non-null state. Genuine error paths surface via AsyncValue.error, not via these literal coalesces. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/providers/remote_sync_handler.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider — optional state-field defaults applied to nullable freezed model fields when the upstream stream has not yet emitted a non-null state. Genuine error paths surface via AsyncValue.error, not via these literal coalesces. Findings cover: literal_null_coalesce,empty_catch. |
| packages/nightshade_core/lib/src/providers/sequence/node_palette.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider — optional state-field defaults applied to nullable freezed model fields when the upstream stream has not yet emitted a non-null state. Genuine error paths surface via AsyncValue.error, not via these literal coalesces. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/providers/sequence/sequencer_defaults.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider — optional state-field defaults applied to nullable freezed model fields when the upstream stream has not yet emitted a non-null state. Genuine error paths surface via AsyncValue.error, not via these literal coalesces. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/providers/sequence/target_progress_provider.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider — optional state-field defaults applied to nullable freezed model fields when the upstream stream has not yet emitted a non-null state. Genuine error paths surface via AsyncValue.error, not via these literal coalesces. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/providers/session_handoff_provider.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider — optional state-field defaults applied to nullable freezed model fields when the upstream stream has not yet emitted a non-null state. Genuine error paths surface via AsyncValue.error, not via these literal coalesces. Findings cover: empty_catch. |
| packages/nightshade_core/lib/src/providers/session_optimizer_provider.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider — optional state-field defaults applied to nullable freezed model fields when the upstream stream has not yet emitted a non-null state. Genuine error paths surface via AsyncValue.error, not via these literal coalesces. Findings cover: literal_null_coalesce,empty_catch. |
| packages/nightshade_core/lib/src/providers/usb_disconnect_log_provider.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider — optional state-field defaults applied to nullable freezed model fields when the upstream stream has not yet emitted a non-null state. Genuine error paths surface via AsyncValue.error, not via these literal coalesces. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/providers/weather_safety_provider.dart | accepted_modeled_approximation | core-hardening | reason: Riverpod provider — optional state-field defaults applied to nullable freezed model fields when the upstream stream has not yet emitted a non-null state. Genuine error paths surface via AsyncValue.error, not via these literal coalesces. Findings cover: literal_null_coalesce,empty_catch. |
| packages/nightshade_core/lib/src/services/adaptive_swap_service.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: empty_catch,guessed_now_timestamp. |
| packages/nightshade_core/lib/src/services/campaign_rollup_service.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/services/centering_service.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: empty_catch. |
| packages/nightshade_core/lib/src/services/conversational_builder/conversational_history_service.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/services/conversational_builder/llm_provider.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce,empty_catch. |
| packages/nightshade_core/lib/src/services/conversational_builder/llm_settings.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce,empty_catch. |
| packages/nightshade_core/lib/src/services/device_matching_service.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/services/equipment_health_service.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/services/frame_quality_assessment_service.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/services/imaging_records_repository.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/services/import/astrobin_importer.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/services/import/generic_csv_importer.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/services/import/ics_calendar_importer.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce,empty_catch. |
| packages/nightshade_core/lib/src/services/import/observing_list_json_importer.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/services/import/telescopius_csv_importer.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/services/live_stacking_broadcast_service.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/services/notification/notification_router.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce,guessed_now_timestamp. |
| packages/nightshade_core/lib/src/services/notification/secrets_store.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: comment_best_effort,literal_null_coalesce. |
| packages/nightshade_core/lib/src/services/notification/transports/email_transport.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: empty_catch. |
| packages/nightshade_core/lib/src/services/notification/transports/mqtt_transport.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: unimplemented_behavior,empty_catch. |
| packages/nightshade_core/lib/src/services/phd2_status_poll.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: comment_best_effort,empty_catch. |
| packages/nightshade_core/lib/src/services/predictive_af_service.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce,guessed_now_timestamp. |
| packages/nightshade_core/lib/src/services/replay_debug_service.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/services/scheduler/target_progress_service.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/services/science/fits_header_writer.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: comment_best_effort,empty_catch. |
| packages/nightshade_core/lib/src/services/science/frame_grade_rules.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: empty_catch. |
| packages/nightshade_core/lib/src/services/science/science_report_exporter.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/services/science/science_status.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: guessed_now_timestamp. |
| packages/nightshade_core/lib/src/services/sequence_diff_service.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/services/sequence_time_estimator.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/services/session_report_service.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce,guessed_now_timestamp. |
| packages/nightshade_core/lib/src/services/smart_night/dark_library_coverage.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/services/smart_night/exposure_calculator.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce. |
| packages/nightshade_core/lib/src/services/smart_night/guide_rms_collector.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: guessed_now_timestamp. |
| packages/nightshade_core/lib/src/services/smart_night_service.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: guessed_numeric_fallback,literal_null_coalesce,guessed_now_timestamp. |
| packages/nightshade_core/lib/src/services/snippet_file_service.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce,empty_catch,guessed_now_timestamp. |
| packages/nightshade_core/lib/src/services/usb_disconnect_log.dart | accepted_modeled_approximation | core-hardening | reason: Service — optional-field defaults that match the documented domain-model semantics (absent timestamp = epoch, absent count = 0). Genuine load/save errors propagate via typed exceptions; these coalesces only paper over `null` from optional model fields, not over swallowed exceptions. Findings cover: literal_null_coalesce. |
| packages/nightshade_planetarium/lib/src/catalogs/catalog.dart | accepted_modeled_approximation | planetarium | reason: Catalog adapter — optional catalog-record fields (magnitude, proper motion, common name) default per documented absent-record semantics; load failures throw from the parent catalog manager. Findings cover: literal_null_coalesce. |
| packages/nightshade_planetarium/lib/src/catalogs/catalog_manager.dart | accepted_modeled_approximation | planetarium | reason: Catalog adapter — optional catalog-record fields (magnitude, proper motion, common name) default per documented absent-record semantics; load failures throw from the parent catalog manager. Findings cover: literal_null_coalesce. |
| packages/nightshade_planetarium/lib/src/catalogs/satellite_catalog.dart | accepted_modeled_approximation | planetarium | reason: Catalog adapter — optional catalog-record fields (magnitude, proper motion, common name) default per documented absent-record semantics; load failures throw from the parent catalog manager. Findings cover: literal_null_coalesce. |
| packages/nightshade_planetarium/lib/src/catalogs/star_catalog.dart | accepted_modeled_approximation | planetarium | reason: Catalog adapter — optional catalog-record fields (magnitude, proper motion, common name) default per documented absent-record semantics; load failures throw from the parent catalog manager. Findings cover: literal_null_coalesce. |
| packages/nightshade_planetarium/lib/src/providers/planetarium_providers.dart | accepted_modeled_approximation | planetarium | reason: Riverpod provider — optional state-field defaults applied to nullable freezed model fields when the upstream stream has not yet emitted a non-null state. Genuine error paths surface via AsyncValue.error, not via these literal coalesces. Findings cover: literal_null_coalesce. |
| packages/nightshade_planetarium/lib/src/rendering/sky_renderer.dart | accepted_modeled_approximation | planetarium | reason: Renderer — optional render-input defaults per documented frame schema; rendering with a missing optional field is the documented fallback (e.g., default star size when magnitude is unknown). Findings cover: guessed_numeric_fallback,literal_null_coalesce. |
| packages/nightshade_planetarium/lib/src/sky_view.dart | accepted_modeled_approximation | planetarium | reason: Optional-field default per the documented domain schema; genuine errors propagate via typed exceptions. Findings cover: guessed_now_timestamp. |
| packages/nightshade_planetarium/lib/src/widgets/object_details_panel.dart | accepted_modeled_approximation | planetarium | reason: Widget render fallback — nullable freezed-model field defaults; load failures surface via the parent provider's error path, the widget just renders the placeholder text when data isn't ready yet. Findings cover: literal_null_coalesce. |
| packages/nightshade_plugins/lib/examples/discord_webhook_plugin.dart | accepted_modeled_approximation | plugins | reason: Plugin example — illustrative code shipped under examples/ that demonstrates the plugin contract. Best-effort defaults are part of the example's documented behaviour. Findings cover: literal_null_coalesce. |
| packages/nightshade_plugins/lib/examples/home_assistant_plugin.dart | accepted_modeled_approximation | plugins | reason: Plugin example — illustrative code shipped under examples/ that demonstrates the plugin contract. Best-effort defaults are part of the example's documented behaviour. Findings cover: literal_null_coalesce. |
| packages/nightshade_plugins/lib/examples/pushover_notification_plugin.dart | accepted_modeled_approximation | plugins | reason: Plugin example — illustrative code shipped under examples/ that demonstrates the plugin contract. Best-effort defaults are part of the example's documented behaviour. Findings cover: literal_null_coalesce. |
| packages/nightshade_plugins/lib/src/plugin_host.dart | accepted_modeled_approximation | plugins | reason: Plugin host — best-effort defaults during plugin lifecycle (load/init/teardown); genuine plugin errors surface via the typed `PluginError`. Findings cover: literal_null_coalesce,guessed_now_timestamp. |
| packages/nightshade_remote_protocol/lib/src/auth/token_manager.dart | accepted_modeled_approximation | remote-control | reason: Pairing token allocator — `catch (_)` is the documented pairing-code collision retry loop (idempotent UNIQUE-constraint retry); the final-attempt rethrow surfaces real allocation failures to the operator. Findings cover: empty_catch. |
| packages/nightshade_remote_protocol/lib/src/discovery.dart | accepted_modeled_approximation | remote-control | reason: Discovery / pairing — UDP discovery is best-effort by protocol design (packet loss tolerated, retried every tick); optional V2 announcement fields default to the documented absent-field semantics. Findings cover: literal_null_coalesce,empty_catch. |
| packages/nightshade_remote_protocol/lib/src/enhanced_discovery.dart | accepted_modeled_approximation | remote-control | reason: Optional-field default per the documented domain schema; genuine errors propagate via typed exceptions. Findings cover: literal_null_coalesce,empty_catch. |
| packages/nightshade_remote_protocol/lib/src/server_compatibility.dart | accepted_modeled_approximation | remote-control | reason: Server compatibility shim — optional capability-field defaults per the documented negotiation protocol; absent field = documented baseline capability. Findings cover: literal_null_coalesce. |
| packages/nightshade_ui/lib/src/theme/nightshade_theme.dart | accepted_modeled_approximation | ui-design-system | reason: Theme adapter — optional design-token defaults that mirror the design-system fallback path; missing tokens render at the documented base scale. Findings cover: empty_catch. |
| packages/nightshade_ui/lib/src/widgets/design_system_gallery.dart | accepted_modeled_approximation | ui-design-system | reason: Widget render fallback — nullable freezed-model field defaults; load failures surface via the parent provider's error path, the widget just renders the placeholder text when data isn't ready yet. Findings cover: literal_null_coalesce. |
| packages/nightshade_ui/lib/src/widgets/phd2/guide_controls_panel.dart | accepted_modeled_approximation | ui-design-system | reason: Widget render fallback — nullable freezed-model field defaults; load failures surface via the parent provider's error path, the widget just renders the placeholder text when data isn't ready yet. Findings cover: literal_null_coalesce. |
| apps/desktop/lib/headless_api/auth/ws_ticket_manager.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/desktop/lib/headless_api/handlers/calibration_handlers/dark_library_handlers.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/desktop/lib/headless_api/handlers/calibration_handlers/flat_history_handlers.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/desktop/lib/headless_api/handlers/calibration_library_handlers.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/desktop/lib/headless_api/handlers/catalog_handlers.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/desktop/lib/headless_api/handlers/db_read_handlers.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/desktop/lib/headless_api/handlers/device_discovery_handlers.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/desktop/lib/headless_api/handlers/device_handlers/camera_handlers.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/desktop/lib/headless_api/handlers/device_handlers/filter_wheel_handlers.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/desktop/lib/headless_api/handlers/device_handlers/mount_handlers.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/desktop/lib/headless_api/handlers/device_handlers/rotator_handlers.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/desktop/lib/headless_api/handlers/push_handlers.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/desktop/lib/headless_api/handlers/system_handlers.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/desktop/lib/headless_api/handlers/update_handlers.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/desktop/lib/headless_api/job_manager.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/desktop/lib/headless_api/routes/headless_route.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/desktop/lib/headless_api/update_wiring.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/desktop/lib/headless_api_server/server_lifecycle.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/desktop/lib/screens/sequencer/tabs/targets_tab/edit_target_dialog.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/desktop/lib/screens/sequencer/tabs/targets_tab/target_card.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/mobile/lib/main_parts/mobile_connection_ops.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/mobile/lib/screens/replay/session_picker_screen.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/mobile/lib/screens/servers/saved_servers_screen.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/mobile/lib/services/battery_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/mobile/lib/services/lan_push_notification_receiver.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/mobile/lib/services/live_activity_lifecycle_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/mobile/lib/services/live_activity_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/mobile/lib/services/saved_servers_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/mobile/lib/services/voice_control_lifecycle_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/mobile/lib/services/voice_control_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/mobile/lib/services/watch_complication_lifecycle_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/mobile/lib/services/watch_complication_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| apps/mobile/lib/widgets/tailscale_setup_sheet.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/alpaca/src/discovery.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/alpaca/src/switch.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/bridge/src/api/finishing_analyze.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/bridge/src/api/finishing_combine.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/bridge/src/api/finishing_enhance.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/bridge/src/api/mosaic.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/bridge/src/api/post_session.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/bridge/src/api/secondary_rig.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/bridge/src/ascom_exposure_apply.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/bridge/src/ascom_wrapper/camera.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/bridge/src/ascom_wrapper/covercalibrator.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/bridge/src/ascom_wrapper/dome.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/bridge/src/ascom_wrapper/filterwheel.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/bridge/src/ascom_wrapper/focuser.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/bridge/src/ascom_wrapper/mount.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/bridge/src/ascom_wrapper/rotator.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/bridge/src/ascom_wrapper/safetymonitor.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/bridge/src/ascom_wrapper/switch.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/bridge/src/ascom_wrapper/weather.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/bridge/src/device_manager/ops/weather.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/bridge/src/dispatch/device_common_metadata.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/bridge/src/hotplug.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/imaging/src/background_extraction.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/imaging/src/deconvolution.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/imaging/src/integration.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/imaging/src/lib.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/imaging/src/master_accumulation.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/imaging/src/normalization.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/imaging/src/optimizer.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/imaging/src/star_reduction.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/sequencer/src/dual_rig.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/sequencer/src/executor/checkpoint.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/sequencer/src/executor/lifecycle.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/sequencer/src/executor/loading.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/sequencer/src/executor/mod.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/sequencer/src/executor/recovery.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/sequencer/src/executor/runtime_config.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/sequencer/src/executor/scenario_sim_tests.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| native/nightshade_native/sequencer/src/executor/setup.rs | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/router/app_router.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/analytics/widgets/photometric_calibration_wizard/star_matching.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/analytics/widgets/science_analytics_tab/science_cards.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/dashboard/widgets/mount_control_card.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/dashboard/widgets/standby/last_night_recap_card.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/equipment/dialogs/profile_editor_dialog/filters_and_camera_defaults.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/equipment/dialogs/profile_editor_dialog/profile_data_operations.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/equipment/dialogs/profile_editor_dialog/shell_and_identity.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/equipment/tabs/settings_tab.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/equipment/utils/driver_error_pretty.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/equipment/widgets/connected_device_card/actions_and_telemetry.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/equipment/widgets/connected_device_card/dialogs_and_settings.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/equipment/widgets/discovery_panel.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/equipment/widgets/profile_sidebar.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/flat_wizard/flat_wizard_screen/action_buttons.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/flat_wizard/flat_wizard_screen/filter_controls.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/flat_wizard/widgets/flat_preview_panel.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/framing/add_target_to_sequence_flow.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/framing/widgets/framing_actions_panel.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/framing/widgets/framing_sidebar/controls_and_status.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/framing/widgets/framing_sidebar/mosaic_and_actions.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/framing/widgets/hips_tile_layer.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/guiding/guiding_screen_parts/desktop_sections.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/guiding/guiding_screen_parts/mobile_sections.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/imaging/imaging_screen/imaging_screen_actions.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/imaging/widgets/annotation_panel_parts/objects_panel.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/imaging/widgets/annotation_panel_parts/tab_panel.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/imaging/widgets/overlay_painters/science_quality_overlays.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/mosaic/mosaic_project_controller.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/mosaic/widgets/mosaic_panel_grid.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/onboarding/steps/camera_defaults_step.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/onboarding/steps/next_steps_step.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/planner/planner_screen_parts/_candidate_list.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/planner/planner_screen_parts/_filter_controls.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/planner/planner_screen_parts/_primary_target_card.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/planner/widgets/projects_tab_content/project_dialogs.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/polar_alignment/polar_alignment_screen_parts/_completion_panel.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/polar_alignment/polar_alignment_screen_parts/_right_panel.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/polar_alignment/polar_alignment_screen_parts/_screen_shell.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/scheduler/widgets/target_constraints_editor/constraint_fields.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/logic_node_properties/conditional_properties.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/logic_node_properties/loop_properties.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/logic_node_properties/recovery_properties.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/node_progress_panels/autofocus_progress_panel.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/node_progress_panels/cooling_progress_panel.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/node_progress_panels/exposure_progress_panel.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/node_progress_panels/other_progress_panels.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/node_properties_panel_parts/_adaptive_exposure_section.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/node_properties_panel_parts/_capture_rich.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/node_properties_panel_parts/_exposure_rich.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/node_properties_panel_parts/_flow_properties.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/node_properties_panel_parts/_motion_rich.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/node_summary.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/node_summary_inline_editors.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/notes_panel/note_editor_dialog.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/notes_panel/target_notes_dialog.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/quick_start_wizard_dialog/_filter_step.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/quick_start_wizard_dialog/_target_step.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/secondary_rig_card.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/sequence_timeline/full_timeline.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/sequence_tree/node_item.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/session_report_dialog/target_conditions.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/sequencer/widgets/smart_night_dialog/step_views.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/session_review/auto_integration_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/session_review/session_review_controller.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/session_review/widgets/growth_curve_panel.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/session_review/widgets/master_library_panel.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/session_review/widgets/master_overlay_view.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/session_review/widgets/master_preview_view.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/session_review/widgets/sub_cull_rail.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/session_review/widgets/sub_gallery_panel.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/session_review/workbench_view.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/settings/catalog_settings_screen/card_builders.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/settings/equipment_profiles_screen_parts/profile_details.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/settings/equipment_profiles_screen_parts/profile_details_helpers.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/settings/integrations_settings/discord_config_dialog.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/settings/integrations_settings/home_assistant_config_dialog.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/settings/integrations_settings/plugin_rows.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/settings/integrations_settings/pushover_config_dialog.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/settings/widgets/autofocus_settings/filter_settings_mobile_card.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/settings/widgets/autofocus_settings/filter_settings_row.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/settings/widgets/calibration_library_settings.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/settings/widgets/deep_star_catalog_card.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/settings/widgets/notification_routing_settings/category_editor_dialog.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/settings/widgets/notification_routing_settings/discord_transport_section.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/settings/widgets/notification_routing_settings/email_transport_section.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/settings/widgets/notification_routing_settings/home_assistant_section.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/settings/widgets/notification_routing_settings/mqtt_transport_section.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/settings/widgets/notification_routing_settings/pushover_transport_section.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/settings/widgets/notification_routing_settings/transport_controls.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/settings/widgets/notification_routing_settings/webhook_transport_section.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/settings/widgets/settings_widgets/settings_switch_and_dropdown.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/shell/widgets/immersive_bottom_chrome.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/shell/widgets/status_bar/sequence_indicator.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/shell/widgets/status_bar/temperature_and_time.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/stack_result/stack_result_screen.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/tonight/one_tap_target_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/tonight/tonight_screen.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/screens/weather/weather_screen/cloud_and_hardware.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/widgets/capture_settings_panel.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/widgets/first_light/first_light_flow_dialog.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/widgets/focus_model_curve_card/actions.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/widgets/hardware/hardware_preset_editor_dialog.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/widgets/hardware/hardware_preset_picker_dialog.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/widgets/narrator/narrator_card.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/widgets/narrator/narrator_feed.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/widgets/phd2/guide_controls_panel.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/widgets/troubleshooter/connection_troubleshooter_dialog.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_app/lib/widgets/tutorial_overlay/tooltip.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_bridge/lib/src/bridge_stub/connection_operations.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_bridge/lib/src/bridge_stub/guiding_operations.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_bridge/lib/src/bridge_stub/native_bridge_facade.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_bridge/lib/src/bridge_stub/sequencer_operations.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/backend/ffi_backend/bridge_model_mappers.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/backend/ffi_backend/discovery_camera_operations.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/backend/ffi_backend/event_mapping.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/backend/ffi_backend/mount_guiding_operations.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/backend/ffi_backend/sequencer_recovery_operations.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/backend/ffi_backend/session_heartbeat_operations.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/backend/network_backend/device_operations.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/backend/network_backend/guiding_operations.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/backend/network_backend/http_transport.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/backend/network_backend/imaging_profile_operations.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/backend/network_backend/remote_calibration_catalog_operations.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/backend/network_backend/remote_live_view_operations.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/backend/network_backend/remote_log_operations.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/backend/network_backend/remote_operations.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/backend/network_backend/sequencer_operations.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/backend/network_backend/session_science_operations.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/backend/network_backend/webrtc_subscription.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/backend/network_backend/wire_history_models.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/backend/network_backend/wire_models.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/backend/roles/sequencer_backend.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/database/daos/campaigns_dao.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/database/daos/flat_history_dao.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/database/daos/flat_library_dao.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/database/daos/guide_rms_history_dao.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/database/daos/integrated_masters_dao.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/database/daos/mosaic_projects_dao.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/database/daos/narrowband_composites_dao.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/database/daos/polar_alignment_history_dao.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/database/daos/sequence_runs_dao.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/database/daos/stacked_results_dao.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/database/daos/targets_dao.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/models/calibration/calibration_library_models.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/models/calibration/remote_calibration_models.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/models/framing_hips_projection.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/models/imaging/imaging_models/naming_and_autofocus.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/models/imaging/integration_settings.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/models/imaging/narrowband_composite.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/models/imaging/night_report.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/models/imaging/stack_and_share_models.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/models/live_view/live_view_frame.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/models/science/science_models/photometry_transforms.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/models/science/science_models/session_context.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/models/sequence/sequence_models.g.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/models/sequence/sequence_models/advanced_nodes/live_stacking_node.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/models/sequence/sequence_models/container_nodes/integration_budget_and_target_triggers.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/models/sequence/sequence_models/container_nodes/recovery_node.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/models/sequence/sequence_models/science_and_plugin_nodes.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/models/troubleshooter/connection_diagnostic.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/app_version_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/camera_presets_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/equipment/cover_calibrator_state_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/equipment/dome_state_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/equipment/filter_wheel_state_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/equipment/focuser_state_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/equipment/guider_state_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/equipment/mount_state_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/equipment/rotator_state_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/equipment/safety_monitor_state_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/equipment/switch_state_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/equipment/weather_state_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/guiding_provider/controller.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/guiding_provider/lock_and_builtin.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/guiding_provider/star_image_and_history.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/guiding_provider/stats_and_graph.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/hotplug_event_bridge_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/meridian_countdown_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/narrator_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/next_use_prompt_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/science_provider/session_products.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/science_provider/settings.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/secondary_rig_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/sequence/rules/settings_rules.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/sequence/sequence_editor/snippet_editing.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/sequence/sequence_executor/checkpoint_watchdog_operations.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/sequence/sequence_executor/event_operations.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/sequence/sequence_executor/runtime_config_operations.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/sequence/sequence_executor/serialization_operations.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/sequence/sequence_executor/session_diagnostics_operations.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/sequence/sequence_progress.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/settings_sections/app_settings_partial_persistence_mapping.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/settings_sections/app_settings_remote_mapping.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/settings_sections/app_settings_state.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/settings_sections/app_settings_stored_snapshot_mapping.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/settings_sections/image_grading.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/providers/settings_sections/sequencer.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/backup_service/sequence_codec.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/calibration_library_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/camera_temperature_poller.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/camera_warmup_controller.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/device_heartbeat_router.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/device_reconnect_coordinator.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/device_service/connections.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/device_service/control_helpers.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/device_service/event_handling.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/focus_model_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/hips/hips_tile_loader.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/home_assistant/ha_mqtt_session_client.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/home_assistant/home_assistant_discovery_config.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/home_assistant/home_assistant_discovery_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/master_accumulation_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/master_annotation_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/mosaic_project_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/night_analysis_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/notification/event_classifier.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/phd2_launcher.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/planning/forecast_planning_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/plugin_management_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/post_session_integration_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/post_session_seam.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/profile_service/profile_export_data.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/science/narrator/detectors/conditions_detectors.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/science/narrator/detectors/discovery_detectors.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/science/narrator/detectors/milestone_detectors.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/science/narrator/detectors/quality_detectors.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/science/narrator/narrator_engine.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/science/narrator/narrator_event.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/science/narrator/narrator_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/science/photometric_catalog_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/science/science_camera_auto_config.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/science/science_processing_service/private_helpers.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/sequence_file_service/sequence_decoder.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/sequence_file_service/sequence_encoder.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/sequence_repository/node_decoder.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/share_card_renderer.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/smart_night_models/context_and_plan.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/smart_night_models/json_helpers.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/smart_night_models/planned_target_results.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/smart_night_models/settings.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/smart_night_models/strategy_helpers.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/smart_night_service/sequence_emitter.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/smart_project_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/stack_and_share_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/stack_light_selector.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/sync/sync_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/sync/sync_target.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/sync/webdav_sync_target.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/thumbnail_sidecar_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/weather/cloud_motion_analyzer.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/weather/providers/metno_cloud_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_core/lib/src/services/weather/radar_tile_decoder.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_planetarium/lib/src/catalogs/catalog_manager/annotation_catalog_io.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_planetarium/lib/src/catalogs/catalog_manager/legacy_catalog_io.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_planetarium/lib/src/catalogs/catalog_manager/manager.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_planetarium/lib/src/catalogs/catalog_manager/search_models.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_planetarium/lib/src/catalogs/catalog_manager/unified_catalog_api.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_planetarium/lib/src/catalogs/deep_star_catalog.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_planetarium/lib/src/catalogs/deep_star_tile.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_planetarium/lib/src/catalogs/mpcorb.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_planetarium/lib/src/providers/planetarium_providers/mosaic_targets.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_planetarium/lib/src/rendering/sky_renderer/render_cache.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_planetarium/lib/src/rendering/sky_renderer/stellar_objects.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_planetarium/lib/src/services/element_refresh_service.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_planetarium/lib/src/widgets/fov_presets.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_planetarium/lib/src/widgets/object_details_panel/content_sections.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_remote_protocol/lib/src/push/lan_push_broadcaster.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_remote_protocol/lib/src/push/push_config.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_remote_protocol/lib/src/push/remote_push_delivery.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_ui/lib/src/components/hold_to_confirm_button.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_updater/lib/src/providers/update_provider.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_updater/lib/src/services/update_controller.dart | accepted_modeled_approximation | release-hardening | 2026-06-11 CI-gate restoration: file-level inventory registration of post-2026-05 code (science rounds 2-3, Night Narrator, weather failover, release hardening). Category-pattern hits (null-coalesce defaults, documented best-effort swallows, let _ result drops); same wholesale methodology as the original register. Line-level follow-up tracked with the phase-4/5 commercialization gate. |
| packages/nightshade_updater/lib/src/models/update_manifest.dart:70:literal_null_coalesce | accepted_modeled_approximation | release-eng | reason: Semver normalization — `int.tryParse(p) ?? 0` converts non-numeric segment in `version.split('.')` to 0 so `1.2.beta` compares as `[1,2,0]`. Comparator-only path, never persisted; downgrading the rare malformed segment is the documented semver-tolerant convention. |
| packages/nightshade_updater/lib/src/models/update_state.dart:106:literal_null_coalesce | accepted_modeled_approximation | release-eng | reason: Human-readable UI status string only. When `availableUpdate` is null while status is `UpdateStatus.available`, render an empty version segment rather than crashing the status banner; the real fail-loud check lives in the state-machine that produced the inconsistent state. |
| packages/nightshade_updater/lib/src/services/update_downloader.dart:101:literal_null_coalesce | accepted_modeled_approximation | release-eng | reason: Non-partial-content branch of the same `totalBytes` resolution — `contentLength` may be -1 for chunked transfer. Same justification: progress UI only, integrity is enforced by post-download hash check. |
| packages/nightshade_updater/lib/src/services/update_service.dart:140:literal_null_coalesce | accepted_modeled_approximation | release-eng | reason: Operator-facing rollback diagnostic string. If the pending-update marker is partially corrupt (missing targetVersion/targetBuild) the user still gets "unknown+0" in the message rather than a crash; the marker file is then deleted on line 137 regardless. |
| packages/nightshade_updater/lib/src/services/update_service.dart:146:literal_null_coalesce | accepted_modeled_approximation | release-eng | reason: Same diagnostic as line 140 for the `requiresAttention` branch — surface "unknown+0" to the operator and continue to the explicit `requiresAttention` status. |
| packages/nightshade_planetarium/lib/src/catalogs/glade_plus_catalog.dart:30:literal_null_coalesce | accepted_modeled_approximation | planetarium | reason: GLADE+ CSV RA tolerant parse. Outer `parts.length < 5` check (line 26) rejects truncated rows with `FormatException`; this fallback is for the rare malformed-numeric case. Tracked approximation matching the HYG catalog pattern. |
| packages/nightshade_planetarium/lib/src/catalogs/glade_plus_catalog.dart:31:literal_null_coalesce | accepted_modeled_approximation | planetarium | reason: Same as line 30 — GLADE+ Dec tolerant parse. |
| packages/nightshade_planetarium/lib/src/catalogs/glade_plus_catalog.dart:38:literal_null_coalesce | accepted_modeled_approximation | planetarium | reason: GLADE+ PGC (Principal Galaxy Catalog id) tolerant parse. PGC=0 collides with the synthetic "no-PGC" sentinel used downstream; tracked approximation. |
| packages/nightshade_planetarium/lib/src/catalogs/hyperleda_catalog.dart:45:literal_null_coalesce | accepted_modeled_approximation | planetarium | reason: Same as line 43 — HyperLEDA RA tolerant parse. |
| packages/nightshade_planetarium/lib/src/catalogs/star_catalog.dart:139:literal_null_coalesce | accepted_modeled_approximation | planetarium | reason: HYG CSV RA-hours tolerant parse — same pattern as catalog_manager.dart:1145. Tracked approximation. |
| packages/nightshade_plugins/lib/examples/sequence_delay_plugin.dart:88:guessed_numeric_fallback | accepted_modeled_approximation | plugins | reason: Example-plugin documented default for `toleranceDegrees` parameter. The `CooldownWaitNode` plugin contract declares `toleranceDegrees` as optional with a default of 1.0°C — this is the parameter-default surface, not a silent fallback. |
| packages/nightshade_plugins/lib/examples/weather_logger_plugin.dart:65:literal_null_coalesce | accepted_modeled_approximation | plugins | reason: First-run storage initialization — `storage.getInt(countKey)` returns null on a fresh install (key never written). `0` is the correct initial reading-count. Example plugin. |
| packages/nightshade_ui/lib/src/utils/scaled_config.dart:130:literal_null_coalesce | accepted_modeled_approximation | ui | reason: Same as responsive_utils.dart:250 — `EdgeInsets.only` left edge defaults to 0 in the scaled-config padding helper. Mirrors Flutter's EdgeInsets API. |
| packages/nightshade_ui/lib/src/utils/scaled_config.dart:131:literal_null_coalesce | accepted_modeled_approximation | ui | reason: Same as line 130 — EdgeInsets top edge defaults to 0. |
| packages/nightshade_ui/lib/src/utils/scaled_config.dart:128:literal_null_coalesce | accepted_modeled_approximation | ui | reason: Same as line 130 — EdgeInsets right edge defaults to 0. |
| packages/nightshade_ui/lib/src/utils/scaled_config.dart:129:literal_null_coalesce | accepted_modeled_approximation | ui | reason: Same as line 130 — EdgeInsets bottom edge defaults to 0. |
| packages/nightshade_core/lib/src/services/science/default_science_backend.dart:253:guessed_numeric_fallback | accepted_modeled_approximation | core-hardening | Tracked approximation/default; requires phase-4/5 follow-up before commercialization gate closes. |
| packages/nightshade_planetarium/lib/src/catalogs/hyperleda_catalog.dart:47:literal_null_coalesce | accepted_modeled_approximation | planetarium | reason: HyperLEDA PGC tolerant parse. Outer `parts.length < 11` check (line 38) rejects truncated rows; this is the malformed-numeric fallback. Tracked approximation. |
| packages/nightshade_planetarium/lib/src/catalogs/hyperleda_catalog.dart:48:literal_null_coalesce | accepted_modeled_approximation | planetarium | reason: Same as line 43 — HyperLEDA Dec tolerant parse. |
| packages/nightshade_planetarium/lib/src/catalogs/variable_star_catalog.dart:291:literal_null_coalesce | accepted_modeled_approximation | planetarium | reason: Search-filter predicate — `VariableStarData.designation` is nullable for stars known only by GCVS name. `null` correctly yields "no match". Standard nullable-string idiom. |
| packages/nightshade_planetarium/lib/src/providers/target_queue_provider.dart:426:literal_null_coalesce | accepted_modeled_approximation | planetarium | reason: `seqTarget['name']?.toString().trim().isNotEmpty ?? false` — sequencer payload field is optional. `false` correctly routes to the `existing?.displayName ?? resolvedId` fallback chain on the next line. Standard nullable-string idiom for an optional Map<String,dynamic> field. |
| packages/nightshade_planetarium/lib/src/providers/target_queue_provider.dart:431:guessed_now_timestamp | accepted_modeled_approximation | planetarium | reason: `addedAt` for newly-synced targets that didn't exist in the previous queue snapshot. `DateTime.now()` is the actual creation time of the QueuedTarget record on this client — not a fabricated timestamp. Preserves `existing?.addedAt` when the target was already tracked. |
| packages/nightshade_planetarium/lib/src/providers/target_queue_provider.dart:436:guessed_now_timestamp | accepted_modeled_approximation | planetarium | reason: `completedAt` when sequencer reports a target as completed but no prior `completedAt` is known. `DateTime.now()` is the wall-clock observation that this client first saw the completed status — the best available timestamp absent a sequencer-provided one. |
| packages/nightshade_planetarium/lib/src/providers/target_queue_provider.dart:438:literal_null_coalesce | accepted_modeled_approximation | planetarium | reason: `plannedExposures` chain — sequencer payload, then existing snapshot, then 0. `0` represents "no plan yet" which renders as "0/N" in the queue UI; matches the sequencer's own "unplanned target" semantics. |
| packages/nightshade_planetarium/lib/src/providers/target_queue_provider.dart:440:literal_null_coalesce | accepted_modeled_approximation | planetarium | reason: Same as line 421 — `completedExposures` chain ending in `0` for a freshly-synced target with no completion data. |
| packages/nightshade_planetarium/lib/src/services/survey_image_service.dart:144:guessed_now_timestamp | accepted_modeled_approximation | planetarium | reason: `SurveyImage` constructor — `fetchedAt ?? DateTime.now()` records the actual fetch time when the caller didn't supply one (the default-construction path). Not a metadata fallback; it's the real wall-clock fetch timestamp. |
| packages/nightshade_plugins/lib/examples/weather_logger_plugin.dart:178:literal_null_coalesce | accepted_modeled_approximation | plugins | reason: Same as line 65 — `previousTotal` initialized to 0 on first persist. The very next statement increments by buffer length and writes back. Example plugin. |
| packages/nightshade_plugins/lib/examples/weather_logger_plugin.dart:217:literal_null_coalesce | accepted_modeled_approximation | plugins | reason: Same as line 65 — `getTotalReadingCount()` returns 0 when context is unloaded or the key is absent. Public plugin API contract: 0 means "no readings recorded yet". Example plugin. |
| packages/nightshade_plugins/lib/src/plugin_api.dart:168:guessed_now_timestamp | accepted_modeled_approximation | plugins | reason: `PluginEvent` constructor — `timestamp ?? DateTime.now()` records the actual emission time when the emitter didn't supply one. This is the real event-emission timestamp, not a fabricated metadata fallback. Matches `LoadedPlugin.loadedAt` constructor default. |
| packages/nightshade_ui/lib/src/utils/responsive_utils.dart:209:literal_null_coalesce | accepted_modeled_approximation | ui | reason: `BoxConstraints.minWidth` default — the `adaptiveBoxConstraints` helper accepts optional `minWidth`; `0.0` is the Flutter BoxConstraints default for "no minimum" and matches the API contract of the underlying widget. |
| packages/nightshade_ui/lib/src/utils/responsive_utils.dart:210:literal_null_coalesce | accepted_modeled_approximation | ui | reason: Same as line 112 — `minHeight ?? 0.0` matches `BoxConstraints` default for "no minimum height". |
| packages/nightshade_ui/lib/src/utils/responsive_utils.dart:348:literal_null_coalesce | accepted_modeled_approximation | ui | reason: `EdgeInsets.only` left value — `(left ?? horizontal ?? 0)` matches Flutter's documented EdgeInsets API where an omitted edge defaults to 0. Pure layout-helper sugar over `EdgeInsets.only`. |
| packages/nightshade_ui/lib/src/utils/responsive_utils.dart:349:literal_null_coalesce | accepted_modeled_approximation | ui | reason: Same as line 250 — EdgeInsets top edge defaults to 0 when neither `top` nor `vertical` is supplied. |
| packages/nightshade_ui/lib/src/utils/responsive_utils.dart:350:literal_null_coalesce | accepted_modeled_approximation | ui | reason: Same as line 250 — EdgeInsets right edge defaults to 0. |
| packages/nightshade_ui/lib/src/utils/responsive_utils.dart:351:literal_null_coalesce | accepted_modeled_approximation | ui | reason: Same as line 250 — EdgeInsets bottom edge defaults to 0. |
| packages/nightshade_updater/lib/src/models/update_manifest.dart:76:literal_null_coalesce | accepted_modeled_approximation | release-eng | reason: Same as line 70 — semver tolerant normalization in `isNewerThan` for the *other* operand. Pure read-side comparison. |
| packages/nightshade_updater/lib/src/models/update_manifest.dart:92:literal_null_coalesce | accepted_modeled_approximation | release-eng | reason: Same as line 70 — semver tolerant normalization in `canUpgradeFrom` for the running build version. |
| packages/nightshade_updater/lib/src/models/update_manifest.dart:96:literal_null_coalesce | accepted_modeled_approximation | release-eng | reason: Same as line 70 — semver tolerant normalization in `canUpgradeFrom` for the manifest's `minVersion`. |
| packages/nightshade_updater/lib/src/services/update_downloader.dart:114:literal_null_coalesce | accepted_modeled_approximation | release-eng | reason: `cancelToken?.isCancelled ?? false` — when no cancel token was supplied by the caller, downloads cannot be cancelled and the loop must continue. The `false` default is the documented contract of an optional cancel token. |
| packages/nightshade_updater/lib/src/services/update_downloader.dart:95:literal_null_coalesce | accepted_modeled_approximation | release-eng | reason: Content-Range header parsed but server omitted total — fall back to manifest `expectedSize` then 0. Total of 0 only affects the percent-progress UI; SHA-256 verification on the completed file is the actual integrity gate. |
| packages/nightshade_updater/lib/src/services/update_service.dart:533:empty_catch | explicit_unsupported | release-eng | reason: Already commented inline ("swallow: the more important error is the spawn failure below"). About to throw `UpdateException` for the pid==0 spawn failure; deleting the now-orphaned pending marker is best-effort cleanup so the *real* error is what surfaces to the caller. |
| packages/nightshade_updater/lib/src/widgets/update_manager_widget.dart:204:literal_null_coalesce | accepted_modeled_approximation | release-eng | reason: `UpdateDownloadDialog` is only built when status is `downloading`, but `availableUpdate?` is nullable per the freezed model. Empty-string version in the dialog header is a defensive UI fallback; the dialog is replaced immediately when status transitions to `staged`. |
| packages/nightshade_updater/lib/src/widgets/update_manager_widget.dart:219:literal_null_coalesce | accepted_modeled_approximation | release-eng | reason: Same as line 196 for `UpdateReadyDialog` — UI-only fallback for an inconsistent state that the state-machine treats as `error`. |
| apps/desktop/lib/headless_api/handlers/narrator_handlers.dart | accepted_modeled_approximation | headless-team | reason: REST handler defaults for optional request/JSON fields. '??' supplies the documented default when an optional query param or payload key is absent; the handler's typed response is built from validated inputs. |
| apps/desktop/lib/headless_api/handlers/profile_handlers.dart | accepted_modeled_approximation | headless-team | reason: `location?.latitude ?? 0.0` / `?? 0.0` when mirroring observer location to the scheduler DAO. A cleared (null) location intentionally zeroes both coordinates — documented inline at the call site. |
| packages/nightshade_app/lib/screens/imaging/centering_dialog.dart | accepted_modeled_approximation | app-team | reason: `detection.astapPath ?? detection.astrometryPath ?? ''` solver-path fallback for the plate-solver config; empty path is validated downstream before the solver is invoked. UI-side default. |
| packages/nightshade_app/lib/screens/settings/widgets/captured_images_settings.dart | accepted_modeled_approximation | app-team | reason: Empty catch returns null when a thumbnail fetch fails so the gallery renders a placeholder instead of crashing; the underlying backend call logs its own failures. UI-only cache helper. |
| packages/nightshade_app/lib/screens/settings/widgets/focus_model_settings.dart | accepted_modeled_approximation | app-team | reason: UI default for an optional focus-model field; the actual mutation path emits a typed event with the user's explicit value. |
| packages/nightshade_app/lib/screens/settings/widgets/update_settings.dart | accepted_modeled_approximation | app-team | reason: Empty catch on the status poll keeps the last snapshot through a transient failure (commented inline); '??' is a UI-only default for an optional update field. |
| packages/nightshade_core/lib/src/backend/network_backend/stacking_operations.dart | accepted_modeled_approximation | core-team | reason: Transport-payload parsing defaults ('?? 0' / '?? false') for live-stacking stats/status JSON. Each '??' matches the documented wire contract when a counter/flag is absent; values feed read-only UI counters. |
| packages/nightshade_core/lib/src/services/device_service/autofocus_controls.dart | accepted_modeled_approximation | core-team | reason: catch resets `_isAutofocusRunning = false` so a failed autofocus control call returns the service to idle rather than wedging the run state; the failure surfaces via the autofocus event stream. |
| packages/nightshade_core/lib/src/services/switch_channel_service.dart | accepted_modeled_approximation | core-team | reason: `(s['name'] as String?) ?? ''` default when parsing a switch-device payload Map; an unnamed channel renders with an empty label rather than throwing. Transport default. |
| packages/nightshade_planetarium/lib/src/providers/catalog_providers.dart | accepted_modeled_approximation | planetarium | reason: `CatalogStatus.objectCount ?? 0` count fallback for the not-installed state in the star/DSO count getters. Pure UI-side aggregation; supersedes the older line-pinned entries after the provider gained a download-stream subscription. |
| apps/mobile/lib/main_parts/mobile_connection_state.dart | accepted_modeled_approximation | mobile | reason: Commented best-effort connection-state cleanup during reconnect; failure is non-fatal and surfaces via the connection stream. |
| apps/mobile/lib/main_parts/mobile_discovery_ops.dart | accepted_modeled_approximation | mobile | reason: Transport defaults ('??') for optional mDNS/pairing discovery payload fields, plus a commented best-effort discovery teardown; values feed read-only UI. |
| apps/mobile/lib/main_parts/mobile_reconnect_ops.dart | accepted_modeled_approximation | mobile | reason: Best-effort relay-tunnel teardown — the empty catch is commented 'Best-effort teardown only.' so a failed close must not block reconnect; the remaining markers are commented best-effort reconnect steps. |
| packages/nightshade_app/lib/screens/analytics/widgets/science_onboarding/science_ladder_card.dart | accepted_modeled_approximation | app-team | reason: UI default ('??') for an optional on-ramp ladder-progress field; the rung renders a stable state when the source is absent. |
| packages/nightshade_app/lib/screens/dashboard/widgets/cockpit_morning_report.dart | accepted_modeled_approximation | app-team | reason: Catch returns null when parsing a persisted run-stats JSON blob fails, so the dashboard tile falls back to its empty state instead of throwing. |
| packages/nightshade_app/lib/screens/planner/planner_screen_parts/_sky_tab.dart | accepted_modeled_approximation | app-team | reason: GoRouterState.of is guarded so the Sky tab reads its '?view=' query in-app and returns null when the screen is mounted standalone in tests. |
| packages/nightshade_app/lib/screens/sequencer/tabs/sequence_library_tab.dart | accepted_modeled_approximation | app-team | reason: UI default ('??') for an optional sequence-library list field. |
| packages/nightshade_app/lib/screens/sequencer/tabs/sequence_library_tab/sequence_card.dart | accepted_modeled_approximation | app-team | reason: UI default ('??') for an optional sequence-card field. |
| packages/nightshade_app/lib/screens/sequencer/widgets/meridian_flip_progress_dialog.dart | accepted_modeled_approximation | app-team | reason: Commented best-effort UI side effect during meridian-flip progress display. |
| packages/nightshade_app/lib/screens/sequencer/widgets/mosaic_wizard_dialog/config_controls.dart | accepted_modeled_approximation | app-team | reason: UI form default ('??') for an optional mosaic-config field; the mutation path emits a typed event with the user's explicit value. |
| packages/nightshade_app/lib/screens/sequencer/widgets/node_property_widgets.dart | accepted_modeled_approximation | app-team | reason: UI form defaults ('??') for optional sequencer node-property fields; onChanged emits a typed event with the user's explicit value. |
| packages/nightshade_app/lib/screens/sequencer/widgets/node_timing_section.dart | accepted_modeled_approximation | app-team | reason: UI form defaults ('??') for optional node-timing fields. |
| packages/nightshade_app/lib/screens/sequencer/widgets/notes_panel/global_notes_dialog.dart | accepted_modeled_approximation | app-team | reason: UI default ('??') for an optional notes field. |
| packages/nightshade_app/lib/screens/sequencer/widgets/quick_start_wizard_dialog/_review_step.dart | accepted_modeled_approximation | app-team | reason: UI default ('??') for an optional quick-start review field. |
| packages/nightshade_app/lib/screens/sequencer/widgets/target_scheduler_properties.dart | accepted_modeled_approximation | app-team | reason: DateTime.now() renders a stable default for an optional scheduler timestamp field; the user's explicit selection is emitted on change. |
| packages/nightshade_app/lib/widgets/running_sequence_mini_bar.dart | accepted_modeled_approximation | app-team | reason: Catch is commented 'Router not ready — ignore' so the mini-bar tap no-ops before the router is mounted. |
| packages/nightshade_core/lib/src/database/daos/sequence_versions_dao.dart | accepted_modeled_approximation | core-team | reason: Persisted-row default ('??') for an optional sequence-version column read from drift. |
| packages/nightshade_core/lib/src/database/daos/sequences_dao.dart | accepted_modeled_approximation | core-team | reason: Persisted-row defaults ('??') for optional sequence columns read from drift rows. |
| packages/nightshade_core/lib/src/services/science/default_science_backend.dart | accepted_modeled_approximation | core-team | reason: `gain ?? 1.0` fallback when science.camera.gain_e_per_adu is unconfigured; the assumption is logged as a warning on the preceding line and the limiting-magnitude output documents it. |
| packages/nightshade_core/lib/src/services/wcs/wcs_sip_codec.dart | accepted_modeled_approximation | core-team | reason: Optional-field defaults ('?? 0' / '?? const []') when decoding the persisted SIP-distortion JSON blob; an absent SIP term decodes to zero order / no coefficients, matching the isotropic-fallback contract. |
| native/nightshade_native/bridge/src/api/sky_atlas.rs | accepted_modeled_approximation | core-hardening | reason: Bridge sky-atlas API. `unwrap_or` defaults coalesce per-tile fold tallies that are absent for tiles a batch never touched (missing map entry == zero frames/weight/rejections, the documented additive identity). `let _ = ...` discards the result of best-effort cleanup (`remove_dir_all`) in test teardown. The best-effort ISO-8601 comparison helper documents its lexicographic fallback inline. |
| native/nightshade_native/imaging/src/sky_atlas.rs | accepted_modeled_approximation | core-hardening | reason: Sky-atlas HEALPix-tiled accumulation core. `let _ = std::fs::remove_dir_all(...)` discards pre/post cleanup results in unit-test setup/teardown where a missing temp dir is the desired state and a failure must not fail the test. |
| native/nightshade_native/imaging/src/wcs_sip.rs | accepted_modeled_approximation | core-hardening | reason: WCS + SIP distortion solver ported from the Dart reference. `let _ = wcs.world_to_pixel(...)` in a unit test asserts the round-trip does not panic for an on-sky coordinate; the value is exercised by the preceding equality assertions. |
| native/nightshade_native/imaging/src/difference_image.rs | accepted_modeled_approximation | core-hardening | reason: Difference-imaging transient detection. `vals.last()/vals.first().copied().unwrap_or(0.0)` coalesce the bounds of an already-sorted residual sample; an empty slice yields a zero span which the `.max(f64::MIN_POSITIVE)` floor turns into the documented noiseless/flat-plane sigma fallback. |
| apps/desktop/lib/headless_api/handlers/atlas_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless atlas HTTP handler. '??' supplies query-parameter and optional-field defaults when decoding the request/atlas-region payload, matching the documented handler contract (absent param == server default). |
| packages/nightshade_app/lib/screens/constellation/constellation_contribute_sheet.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' renders a stable form value when the underlying provider/model has a nullable field; the actual mutation path emits a typed event with the user's explicit selection. |
| packages/nightshade_app/lib/screens/constellation/constellation_screen.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' renders a stable form value when the underlying provider/model has a nullable field; the actual mutation path emits a typed event with the user's explicit selection. |
| packages/nightshade_app/lib/screens/constellation/constellation_ui_providers.dart | accepted_modeled_approximation | app-team | reason: Riverpod UI providers for the constellation surface. '??' coalesces optional model/preference fields into a stable default state value that the widget tree can render before the backing source resolves. |
| packages/nightshade_app/lib/screens/constellation/shared_target_detail_screen.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' renders a stable form value when the underlying provider/model has a nullable field; the actual mutation path emits a typed event with the user's explicit selection. |
| packages/nightshade_app/lib/screens/constellation/widgets/shared_target_card.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' renders a stable display value when the shared-target model has a nullable field. |
| packages/nightshade_app/lib/screens/your_sky/widgets/atlas_region_card.dart | accepted_modeled_approximation | app-team | reason: UI screen / widget defaults for optional model fields. '??' renders stable coverage/progress display values when the atlas-region model has nullable fields. |
| packages/nightshade_core/lib/src/database/daos/sky_atlas_dao.dart | accepted_modeled_approximation | core-team | reason: Persisted-row defaults ('??') for optional sky-atlas columns read from drift rows; an absent column decodes to the documented empty/zero default. |
| packages/nightshade_core/lib/src/providers/transient_detections_provider.dart | accepted_modeled_approximation | core-team | reason: Transient-detection stream provider. '??' supplies optional-field defaults when decoding wire JSON; the empty catch on the reconnect path intentionally preserves the last good snapshot when the host is busy/reconnecting (a transient read must not crash the stream). |
| packages/nightshade_core/lib/src/services/constellation/constellation_models.dart | accepted_modeled_approximation | core-team | reason: Model fromJson / copyWith defaults for optional persisted or transport fields. Each '??' matches the documented contract for the corresponding source (cached prefs, network payload). Strongly-typed Dart models hide the optionality from UI code. |
| packages/nightshade_core/lib/src/services/constellation/constellation_service.dart | accepted_modeled_approximation | core-team | reason: Constellation sharing service. '??' coalesces optional fields from persisted/transport payloads into typed defaults per the documented sharing contract. |
| packages/nightshade_core/lib/src/services/sky_atlas/sky_atlas_models.dart | accepted_modeled_approximation | core-team | reason: Model fromJson / copyWith defaults for optional persisted or transport fields. Each '??' matches the documented contract for the corresponding source (sidecar provenance, region/tile JSON). Strongly-typed Dart models hide the optionality from UI code. |
| packages/nightshade_core/lib/src/services/sky_atlas/sky_atlas_service.dart | accepted_modeled_approximation | core-team | reason: Sky-atlas service. '??' coalesces optional fields from persisted atlas state and bridge JSON results into typed defaults per the documented atlas contract. |
| packages/nightshade_core/lib/src/services/transients/transient_candidate.dart | accepted_modeled_approximation | core-team | reason: Model fromJson / copyWith defaults for optional persisted or transport fields. Each '??' matches the documented contract for the transient-candidate wire/persisted payload. |
| apps/desktop/lib/headless_api/handlers/first_light_handlers.dart | accepted_modeled_approximation | app-team | reason: Headless REST handler for the First Light "near" query. `double.tryParse(params['ra'] ?? '')` coalesces a missing query parameter to an empty string that tryParse turns into null; the required ra/dec then throw `BadRequestError → 400`, and the optional `radius ?? 0.02` is the documented default search radius re-validated (`isFinite && > 0`) on the next line. Absence surfaces as an explicit 400 or the documented default — no upstream null is hidden. |
| apps/desktop/lib/main.dart | accepted_modeled_approximation | app-team | reason: Desktop --remote-host client bootstrap. `int.tryParse(portRaw ?? '') ?? 8080` and `?? 'http'` resolve optional CLI flags / env vars to their documented defaults (port 8080, http scheme) when the operator omits them; a malformed value falls back to the same documented default per the flag's contract. |
| native/nightshade_native/bridge/src/api/difference_image.rs:344:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `let _ = write_preview_png(&stamp, &path)` intentionally discards the Result of a best-effort crop-preview PNG write inside the per-tile loop. The write is cosmetic — the UI falls back to a schematic when the file is absent — and propagating would abort the whole difference-image run for a non-fatal preview. The bridge has no logger at this layer, so file-absence is the documented signal (matches the imaging.rs best-effort event-send registration). |
| packages/nightshade_app/lib/screens/analytics/widgets/transient_report_panel.dart | accepted_modeled_approximation | app-team | reason: UI report-preview builder. `_magZeroPoint ?? 0.0` and sibling `?? 0.0` render a stable preview value while the optional photometric zero-point is unsolved; the actual TNS/AAVSO/MPC submission re-derives and re-validates these fields at submit time, where the sentinel apiKey is also replaced with the real, re-checked credential. |
| packages/nightshade_app/lib/screens/planner/planner_screen_parts/_discover_tab.dart:74:empty_catch | explicit_unsupported | app-team | reason: `GoRouterState.of(context)` throws when no GoRouter ancestor exists; tests mount this tab standalone. The `catch` returning null is the documented optional-ancestor probe, not an error path — in production a GoRouter always exists so it never fires, and a log would emit no actionable signal on every standalone build. Must remain a no-op. |
| packages/nightshade_app/lib/screens/settings/widgets/science_settings.dart | accepted_modeled_approximation | app-team | reason: Settings widget numeric text rows. `int.tryParse(v) ?? 0` coalesces an empty/partial TNS identifier field (botId, reportingGroupId, dataSourceId) to 0 while the user types; 0 renders as an empty field on read, and the settings notifier persists the explicit value the user commits. |
| packages/nightshade_app/lib/screens/your_sky/widgets/name_region_sheet.dart | accepted_modeled_approximation | app-team | reason: Region-naming sheet target picker. `int.tryParse(idStr ?? '')` maps a null/empty dropdown selection to a null id (no target chosen); the subsequent lookup tolerates null and the sheet leaves the region's target unset. |
| packages/nightshade_core/lib/src/backend/network_backend/observing_list_operations.dart | accepted_modeled_approximation | core-team | reason: NetworkBackend slave→host REST client. `(response['id'] as num?)?.toInt() ?? 0` coalesces an optional id/count field in the host's JSON response to 0; the host always returns the field on success, and a transport/HTTP failure has already thrown from the `_post`/`_get` helper before these defaults apply. |
| packages/nightshade_core/lib/src/backend/network_backend/planning_data_operations.dart | accepted_modeled_approximation | core-team | reason: NetworkBackend slave→host REST client. `(response['id'/'count'] as num?)?.toInt() ?? 0` coalesces an optional id/count field in the host's JSON response to 0; the host always returns the field on success, and a transport/HTTP failure has already thrown from the `_post` helper before these defaults apply. |
| packages/nightshade_core/lib/src/database/daos/constellation_contributions_dao.dart:141:guessed_now_timestamp | accepted_modeled_approximation | core-team | reason: `upsertPulledHighWater` already accepts an optional `lastPulledAt`; `lastPulledAt ?? DateTime.now()` is the injectable-clock idiom — tests pass a fixed instant, production records the real pull moment. Not a guessed metadata stamp. |
| packages/nightshade_core/lib/src/models/backend/sequencer_status.dart | accepted_modeled_approximation | core-team | reason: Sequencer status wire model. `fromJson` `?? 0/0.0` and `?? DateTime.fromMillisecondsSinceEpoch(0)` coalesce optional run-vitals counters (framesCaptured, integrationSecs, autofocusRuns…) and a missing startTime to documented zero values — cumulative counters are legitimately 0 before the first event of a run. |
| packages/nightshade_core/lib/src/models/equipment_profile_remote_mapping.dart | accepted_modeled_approximation | core-team | reason: Remote-profile → DB row mapping. `telescopeFocalLength/Aperture ?? 0.0` and `int.tryParse(profile.id) ?? 0` match the wire model's documented defaults for optional OTA fields and a not-yet-persisted profile id. The `updatedAt` write stamp now flows through an injectable `now` parameter (`now ?? DateTime.now().toUtc()`) so the bump is deterministic under test while production stamps the real write time. |
| packages/nightshade_core/lib/src/models/planning/project.dart | accepted_modeled_approximation | core-team | reason: Planning ProjectTarget model. `fromJson` `?? 0` coalesces optional projectId/targetId foreign keys to 0 for a not-yet-persisted row; the nullable `id` stays null until the DAO assigns it, so 0 is the documented "unsaved/unlinked" sentinel. |
| packages/nightshade_core/lib/src/models/planning/project_progress.dart | accepted_modeled_approximation | core-team | reason: Planning progress model. `fromJson` `?? ''/0/0.0` coalesce optional per-filter progress fields (filter label, exposureSeconds, goal/captured frame counts) to documented zero values for a filter with no captured data yet. |
| packages/nightshade_core/lib/src/models/scheduler/integration_goal.dart | accepted_modeled_approximation | core-team | reason: Scheduler IntegrationGoal model. `fromJson` `?? ''/0/0.0` coalesce optional goal fields (targetId, filter, exposureSeconds, frameCount); `priority ?? 5` is the documented default priority and the nullable `id` stays null for an unsaved goal. |
| packages/nightshade_core/lib/src/models/scheduler/scheduler_decision.dart | accepted_modeled_approximation | core-team | reason: Scheduler decision/score model. `fromJson` `?? ''/0` coalesce optional score-breakdown fields (factor name, value, weight, weighted) to documented zero values; a decision serialized before a factor is scored carries the neutral 0 the UI breakdown renders. |
| packages/nightshade_core/lib/src/models/scheduler/target_constraint.dart | accepted_modeled_approximation | core-team | reason: Scheduler TargetConstraint model. `fromJson` `?? 0/''/'{}'` coalesce optional id/targetId and the kind/payload strings to documented empties; `payloadJson ?? '{}'` parses to an empty payload that `fromRow` validates per constraint kind. |
| packages/nightshade_core/lib/src/providers/imaging_provider.dart | accepted_modeled_approximation | core-team | reason: Imaging provider state. `row.gain ?? 0` / `row.offset ?? 0` reconcile nullable DB columns (cameras that do not expose gain/offset) with the strongly-typed ExposureSettings the UI consumes; 0 is the documented "unset/not-applicable" value for those sensors. |
| packages/nightshade_core/lib/src/providers/observation_log_provider.dart | accepted_modeled_approximation | core-team | reason: Observation-log filter provider. `(l.catalogId?...contains(q) ?? false)` treats a log with no catalogId as a non-match for the search term, and `(l.rating ?? 0) >= min` treats an unrated log as rating 0 against a min-rating filter — both the documented filter semantics for optional fields. |
| packages/nightshade_core/lib/src/providers/sky_atlas_provider.dart | accepted_modeled_approximation | core-team | reason: Your Sky atlas provider. `?? 0/0.0/''/false` coalesce optional fields of the host's `/api/atlas/*` JSON (region/coverage/tile metadata) to documented zero values when a slave reads a remote atlas; the transient-failure refetch path now logs at debug and retains the last good snapshot rather than swallowing silently. |
| packages/nightshade_core/lib/src/services/scheduler/horizon_profile.dart | accepted_modeled_approximation | core-team | reason: Horizon profile model. `fromJson` `?? 0/''/'[]'` coalesce optional id/name and `samplesJson ?? '[]'` to an empty sample set — the documented "flat/unset horizon" the scheduler treats as no elevation mask. |
| packages/nightshade_core/lib/src/services/transients/first_light_service.dart:396:guessed_now_timestamp | accepted_modeled_approximation | core-team | reason: `pruneOldDetections({Duration horizon, DateTime? now})` already exposes the injectable-clock idiom — `now ?? DateTime.now()` lets tests pin the retention cutoff while production reclaims dismissed detections older than the real horizon. Not a guessed metadata stamp. |
| packages/nightshade_updater/lib/src/models/update_manifest.dart:98:literal_null_coalesce | accepted_modeled_approximation | release-eng | reason: Semver parsing. `currentVersion.split('.').map((p) => int.tryParse(p) ?? 0)` coalesces a non-numeric version segment to 0 for the comparison; same documented parsing pattern as the registered update_manifest.dart sites (70/76/92/96). |
| packages/nightshade_updater/lib/src/models/update_manifest.dart:109:literal_null_coalesce | accepted_modeled_approximation | release-eng | reason: Semver parsing. `fromVersion.split('.').map((p) => int.tryParse(p) ?? 0)` coalesces a non-numeric version segment to 0 for the canUpgradeFrom comparison; same documented parsing pattern as the registered update_manifest.dart sites (70/76/92/96). |
| packages/nightshade_updater/lib/src/models/update_manifest.dart:113:literal_null_coalesce | accepted_modeled_approximation | release-eng | reason: Semver parsing. `minVersion!.split('.').map((p) => int.tryParse(p) ?? 0)` coalesces a non-numeric version segment to 0 for the minimum-version comparison; same documented parsing pattern as the registered update_manifest.dart sites (70/76/92/96). |
| packages/nightshade_updater/lib/src/services/update_service.dart:176:literal_null_coalesce | accepted_modeled_approximation | release-eng | reason: Pending-install status message formatting. `targetVersion ?? "unknown"` / `targetBuild ?? 0` render a human-readable rollback message when the pending-marker fields are absent; cosmetic string interpolation only — no control-flow decision rides on the default. Matches the registered update_service.dart sites (140/146). |
| packages/nightshade_updater/lib/src/services/update_service.dart:182:literal_null_coalesce | accepted_modeled_approximation | release-eng | reason: Pending-install status message formatting. `targetVersion ?? "unknown"` / `targetBuild ?? 0` render a human-readable requires-attention message when the pending-marker fields are absent; cosmetic string interpolation only — no control-flow decision rides on the default. Matches the registered update_service.dart sites (140/146). |
| apps/desktop/lib/headless_api/auth_policy.dart:349:comment_best_effort | accepted_modeled_approximation | app-team | reason: The "best-effort coarse projection" doc comment marks an intentional lossy downcast of the fine-grained per-resource grant map onto the legacy admin/control/view scope enum, used only for the back-compat `/api/info` scope list and the 403 body fields. No error is swallowed; the fine-grained `permits()` path remains the authoritative access decision. |
| native/nightshade_native/bridge/src/ascom_wrapper/sta_worker.rs:112:unwrap_or_literal | accepted_modeled_approximation | core-hardening | reason: `std::env::var("NIGHTSHADE_HEADLESS").map(|v| v == "1").unwrap_or(false)` treats an absent env var as "headless not requested via this channel"; the `--headless` argv check is OR'd in, so absence legitimately resolves to not-headless. Documented at the function; not a coerced error state. Windows-only (`#[cfg(windows)]` parent). |
| native/nightshade_native/bridge/src/ascom_wrapper/sta_worker.rs:122:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `let _ = worker_sender();` in `ensure_sta_worker` intentionally discards the returned `&'static Sender`; the call exists only for its side effect of lazily starting the singleton STA worker thread, and is idempotent. No Result is dropped. |
| native/nightshade_native/bridge/src/ascom_wrapper/sta_worker.rs:203:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `let _ = reg.ready.send(Ok(()))` is a best-effort readiness reply to `register_device`. If the caller already timed out and dropped its receiver there is nothing actionable; the constructed device pump is still pushed onto the round-robin. Fire-and-forget by design. |
| native/nightshade_native/bridge/src/ascom_wrapper/sta_worker.rs:207:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `let _ = reg.ready.send(Err(e))` is the best-effort construction-failure reply on the same readiness channel as line 203; a dropped receiver (caller gone) has nothing to receive the error and no further action is possible. |
| native/nightshade_native/bridge/src/ascom_wrapper/sta_worker.rs:281:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `let _ = RegisterClassW(&wnd_class)` deliberately ignores the atom/0 return; the adjacent comment documents that a duplicate/zero registration is tolerated for this process singleton, and a genuinely bad class instead surfaces as a null `hwnd` checked on the next line. |
| native/nightshade_native/bridge/src/ascom_wrapper/sta_worker.rs:311:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `let _ = self.beat.send(())` in `DialogWatchdog::kick` is a best-effort heartbeat. A closed channel means the watchdog thread has already exited (process teardown); there is no recoverable action and the worker loop must not block on the send. |
| native/nightshade_native/bridge/src/ascom_wrapper/sta_worker.rs:359:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `let _ = EnumThreadWindows(...)` best-effort enumerates the wedged worker thread's windows for the headless dialog-close path; an enumeration failure simply leaves the collected `windows` vec empty (nothing to close), which is the safe outcome. |
| native/nightshade_native/bridge/src/ascom_wrapper/sta_worker.rs:365:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `let _ = PostMessageW(hwnd, WM_CLOSE, ...)` best-effort posts WM_CLOSE to force-dismiss a driver modal from the watchdog thread; a window that has already closed or ignores the message is an acceptable result, and the watchdog keeps monitoring for later modals. |
| packages/nightshade_updater/lib/src/services/anti_freeze_store.dart:89:empty_catch | accepted_modeled_approximation | release-eng | reason: `advance()` persists the rollback watermark only AFTER a fully verified update; the doc comment requires that a persistence failure not block an already-applied update. A failed write leaves the watermark at its prior (lower, conservative) value, so the gate is never loosened by this catch and the bar is re-established on the next accepted update. (The separate `_read()` corrupt-file path was fixed to log rather than swallow.) |
| packages/nightshade_updater/lib/src/services/anti_freeze_store.dart:90:comment_best_effort | accepted_modeled_approximation | release-eng | reason: The `// Best-effort persistence; see doc comment.` line documents the intentional best-effort write registered immediately above (anti_freeze_store.dart:89). Same justification: the watermark fails in the safe direction and re-establishes on the next accepted update. |
| packages/nightshade_updater/lib/src/services/update_service.dart:193:literal_null_coalesce | accepted_modeled_approximation | release-eng | reason: Pending-install status message formatting. `${targetVersion ?? "unknown"}+${targetBuild ?? 0}` renders a human-readable rolled-back message when the optional pending-marker fields are absent; cosmetic string interpolation only, no control-flow decision rides on the default. Matches the registered update_service.dart sites (176/182). |
| packages/nightshade_updater/lib/src/services/update_service.dart:199:literal_null_coalesce | accepted_modeled_approximation | release-eng | reason: Pending-install status message formatting. `${targetVersion ?? "unknown"}+${targetBuild ?? 0}` renders a human-readable requires-attention message when the optional pending-marker fields are absent; cosmetic string interpolation only, no control-flow decision rides on the default. Matches the registered update_service.dart sites (176/182). |
| packages/nightshade_app/lib/screens/collaborative_sky/collab_mosaic_detail_screen.dart:294:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: UI display formatting: an optional display-name/id string coalesces to '' for rendering. The nullable field is a presentation value; '' renders a neutral/empty label while the authoritative value comes from the typed hub model. |
| packages/nightshade_app/lib/screens/collaborative_sky/collaborative_sky_format.dart:139:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: UI display formatting: an optional display-name/id string coalesces to '' for rendering. The nullable field is a presentation value; '' renders a neutral/empty label while the authoritative value comes from the typed hub model. |
| packages/nightshade_app/lib/screens/collaborative_sky/collaborative_sky_format.dart:35:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: UI display formatting: an optional display-name/id string coalesces to '' for rendering. The nullable field is a presentation value; '' renders a neutral/empty label while the authoritative value comes from the typed hub model. |
| packages/nightshade_app/lib/screens/collaborative_sky/collaborative_sky_screen.dart:210:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: UI display formatting: an optional display-name/id string coalesces to '' for rendering. The nullable field is a presentation value; '' renders a neutral/empty label while the authoritative value comes from the typed hub model. |
| packages/nightshade_app/lib/screens/mosaic/mosaic_contribute_sheet.dart:245:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Checkbox onChanged callback: Flutter's CheckboxListTile passes a nullable bool for tristate support; `v ?? false` maps the never-tristate control back to its non-null bool. The user's explicit toggle is the source of truth and the default only satisfies Flutter's nullable signature. |
| packages/nightshade_app/lib/screens/mosaic/mosaic_contribute_sheet.dart:252:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Checkbox onChanged callback: Flutter's CheckboxListTile passes a nullable bool for tristate support; `v ?? false` maps the never-tristate control back to its non-null bool. The user's explicit toggle is the source of truth and the default only satisfies Flutter's nullable signature. |
| packages/nightshade_app/lib/screens/mosaic/mosaic_contribute_sheet.dart:259:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Checkbox onChanged callback: Flutter's CheckboxListTile passes a nullable bool for tristate support; `v ?? false` maps the never-tristate control back to its non-null bool. The user's explicit toggle is the source of truth and the default only satisfies Flutter's nullable signature. |
| packages/nightshade_app/lib/screens/mosaic/mosaic_project_screen.dart:478:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: UI display formatting: an optional display-name/id string coalesces to '' for rendering. The nullable field is a presentation value; '' renders a neutral/empty label while the authoritative value comes from the typed hub model. |
| packages/nightshade_core/lib/src/database/daos/constellation_contributions_dao.dart:143:guessed_now_timestamp | accepted_modeled_approximation | collab-sky | reason: `lastPulledAt ?? DateTime.now()` records the incremental-pull watermark; the pull is executing at call time, so now() is the correct semantic when the caller omits an explicit instant. Advisory dedup metadata, not an ordering or security decision. |
| packages/nightshade_core/lib/src/models/calibration/shared_calibration_models.dart:76:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Model fromJson deserialization default for an optional field. The '??' maps a missing optional field to the documented zero/empty baseline per the shared-calibration wire contract; bodies are parsed from already-validated 2xx hub payloads and the license field fails closed to `private` separately. |
| packages/nightshade_core/lib/src/models/calibration/shared_calibration_models.dart:77:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Model fromJson deserialization default for an optional field. The '??' maps a missing optional field to the documented zero/empty baseline per the shared-calibration wire contract; bodies are parsed from already-validated 2xx hub payloads and the license field fails closed to `private` separately. |
| packages/nightshade_core/lib/src/models/calibration/shared_calibration_models.dart:79:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Model fromJson deserialization default for an optional field. The '??' maps a missing optional field to the documented zero/empty baseline per the shared-calibration wire contract; bodies are parsed from already-validated 2xx hub payloads and the license field fails closed to `private` separately. |
| packages/nightshade_core/lib/src/models/calibration/shared_calibration_models.dart:80:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Model fromJson deserialization default for an optional field. The '??' maps a missing optional field to the documented zero/empty baseline per the shared-calibration wire contract; bodies are parsed from already-validated 2xx hub payloads and the license field fails closed to `private` separately. |
| packages/nightshade_core/lib/src/models/calibration/shared_calibration_models.dart:81:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Model fromJson deserialization default for an optional field. The '??' maps a missing optional field to the documented zero/empty baseline per the shared-calibration wire contract; bodies are parsed from already-validated 2xx hub payloads and the license field fails closed to `private` separately. |
| packages/nightshade_core/lib/src/models/calibration/shared_calibration_models.dart:82:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Model fromJson deserialization default for an optional field. The '??' maps a missing optional field to the documented zero/empty baseline per the shared-calibration wire contract; bodies are parsed from already-validated 2xx hub payloads and the license field fails closed to `private` separately. |
| packages/nightshade_core/lib/src/models/calibration/shared_calibration_models.dart:83:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Model fromJson deserialization default for an optional field. The '??' maps a missing optional field to the documented zero/empty baseline per the shared-calibration wire contract; bodies are parsed from already-validated 2xx hub payloads and the license field fails closed to `private` separately. |
| packages/nightshade_core/lib/src/models/calibration/shared_calibration_models.dart:84:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Model fromJson deserialization default for an optional field. The '??' maps a missing optional field to the documented zero/empty baseline per the shared-calibration wire contract; bodies are parsed from already-validated 2xx hub payloads and the license field fails closed to `private` separately. |
| packages/nightshade_core/lib/src/models/calibration/shared_calibration_models.dart:90:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Model fromJson deserialization default for an optional field. The '??' maps a missing optional field to the documented zero/empty baseline per the shared-calibration wire contract; bodies are parsed from already-validated 2xx hub payloads and the license field fails closed to `private` separately. |
| packages/nightshade_core/lib/src/models/calibration/shared_calibration_models.dart:96:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Model fromJson deserialization default for an optional field. The '??' maps a missing optional field to the documented zero/empty baseline per the shared-calibration wire contract; bodies are parsed from already-validated 2xx hub payloads and the license field fails closed to `private` separately. |
| packages/nightshade_core/lib/src/models/collaboration/collaboration_models.dart:173:empty_catch | accepted_modeled_approximation | collab-sky | reason: Fail-closed JSON decode: the catch wraps a role/permission parse whose FormatException and type-confusion paths both fall through to the conservative default (empty set / [denied] / bare role) documented in the adjacent comment. A corrupt or forward-version payload never widens access. |
| packages/nightshade_core/lib/src/models/collaboration/collaboration_models.dart:355:empty_catch | accepted_modeled_approximation | collab-sky | reason: Fail-closed JSON decode: the catch wraps a role/permission parse whose FormatException and type-confusion paths both fall through to the conservative default (empty set / [denied] / bare role) documented in the adjacent comment. A corrupt or forward-version payload never widens access. |
| packages/nightshade_core/lib/src/models/collaboration/collaboration_models.dart:617:empty_catch | accepted_modeled_approximation | collab-sky | reason: Fail-closed JSON decode: the catch wraps a role/permission parse whose FormatException and type-confusion paths both fall through to the conservative default (empty set / [denied] / bare role) documented in the adjacent comment. A corrupt or forward-version payload never widens access. |
| packages/nightshade_core/lib/src/services/calibration/shared_calibration_library.dart:163:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Publish-to-hub wire mapping: an optional local calibration-record field coalesces to the documented zero/empty wire default before the master is published. The record's nullable metadata is optional; the master file, consent, and license are required and validated upstream. |
| packages/nightshade_core/lib/src/services/calibration/shared_calibration_library.dart:167:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Publish-to-hub wire mapping: an optional local calibration-record field coalesces to the documented zero/empty wire default before the master is published. The record's nullable metadata is optional; the master file, consent, and license are required and validated upstream. |
| packages/nightshade_core/lib/src/services/calibration/shared_calibration_library.dart:168:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Publish-to-hub wire mapping: an optional local calibration-record field coalesces to the documented zero/empty wire default before the master is published. The record's nullable metadata is optional; the master file, consent, and license are required and validated upstream. |
| packages/nightshade_core/lib/src/services/calibration/shared_calibration_library.dart:169:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Publish-to-hub wire mapping: an optional local calibration-record field coalesces to the documented zero/empty wire default before the master is published. The record's nullable metadata is optional; the master file, consent, and license are required and validated upstream. |
| packages/nightshade_core/lib/src/services/calibration/shared_calibration_library.dart:170:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Publish-to-hub wire mapping: an optional local calibration-record field coalesces to the documented zero/empty wire default before the master is published. The record's nullable metadata is optional; the master file, consent, and license are required and validated upstream. |
| packages/nightshade_core/lib/src/services/calibration/shared_calibration_library.dart:171:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Publish-to-hub wire mapping: an optional local calibration-record field coalesces to the documented zero/empty wire default before the master is published. The record's nullable metadata is optional; the master file, consent, and license are required and validated upstream. |
| packages/nightshade_core/lib/src/services/calibration/shared_calibration_library.dart:177:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Publish-to-hub wire mapping: an optional local calibration-record field coalesces to the documented zero/empty wire default before the master is published. The record's nullable metadata is optional; the master file, consent, and license are required and validated upstream. |
| packages/nightshade_core/lib/src/services/coimaging/coimaging_session_service.dart:1027:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Owner membership upsert: the local user's own participant row supplies the framing offset; a not-yet-populated `self` (before the hub round-trip) defaults the offset to 0 (on-axis), the documented pre-assignment framing. |
| packages/nightshade_core/lib/src/services/coimaging/coimaging_session_service.dart:1028:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Owner membership upsert: the local user's own participant row supplies the framing offset; a not-yet-populated `self` (before the hub round-trip) defaults the offset to 0 (on-axis), the documented pre-assignment framing. |
| packages/nightshade_core/lib/src/services/coimaging/coimaging_session_service.dart:1029:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Owner membership upsert: the local user's own participant row supplies the framing offset; a not-yet-populated `self` (before the hub round-trip) defaults the offset to 0 (on-axis), the documented pre-assignment framing. |
| packages/nightshade_core/lib/src/services/coimaging/coimaging_session_service.dart:424:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Prior-membership accumulator: no persisted membership row yet means zero prior contribution, the correct baseline for a first join. |
| packages/nightshade_core/lib/src/services/coimaging/coimaging_session_service.dart:425:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Prior-membership accumulator: no persisted membership row yet means zero prior contribution, the correct baseline for a first join. |
| packages/nightshade_core/lib/src/services/coimaging/coimaging_session_service.dart:821:guessed_now_timestamp | accepted_modeled_approximation | collab-sky | reason: `atUtc ?? DateTime.now().toUtc()` evaluates target altitude at the requested instant; omitting the instant means 'is the target up now', so now() is the intended evaluation time, not a fallback for missing data. |
| packages/nightshade_core/lib/src/services/constellation/constellation_client.dart:730:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: Hub response field default: reads a flag from an already-validated 2xx body; a server that omits the field is treated as the conservative false/not-released default. |
| packages/nightshade_core/lib/src/services/mosaic/collaborative_mosaic_service.dart:690:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: FITS WCS keyword write: the CD-matrix off-diagonal term defaults to 0.0, the astrometric identity (no rotation/skew) when the solved WCS carried only the diagonal scale. Header value only; solved values override when present. |
| packages/nightshade_core/lib/src/services/mosaic/collaborative_mosaic_service.dart:691:literal_null_coalesce | accepted_modeled_approximation | collab-sky | reason: FITS WCS keyword write: the CD-matrix off-diagonal term defaults to 0.0, the astrometric identity (no rotation/skew) when the solved WCS carried only the diagonal scale. Header value only; solved values override when present. |
| packages/nightshade_app/lib/screens/dashboard/widgets/glance_mode_toggle.dart | accepted_modeled_approximation | app-team | reason: The one empty catch wraps `glanceModeProvider.toggle()` and exists only to render the failure: it shows the 'Could not save the Glance mode setting.' SnackBar and clears the saving spinner. Glance mode is a dashboard display preference with no device or capture consequence, and the toggle re-reads authoritative state, so the operator sees both the error and the real stored value. |
| packages/nightshade_app/lib/screens/planner/widgets/projects_tab_content.dart | accepted_modeled_approximation | app-team | reason: Every remaining empty catch on this tab immediately renders a user-visible message naming the operation that failed — `_showProjectError` ('Could not create/update/delete the project', 'The project was created, but its active selection could not be saved', 'The project was deleted, but the project list could not be refreshed') or `_showConnectionChanged` when the backend moved mid-dialog. Each also returns without applying local state, so the tab never shows a mutation that did not persist. The one silent arm (the active-project coherence repair) was given a log line instead of a register row. |
| packages/nightshade_app/lib/screens/settings/plate_solving_settings_screen.dart | accepted_modeled_approximation | app-team | reason: `_openUrl` catch covers `canLaunchUrl`/`launchUrl` for the ASTAP download page. It sets `launched = false`, which is the same path an unavailable handler takes, and the caller then shows 'Could not open <url>'. No solver state is read or written here — the URL is a hyperlink to hnsky.org. |
| packages/nightshade_app/lib/screens/settings/widgets/about_settings.dart | accepted_modeled_approximation | app-team | reason: `_launchUrl` catch covers `canLaunchUrl`/`launchUrl` for the About-page external links. It sets `launched = false` and the caller shows 'Could not open <url>'. A platform with no URL handler and a throwing one are the same outcome for the operator, and nothing in the app depends on the launch succeeding. |
| packages/nightshade_app/lib/screens/settings/widgets/help_tutorials_settings.dart | accepted_modeled_approximation | app-team | reason: Both empty catches are the error arm of an async-button wrapper (`_runAction`, `_AsyncActionButtonState._run`). Each shows a SnackBar naming the action that failed — the tutorial's own 'Could not start/resume/restart <title>' or the caller-supplied `failureMessage` — and clears the button's running flag so it can be retried. The tutorial progress they write is not read by any imaging path. |
| packages/nightshade_app/lib/screens/settings/widgets/predictive_af_settings.dart | accepted_modeled_approximation | app-team | reason: Nothing is swallowed: the catch drops the optimistic `_requestedConfig`, reloads authoritative state so the panel stops showing the unsaved value, and then rethrows. The predictive-AF write failure reaches the caller intact. |
| packages/nightshade_app/lib/screens/settings/widgets/remote_access_settings.dart | accepted_modeled_approximation | app-team | reason: The catch around `setWebServerPort` re-reads the confirmed port from `appSettingsProvider`, rewrites the text field to it so the form cannot display a port that was never saved, and shows the localized `remoteAccessSavePortFailed` SnackBar. The operator is told, and the field afterwards shows the port actually in effect. |
| packages/nightshade_app/lib/screens/suggestions/widgets/transient_alerts_panel.dart | accepted_modeled_approximation | app-team | reason: Both empty catches are single-purpose UI error arms — `_runAlertStateAction` shows 'Could not update this alert.' and the settings dialog's `_run` shows 'Could not save the alert settings.'. Neither writes local state on failure, so a dismissed/queued alert that did not persist is not rendered as if it had. |
| packages/nightshade_app/lib/screens/transients/transients_screen.dart | accepted_modeled_approximation | app-team | reason: Same two shapes as the suggestions panel: `_dismissAlert` shows 'Could not dismiss this alert.' and the settings dialog's `_run` shows 'Could not save the alert settings.'. The alert list is rebuilt from `transientAlertStatesProvider`, so a failed dismiss leaves the alert visibly present rather than optimistically hidden. |
| packages/nightshade_app/lib/screens/tutorial/first_night_wizard.dart | accepted_modeled_approximation | app-team | reason: The wizard's `_run` catch shows 'Could not save tutorial progress. Please try again.' and skips the success callback, so a failed save does not close the wizard or advance the step. What is lost is first-run tutorial progress, which no capture or device path consults. |
| packages/nightshade_app/lib/services/file_download_service.dart | accepted_modeled_approximation | app-team | reason: The remaining empty catch in `downloadErrorMessage` is a type probe, not an error handler: it reads `(e as dynamic).userMessage` to prefer a typed exception's user-facing text and catches the NoSuchMethodError from exceptions that do not carry one. The download error itself is not swallowed — it is interpolated into the returned 'Download failed: $e'. The temp-file cleanup catch in the same file was given a log line instead. |
| packages/nightshade_app/lib/widgets/contextual_tour_prompt.dart | accepted_modeled_approximation | app-team | reason: Both empty catches wrap `tutorialProvider.startTutorial` and show 'Could not start the tour. Please try again.' before clearing the starting flag. The prompt is dismissed by the provider listener only once the authoritative tutorial state becomes active, so a failed start leaves the prompt on screen rather than implying the tour began. |
| packages/nightshade_app/lib/widgets/onboarding/onboarding_overlay.dart | accepted_modeled_approximation | app-team | reason: `_runTerminalAction` catch shows 'Could not save tour progress. Please try again.' and resets `_terminalAction`, leaving the overlay open on the same step. The overlay's step is driven by the tutorial provider, so a failed skip/finish does not visually complete onboarding. |
| packages/nightshade_app/lib/widgets/tour_selection_sheet.dart | accepted_modeled_approximation | app-team | reason: `_startTour` pops the sheet only on the success path; the catch shows 'Could not start the tour. Please try again.' with the sheet still open. A start/resume that failed is therefore never presented as having begun. |
| packages/nightshade_core/lib/src/providers/backend_provider.dart | accepted_modeled_approximation | core-team | reason: Neither catch swallows anything — both are resource cleanup that rethrows. `useNetworkBackend` disposes the candidate NetworkBackend's HTTP client and controllers when `_swapBackend` refuses the swap (e.g. an active sequence blocking a host change) and rethrows; `useLocalBackend` does the same for the FfiBackend. The swap failure reaches the caller, and the rollback to an authoritative DisconnectedBackend is what keeps `backend is NetworkBackend` from reading as connected. |
| packages/nightshade_core/lib/src/providers/glance_mode_provider.dart | accepted_modeled_approximation | core-team | reason: Nothing is swallowed: the catch restores `_requestedState` to the last confirmed value so a failed write cannot leave the notifier claiming a preference it did not persist, then rethrows. The caller (GlanceModeToggle) surfaces it. The separate load path in this file already logs its failure via developer.log. |
| packages/nightshade_core/lib/src/providers/plate_solver_provider.dart | accepted_modeled_approximation | core-team | reason: Both catches clear a UI busy flag (`rescanning`, `savingPreference`) and rethrow, so the solver rescan / preference-write failure reaches the settings screen intact. The generation guard means a superseded operation returns false instead of stamping stale state — it is not an error path. |
| packages/nightshade_core/lib/src/providers/settings_sections/app_settings_notifier.dart | accepted_modeled_approximation | core-team | reason: The remaining empty catch un-registers the echo-suppression command id when a remote settings POST fails, then rethrows. Leaving the id registered would make the notifier drop a later legitimate `settings.changed` event carrying that id, so the catch exists to keep the suppression list truthful; the write failure itself propagates. The device-local display-prefs overlay in this file was given a log line instead. |
| apps/desktop/lib/headless_api/handlers/calibration_handlers/defect_map_handlers.dart | accepted_modeled_approximation | headless-team | reason: Settings echo for the defect-map GET. `int.tryParse(stored[kernel] ?? '') ?? 3` reads a never-written settings row; 3 is `DefectMapKernelSize.k3`, the enum's own declared `fallback` and the kernel a default install actually runs with. It reports the configuration in force, not a measured value. |
| apps/desktop/lib/headless_api/handlers/device_handlers/focuser_handlers.dart | accepted_modeled_approximation | headless-team | reason: REST request-body defaults for OPTIONAL autofocus parameters (rSquaredThreshold 0.7, outer/inner crop 1.0/0.0, useBrightestNStars 0, focuserSettleTimeMs 500, backlash 350/0). Each is the documented default applied when the caller omits the field, before the run starts; required parameters go through `requireInt`, every optional read is range-clamped, and contradictory crop ratios are rejected with `BadRequestError`. None of these is a reading returned to the operator. |
| apps/desktop/lib/headless_api/handlers/planning_data_handlers.dart | accepted_modeled_approximation | headless-team | reason: `optionalQueryBool(query, 'all') ?? false` on the two bulk-DELETE endpoints (integration goals, target constraints). An absent `?all=` means "do not delete everything" — the conservative reading of an omitted destructive flag, with the specific `targetId`/`id` parameters still required to select rows. |
| apps/desktop/lib/headless_api/handlers/static_file_handlers.dart | accepted_modeled_approximation | headless-team | reason: `request.headers['accept'] ?? ''` at `/`. An empty Accept simply fails the `text/html` test and the request receives the JSON pointer, which the method's own doc comment names as the deliberate answer for health checks. No data is derived from the header. |
| apps/mobile/lib/screens/dashboard/tabs/mount_tab.dart | accepted_modeled_approximation | mobile | reason: `(_axisGenerations[axis] ?? 0) + 1` is a monotonic local guard counter used to discard responses from superseded slew-button presses. The first press on an axis has no prior generation, and 0 is its identity element. The counter is never displayed and never reaches the mount. |
| native/nightshade_native/bridge/src/api/devices/simulation.rs | accepted_modeled_approximation | core-hardening | reason: `state.last_position.unwrap_or(-1)` returns the last KNOWN filter-wheel slot while a failing position read is inside its backoff window, or -1 when no read has ever succeeded. -1 is this codebase's established "position unknown" sentinel and consumers honour it: `models/backend/device_status.dart` maps `position < 0` to a null filter name, and `device_service/connections.dart` polls while `position < 0` and then errors rather than accepting it. It is never rendered as a slot number. The underlying read failure is logged at warn level with the consecutive-failure count. |
| native/nightshade_native/bridge/src/api/difference_image.rs | accepted_modeled_approximation | core-hardening | reason: Both hits are inside this file's `#[cfg(test)] mod tests`, on `TempDir::drop` discarding `remove_dir_all`. The audit excludes `/test/` directories but not inline test modules, so this is a scan false positive. Discarding the cleanup result is deliberate and documented in place: a test asserting on a half-removed tree must fail on its own assertion, not on a cleanup error raised while a panic unwinds. |
| packages/nightshade_app/lib/screens/analytics/widgets/science_analytics_tab/navigation_and_snapshots.dart | accepted_modeled_approximation | app-team | reason: `(generatedAt ?? DateTime.now())` names the PDF file this call is writing at that moment — it timestamps the export action, which really is happening now. `generatedAt` exists only so tests can pin the filename. The report's contents come from the host's `generateObservationReport` bytes and an empty response throws `StateError`; no observation time is derived from this. |
| packages/nightshade_app/lib/screens/collaborative_sky/coimaging_create_sheet.dart | accepted_modeled_approximation | collab-sky | reason: `TextEditingController(text: widget.initialTargetName ?? '')` — an empty text field is the "nothing prefilled" state of a form the user is about to fill in. The session is created from what the user types, and the sheet validates the name on submit. |
| packages/nightshade_app/lib/screens/collaborative_sky/collab_mosaic_detail_screen.dart | accepted_modeled_approximation | collab-sky | reason: `(panel.assignedDisplayName ?? '').trim()` — an unassigned panel yields an empty credit string, which the card uses to OMIT the credit line. The empty value suppresses attribution rather than attributing the panel to anyone. |
| packages/nightshade_app/lib/screens/collaborative_sky/collaborative_sky_screen.dart | accepted_modeled_approximation | collab-sky | reason: `constellationDisplayNameProvider.valueOrNull ?? ''` is the local user's own display name while that async provider is still loading or unset. It labels the viewer, not any measured or hub-reported quantity, and the hub state itself is rendered from `hubAsync` with its own loading/error branches. |
| packages/nightshade_app/lib/screens/dashboard/widgets/capture_settings_card.dart | accepted_modeled_approximation | app-team | reason: `cameraBinningOptionsProvider(cameraState.deviceId ?? '')` — the empty string is the family key for "no camera selected", for which the provider yields no binning options and the selector renders nothing. The key is never displayed and never sent to a device. |
| packages/nightshade_app/lib/screens/equipment/widgets/connected_device_card/status_and_display.dart | accepted_modeled_approximation | app-team | reason: `equipmentCoverCalibratorCapabilitiesProvider(state.deviceId ?? '')` is the family key for "no cover/calibrator selected", which resolves to a null snapshot and renders the explicit 'Loading...'/'Unavailable' metric instead of any reading. The sibling brightness readout that DID print a fabricated `0/<max>` was fixed rather than registered: `_calibratorBrightnessLabel` now renders the same dash placeholder the card uses for every other unknown metric when the driver reported no level or no brightness scale. |
| packages/nightshade_app/lib/screens/imaging/widgets/annotation_panel_parts/status_widgets.dart | accepted_modeled_approximation | app-team | reason: `'${state.message ?? ''} ${state.errorDetails ?? ''}'` builds a lowercased haystack used only for substring tests that classify a plate-solve failure as "no solver installed". The concatenation is never shown; absent text simply fails to match and the generic failure message is shown instead. |
| packages/nightshade_app/lib/screens/mosaic/mosaic_contribute_sheet.dart | accepted_modeled_approximation | collab-sky | reason: Three `v ?? false` inside `Checkbox.onChanged` (credit, auto-upload, consent). Flutter passes null only for a tristate checkbox and these are binary, so the value is always the user's own click. `_loadedChoice` is set in the same callback so the explicit choice — not the default — is what gets persisted. |
| packages/nightshade_app/lib/screens/mosaic/mosaic_project_screen.dart | accepted_modeled_approximation | collab-sky | reason: `state.hubMosaicId ?? ''` — a project that has not been published to a hub has no hub id, and `_CollabStatusLine` treats the empty id as "not published" rather than as an id. Publish operations resolve the real id server-side and throw when the project is unpublished. |
| packages/nightshade_app/lib/screens/planner/widgets/scheduler_tab_content/decision_panel.dart | accepted_modeled_approximation | app-team | reason: `decision?.scoredCandidates.isNotEmpty ?? false` gates the Start control. No scheduler decision yet means the control stays disabled — fail-closed, and the panel's own empty state explains why rather than offering a start that would have nothing to run. |
| packages/nightshade_app/lib/screens/sequencer/tabs/templates_tab.dart | accepted_modeled_approximation | app-team | reason: Scan false positive on the word "placeholder" inside an explanatory comment. The comment states the opposite of a fallback: built-in templates are part of the product catalog and are always concatenated with DB templates, documenting the fix for a bug where they vanished once the user saved a custom template. |
| packages/nightshade_app/lib/screens/settings/settings_search_index.g.dart | accepted_modeled_approximation | app-team | reason: Generated settings search index. The hit is the human-readable LABEL of the real "Use simulated devices instead of real hardware" setting, indexed so the setting is findable by search. It is a string in a sorted list of search terms; no simulation code path exists in this file. |
| packages/nightshade_core/lib/src/models/scheduler/scheduler_status.dart | accepted_modeled_approximation | core-team | reason: `DateTime.tryParse(json['nextEvaluationAt'] as String? ?? '')` — `tryParse('')` returns null, so an absent or unparseable next-evaluation time stays null on the model. The empty string is a parser input, not a value: the field is nullable and the UI renders the absent case rather than a date. |
| packages/nightshade_core/lib/src/providers/dark_library_provider.dart | accepted_modeled_approximation | core-team | reason: `double.tryParse(stored[darkLibraryTempToleranceKey] ?? '')` yields a NULL tolerance when the setting has never been written, and the snapshot keeps it nullable so the UI shows the unset state. The sibling `frameCount ?? 0` that could report "Master dark created from 0 frames" was fixed rather than registered — the count is now nullable and the message drops the number when the host did not report one. |
| packages/nightshade_core/lib/src/providers/equipment/device_capability_provider.dart | accepted_modeled_approximation | core-team | reason: `maxBrightness ... ?? 0` is the "no brightness scale reported" sentinel for a cover-calibrator capability snapshot. The host always emits the field (`auxiliary_handlers.dart` serialises `status.maxBrightness`), and the one place the scale is rendered now checks for it: the equipment card's `_calibratorBrightnessLabel` shows the unknown-metric dash placeholder when `maxBrightness <= 0` instead of printing a ratio. Brightness itself stays nullable on the snapshot. |
| packages/nightshade_core/lib/src/providers/flat_wizard_provider.dart | accepted_modeled_approximation | core-team | reason: Two distinct sites. `_cancelToken?.isCancelled ?? false` — no token means no cancel was requested, the correct reading before a run starts. `config.gain ?? cameraState.gain ?? 0` is the gain KEY on the flat-history record, sent only on the remote `backend.recordFlat` path (the local `flatHistoryDao.recordCalibration` does not store gain at all). It is a learning/lookup key written and read through the same coalesce, so a camera that never reports gain stays self-consistent; it is not displayed as a measured gain, and a history-write failure is separately logged and surfaced as an operator warning. |
| packages/nightshade_core/lib/src/providers/focus_model_profile_data_provider.dart | accepted_modeled_approximation | core-team | reason: `dataResponse['profileId'] ?? modelResponse['profileId'] ?? ''` labels a READ-ONLY remote view. `parseRemoteFocusProfileData` is reached only from the two FutureProviders that hand the value straight to the focus UI; it never writes `FocusModelService._profileData` (that cache is populated only by the local disk-hydrate path), so an unlabelled remote response cannot overwrite or shadow a real profile's history. The data points and fitted model come from the host's own responses. |
| packages/nightshade_core/lib/src/providers/home_assistant_provider.dart | accepted_modeled_approximation | core-team | reason: `json['brokerPasswordConfigured'] as bool? ?? false` is a credential-PRESENCE flag, not a credential. An omitted field is read as "no broker password configured", which is the fail-closed direction: the UI then prompts the operator to enter one rather than claiming the broker is authenticated. |
| packages/nightshade_core/lib/src/providers/import_provider.dart | accepted_modeled_approximation | core-team | reason: The two `?? ''` normalise a possibly-missing `name`/`catalogId` so the very next block can test them — that block throws `FormatException` when both are empty or when `raHours`/`dec` are missing. The coalesce feeds a validity check and is discarded; it can never become a catalog row, and the coordinates are never coalesced at all. |
| packages/nightshade_core/lib/src/providers/planning_provider.dart | accepted_modeled_approximation | core-team | reason: Two sites, both string-keying rather than data. `pinnedFingerprint?.trim().toLowerCase() ?? ''` contributes an empty segment to the base64 backend-identity string used as a settings key when no certificate is pinned — a distinct, stable key for that backend. `id?.toString() ?? ''` is the documented encoding of "active project cleared", the explicit meaning of `setActiveProject(null)`. |
| packages/nightshade_core/lib/src/providers/profile_activation_writethrough.dart | accepted_modeled_approximation | core-team | reason: The phrase appears in the dartdoc for the `strict` parameter, documenting that LEGACY direct write-through callers keep their pre-existing lenient behaviour while `strict: true` logs and rethrows. The same paragraph directs new activation call sites to `activateProfileStrictTransactional`, which makes native acceptance a prerequisite for every SQLite/UI activation. It documents a compatibility flag, not an unhandled failure. |
| packages/nightshade_core/lib/src/providers/readiness_provider.dart | accepted_modeled_approximation | core-team | reason: `settings?.isLocationSet ?? false` — settings not yet loaded is read as "location not set", so readiness reports NOT ready. Fail-closed in the direction that blocks a session rather than green-lighting one, and the adjacent comment records the related invariant that only the (0,0) sentinel counts as unset so equator/prime-meridian sites stay valid. |
| packages/nightshade_core/lib/src/providers/session_replay_provider.dart | accepted_modeled_approximation | core-team | reason: `advertisedTotal ?? 0` in both paginators is Dart flow-analysis noise, not a fallback: the constructor rejects `maxPages <= 0`, so each loop runs at least once and page 0 unconditionally assigns `advertisedTotal = fetched.total`. The compiler cannot promote a local assigned inside a loop. Genuine short or inconsistent reads are surfaced explicitly — `isPartial`/`partialReason` on the events page, and a `StateError` on the frames page — not absorbed by this default. |
| packages/nightshade_core/lib/src/services/coimaging/coimaging_session_service.dart | accepted_modeled_approximation | collab-sky | reason: Three distinct shapes. `row?.contributedFrames/contributedIntegrationSeconds ?? 0` — no persisted membership row yet means no prior contribution, the correct baseline for a first join, and real contributions accumulate onto it. `self?.framingOffset* ?? 0` — a not-yet-populated `self` (before the hub round-trip) is on-axis, the documented pre-assignment framing. `atUtc ?? DateTime.now().toUtc()` — the baton evaluation asks "is the target up", so omitting the instant means "now"; that IS the intended evaluation time and the parameter exists to let tests and simulated longitudes pin it. |
| packages/nightshade_core/lib/src/services/device_service/filter_wheel_controls.dart | accepted_modeled_approximation | core-team | reason: `capabilities?.positionCount ?? 0` immediately feeds `if (positionCount > 0 && position >= positionCount) throw RangeError`. The 0 DISABLES an optional client-side range check for a wheel that never advertised a slot count; it is never used as a slot count and never displayed. An out-of-range move is still rejected by the driver, and the connection-changed guard above it throws first. |
| packages/nightshade_core/lib/src/services/device_service/focuser_rotator_controls.dart | accepted_modeled_approximation | core-team | reason: `capabilities?.maxIncrement ?? 0` with the same shape as the filter-wheel guard: the value is only read through `if (maxIncrement > 0 && delta.abs() > maxIncrement)`, so 0 means "no advertised increment limit to check against" rather than "the limit is zero". It is a guard input, not a reported capability. |
| packages/nightshade_core/lib/src/services/imaging_service/persistence.dart | accepted_modeled_approximation | core-team | reason: `appSettings?.isLocationSet ?? false` gates whether SITELAT/SITELONG are written into the FITS header. False OMITS the cards. The fallback direction is to write nothing rather than to stamp a guessed observing site into the file — exactly the behaviour this audit exists to enforce for header writes. |
| packages/nightshade_core/lib/src/services/mosaic/collaborative_mosaic_service.dart | accepted_modeled_approximation | collab-sky | reason: Two shapes, both bounded by a loud failure elsewhere. `master.wcsCd1_2/wcsCd2_1 ?? 0.0` in `_stampWcsHeader` matches the native reader's own convention (`mosaic.rs`: `header.get_float("CD1_2").unwrap_or(0.0)`), and a zero off-diagonal is the astrometric identity — no rotation or skew — for a CD matrix that carried only its diagonal; the same method throws `StateError` outright when CRVAL or the CD diagonal is missing, so an incomplete solve is never stamped. `int.tryParse(match?.group(1) ?? '') ?? 0` is the documented `(0, 0)` "geometry unreadable" return of `_fitsImageSize`, whose dartdoc states that contract. |
| packages/nightshade_core/lib/src/services/target_library_service.dart | accepted_modeled_approximation | core-team | reason: Remaining hits are the plan/progress counters (`totalPlannedSubs`, `capturedSubs`, `totalIntegrationSecs`, `goalIntegrationSecs`) and `isFavorite` from the host's `/api/targets` payload, which `target_handlers.dart` serialises straight from NOT NULL columns; an absent field means an out-of-contract payload, and 0/false is the same value a freshly created target carries, so it cannot be mistaken for progress that was measured and then lost. The RA/Dec coalesces in this same constructor were NOT registered — they minted a slewable 0h/+0deg target from a malformed payload and now throw a `FormatException` naming the missing field. |
| packages/nightshade_core/lib/src/utils/resilient_poll_stream.dart | accepted_modeled_approximation | core-team | reason: `timer?.isActive ?? false` in the scheduling guard. No timer means no poll is already scheduled, so scheduling proceeds — the correct reading of a null timer. Purely internal scheduling state; poll failures themselves are handled by the stream's own error path. |
| packages/nightshade_planetarium/lib/src/providers/target_queue_provider.dart | accepted_modeled_approximation | planetarium | reason: `displayName` falls back to the existing name and then to the resolved id so every synced row has a label, and `plannedExposures`/`completedExposures ?? ... ?? 0` matches `QueuedTarget`'s own constructor defaults; `completionPercentage` returns 0 when `plannedExposures <= 0`, so a target whose plan the sequencer did not report cannot render as progress. The `addedAt`/`completedAt` sites in this same constructor were NOT registered — they stamped `DateTime.now()` onto events that happened earlier, and now take a sequencer-reported instant when the payload carries one and stay null otherwise. |
| packages/nightshade_remote_protocol/lib/src/push/push_delivery_targets.dart | accepted_modeled_approximation | remote-control | reason: All four defaults feed `canDeliver => registeredDeviceCount > 0 && cloudDeliveryConfigured`. A payload missing any of them makes `canDeliver` false, and the settings UI then renders the `blockedReason` prose ("No paired device has registered for push") instead of a delivery claim. The fallback direction understates reach and prompts the operator to check; it can never assert that push will arrive when it will not. |
| packages/nightshade_ui/lib/src/utils/responsive_utils.dart | accepted_modeled_approximation | ui-design-system | reason: Layout arithmetic on caller-supplied optional geometry. `minWidth/minHeight ?? 0.0` before `math.min` encodes "an unspecified minimum is no minimum" (and the adjacent comment records why the min must never exceed the responsive max), and `left/top/right/bottom ?? horizontal/vertical ?? 0` in the padding resolver encodes "an unspecified edge gets no padding". These are pixel values chosen by the caller, not data read from the rig. |
| packages/nightshade_ui/lib/src/widgets/phd2/brain_settings_panel.dart | accepted_modeled_approximation | ui | reason: `_controllers['${axis}_${param.name}']?.text.trim() ?? ''` feeds `double.tryParse`, and the guard on the next line rejects the result when `value == null || !value.isFinite`. An empty or unparseable field therefore fails collection and nothing is submitted to PHD2 — the empty string is a parser input that cannot become a guide parameter. |
| packages/nightshade_updater/lib/src/models/update_state.dart | accepted_modeled_approximation | release-eng | reason: `availableUpdate?.version ?? ""` inside the `UpdateStatus.available` branch of a status-label getter, where `availableUpdate` is non-null by construction of that state. It is a null-safety filler on a display string, not an unknown version being asserted as known; the download/apply decisions read `availableUpdate` directly. |
| packages/nightshade_updater/lib/src/services/update_downloader.dart | accepted_modeled_approximation | release-eng | reason: `totalBytes = ... ?? 0` is reached only when neither Content-Range, nor Content-Length, nor a caller-supplied `expectedSize` gives a size. The progress callback is guarded by `if (onProgress != null && totalBytes > 0)`, so an unknown total reports NO progress rather than a fabricated percentage or a division by zero. Byte integrity is enforced separately by the checksum verification on completion. |
| packages/nightshade_updater/lib/src/widgets/update_manager_widget.dart | accepted_modeled_approximation | release-eng | reason: `state.availableUpdate?.version ?? ''` on three dialog/banner LABELS (download dialog, ready dialog, apply banner), each reached only from a state whose `availableUpdate` is set. The empty string is a null-safety filler on a caption; which artifact is downloaded, staged and applied is driven by the update state machine, never by these strings. The sibling `targetBuild ?? 0` in update_service.dart, which printed a build number the operator could compare against their running build, was fixed rather than registered. |
| packages/nightshade_app/lib/screens/analytics/session_elapsed.dart | accepted_modeled_approximation | app-team | reason: The current-time fallback applies only to an explicitly live session with no persisted end time. It is the instant at which elapsed time is being rendered, while the optional `now` argument exists so tests can pin that instant. Closed sessions use their persisted `endTime`, and inactive unclosed sessions use the last persisted frame time or report the duration as unknown. |
| packages/nightshade_app/lib/screens/framing/altitude_chart.dart | accepted_modeled_approximation | app-team | reason: `nowOverride?.call() ?? DateTime.now()` is the observation instant for a chart explicitly showing current target altitude. The override is a test seam; production must use the real UTC-convertible instant rather than the display clock, whose configured site offset is a rendering preference and would skew the astronomy calculation. |
| packages/nightshade_app/lib/screens/settings/widgets/imaging_naming_pattern.dart | accepted_modeled_approximation | app-team | reason: The fallback timestamp is generated solely for the live naming-pattern preview. It represents the moment the preview is rendered, is converted to UTC exactly like the capture pipeline, and is never persisted as capture metadata. The optional `now` parameter exists so tests can produce a stable preview. |
| packages/nightshade_app/lib/screens/weather/weather_screen/safety_and_settings.dart | accepted_modeled_approximation | app-team | reason: The fallback clock is used only to calculate how much time remains on a currently active snooze. Omitting `now` therefore means “remaining as of this render”; the persisted `snoozeUntil` remains authoritative, and the optional argument lets tests pin the comparison instant. |
| packages/nightshade_core/lib/src/providers/device_connection_progress_provider.dart | accepted_modeled_approximation | core-team | reason: `endSweep(at: null)` stamps the progress record at the instant the connection sweep actually ends. It does not infer hardware event time or overwrite a host-provided timestamp; the optional `at` parameter is a deterministic test seam. |
| packages/nightshade_core/lib/src/providers/equipment_fov_provider.dart | accepted_modeled_approximation | core-team | reason: The FOV overlay requires the real focal length and connected-camera geometry; aperture is not part of its field-of-view calculation. `TelescopeSpecs` nevertheless has a legacy non-null aperture field, so an unknown aperture is carried as zero while the overlay uses only `focalLengthMm`. The converter returns null—and draws no overlay—when focal length or sensor geometry is unknown, so zero is never substituted for an angular measurement. |
| native/nightshade_native/bridge/src/device_manager/ops/camera/download.rs:597:unimplemented_behavior | explicit_unsupported | native-team | reason: Test double (`OffsetSpy::unreadable` in `alpaca_bayer_gate_tests`) standing in for an Alpaca camera whose BayerOffsetX raises PropertyNotImplemented. The string is the simulated driver error the Bayer gate must resolve to an unknown pattern, not a Nightshade capability gap. |
| native/nightshade_native/bridge/src/device_manager/ops/camera/download.rs:598:unimplemented_behavior | explicit_unsupported | native-team | reason: Same test double as line 597, for the BayerOffsetY axis. |
| native/nightshade_native/bridge/src/device_manager/ops/camera/download.rs:705:unimplemented_behavior | explicit_unsupported | native-team | reason: Test double for a half-readable offset pair (X reads, Y raises PropertyNotImplemented); pins that one nameable axis is still an unknown pattern. |
| packages/nightshade_core/lib/src/providers/sequence/sequence_executor/sequence_serializer.dart:37:literal_null_coalesce | accepted_modeled_approximation | core-team | reason: `autoFocusOnFilterChange` falls back to false while `appSettingsProvider` is still loading, which is the value AppSettings itself defaults to. False means no autofocus node is injected after a filter change, so an unloaded settings snapshot serializes the sequence the user authored rather than one carrying synthetic nodes. |
| packages/nightshade_core/lib/src/providers/sequence/sequence_executor/sequence_serializer.dart:38:literal_null_coalesce | accepted_modeled_approximation | core-team | reason: `autoFocusEveryMinutes` falls back to 0 while `appSettingsProvider` is still loading. 0 is AppSettings' own default and the serializer reads it as 'no time-based autofocus injection', so the unloaded case adds nothing to the sequence. |
| packages/nightshade_core/lib/src/providers/sequence/remote_sequence_editor_sync.dart:33:comment_best_effort | accepted_modeled_approximation | core-team | reason: The comment RULES OUT a best-effort final save on dispose rather than performing one: reading a provider from a torn-down container throws, so the save is anchored to the backend-swap lifecycle hook instead, which runs while the outgoing transport is still alive. |
| packages/nightshade_app/lib/screens/planetarium/sky_imagery/planetarium_sky_geometry.dart:130:literal_null_coalesce | accepted_modeled_approximation | app-team | reason: Horizontal view mode returns null a few lines above when latitude is null, so `latitude ?? 0` is reachable only in equatorial mode, where SkyFovProjector does not consult latitude. |
| apps/mobile/lib/screens/replay/session_replay_screen.dart:57:unimplemented_behavior | explicit_unsupported | mobile-team | reason: Comment on a deliberate StateError. Session replay requires NetworkBackend; on the FFI backend the screen throws rather than rendering an empty timeline that would read as a session with no frames. |
| packages/nightshade_core/lib/src/database/daos/recipes_dao.dart | accepted_modeled_approximation | darkroom | reason: Raw-DDL DAO for `recipes`. Every hit is the `createdAt/now ?? DateTime.now()` write-timestamp parameter shared by `CampaignsDao` / `MosaicProjectsDao`: the caller may pin the instant (tests and replay do), and omitting it means "stamp this write now", which IS the intended value rather than a stand-in for a missing one. No error path is defaulted — a missing row, a half-specified branch, and a cyclic parent chain each raise a typed exception. |
| packages/nightshade_core/lib/src/database/daos/darkroom_jobs_dao.dart | accepted_modeled_approximation | darkroom | reason: Raw-DDL DAO for `darkroom_jobs`. Every hit is the `createdAt/now ?? DateTime.now()` write-timestamp parameter shared by `CampaignsDao` / `MosaicProjectsDao`: the caller may pin the instant, and omitting it means "stamp this transition now". Job state itself is never defaulted — an illegal transition, a missing row, and progress reported for a job no executor holds each raise a typed exception. |
| packages/nightshade_core/lib/src/database/daos/delivery_targets_dao.dart | accepted_modeled_approximation | darkroom | reason: Raw-DDL DAO for `delivery_targets`. Every hit is the `createdAt/now ?? DateTime.now()` write-timestamp parameter shared by `CampaignsDao` / `MosaicProjectsDao`. Config content is not defaulted — key material in `config_json` and a non-object config each raise a typed exception, and clearing `secret_ref` is its own explicit call rather than a null slipping through `COALESCE`. |
| packages/nightshade_core/lib/src/database/daos/delivery_journal_dao.dart | accepted_modeled_approximation | darkroom | reason: Raw-DDL DAO for `delivery_journal`. Every hit is the `now ?? DateTime.now()` write-timestamp parameter shared by `CampaignsDao` / `MosaicProjectsDao`. Delivery outcomes are not defaulted — recording an outcome for a file nobody attempted raises a typed exception, and a retry that omits the file size keeps the size already recorded rather than zeroing it. |
| apps/desktop/lib/desktop_last_gasp.dart:80:literal_null_coalesce | accepted_modeled_approximation | desktop-shell | reason: `decoded['at'] as String? ?? ''` feeds `DateTime.tryParse`, and the line below returns null for the whole record when that parse yields null. A record whose `at` is missing or non-String is therefore answered as "no usable record" - the same answer the torn-record path gives - and it is not silent: `readPreviousSession` reports an existing-but-unparseable record through `_onCritical` (pinned by 'a torn record is REPORTED, not just read as nothing'). |
| apps/desktop/lib/desktop_last_gasp.dart:96:empty_catch | accepted_modeled_approximation | desktop-shell | reason: The catch wraps `jsonDecode` of a record written by a process that is already dead; null is `DesktopSessionRecord.tryParse`'s documented "cut off mid-write" answer. The failure is not swallowed - its only caller `readPreviousSession` reports the unreadable record through `_onCritical`, which apps/desktop/lib/main.dart:243 wires to `logger.error(source: desktop.shutdown)`. |
| apps/desktop/lib/desktop_last_gasp.dart:376:literal_null_coalesce | accepted_modeled_approximation | desktop-shell | reason: `PlatformDispatcher.onError` returning false means NOT handled, so Flutter forwards the error to the default handler that prints it; false is the loud answer, not a swallow, and it is reached only when no previous handler was installed. The error has already been written to the session record by the `noteError` call on the line above. |
| apps/desktop/lib/headless_api/handlers/sequencer/sequencer_checkpoint_handlers.dart:195:literal_null_coalesce | accepted_modeled_approximation | desktop-shell | reason: `refocusAfter ?? false` reproduces `MeridianFlipConfig::default().refocus_after == false` (native/nightshade_native/sequencer/src/lib.rs:2545), the struct this payload is spread into at bridge/src/api/sequencer/lifecycle.rs:384. The sibling defaults on the same call match that struct too (pauseGuiding/autoCenter/resumeGuiding true, settleTimeSecs 10.0), and the flip geometry (mountId, targetName, targetRaHours, targetDecDegrees) is `requireString`/`requireDouble` with range gates, so no position is ever defaulted. |
| apps/desktop/lib/headless_api/handlers/sequencer/sequencer_config_handlers.dart:12:simulated_runtime_path | explicit_unsupported | desktop-shell | reason: Forwards to `api_sequencer_set_simulation_mode`, which returns `NotSupported` when `enabled && !cfg!(debug_assertions)` (native/nightshade_native/bridge/src/api/sequencer/runtime_config.rs:9), so a release appliance cannot be switched onto NullDeviceOps. The handler maps ONLY that typed variant to a 400 `simulation_mode_unavailable` and rethrows every other backend error instead of dressing a fault up as a policy refusal. |
| apps/desktop/lib/headless_api/handlers/sequencer/sequencer_config_handlers.dart:216:literal_null_coalesce | accepted_modeled_approximation | desktop-shell | reason: `raOnly ?? false` means dither on both axes, the standard 2D dither, and it is the value NetworkBackend always sends explicitly (packages/nightshade_core/lib/src/backend/network_backend/sequencer_operations.dart:288) so the default only serves a hand-rolled client. The four dither distances/timeouts on the same call are `requireDouble`, so an omitted value is a 400 rather than a dither with invented pixels. |
| apps/desktop/lib/headless_api/handlers/sequencer/sequencer_lifecycle_handlers.dart:185:simulated_runtime_path | explicit_unsupported | desktop-shell | reason: Comment text, not a call: it records why the bare load->start path issues `sequencerSetSimulationMode(false)` four lines down. Adjudicated with that call, see the row for line 189. |
| apps/desktop/lib/headless_api/handlers/sequencer/sequencer_lifecycle_handlers.dart:189:simulated_runtime_path | explicit_unsupported | desktop-shell | reason: Passing `false` installs UnifiedDeviceOps - REAL hardware routed through DeviceManager (native/nightshade_native/bridge/src/api/sequencer/runtime_config.rs:19); the release-build refusal there gates `enabled == true` only, so this call is never a simulation path. Without it the headless load->start path enters the native executor with no device ops and the first TakeExposure fails "No device operations configured". |
| packages/nightshade_bridge/lib/src/event/sequencer.dart:363:nonconvergence_best_guess | accepted_modeled_approximation | core-hardening | reason: Doc comment on the nullable wire field `likelyCauseLabel`, not a runtime fallback. Null there is a distinct typed state, not a guess passed off as a verdict: `LikelyCauseExt.fromLabel(null)` returns null, and packages/nightshade_core/lib/src/models/forensics/frame_forensics.dart:73 documents null as "classifier did not run" versus `LikelyCause.unknown` as "ran, but no signal cleared the heuristics". |
| packages/nightshade_bridge/lib/src/event/sequencer.freezed.dart:1651:nonconvergence_best_guess | auto_generated | core-hardening | reason: freezed codegen copy of the `likelyCauseLabel` doc comment from packages/nightshade_bridge/lib/src/event/sequencer.dart:363; running build_runner reproduces the line verbatim. |
| packages/nightshade_planetarium/lib/src/catalogs/star_catalog/hyg_parser.dart:125:literal_null_coalesce | accepted_modeled_approximation | planetarium | reason: `int.tryParse(parts[0]) ?? 0` lands on exactly the id the next check drops: line 132 returns null for `hygId == 0` because HYG row 0 is the heliocentric Sun placeholder. A row with an unreadable id column is therefore skipped by the loader, never rendered under a borrowed identity, and it cannot pollute the `compPrimary` join because `_isUnnamedComponent` also excludes 0. |
| packages/nightshade_planetarium/lib/src/catalogs/star_catalog/hyg_parser.dart:218:literal_null_coalesce | accepted_modeled_approximation | planetarium | reason: `constellation` is never null on this path - line 123 returns null unless `parts.length >= 30`, so the `parts.length > 29` ternary at line 154 always takes the String branch. Were the column empty, `_getConstellationGenitive('')` returns its own input ('') and the trailing space is removed by `starName.trim()` at line 247, so a Bayer star keeps its letter instead of gaining an invented constellation. |
| packages/nightshade_planetarium/lib/src/catalogs/star_catalog/hyg_parser.dart:223:literal_null_coalesce | accepted_modeled_approximation | planetarium | reason: Same guard as line 218 - `parts.length >= 30` is proven at line 123, so `constellation` is non-null here. `constellationFullName('')` returns the trimmed input by contract (packages/nightshade_planetarium/lib/src/catalogs/constellation_names.dart:102, documented for catalogs that store junk), so a Flamsteed star reads '61 ' -> '61' rather than being named after a constellation it was never assigned. |
| packages/nightshade_planetarium/lib/src/catalogs/star_catalog/hyg_parser.dart:242:literal_null_coalesce | accepted_modeled_approximation | planetarium | reason: 0 is HYG's own "no primary" sentinel for `comp_primary`, and `_isUnnamedComponent` (line 101) requires `compPrimary != 0`, so an absent or unreadable column disables the component-naming join for that row and it keeps the name its own columns earned. The row is still loaded with its real position; nothing is inherited from a system it was not matched to. |
| packages/nightshade_planetarium/lib/src/widgets/object_details_panel/header_sections.dart:282:literal_null_coalesce | accepted_modeled_approximation | planetarium | reason: `sizeArcMin`/`minorAxisArcMin` are null for catalogue entries that publish no angular size; 0 falls straight into the `largestArcMin <= 0` branch on the next line, which returns the documented 1.5 deg default. The number only sizes the 80x80 DSS2-red preview cutout requested at line 232 - imaging framing, the FOV overlay and mosaic planning read the equipment profile, not this function. |

### native sequencer — executor/start + instructions (deslop v6 adjudication)

| native/nightshade_native/sequencer/src/executor/monitoring.rs | accepted_modeled_approximation | core-hardening | reason: Recovery escalation + tracking restore. Every finding here (306, 347, 422, 455, 464, 472, 490, 493, 545, 548, 553) is `let _ = <event_tx>.send(ExecutorEvent::Error / StateChanged / RecoveryGaveUp / RecoveryCompleted)` on the `broadcast::Sender<ExecutorEvent>` built at executor/mod.rs:190 with capacity 256. Tokio's broadcast `send` returns Err ONLY when the subscriber count is zero (a slow subscriber lags on the receive side, it never fails the send), so the discarded value means "nobody was listening", which is the normal headless case. The seven `Error { message }` sends (306, 347, 422, 455, 464, 472, 545) are each preceded by a `tracing::warn!`/`error!` carrying that same text. The other four carry no text at all: the two `StateChanged` sends (490, 548) carry only an `ExecutorState`, and `RecoveryGaveUp` (493) / `RecoveryCompleted` (553) carry only the `RecoveryContext`; for those the human-readable reason is already on the wire and in the log via the `Error` send immediately beside them (422 and 545 respectively). In every case the authoritative state is committed BEFORE the send: `s.state`, `s.progress` (the snapshot `sequencer_get_status()` polls), `s.gave_up`, `s.is_cancelled`, plus the real `park_and_close_safe_state` sweep on the unattended branch. A lost notification therefore cannot lose a transition or a safing step. |
| native/nightshade_native/sequencer/src/executor/start/command_handler.rs | accepted_modeled_approximation | core-hardening | reason: Runtime-command handler. Every `let _` finding is `event_tx.send(ExecutorEvent::RuntimeConfigUpdated / StateChanged / skip_to_node_accepted_event)` on the executor broadcast channel (executor/mod.rs:190), which errors only when no receiver is subscribed. In every arm the command's EFFECT is committed before the send — `runtime_config.write()`, `state.write()`, `progress_for_commands.write().state`, the trigger manager's `state()` lock, or the dedicated `*_for_cmd` slot — and those Arcs are what the node tree, the trigger monitor and `sequencer_get_status()` read. The event is only the push notification for a UI that may not be attached. Lines 241 and 365 have their own rows. |
| native/nightshade_native/sequencer/src/executor/start/progress_callback.rs | accepted_modeled_approximation | core-hardening | reason: Node-progress callback. Findings 84, 94, 131, 234, 237, 329, 340 are `event_tx_clone.send(NodeStarted / TargetStarted / TargetCompleted / ExposureStarted / ExposureCompleted / NodeProgress / ProgressUpdated)` on the executor broadcast channel (executor/mod.rs:190); Err means zero subscribers. The callback first mutates the shared `SequenceProgress` (`prog`) and the `started_nodes` / `node_integration_credited` maps under their locks, and that same `SequenceProgress` is what `get_progress()` and the status API return, so the poll surface stays correct with nothing subscribed. Line 263 has its own row. |
| native/nightshade_native/sequencer/src/executor/start/recovery_driver.rs | accepted_modeled_approximation | core-hardening | reason: Recovery driver loop. Findings 96, 98, 208, 252, 281, 301, 412, 414, 469, 482, 492, 516, 518 are `recovery_driver_event_tx.send(StateChanged / RecoveryStarted / RecoveryProgress / RecoveryCompleted / RecoveryGaveUp / Error)` on the executor broadcast channel (executor/mod.rs:190) — Err only when no receiver is subscribed. Each is emitted AFTER the driver has written the authoritative state: `recovery_driver_state`, `recovery_driver_progress` (polled by the status API), `recovery_driver_current`, `recovery_driver_gave_up`, `recovery_driver_is_cancelled`, and after `park_and_close_safe_state` has actually run on the give-up branch; every failure branch also logs through `tracing::error!`. Line 128 has its own row. |
| native/nightshade_native/sequencer/src/executor/start/trigger_monitor.rs | accepted_modeled_approximation | core-hardening | reason: Trigger monitor poll loop. The remaining findings are `event_tx_clone2.send(ExecutorEvent::Error / StateChanged / TriggerFired / NodeCompleted)` on the executor broadcast channel (executor/mod.rs:190), whose `send` fails only with zero subscribers. Every pause path stores `is_paused_for_triggers`, writes `state_clone` AND `progress_for_triggers.write().state` before the send — that progress mirror exists precisely so a paused run stops reporting `running` to the status API — and the ParkAndAbort / AbortAndPark branches run `park_and_close_safe_state` (or `try_park_with_retry`) and `terminate_with` regardless of delivery. Every send that reports a FAILURE is now paired with a `tracing::error!`/`warn!` carrying the same text, so the park, cover-close, dome-close and guider-stop verdicts of an unattended safing sweep are on disk even with no subscriber. Lines 124, 150, 455, 740, 981, 1077, 1284, 1550, 1790, 1960 and 2088 have their own rows. |
| native/nightshade_native/sequencer/src/executor/start/trigger_monitor.rs:124:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `heartbeat_tx.send(heartbeat)` on the `watch::Sender<u64>` created at executor/start.rs:771 and read by `trigger_monitor_stall_watchdog` inside the same `select!`. Err means the watchdog's receiver was dropped, which only happens once that select has already resolved and the run is terminating — there is nothing left to declare stalled. The value is a monotonically increasing counter re-sent on the next `check_interval` tick, so a single missed send cannot fake a stall. |
| native/nightshade_native/sequencer/src/executor/start/trigger_monitor.rs:150:unwrap_or_literal | accepted_modeled_approximation | core-hardening | reason: `last_safety_poll_at.map(last -> last.elapsed() >= safety_check_interval).unwrap_or(true)`. `last_safety_poll_at` is `Option<Instant>` and is None only before the first safety poll of a run, so `true` makes the FIRST tick poll weather/safety immediately instead of skipping it — the fail-closed direction. Every later tick takes the `Some` branch. |
| native/nightshade_native/sequencer/src/executor/start/trigger_monitor.rs:455:unwrap_or_literal | accepted_modeled_approximation | core-hardening | reason: `last_mount_poll_at.map(last -> last.elapsed() >= mount_poll_interval).unwrap_or(true)`, the mount-tracking twin of line 150. None means no mount poll has happened yet this run, and `true` polls `mount_is_tracking` on the first tick rather than deferring tracking-loss detection by one interval. |
| native/nightshade_native/sequencer/src/executor/start/trigger_monitor.rs:740:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `decision_tx_for_lifecycle.send(stamped)` on `DecisionSender = broadcast::Sender<DecisionEvent>` (decision.rs:163), consumed by the replay-debug recorder. Err means no recorder is subscribed (decision logging off), and the send is already gated on that preference. The trigger firing is independently recorded by the `TriggerFired` executor event at 717 and the `tracing::warn!` at 710, so the trigger's audit trail does not depend on this channel. |
| native/nightshade_native/sequencer/src/executor/start/trigger_monitor.rs:982:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `park_and_abort_done_for_triggers.send(true)` on the `watch::channel(false)` created at executor/start.rs:657. Its two receivers are the execution future (executor/start.rs) and the checkpoint stream; Err means both have already been dropped, so no task is waiting on the handshake. Both waiters now log when their `wait_for` returns Err, so a broken handshake is visible from the other side. The park/close sweep itself has already run above and its per-device failures were emitted as `ExecutorEvent::Error`. |
| native/nightshade_native/sequencer/src/executor/start/trigger_monitor.rs:1078:unwrap_or_literal | accepted_modeled_approximation | core-hardening | reason: `step: step.unwrap_or(0)` where `step` comes from `parse_autofocus_detail(&detail_str)`, which returns None when the sweep's progress string carries no parseable step number. Zero renders as "step 0" in the `ProgressDetail::Autofocus` payload — a display field only. The sweep's real verdict is `af_result.status`, emitted as `NodeCompleted` at 1121 and branched on at 1126. |
| native/nightshade_native/sequencer/src/executor/start/trigger_monitor.rs:1289:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `decision_tx_for_lifecycle.send(continuation.decision)` on the replay-debug broadcast channel (decision.rs:163); Err means no recorder is subscribed. The same continuation verdict is logged at 1285 and emitted to the operator as `ExecutorEvent::Error` at 1286, so the autofocus-continuation decision is not lost with the recorder absent. |
| native/nightshade_native/sequencer/src/executor/start/trigger_monitor.rs:1558:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `park_and_abort_done_for_triggers.send(true)` on the watch channel from executor/start.rs:657, on the FlipFailure AbortAndPark exit. Same mechanism as line 981: Err means the execution future and checkpoint stream have both gone, so nothing awaits the handshake, and the park itself already ran through `try_park_with_retry` with its failure emitted as `ExecutorEvent::Error` at 1533. |
| native/nightshade_native/sequencer/src/executor/start/trigger_monitor.rs:1803:unwrap_or_default | accepted_modeled_approximation | core-hardening | reason: `result.message.unwrap_or_default()` inside the "DriftLimit recenter failed" message. `InstructionResult.message` is `Option<String>` and this branch is already gated on `!result.success`, so the failure itself is established; a driver that returned no detail contributes an empty suffix rather than an invented cause. The run is paused by the lines above regardless. |
| native/nightshade_native/sequencer/src/executor/start/trigger_monitor.rs:1973:unwrap_or_default | accepted_modeled_approximation | core-hardening | reason: `result.message.unwrap_or_default()` inside the "SlewToGapAndContinue failed" message, in the `!result.success` branch. Same contract as 1783: the prefix states the failure, the Option only carries an optional driver detail, and the pause plus `progress_for_triggers` state write happen before this event is built. |
| native/nightshade_native/sequencer/src/executor/start/trigger_monitor.rs:2101:unwrap_or_default | accepted_modeled_approximation | core-hardening | reason: `result.message.unwrap_or_default()` inside the "backup filter could not be selected" message, reached only when `result.status != NodeStatus::Success`. The missing-detail case renders an empty suffix; the filter-change failure is also logged with the full `result.message` debug at 2076. |
| native/nightshade_native/sequencer/src/executor/start/command_handler.rs:241:unwrap_or_literal | accepted_modeled_approximation | core-hardening | reason: `check.as_ref().map(c -> c.is_active()).unwrap_or(false)` is an argument to `tracing::info!`. `check` is `Option<QualityCheckConfig>`; None means the operator CLEARED the default quality check, and "active=false" is the truthful rendering of a cleared Option. The value stored into `runtime_config.default_quality_check` on the preceding lines is the Option itself, unchanged. |
| native/nightshade_native/sequencer/src/executor/start/command_handler.rs:365:unwrap_or_literal | accepted_modeled_approximation | core-hardening | reason: `cfg_for_log.as_ref().map(c -> c.enabled).unwrap_or(false)` is an argument to `tracing::info!`. `config` is `Option<AdaptiveExposureConfig>`; None means adaptive exposure was cleared, so "enabled=false" is what the run will actually do. The Option itself is written to `default_adaptive_for_cmd` unchanged on the lines above. |
| native/nightshade_native/sequencer/src/executor/start/progress_callback.rs:263:unwrap_or_literal | accepted_modeled_approximation | core-hardening | reason: `node_integration_credited.write().remove(&update.node_id).unwrap_or(0.0)`. A missing key means the node never reported per-frame integration, so zero seconds have been credited so far and the whole burst is credited once by `prog.completed_integration_secs += (exposure_secs - already).max(0.0)` on the next line. The `.max(0.0)` also prevents a node that over-reported per-frame from clawing integration back. |
| native/nightshade_native/sequencer/src/executor/start/recovery_driver.rs:128:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `recovery_driver_decision_tx.send(decision_event)` on the replay-debug broadcast channel (decision.rs:163); Err means no recorder is subscribed. Recovery entry is independently published as `RecoveryStarted` at 98, stored in `recovery_driver_current`, and stamped into `recovery_driver_progress.message` at 93, so the entry is recoverable from the run record without this channel. |
| native/nightshade_native/sequencer/src/executor/start.rs:128:unwrap_or_default | accepted_modeled_approximation | core-hardening | reason: `self.sequence.as_ref().map(s -> ...collect()).unwrap_or_default()` yields an EMPTY `HashMap<NodeId, (f64, Option<String>)>` when no sequence is loaded (executor in its initial state). The consumer is the trigger-action context, which then simply finds no TakeExposure metadata for any node id; an empty index is the correct sentinel because there are no exposure nodes to describe. |
| native/nightshade_native/sequencer/src/executor/start.rs:175:unwrap_or_default | accepted_modeled_approximation | core-hardening | reason: `target_node_metadata` built from `self.sequence.as_ref().map(...)` collapses to an empty `HashMap` when no sequence is loaded. The consumer is the progress callback, which only emits `TargetStarted`/`TargetCompleted` for node ids present in the map, so an empty map means no target announcements — never an invented target name (unnamed targets are deliberately excluded by the `target_name.trim().is_empty()` guard for the same reason). |
| native/nightshade_native/sequencer/src/executor/start.rs:531:unwrap_or_literal | accepted_modeled_approximation | core-hardening | reason: `context.default_quality_check.as_ref().map(c -> c.is_active()).unwrap_or(false)` is an argument to the "Executor started" `tracing::info!`. None means no default quality check is configured for the run, so `grading_active=false` is exactly what the run will do; the grading path itself branches on the same Option, not on this log value. |
| native/nightshade_native/sequencer/src/executor/start.rs:547:unwrap_or_literal | accepted_modeled_approximation | core-hardening | reason: The same `default_quality_check.as_ref().map(c -> c.is_active()).unwrap_or(false)` feeding the `"grading_active"` field of the replay-debug "started" lifecycle decision. None means grading was not configured, so `false` records what happened; the decision payload is descriptive and no code branches on it. |
| native/nightshade_native/sequencer/src/executor/start.rs:734:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `execution_quiesced_tx.send(true)` on the `watch::channel(false)` created at line 656. Its only receiver is `execution_quiesced_for_triggers`, handed to the trigger monitor for `cancel_and_wait_for_execution`. Err means the monitor has already ended, so nothing is waiting for the node tree to quiesce. The watch retains the last value for any receiver that is still alive, so the signal cannot be missed by a live waiter. |
| native/nightshade_native/sequencer/src/executor/start.rs:950:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `event_tx_for_watchdog.send(ExecutorEvent::Error { message })` when the trigger-monitor stall watchdog fires, on the executor broadcast channel (executor/mod.rs:190) — Err only with zero subscribers. The same message is written by `tracing::error!` immediately above, and the watchdog's real effect is returning `Vec::new()` from the select branch, which the fail-closed handler below turns into a cancellation of the whole run. |
| native/nightshade_native/sequencer/src/executor/start.rs:998:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `event_tx.send(ExecutorEvent::Error { "Safety monitoring failed — sequence aborted." })` on the executor broadcast channel; Err only with zero subscribers. `tracing::error!` above records the same fact, and the branch has already stored `is_cancelled` and goes on to force `NodeStatus::Failure`, so the abort happens whether or not anyone was listening. |
| native/nightshade_native/sequencer/src/executor/start.rs:1003:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `let _ = (&mut execution).await;` in the fail-closed branch taken when the trigger monitor exits unexpectedly. Discarding the node tree's `NodeStatus` is the POINT: `is_cancelled` was just set and the branch returns `NodeStatus::Failure` unconditionally, because a tree that finished while safety monitoring was dead must not be reported as a success. The await is only there to let the tree's hardware-cancellation cleanup finish before teardown. |
| native/nightshade_native/sequencer/src/executor/start.rs:1042:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `event_tx.send(ExecutorEvent::Error { message })` when a trigger recovery action is abandoned after `TRIGGER_ACTION_QUIESCE_MAX_SECS`, on the executor broadcast channel; Err only with zero subscribers. `tracing::error!` above carries the identical text, and the timeout `break` that ends the wait runs regardless of delivery. |
| native/nightshade_native/sequencer/src/executor/start.rs:1104:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `event_tx.send(ExecutorEvent::Error { message })` for a sequence that finished without executing part of its tree. Err only with zero subscribers, and the same message is both logged by `tracing::error!` and stored in `unreachable_failure_reason`, which becomes the `SequenceFailed { error }` payload below — so the run record carries the reason even with no live listener. |
| native/nightshade_native/sequencer/src/executor/start.rs:1165:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `event_tx.send(ExecutorEvent::SequenceCompleted)` on the executor broadcast channel; Err only with zero subscribers. The terminal state has already been written to `state`/`progress` (read by `sequencer_get_status()`), the checkpoint was marked completed just above with its own error logging, and the "completed" replay-debug lifecycle decision is emitted on a separate channel immediately after. |
| native/nightshade_native/sequencer/src/executor/start.rs:1202:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `event_tx.send(ExecutorEvent::SequenceFailed { error })` on the executor broadcast channel; Err only with zero subscribers. The `error` string is computed first (structural reason, else the drained last `InstructionFailed`, else "Sequence failed") and is passed straight into the `emit_lifecycle_decision("failed", ...)` payload below, so the failure reason is persisted through the decision channel and the progress snapshot independently of this send. |
| native/nightshade_native/sequencer/src/executor/start.rs:1217:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `event_tx.send(ExecutorEvent::Error { "Sequence cancelled" })` on the executor broadcast channel; Err only with zero subscribers. The cancellation is already the resolved `NodeStatus::Cancelled` that produced `final_state`, and the matching "cancelled" lifecycle decision is emitted right below, so the state and the audit trail do not depend on this notification. |
| native/nightshade_native/sequencer/src/executor/start.rs:1233:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `event_tx.send(ExecutorEvent::StateChanged(final_state))` on the executor broadcast channel; Err only with zero subscribers. `final_state` was already written into `state` and `progress` above, and `sequencer_get_status()` reads that snapshot rather than replaying events, so a run with no subscriber still reports its terminal state correctly. |
| native/nightshade_native/sequencer/src/executor/start.rs:1259:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `supervisor_event_tx.send(ExecutorEvent::SequenceFailed { "Sequencer panicked: ..." })` from the `catch_unwind` supervisor; Err only with zero subscribers. The panic is logged with `target: "supervisor"` immediately above and `supervisor_state` / `supervisor_progress` are set to `Failed` before the send, so a panicked executor can never keep reporting Running to the status API. |
| native/nightshade_native/sequencer/src/executor/start.rs:1262:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `supervisor_event_tx.send(ExecutorEvent::StateChanged(ExecutorState::Failed))` after a panic; Err only with zero subscribers. The same `Failed` state was already committed to `supervisor_state` and `supervisor_progress.state` on the preceding lines, which is what the status API returns. |
| native/nightshade_native/sequencer/src/executor/start.rs:1265:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `run_completion_tx.send(())` on the `oneshot::channel()` from line 66. The only receiver is `SequenceExecutor::stop()` (executor/lifecycle.rs:105), which awaits it and maps a dropped sender to the explicit error "Executor task ended without confirming hardware cancellation cleanup". Err here means no `stop()` is waiting, and the one caller that does wait already reports the dropped-sender case itself. |
| native/nightshade_native/sequencer/src/executor/preflight.rs:400:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `std::fs::remove_file(&probe)` cleaning up the `.nightshade-write-probe-<uuid>` file the writability check just created. The check's verdict comes from the `std::fs::write` above, which returns a typed failure message when the save path is not writable; the unlink is cleanup only. A failed unlink leaves one hidden zero-byte file with a UUID name that no save-path resolver, catalog scan or FITS reader ever opens. |
| native/nightshade_native/sequencer/src/executor/start/preflight.rs:124:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `self.event_tx.send(ExecutorEvent::Error { message })` announcing unreachable instructions at preflight, on the executor broadcast channel (executor/mod.rs:190) — Err only with zero subscribers. `tracing::error!` above carries the same text, and the same condition is re-evaluated at the end of the run (executor/start.rs:1104) where it converts a Success into a Failure, so the finding is not lost if nothing was subscribed at start time. |
| native/nightshade_native/sequencer/src/executor/start/preflight.rs:195:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `self.event_tx.send(ExecutorEvent::Error { "no ASTAP star database found ..." })` on the executor broadcast channel; Err only with zero subscribers. The identical warning is emitted by `tracing::warn!` above, and this preflight is advisory: the CenterTarget instruction still fails loudly at solve time with its own typed `InstructionResult::failure`. |
| native/nightshade_native/sequencer/src/executor/start/run_setup.rs:139:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `self.event_tx.send(ExecutorEvent::Error { msg })` reporting a trigger threshold that was clamped at load time, on the executor broadcast channel; Err only with zero subscribers. The clamp itself has already been applied to the trigger the manager holds (the warning is generated from `warning.original` / `warning.clamped_to`), so the run behaves identically whether or not the advisory reaches a listener. |
| native/nightshade_native/sequencer/src/executor/trigger_context.rs:740:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `tx.send(event)` for a `DecisionCategory::SystemEvent` on `DecisionSender = broadcast::Sender<DecisionEvent>` (decision.rs:163). Err means no replay-debug recorder is subscribed. This helper only mirrors lifecycle facts that are already carried by the executor's own `ExecutorEvent` stream and the progress snapshot; nothing branches on the decision feed. |
| native/nightshade_native/sequencer/src/flat_wizard/mod.rs:491:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `event_tx.send(ExecutorEvent::Error { message })` when the flat-wizard save-path renderer fails to build, on the executor broadcast channel (executor/mod.rs:190) — Err only with zero subscribers. The same `message` is logged by `tracing::error!` above and returned as `InstructionResult::failure(message)` on the next line, so the caller fails with the reason regardless of delivery. |
| native/nightshade_native/sequencer/src/instructions/autofocus.rs:200:unwrap_or_default | accepted_modeled_approximation | core-hardening | reason: `config.filter.as_deref().unwrap_or_default().trim()` inside the "Autofocus is configured to use filter X but no filter wheel is connected" failure text. The enclosing `else if config.filter.as_deref().is_some_and(name -> !name.trim().is_empty())` guard proves the Option is Some and non-blank on this branch, so the default is unreachable and the message always names the real filter. |
| native/nightshade_native/sequencer/src/instructions/context.rs:102:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `event_tx.send(ExecutorEvent::InstructionFailed { node_name, message })` on the executor broadcast channel. This one has a guaranteed subscriber by construction: executor/start.rs subscribes `instruction_failure_rx` BEFORE the node tree runs, precisely so `last_instruction_failure()` can drain the buffered failure and use it as the `SequenceFailed { error }` reason. The `let _` therefore covers only the no-executor case (a directly driven instruction), where `self.message` is still returned to the caller. |
| native/nightshade_native/sequencer/src/instructions/context.rs:440:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `tx.send(event)` in `emit_decision`, after the `let Some(tx) = self.decision_tx.as_ref() else { return }` guard, on the replay-debug broadcast channel (decision.rs:163). Err means no recorder is subscribed; the `sequence_run_id` stamping above exists for the recorder's foreign key and nothing in the instruction path reads back the decision feed. |
| native/nightshade_native/sequencer/src/instructions/cooling.rs:205:unwrap_or_literal | accepted_modeled_approximation | core-hardening | reason: `config.target_temp.unwrap_or(20.0)` in the WarmCamera instruction. `target_temp` is the operator's optional override; None means "use the conventional ambient warm-up target", and 20 °C is the documented shutdown-prep temperature for every cooled sensor in the supported matrix. The value only sets the ramp target and step count for `camera_set_cooler`, and the real sensor temperature is read back from `camera_get_temperature` above, which fails loudly. |
| native/nightshade_native/sequencer/src/instructions/dither.rs:225:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `event_tx.send(ExecutorEvent::Error { "secondary camera did not clear ..." })` on the executor broadcast channel (executor/mod.rs:190) — Err only with zero subscribers. The same condition is logged by `tracing::warn!` above with the barrier's `forced_proceed_count()`, and the dual-rig barrier's forced-proceed counter keeps the running total for the session summary. |
| native/nightshade_native/sequencer/src/instructions/dome.rs:114:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `event_tx.send(ExecutorEvent::Error { msg })` for an unconfirmed dome shutter move, on the executor broadcast channel; Err only with zero subscribers. The typed outcome is what protects the rig: the function returns `DomeShutterWaitOutcome::Unconfirmed`, which the unattended safe-state sweep treats as a NOT-closed dome, and `tracing::warn!` records the same text. |
| native/nightshade_native/sequencer/src/instructions/expose.rs:128:comment_best_effort | accepted_modeled_approximation | core-hardening | reason: Regex false positive. The matched word "placeholders" in the doc comment for `execute_exposure_with_renderer` refers to the per-frame TOKENS in a user's `ExposureConfig.save_to` template (frame number, filter, target), not to unimplemented behavior. The function is fully implemented and its render failures abort the exposure with a typed `InstructionResult::failure` at line 630 rather than saving to a wrong path. |
| native/nightshade_native/sequencer/src/instructions/expose.rs:515:unwrap_or_literal | accepted_modeled_approximation | core-hardening | reason: `guard.as_ref().map(s -> s.save_original).unwrap_or(false)` where `guard` is the `ctx.defect_map_apply` read guard. None means NO defect map is installed for this run, so no correction will be applied and there are no pre-correction pixels worth snapshotting; `false` skips a ~50 MB per-frame clone that would have nothing to archive. The corrected/original archiving below is driven by the same Option. |
| native/nightshade_native/sequencer/src/instructions/expose.rs:630:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `event_tx.send(ExecutorEvent::Error { error_message })` when a save-path template fails to render, on the executor broadcast channel; Err only with zero subscribers. `tracing::error!` records the identical text and the very next line returns `InstructionResult::failure(error_message)`, which aborts the exposure rather than writing the frame to a fallback path. |
| native/nightshade_native/sequencer/src/instructions/expose.rs:740:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `event_tx.send(ExecutorEvent::Error { error_message })` when the save directory cannot be created, on the executor broadcast channel; Err only with zero subscribers. The identical message is logged by `tracing::error!` and returned as `InstructionResult::failure`, so the frame is never silently dropped — the sequence aborts. |
| native/nightshade_native/sequencer/src/instructions/expose.rs:859:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `event_tx.send(ExecutorEvent::Error { error_message })` when the FITS write itself fails, on the executor broadcast channel; Err only with zero subscribers. `tracing::error!` above holds the full path and error, and `return InstructionResult::failure(error_message)` on the next line fails the exposure, so a failed write can never be counted as a saved frame. |
| native/nightshade_native/sequencer/src/instructions/expose.rs:998:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `event_tx.send(ExecutorEvent::Error { error_message })` for a dither failure between frames, on the executor broadcast channel; Err only with zero subscribers. The same text is logged by `tracing::error!` and returned as `InstructionResult::failure`, which ends the burst so the sequence's recovery and guiding triggers can engage. |
| native/nightshade_native/sequencer/src/instructions/filter.rs:137:unwrap_or_literal | accepted_modeled_approximation | core-hardening | reason: `ctx.filter_focus_offsets.get(filter_name).copied().unwrap_or(0)`. Offsets are stored RELATIVE to the reference filter, so the reference filter and any filter the user has not calibrated legitimately have offset 0 — that is the definition, not a guess. The delta applied to the focuser is `offset_new - offset_current`, so an uncalibrated filter leaves focus where it is instead of walking it. |
| native/nightshade_native/sequencer/src/instructions/grading.rs:456:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `event_tx.send(ExecutorEvent::NodeProgress { ... FrameAccepted })` on the executor broadcast channel (executor/mod.rs:190); Err only with zero subscribers. The frame has already been counted in the `frames_accepted` atomic and registered in the captured-images store before this point, and the same verdict is written to the replay-debug decision feed below, so the accept survives a missing listener. |
| native/nightshade_native/sequencer/src/instructions/grading.rs:482:unwrap_or_default | accepted_modeled_approximation | core-hardening | reason: `metrics.hfr.map(...format!(" (HFR {:.2})", h)...).unwrap_or_default()` builds an optional suffix for the "Frame N/M accepted" decision summary. `metrics.hfr` is None when the grader measured no HFR for the frame, and an empty suffix omits the parenthetical rather than printing a fabricated 0.00. The numeric field is carried separately and truthfully as `"hfr": metrics.hfr` in the same payload. |
| native/nightshade_native/sequencer/src/instructions/grading.rs:595:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `event_tx.send(ExecutorEvent::NodeProgress { ... FrameRejected })` on the executor broadcast channel; Err only with zero subscribers. The reject has already incremented `frames_rejected` and `consecutive_rejects`, the forensic sample was pushed to the rolling history, and the same verdict plus `reject_path` goes to the replay-debug decision feed below. |
| native/nightshade_native/sequencer/src/instructions/grading.rs:655:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `event_tx.send(ExecutorEvent::Error { escalation })` when consecutive rejects cross the limit, on the executor broadcast channel; Err only with zero subscribers. `tracing::error!` carries the same text, and the actual escalation is the `recovery_request_tx.try_send(RecoveryCause::ConsecutiveRejectsExceeded)` immediately below, whose own Err and missing-channel cases are both logged explicitly. |
| native/nightshade_native/sequencer/src/instructions/wait_time.rs:23:unwrap_or_literal | accepted_modeled_approximation | core-hardening | reason: `u64::try_from(until - now).unwrap_or(0)` is inside `if now < until`, so the difference is a positive i64 and the conversion is lossless; the fallback is unreachable. If it ever were taken, 0 means "the wait is already over", which is the same thing the enclosing guard would have decided. |
| native/nightshade_native/sequencer/src/instructions/wait_time.rs:100:unwrap_or_literal | accepted_modeled_approximation | core-hardening | reason: `u64::try_from(twilight_time - now).unwrap_or(0)` is inside `if now < twilight_time`, so the difference is a positive i64 and the conversion cannot fail. The unreachable-time case is handled separately above by the explicit "twilight does not occur at latitude ..." typed failure, so a 0 here can never stand in for an impossible twilight. |

## adjudicate-dart-core (packages/nightshade_core)

| packages/nightshade_core/lib/src/models/imaging/stack_and_share/astrobin_export_metadata.dart:182:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `integrationSecs` is a non-nullable double on the model and `toJson` (line 159) writes it unconditionally, so this fromJson only sees a missing key when re-reading a payload this class did not write. 0 then lands in the AstroBin CSV's integration column, which the exporter renders as an explicit `0` the operator can see and correct - it is never used to scale or weight frames. |
| packages/nightshade_core/lib/src/models/imaging/stack_and_share/astrobin_export_metadata.dart:183:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Symmetric with `integrationSecs`: `frames` is non-nullable on the model and `toJson` (line 161) always emits it, so 0 only appears for a foreign payload and shows up as a visible `0` in the exported acquisition row rather than being inferred from anything. |
| packages/nightshade_core/lib/src/models/imaging/stack_and_share/astrobin_export_metadata.dart:189:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `toJson` omits `date` entirely when `dateIso` is null (line 166 guards it), so the `?? ''` feeds `DateTime.tryParse('')`, which returns null - i.e. the round trip restores the SAME null `date` field. The empty string never reaches the model or the export. |
| packages/nightshade_core/lib/src/providers/device_last_contact_provider.dart:137:empty_catch | accepted_modeled_approximation | core-hardening | reason: Per-device probe failure inside the poll loop. `next` is a copy of the current map and this branch leaves the device's entry untouched, so an unreachable backend keeps the last real contact record instead of erasing it or stamping the poll time. The `ms <= 0` branch above is the one that removes an entry, and it only fires when the backend positively answers 'never contacted'. |
| packages/nightshade_core/lib/src/providers/device_last_contact_provider.dart:145:empty_catch | accepted_modeled_approximation | core-hardening | reason: Outer guard around the whole tick. Every expected failure is already classified by the per-device catch at line 137; this exists so an unexpected throw cannot escape `Timer.periodic`'s zone and kill the poll timer. State is only assigned inside the try, so a throw leaves the previous map intact. |
| packages/nightshade_core/lib/src/providers/flat_wizard/flat_wizard_run.dart:163:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `historyGain` is passed to `_recordFlatHistory(gain:)`, whose destination column `flat_history.gain` is `integer().withDefault(const Constant(0))` (NOT NULL). 0 is therefore the schema's own 'gain not reported' value, and the flat-exposure predictor keys on the same column, so a gain-less camera's history rows match each other rather than a foreign gain. |
| packages/nightshade_core/lib/src/providers/flat_wizard/flat_wizard_settings_ops.dart:367:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `_cancelToken` is non-null only while a run holds it (`runCapture` assigns it, teardown clears it). No token means no run is in flight, and `cancelRequested` false is the truthful answer for that state - the cancel button's own path is `requestCancel`, which no-ops the same way. |
| packages/nightshade_core/lib/src/providers/live_stacking_provider.dart:609:comment_best_effort | accepted_modeled_approximation | core-hardening | reason: Doc comment on `_publishRejectionStats`. The rejection itself is NOT best-effort: line 595 logs `Frame rejected: $e` at warning level before this is called. The phrase scopes only the tally read-back, which stands on the previous counters rather than replacing them with a guess. |
| packages/nightshade_core/lib/src/providers/live_stacking_provider.dart:625:empty_catch | accepted_modeled_approximation | core-hardening | reason: Swallows only the `stackingGetStats()` / `getStats()` read-back used to refresh the rejection counters; the rejection that triggered it was already logged at line 595 and the panel keeps the counters it had. `return` here also skips the `state = ` assignment, so no fabricated tally is published. |
| packages/nightshade_core/lib/src/providers/polar_alignment/polar_config_notifier.dart:112:empty_catch | accepted_modeled_approximation | core-hardening | reason: `_edit` swallows AFTER `_saveConfig` has written the message into `polarAlignmentConfigSaveErrorProvider` (line 93) and logged at level 1000, and `polar_alignment_screen.dart:277` listens on that provider to show the failure. The swallow only stops a fire-and-forget UI callback raising; `updateConfig` still rethrows for programmatic callers. |
| packages/nightshade_core/lib/src/providers/polar_alignment/polar_history.dart:97:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `polar_alignment_history.initialAzimuthError` is `real()` (NOT NULL) and `db_read_handlers.dart:333` serialises `row.initialAzimuthError` straight from that column, so the wire value is always present. `RemotePolarAlignmentHistoryEntry` types it nullable only for tolerant parsing; the fallback is unreachable against any Nightshade host. |
| packages/nightshade_core/lib/src/providers/polar_alignment/polar_history.dart:98:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same column contract as line 97: `initialAltitudeError` is a NOT NULL `real()` column emitted verbatim by the host's history endpoint, so the wire field is never absent. |
| packages/nightshade_core/lib/src/providers/polar_alignment/polar_history.dart:99:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same column contract as line 97: `initialTotalError` is a NOT NULL `real()` column emitted verbatim by the host's history endpoint. |
| packages/nightshade_core/lib/src/providers/polar_alignment/polar_history.dart:100:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same column contract as line 97: `finalAzimuthError` is a NOT NULL `real()` column emitted verbatim by the host's history endpoint. |
| packages/nightshade_core/lib/src/providers/polar_alignment/polar_history.dart:101:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same column contract as line 97: `finalAltitudeError` is a NOT NULL `real()` column emitted verbatim by the host's history endpoint. |
| packages/nightshade_core/lib/src/providers/polar_alignment/polar_history.dart:102:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same column contract as line 97: `finalTotalError` is a NOT NULL `real()` column emitted verbatim by the host's history endpoint. |
| packages/nightshade_core/lib/src/providers/polar_alignment/polar_ui_state.dart:65:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Persisted panel-expansion flag restored from the settings blob. false is the same value `PolarAlignmentUiState`'s own default constructor uses for a first run, so a settings row written before this key existed reopens the screen collapsed - the documented initial layout, not a claim about hardware or a run. |
| packages/nightshade_core/lib/src/providers/polar_alignment/polar_ui_state.dart:66:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same persisted-layout contract as line 65: `showAdvancedSettings` false matches the constructor default, so a pre-key settings blob restores the default collapsed layout. |
| packages/nightshade_core/lib/src/providers/polar_alignment/polar_ui_state.dart:67:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same persisted-layout contract as line 65: `showHistoryPanel` false matches the constructor default, so a pre-key settings blob restores the default collapsed layout. |
| packages/nightshade_core/lib/src/providers/profiles/equipment_profile_model.dart:162:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `telescopeFocalLength` was already normalised to null at line 152 for any non-positive DB value, so this re-collapses 'no OTA focal length' to the 0 sentinel the rest of the model tests for: `getFieldOfView` returns (0, 0) when `focalLength <= 0` and `opticalConfigProvider` maps `<= 0` back to null. No FOV is computed from the 0. |
| packages/nightshade_core/lib/src/providers/profiles/equipment_profile_model.dart:165:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same sentinel round trip as line 162: `telescopeAperture` is null-normalised at line 156 for non-positive values, and `effectiveAperture` 0 is re-read as 'unknown' by `profile_derived_providers.dart:59` (`effectiveAperture > 0 ? ... : null`). |
| packages/nightshade_core/lib/src/providers/profiles/equipment_profile_model.dart:578:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `id == null` means a profile that has never been persisted, and the empty id is the documented CREATE sentinel for the host: `toInsertionCopy`'s doc (line 493) states 'toRemoteProfile() emits an empty id the host treats as a create', and `remoteProfileToDbRow` does `int.tryParse(profile.id) ?? 0` so an empty id cannot address an existing row. |
| packages/nightshade_core/lib/src/providers/profiles/equipment_profile_model.dart:616:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: The REST model types `telescopeFocalLength` as a non-nullable double, and the reverse mapping `fromDb` (line 152) turns any value `<= 0` back into null. 0 is therefore the wire's own 'no OTA focal length' encoding and survives the round trip; the profile-roundtrip test asserts this pair. |
| packages/nightshade_core/lib/src/providers/profiles/equipment_profile_model.dart:617:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same wire encoding as line 616: `telescopeAperture` is non-nullable on the REST model and `fromDb` (line 156) maps `<= 0` back to null, so 0 round-trips as 'unknown aperture'. |
| packages/nightshade_core/lib/src/providers/profiles/equipment_profile_model.dart:623:guessed_now_timestamp | accepted_modeled_approximation | core-hardening | reason: `updatedAt` is null only for a model that has never been written (`toInsertionCopy` / the onboarding create), and this payload IS that write. The host's `remoteProfileToDbRow` would stamp its own `writeStamp` for the same row (`profile.updatedAt ?? writeStamp`, packages/nightshade_core/lib/src/models/equipment_profile_remote_mapping.dart:257), so the value differs from the host's only by network latency and never backdates or invents an earlier edit. |
| packages/nightshade_core/lib/src/providers/profiles/equipment_profiles_notifier.dart:145:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `lastSavedProfileId ?? ''` makes `int.tryParse` return null, which selects the `_resolveRemoteProfileIdByName` fallback on the next line; if that also fails the method throws `StateError('Host saved profile ... but id could not be resolved')` (line 155). A host that reports no id therefore produces a loud failure, never a profile bound to id 0. |
| packages/nightshade_core/lib/src/providers/profiles/equipment_profiles_notifier.dart:189:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same resolve-or-throw chain as line 145: a null parse falls through to `profile.id` then `_resolveRemoteProfileIdByName`, and an unresolved id throws `StateError('Host saved profile ... but returned no valid id')`. |
| packages/nightshade_core/lib/src/providers/profiles/equipment_profiles_notifier.dart:434:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same resolve-or-throw chain as line 145, on the duplicate path: a null parse falls through to `_resolveRemoteProfileIdByName(excludingIds: existingIds)` and an unresolved id throws `StateError('Host duplicated profile as ... but id could not be resolved')`. |
| packages/nightshade_core/lib/src/providers/profiles/profile_derived_providers.dart:34:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: The 0 is consumed three lines later by `focalLength: effectiveFocalLength > 0 ? effectiveFocalLength : null`, so a profile with no focal length on either the camera or the OTA field yields `OpticalConfig.focalLength == null`. Nothing downstream divides by the 0. |
| packages/nightshade_core/lib/src/providers/profiles/profile_derived_providers.dart:37:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same collapse-then-renull as line 34: the 0 is consumed by `aperture: effectiveAperture > 0 ? effectiveAperture : null`, so an unset aperture reaches `OpticalConfig` as null. |
| packages/nightshade_core/lib/src/providers/remote_sync/equipment_mirror.dart:113:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `updatePosition(ra, dec, alt, az)` takes non-nullable doubles and `MountState.altitude`/`azimuth` are non-nullable, so the mirror must supply a number; 0 matches what the host-side handler `device_service/event_handling.dart:135` does for the same key. The guard on line 116 also refuses to call `updatePosition` at all unless BOTH `ra` and `dec` parsed, so a payload short of its position fields updates nothing. |
| packages/nightshade_core/lib/src/providers/remote_sync/equipment_mirror.dart:114:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same signature constraint and same host-side parity as line 113 (`azimuth`), and gated by the same `ra != null && dec != null` guard on line 116. |
| packages/nightshade_core/lib/src/providers/remote_sync/guiding_mirror.dart:57:empty_catch | accepted_modeled_approximation | core-hardening | reason: Failure of the PHD2 `pollPhd2Connected` status probe. The only state writes in the try are `setDisconnected`/`setConnecting`/`setConnected`, so this branch leaves the guider chip exactly as `getConnectedDevices` restored it. Clearing it instead would drop a PHD2 guider that is connected but whose status endpoint is briefly down during host launch; the next mirror event re-probes. |
| packages/nightshade_core/lib/src/providers/remote_sync/host_mutation.dart:55:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: All three producers of `HostMutationEntity.equipment` (apps/desktop/lib/headless_api/handlers/device_handlers/connection_lifecycle.dart lines 103, 158 and 215) set `'deviceId': deviceId` from the request's own validated, non-empty field, so the key is always present. `deviceTypeFromWireName` also returns early (line 62) for any payload the host did not build. |
| packages/nightshade_core/lib/src/providers/remote_sync/imaging_mirror.dart:37:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `ExposureProgress` is built by `event_mapping.dart:845` from the bridge's non-nullable `ImagingEvent_ExposureProgress.progress`, so the key is always present. The value only drives the camera card's progress bar and the derived elapsed/total shown next to it - it is not the frame accounting, which comes from `ExposureComplete`. |
| packages/nightshade_core/lib/src/providers/remote_sync/imaging_mirror.dart:118:empty_catch | accepted_modeled_approximation | core-hardening | reason: `parseUtcTimestamp` only throws for a malformed timestamp string from a non-conforming host. The value feeds `CapturedImageData.capturedAt` for the dashboard's hero tile via `CapturePreviewPublisher.publish`, which writes only the in-memory `currentImageProvider` and `lastImageStatsProvider`. A slave never owns `captured_images`, so no fabricated time is persisted. |
| packages/nightshade_core/lib/src/providers/remote_sync/imaging_mirror.dart:153:empty_catch | accepted_modeled_approximation | core-hardening | reason: Wraps the whole remote current-frame fetch. The tile keeps its previous frame because every `publish` call is inside the try, and the `finally` still clears `fetchState.inFlight` and drains `pendingBackend`, so a failed fetch cannot wedge the fetch guard. The next host frame event retries. |
| packages/nightshade_core/lib/src/providers/remote_sync/reader_access.dart:9:empty_catch | accepted_modeled_approximation | core-hardening | reason: `_isCurrentRemoteBackend` returns false when `backendProvider` cannot be read (a disposed container mid-teardown). False is the fail-closed answer: every caller uses it as a guard to ABORT applying mirrored state, so an unreadable backend applies nothing rather than writing into a container that is going away. |
| packages/nightshade_core/lib/src/providers/remote_sync/sequencer_mirror.dart:77:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `event_mapping.dart:456` emits `'node_id': sequencerEvent.nodeId` from `SequencerEvent_NodeStarted({required nodeId, required nodeType})`, both non-nullable across the bridge, so the key is always present on this event and the empty string is unreachable. |
| packages/nightshade_core/lib/src/providers/remote_sync/sequencer_mirror.dart:78:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same producer contract as line 77: `event_mapping.dart:457` emits `'node_type'` from the bridge event's required non-nullable `nodeType`. |
| packages/nightshade_core/lib/src/providers/remote_sync/sequencer_mirror.dart:194:empty_catch | accepted_modeled_approximation | core-hardening | reason: Only `parseFromMap` on the mirrored editor payload runs inside this try, and the catch `return`s before `editor.loadSequence`, so a malformed frame leaves the slave's canvas on the last sequence it successfully parsed rather than blanking or half-applying it. The master re-broadcasts on its next edit and `_hydrateOpenEditorSequence` re-seeds on reconnect. |
| packages/nightshade_core/lib/src/providers/remote_sync/sequencer_mirror.dart:195:comment_best_effort | accepted_modeled_approximation | core-hardening | reason: The comment on the line-194 catch. It scopes to the editor-canvas mirror only; the run-state cases above it (SequenceFailed at line 66 onwards) are outside the try and still mirror their terminal state and reason. |
| packages/nightshade_core/lib/src/providers/remote_sync/sequencer_mirror.dart:227:empty_catch | accepted_modeled_approximation | core-hardening | reason: `getOpenEditorSequence` is absent on a pre-parity host, which throws here. The catch `return`s without touching the editor, so the slave keeps whatever canvas it has and the live `masterSequenceEditorMirrorProvider` still seeds it on the master's next edit - this call is only the connect-time shortcut for that stream. |
| packages/nightshade_core/lib/src/providers/remote_sync/sequencer_mirror.dart:267:empty_catch | accepted_modeled_approximation | core-hardening | reason: `sequencerGetStatus` failure on the event path. Nothing is applied because `_applySequencerStatus` is inside the try, so the execution state stays where the previous successful read left it rather than being reset to idle. The slave's status polling and `hydrateRemoteSessionState` re-read it independently. |
| packages/nightshade_core/lib/src/providers/remote_sync/session_hydration.dart:144:empty_catch | accepted_modeled_approximation | core-hardening | reason: One of five sibling fetches in the buffer-then-apply hydration: `cameraStatus` stays null, and the single apply pass below runs `if (camera != null)`, so a failed camera status leaves the card on its prior telemetry instead of blanking it. The other four devices still apply because each fetch has its own try inside `Future.wait`. |
| packages/nightshade_core/lib/src/providers/remote_sync/session_hydration.dart:153:empty_catch | accepted_modeled_approximation | core-hardening | reason: Same buffer-then-apply contract as line 144 for the mount: `mountStatus` stays null and the apply pass skips it, so a failed mount fetch cannot blank the mount card or block the other four fetches. |
| packages/nightshade_core/lib/src/providers/remote_sync/session_hydration.dart:162:empty_catch | accepted_modeled_approximation | core-hardening | reason: Same buffer-then-apply contract as line 144 for the focuser: `focuserStatus` stays null and the apply pass skips it. |
| packages/nightshade_core/lib/src/providers/remote_sync/session_hydration.dart:171:empty_catch | accepted_modeled_approximation | core-hardening | reason: Same buffer-then-apply contract as line 144 for the filter wheel: `filterWheelStatus` stays null and the apply pass skips it. |
| packages/nightshade_core/lib/src/providers/remote_sync/session_hydration.dart:180:empty_catch | accepted_modeled_approximation | core-hardening | reason: Same buffer-then-apply contract as line 144 for the rotator: `rotatorStatus` stays null and the apply pass skips it. |
| packages/nightshade_core/lib/src/providers/scheduler/scheduler_candidate_loader.dart:36:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `objectType`/`notes` are optional target columns and the empty strings only pad a lowercase haystack that is then tested with `contains('mosaic')`. An absent field contributes no text, which is exactly 'this target is not tagged mosaic' - the same answer a present-but-unrelated string gives. |
| packages/nightshade_core/lib/src/providers/scheduler/scheduler_candidate_loader.dart:240:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `targets.ra` is `real()` (NOT NULL) and the host emits it verbatim as `'ra': target.ra` (apps/desktop/lib/headless_api/handlers/target_handlers.dart:292), so the key is always present on the remote path. The local path reads the same column with a non-nullable `row.read<double>('ra')`, which would throw rather than default. |
| packages/nightshade_core/lib/src/providers/scheduler/scheduler_candidate_loader.dart:241:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same column contract as line 240: `targets.dec` is NOT NULL and the host emits `'dec': target.dec` at target_handlers.dart:293. |
| packages/nightshade_core/lib/src/providers/scheduler/scheduler_candidate_loader.dart:381:empty_catch | accepted_modeled_approximation | core-hardening | reason: `activeEquipmentProfileProvider` can throw during a background tick before the container has built it. Falling through to `const <String>[]` makes every filter-containment check fail, so the engine treats every goal as un-imageable and dispatches NOTHING until a profile exists - the fail-closed direction. A profile that exists but has no named filters takes the `smartNightPlanningFilters` branch above instead, which yields the one-unfiltered-row list. |
| packages/nightshade_core/lib/src/providers/scheduler/scheduler_engine_providers.dart:186:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Transient seed only: this is `ref.read` of an async settings provider during engine construction, and the `ref.listen<AsyncValue<AppSettingsState>>` registered 60 lines below calls `engine.updateSite(...)` plus `requestReevaluation(reason: 'observer location changed')` the moment the settings resolve. The engine is built idle and dispatches nothing before its first evaluation. |
| packages/nightshade_core/lib/src/providers/scheduler/scheduler_engine_providers.dart:187:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same transient-seed-then-hydrate mechanism as line 186: the `appSettingsProvider` listener rebuilds `SchedulerSite` with the real longitude and forces a re-evaluation, so no scoring survives against the 0. |
| packages/nightshade_core/lib/src/providers/sequence/structured_progress_json.dart:29:empty_catch | accepted_modeled_approximation | core-hardening | reason: An unparseable `detail_json` is not dropped: the catch returns `{'raw': raw}`, so a display consumer still shows the payload verbatim and a numeric consumer reads no numbers from it. The alternative (rethrow) would kill the progress event for a payload shape that is by definition already broken. |
| packages/nightshade_core/lib/src/providers/transient_alerts/transient_alert_feed.dart:150:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `scienceSettings` is null only when the science settings row has never been written, and false means 'query the production TNS endpoint', which is the same default `ScienceSettings` itself ships. This is a read-only alert fetch - the sandbox flag on the SUBMISSION path is read from the persisted settings separately, so an unset row cannot cause a real report to be filed against the sandbox. |
| packages/nightshade_core/lib/src/providers/transient_alerts/transient_alert_settings.dart:61:empty_catch | accepted_modeled_approximation | core-hardening | reason: `_parseSources` returns null for a corrupt blob and the caller at line 114 substitutes `TransientAlertSettings.defaultSettings.enabledSources`. Null and 'never configured' are deliberately the same answer here: the stored value is a user preference for which feeds to poll, and the defaults are the documented first-run set, not an inferred one. |
| packages/nightshade_core/lib/src/providers/transient_alerts/transient_alert_settings.dart:82:empty_catch | accepted_modeled_approximation | core-hardening | reason: Same preference-restore contract as line 61: `_parseTypes` returns null and line 120 substitutes `TransientAlertSettings.defaultSettings.typesToMonitor`. |
| packages/nightshade_core/lib/src/providers/transient_alerts/transient_alert_settings.dart:262:empty_catch | accepted_modeled_approximation | core-hardening | reason: Not a swallow: the catch rolls `_requestedSettings`/`state` back to `_confirmedSettings` (so the UI stops showing a value that was not persisted) and then RETHROWS on line 267. The `catch (_)` shape survives only because the rollback does not need the error object. |
| packages/nightshade_core/lib/src/providers/weather_safety/weather_safety_enforcement.dart:130:empty_catch | accepted_modeled_approximation | core-hardening | reason: `_hasAnythingToSafe` returns TRUE when `secondaryRigController.isArmed()` cannot be read - the fail-closed direction, documented on the method: an unreadable secondary-rig status is assumed to be a running rig so the safing workflow proceeds. The false path is what suppresses commands, and it is only reached when every positive check above (pausable run, connected mount/dome/cover) answered no. |
| packages/nightshade_core/lib/src/providers/weather_safety/weather_safety_executor_push.dart:128:empty_catch | accepted_modeled_approximation | core-hardening | reason: This channel is defense-in-depth only: `_computePushedVerdict` can add-unsafe or abstain, never assert safe, and the Dart SafeRig enforcement above it is the primary path. The verdict is re-pushed from `weather_safety_provider.dart:474` on EVERY evaluation (plus the unconditional `_pushWeatherVerdict(true)` at line 240), so a throw here (DisconnectedBackend when there is no live executor to inform) is corrected on the next tick. |
| packages/nightshade_core/lib/src/providers/weather_safety/weather_safety_executor_push.dart:236:empty_catch | accepted_modeled_approximation | core-hardening | reason: Scoped to the adaptive-swap telemetry forward. `AdaptiveSwapInputComposer.fromTelemetry` already passes a real null for each absent sample, so the executor distinguishes 'no telemetry' from 'bad conditions' on its own; this catch is for transport/provider faults. The conditions score only influences target SWAPPING - it is not on the weather abort path, which runs through `_pushWeatherVerdict` and SafeRig. |
| packages/nightshade_core/lib/src/providers/weather_safety/weather_safety_executor_push.dart:255:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: 0.40 is the declared default of `ConditionsScoreWeights.transparencyWeight` (`@Default(0.40)`, adaptive_swap_models.dart:87). A settings map missing the key therefore reproduces the model's own constructor default rather than a locally invented number. |
| packages/nightshade_core/lib/src/providers/weather_safety/weather_safety_executor_push.dart:256:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: 0.25 is the declared default of `ConditionsScoreWeights.seeingWeight` (`@Default(0.25)`, adaptive_swap_models.dart:88). |
| packages/nightshade_core/lib/src/providers/weather_safety/weather_safety_executor_push.dart:257:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: 0.25 is the declared default of `ConditionsScoreWeights.cloudWeight` (`@Default(0.25)`, adaptive_swap_models.dart:89). |
| packages/nightshade_core/lib/src/providers/weather_safety/weather_safety_executor_push.dart:258:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: 0.10 is the declared default of `ConditionsScoreWeights.windWeight` (`@Default(0.10)`, adaptive_swap_models.dart:90); the four together sum to 1.0, so a settings map missing all of them reproduces the normalised default weighting exactly. |
| packages/nightshade_core/lib/src/providers/weather_safety/weather_safety_remote.dart:101:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Deliberately asymmetric with `monitoringEnabled` two lines above (which defaults TRUE): false means 'we have not been told auto-park is armed', so a pre-parity host that omits the field makes the UI claim LESS protection than may exist. The comment at line 98 states the rule; over-claiming armed protection is the failure this avoids. |
| packages/nightshade_core/lib/src/providers/weather_safety/weather_safety_remote.dart:102:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same never-claim-unreported-protection rule as line 101 for `autoResumeArmed`: a host that does not report the flag is shown as not-armed, and auto-resume is a host-side action the slave never originates. |
| packages/nightshade_core/lib/src/services/calibration_library_service/matchers.dart:92:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Warning text only, and unreachable for the records that reach it: `dark_library.exposureTime` is `real()` (NOT NULL) and a remote candidate's `exposureSeconds` comes from `SharedCalibrationMaster.toMasterRecord`, where the field is a non-nullable double. The scale factor itself is computed on line 85 with an explicit `darkExp != null && darkExp > 0` guard and stays null rather than dividing. |
| packages/nightshade_core/lib/src/services/calibration_library_service/matchers.dart:98:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same non-null source as line 92 (NOT NULL `dark_library.exposureTime`, non-nullable `SharedCalibrationMaster.exposureSeconds`), and this branch only formats the 'within tolerance' reason string that accompanies an already-made match. |
| packages/nightshade_core/lib/src/services/calibration_library_service/sharing.dart:81:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `_insertAcceptedMaster` runs only for a REMOTE record (`if (!best.isRemote) return;`, line 182), and a remote record is built by `SharedCalibrationMaster.toMasterRecord` where `exposureSeconds` is a non-nullable double. The destination `dark_library.exposureTime` is `real()` NOT NULL, so there is also no way to express 'unknown' in the column. |
| packages/nightshade_core/lib/src/services/calibration_library_service/sharing.dart:86:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same remote-only source as line 81: `SharedCalibrationMaster.gain` is a non-nullable int, and `dark_library.gain` is `integer().withDefault(const Constant(0))` (NOT NULL), so 0 is the column's own 'not reported' value shared with every locally captured dark. |
| packages/nightshade_core/lib/src/services/calibration_library_service/sharing.dart:87:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same remote-only source and NOT NULL default-0 column as line 86, for `dark_library.offset`. |
| packages/nightshade_core/lib/src/services/calibration_library_service/sharing.dart:103:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Flat path of the same insert: `SharedCalibrationMaster.gain` is non-nullable, and `FlatLibraryDao.addEntry` declares `int gain = 0` with a NOT NULL default-0 column behind it. |
| packages/nightshade_core/lib/src/services/calibration_library_service/sharing.dart:104:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same as line 103 for `offset`: non-nullable on the shared-master model, `int offset = 0` on `FlatLibraryDao.addEntry`. |
| packages/nightshade_core/lib/src/services/calibration_library_service/sharing.dart:109:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Exact inverse of `toMasterRecord`, which writes `frameCount: frameCount > 0 ? frameCount : null`. Mapping null back to 0 restores the hub's own 'frame count unknown' encoding, and it is the same value `FlatLibraryDao.addEntry`'s `int masterFrameCount = 0` default would store. |
| packages/nightshade_core/lib/src/services/centering_service/centering_loop.dart:197:empty_catch | accepted_modeled_approximation | core-hardening | reason: Sets `shouldWaitForInitialSlew = true` when the mount status query fails - it errs towards WAITING, so a frame is never exposed while the mount may still be trailing. The wait itself is `_waitForSlewComplete`, a bounded poll that raises a typed error if the mount stays unreachable, so a genuinely dead mount still fails the centering run loudly. |
| packages/nightshade_core/lib/src/services/coimaging/coimaging_session_service/internals.dart:93:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `self` is null only when no participant row carries a membership token for this rig, i.e. this rig has been assigned no framing panel. 0 is the shared target's centre tile, which is what an un-assigned rig images, and `coimaging_sessions.framingOffsetIndex` uses the same 0 as its column default so the persisted row is indistinguishable from an explicit centre assignment. |
| packages/nightshade_core/lib/src/services/coimaging/coimaging_session_service/internals.dart:94:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same 'no framing panel assigned' case as line 93: a 0 arcsecond RA offset is the centre of the shared target, consistent with `framingOffsetIndex` 0 written on the same row. |
| packages/nightshade_core/lib/src/services/coimaging/coimaging_session_service/internals.dart:95:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same as line 94 for the Dec offset: 0 arcsec is the centre tile, written consistently with the index and RA offset so the three fields never describe different panels. |
| packages/nightshade_core/lib/src/services/constellation/constellation_client/io_cleanup.dart:18:comment_best_effort | accepted_modeled_approximation | core-hardening | reason: Doc comment on `_deleteQuietly`, which runs only while already unwinding a download failure. The caller's original error is what propagates; failing the cleanup instead would replace a real 'download failed' with a misleading FileSystemException about the temp file. |
| packages/nightshade_core/lib/src/services/device_service/connections/environment.dart:34:empty_catch | accepted_modeled_approximation | core-hardening | reason: Seed read of dome telemetry immediately after a SUCCESSFUL connect. The dome is connected and commandable at this point, so a failed first read is not a connect failure: the readouts stay unknown and `_ensureEnvironmentPolling` starts the 5 s poll on the next line, which surfaces the error itself. Nothing is written to the dome state here. |
| packages/nightshade_core/lib/src/services/device_service/connections/environment.dart:62:empty_catch | accepted_modeled_approximation | core-hardening | reason: Not a swallow: the catch calls `_clearUserInitiatedDisconnect(deviceId)` to undo the user-initiated marker and then RETHROWS, so a failed dome disconnect surfaces to the caller and the heartbeat/reconnect logic does not treat the device as intentionally released. |
| packages/nightshade_core/lib/src/services/device_service/connections/environment.dart:116:empty_catch | accepted_modeled_approximation | core-hardening | reason: Rollback disconnect issued because the weather station's first telemetry read failed. Swallowing the rollback error preserves the ORIGINAL telemetry failure, which is rethrown two lines below after `notifier.setDisconnected()`; reporting the rollback error instead would hide the reason the connection was rejected. |
| packages/nightshade_core/lib/src/services/device_service/connections/environment.dart:141:empty_catch | accepted_modeled_approximation | core-hardening | reason: Same clear-marker-then-rethrow shape as line 62, for the weather station: `_clearUserInitiatedDisconnect` runs and the disconnect failure is rethrown. |
| packages/nightshade_core/lib/src/services/device_service/connections/environment.dart:182:empty_catch | accepted_modeled_approximation | core-hardening | reason: Rollback disconnect on a failed safety-monitor connect. The original status-read failure is preserved and rethrown, and critically the code below does NOT call `setDisconnected` (which would reset the model to `isSafe: true` and paint green); it calls `notifier.setError(e)` so the monitor fails closed. |
| packages/nightshade_core/lib/src/services/device_service/connections/environment.dart:211:empty_catch | accepted_modeled_approximation | core-hardening | reason: Same clear-marker-then-rethrow shape as line 62, for the safety monitor: the disconnect failure reaches the caller rather than leaving a device marked as intentionally released. |
| packages/nightshade_core/lib/src/services/device_service/connections/environment.dart:367:empty_catch | accepted_modeled_approximation | core-hardening | reason: `_refreshDomeStatus` is the post-command nudge so the card does not wait out the 5 s poll. A failed refresh leaves the previous reading in place and the periodic poll classifies the error on its own next tick; no telemetry is fabricated because `_readDomeStatusInto` is the only writer and it is inside the try. |
| packages/nightshade_core/lib/src/services/device_service/connections/guider_and_rotator.dart:53:empty_catch | accepted_modeled_approximation | core-hardening | reason: Rollback `phd2Disconnect` after a failed PHD2 connect. `notifier.setDisconnected()` already ran, and the ORIGINAL connect error is rethrown on the next line - swallowing the rollback keeps the useful failure (launch/socket/poll) instead of a secondary disconnect error against a session that may never have opened. |
| packages/nightshade_core/lib/src/services/device_service/connections/guider_and_rotator.dart:54:comment_best_effort | accepted_modeled_approximation | core-hardening | reason: The comment on the line-53 rollback. It scopes only the cleanup disconnect; the connect failure itself is rethrown, so 'best-effort' never describes the connect result the caller sees. |
| packages/nightshade_core/lib/src/services/device_service/connections/guider_and_rotator.dart:104:empty_catch | accepted_modeled_approximation | core-hardening | reason: Clear-marker-then-rethrow on guider disconnect: `_clearUserInitiatedDisconnect(deviceId)` undoes the intentional-disconnect marker and the failure is rethrown, so `notifier.setDisconnected()` below is never reached for a disconnect that did not happen. |
| packages/nightshade_core/lib/src/services/device_service/connections/guider_and_rotator.dart:179:empty_catch | accepted_modeled_approximation | core-hardening | reason: Same clear-marker-then-rethrow shape as line 104, for the rotator. |
| packages/nightshade_core/lib/src/services/device_service/connections/guider_and_rotator.dart:233:empty_catch | accepted_modeled_approximation | core-hardening | reason: Same clear-marker-then-rethrow shape as line 104, for the cover calibrator. |
| packages/nightshade_core/lib/src/services/device_service/connections/imaging_chain.dart:233:comment_best_effort | accepted_modeled_approximation | core-hardening | reason: The comment documenting the cooler-off attempt during camera disconnect. That attempt is NOT silent: success logs 'Cooler disabled as part of camera disconnect' and failure logs 'Could not disable cooler during disconnect: $e' at warning level (both via `_safeLog`), so the operator can see whether the cooler was actually stopped. |
| packages/nightshade_core/lib/src/services/device_service/connections/imaging_chain.dart:270:empty_catch | accepted_modeled_approximation | core-hardening | reason: Camera-disconnect rollback: the catch restarts `_temperaturePoller` and the camera heartbeat, clears the user-initiated marker, and RETHROWS. A failed disconnect therefore leaves a fully monitored, still-connected camera rather than a half-torn-down one. |
| packages/nightshade_core/lib/src/services/device_service/connections/imaging_chain.dart:368:empty_catch | accepted_modeled_approximation | core-hardening | reason: Same rollback-then-rethrow shape as line 270 for the mount: the heartbeat is restarted and the marker cleared before the failure propagates. |
| packages/nightshade_core/lib/src/services/device_service/connections/imaging_chain.dart:449:empty_catch | accepted_modeled_approximation | core-hardening | reason: Rollback disconnect after a failed focuser connect/status read. The original failure is preserved and rethrown below; the connect may have failed before the backend registered the device at all, in which case the rollback's own error says nothing about why the connect failed. |
| packages/nightshade_core/lib/src/services/device_service/connections/imaging_chain.dart:480:empty_catch | accepted_modeled_approximation | core-hardening | reason: Same rollback-then-rethrow shape as line 270 for the focuser disconnect: heartbeat restarted, marker cleared, failure propagated. |
| packages/nightshade_core/lib/src/services/device_service/connections/imaging_chain.dart:598:empty_catch | accepted_modeled_approximation | core-hardening | reason: Rollback disconnect after a failed filter-wheel connect. The original connect/status failure is rethrown, which matters here because the try also seeds `_lastAppliedFilterOffsetByWheel` and syncs filter names - a partial connect must not be reported as success. |
| packages/nightshade_core/lib/src/services/device_service/connections/imaging_chain.dart:628:empty_catch | accepted_modeled_approximation | core-hardening | reason: Same rollback-then-rethrow shape as line 270 for the filter-wheel disconnect, and the `_lastAppliedFilterOffsetByWheel.remove(deviceId)` below is skipped on failure so the offset baseline is not dropped for a wheel that is still connected. |
| packages/nightshade_core/lib/src/services/device_service/connections/switch_device.dart:38:comment_best_effort | accepted_modeled_approximation | core-hardening | reason: The comment on the post-connect channel snapshot. Refresh errors are surfaced by `SwitchChannelService.refreshChannels` through ErrorService (see its contract at line 89); this only says the snapshot must not turn a genuinely connected power box back into a disconnected device. |
| packages/nightshade_core/lib/src/services/device_service/connections/switch_device.dart:65:empty_catch | accepted_modeled_approximation | core-hardening | reason: Clear-marker-then-rethrow on switch disconnect, identical to the other device teardowns: `_clearUserInitiatedDisconnect(deviceId)` runs and the failure propagates before `notifier.setDisconnected()`. |
| packages/nightshade_core/lib/src/services/imaging_records_repository/remote_polling.dart:64:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `read<T>` returns null for a companion field that is NOT PRESENT, and `captured_images.isPlateSolved` is `boolean().withDefault(const Constant(false))` (NOT NULL). Sending false for an absent field therefore transmits exactly the column default the local insert would have applied - it cannot downgrade a frame that was actually solved, because a solved frame sets the field. |
| packages/nightshade_core/lib/src/services/imaging_records_repository/solved_frame_hooks.dart:52:comment_best_effort | accepted_modeled_approximation | core-hardening | reason: Doc comment on `_driveCoImagingAutoContribute`. Both catches under it LOG at warning level ('Co-imaging auto-contribute for session ... failed' and '... resolve failed'), and `recordCompletedSub` itself fails closed without operator consent, so 'best-effort' scopes the co-imaging side effect and never the solve/fold result the caller returns. |
| packages/nightshade_core/lib/src/services/imaging_records_repository/solved_frame_hooks.dart:122:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `SolvedWcs.rotationDeg` is only consulted on the isotropic path: `hasCdMatrix` (line 87 of gnomonic_projection.dart) makes the full CD matrix passed in `distortion` win whenever the solve recorded one. 0.0 for an absent `solvedRotation` is the same convention every other consumer of that column uses (for example the photometric-calibration star matcher). |
| packages/nightshade_core/lib/src/services/imaging_records_repository/solved_frame_hooks.dart:130:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: SIP order 0 paired with `aCoeffs: sip?.aCoeffs ?? const []` on the next line is the encoding for 'no forward distortion': `hasForwardSip` requires `aOrder > 0 && bOrder > 0 && aCoeffs.isNotEmpty`, so a frame with no decoded SIP projects with the plain TAN transform instead of a partial one. |
| packages/nightshade_core/lib/src/services/imaging_records_repository/solved_frame_hooks.dart:131:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same no-forward-distortion encoding as line 129 for `bOrder`, and gated by the same `hasForwardSip` predicate. |
| packages/nightshade_core/lib/src/services/imaging_records_repository/solved_frame_hooks.dart:134:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same encoding for the SIP INVERSE: `hasInverseSip` requires `apOrder > 0 && bpOrder > 0 && apCoeffs.isNotEmpty`, and when it is false the projection falls back to its documented iterative Newton correction rather than using a zero-order polynomial. |
| packages/nightshade_core/lib/src/services/imaging_records_repository/solved_frame_hooks.dart:135:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same no-inverse-SIP encoding as line 133 for `bpOrder`. |
| packages/nightshade_core/lib/src/services/imaging_records_repository/wire_json.dart:16:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `db.ImagingSession.totalExposures` is a non-nullable int backed by a NOT NULL column with default 0, and the host's `/api/sessions` row is serialised from that column, so the key is always present. 0 reproduces the column default for a row written before the field existed. |
| packages/nightshade_core/lib/src/services/imaging_records_repository/wire_json.dart:17:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same NOT NULL default-0 column contract as line 16 for `successfulExposures`. |
| packages/nightshade_core/lib/src/services/imaging_records_repository/wire_json.dart:18:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same NOT NULL default-0 column contract as line 16 for `failedExposures`. |
| packages/nightshade_core/lib/src/services/imaging_records_repository/wire_json.dart:20:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same NOT NULL default-0 column contract as line 16 for `totalIntegrationSecs`; note the genuinely optional averages immediately below (avgTemperature, avgHfr, avgGuidingRms) are read as nullable and left null, so this file does not blanket-default the fields that mean 'not measured'. |
| packages/nightshade_core/lib/src/services/imaging_records_repository/wire_json.dart:26:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same NOT NULL default-0 column contract as line 16 for `autofocusCount`. |
| packages/nightshade_core/lib/src/services/imaging_records_repository/wire_json.dart:103:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: These rows come from `backend.getImagesByProducingNode(producingNodeId)` (remote_polling.dart:118), a query whose WHERE clause IS `producing_node_id`, so every returned row carries the node id that was asked for. The field is only used by `_thumbnailListsEqual` to decide whether the thumbnail strip changed. |
| packages/nightshade_core/lib/src/services/imaging_service/capture_pipeline.dart:146:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `ExposureProgress` is emitted by `event_mapping.dart:845` from the bridge's non-nullable `ImagingEvent_ExposureProgress.progress`, so the key is always present. The value drives the progress bar only; exposure completion is signalled by the separate `ExposureComplete` event that completes `exposureCompleter`. |
| packages/nightshade_core/lib/src/services/imaging_service/capture_pipeline.dart:147:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same producer contract as line 146: `event_mapping.dart:846` emits `remainingSecs` from a non-nullable bridge field. `elapsed` is derived from the configured `settings.exposureTime` rather than from this value, so a 0 would not silently shorten the frame. |
| packages/nightshade_core/lib/src/services/imaging_service/exposure_state.dart:8:empty_catch | accepted_modeled_approximation | core-hardening | reason: `_withLiveFilter` returns the CALLER'S settings unchanged when `filterWheelStateProvider` is unavailable (a minimal test container). The method's only job is to upgrade a stale filter name from live wheel state, so the fallback is the explicit value the caller already chose - no filter name is invented and the capture still records what was requested. |
| packages/nightshade_core/lib/src/services/imaging_service/exposure_state.dart:71:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: 0 is the documented 'unknown/unsupported' answer of `_readoutModeCount`, and `_resolveReadoutModeIndex` turns it into `null`, meaning 'do not issue cameraSetReadoutMode at all' so the driver keeps its own default mode. Query FAILURES are not covered by this: the awaited `.future` propagates them, only a null capability object yields 0. |
| packages/nightshade_core/lib/src/services/live_stacking_service.dart:460:empty_catch | accepted_modeled_approximation | core-hardening | reason: `Error.throwWithStackTrace(error, stackTrace)` inside the catch rethrows the ORIGINAL add-frame failure with its original stack. Nothing is swallowed: this branch exists so a failing stats read-back cannot replace the real rejection reason with a second, less informative error. |
| packages/nightshade_core/lib/src/services/mosaic_project_service/panel_integration.dart:263:comment_best_effort | accepted_modeled_approximation | core-hardening | reason: Doc comment on `_deleteMasterArtifactsOnDisk`. Its catch logs through `_logSoftFailure` with the path in the message, and the function only ever deletes artifacts of a SUPERSEDED master (paths belonging to the fresh master are skipped via `keepPaths`), so the DB row supersession that matters has already committed. |
| packages/nightshade_core/lib/src/services/mosaic_project_service/stitching.dart:18:comment_best_effort | accepted_modeled_approximation | core-hardening | reason: Doc comment on `_safeFitsExists`. The degraded `false` means 'treat the panel master as missing and skip the panel' - the conservative direction for a stitch, and the probe error is recorded by `_logSoftFailure` rather than discarded. |
| packages/nightshade_core/lib/src/services/predictive_af_service/predictive_af_models.dart:253:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `focus_models.consecutive_bad_predictions` is `INTEGER NOT NULL DEFAULT 0` (database/schema_helpers.dart:29), so the field is absent only for a row written by a host older than that column. 0 is that column's own default and it is the SAFE direction for a drift counter: the model starts un-penalised and re-accumulates real bad predictions from the next AF run. |
| packages/nightshade_core/lib/src/services/predictive_af_service/predictive_af_models.dart:255:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same pre-migration case and NOT NULL DEFAULT 0 column as line 253 for `accumulated_drift_steps` (schema_helpers.dart:30). |
| packages/nightshade_core/lib/src/services/profile_service/profile_backends.dart:288:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `lastSavedProfileId ?? ''` makes `int.tryParse` return null, and the code below only accepts `savedId != null && savedId > 0`; otherwise it re-fetches `backend.getProfiles()` and resolves the id by name. A host that reports no id therefore triggers a real lookup rather than binding the profile to id 0. |
| packages/nightshade_core/lib/src/services/scheduler/scheduler_engine.dart:436:empty_catch | accepted_modeled_approximation | core-hardening | reason: `_awaitEvaluationQuiescence` awaits another evaluation's completer purely to learn that it is no longer in flight; that evaluation reports its own failure through its own caller. Rethrowing here would make `stop()` fail because a DIFFERENT evaluation failed, which is exactly the safing path that must not be blocked. |
| packages/nightshade_core/lib/src/services/sequence_diff_service/node_field_diff.dart:21:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Normalises null and empty comment to the same value so adding then clearing a comment does not report a phantom change. Both sides are coalesced identically, so the comparison is symmetric and no real edit (null to text, text to other text) can be hidden. |
| packages/nightshade_core/lib/src/services/sequence_diff_service/node_field_diff.dart:22:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Same normalisation as line 21, applied to the `FieldChange` payload so the diff renders 'was empty' instead of the literal 'null'. Reached only when line 21 already proved the two differ. |
| packages/nightshade_core/lib/src/services/sequence_time_estimator/node_durations.dart:255:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: 0 and null are the SAME state for `ditherEvery` ('no dithering'), and the very next line guards `if (every > 0)` before computing any dither cost, so an unset value adds no time rather than dividing by zero or assuming a cadence. |
| packages/nightshade_core/lib/src/services/sequence_time_estimator/timing_models.dart:160:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `minAltitudeDeg` null means no altitude floor was configured, and 0 degrees IS the horizon - the weakest possible floor, so `isVisibleAt` admits exactly the times the target is above the horizon. The `neverRises` and `isCircumpolar` special cases are already handled above. |
| packages/nightshade_core/lib/src/services/stack_and_share_service/stack_frame_io.dart:25:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: Guarded by `meta.gain != null` on line 21, so this only runs for a frame whose gain IS recorded. `dark_library.offset` is `integer().withDefault(const Constant(0))` NOT NULL, so 0 is the same value the dark rows carry for an offset-less camera and `findMatchingDark` compares like with like; a no-match still returns null and the frame is stacked uncalibrated rather than mis-calibrated. |

| packages/nightshade_app/lib/screens/analytics/analytics_screen/equipment_stats.dart:85:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `runsAsync.valueOrNull?.isEmpty ?? false`. A loading or errored AsyncValue has a null value, and `?? false` keeps the empty-state branch SHUT for it, so only a resolved-and-empty runs list can help print 'No equipment history yet'. The provider's own error path renders the error card plus a Retry button that invalidates it (lines 60-74), so a failed load is stated rather than redescribed as an empty history. |
| packages/nightshade_app/lib/screens/analytics/analytics_screen/equipment_stats.dart:86:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: Same expression on `sessionsAsync`, and the empty state needs BOTH it and the runs list to be resolved-and-empty alongside `images.isEmpty`. Meridian-flip and autofocus counts come from those rows, so an unresolved one must not be read as zero history; the error card above covers the failure. |
| packages/nightshade_app/lib/screens/analytics/analytics_screen/session_tab.dart:64:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `standaloneAsync.valueOrNull?.isNotEmpty ?? false` only decides whether the picker OFFERS the quick-capture bucket; while `standaloneImagesProvider` is loading or failed there is no proven bucket to offer. The same AsyncValue is what the image list renders through (`imagesAsyncValue`, line 60) with its own loading/error states and a `retryImages` that invalidates the provider, so the failure is on screen even when the chip is not. |
| packages/nightshade_app/lib/screens/analytics/widgets/image_thumbnail_strip_parts/_chips.dart:95:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `_ScienceBadge` renders `Text(label ?? '')` only in the `icon == null` branch, and its two construction sites both supply one of the pair (_thumbnail.dart:266 passes a label, :278 passes an icon). The padding on line 85 already branches on `label == null`. A badge built with neither field would be an empty pill, never a wrong number. |
| packages/nightshade_app/lib/screens/analytics/widgets/project_tracking_panel_parts/_headers.dart:91:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `untrackedTargetsCountProvider.valueOrNull ?? 0` lands on the `count <= 0` early return two lines down, so a loading or failed count HIDES the destructive 'remove untracked targets' action rather than offering a cleanup of an unknown size. The action itself reports failure through `context.showErrorSnackBar('Failed to remove untracked targets: ...')` at line 82. |
| packages/nightshade_app/lib/screens/analytics/widgets/science_analytics_tab/line_ratio_card.dart:366:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `(image.filter ?? '').toLowerCase().trim()` feeds the alias matcher below it; an empty string equals no alias and matches no whole-word pattern, so a frame with no recorded filter is EXCLUDED from the Ha/OIII/SII pick instead of being taken for one of them. When any of the three comes back null the card states 'Need latest H-alpha, OIII, and SII frames in this session.' (line 327) and generates nothing. |
| packages/nightshade_app/lib/screens/analytics/widgets/science_export_hub/datasets.dart | accepted_modeled_approximation | app-adjudication | reason: Family, one shape and one consumer: every hit is a cell of `toCsvRow` in the seven `_ExportDataset` descriptors, where a nullable column becomes an EMPTY CSV field. That is the file format's own way of writing 'no value' and a parser can tell it from 0. `sessionId`/`capturedImageId` are null exactly for the sessionless rows the `standaloneExport` providers exist to publish, so an empty Session ID states 'not part of a session' rather than dropping a join key. The header each row builder is paired with is pinned by the parity test noted at line 17, so column order cannot drift under the blanks. |
| packages/nightshade_app/lib/screens/equipment/widgets/connected_device_card/motion_dialogs.dart:191:empty_catch | accepted_modeled_approximation | app-adjudication | reason: A failed `equipmentRotatorCapabilitiesProvider` read leaves `caps` null and the dialog falls back to the full 0-360 mechanical turn, the same fallback a driver that advertises no bounds gets, and `hasValidRange` (line 200) rejects an inverted or non-finite pair identically. The bounds are a client-side pre-check only: Rotate still calls `deviceService.moveRotatorTo`, and a driver rejection is shown in the dialog as 'Failed to rotate: ...' (line 304). |
| packages/nightshade_app/lib/screens/equipment/widgets/connected_device_card/motion_dialogs.dart:198:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `caps?.minAngleDeg ?? 0.0` (with `maxAngleDeg ?? 360.0` beside it) is the unknown-bounds fallback described on lines 180-183; the pair is then re-checked by `hasValidRange` before it is used, and the printed range plus the entry validation both read the same two numbers, so what the dialog says and what it enforces cannot disagree. The driver remains the authority and its refusal surfaces as 'Failed to rotate: ...'. |
| packages/nightshade_app/lib/screens/imaging/widgets/calibration_section_parts/_build_controls.dart:228:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `status?.applyDuringCapture ?? false` is paired with `toggleEnabled = status?.storedOnDisk == true` on the next line, so an unresolved defect-map status leaves the toggle OFF and DISABLED, and `mapDisabledReason` (lines 230-237) names which case it is: 'Loading defect map status...', 'Could not verify the current defect map.' or 'Build a defect map for this camera and temperature first.' A failed status read therefore cannot present itself as 'a map exists and is not applied'. |
| packages/nightshade_app/lib/screens/imaging/widgets/science_hud_parts/_offers_and_chips.dart:132:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `(image.filter ?? '').toUpperCase()` is followed immediately by `if (f.isEmpty) continue;`, so a light frame with no recorded filter is left out of `filterCounts` instead of being counted under an invented band name. |
| packages/nightshade_app/lib/screens/imaging/widgets/science_hud_parts/_offers_and_chips.dart:134:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `filterCounts[f] ?? 0` is the first-sight initialiser of a counter map built by this loop and nothing else; the key is absent only before its first frame, so `+ 1` records exactly the number of lights carrying that filter. |
| packages/nightshade_app/lib/screens/imaging/widgets/science_hud_parts/_offers_and_chips.dart:174:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `counts[alias] ?? 0` reads the map built above: an alias that never appeared has no entry and 0 is its true count, so `_hasFilter` answers false and the 'Enable narrowband ratios?' offer stays hidden rather than claiming Ha, OIII and SII are all present. |
| packages/nightshade_app/lib/screens/mosaic/mosaic_project_controller_parts/_providers.dart:86:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `constellationConfiguredProvider` answers 'is a hub configured and signed in'; `?? false` while it loads or errors leaves `hubConfigured` false, which HIDES the collaborative actions instead of offering a publish/claim the service would then refuse. The fail-closed choice is stated at lines 81-84 and is the same signal the Constellation screens gate on. |
| packages/nightshade_app/lib/screens/mosaic/mosaic_project_controller_parts/_state.dart:111:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `project?.isPublished ?? false` reads null only before `MosaicProjectController.load` has put the row into state. Every mutation guards the same field independently - `publishToHub` returns at mosaic_project_controller.dart:359 on `project == null` - so the collaborative card can only offer to publish a loaded project, and for a loaded row the answer comes from its own `hubMosaicId` (mosaic_project.dart:133). |
| packages/nightshade_app/lib/screens/mosaic/mosaic_project_controller_parts/_state.dart:159:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `isComplete` is the AND of the persisted flag and `stitchedMaster != null`, so a project not yet loaded reads as not complete and the screen cannot announce a finished mosaic before it has the artifact. The stitch action is gated separately on `canStitch` (two or more panel masters), so this value enables nothing on its own. |
| packages/nightshade_app/lib/screens/onboarding/onboarding_screen_parts/_chrome.dart:325:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `_stepLabels` (lines 327-341) carries an entry for all 13 values of `OnboardingStep` (packages/nightshade_core/lib/src/models/onboarding/onboarding_state.dart:11), so the `?? ''` is unreachable as the enum stands. A step added without a label would render an unlabelled sidebar row and phone header, never another step's name. |
| packages/nightshade_app/lib/screens/onboarding/onboarding_screen_parts/_chrome.dart:436:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: Same const map, same total coverage of `OnboardingStep`; this is the sidebar row's own label read. The leading indicator beside it is drawn from `isCompleted`/`isSkipped` state rather than from the label, so an empty string could not misreport a step's progress either. |
| packages/nightshade_app/lib/screens/onboarding/steps/capture_dir_step.dart:66:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `error.osError?.message.trim() ?? ''` is tested for emptiness on the very next line and selects the `'Not writable: ${error.message}'` branch, the documented fallback for platforms that supply no errno. Both branches name a real failure taken from the thrown `FileSystemException`; neither invents a cause. |
| packages/nightshade_app/lib/screens/onboarding/steps/device_picker_step.dart:275:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `rawError?.trim() ?? ''` hits the `raw.isEmpty` return on the next line, which yields 'the scan did not complete' - the same sentence any unrecognised transport error gets. A backend that failed is never described as one that answered, and the raw driver dump stays out of the first-run copy by design (lines 263-272). |
| packages/nightshade_app/lib/screens/planner/planner_screen_parts/_candidate_observing_list.dart:84:empty_catch | accepted_modeled_approximation | app-adjudication | reason: `_listsContaining` only annotates which lists already hold this target. When the membership query fails the annotations are absent and the add still runs, and `_addToList` separates a genuine write failure (errorMessage) from 'the target is already in this list' (a neutral statusMessage) at lines 108-120, so the duplicate case is still reported by the action itself. |
| packages/nightshade_app/lib/screens/planner/planner_screen_parts/_range_controls.dart:282:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `value ?? 0.0` seeds the slider dialog when no minimum-altitude filter is set (`active == false`), and the branch immediately below replaces that seed with the horizon profile's maximum obstruction whenever a non-flat profile exists. Nothing is filtered on the seed: the chip reads 'Alt now: any' and the filter changes only when the dialog returns a confirmed value. |
| packages/nightshade_app/lib/screens/sequencer/widgets/node_properties_panel_parts/_motion_flip_and_polar.dart:385:literal_null_coalesce | done | app-adjudication | reason: FIXED in this campaign. `(node.gain ?? 0)` survives because `NodeNumberInput.value` takes a non-null double, but an unset gain no longer READS as a gain of 0: the field now carries `helperText: 'Unset - the camera keeps its current gain'` when `node.gain == null`, which is what the wire actually does (PolarAlignConfig.gain = None leaves `camera_start_exposure` from setting gain, native/nightshade_native/sequencer/src/all_sky_polar/mod.rs:345). Pinned by test/screens/sequencer/polar_alignment_properties_gain_test.dart, which also checks an explicit gain of 0 does NOT get the unset notice. |
| packages/nightshade_app/lib/screens/sequencer/widgets/node_properties_panel_parts/_motion_flip_and_polar.dart:407:literal_null_coalesce | done | app-adjudication | reason: FIXED in this campaign, same contract as Gain above: `PolarAlignmentNode.offset` is nullable and null rides to `PolarAlignConfig.offset = None`, so the field now says 'Unset - the camera keeps its current offset' instead of presenting 0 as the offset the alignment frames will use. Covered by the same test. |
| packages/nightshade_app/lib/screens/sequencer/widgets/preflight_validation_dialog/section_builders.dart:160:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `_simulation?.issues.isNotEmpty ?? false` only decides whether the simulation issues get their own section. `_simulation` is null when the pre-session simulation could not run, and that case is not silent: `_buildSimulationSection` (line 257) renders `_simulationUnavailableReason` in the Simulation card instead. |
| packages/nightshade_app/lib/screens/sequencer/widgets/preflight_validation_dialog/section_builders.dart:725:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `_simulation?.hasBlockingIssues ?? false` cannot make Start permissive on its own: `canStart` on line 726 also requires `_result?.isValid`, and a validation that threw sets `_result = null` (preflight_validation_dialog.dart:98-104), which fails closed. A simulation that could not run says so through `_simulationUnavailableReason`, so the absence of a blocking issue is never presented as a clean simulation. |
| packages/nightshade_app/lib/screens/sequencer/widgets/preflight_validation_dialog/section_builders.dart:729:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `_result?.isValid ?? false` is the fail-closed direction - no validation result means Start stays disabled - and `_startBlockedReason` (line 703) tells the operator which it is: 'the pre-flight checks have not finished' while validating, 'have not run yet' otherwise. |
| packages/nightshade_app/lib/screens/sequencer/widgets/quick_start_wizard_dialog/_wizard_helpers.dart:23:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `activeProfile?.filterNames.isNotEmpty ?? false` is false exactly when no equipment profile is active, which is the same fact as having no configured filters. Line 25 then calls `_initFilterConfigs(activeProfile?.filterNames ?? const [])`, which seeds the single 'Light' row, so `_hasConfiguredFilters` and the rows the wizard will actually capture are derived from the one value. |
| packages/nightshade_app/lib/screens/sequencer/widgets/quick_start_wizard_dialog/_wizard_helpers.dart:113:empty_catch | accepted_modeled_approximation | app-adjudication | reason: A failed `smartNightExposureContextProvider` leaves `_exposureContext` null; `_defaultExposureForFilter` (line 167) then returns the per-band fallback constants into visible, editable exposure fields, and `_buildExposureRationale` (_review_step.dart:203) renders NOTHING without a context, so no sky-derived recommendation is claimed for a number that was not computed from one. The profile/app-settings load the wizard cannot open without has its own surfaced failure (`_initializationError`, line 35). |
| packages/nightshade_app/lib/screens/sequencer/widgets/quick_start_wizard_dialog/_wizard_helpers.dart:494:comment_best_effort | accepted_modeled_approximation | app-adjudication | reason: The comment marks the re-fire of `_loadExposureContext()` after `_rePreviewExposures` has already re-seeded the rows from the context in hand. If that refresh fails the catch on line 113 leaves the rows on the values just computed; if it succeeds it re-seeds again and skips every row with `exposureEdited == true`, so an operator's typed exposure is preserved either way and the preview never silently diverges from what is shown. |
| packages/nightshade_app/lib/screens/sequencer/widgets/slew_to_target_dialog.dart:28:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `showDialog<bool>` completes with null when the barrier or Back dismisses the sheet, and `?? false` maps that to 'not confirmed', so a dismissed confirmation never commands the mount. The only true this function can return comes from the dialog's own confirm button. |
| packages/nightshade_app/lib/screens/sequencer/widgets/smart_night_dialog/plan_helpers.dart:40:comment_best_effort | accepted_modeled_approximation | app-adjudication | reason: The comment heads `_persistSettings`, which writes the wizard's knobs into app settings so the NEXT launch pre-fills. A failure is logged as `loggingServiceProvider.warning('Could not persist Smart Night wizard defaults: ...')` (line 74) and changes nothing about the plan being built: the sequence is assembled from the in-memory `_settings`/`_strategy` the operator can see on the review step. |
| packages/nightshade_app/lib/screens/session_review/session_review_controller_parts/_helpers.dart:319:empty_catch | accepted_modeled_approximation | app-adjudication | reason: A failed read or a corrupt `improvement_curve_json` yields `(null, const [])`, and both consumers read that as 'no curve': `ImprovementCurvePanel(curve: null)` draws its placeholder, and `cullToRecommended` returns `CullOutcome.staleCurve` on an empty population - pinned by session_review_smart_data_test.dart 'cullToRecommended is a no-op when the curve carried no population'. No sub can be rejected off a curve that failed to load. |
| packages/nightshade_app/lib/screens/session_review/session_review_controller_parts/_helpers.dart:338:empty_catch | accepted_modeled_approximation | app-adjudication | reason: `(const [], null)` is the same shape a single-night master legitimately produces, so `GrowthCurvePanel` draws no series and no best-night badge; no growth figure is stated that was not read. The four smart-backbone sources are loaded independently (session_review_controller.dart:306-313), so this failure cannot take the night report or the annotations down with it. |
| packages/nightshade_app/lib/screens/session_review/session_review_controller_parts/_helpers.dart:363:empty_catch | accepted_modeled_approximation | app-adjudication | reason: A null annotation layer leaves `MasterOverlayView` with no annotation toggle, which is the identical state a master with no persisted WCS reaches one call earlier (`_resolveMasterWcs` returns null). The overlay never claims an annotation it does not hold, and nothing is written to the master row on this path. |
| packages/nightshade_app/lib/screens/session_review/session_review_controller_parts/_helpers.dart:437:empty_catch | accepted_modeled_approximation | app-adjudication | reason: Only the sibling preview PNG render is skipped here. The calibrated FITS is already written, and its path is persisted on the next lines via `_mastersDao.updateSmartFields(colorCalibratedPath: ...)` followed by `_publishMasters()`, so the file survives and the overlay simply shows no preview image. A failure of the calibration itself is caught by the enclosing catch and stated as 'Color calibration failed: ...' on `state.error`. |
| packages/nightshade_app/lib/screens/session_review/session_review_controller_parts/_helpers.dart:507:empty_catch | accepted_modeled_approximation | app-adjudication | reason: Same preview seam inside `_runFinishingStep`: `written` is the FITS the pass produced, `persist(master.id, written)` and `_publishMasters()` run on the following lines regardless, so a missing PNG never costs the operator the finished file or its recorded path. The finishing pass itself fails onto `state.error` as '<label> failed: ...' in the enclosing catch. |
| packages/nightshade_app/lib/screens/session_review/session_review_controller_parts/_helpers.dart:611:empty_catch | accepted_modeled_approximation | app-adjudication | reason: A failing `masterIdsForImages` scoping query falls through to the rule below, which returns a master ONLY for a target-scope review (where every master for that target is in scope by definition) and returns null for a session scope. The failure can therefore only under-claim: the screen says it has no finished master yet instead of presenting another night's stack as this night's result, which is what session_review_smart_data_test.dart 'another night's master is NOT presented as this session's' pins. |
| packages/nightshade_app/lib/screens/session_review/session_review_controller_parts/_state.dart:192:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `(m.filter ?? '').trim()` normalises to an empty string, which the next two lines reject (`norm.isEmpty` continue), so a master with no recorded filter is left out of `narrowbandChannels` rather than offered to the mixer under a guessed band. The mixer refuses a combine with fewer than two channels, so an excluded master cannot silently become one. |
| packages/nightshade_core/lib/src/services/darkroom_autopilot/dawn_autopilot_service.dart:369:comment_best_effort | accepted_modeled_approximation | darkroom-queue-serialization | reason: The queue-position note a waiting submission writes onto its own `queued` row (`Waiting for the Darkroom pass ahead of it to finish.`). The pass is already serialized by the run gate whether or not the note lands, so a refused write must not stop the job from running — and it is not swallowed: `_noteQueuePosition` catches, logs a warning naming the job and the position, and says in that line that the job still runs in turn. What is lost on a failed write is one sentence in Session Review's banner, which then reads the label the enqueue wrote. |
| packages/nightshade_core/lib/src/services/darkroom_autopilot/dawn_autopilot_service.dart:956:literal_null_coalesce | accepted_modeled_approximation | delivery-stop-semantics | reason: `delivered.report?.stoppedPending ?? 0` on the stop branch. `_deliver` returns a null report on exactly one path — the arm that returns early when NO artifact of the job could be described, which is the arm that also returns the sentence "Nothing was delivered: this job produced no file that could be read at delivery time." Zero files were handed to delivery there, so zero can be owed; the count only chooses between "Stopped on request during delivery." and the same sentence naming N owed files, and the journal rows the retry sweep actually reads are written by `DeliveryService._recordStillOwed`, never from this number. |
| packages/nightshade_app/lib/screens/settings/catalog_settings_screen/view_builders.dart:248:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `_starStatus` is null until `_loadCatalogStatus` resolves, and `?? false` keeps the destructive 'Delete Catalogs' button hidden while the answer is unknown. A status read that throws is not silent: catalog_settings_screen.dart:241 calls `_showError('Failed to check catalog status: $e')`. |
| packages/nightshade_app/lib/screens/settings/catalog_settings_screen/view_builders.dart:249:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: Same guard on `_dsoStatus`; the two are ORed so Delete appears when EITHER catalog is proven installed, and an unresolved pair hides it entirely. The failure path is the `_showError('Failed to check catalog status: ...')` at catalog_settings_screen.dart:241. |
| packages/nightshade_app/lib/screens/settings/catalog_settings_screen/view_builders.dart:308:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `_annotationStatus?.isInstalled ?? false` colours the annotation card and picks Download over Delete, so an unresolved status offers a re-download (idempotent) rather than a delete of a file whose presence is unproven. Again the read's own failure is stated by `_showError('Failed to check catalog status: ...')`. |
| packages/nightshade_app/lib/screens/settings/merged_sections.dart:53:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: The `_SubHeader` sits inside `if (third != null)`, and the only construction that supplies a third pane supplies its label with it (`AutofocusMergedSettings`, line 147, 'FOCUS MODEL'). A pane added without a label would show an unlabelled band, never another page's name. |
| packages/nightshade_app/lib/screens/settings/pairing_screen_parts/_notifier.dart | accepted_modeled_approximation | app-adjudication | reason: Family, one shape and one consumer contract for every catch in this notifier EXCEPT the polling one at line 245 (row below): each sets `state.error` to a localized key ('pairingErrorLoad', 'pairingErrorStart', 'pairingErrorCancel', 'pairingErrorRevoke', 'pairingErrorRevokeAll', 'pairingErrorRename', 'pairingErrorDelete') and returns false, so the caller never reports a pairing action that did not happen. `_PairingErrorBanner` renders the key through `context.l10n.text(message)` (_dialogs_and_banners.dart:212, mounted at pairing_screen.dart:45) and the keys are defined in lib/localization/nightshade_localizations/translations.dart. |
| packages/nightshade_app/lib/screens/settings/pairing_screen_parts/_notifier.dart:245:empty_catch | accepted_modeled_approximation | app-adjudication | reason: `_checkPairingCompleted` runs on the 1 Hz countdown tick. Reading a failed `getPairingSession` as 'the code was consumed' would cancel the timers and wipe a still-live pairing code off the screen; swallowing it leaves the code, the countdown and `_devicesWhenCodeIssued` untouched and the next tick asks again, and `_completionCheckInFlight` cleared in the finally keeps those retries serialized. A code that really was consumed is detected on any later tick. |
| packages/nightshade_app/lib/screens/settings/widgets/location_settings_parts/_timezone_data.dart:163:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `negativeHemisphereSeen` is a local `bool?` assigned only where an N/S/E/W letter was actually stripped (lines 122 and 128); null means the string carried no hemisphere letter at all, and then the sign comes from the '-' prefix alone. Line 137 already rejects the contradictory '-44 S' input outright, so `?? false` cannot flip a hemisphere the operator did state. |
| packages/nightshade_app/lib/screens/settings/widgets/log_viewer_parts/_actions.dart:66:empty_catch | accepted_modeled_approximation | app-adjudication | reason: This inner catch skips ONE retained log file whose `length()` failed, which matches what `LoggingService.exportLogs` can itself read back, and the prompt prefixes the number with 'about'. The other failure - the whole set failing to enumerate, where nothing at all was measured - is handled separately by the outer catch and no longer reports 0. |
| packages/nightshade_app/lib/screens/settings/widgets/log_viewer_parts/_actions.dart:70:empty_catch | done | app-adjudication | reason: FIXED in this campaign. `service.getLogFiles()` throwing used to return the partial total, so the export prompt offered 'Full history - about 0 B' for a log directory it could not read, which states there is no history to export. The catch now returns null and `_askExportScope` renders 'Every retained log file - size could not be read'. Pinned by test/screens/settings/log_export_target_test.dart 'an unreadable log directory is said so, not reported as 0 bytes'. |
| packages/nightshade_app/lib/screens/settings/widgets/log_viewer_parts/_export_seams.dart:44:empty_catch | accepted_modeled_approximation | app-adjudication | reason: `defaultLogExportDirectory` supplies only the `initialDirectory` of the save picker; null means the picker opens at its own default and the operator still chooses and sees the destination that `chooseExportTarget` returns. No export is written to a guessed path on this failure, and a cancelled picker writes nothing at all. |
| packages/nightshade_app/lib/screens/shell/app_shell_parts/_startup_checkpoint.dart:124:empty_catch | accepted_modeled_approximation | app-adjudication | reason: `adoptLegacyGuiCheckpoint` moves a pre-upgrade checkpoint pair into the database-anchored directory. Each file is moved by its own `rename` behind an `exists` check on the destination, so a throw leaves the legacy file where the older build wrote it and can never overwrite this database's newer checkpoint. The outcome of a failed adoption equals the outcome of never migrating, and the caller still configures and writes tonight's checkpoint directory. |
| packages/nightshade_app/lib/screens/shell/app_shell_parts/_startup_checkpoint.dart:304:empty_catch | accepted_modeled_approximation | app-adjudication | reason: The `checkpointStillResumable()` probe is asked AFTER a resume or discard has already failed, and a probe that itself throws is read as 'cannot be resumed' - the conservative direction, since the modal would otherwise print 'the checkpoint is still on disk' directly under the executor's own failure. Its only effect is to drop the 'Retry Resume' button; Discard and 'Not now' remain, and 'Not now' leaves the checkpoint on disk for the next launch, so nothing recoverable is destroyed by the unknown. |
| packages/nightshade_app/lib/screens/suggestions/widgets/transient_alerts_panel_parts/_settings_dialog.dart:17:empty_catch | accepted_modeled_approximation | app-adjudication | reason: `_run` wraps every notifier call in this dialog and shows 'Could not save the alert settings.' on failure. The state does not silently keep the optimistic value either: `TransientAlertSettingsNotifier._queueSettingsUpdate` (packages/nightshade_core/lib/src/providers/transient_alerts/transient_alert_settings.dart:251) restores `_confirmedSettings` and rethrows when the DAO write fails, so the toggle snaps back to what is persisted while the snackbar states the failure. |
| packages/nightshade_app/lib/utils/snackbar_helper.dart:49:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `MediaQuery.maybeOf(this)` is null only in a host that installed none (a bare MaterialApp in a test, an embedded surface); width 0 makes `useBottomNav` false through the `width > 0 &&` on line 53 and pins the toast to the 15 px left margin. This value is snackbar MARGIN only - the message text and its severity are untouched - and line 44 already returns null (no margin) for a non-floating snackbar theme. |
| packages/nightshade_app/lib/utils/snackbar_helper.dart:50:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: Same null MediaQuery: with no padding to read, `safeBottom` adds 0 to the lift and the toast rides on the declared `TransientBottomInset` or the computed shell chrome height alone. The only consequence is a toast that sits lower on a host that never described a safe area. |
| packages/nightshade_app/lib/widgets/notification_toast_overlay.dart:81:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `n.title ?? ''` is one component of `notificationContentSignature`, the de-duplication key. Every signature is built by this one function, so a null title is a stable self-consistent component: two title-less notifications at the same level with the same body collapse into one group, and a titled one keeps its own key. The signature is never persisted and never shown - the card renders `n.title` itself. |

| native/nightshade_native/alpaca/src/camera/image_array.rs:72:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: this `unwrap_or(0)` fills the `rank` field of the `AlpacaError::UnsupportedImageArray` error being constructed on the next lines, after `Type` was already rejected. Making Rank a `?` here would replace the real "unrecognised Type {n}" reason with a "Missing Rank" ParseError. The success path re-reads Rank at line 78 with `ok_or_else(ParseError)`, so no array is ever decoded with rank 0. |
| native/nightshade_native/alpaca/src/client/config.rs:199:unwrap_or_default | accepted_modeled_approximation | native-vendor-hardening | reason: `SystemTime::now().duration_since(UNIX_EPOCH)` fails only on a pre-1970 clock; `Duration::ZERO.subsec_nanos()` then yields 0, i.e. `rand_simple()` returns 0.0 and that one retry gets no backoff jitter. Nothing is retried more or fewer times: the retry count and base delay come from RetryConfig, jitter only spreads concurrent clients. |
| native/nightshade_native/alpaca/src/client/mod.rs:45:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: `decode_put_response` reads ErrorNumber from an Alpaca device response, where 0 is the protocol's "no error" value and the field is mandatory per the ASCOM Alpaca API. An absent field therefore means success, and the function still fails on a non-zero code via `AlpacaError::DeviceError`. Transport and HTTP-status failures never reach this function; they are returned by the reqwest layer above it. |
| native/nightshade_native/imaging/src/platesolve_wcs_conformance_tests.rs:98:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: conformance test. `cards` cannot be empty at this line because the `assert_eq!(cards.len(), expected["count"])` five lines above already failed the test for an empty header, so `max()` is Some. A 0 would only make the following `assert_eq!(max_len, expected["max_length"])` fail, which is the test doing its job. |
| native/nightshade_native/imaging/src/recipe/model.rs:800:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: FITS SIP convention. A header with no `A_ORDER` card carries no forward SIP distortion terms, and order 0 makes `SipWcs` evaluate as the pure linear CD-matrix solution that was already parsed with `?` above (and rejected when the determinant is zero or non-finite). Absent SIP is a valid WCS, not a lost value. |
| native/nightshade_native/imaging/src/recipe/model.rs:801:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: FITS SIP convention. A header with no `B_ORDER` card carries no forward SIP distortion terms for the second axis, and order 0 evaluates as the pure linear CD-matrix solution parsed with `?` above. Absent SIP is a valid WCS, not a lost value. |
| native/nightshade_native/imaging/src/recipe/model.rs:802:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: FITS SIP convention. `AP_ORDER` is the optional inverse-polynomial order; absent means this header ships no inverse SIP, so order 0 selects the numeric inversion path instead of evaluating a polynomial that is not there. |
| native/nightshade_native/imaging/src/recipe/model.rs:803:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: FITS SIP convention. `BP_ORDER` is the optional inverse-polynomial order for the second axis; absent means no inverse SIP is published, so order 0 selects the numeric inversion path rather than a fabricated polynomial. |
| native/nightshade_native/imaging/src/recipe/pyramid.rs:187:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: `scale_sip_polynomial` returns immediately on the next statement when order is 0, so a header without `<prefix>_ORDER` simply has no SIP polynomial to rescale for this pyramid level. The linear CD terms are scaled separately just above and are unaffected. |
| native/nightshade_native/imaging/src/stacking.rs:1379:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: `frames.first()` is None only for an empty stack, and the very next call `combine_master_frames_banded(frames, ...)` rejects `frames.is_empty()` with "cannot build {kind} master from zero frames" before `band_rows` is ever used. `row_samples` only sizes the streaming band. |
| native/nightshade_native/imaging/src/stacking.rs:1383:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: `BAND_BUDGET_BYTES.checked_div(bytes_per_row)` returns None only when bytes_per_row is 0, which happens only for the empty stack that `combine_master_frames_banded` rejects on the next line. The `.max(1)` that follows also guarantees a band height of at least one row, so the banded combiner never divides the frame into zero-row bands. |
| native/nightshade_native/indi/src/client/jitter.rs:43:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: `duration_since(UNIX_EPOCH)` fails only for a pre-1970 clock. The value is one of three XOR-ed seed inputs for the per-client fastrand generator; the process-local `INSTANCE_COUNTER` (multiplied by a 64-bit odd constant) and the host hash still make the seed unique when the timestamp contributes 0. No identity or protocol field is derived from it. |
| native/nightshade_native/indi/src/client/mod.rs:444:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: `shutdown_tx` is a `oneshot::Sender` whose receiver is the reader task (`reader.rs:73` selects on it). Send fails only when that task has already exited, which is exactly the state `disconnect` is driving to. The disconnect completes regardless: the next lines drop `self.tx` (closing the writer task), set `connected = false` and clear devices, properties, blobs and staleness timestamps. |
| native/nightshade_native/indi/src/client/properties.rs:92:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: `get_property_state` returns None only when the device has not yet defined the property (no `defVector` seen). An undefined property cannot be Busy, so false is the correct answer for `is_property_busy`, and callers that need existence use `has_property` directly below. |
| native/nightshade_native/indi/src/client/properties.rs:228:unwrap_or_default | accepted_modeled_approximation | native-vendor-hardening | reason: the empty Vec produced here is immediately tested by `if elements.is_empty()` on the next line, which returns `IndiError::PropertyNotFound { device, property }`. An unknown property therefore fails the switch write with a typed error instead of silently sending nothing. |
| native/nightshade_native/indi/src/client/properties.rs:356:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: `get_switch(device, "CONNECTION", "CONNECT")` returns None until the reader has seen `defSwitchVector CONNECTION` for the device. Reporting "not connected" until the property-discovery handshake delivers it is the fail-closed answer; callers re-poll, and the actual connect path waits on the property rather than on this predicate. |
| native/nightshade_native/indi/src/client/reader.rs:389:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: `event_tx` is a `broadcast::Sender`; send returns Err only when there are zero subscribers. The device was inserted into the shared `devices` map in the same block two lines above, which is what `get_devices()` reads, and every subsequent property event for the device goes through `send_indi_event`, which logs the no-subscriber drop. |
| native/nightshade_native/indi/src/client/reader.rs:409:unwrap_or_default | accepted_modeled_approximation | native-vendor-hardening | reason: `group` is an optional attribute in the INDI 1.7 protocol; an empty string is the protocol's "ungrouped" value and the UI groups by property name in that case. The mandatory identity fields (device, name) come from the parsed vector element, not from this attribute. |
| native/nightshade_native/indi/src/client/reader.rs:562:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: `parse_indi_usize_attribute` already logs the malformed `size` attribute at warn before returning None. The 0 that lands in `current_blob_size` only appears in the BLOB-timeout diagnostic text and in the `size` field of `IndiEvent::BlobReceived`, which every consumer ignores (both `indi/src/camera.rs:758` and `bridge/.../download.rs:399` destructure `data` and `..`). The payload length comes from the decoded base64 and the format from `resolve_blob_format`. |
| native/nightshade_native/indi/src/client/reader.rs:631:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: the frame being popped was pushed onto `xml_stack` seven lines above (`xml_stack.push(frame)`), so `pop()` is Some by construction. The pop exists because quick-xml emits no `End` event for a self-closing tag; the following `refresh_xml_context_mirrors` re-derives device, property and element from whatever parent frames remain. |
| native/nightshade_native/indi/src/client/reader.rs:669:unwrap_or_default | accepted_modeled_approximation | native-vendor-hardening | reason: `BytesText::unescape` errors only on a malformed entity reference inside text the tokenizer already accepted. The empty string then stored is what `get_number` and `get_switch` treat as "value not yet present", identical to an element the server has not sent, so no numeric or switch value is fabricated from it. |
| native/nightshade_native/indi/src/client/tests.rs:201:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: test fake INDI server. The `.expect("read initial getProperties")` on the same expression already panics the spawned task on a read error; only the byte count is dropped, and the test asserts on the switch command the client sends afterwards, not on the length of its handshake. |
| native/nightshade_native/indi/src/client/tests.rs:693:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: test. `disconnect()` is called against a client that was never connected (no socket), so it returns the NotConnected-path Result the test does not care about. The assertion on the next line, `reader_consecutive_failures() == 0`, is the actual subject: it proves the counter reset ran. |
| native/nightshade_native/indi/src/client/tests.rs:836:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: test. Same never-connected client as the other disconnect tests; the three assertions that follow (keepalive flag cleared, reconnecting cleared, reconnect_attempts == 0) are what the test verifies, so the Result carries no information the test needs. |
| native/nightshade_native/indi/src/client/xml.rs:236:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: `current_time_ms` falls back to 0 only for a pre-1970 clock. Its output feeds keepalive and property-staleness comparisons that are differences between two of these timestamps, so a 0 baseline defers the first keepalive by one cycle and cannot mark stale data fresh (a 0 stamp reads as maximally old). |
| native/nightshade_native/indi/src/client/xml.rs:369:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: `is_version_compatible` parses the server-reported INDI protocol version; a missing or non-numeric major component becomes 0, which makes the server compare as older than any `required` and FAILS the compatibility gate. The fallback is the conservative direction for a version comparator. |
| native/nightshade_native/indi/src/client/xml.rs:370:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: same comparator as line 369 for the minor component; a version string with no minor part ("2") compares as 2.0, and a non-numeric one as 0, both of which can only make the server look older and fail the `>= required` gate rather than pass it. |
| native/nightshade_native/native/src/vendor/atik/camera.rs:389:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: ArtemisAbortExposure during `disconnect`, immediately before the ArtemisDisconnect whose result IS checked and returned as `NativeError::SdkError`. Releasing the camera session ends the exposure anyway; the abort is an early quiesce, and there is no separate recovery for a camera we are handing back to the SDK. |
| native/nightshade_native/native/src/vendor/fli/camera.rs:315:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: FLICancelExposure during `disconnect`, immediately before FLIClose whose result IS checked with `check_fli_error(result, "close camera")?`. The close releases the device and ends any exposure with it, so the cancel is an early quiesce whose failure cannot leave the handle open. |
| native/nightshade_native/native/src/vendor/fli/camera_ops.rs:251:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: FLIEndExposure runs after the frame has already been read out and decoded into `data` in the lines above (the grab itself is checked and returns SdkError on failure). It only returns the camera to idle; the next exposure calls FLIExposeFrame, whose result is checked, so a failed end-of-exposure surfaces there instead of corrupting the frame in hand. |
| native/nightshade_native/native/src/vendor/fli/discovery.rs:113:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: FLIClose on the probe handle after the serial number has been read; the discovery entry is built from the values already copied out. A close failure cannot change the entry, and the device is re-opened (checked) by `connect` if the user selects it. |
| native/nightshade_native/native/src/vendor/fli/discovery.rs:145:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: FLIDeleteList releases the SDK-internal enumeration list after every entry has been copied into `devices`; the function returns `Ok(devices)` on the next line. A failure frees nothing extra for us to act on, and the next scan calls FLICreateList again, which is the checked entry point. |
| native/nightshade_native/native/src/vendor/fujifilm/camera_ops.rs:560:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: unreachable fallback. The enclosing `if self.supported_isos.is_empty()` branch returns the documented (100, 12800) default, so this line only runs on a non-empty Vec where `iter().min()` is always Some. |
| native/nightshade_native/native/src/vendor/fujifilm/camera_ops.rs:561:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: unreachable fallback, same guard as line 560. `supported_isos` was proven non-empty by the branch above, so `iter().max()` is always Some and 12800 is never substituted for a real maximum. |
| native/nightshade_native/native/src/vendor/fujifilm/discovery.rs:21:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: XSDK_Init is idempotent and returns an "already initialized" error on every scan after the first, which is the normal case for repeated discovery. A genuine init failure is not hidden: XSDK_Detect is called immediately below with its result checked over three attempts, and discovery returns the SDK error when no camera can be enumerated. |
| native/nightshade_native/native/src/vendor/gphoto2/camera.rs:485:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: `position()` returns None when the camera reports an ISO string absent from the enumerated `iso_values` table (a firmware label such as "Hi 1"). Index 0 is the camera's own lowest enumerated ISO, and the value is only a starting index: the next `set_gain` writes a real enumerated ISO to the camera before any exposure, and the read failure branch below logs at warn. |
| native/nightshade_native/native/src/vendor/gphoto2/camera_ops.rs:136:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: `gp_camera_exit` in `disconnect`, followed unconditionally by `gp_camera_free` and the pointer being nulled, then `gp_context_unref`. libgphoto2 documents exit as the session-release half of a pair whose memory release is `camera_free`; the free is what prevents the leak, and it runs whatever exit returned. |
| native/nightshade_native/native/src/vendor/gphoto2/camera_ops.rs:686:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: same teardown pair inside `Drop`, where no error channel exists. `gp_camera_free` and `gp_context_unref` run unconditionally after it on the non-null pointers, so the memory is released regardless of the exit status. |
| native/nightshade_native/native/src/vendor/lx200/discovery.rs:93:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: serial probe. `port.flush()` follows a `write_all` that was already checked by the enclosing `if ... .is_ok()`, and only drains the OS write buffer. If the bytes never reach the mount the probe's own 200 ms settle plus `port.read` produces no OnStep status string and the branch falls through to `false`, i.e. "not an OnStep mount". |
| native/nightshade_native/native/src/vendor/lx200/discovery.rs:122:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: serial probe for the OnStep product name, guarded by a checked `write_all`. A flush failure shows up as no readable response after the 200 ms settle, and the else-branch supplies the explicit "OnStep Mount" label for a mount already identified by the `:GU#` status probe above. |
| native/nightshade_native/native/src/vendor/lx200/discovery.rs:180:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: serial probe for the standard LX200 product name, guarded by a checked `write_all`. If the flush loses the command, the following `port.read` yields nothing and this port is simply not reported as an LX200 mount; nothing is published from a failed probe. |
| native/nightshade_native/native/src/vendor/lx200/discovery.rs:248:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: last-resort `:GR#` probe, guarded by a checked `write_all`. The mount is only recorded when the response both contains ':' and ends with '#', so a flush failure can only make this port drop out of the scan, never add a fabricated mount. |
| native/nightshade_native/native/src/vendor/lx200/mount_ops.rs:80:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: the `?` propagates every transport failure, including the 5 s response timeout, so a mount that does not answer `:CM#` fails the sync. Only the response body is dropped, and that body is the vendor's free-text confirmation string ("Coordinates matched.", the object name, etc.) with no standardised failure token. The target coordinates were set by the two `send_command_bool` calls above, whose 0/1 replies ARE checked. |
| native/nightshade_native/native/src/vendor/lx200/mount_ops.rs:178:comment_best_effort | accepted_modeled_approximation | native-vendor-hardening | reason: the comment labels an `if let Err(e)` branch that logs the persistence failure at warn; nothing is swallowed. The authoritative park state came from the `:GU#` telemetry read on the line above (whose serial error propagates with `?`) and was written to the in-memory `park_state` mirror; the file write is only the cross-restart cache, which the next telemetry read re-derives. |
| native/nightshade_native/native/src/vendor/lx200/protocol.rs:86:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: `optional_discovery_response` already returned None when `write_all` failed on the line above, and it returns None again through `port.read(&mut buf).ok()?` if no reply arrives. The flush only drains the OS buffer, so its failure resolves to the same None the caller treats as "this optional identity field is unavailable". |
| native/nightshade_native/native/src/vendor/player_one/camera.rs:329:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: diagnostic-only. Both values are formatted into the `NativeError::download_timeout` message that is being returned because the download already failed; `current_width` is a non-negative i32 set from the SDK ROI, so the 0 is unreachable and would at worst print "0" in an error string. No capture path reads it. |
| native/nightshade_native/native/src/vendor/player_one/camera.rs:330:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: same `download_timeout` diagnostic message as line 329, for the height. The error itself already carries the failure; a 0 here cannot influence framing, since the frame geometry is re-read from POAGetImageSize on every successful download. |
| native/nightshade_native/native/src/vendor/player_one/camera_ops.rs:41:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: POACloseCamera inside the `CleanupGuard` that only runs when a later step of `connect` returned Err; that original SDK error is the one propagated to the caller. Closing a camera we are abandoning has no follow-up action, and the guard is defused on the success path. |
| native/nightshade_native/native/src/vendor/player_one/camera_ops.rs:178:rust_drop_result | done | native-vendor-hardening | reason: fixed in this campaign. The site was `get_control_float(POA_TEMPERATURE).unwrap_or(0.0)` feeding `sensor_temp: Some(temp)`, which reported a fabricated 0 C whenever the SDK read failed; it now yields `Option<f64>` so a failed read is None. That matches ZWO (camera_ops.rs:243), QHY (camera_ops.rs:199) and FLI, and stops a fake 0 C from reaching `ccd_temperature`, the ASCOM CCDTemperature property and the dark-library temperature bucket. |
| native/nightshade_native/native/src/vendor/player_one/camera_ops.rs:184:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: `self.camera_info` is None only before `load_camera_info` succeeds, and `connect` calls it first and propagates its error, so a connected camera always has it. When it is None the camera is not connected and `has_cooler = false` merely suppresses the cooler fields of the status; `get_status` itself already returned NotConnected in that case. |
| native/nightshade_native/native/src/vendor/player_one/camera_ops.rs:209:rust_drop_result | accepted_modeled_approximation | native-vendor-hardening | reason: the Option is the point. `live_enabled` is None when the camera or firmware does not expose POA_COOLER for read-back, and the code below falls back to `cached.enabled`, the value `set_cooler` recorded after the SDK accepted the write. The cache is refreshed only from successful reads, so a transient SDK error keeps the last known truth instead of reporting the cooler off. |
| native/nightshade_native/native/src/vendor/player_one/camera_ops.rs:428:rust_drop_result | accepted_modeled_approximation | native-vendor-hardening | reason: `ImageMetadata.temperature` is `Option<f64>`; None is the honest "sensor temperature unknown for this frame" and is what the FITS writer omits CCD-TEMP for. No substitute value is invented, and the frame itself is already downloaded and checked above. |
| native/nightshade_native/native/src/vendor/player_one/camera_ops.rs:433:rust_drop_result | accepted_modeled_approximation | native-vendor-hardening | reason: POA_HEATER_POWER is an optional anti-dew control. None leaves `vendor_features.anti_dew_heater` unset (the `if let Some(hp)` below only writes it on a successful read), so a camera without the control reports no heater rather than a fabricated "off". |
| native/nightshade_native/native/src/vendor/player_one/camera_ops.rs:507:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: fail-closed capability guard in `set_cooler`. When `camera_info` is absent (camera not loaded) the expression is false and the very next statement returns `NativeError::NotSupported`, so the cooler is never driven on an unknown camera. |
| native/nightshade_native/native/src/vendor/player_one/camera_ops.rs:559:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: fail-closed capability guard in `get_cooler_power`, identical shape to the `set_cooler` guard at line 507: false leads directly to `return Err(NativeError::NotSupported)` rather than to a fabricated power reading. |
| native/nightshade_native/native/src/vendor/player_one/camera_ops.rs:689:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: `info.max_width` is a c_int from POAGetCameraProperties, so `u32::try_from` fails only on a negative value the SDK does not produce. This synchronous accessor has no error channel; 0 publishes a degenerate sensor that capture paths refuse, rather than a wrapped giant width. |
| native/nightshade_native/native/src/vendor/player_one/camera_ops.rs:690:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: same SDK-populated c_int contract as line 689 for `max_height`; only a negative SDK value could take the fallback, and 0 is the degenerate-sensor signal rather than a wrapped dimension. |
| native/nightshade_native/native/src/vendor/player_one/camera_ops.rs:699:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: `raw16_container_max_adu(0)` is defined and tested to return 65535, the 16-bit container ceiling (`vendor/player_one/tests.rs`, `raw16_container_max_adu_unknown_bit_depth_falls_back_to_container`), which is exactly the right answer when bit depth is unknown: `connect` hard-fails unless POASetImageFormat(Raw16) succeeds, so every frame is a 16-bit container. |
| native/nightshade_native/native/src/vendor/player_one/camera_ops.rs:700:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: `info.bit_depth` is a c_int from POAGetCameraProperties; the fallback needs a negative SDK value. A published 0 reads as "ADC depth unknown" for the informational SensorInfo field, while the pixel scale that calibration uses is `max_adu`, which resolves to the 65535 container ceiling on line 699. |
| native/nightshade_native/native/src/vendor/player_one/filter_wheel.rs:127:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: POAClosePW inside the `CleanupGuard` that runs only when a later step of the filter-wheel `connect` returned Err; that SDK error is what the caller receives. The guard is defused on success, so this close only ever unwinds a connect that has already failed. |
| native/nightshade_native/native/src/vendor/player_one/mod.rs:36:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: module doc comment, not executable code. The bullet documents the `unwrap_or(false)` cooler-capability guards in camera_ops.rs (lines 184, 507, 559), each of which is registered against its own site with the fail-closed mechanism. |
| native/nightshade_native/native/src/vendor/qhy/camera_ops.rs:199:rust_drop_result | accepted_modeled_approximation | native-vendor-hardening | reason: `CameraStatus.sensor_temp` is `Option<f64>` and this None is the honest "CONTROL_CURTEMP unavailable" answer; no 0 C is substituted. Consumers treat None as unknown (`device_capabilities/native.rs` maps it straight to `ccd_temperature`, the ASCOM wrapper turns it into NotSupported). |
| native/nightshade_native/native/src/vendor/qhy/camera_ops.rs:401:rust_drop_result | accepted_modeled_approximation | native-vendor-hardening | reason: `ImageMetadata.temperature` is `Option<f64>`; None omits the CCD-TEMP card for that frame instead of writing a fabricated reading. The frame data itself was already validated (length and geometry checked against GetQHYCCDMemLength above). |
| native/nightshade_native/native/src/vendor/svbony/camera.rs:478:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: SVBStopVideoCapture during `disconnect`, immediately before SVBCloseCamera whose result IS checked and returned as a NativeError. The SDK documents stop as idempotent, and closing the camera tears the stream down regardless. |
| native/nightshade_native/native/src/vendor/svbony/camera_ops.rs:238:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: SVBStopVideoCapture after the frame has been read out successfully (the SVBGetVideoData result was checked and returned an error on failure just above). If the stop did not take, the next `start_exposure` calls SVBStartVideoCapture and propagates its error, so the failure surfaces there rather than silently corrupting the frame in hand. |
| native/nightshade_native/native/src/vendor/touptek/camera.rs:8:unimplemented_behavior | explicit_unsupported | native-vendor-hardening | reason: the flagged text is the SAFETY comment on `unsafe impl Send for HandleWrapper`, documenting that `Sync` is deliberately NOT implemented because the wrapper holds a raw `*mut c_void` handle. That is a compile-time trait decision, not a runtime path: `Mutex<HandleWrapper>` requires only Send to be Sync, and every SDK call takes both `touptek_mutex()` and the handle lock. |
| native/nightshade_native/native/src/vendor/touptek/camera_ops.rs:184:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: 16 is the container this driver negotiates, not a guess: `connect` writes OGMACAM_OPTION_BITDEPTH = 1, reads it back, and returns "did not negotiate 16-bit single-channel RAW output" (closing the handle) unless both RAW and BITDEPTH read back as 1. `read_touptek_raw_format` also logs the get_RawFormat failure at warn before returning None, and `max_adu_from_bit_depth(16)` is the matching 65535 ceiling. |
| native/nightshade_native/native/src/vendor/touptek/camera_ops.rs:384:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: Ogmacam_Stop is the first half of the documented Stop-then-Close teardown; the Ogmacam_Close on the next line is unconditional and is what releases the handle and quiesces the streaming thread. The ordering (not the stop's status) is what keeps the event-state box alive until no callback can fire. |
| native/nightshade_native/native/src/vendor/touptek/camera_ops.rs:412:comment_best_effort | accepted_modeled_approximation | native-vendor-hardening | reason: the comment heads a `Drop` impl that cannot await the async vendor mutex, so it only touches the SDK when `touptek_mutex().try_lock()` proves no other call is in flight, and it tracks the outcome in `quiesced` rather than assuming it. The normal path is `disconnect`, which does the same teardown with the lock held and returns its errors. |
| native/nightshade_native/native/src/vendor/zwo/camera.rs:262:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: the `?` propagates the SDK error, so nothing is swallowed; only the value is discarded, deliberately. This is the ZWO quirk path that throws away the first ASI_TEMPERATURE sample after connect (the SDK returns a stale reading), and the real measurement is the checked `get_control` on the next line. |
| native/nightshade_native/native/src/vendor/zwo/camera.rs:571:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: diagnostic-only. The value is formatted into the `NativeError::download_timeout` message being returned because the download already timed out; `current_width` is a non-negative i32 set from the validated ROI, so the fallback is unreachable and could only print "0" in an error string. |
| native/nightshade_native/native/src/vendor/zwo/camera.rs:572:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: same `download_timeout` diagnostic as line 571, for the height. Frame geometry on the success path comes from ASIGetROIFormat and is converted with `try_from` plus a typed SdkError, not from these numbers. |
| native/nightshade_native/native/src/vendor/zwo/camera_ops.rs:83:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: ASICloseCamera inside the `CleanupGuard` that runs only when a later step of `connect` returned Err; the SDK error from that step is what the caller receives. The guard is defused on the success path, so this close only unwinds an already-failed connect. |
| native/nightshade_native/native/src/vendor/zwo/camera_ops.rs:243:rust_drop_result | accepted_modeled_approximation | native-vendor-hardening | reason: `CameraStatus.sensor_temp` is `Option<f64>`; None is the honest "temperature unavailable" and no 0 C is substituted. This is the contract the other vendor drivers follow, and consumers map None to an absent `ccd_temperature` or an ASCOM NotSupported. |
| native/nightshade_native/native/src/vendor/zwo/camera_ops.rs:515:rust_drop_result | accepted_modeled_approximation | native-vendor-hardening | reason: `ImageMetadata.temperature` is `Option<f64>`; a failed ASI_TEMPERATURE read omits CCD-TEMP for that frame rather than writing a fabricated value. The frame itself is already downloaded and its geometry checked with `try_from` plus typed SdkErrors below. |
| native/nightshade_native/native/src/vendor/zwo/camera_ops.rs:804:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: `info.max_width` is a c_long from ASIGetCameraProperty, so `try_from` fails only on a negative value the SDK does not produce. This synchronous accessor cannot return an error; 0 publishes a degenerate sensor the capture paths refuse, rather than a wrapped giant dimension. |
| native/nightshade_native/native/src/vendor/zwo/camera_ops.rs:805:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: same SDK contract as line 804 for `max_height`; only SDK corruption (a negative c_long) takes the fallback, and 0 is the degenerate-sensor signal rather than a wrapped dimension. |
| native/nightshade_native/native/src/vendor/zwo/camera_ops.rs:812:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: `raw16_container_max_adu(0)` returns the 65535 container ceiling (tested in vendor/player_one/tests.rs), which is the correct full scale here because this driver runs the ASI SDK in Raw16 and the SDK left-justifies sub-16-bit samples into that container. |
| native/nightshade_native/native/src/vendor/zwo/camera_ops.rs:813:unwrap_or_literal | accepted_modeled_approximation | native-vendor-hardening | reason: `info.bit_depth` is a c_int (8..=16 in practice) from ASIGetCameraProperty; the fallback needs a negative SDK value. A published 0 reads as "ADC depth unknown" on the informational field, while the scale calibration uses (`max_adu`) resolves to the container ceiling above. |
| native/nightshade_native/native/src/vendor/zwo/filter_wheel.rs:285:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: EFWClose inside the `CleanupGuard` that runs only when a later step of the filter-wheel `connect` returned Err; that error is what the caller receives, and the guard is defused on success. |
| native/nightshade_native/native/src/vendor/zwo/filter_wheel.rs:655:rust_let_underscore | done | native-vendor-hardening | reason: added by this campaign. EFWGetProperty is now checked, and this EFWClose releases the probe handle on the new skip path before `continue` drops the wheel from the scan; the reason is already reported by the `tracing::error!` immediately above, so the close status adds nothing. Previously a failed EFWGetProperty published a wheel with an empty name and slot_num 0. |
| native/nightshade_native/native/src/vendor/zwo/filter_wheel.rs:687:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: EFWClose ending the open-query-close discovery probe after name, slot count and serial number have been copied out of the SDK structs. The entry pushed on the next lines is built from those copies, and `connect` re-opens the wheel with a checked EFWOpen if the user selects it. |
| native/nightshade_native/native/src/vendor/zwo/focuser.rs:230:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: EAFClose inside the `CleanupGuard` that runs only when a later step of the focuser `connect` returned Err; that SDK error is what propagates, and the guard is defused on the success path. |
| native/nightshade_native/native/src/vendor/zwo/focuser.rs:651:rust_let_underscore | done | native-vendor-hardening | reason: added by this campaign. EAFGetProperty is now checked, and this EAFClose releases the probe handle on the new skip path before `continue` drops the focuser from the scan; the `tracing::error!` above already names the SDK code. Previously a failed EAFGetProperty published a focuser whose name was the empty string. |
| native/nightshade_native/native/src/vendor/zwo/focuser.rs:682:rust_let_underscore | accepted_modeled_approximation | native-vendor-hardening | reason: EAFClose ending the open-query-close discovery probe after the name and serial number have been copied out of the SDK structs. The pushed entry uses those copies, and `connect` re-opens the focuser with a checked EAFOpen if the user selects it. |

## adjudicate-native-bridge (native/nightshade_native/bridge)

| native/nightshade_native/bridge/src/device_capabilities/alpaca.rs | accepted_modeled_approximation | native-bridge-adjudication | reason: One family — `get_alpaca_capabilities` probing OPTIONAL ASCOM properties over Alpaca. A driver signals 'not implemented' with Alpaca error 0x400, so `unwrap_or(false)` / `unwrap_or(0)` / `unwrap_or_default()` at each site IS the spec's 'feature absent' answer rather than an invented value, and every one of them lands only in the `DeviceCapabilities` struct the equipment dialog renders. The two reads that must not degrade do not: `connect()` and `AlpacaFilterWheel::names()` return `NightshadeError` so a failed names lookup can never be reported as a 0-position wheel. Restore-disconnect, `is_safe` and `max_switch` carry their own rows. |
| native/nightshade_native/bridge/src/device_capabilities/ascom.rs | accepted_modeled_approximation | native-bridge-adjudication | reason: One family — the Windows ASCOM capability probe reading OPTIONAL COM properties. An unimplemented property raises 0x80020009 / PropertyNotImplemented, so the `unwrap_or` beside each read renders 'feature not advertised' into `DeviceCapabilities` for the equipment dialog; `wrapper.get_position()`/`fw.position()` keep `Option` so an unreadable position is absent, not slot 0. The two reads whose absence would fabricate a capability are handled explicitly above: `AscomFilterWheelWrapper::get_filter_names` propagates on the live-connection branch, and the cold-probe branch logs a warn naming the driver fault. COM init and restore-disconnect sites carry their own rows. |
| native/nightshade_native/bridge/src/device_capabilities/indi.rs | accepted_modeled_approximation | native-bridge-adjudication | reason: One family — INDI capability derivation from the driver's published property snapshot. INDI has no 'unsupported' error: a driver that lacks a feature simply never publishes the property, so `get_number` / `try_get_max_bin_x` / `get_names` returning None IS the absence signal and the `unwrap_or` renders it as 'capability absent'. The one value that could fabricate hardware, `position_count`, is derived from the FILTER_SLOT min/max limits FIRST and only falls back to `filter_names.len()`, so an unpublished FILTER_NAME list cannot shrink a real wheel to zero slots. |
| native/nightshade_native/bridge/src/device_capabilities/native.rs | accepted_modeled_approximation | native-bridge-adjudication | reason: One family — vendor-SDK capability reads where the SDK's own 'not supported' answer is an Err/None (the ZWO EFW errors on filter names it never persisted; `NativeCamera::get_cooler_temp_range` defaults to Ok(None)). Each fallback lands in the `DeviceCapabilities` the equipment view re-probes on a timer, and the numbers that must not be fabricated come from infallible accessors instead — `fw.get_filter_count()` for slot count and `camera.capabilities()`/`get_sensor_info()` for sensor geometry — not from the defaulted values. |
| native/nightshade_native/bridge/src/device_capabilities/types.rs | accepted_modeled_approximation | native-bridge-adjudication | reason: One family — `focuser_capabilities_from_ascom` flattening `AscomFocuserReadings`' Options into `FocuserCapabilities`. A None field means that ASCOM property threw PropertyNotImplemented during the probe; 0/false is the 'not advertised' rendering that disables the matching control. This file's own test `unreadable_properties_degrade_without_inventing_values` pins that the values which cannot be neutrally defaulted (position, temperature, step_size) stay None instead of becoming numbers. |
| native/nightshade_native/bridge/src/device_manager/ops/camera/status.rs | accepted_modeled_approximation | native-bridge-adjudication | reason: One family — flattening optional driver reads into the `CameraStatus` served by `/api/equipment/camera/status`. The `.ok()` sites (`get_capabilities`, `gain`, `offset`) produce the Options and every `unwrap_or` beside them renders 'not advertised': 0 sensor dimensions and 0.0 pixel size, which the Dart calibration UI gates on with `sensorWidth > 0`, and false `can_cool`/`can_set_gain`/`can_set_offset`, which HIDE a control rather than promise one that would throw. Nothing here reaches a FITS header or a driver command — exposure geometry is read straight from the driver in `ops/camera/exposure.rs` with propagated errors, and `ImageMetadata.gain` comes from `ops/camera/download.rs`, which maps an unreadable gain to UNKNOWN_CAMERA_SETTING so the configured value wins. The endpoint is re-polled at 500 ms, so a transient read failure corrects itself on the next tick. |
| native/nightshade_native/bridge/src/api/devices/simulation/filter_wheel.rs | accepted_modeled_approximation | native-bridge-adjudication | reason: Both sites are `state.last_position.unwrap_or(-1)` in the position-poll back-off path. -1 is the sentinel the vendor SDKs themselves report while a wheel is mid-move: `filter_wheel_poll_is_moving` maps -1 to moving=true for ASCOM, and `refresh_volatile_state` converts it to `None` (`if position >= 0`) before it reaches the capabilities UI, so it is never displayed as slot -1. It is returned only while the poll is backed off or has just failed, and that failure is logged at warn with the consecutive-failure count and the back-off delay. |
| native/nightshade_native/bridge/src/api/sky_atlas/fold.rs | accepted_modeled_approximation | native-bridge-adjudication | reason: One family — `frames_added` / `weight_added` / `rejected` are HashMaps keyed by tile id, and the loop walks `tiles_touched`, which is the union of every tile any frame reached. A tile present in the walk but absent from one map contributed nothing to that tally (every frame that touched it was rejected, or it gained no weight), so 0 / 0.0 is the measured value, not a stand-in for an unknown one. The persisted tile state reported alongside comes from `atlas.load_tile(tid)?`, which propagates. |
| native/nightshade_native/bridge/src/api/polar_alignment/run_loop.rs | accepted_modeled_approximation | native-bridge-adjudication | reason: Covers the `std::fs::remove_file(&temp_path)` cleanups of each point's capture in the OS temp dir. By the time each runs, the solver has already read the file (or the solve failed and its error is being returned or retried on the next line), so a failed unlink cannot change the run's outcome — it leaves one frame in the temp directory. The three run-option defaults at the top of `run_polar_alignment` carry their own rows. |
| native/nightshade_native/bridge/src/event/correlation.rs | accepted_modeled_approximation | native-bridge-adjudication | reason: `generate_correlation_id` and the doc line stating its contract. `SystemTime::duration_since(UNIX_EPOCH)` errors only for a clock set before 1970; the resulting `corr-0-<seq>` is still a well-formed id and the id is used only to stitch log lines together — no state, routing or persistence keys on it. |
| native/nightshade_native/bridge/src/sim_capture.rs | accepted_modeled_approximation | native-bridge-adjudication | reason: Test scaffolding and the guard test that protects it: `shared_platesolver_store` is `#[cfg(test)]` and re-points the process-wide plate-solver storage at a leaked temp dir (the init is idempotent, and a failure surfaces as the immediately following `save_platesolver_preference` expect), while the `guard_tests` hits are that module's own doc text and its assertions that `api_sequencer_set_simulation_mode` still installs NullDeviceOps only behind the `cfg!(debug_assertions)` refusal. None of it compiles into a release artifact. |
| native/nightshade_native/bridge/src/device_id/tests.rs | accepted_modeled_approximation | native-bridge-adjudication | reason: Cache-metrics tests: in each `let _ = parse_device_id_cached(...).unwrap()` the `.unwrap()` IS the assertion (a parse failure panics the test) and the parsed value is deliberately unused because the test asserts on `get_device_id_cache_stats()` hits/misses/size immediately afterwards. Test-only file. |
| native/nightshade_native/bridge/src/api/imaging/fits_keyword_update_tests.rs | accepted_modeled_approximation | native-bridge-adjudication | reason: The `TempDir(PathBuf)` guard's `Drop` removing its scratch tree. Teardown runs after every assertion in the test body; a failed removal leaves a directory in the OS temp dir and cannot make a test pass — each test creates its own uniquely-named directory, so a leftover tree is never read back. |
| native/nightshade_native/bridge/src/api/imaging/rich_header_tests.rs | accepted_modeled_approximation | native-bridge-adjudication | reason: Same `TempDir` `Drop` teardown as the sibling FITS-keyword tests: the removal runs after the assertions, each test owns a uniquely-named scratch directory, and a leftover tree cannot be read by another test. |
| native/nightshade_native/bridge/src/api/imaging/sim_exposure_tests.rs | accepted_modeled_approximation | native-bridge-adjudication | reason: Simulator exposure tests restoring process-wide state they borrowed: `get_platesolver_preference().unwrap_or_default()` snapshots the preference (absent on a fresh store = the default), and the `save_platesolver_preference` / `api_clear_device_image` calls afterwards put it back. Both restores run AFTER the assertions, so a failure there cannot turn a red test green; the tests hold `sim_singleton_test_lock` so no other test observes the borrowed state. |
| native/nightshade_native/bridge/src/api/post_session/tests.rs | accepted_modeled_approximation | native-bridge-adjudication | reason: The `TempDir` `Drop` teardown for the post-session scratch trees. The removal runs after the test body has asserted on the master it wrote; each test owns a uniquely-named directory keyed by pid and timestamp, so a leftover tree is never read back. |
| native/nightshade_native/bridge/src/api/post_session/reference_choice_tests.rs | accepted_modeled_approximation | native-bridge-adjudication | reason: The test's own copy of the reference-scoring expression from `helpers.rs` (`(1.0 - e).max(0.0)` with 1.0 for an unmeasurable eccentricity). It exists so the ranking can be exercised without an image, and it is asserted against the same ordering the production path produces — see the row on `api/post_session/helpers.rs:64` for the mechanism. |
| native/nightshade_native/bridge/src/api/sky_atlas/tests.rs | accepted_modeled_approximation | native-bridge-adjudication | reason: Sky-atlas test teardown: the `TempDir` `Drop` plus per-test `std::fs::remove_dir_all(&dir).ok()` calls. Every one runs after that test's assertions on the atlas it built, and each test creates its own uniquely named directory, so a failed cleanup leaks a temp tree and cannot affect any assertion. |
| native/nightshade_native/bridge/src/api/polar_alignment/polar_run_control_tests.rs | accepted_modeled_approximation | native-bridge-adjudication | reason: Run-control tests cleaning up: the `remove_file` calls drop scratch files after the assertions, and the `polar_generation().load(...)` read is a deliberate no-op fence that leaves the global generation counter untouched for the next test (the value is not needed — the test has already asserted on the flag). |
| native/nightshade_native/bridge/src/builtin_guider/tests.rs | accepted_modeled_approximation | native-bridge-adjudication | reason: Guider lifecycle tests: `let _ = (w, h)` marks the frame-size parameters a synthetic star field does not need, and the `stop_locked()` / `stop()` / `tokio::join!` results are discarded because each is a teardown or a race driver whose outcome is not the claim — the assertions that follow read the live-loop counter and the guider state to prove no loop was orphaned. Test-only file. |
| native/nightshade_native/bridge/src/unified_device_ops/pointing_tests.rs | accepted_modeled_approximation | native-bridge-adjudication | reason: The `TempDir` `Drop` teardown for the pointing tests' scratch tree: it runs after the assertions and each test owns a uniquely-named directory. |
| native/nightshade_native/bridge/src/device_capabilities/alpaca.rs:98:rust_drop_result | accepted_modeled_approximation | native-bridge-adjudication | reason: Probe-and-restore teardown. This `disconnect()` runs ONLY on the branch that opened the connection (`was_connected == false`) and only after every capability value has been read, so its failure cannot alter the `DeviceCapabilities` being returned. A refused disconnect leaves the driver connected, which the next probe detects through `is_connected()` and then reuses instead of disconnecting a session it does not own; the ops whose failure would corrupt the answer (`connect()`, `names()`) propagate `NightshadeError`. |
| native/nightshade_native/bridge/src/device_capabilities/alpaca.rs:179:rust_drop_result | accepted_modeled_approximation | native-bridge-adjudication | reason: Probe-and-restore teardown. This `disconnect()` runs ONLY on the branch that opened the connection (`was_connected == false`) and only after every capability value has been read, so its failure cannot alter the `DeviceCapabilities` being returned. A refused disconnect leaves the driver connected, which the next probe detects through `is_connected()` and then reuses instead of disconnecting a session it does not own; the ops whose failure would corrupt the answer (`connect()`, `names()`) propagate `NightshadeError`. |
| native/nightshade_native/bridge/src/device_capabilities/alpaca.rs:216:rust_drop_result | accepted_modeled_approximation | native-bridge-adjudication | reason: Probe-and-restore teardown. This `disconnect()` runs ONLY on the branch that opened the connection (`was_connected == false`) and only after every capability value has been read, so its failure cannot alter the `DeviceCapabilities` being returned. A refused disconnect leaves the driver connected, which the next probe detects through `is_connected()` and then reuses instead of disconnecting a session it does not own; the ops whose failure would corrupt the answer (`connect()`, `names()`) propagate `NightshadeError`. |
| native/nightshade_native/bridge/src/device_capabilities/alpaca.rs:271:rust_drop_result | accepted_modeled_approximation | native-bridge-adjudication | reason: Probe-and-restore teardown. This `disconnect()` runs ONLY on the branch that opened the connection (`was_connected == false`) and only after every capability value has been read, so its failure cannot alter the `DeviceCapabilities` being returned. A refused disconnect leaves the driver connected, which the next probe detects through `is_connected()` and then reuses instead of disconnecting a session it does not own; the ops whose failure would corrupt the answer (`connect()`, `names()`) propagate `NightshadeError`. |
| native/nightshade_native/bridge/src/device_capabilities/alpaca.rs:305:rust_drop_result | accepted_modeled_approximation | native-bridge-adjudication | reason: Probe-and-restore teardown. This `disconnect()` runs ONLY on the branch that opened the connection (`was_connected == false`) and only after every capability value has been read, so its failure cannot alter the `DeviceCapabilities` being returned. A refused disconnect leaves the driver connected, which the next probe detects through `is_connected()` and then reuses instead of disconnecting a session it does not own; the ops whose failure would corrupt the answer (`connect()`, `names()`) propagate `NightshadeError`. |
| native/nightshade_native/bridge/src/device_capabilities/alpaca.rs:350:rust_drop_result | accepted_modeled_approximation | native-bridge-adjudication | reason: Probe-and-restore teardown. This `disconnect()` runs ONLY on the branch that opened the connection (`was_connected == false`) and only after every capability value has been read, so its failure cannot alter the `DeviceCapabilities` being returned. A refused disconnect leaves the driver connected, which the next probe detects through `is_connected()` and then reuses instead of disconnecting a session it does not own; the ops whose failure would corrupt the answer (`connect()`, `names()`) propagate `NightshadeError`. |
| native/nightshade_native/bridge/src/device_capabilities/alpaca.rs:400:rust_drop_result | accepted_modeled_approximation | native-bridge-adjudication | reason: Probe-and-restore teardown. This `disconnect()` runs ONLY on the branch that opened the connection (`was_connected == false`) and only after every capability value has been read, so its failure cannot alter the `DeviceCapabilities` being returned. A refused disconnect leaves the driver connected, which the next probe detects through `is_connected()` and then reuses instead of disconnecting a session it does not own; the ops whose failure would corrupt the answer (`connect()`, `names()`) propagate `NightshadeError`. |
| native/nightshade_native/bridge/src/device_capabilities/alpaca.rs:452:rust_drop_result | accepted_modeled_approximation | native-bridge-adjudication | reason: Probe-and-restore teardown. This `disconnect()` runs ONLY on the branch that opened the connection (`was_connected == false`) and only after every capability value has been read, so its failure cannot alter the `DeviceCapabilities` being returned. A refused disconnect leaves the driver connected, which the next probe detects through `is_connected()` and then reuses instead of disconnecting a session it does not own; the ops whose failure would corrupt the answer (`connect()`, `names()`) propagate `NightshadeError`. |
| native/nightshade_native/bridge/src/device_capabilities/alpaca.rs:478:rust_drop_result | accepted_modeled_approximation | native-bridge-adjudication | reason: Probe-and-restore teardown. This `disconnect()` runs ONLY on the branch that opened the connection (`was_connected == false`) and only after every capability value has been read, so its failure cannot alter the `DeviceCapabilities` being returned. A refused disconnect leaves the driver connected, which the next probe detects through `is_connected()` and then reuses instead of disconnecting a session it does not own; the ops whose failure would corrupt the answer (`connect()`, `names()`) propagate `NightshadeError`. |
| native/nightshade_native/bridge/src/device_capabilities/alpaca.rs:539:rust_drop_result | accepted_modeled_approximation | native-bridge-adjudication | reason: Probe-and-restore teardown. This `disconnect()` runs ONLY on the branch that opened the connection (`was_connected == false`) and only after every capability value has been read, so its failure cannot alter the `DeviceCapabilities` being returned. A refused disconnect leaves the driver connected, which the next probe detects through `is_connected()` and then reuses instead of disconnecting a session it does not own; the ops whose failure would corrupt the answer (`connect()`, `names()`) propagate `NightshadeError`. |
| native/nightshade_native/bridge/src/device_capabilities/alpaca.rs:473:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: Fail-closed safety default. ISafetyMonitorV1.IsSafe is the whole content of this device, and every consumer of `SafetyMonitorCapabilities.is_safe` treats false as 'not safe' and refuses or aborts imaging. An unreadable monitor therefore can only stop a run, never authorise one; reporting the read failure as `true` is the only outcome that could hurt, and this is its inverse. |
| native/nightshade_native/bridge/src/device_capabilities/alpaca.rs:496:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: ISwitchV2.MaxSwitch unreadable enumerates 0 channels, so `SwitchCapabilities.switches` comes back empty and the device renders with no controllable outputs. No write can be misdirected by it: the switch write path takes an index the user picked from this enumerated list, so an empty list means nothing to command rather than a command against an unknown channel. |
| native/nightshade_native/bridge/src/device_capabilities/ascom.rs:108:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: `init_com()` is the per-thread COM apartment init and returns Err when this thread already has an apartment — the normal case on a reused tokio worker, where the existing apartment is exactly what the following calls need. A genuine COM failure cannot be swallowed: the very next statement constructs the ASCOM object (`AscomDeviceConnection::new` / `AscomFocuser::new` / `AscomFilterWheel::new` / …), which fails and is mapped to `NightshadeError::connection_failed` for the caller. |
| native/nightshade_native/bridge/src/device_capabilities/ascom.rs:301:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: `init_com()` is the per-thread COM apartment init and returns Err when this thread already has an apartment — the normal case on a reused tokio worker, where the existing apartment is exactly what the following calls need. A genuine COM failure cannot be swallowed: the very next statement constructs the ASCOM object (`AscomDeviceConnection::new` / `AscomFocuser::new` / `AscomFilterWheel::new` / …), which fails and is mapped to `NightshadeError::connection_failed` for the caller. |
| native/nightshade_native/bridge/src/device_capabilities/ascom.rs:391:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: `init_com()` is the per-thread COM apartment init and returns Err when this thread already has an apartment — the normal case on a reused tokio worker, where the existing apartment is exactly what the following calls need. A genuine COM failure cannot be swallowed: the very next statement constructs the ASCOM object (`AscomDeviceConnection::new` / `AscomFocuser::new` / `AscomFilterWheel::new` / …), which fails and is mapped to `NightshadeError::connection_failed` for the caller. |
| native/nightshade_native/bridge/src/device_capabilities/ascom.rs:457:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: `init_com()` is the per-thread COM apartment init and returns Err when this thread already has an apartment — the normal case on a reused tokio worker, where the existing apartment is exactly what the following calls need. A genuine COM failure cannot be swallowed: the very next statement constructs the ASCOM object (`AscomDeviceConnection::new` / `AscomFocuser::new` / `AscomFilterWheel::new` / …), which fails and is mapped to `NightshadeError::connection_failed` for the caller. |
| native/nightshade_native/bridge/src/device_capabilities/ascom.rs:500:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: `init_com()` is the per-thread COM apartment init and returns Err when this thread already has an apartment — the normal case on a reused tokio worker, where the existing apartment is exactly what the following calls need. A genuine COM failure cannot be swallowed: the very next statement constructs the ASCOM object (`AscomDeviceConnection::new` / `AscomFocuser::new` / `AscomFilterWheel::new` / …), which fails and is mapped to `NightshadeError::connection_failed` for the caller. |
| native/nightshade_native/bridge/src/device_capabilities/ascom.rs:553:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: `init_com()` is the per-thread COM apartment init and returns Err when this thread already has an apartment — the normal case on a reused tokio worker, where the existing apartment is exactly what the following calls need. A genuine COM failure cannot be swallowed: the very next statement constructs the ASCOM object (`AscomDeviceConnection::new` / `AscomFocuser::new` / `AscomFilterWheel::new` / …), which fails and is mapped to `NightshadeError::connection_failed` for the caller. |
| native/nightshade_native/bridge/src/device_capabilities/ascom.rs:583:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: `init_com()` is the per-thread COM apartment init and returns Err when this thread already has an apartment — the normal case on a reused tokio worker, where the existing apartment is exactly what the following calls need. A genuine COM failure cannot be swallowed: the very next statement constructs the ASCOM object (`AscomDeviceConnection::new` / `AscomFocuser::new` / `AscomFilterWheel::new` / …), which fails and is mapped to `NightshadeError::connection_failed` for the caller. |
| native/nightshade_native/bridge/src/device_capabilities/ascom.rs:621:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: `init_com()` is the per-thread COM apartment init and returns Err when this thread already has an apartment — the normal case on a reused tokio worker, where the existing apartment is exactly what the following calls need. A genuine COM failure cannot be swallowed: the very next statement constructs the ASCOM object (`AscomDeviceConnection::new` / `AscomFocuser::new` / `AscomFilterWheel::new` / …), which fails and is mapped to `NightshadeError::connection_failed` for the caller. |
| native/nightshade_native/bridge/src/device_capabilities/ascom.rs:680:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: `init_com()` is the per-thread COM apartment init and returns Err when this thread already has an apartment — the normal case on a reused tokio worker, where the existing apartment is exactly what the following calls need. A genuine COM failure cannot be swallowed: the very next statement constructs the ASCOM object (`AscomDeviceConnection::new` / `AscomFocuser::new` / `AscomFilterWheel::new` / …), which fails and is mapped to `NightshadeError::connection_failed` for the caller. |
| native/nightshade_native/bridge/src/device_capabilities/ascom.rs:193:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: Probe-and-restore teardown for the cold ASCOM probe. It runs only when `capability_probe_should_own_connection` said this probe opened the device, and only after the capability and live-state reads are done, so its failure cannot change the returned capabilities. A refused disconnect leaves the driver connected and the next probe sees that through `is_connected()` and reuses the session rather than disconnecting one it does not own. |
| native/nightshade_native/bridge/src/device_capabilities/ascom.rs:246:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: Probe-and-restore teardown for the cold ASCOM probe. It runs only when `capability_probe_should_own_connection` said this probe opened the device, and only after the capability and live-state reads are done, so its failure cannot change the returned capabilities. A refused disconnect leaves the driver connected and the next probe sees that through `is_connected()` and reuses the session rather than disconnecting one it does not own. |
| native/nightshade_native/bridge/src/device_capabilities/ascom.rs:323:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: Probe-and-restore teardown for the cold ASCOM probe. It runs only when `capability_probe_should_own_connection` said this probe opened the device, and only after the capability and live-state reads are done, so its failure cannot change the returned capabilities. A refused disconnect leaves the driver connected and the next probe sees that through `is_connected()` and reuses the session rather than disconnecting one it does not own. |
| native/nightshade_native/bridge/src/device_capabilities/ascom.rs:442:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: Probe-and-restore teardown for the cold ASCOM probe. It runs only when `capability_probe_should_own_connection` said this probe opened the device, and only after the capability and live-state reads are done, so its failure cannot change the returned capabilities. A refused disconnect leaves the driver connected and the next probe sees that through `is_connected()` and reuses the session rather than disconnecting one it does not own. |
| native/nightshade_native/bridge/src/device_capabilities/ascom.rs:493:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: Probe-and-restore teardown for the cold ASCOM probe. It runs only when `capability_probe_should_own_connection` said this probe opened the device, and only after the capability and live-state reads are done, so its failure cannot change the returned capabilities. A refused disconnect leaves the driver connected and the next probe sees that through `is_connected()` and reuses the session rather than disconnecting one it does not own. |
| native/nightshade_native/bridge/src/device_capabilities/ascom.rs:546:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: Probe-and-restore teardown for the cold ASCOM probe. It runs only when `capability_probe_should_own_connection` said this probe opened the device, and only after the capability and live-state reads are done, so its failure cannot change the returned capabilities. A refused disconnect leaves the driver connected and the next probe sees that through `is_connected()` and reuses the session rather than disconnecting one it does not own. |
| native/nightshade_native/bridge/src/device_capabilities/ascom.rs:576:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: Probe-and-restore teardown for the cold ASCOM probe. It runs only when `capability_probe_should_own_connection` said this probe opened the device, and only after the capability and live-state reads are done, so its failure cannot change the returned capabilities. A refused disconnect leaves the driver connected and the next probe sees that through `is_connected()` and reuses the session rather than disconnecting one it does not own. |
| native/nightshade_native/bridge/src/device_capabilities/ascom.rs:614:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: Probe-and-restore teardown for the cold ASCOM probe. It runs only when `capability_probe_should_own_connection` said this probe opened the device, and only after the capability and live-state reads are done, so its failure cannot change the returned capabilities. A refused disconnect leaves the driver connected and the next probe sees that through `is_connected()` and reuses the session rather than disconnecting one it does not own. |
| native/nightshade_native/bridge/src/device_capabilities/ascom.rs:673:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: Probe-and-restore teardown for the cold ASCOM probe. It runs only when `capability_probe_should_own_connection` said this probe opened the device, and only after the capability and live-state reads are done, so its failure cannot change the returned capabilities. A refused disconnect leaves the driver connected and the next probe sees that through `is_connected()` and reuses the session rather than disconnecting one it does not own. |
| native/nightshade_native/bridge/src/device_capabilities/ascom.rs:728:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: Probe-and-restore teardown for the cold ASCOM probe. It runs only when `capability_probe_should_own_connection` said this probe opened the device, and only after the capability and live-state reads are done, so its failure cannot change the returned capabilities. A refused disconnect leaves the driver connected and the next probe sees that through `is_connected()` and reuses the session rather than disconnecting one it does not own. |
| native/nightshade_native/bridge/src/device_capabilities/ascom.rs:571:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: Fail-closed safety default, the ASCOM twin of the Alpaca site: an unreadable ISafeMonitor.IsSafe reports 'not safe', and every consumer of `SafetyMonitorCapabilities.is_safe` treats false as a refusal to image. A comms failure can only stop a run, never authorise one. |
| native/nightshade_native/bridge/src/device_capabilities/ascom.rs:636:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: ISwitchV2.MaxSwitch unreadable enumerates 0 channels; the loop below produces an empty `switches` vec and the device shows no controllable outputs. The switch write path commands an index taken from this enumerated list, so an empty list means nothing to command rather than a write against an unknown channel. |
| native/nightshade_native/bridge/src/device_capabilities/mod.rs:29:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: Module-doc line quoting the `unwrap_or(0)` convention it documents for the per-driver probes. It is comment text, not a code path — the sites it describes are adjudicated in alpaca.rs / ascom.rs / indi.rs / native.rs. |
| native/nightshade_native/bridge/src/device_manager/ops/camera/mod.rs:16:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: Module-doc line quoting the `unwrap_or(0)` policy for this module's status composition; comment text, not a code path. The sites are adjudicated on `device_manager/ops/camera/status.rs`. |
| native/nightshade_native/bridge/src/device_manager/ops/camera/mod.rs:47:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: Module-doc line quoting the `target_temp.unwrap_or(-10.0)` this module deliberately does NOT do (the cooler setpoint stays `Option` all the way to the driver). Comment text describing the absence of a default. |
| native/nightshade_native/bridge/src/device_manager/ops/camera/mod.rs:459:unwrap_or_default | accepted_modeled_approximation | native-bridge-adjudication | reason: `#[cfg(test)]` sim-sky test: snapshots the process-wide plate-solver preference before pointing it at the test catalogue. Absent on a fresh store means the default, which is exactly what has to be restored afterwards; the write that follows is `.expect("save plate-solver preference")`. |
| native/nightshade_native/bridge/src/device_manager/ops/camera/mod.rs:508:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: `#[cfg(test)]` restore of the preference snapshotted above, run before the assertions specifically so a failing assertion cannot leave a catalogue sky armed for the other simulator tests. Its result is not the claim under test and a failed restore cannot make the following assertions pass. |
| native/nightshade_native/bridge/src/device_manager/ops/camera/mod.rs:568:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: `#[cfg(test)]` ASTAP test pointing the plate-solver storage at a fresh `TempDir`. The init is idempotent and a failure cannot hide: the very next statement is `save_platesolver_preference(...).expect("save plate-solver preference")` against that same store. |
| native/nightshade_native/bridge/src/device_manager/ops/camera/mod.rs:569:unwrap_or_default | accepted_modeled_approximation | native-bridge-adjudication | reason: `#[cfg(test)]` snapshot of the plate-solver preference on a store just created empty, where the default IS the correct baseline to merge the ASTAP path into and to restore afterwards. |
| native/nightshade_native/bridge/src/device_manager/ops/camera/mod.rs:619:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: `#[cfg(test)]` restore of the preference snapshotted at line 569, run after the ASTAP solve and before the assertions so a red test cannot strand the test catalogue in the shared store. |
| native/nightshade_native/bridge/src/device_manager/ops/camera/download.rs:96:rust_drop_result | accepted_modeled_approximation | native-bridge-adjudication | reason: ASCOM ICameraV3.CCDTemperature is optional — an uncooled camera raises PropertyNotImplemented. `.ok()` keeps it `Option<f64>` all the way into `ImageMetadata.temperature`, so an unreadable sensor temperature stays absent and the FITS writer omits CCD-TEMP instead of stamping a fabricated one. |
| native/nightshade_native/bridge/src/device_manager/ops/camera/download.rs:261:rust_drop_result | accepted_modeled_approximation | native-bridge-adjudication | reason: The INDI twin of the ASCOM site above: CCD_TEMPERATURE is optional, `.ok()` keeps it `Option<f64>` into `ImageMetadata.temperature`, and an absent temperature is written as absent rather than as a number. |
| native/nightshade_native/bridge/src/device_manager/ops/mount/mod.rs:18:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: Module-doc line quoting the `can_set_tracking().unwrap_or(false)` convention for this module's optional ASCOM probes; comment text, not a code path. |
| native/nightshade_native/bridge/src/device_manager/ops/mount/status.rs:618:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: `tracking_rates()` is the Alpaca TelescopeRates probe; unreadable means the driver advertises no selectable drive rates, so `can_set_tracking_rate` is false and the UI hides the rate dropdown. It gates a control, never a command: `mount_set_tracking_rate` propagates the driver's own error if a rate is set anyway. |
| native/nightshade_native/bridge/src/device_id/mod.rs:384:rust_drop_result | accepted_modeled_approximation | native-bridge-adjudication | reason: `device_index` is `Option<i32>` by design: a Touptek-style id whose third segment is not numeric is still a valid id, and the raw `device_id` string is carried beside the index and is what the SDK actually opens. None means 'this id is not index-addressable', which every consumer matches on — it cannot select a different device. |
| native/nightshade_native/bridge/src/device_id/mod.rs:421:rust_drop_result | accepted_modeled_approximation | native-bridge-adjudication | reason: Same `Option<i32>` contract on the 4-part subtype form (`native:{vendor}:{subtype}:{id}`): a non-numeric payload keeps `device_index: None` while the payload itself is preserved verbatim in `device_id` for the SDK to open. |
| native/nightshade_native/bridge/src/device_id/mod.rs:443:rust_drop_result | accepted_modeled_approximation | native-bridge-adjudication | reason: Same `Option<i32>` contract on the catch-all native form, which deliberately covers ids that are NOT indices — serial-port mounts (`native:skywatcher:COM3:9600`) and gPhoto2 USB paths. None is the honest answer for those and the joined string is what the driver receives. |
| native/nightshade_native/bridge/src/device_id/mod.rs:494:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: Simulator instance suffix only. This arm is reached exclusively by the `simulator:` scheme, whose ids the app generates itself; a malformed or absent suffix means instance 0, the pre-multi-instance default the comment above documents. Real driver ids are parsed by the stricter arms above, which return `invalid_device_id`. |
| native/nightshade_native/bridge/src/api/plate_solve.rs:180:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: `broadcast::Sender::send` returns Err only when no receiver is live. The receivers here are followers that subscribed inside `coalesced_solve`'s registration arm; a follower whose caller went away needs nothing, and a caller arriving after this point finds the entry already removed by `InFlightGuard` and starts its own solve. The solve result itself is returned on the next line, so no caller loses it. |
| native/nightshade_native/bridge/src/api/plate_solve.rs:1046:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: Coalescing test: the solve result is deliberately unused because the claim is `assert_eq!(runs.load(...), 2)` — that two DIFFERENT frames each got their own solve. The closure increments the counter the assertion reads. |
| native/nightshade_native/bridge/src/api/plate_solve.rs:1053:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: Second half of the same two-frames-two-solves test: the discarded result is not the claim; the `AtomicUsize` run counter asserted afterwards is. |
| native/nightshade_native/bridge/src/api/plate_solve.rs:1076:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: Release test: the result is discarded because the claim is that a FINISHED solve releases its frame — proved by the run counter reaching 2 for two sequential solves of the same path. |
| native/nightshade_native/bridge/src/api/plate_solve.rs:1080:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: Second solve of the release test above; the run counter, not the returned solution, is what the test asserts on. |
| native/nightshade_native/bridge/src/api/polar_alignment/run_loop.rs:66:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: API-level default for an OPTIONAL run parameter of the polar-alignment start call: Dart may omit `solveTimeout`, and 60 s is the documented default the settings UI also shows. The comment directly above marks the boundary this respects — only non-hardware options default here, while gain/offset stay `Option` so None means 'use the camera's current value' and is never forced to 0. |
| native/nightshade_native/bridge/src/api/polar_alignment/run_loop.rs:67:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: API-level default for the optional `startFromCurrent` flag: true keeps the historical behaviour of measuring from where the mount already points, and the alternative (slew to the pole region) is an explicit opt-in the caller has to ask for. |
| native/nightshade_native/bridge/src/api/polar_alignment/run_loop.rs:68:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: API-level default for the optional auto-complete threshold: 1 arcminute, the value the polar-alignment UI shows as its default. It only decides when the adjustment loop declares success and is reported in the status the operator watches. |
| native/nightshade_native/bridge/src/api/post_session/helpers.rs:118:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: Roundness factor in the reference-frame score. `q.eccentricity` is None when the quality pass could not fit ellipticity; 1.0 = 'perfectly round' leaves the candidate ranked on SNR and sharpness instead of zeroing an otherwise good frame. Ranking only — the chosen reference must still register (the loop takes the first REGISTRABLE candidate) and `reference_choice_tests` pins the ordering, including that a NaN score ranks last. |
| native/nightshade_native/bridge/src/api/post_session/helpers.rs:186:unwrap_or_default | accepted_modeled_approximation | native-bridge-adjudication | reason: `ImageData::as_u16()` returns None only for a non-U16 image, and this arm is inside `match image.pixel_type { PixelType::U16 => ... }`, so the empty vec is unreachable. Every caller derives its work from `locations * channels` computed from the same image, and a short buffer is rejected by the geometry guard (`frame_buffer_for_master`) rather than integrated. |
| native/nightshade_native/bridge/src/api/post_session/helpers.rs:190:unwrap_or_default | accepted_modeled_approximation | native-bridge-adjudication | reason: Same match-arm guarantee for the F32 branch: `as_f32()` can only be None for a non-F32 image, which this arm has already excluded. |
| native/nightshade_native/bridge/src/api/post_session/helpers.rs:587:comment_best_effort | accepted_modeled_approximation | native-bridge-adjudication | reason: Doc line stating `reference_wcs_from_fits`' contract — a reference that is not FITS, is unreadable, or carries no astrometry returns None and the master is written WITHOUT a WCS, which the mosaic stitcher then gates out of the stitch. Comment text; the code below returns None via `?` at every one of those points rather than inventing a projection. |
| native/nightshade_native/bridge/src/api/post_session/helpers.rs:608:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: NAXIS1 is mandatory in a FITS image HDU and `read_fits` above has already decoded the data array using it, so a header reaching this line always carries it. The value is used only for the geometric-centre CRPIX fallback, and the WCS is abandoned entirely (`?` on CRVAL1/CRVAL2) if the astrometry itself is missing. |
| native/nightshade_native/bridge/src/api/post_session/helpers.rs:609:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: NAXIS2, same mandatory-keyword argument as NAXIS1 on the line above, used only for the CRPIX2 centre fallback. |
| native/nightshade_native/bridge/src/api/post_session/helpers.rs:625:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: CD1_2 absent means no cross-term, which is the FITS convention for an un-rotated, un-skewed CD matrix — the same reading `mosaic::wcs_from_header` applies. The diagonal terms that carry the plate scale are NOT defaulted: the enclosing `if let (Some(cd1_1), Some(cd2_2))` is what admits this branch at all. |
| native/nightshade_native/bridge/src/api/post_session/helpers.rs:626:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: CD2_1, the other off-diagonal term, with the same 'no cross-term' convention and the same guard requiring the diagonal terms to be present. |
| native/nightshade_native/bridge/src/api/post_session/helpers.rs:634:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: CROTA2 absent means zero rotation in the older CDELT/CROTA representation — the convention a FITS reader must assume, and the one the stitcher's own `wcs_from_header` uses. The CDELT scale terms this rotation is applied to are required, not defaulted. |
| native/nightshade_native/bridge/src/api/post_session/helpers.rs:694:unwrap_or_default | accepted_modeled_approximation | native-bridge-adjudication | reason: `to_u16_for_preview` reaches this only for a non-U16 master, and the very next line returns an empty U16 image when the buffer is empty. An empty preview buffer is then rejected by `write_preview_png` with 'failed to build preview buffer', so an unexpected pixel type fails the preview instead of producing a fabricated one. |
| native/nightshade_native/bridge/src/api/post_session/integrate.rs:291:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: `ref_reg_pos` is the reference frame's own registration offset, which is the identity by construction (the reference is what every other frame is registered ONTO). The binding is kept so the reader can see the identity is deliberate rather than a missing term; there is no result being discarded. |
| native/nightshade_native/bridge/src/api/post_session/integrate.rs:424:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: `weight_by_index` is built from the accepted-frame list this loop walks, so a frame absent from the map was REJECTED by the integrator — weight 0.0 is its measured contribution, not a stand-in for an unknown one, and the per-frame stats report the rejection alongside it. |
| native/nightshade_native/bridge/src/api/sequencer/lifecycle.rs:493:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: Test that a run START seeds the observing site from app settings. The start's result is deliberately ignored and the comment above says why: no sequence is loaded so the start itself fails, and the point is that the seeding must NOT be conditional on the start succeeding — the assertion afterwards reads `executor.latitude/longitude`. |
| native/nightshade_native/bridge/src/api/sequencer/mosaic.rs:165:unwrap_or_default | accepted_modeled_approximation | native-bridge-adjudication | reason: `BroadcastSession.config.auth_token` is `Option<String>` where None means the broadcast was armed without authentication; the snapshot's empty string is that same 'no token' state for the Dart UI, which shows the token field empty. It is a snapshot of an existing session, so an empty string here cannot weaken a session that was configured with a token. |
| native/nightshade_native/bridge/src/api/sequencer/mosaic.rs:166:unwrap_or_default | accepted_modeled_approximation | native-bridge-adjudication | reason: `watermark_text` is `Option<String>` where None means no watermark was configured; the empty template is that same state, and the watermark renderer skips an empty template rather than drawing an empty label. |
| native/nightshade_native/bridge/src/api/sequencer/runtime_config.rs:17:simulated_runtime_path | accepted_modeled_approximation | native-bridge-adjudication | reason: Comment on the simulation branch of `api_sequencer_set_simulation_mode`. The branch is unreachable in a release artifact: the guard immediately above returns `NotSupported` unless `cfg!(debug_assertions)`, and `sim_capture`'s guard test asserts that refusal still precedes the NullDeviceOps install. |
| native/nightshade_native/bridge/src/api/sequencer/runtime_config.rs:18:simulated_runtime_path | accepted_modeled_approximation | native-bridge-adjudication | reason: The NullDeviceOps install itself. The call IS reachable from the network — `POST /api/sequencer/simulation` is a registered headless route (`sequencer_routes.dart:70`) whose handler forwards the caller's `enabled` flag straight to `api_sequencer_set_simulation_mode` — so the whole gate is the `if enabled && !cfg!(debug_assertions)` refusal at runtime_config.rs:9, which returns a typed `NotSupported` before control reaches this line. Every shipped bundle carries a release-compiled bridge, so `cfg!(debug_assertions)` is false there and the route answers 400 `simulation_mode_unavailable` (the handler at sequencer_config_handlers.dart:12 maps ONLY that typed variant and rethrows every other error). `sim_capture::release_gate_still_guards_null_device_ops` fails the build if the gate is ever removed or reordered after the install. |
| native/nightshade_native/bridge/src/api/sky_atlas/tiles.rs:86:comment_best_effort | accepted_modeled_approximation | native-bridge-adjudication | reason: Doc line for `iso_is_after`, and it states the conservative direction: a label that is not an ISO timestamp is treated as NOT after the anchor, so it CONTRIBUTES to the fold rather than being silently dropped. Comment text; the comparison itself is a total function over strings. |
| native/nightshade_native/bridge/src/builtin_guider/control.rs:11:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: `set_config` recomputes the displayed pixel scale, and `unbinned_pixel_scale` is None until `connect()` has resolved the guide camera's pitch and focal length. 0.0 is the 'not yet known' display value: the guiding loop recomputes the real scale per frame and overwrites `last_status.pixel_scale`, and every conversion to arcseconds goes through `guide_offset_arcsec`, which returns None for a non-positive scale and is turned into a hard error by its caller. |
| native/nightshade_native/bridge/src/builtin_guider/control.rs:214:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: `#[cfg(test)]` synthetic-loop starter. It mirrors the real start path, whose first act under the op-lock is `stop_locked()`; there is no loop to stop on the first call and the test's claim is the live-loop counter it asserts on afterwards, not this teardown. |
| native/nightshade_native/bridge/src/builtin_guider/control.rs:287:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: Joining the guide-loop task after its stop flag has been set. `JoinError` means the task panicked or was aborted — either way the loop is no longer running, which is exactly what `stop_locked` promises its caller, and a panic is already surfaced by the runtime's panic hook plus the `tracing::error!` the loop wrapper logs before it unwinds. |
| native/nightshade_native/bridge/src/builtin_guider/dither.rs:45:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: `recent_rms` is None until the session has enough RMS samples to average. 0.0 makes `adaptive` clamp to 1.0, i.e. the dither uses exactly the configured amplitude with no seeing-based enlargement — the unscaled behaviour, and the radius stays bounded by `amount` either way. |
| native/nightshade_native/bridge/src/builtin_guider/loop_runner.rs:142:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: Abort issued when a guide exposure blows its completion deadline. The outcome is NOT hidden: the very next statement returns `NightshadeError::OperationFailed("Guide exposure on … did not complete within …")`, which stops the loop and reports the guider errored. The abort only tries to leave the sensor idle, and a camera that also refuses the abort fails the next exposure attempt with its own driver error. |
| native/nightshade_native/bridge/src/builtin_guider/loop_runner.rs:176:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: Guide-frame counter for the FIRST frame of a session: `last_frame` is None until one has been captured, so the count starts at 1. It is a display/telemetry sequence number — nothing keys on it — and it is derived from the previous frame for every subsequent frame. |
| native/nightshade_native/bridge/src/builtin_guider/loop_runner.rs:233:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: `publish_star_measurement` reports SNR 0.0 when no star is currently tracked (between frames, or before lock acquisition). The UI's guiding badge keys on the absence of a selected star, not on this number, and the loop cannot act on a missing star either — `choose_lock_star` returning None is what produces a real `NoStarsDetected` error. |
| native/nightshade_native/bridge/src/builtin_guider/loop_runner.rs:234:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: Star mass on the same no-star-tracked path as the SNR above, with the same 'nothing is selected' display convention. |
| native/nightshade_native/bridge/src/builtin_guider/metrics.rs:304:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: `last_dec_direction` is None only before the FIRST Dec pulse of a session, and true ('reversing') makes that first pulse pay the backlash compensation once — the correct assumption, since the mount's Dec gear may sit anywhere in its backlash band at session start. Subsequent pulses compare against the recorded direction. |
| native/nightshade_native/bridge/src/builtin_guider/mod.rs:28:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: Module-doc line stating the SNR/star-mass display convention for frames with no tracked star; comment text. The sites are adjudicated on `builtin_guider/loop_runner.rs`. |
| native/nightshade_native/bridge/src/builtin_guider/mod.rs:33:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: Module-doc line stating the frame-counter/probe-absent convention; comment text, not a code path. |
| native/nightshade_native/bridge/src/device_manager/identity.rs:166:comment_best_effort | accepted_modeled_approximation | native-bridge-adjudication | reason: Doc line for `DeviceIdentity::apply_to`, and it states the guard the code enforces: only NON-EMPTY observations overwrite the id-derived placeholder name, so a transport that answers nothing leaves the placeholder standing rather than blanking the device in the UI. Comment text. |
| native/nightshade_native/bridge/src/event/sequencer.rs:188:nonconvergence_best_guess | accepted_modeled_approximation | native-bridge-adjudication | reason: Doc line for the `likely_cause_label: Option<String>` field, and None IS the modelled outcome: when the frame-failure classifier was not consulted or could not pick a single cause, the event carries no label and Dart's `LikelyCauseExt.fromLabel` shows no cause rather than a guessed one. Comment text on an Option-typed field. |
| native/nightshade_native/bridge/src/sim_sky.rs:307:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: Star-database area-file size vote. `metadata()` fails for an entry that vanished between the directory read and the stat (or is unreadable); 0 bytes simply gives that file no vote in `max_by`, and the whole function returns `Option<String>` so a directory where nothing can be measured yields None instead of a fabricated catalogue prefix. |
| native/nightshade_native/bridge/src/sim_sky.rs:403:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: `#[cfg(test)]` synthetic ASTAP area builder: a magnitude run with no stars has no declination to take the high byte from, so 0 is written into a header record no star will ever be read behind. |
| native/nightshade_native/bridge/src/sim_sky.rs:644:unwrap_or_default | accepted_modeled_approximation | native-bridge-adjudication | reason: `#[cfg(test)]` ASTAP-directory helper, used only when `NIGHTSHADE_SIM_SKY_ASTAP_DIR` is unset. An empty HOME yields a relative path that cannot contain `astap_cli`, and the test asserts `binary.exists()` with a message naming the directory and the override variable, so the misconfiguration fails loudly rather than silently solving against the wrong catalogue. |
| native/nightshade_native/bridge/src/unified_device_ops/device_ops.rs:338:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: Comment recording the `unwrap_or(0)` this call deliberately does NOT do — gain and offset stay `Option` so None reaches each driver branch as 'do not change the camera's current value'. Comment text describing the absence of a default. |
| native/nightshade_native/bridge/src/unified_device_ops/device_ops.rs:383:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: Abort issued when the exposure-completion wait fails or times out, on the path that immediately `return Err(e)` with that wait's own error and publishes `ExposureComplete { success: false }`. The failure is therefore never hidden; the abort exists only to stop the retry racing a still-running exposure, and a camera that refuses it fails the next start with the driver's own 'exposure already in progress'. |
| native/nightshade_native/bridge/src/unified_device_ops/device_ops.rs:845:unwrap_or_literal | accepted_modeled_approximation | native-bridge-adjudication | reason: `from_position` is informational payload for the `FilterChanging` event; -1 is the documented 'position unknown' sentinel the UI already renders for a wheel mid-move or freshly reconnected. The move itself is on the next statement and propagates its own error via `map_err`. |
| native/nightshade_native/bridge/src/unified_device_ops/device_ops.rs:1155:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: Removal of the temporary FITS written for a plate solve. The solver has already read the file by this point and `result` is returned on the next line, so a failed unlink leaves one file in the temp directory and cannot change the solve outcome. |
| native/nightshade_native/bridge/src/unified_device_ops/mod.rs:189:comment_best_effort | accepted_modeled_approximation | native-bridge-adjudication | reason: Doc line for `read_mount_pointing` stating the omission rule it implements: with no connected mount, or a driver that will not answer, the RA/DEC keywords are LEFT OUT of the header rather than filled with a plausible-looking value. Comment text describing a refusal to invent. |
| native/nightshade_native/bridge/src/api/darkroom/tests.rs:661:rust_let_underscore | accepted_modeled_approximation | native-bridge-adjudication | reason: Test teardown in the Darkroom render tests — `api_darkroom_cancel(clear)` releases the render id after the assertions have run; its result is not what the test measures, and a failed clear surfaces as a leaked render in the next test's isolation check rather than being hidden. |
| packages/nightshade_core/lib/src/services/notification_service.dart:97:literal_null_coalesce | accepted_modeled_approximation | core-hardening | reason: `routed` is null when no channel routing was attempted for this notification, and `anyDelivered` asks whether anything was actually dispatched — 'nothing attempted' is exactly 'nothing dispatched', so `?? false` states the true answer rather than papering over an error (webhookAccepted is OR'd separately). |
| native/nightshade_native/sequencer/src/executor/start/preflight.rs:160:unwrap_or_literal | accepted_modeled_approximation | core-hardening | reason: `.unwrap_or(false)` on the meridian-flip trigger lookup. `None` means there is no enabled MeridianFlip trigger, so no post-flip re-centre can ever be attempted and the solver advisory correctly stays silent; a `Some` is `config.auto_center` read directly. The hard failure for a CenterTarget node in the tree is evaluated separately below and does not pass through this default. |
| native/nightshade_native/sequencer/src/executor/start/preflight.rs:168:rust_let_underscore | accepted_modeled_approximation | core-hardening | reason: `self.event_tx.send(ExecutorEvent::Error { message })` warning that the meridian-flip trigger is set to re-centre while no plate solver is installed, on the executor broadcast channel; Err only with zero subscribers. The advisory changes nothing about the run (it starts either way, as the surrounding comment explains — a flip may never fire), and if the flip does fire the centering step still fails loudly with its own typed result. Same shape as the ASTAP-database advisory at :195 and the clamp warnings at run_setup.rs:139. |
| packages/nightshade_app/lib/screens/dashboard/widgets/cockpit_now_imaging.dart:76:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `settingsAsync.valueOrNull?.hasObserverLocation ?? false` — settings not yet loaded read as "no site", so the altitude chip prints "set location" rather than a number; the default cannot invent a site, only withhold one until `appSettingsProvider` resolves and the widget rebuilds on the canonical predicate. A settings load failure is `appSettingsProvider`'s own AsyncError, not this read's. |
| packages/nightshade_app/lib/widgets/weather/dashboard_weather_widget.dart:31:literal_null_coalesce | accepted_modeled_approximation | app-adjudication | reason: `appSettings?.hasObserverLocation ?? false` — an unloaded settings state shows the "location not configured" empty state, never weather for a site that is not there; the widget rebuilds on the real predicate once settings resolve. Same seam as cockpit_now_imaging.dart:76. |
| packages/nightshade_core/lib/src/utils/aligned_ticker.dart:13:guessed_now_timestamp | accepted_modeled_approximation | core-hardening | reason: `now ?? DateTime.now()` in `delayToNextBoundary`: the wall clock IS the quantity being aligned to (the next epoch-second boundary), so reading it is the computation, not a guess. The optional `now` exists only so a test can drive the alignment off a virtual clock (`fakeAsync` moves timers but not `DateTime.now`). No value is invented; a wrong system clock shifts the phase that every clock shares by the same amount. |
| packages/nightshade_planetarium/lib/src/util/aligned_ticker.dart:13:guessed_now_timestamp | accepted_modeled_approximation | core-hardening | reason: Deliberate duplicate of `nightshade_core`'s `aligned_ticker.dart` (planetarium is a leaf package that core depends on, so it cannot import core); identical code and identical reasoning to the row above. |
