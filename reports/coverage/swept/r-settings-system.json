{
 "area": "settings (remote-access, updates, about, help, replay-debug, backup, equipment-profiles, catalogs, rig-catalogs)",
 "units_visited": [
  "screen:settings/about_settings.dart",
  "screen:settings/backup_screen.dart",
  "screen:settings/deep_star_catalog_card.dart",
  "screen:settings/element_refresh_card.dart",
  "screen:settings/help_tutorials_settings.dart",
  "screen:settings/log_viewer.dart",
  "screen:settings/pairing_screen.dart",
  "screen:settings/profile_controls.dart",
  "screen:settings/profile_details_helpers.dart",
  "screen:settings/profile_details_rendering.dart",
  "screen:settings/profile_list.dart",
  "screen:settings/remote_access_settings.dart",
  "screen:settings/replay_debug_settings.dart",
  "settings:about",
  "settings:backup",
  "settings:catalogs",
  "settings:equipment-profiles",
  "settings:help",
  "settings:remote-access",
  "settings:replay-debug",
  "settings:rig-catalogs",
  "settings:updates"
 ],
 "units_unreached": [
  {
   "unit": "settings:rig-catalogs (connected state)",
   "why": "the page requires an active NetworkBackend session to a remote appliance; with no appliance paired it renders only its explanatory note, so the download/verify controls could not be exercised."
  },
  {
   "unit": "settings:updates (connected state)",
   "why": "same -- Appliance Updates drives /api/system/update/* on a remote rig and degrades to a note off-network. The local-desktop half is covered as a finding."
  },
  {
   "unit": "settings:backup (WebDAV/S3 push and remote restore)",
   "why": "no WebDAV or S3 endpoint is available to this sandbox; I exercised the three buttons only far enough to confirm they fail closed with 'Server URL is required.'"
  }
 ],
 "verified_correct": [
  "Help & Tutorials > Reset All Progress: shows a real confirmation Alert ('Reset Tutorial Progress? ... cannot be undone'), and pressing Reset actually empties tutorial_progress in the DB (was 6 rows incl. a completed one -> 0 rows). Attacked it by first driving a tutorial to completed=1 so a no-op reset would have been visible.",
  "Help & Tutorials > Enable tutorials switch persists: flipping it wrote app_settings tutorials_enabled='false' (the key did not exist before, so this is a real write, not a pre-existing value).",
  "Tutorial 'Done' on the last step really completes: tutorial_progress row went completed=0/completed_at=NULL -> completed=1/completed_at=1785609125 and the row label changed to 'Completed'. 'Skip tour' really dismisses: dismissed 0 -> 1.",
  "About > Version string matches the built artifact: UI says 6.1.0, apps/desktop/pubspec.yaml says 6.1.0+25. Platform/OS Version/Dart Version are read live from dart:io and matched the host (Linux 7.1.5-1-cachyos, Dart 3.12.1) rather than being hardcoded -- I checked them against uname on the host.",
  "Remote Access > Server Port really rebinds: changing 8080 -> 8099 released 0.0.0.0:8080 and bound 0.0.0.0:8099 on the same pid (ss -ltnp), wrote app_settings web_server_port=8099, and both displayed URLs updated to :8099 in the same frame. I checked the old port was actually gone, not just that the new one appeared.",
  "Remote Access > Enable Remote Access really starts the server: before the toggle nothing was listening on 8080; after it, pid 1701064 held 0.0.0.0:8080 and GET / and GET /dashboard returned 200. app_settings web_server_enabled='true'.",
  "The LAN link is computed from the right interface: it showed http://192.168.1.20:8080/dashboard while this host has 192.168.1.20 (enp5s0) and 172.17.0.1 (docker0) -- it did not pick the docker bridge, which is the usual failure.",
  "Pairing codes are genuinely single-use: re-POSTing the same code to /api/pairing/verify after a successful pair returned {'error':'invalid_pairing_code'} and did not create a 14th device.",
  "The API fails closed for unauthenticated callers on every route I tried (/api/status, /api/images/recent, /api/auth/csrf, /api/auth/cookie, /api/pairing/status): all 401, from loopback and from the LAN address alike. I specifically tried to get a session by loading /dashboard first and reusing its cookie jar -- no cookie is issued.",
  "Equipment Profiles create/edit/save round-trips to the DB exactly: I created 'Audit Rig Alpha', set focal 1625 / aperture 203 / gain 100 / offset 50 / cooling -10 / binning 2x1 / filters Lum,Ha,OIII,SII / Ha focus offset 250, and equipment_profiles held focal_length=1625.0, aperture=203.0, focal_ratio=8.00492610837438, default_gain=100, default_offset=50, default_bin_x=2, default_bin_y=1, default_cooling_temp=-10.0, filter_names=[\"Lum\",\"Ha\",\"OIII\",\"SII\"], filter_focus_offsets={\"Ha\":250}. I attacked it by pressing Create with an empty name (rejected inline with 'Enter a profile name.', no row written) and by entering a negative cooling setpoint (accepted, stored as -10.0).",
  "Equipment Profile Export writes a complete, faithful JSON (all 40 fields incl. filterFocusOffsets) and Import round-trips it: importing the file I had just exported created a second row named 'Audit Rig Alpha (1)' -- the name collision is resolved rather than silently overwriting the original. I tried to break the importer with a non-JSON file and with a valid-JSON-wrong-shape file; both were rejected and neither wrote a row (count stayed at 2).",
  "Equipment Profile Delete is real and confirmed: the dialog names the profile ('Delete profile \"Audit Rig Alpha (1)\"? This action cannot be undone.') and pressing Delete removes the row from equipment_profiles (2 rows -> 1). Cancel-by-Escape left the row intact. Deleting a non-selected-but-not-last profile correctly re-selected the survivor.",
  "Backup & Restore > Create Backup really writes a full bundle: it produced nightshade_backup_2026-08-02T10-43-32.295333.nsbackup (7198 bytes on disk, listed as '7.0 KB | Aug 2, 2026 10:43' -- the size and timestamp the UI shows are the file's real ones), the JSON contains 28 top-level sections and metadata profilesCount:1, and equipmentProfiles[0] is exactly the profile that existed at that moment. Restore really restores: I deleted every equipment_profiles row and pressed restore on that bundle, and the profile came back with the same id and name.",
  "Backup bundles are honest about their own contents: metadata counts matched the DB in every bundle I opened (the 10:30 autosave taken right after I emptied the profile table records profilesCount 0).",
  "Catalogs > the installed-catalog cards do not lie about what is on disk: HYG says '119.6k objects / 32.4 MB / v4.2 / installed 2026-06-11' and hyg_v42.csv is 119,627 lines (119,626 records) and 33,932,800 bytes (32.36 MiB) with stars_metadata.json recording objectCount 119626 and the same date; OpenNGC says '14.0k / 3.7 MB / 2023.12' and NGC.csv is 13,970 lines (13,969 records) and 3,876,288 bytes (3.70 MiB). The MPC card's 'Oldest source updated 2026-07-30 - 312 asteroids, 761 comets' matches element_refresh_metadata.json exactly. I tried to catch it reporting a stale cached number by comparing against the raw files rather than the metadata, and the files agree.",
  "Backup & Sync's three buttons all fail closed and say why when nothing is configured: Test Connection, Push Now and Browse Remote Backups each report 'Server URL is required.' (Push Now takes ~3 s to do so, long enough that I first mis-recorded it as silent -- it is not).",
  "Replay & Debug > Replay decision logging toggle persists for real: flipping it off wrote app_settings replay_debug.enabled='false' (the key did not exist in the fresh DB beforehand, so this is a genuine write).",
  "Replay & Debug > Retention (days) validates and clamps honestly and shows the clamped value: I entered 5000 -> field displayed '3650 d' and app_settings replay_debug.retention_days='3650' (the documented max); 0 -> clamped to 1 (the documented min); 'abc' -> left at 1, no crash and no bogus write.",
  "Replay & Debug > Clear all replay history tells the truth about what it did: with 3 rows seeded into sequence_decisions it reported 'Cleared 3 replay decision row(s).' and the table went to 0. I specifically seeded rows across two different sequence_run_ids to check it was not counting runs.",
  "Settings > Logs filters are honest: the counter is always 'N of TOTAL', searching 'TransientAlertService' gave '10 of 136', Error+ narrowed to '1 of 139' and Warn+ to '3 of 139', and combining a search with Error+ correctly gave '0 of 136 / No entries match current filters' rather than silently dropping one of the two filters.",
  "Equipment Profiles > Set Active is exclusive and reaches the rest of the app: selecting the second profile and pressing Set Active moved is_active from row 3 to row 4 with exactly one row set (3|0, 4|1, 5|0) and the shell's status-bar profile chip changed to the new name in the same frame. I checked all three rows rather than only the one I clicked, to catch a second row being left active.",
  "Equipment Profiles > Duplicate copies the payload, not just the name: the duplicate carried the source profile's optics/camera defaults and was created with is_active=0 and is_default=0 rather than stealing active status from the original."
 ],
 "coverage_note": "Resumed an interrupted run: the 9 findings and 9 verified_correct entries about help / about / remote-access / pairing came from the first attempt and are kept verbatim. This session covered the rest of the scope live on display :82 against a fresh scratch profile: equipment-profiles (create, edit, filters+focus offsets, export, import, duplicate, set-active, delete -- every claim checked against equipment_profiles in the scratch nightshade.db), backup (create, restore, restore-with-id-collision, the three Backup&Sync buttons, the bundle format and the backup directory), catalogs (installed-catalog claims checked byte-for-byte against the files on disk, GLADE+ download driven to failure and the failing TAP URL reproduced with curl), rig-catalogs, updates and replay-debug (logging toggle, retention validation, clear-history count, plus the Logs viewer's filters, search and export). I stand by every finding already in the file. Two notes on scope: (1) the single biggest thing this session found is systemic rather than per-section -- every Settings snackbar is painted underneath the persistent bottom status bar, so all of the success and failure messages the other findings quote were only ever visible to me through the accessibility tree, never on screen; (2) three separate subsystems (pairing.db, backups, catalogs and the rotated log files) resolve their storage through getApplicationDocumentsDirectory()/the app-support dir and ignore NIGHTSHADE_DATA_DIR, which is why a brand-new scratch profile arrived with 12 paired devices, 8 backups and two installed catalogs. Not reached: the Backup&Sync WebDAV/S3 push and 'Browse Remote Backups' beyond their unconfigured error paths (no server available), Appliance Catalogs and Appliance Updates beyond their off-network state (no appliance), and the effect of the replay-logging toggle on an actual sequencer run.",
 "findings": [
  {
   "title": "Tutorial Tours run as a modal on whatever screen you are on and tell you to click tabs that are not there -- and the modal blocks every one of them",
   "kind": "defect",
   "severity": "P2",
   "unit": "settings:help / screen:settings/help_tutorials_settings.dart",
   "what_happened": "Settings > Help & Tutorials > Tutorial Tours > Equipment Setup > Start opens a centred modal dialog with a full-screen scrim, ON THE SETTINGS SCREEN. Step 1 reads 'Equipment Profiles -- Click the Profiles tab to manage your equipment configurations.' There is no Profiles tab on the Settings screen, and the scrim swallows clicks so the user cannot go find one: with the overlay up I clicked the left-nav 'Imaging' entry (root 240,350) and the app stayed on Settings with the overlay unchanged. All 4 steps behave the same way ('Click the Connections tab', 'Check the status indicators for each device') -- it is a slideshow of instructions about a screen you are not on and cannot navigate to.",
   "expected": "Starting a tutorial should route to the screen the tutorial is about (Equipment) and spotlight the real control, or at minimum let the underlying UI stay clickable so the instruction can be followed. NINA/SGP tours anchor to the widget being described.",
   "repro": "Fresh profile > skip onboarding > gear icon > Help & Tutorials > scroll to Tutorial Tours > Equipment Setup 'Start'. Observe the modal appears over Settings. Click left-nav Imaging: nothing happens.",
   "evidence": "/tmp/ns-audit/shots/rss-05.png (modal 'Equipment Profiles / Click the Profiles tab' rendered over the Settings > Help page), /tmp/ns-audit/shots/rss-06.png (step 3 'Click the Connections tab'). Tree after clicking left-nav Imaging still reports the Settings panel plus 'Tutorial step 1 of 4: Equipment Profiles'.",
   "where": "packages/nightshade_app/lib/screens/settings/widgets/help_tutorials_settings.dart:179 (onStart -> notifier.restartTutorial) and packages/nightshade_core/lib/src/providers/tutorial_provider.dart:640 _restartTutorial -- it only sets state.activeCategory/currentStepIndex, it never navigates.",
   "proposed_fix": "Give TutorialStep a target route and have _restartTutorial/resumeTutorial push it before showing step 0; make the overlay a non-blocking anchored coach-mark rather than a modal barrier.",
   "verdict": "CONFIRMED",
   "verdict_reasoning": "Reproduced live (display :83, profile v2-settings-system). Settings > Help & Tutorials > Tutorial Tours > Equipment Setup > Start renders a centred modal with a full-screen scrim ON the Settings screen (/tmp/ns-audit/shots/v2-05.png). Step 1 reads 'Equipment Profiles / Click the Profiles tab...' and there is no Profiles tab on Settings. With the overlay up I clicked the left-nav 'Imaging' entry; the tree afterwards still reported 'Tutorial step 1 of 4: Equipment Profiles [DISABLED]' over Settings > Help & Tutorials, so the scrim swallowed the click and no navigation happened. Cited locations check out: help_tutorials_settings.dart:179-180 onStart -> notifier.restartTutorial(equipmentSetup); tutorial_provider.dart:640 _restartTutorial only sets activeCategory/currentStepIndex and never routes. Extra observation: the row still reads 'Not started' while the tour is running.",
   "corrected_severity": "P2"
  },
  {
   "title": "About page's GitHub and Documentation links are placeholders that 404 / do not resolve",
   "kind": "defect",
   "severity": "P1",
   "unit": "settings:about / screen:settings/about_settings.dart",
   "what_happened": "Settings > About offers three link buttons: GitHub, Documentation, Discord. GitHub opens https://github.com/nightshade-astro, which returns HTTP 404 -- the real project repo is https://github.com/Scdouglas1999/Nightshade (HTTP 200, per the repo README badge). Documentation opens https://nightshade.astro/docs; 'nightshade.astro' does not resolve in DNS at all (.astro is not a delegated TLD). Discord opens https://discord.gg/nightshade, which is not a Nightshade invite. Clicking GitHub in the app produced no error banner -- the launch is reported as successful and the user is dropped on a 404.",
   "expected": "The three community links a shipped app puts on its About page must point at the real destinations: github.com/Scdouglas1999/Nightshade, the docs (docs/index.md is what the README points at), and a real Discord invite -- or the button should not exist.",
   "repro": "gear icon > Settings > About > click GitHub. Then: curl -o /dev/null -w '%{http_code}' https://github.com/nightshade-astro -> 404; getent hosts nightshade.astro -> no answer.",
   "evidence": "/tmp/ns-audit/shots/rss-15.png (the three buttons rendered). curl: github.com/nightshade-astro -> 404, github.com/Scdouglas1999/Nightshade -> 200, nightshade.astro -> DNS FAILS.",
   "where": "packages/nightshade_app/lib/screens/settings/widgets/about_settings.dart:102,109,116 (and the mobile duplicates at :102/:109/:116 vs desktop :128/:135/:142)",
   "proposed_fix": "Hoist the three URLs into one const block and set them to the real repo/docs/invite; add a launch-failure path that surfaces the URL (the existing showErrorSnackBar only fires when canLaunchUrl is false, which is not the failure mode here).",
   "verdict": "CONFIRMED",
   "verdict_reasoning": "Verified at source and over the network. about_settings.dart hard-codes the three URLs twice (mobile Wrap :102/:109/:116, desktop Row :128/:135/:142), exactly as cited. curl https://github.com/nightshade-astro -> 404; curl https://github.com/Scdouglas1999/Nightshade -> 200; getent hosts nightshade.astro -> no answer (.astro is not a delegated TLD, so Documentation can never resolve). The Discord claim is true but for a sharper reason than stated: discord.gg/nightshade 301s to discord.com/invite/nightshade and the Discord API returns a VALID invite to guild 1370106030754631690 'Nightshade Gaming Community' -- an unrelated gaming server, so the button works and drops the user in a stranger's Discord, which is worse than a dead link. _launchUrl only surfaces an error when canLaunchUrl/launchUrl returns false, which a 404 page never does, so 'no error banner' is correct by construction.",
   "corrected_severity": "P1"
  },
  {
   "title": "Settings search sends 'license' and 'credits' to About, which contains neither",
   "kind": "defect",
   "severity": "P3",
   "unit": "settings:about",
   "what_happened": "The About section declares keywords ['version','license','credits','updates','build'] (settings_catalog.dart:205-214). Typing 'license' into the Settings search box filters the section list down to exactly one entry, About -- and the About page has no licence text, no third-party/credits list, and no build number. The search promises information the destination does not have.",
   "expected": "Either About carries a licence + open-source-credits block (Flutter's showLicensePage is one line) and the build number, or those keywords come out of the catalog so the search does not lie.",
   "repro": "Settings > search box > type 'license'. Only 'About' is listed. Open it: version 6.1.0, tagline, 3 links, Platform/OS/Dart rows. Nothing else.",
   "evidence": "tree after typing 'license': the section list collapses to a single 'About' entry; the About page tree is Nightshade / Version 6.1.0 / Advanced astrophotography suite / GitHub / Documentation / Discord / System Information / Platform / OS Version / Dart Version.",
   "where": "packages/nightshade_app/lib/screens/settings/settings_catalog.dart:205-214 vs packages/nightshade_app/lib/screens/settings/widgets/about_settings.dart",
   "proposed_fix": "Add a licence/credits row (showLicensePage) and show the full build string; keep the keywords.",
   "verdict": "CONFIRMED",
   "verdict_reasoning": "Confirmed live and in source. Typing 'license' in the Settings search collapses the section list to exactly one entry, About (/tmp/ns-audit/shots/v2-06.png). Opening it gives: Nightshade / Version 6.1.0 / 'Advanced astrophotography suite' / GitHub / Documentation / Report an Issue / System Information (Platform linux, OS Version, Dart Version 3.12.1). No licence text, no credits, no build number. settings_catalog.dart:205-214 still declares keywords ['version','license','credits','updates','build']; about_settings.dart (read in full) has no showLicensePage/LicenseRegistry and prints only appVersion.version, never AppVersionInfo.buildNumber. Location as cited is accurate.",
   "corrected_severity": "P3"
  },
  {
   "title": "About shows 6.1.0 but drops the build number, and System Information omits everything a support ticket needs",
   "kind": "ux",
   "severity": "P3",
   "unit": "settings:about",
   "what_happened": "About reports 'Version 6.1.0'. The shipped desktop version is 6.1.0+25 (apps/desktop/pubspec.yaml:4) -- the build number, the one field that distinguishes two builds of the same release, is not shown. 'System Information' lists only Platform / OS Version / Dart Version. It does not list the Rust bridge version, the database schema version, the data directory, the active backend, or the release channel, all of which are the first things asked for on a bug report.",
   "expected": "NINA's About shows version+build+commit and a one-click 'copy system info'. Show '6.1.0 (build 25)', add schema version / data dir / bridge version, and give the card a Copy button.",
   "repro": "Settings > About.",
   "evidence": "/tmp/ns-audit/shots/rss-15.png; apps/desktop/pubspec.yaml:4 'version: 6.1.0+25'.",
   "where": "packages/nightshade_app/lib/screens/settings/widgets/about_settings.dart:73,159-166",
   "proposed_fix": "Render '${appVersion.version} (build ${appVersion.buildNumber})' and extend the System Information card; add a copy-to-clipboard action."
  },
  {
   "title": "Remote Access claims 'Localhost stays frictionless for this machine' -- localhost is not exempt from auth at all",
   "kind": "defect",
   "severity": "P2",
   "unit": "settings:remote-access / screen:settings/remote_access_settings.dart",
   "what_happened": "With Enable Remote Access on, the page's footer note reads 'Localhost stays frictionless for this machine, while non-local clients must pair before they can control the app', the 'Open on this computer' card says 'Use this link from the same machine to confirm the dashboard is working before sharing it elsewhere', and Connection details reports Authentication = 'Paired devices required remotely'. All three say local is special. It is not: from 127.0.0.1, GET /api/status -> 401 {'error':'Authentication required','message':'Missing Authorization header or session cookie'}; so do /api/images/recent, /api/auth/csrf and POST /api/auth/cookie. The bytes returned to 127.0.0.1 and to 192.168.1.20 are identical 401s. Only the static /dashboard shell is 200, and it can load no data. The auth middleware has no loopback branch at all.",
   "expected": "Either implement the documented loopback exemption (peer address is loopback -> full-scope identity), or change the copy so it does not promise a local escape hatch that does not exist. As written, an operator clicking 'Open Local Dashboard' to 'confirm the dashboard is working' sees a dead page and concludes the server is broken.",
   "repro": "Settings > Remote Access > Enable Remote Access. Then: curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:<port>/api/status -> 401. curl http://localhost:<port>/api/status -> 401. curl http://192.168.1.20:<port>/api/status -> 401.",
   "evidence": "Live 401 bodies captured from 127.0.0.1:8080 and 127.0.0.1:8099 (both ports I ran on). /tmp/ns-audit/shots/rss-20.png shows the 'Open on this computer' copy, /tmp/ns-audit/shots/rss-19.png shows Connection details.",
   "where": "copy: packages/nightshade_app/lib/localization/nightshade_localizations/translations.dart:389 and :399; enforcement: apps/desktop/lib/headless_api_server/http_middleware.dart:758-770 (token==null -> 401, no peer-address check anywhere in the chain).",
   "proposed_fix": "Add an explicit loopback grant in the auth middleware keyed on the shelf connectionInfo remote address (and NOT on any forwarded header), or drop the 'frictionless' sentence and relabel the card 'Open the dashboard (you will need to pair this browser)'.",
   "verdict": "CONFIRMED",
   "verdict_reasoning": "Reproduced live. Enabled remote access on port 8391 (ss shows 0.0.0.0:8391). From 127.0.0.1: /api/status -> 401 {'error':'Authentication required','message':'Missing Authorization header or session cookie'}; /api/images/recent -> 401; /api/auth/csrf -> 401; /api/system/info -> 401. Only GET /dashboard is 200 (static shell). All three copy strings are still live: the footer note, the 'Open on this computer / Use this link from the same machine to confirm the dashboard is working' card, and Connection details -> Authentication 'Paired devices required remotely' (/tmp/ns-audit/shots/v2-15.png). Locations check out: translations.dart:389 remoteAccessInfoBody and :398-399 remoteAccessLocalActionTitle/Body; http_middleware.dart has no peer-address branch anywhere in the auth chain (its only 'loopback' mentions concern the rate-limit trusted-proxy header and CORS) and the token==null -> 401 return at :759-771 fires regardless of peer.",
   "corrected_severity": "P2"
  },
  {
   "title": "Paired devices live in ~/Documents/Nightshade/pairing.db -- outside the app database, outside the configured data dir, and split across two stores",
   "kind": "defect",
   "severity": "P1",
   "unit": "settings:remote-access / screen:settings/pairing_screen.dart",
   "what_happened": "I launched a completely fresh scratch profile (NIGHTSHADE_DATABASE_DIR=NIGHTSHADE_DATA_DIR=/tmp/ns-audit/r-settings-system/data, --fresh, empty nightshade.db). Settings > Remote Access > Manage Pairing immediately listed 12 devices paired 6/15/2026 and 7/20/2026 -- the developer's real, months-old pairings. They are not in nightshade.db; PairingDatabase opens getApplicationDocumentsDirectory()/Nightshade/pairing.db, i.e. ~/Documents/Nightshade/pairing.db, which ignores NIGHTSHADE_DATA_DIR entirely. Two such stores exist on this machine: ~/Documents/Nightshade/pairing.db (12 active rows, mtime Aug 1) and ~/Nightshade/pairing.db (0 rows, mtime Jul 29) -- the same install has written to two different locations depending on how getApplicationDocumentsDirectory resolved.",
   "expected": "Long-lived remote credentials belong in the app database (or at minimum under the configured data dir) so that (a) Backup & Restore captures them, (b) moving the data directory moves them, (c) they cannot silently fork into two stores. As shipped, restoring a backup on a new machine silently loses every pairing, and a launch context where XDG_DOCUMENTS_DIR differs presents an empty device list with no error.",
   "repro": "drive_linux.py start --profile <new> --fresh (scratch data dir) > Settings > Remote Access > Manage Pairing. Observe 12 devices. sqlite3 /tmp/ns-audit/<profile>/data/nightshade.db '.tables' -> no pairing tables. sqlite3 ~/Documents/Nightshade/pairing.db 'select count(*) from paired_devices where is_active=1' -> 12, matching the UI exactly. ls ~/Nightshade/pairing.db -> a second, empty store.",
   "evidence": "/tmp/ns-audit/shots/rss-22.png (12 devices in a fresh profile). UI count 12 == DB is_active count 12. Device names/dates match row-for-row (Dashboard 2026-06-15, Android companion 2026-07-20 x3...).",
   "where": "packages/nightshade_remote_protocol/lib/src/database/pairing_database.dart:401-410 (_openConnection -> getApplicationDocumentsDirectory()/Nightshade/pairing.db)",
   "proposed_fix": "Resolve the pairing DB through the same directory resolver the main database uses (NIGHTSHADE_DATA_DIR-aware application-support dir), and migrate any existing Documents/Nightshade/pairing.db on first run. Include it in the backup manifest.",
   "verdict": "CONFIRMED",
   "verdict_reasoning": "Reproduced from a --fresh scratch profile (NIGHTSHADE_DATABASE_DIR=NIGHTSHADE_DATA_DIR=/tmp/ns-audit/v-settings-system/data). Settings > Remote Access > Manage Pairing immediately listed 13 devices paired 6/15/2026, 7/20 x3, 7/22, 7/25 x3, 7/26 -- months older than the profile. The scratch nightshade.db has NO pairing table; sqlite3 ~/Documents/Nightshade/pairing.db 'select count(*) ... is_active=1' returns 13, matching the UI count exactly and the names row-for-row. (Finding said 12; it is 13 now because concurrent audit agents paired since -- the count tracking the shared store IS the defect.) Cited location is exact: pairing_database.dart:401-410 _openConnection uses getApplicationDocumentsDirectory()/Nightshade/pairing.db with no data-dir override anywhere. Two-store claim holds: ~/Documents/Nightshade/pairing.db (13 active, mtime Aug 1) and ~/Nightshade/pairing.db (0 rows, mtime Jul 29).",
   "corrected_severity": "P1"
  },
  {
   "title": "After a device pairs successfully the desktop keeps showing the pairing code counting down, and the device list does not update",
   "kind": "defect",
   "severity": "P2",
   "unit": "settings:remote-access / screen:settings/pairing_screen.dart",
   "what_happened": "I pressed Start Pairing Mode, read the code ASTRO-NEBULA-1815 off the screen, and completed pairing over HTTP (POST /api/pairing/verify -> 200 with a bearer token). The server immediately invalidated the code (a second verify with the same code returned invalid_pairing_code) and wrote the 13th row into paired_devices. The desktop screen did not react at all: 30s later it still displayed 'Enter this code on your device: ASTRO-NEBULA-1815 / Expires in 04:18 / Cancel Pairing', and Paired Devices still listed 12 entries. Only after manually pressing the small refresh icon did the 13th device appear.",
   "expected": "The instant a code is consumed the panel should close and announce which device paired -- that confirmation is the operator's only way to know the phone in their hand is the device that just connected. Showing a live countdown for a code the server has already destroyed is a false statement, and it invites the operator to read the dead code to a second device.",
   "repro": "Settings > Remote Access > Manage Pairing > Start Pairing Mode. From a shell: curl -X POST -d '{\"code\":\"<code>\",\"deviceName\":\"x\",\"deviceId\":\"x\",\"deviceType\":\"browser\"}' http://127.0.0.1:<port>/api/pairing/verify. Watch the desktop: unchanged. sqlite3 ~/Documents/Nightshade/pairing.db 'select count(*) from paired_devices where is_active=1' -> 13 while the UI shows 12.",
   "evidence": "/tmp/ns-audit/shots/rss-23.png (code panel), /tmp/ns-audit/shots/rss-24.png (still counting down at 04:03 after the pair completed). Tree: 12 'Paired:' rows vs DB 13. After pressing refresh: 13 rows, 'audit-probe ... Paired: 1m ago'.",
   "where": "packages/nightshade_app/lib/screens/settings/pairing_screen.dart:96/215 (loadPairedDevices is only called on init and on the manual refresh button; nothing subscribes to pairing completion).",
   "proposed_fix": "Have the pairing session emit a completion event (the server already knows) and drive the panel from it: close the code card, toast '<deviceName> paired', and reload the list.",
   "verdict": "CONFIRMED",
   "verdict_reasoning": "Reproduced live. Manage Pairing > Start Pairing Mode; code NEBULA-MOON-4612. POST /api/pairing/verify -> 200 with a bearer token; a second verify with the same code -> 401 invalid_pairing_code, so the server destroyed it immediately, and paired_devices went 13 -> 14 active rows. The desktop did not react: at +5s and again at +60s the tree still read 'Enter this code on your device: / Expires in 04:35 ... 04:18 / Cancel Pairing', and the list rendered 13 'Paired:' rows against the DB's 14. Location is right: pairing_screen.dart:94-96 _initialize() is the constructor-time load, :215 loadPairedDevices, and the only other caller is the manual refresh IconButton at :484 -- nothing subscribes to pairing completion.",
   "corrected_severity": "P2"
  },
  {
   "title": "Paired devices always say 'Not seen yet / No connection recorded yet' -- last_connected_at is never written",
   "kind": "defect",
   "severity": "P2",
   "unit": "settings:remote-access / screen:settings/pairing_screen.dart",
   "what_happened": "Every one of the 13 paired devices shows the badge 'Not seen yet' and the line 'No connection recorded yet'. I proved this is untrue for at least one of them: after pairing 'audit-probe' I made three authenticated GET /api/status calls with its bearer (200, 200, 200) and then opened an authenticated WebSocket to /events (HTTP/1.1 101 Switching Protocols, held open 60s). paired_devices.last_connected_at for that device stayed NULL and the UI still says 'Not seen yet'. All 12 pre-existing devices -- including ones that demonstrably drove this app in earlier sessions ('ui-audit-avd', 'Lead Harness') -- are likewise NULL.",
   "expected": "last_connected_at should be stamped whenever a device authenticates. Without it the operator cannot tell a phone that is still in use from a stale credential, which makes the revoke list unusable: 7 of the 13 rows are named 'Android companion' with identical subtitles and no last-seen, IP or model to tell them apart.",
   "repro": "Pair a device via POST /api/pairing/verify, call GET /api/status with the returned bearer (200), then sqlite3 ~/Documents/Nightshade/pairing.db \"select device_name,last_connected_at from paired_devices where device_id='audit-probe-rss'\" -> last_connected_at empty. Settings > Remote Access > Manage Pairing > refresh: row still reads 'No connection recorded yet'.",
   "evidence": "/tmp/ns-audit/shots/rss-24.png (every row 'Not seen yet'); SQL output showing NULL last_connected_at for all 13 rows; curl 200s and the 101 upgrade transcript.",
   "where": "packages/nightshade_remote_protocol/lib/src/database/pairing_database.dart (no update of last_connected_at on the auth path) / apps/desktop/lib/headless_api_server/http_middleware.dart token resolution.",
   "proposed_fix": "Stamp last_connected_at (throttled, e.g. at most once a minute per device) when a bearer resolves, and surface it plus the peer IP in the device row.",
   "verdict": "CONFIRMED",
   "verdict_reasoning": "Reproduced live. After pairing device_id v2-probe-adv I made three authenticated GET /api/status calls with its bearer (200, 200, 200) and held an authenticated WebSocket on /events (101 Switching Protocols, ESTAB in ss for minutes). sqlite3 on the scratch pairing.db shows last_connected_at NULL for v2-probe-adv and for all 13 other rows, and every UI row reads 'Not seen yet / No connection recorded yet'. Root cause is slightly more precise than stated: the write exists (pairing_database.dart:138 updateLastConnected) and its only non-test caller is token_manager.dart:297 inside TokenManager.verifySessionToken, which the HTTP auth path never calls -- the desktop server authenticates from its in-memory BoundedTokenGrantMap _pairedSessionTokens (declared headless_api_server.dart:233, hydrated at server_lifecycle.dart:366, compared at http_middleware.dart:1030), so no request ever reaches the stamping code.",
   "corrected_severity": "P2"
  },
  {
   "title": "'Active Viewers' on the Remote Access page counts co-imaging collaborators, not connected remote clients -- it stays 0 with a live dashboard socket open",
   "kind": "defect",
   "severity": "P2",
   "unit": "settings:remote-access / screen:settings/remote_access_settings.dart",
   "what_happened": "Connection details is introduced as 'Open this section when you need viewer counts, authentication state, or the last runtime error' and shows 'Active Viewers'. With a paired client holding an authenticated WebSocket on /events (verified: HTTP/1.1 101 Switching Protocols, socket ESTAB in ss for the whole window) the panel read 'Active Viewers 0' throughout. The field is only ever set from the collaboration manager's viewer list (desktop_app_bootstrap.dart:346-349 -> collabState.viewers.length), i.e. co-imaging participants, not remote-access clients. The same field drives the status-bar 'N viewers connected' chip, so that indicator is dead for ordinary phone/browser clients too.",
   "expected": "On the Remote Access page 'Active Viewers' must mean 'remote clients currently connected to this server'. Count the live event-stream/WS sessions; if the co-imaging number is wanted too, label it separately.",
   "repro": "Enable remote access, pair a client, POST /api/ws/ticket, connect ws to /events?ticket=..., hold. Settings > Remote Access > Connection details: Active Viewers 0.",
   "evidence": "/tmp/ns-audit/shots/rss-28.png ('Active Viewers 0') captured while ss showed the ESTAB pair on :8099 and the probe had logged '101 Switching Protocols'.",
   "where": "packages/nightshade_app/lib/screens/settings/widgets/remote_access_settings.dart:520-524; fed only by apps/desktop/lib/desktop_app_bootstrap.dart:346-349; packages/nightshade_core/lib/src/providers/web_server_provider.dart:193 setActiveViewers.",
   "proposed_fix": "Publish a connected-client count from the headless API server's WS/SSE registry into webServerStateProvider and bind the row to that.",
   "verdict": "CONFIRMED",
   "verdict_reasoning": "Reproduced live. With a paired client holding an authenticated WebSocket on /events (101 Switching Protocols; ss shows the ESTAB pair on :8391 for the whole window) Connection details read 'Active Viewers 0' (/tmp/ns-audit/shots/v2-15.png). Code makes it structural: web_server_provider.dart:193 setActiveViewers is the only mutator of WebServerState.activeViewers, and its only non-test call site is desktop_app_bootstrap.dart:348 inside a listener on nextServer.collaborationManager.stream assigning collabState.viewers.length -- CollaborationViewer is the co-imaging participant type (live_collaboration_session.dart:69). The same field drives the status-bar chip (status_bar/session_sharing.dart:19,70,177-203). All three cited locations are accurate.",
   "corrected_severity": "P2"
  },
  {
   "title": "Seven paired devices are all called 'Android companion' with no way to tell them apart before revoking",
   "kind": "ux",
   "severity": "P3",
   "unit": "screen:settings/pairing_screen.dart",
   "what_happened": "The Paired Devices list shows 7 identical rows: title 'Android companion', subtitle 'Android phone or tablet', 'Not seen yet', 'No connection recorded yet', differing only by a paired-on date. There is no IP, no last-seen, no device model, no scope, and no rename. Revoking the phone you sold is guesswork.",
   "expected": "Show last-seen, last IP, granted scope (the DB already stores auth_grant_spec -- one of these devices holds 'admin' and the UI never says so) and allow renaming. NINA/ASIAIR-class tools name the client and show when it last talked.",
   "repro": "Settings > Remote Access > Manage Pairing.",
   "evidence": "/tmp/ns-audit/shots/rss-22.png; sqlite3 ~/Documents/Nightshade/pairing.db 'select device_name,auth_grant_spec from paired_devices' shows one row with auth_grant_spec='admin' that renders identically to the 'control' rows.",
   "where": "packages/nightshade_app/lib/screens/settings/pairing_screen.dart:543-620 (_buildDeviceRow)",
   "proposed_fix": "Render auth_grant_spec as a badge, add last-seen/IP, and add a rename action."
  },
  {
   "title": "Filter Focus Offsets silently omits the filter you just added -- you cannot set an offset for it without saving and re-opening the editor",
   "kind": "defect",
   "severity": "P3",
   "unit": "settings:equipment-profiles / screen:settings/profile_details_helpers.dart",
   "what_happened": "In Settings > Equipment Profiles > (profile) > Edit, I added a 4th filter 'SII' with the + button and typed its name. Filter Configuration then showed 4 chips (Lum, Ha, OIII, SII) but the Filter Focus Offsets card directly underneath still listed only 3 rows (Lum, Ha, OIII) -- no offset input exists for SII. The section is built from _filterControllers, but nothing calls setState when a filter-name field changes, so the offsets list lags one interaction behind. Pressing Save and re-entering Edit made the SII row appear. Same lag on the very first filter: the offsets section does not appear at all until some other widget forces a rebuild.",
   "expected": "Naming a filter should immediately add its focus-offset row; the two lists describe the same filters and must stay in sync within one edit session.",
   "repro": "Settings > Equipment Profiles > New Profile > Edit > Filter Configuration '+' > type a filter name > look at Filter Focus Offsets: the new filter is missing. Save, re-open Edit: now present.",
   "evidence": "/tmp/ns-audit/shots/rss-49.png -- Filter Configuration row reads Lum | Ha | OIII | SII while Filter Focus Offsets below lists Lum, Ha, OIII only. Confirmed in tree output too (4 filter fields, 3 offset rows).",
   "where": "packages/nightshade_app/lib/screens/settings/equipment_profiles_screen_parts/profile_details_helpers.dart:154-165 (_buildFilterOffsetRows reads _filterControllers) with no onChanged->setState on the filter-name TextFields in profile_details_rendering.dart:449-500",
   "proposed_fix": "Add onChanged: (_) => setState(() {}) to the filter-name fields (or drive both lists off a single ValueNotifier).",
   "verdict": "CONFIRMED",
   "verdict_reasoning": "Reproduced live on a fresh profile. In Edit, typing 'Lum' into the pre-seeded filter chip produced NO Filter Focus Offsets section at all -- it appeared only after I pressed '+', which forced a rebuild. Then with chips 'Lum | Ha', typing 'Ha' into the second chip left Filter Focus Offsets listing only 'Lum' with no input for Ha (/tmp/ns-audit/shots/v2-24.png; full page /tmp/ns-audit/shots/v2-27.png). Save + re-open Edit made both rows appear (/tmp/ns-audit/shots/v2-28.png). Mechanism as described: profile_details_helpers.dart:155-166 builds the offset rows from _filterControllers[i].text and the filter-name TextField has no onChanged, so nothing calls setState when the name is typed.",
   "corrected_severity": "P3",
   "location_wrong": "First half right (profile_details_helpers.dart:154-165 is the offsets loop). Second half off: the filter-name TextField that lacks onChanged is _EditableFilterChip in profile_controls.dart:306-321, not profile_details_rendering.dart:449-500 -- that range only instantiates the chip (:486-492)."
  },
  {
   "title": "Deleting your last equipment profile leaves a ghost profile on screen whose 'Set Active' throws an unhandled exception and does nothing",
   "kind": "defect",
   "severity": "P2",
   "unit": "settings:equipment-profiles / screen:settings/screen_shell.dart",
   "what_happened": "Settings > Equipment Profiles with exactly one profile ('Audit Rig Alpha'): overflow > Delete > Delete. equipment_profiles goes to 0 rows and the list column correctly reads 'No profiles yet' -- but the detail pane on the right keeps rendering the deleted profile in full (name, f/8.0, gain 100, all four filters, focus offsets) with a live 'Set Active' button and a live overflow menu. Two surfaces in the same screen disagree about whether the profile exists. Clicking 'Set Active' produces no toast, no error, no visible change: it throws [ERROR] Unhandled Exception: Bad state: Cannot activate profile 1: no such profile row (activateProfileStrictTransactional) straight into the log. Clicking Edit puts the ghost into an editable form; typing a new name and pressing Save shows 'Failed to save profile: Exception: Profile not found' and the typed work is lost.",
   "expected": "Deleting the selected profile must clear the selection when nothing is left -- the pane should fall back to the 'Select a profile / Choose a profile from the list or create a new one' empty state (which the app already has and shows before the first profile is created). No control should remain that acts on a deleted row, and a failed activation must never be an unhandled exception.",
   "repro": "Fresh profile > Settings > Equipment Profiles > New Profile 'X' > Create > overflow (three dots) > Delete > Delete. Observe list 'No profiles yet' + full detail pane still shown. Click 'Set Active'. sqlite3 <data>/nightshade.db 'select count(*) from equipment_profiles' -> 0. drive_linux.py log -> Unhandled Exception: Bad state: Cannot activate profile 1: no such profile row.",
   "evidence": "/tmp/ns-audit/shots/rss-55.png -- middle column reads 'No profiles yet' while the right pane renders 'Audit Rig Alpha' with Set Active. Log lines quoted above; DB count 0 before and after the Set Active click.",
   "where": "packages/nightshade_app/lib/screens/settings/equipment_profiles_screen_parts/screen_shell.dart:232 (onSetActive -> _activateProfile, unawaited/uncaught) and packages/nightshade_core/lib/src/providers/profiles_provider.dart:980 -> profile_activation_writethrough.dart:156",
   "proposed_fix": "In the profiles notifier, null out the selected profile when the deleted id is the selected one and the list is empty; guard the detail pane on 'selected profile still exists in state.profiles'; wrap _activateProfile in a try/catch that surfaces the failure as a snackbar.",
   "verdict": "CONFIRMED",
   "verdict_reasoning": "Reproduced end to end. With exactly one profile left, overflow > Delete > Delete drove equipment_profiles to 0 rows (sqlite3 confirmed) and the middle column correctly showed 'No profiles yet' -- while the right pane kept rendering the deleted profile in full (name, f/5.0, 530 mm, 106 mm, both filters, focus offsets) with a live 'Set Active' button and a live overflow menu (/tmp/ns-audit/shots/v2-36.png). Clicking Set Active produced no toast and no visible change; the log carries the claimed unhandled exception with a stack trace matching all three cited locations verbatim: 'Unhandled Exception: Bad state: Cannot activate profile 6: no such profile row (strict activation requires an existing target)' at profile_activation_writethrough.dart:156 <- profiles_provider.dart:980 _activateProfile <- screen_shell.dart:232. Not separately re-tested this pass: the Edit-then-Save 'Profile not found' half of the report.",
   "corrected_severity": "P2"
  },
  {
   "title": "Import failures are reported as raw Dart exception text ('Import failed: FormatException: Unexpected character (at character 1)')",
   "kind": "ux",
   "severity": "P3",
   "unit": "settings:equipment-profiles / screen:settings/profile_controls.dart",
   "what_happened": "Equipment Profiles > import (the download-arrow button next to New Profile) accepts any file. Choosing a non-JSON file shows the snackbar 'Import failed: FormatException: Unexpected character (at character 1)'; choosing a syntactically valid but unrelated JSON shows 'Import failed: FormatException: Profile name must be a non-empty string'. Both are the raw toString() of the thrown exception.",
   "expected": "'That file is not a Nightshade profile export' plus the file name. The exported JSON also carries no format marker or version field at all (I exported one and read it: it is a bare map of profile fields), so the importer has nothing to check against and cannot warn about profiles exported by a newer build.",
   "repro": "Settings > Equipment Profiles > import icon > pick any .txt. Then pick a JSON that is not a profile.",
   "evidence": "tree after import: 'panel: Import failed: FormatException: Unexpected character (at character 1)' and 'Import failed: FormatException: Profile name must be a non-empty string'. Export file /tmp/ns-audit/r-settings-system/exported-profile.json contains no schema/version key.",
   "where": "packages/nightshade_app/lib/screens/settings/equipment_profiles_screen_parts/profile_controls.dart (import handler) and the profile toJson used by Export",
   "proposed_fix": "Add {\"format\":\"nightshade.equipment-profile\",\"version\":1} to the export and validate it on import; map failures to plain-language messages."
  },
  {
   "title": "A profile saved without optics can never be edited again: Save is rejected because of the 0 the app itself stored, and the rejection is invisible on screen",
   "kind": "defect",
   "severity": "P1",
   "unit": "settings:equipment-profiles / screen:settings/profile_details_rendering.dart",
   "what_happened": "New Profile writes focal_length=0.0 / aperture=0.0. Re-open that profile, press Edit, change ONLY the name, press Save: nothing happens. No banner, no field-level error, no colour change -- the button appears dead. I clicked Save five times over three minutes and the DB never changed (still 'Backup Test Rig' while the field read 'Local Work Do Not Lose'). The failure is real and has a message -- the accessibility tree at that moment contains 'Focal length must be a positive number (leave blank if unknown)' and 'Aperture must be a positive number (leave blank if unknown)' -- but a full-window screenshot taken between the click and that tree read shows no such text anywhere on screen (/tmp/ns-audit/shots/rss-67.png), and neither does a full-resolution capture of the bottom 160px where the snackbar would sit (/tmp/ns-audit/shots/rss-snack-1.png). The snackbar is rendered underneath the persistent bottom status bar. Clearing both optics fields let the save through -- and it stored 0.0 again, so the profile is right back in the unsavable state next time.",
   "expected": "Blank optics must round-trip as blank/NULL, not as 0.0 that the validator then rejects; and a rejected save must show its reason where the user is looking (inline under the offending field, plus a visible banner). As shipped, the very first thing a user does after creating a profile -- open it and fill something in -- hits a dead Save button with no explanation.",
   "repro": "Settings > Equipment Profiles > New Profile > name 'X' > Create. Overflow menu > Edit > change the name > Save. Nothing happens; sqlite3 <data>/nightshade.db 'select name,focal_length,aperture from equipment_profiles' still shows the old name and 0.0/0.0. Now clear Focal Length and Aperture and press Save: it saves, and stores 0.0/0.0 again.",
   "evidence": "/tmp/ns-audit/shots/rss-64.png and /tmp/ns-audit/shots/rss-67.png (identical -- edit form with 'Local Work Do Not Lose' typed, Focal Length 0, Aperture 0, no error visible) taken immediately after the Save click; tree read after that same click contains the two validation strings; DB unchanged across all attempts.",
   "where": "validator: ProfileValidator.parseOptional... (focal/aperture 'must be a positive number') reached from packages/nightshade_app/lib/screens/settings/equipment_profiles_screen_parts/profile_details.dart:175-200; the empty-field -> 0.0 write is in the same save path; the snackbar is shown by the settings scaffold underneath the app's bottom status bar.",
   "proposed_fix": "Persist blank optics as NULL (the column is already nullable in the export -- focalLength shows as a number only when set), treat 0 as 'unset' on read, and render validation failures inline on the fields plus a banner above the status bar.",
   "verdict": "CONFIRMED",
   "verdict_reasoning": "SYMPTOM CONFIRMED, HALF THE STATED CAUSE REFUTED. Confirmed: New Profile wrote id=1 focal_length=0.0 aperture=0.0; the Edit form pre-fills both optics fields with a literal '0'; changing only the name and pressing Save left the DB untouched across repeated attempts; clearing BOTH optics fields let the save through and stored 0.0/0.0 again, putting the profile straight back in the rejecting state. Code is exact: _parseOptic (equipment_profile_validation.dart) returns ok(0.0) for blank but fails any value <= 0 with 'must be a positive number (leave blank if unknown)', while the model stores blank as the 0 sentinel and the editor renders that sentinel as '0'. REFUTED: 'nothing happens, no banner, the button appears dead, a full-window screenshot shows no such text'. Burst capture (250 ms interval, numpy diff vs baseline) shows a full-width red SnackBar carrying both messages appearing ~0.5 s after the click and persisting for its 4 s duration. The earlier screenshot was taken inside the entrance-animation latency. The finding IS right that there is no field-level error and no colour change on the offending fields. Downgraded to P2: the user gets a visible banner naming the exact fix, so this is a round-trip trap, not silent failure.",
   "corrected_severity": "P2",
   "location_wrong": "profile_details.dart:175-200 -- the optics validators are actually at :170 and :172, the error is raised at :219, and the whole _validateAndBuild is :167-248"
  },
  {
   "title": "Backups are written to ~/Documents/Nightshade/backups regardless of the configured data directory, so bundles from different databases pile into one undifferentiated 'Recent Backups' list",
   "kind": "defect",
   "severity": "P1",
   "unit": "settings:backup / screen:settings/backup_screen.dart",
   "what_happened": "I launched a brand-new scratch profile with NIGHTSHADE_DATABASE_DIR=NIGHTSHADE_DATA_DIR=/tmp/ns-audit/r-settings-system/data and an empty nightshade.db. Settings > Backup & Restore immediately listed 8 'Recent Backups' dated Aug 1 -- the developer's real bundles -- and reported 'Last Full Backup Aug 2, 2026 10:27'. The backup directory is ~/Documents/Nightshade/backups and ignores the data-dir override entirely, so this sandboxed instance also *wrote* into it: nightshade_autosave_2026-08-02T10-27-37, -27-44, -27-45 and -30-25 are snapshots of my throwaway database (the 10:30 one records profilesCount: 0, taken right after I deleted every profile) sitting in the same list as the real ones. Nothing in the row -- name, size, date, icon -- says which database a bundle came from. A user with two installs, or anyone who has ever moved their data directory, is one click away from restoring a foreign configuration over their live one. As with pairing.db there are also two directories on this machine (~/Documents/Nightshade/backups with 9 bundles and ~/Nightshade/backups with 4), i.e. the same install has written backups to two places.",
   "expected": "Backups belong under the configured data directory, and each row should show what it contains (profiles/sequences/targets counts are already in the bundle metadata) and which install/machine wrote it. The bundle already carries appVersion and platform; add a database/install id and show it.",
   "repro": "drive_linux.py start --profile <new> --fresh > Settings > Backup & Restore. The list is non-empty on a first-ever launch. ls ~/Documents/Nightshade/backups -> the same 8 files, plus new nightshade_autosave_* written by the sandbox. python3 -c \"import json;print(json.load(open('.../nightshade_autosave_2026-08-02T10-30-25.892145.nsbackup'))['metadata'])\" -> profilesCount 0, i.e. my scratch DB.",
   "evidence": "/tmp/ns-audit/shots/rss-59.png (Recent Backups list mixing Aug 1 and Aug 2 bundles). Directory listing of ~/Documents/Nightshade/backups and ~/Nightshade/backups. Bundle metadata quoted above.",
   "where": "the backup service's directory resolution (getApplicationDocumentsDirectory()/Nightshade/backups) rather than the NIGHTSHADE_DATA_DIR-aware resolver used by nightshade.db",
   "proposed_fix": "Resolve the backup directory through the same data-dir resolver as the main database; stamp an install id into the bundle and show bundle contents (profiles/sequences/targets) in each Recent Backups row.",
   "verdict": "CONFIRMED",
   "verdict_reasoning": "Reproduced on a first-ever launch. Settings > Backup & Restore came up with a NON-EMPTY list -- 9 bundles dated Aug 1/Aug 2 this profile never created -- and 'Last Full Backup Aug 2, 2026 12:03'. Merely opening the screen also WROTE into the shared dir: ~/Documents/Nightshade/backups gained autosaves at 12-03-46/47/48 whose metadata is my throwaway DB (settingsCount 121, profilesCount 0), listed alongside the developer's real bundles. A fourth file appeared at 12-03-53 with profilesCount 1 while my DB had zero profiles -- a CONCURRENT audit instance writing into the same list, i.e. the finding's two-installs scenario happening live. `find /tmp/ns-audit/v-settings-system -iname '*backup*'` returns nothing. Root cause pinned: backup_service.dart:1025-1030 _getBackupDirectory -> getApplicationDocumentsDirectory()/Nightshade/backups; the _backupDirectoryProvider override (:121/:149) is referenced from exactly one place in the repo, backup_service_listing_test.dart:34, so production has no data-dir-aware path. Rows render name + size + date only, so nothing identifies the source database.",
   "corrected_severity": "P1"
  },
  {
   "title": "Backup & Restore contradicts itself about what restore does -- one card says nothing is merged, the confirm dialog says nothing is deleted",
   "kind": "defect",
   "severity": "P2",
   "unit": "settings:backup / screen:settings/backup_screen.dart",
   "what_happened": "The Backup & Sync card states: 'Sync is bundle-based: restoring replaces local configuration - nothing is merged.' The restore confirmation on the same screen states the opposite: 'This imports \"<file>\" without deleting your current data. Settings with the same keys may be updated, and library items from the backup may be added.' I measured which is true for the local path: I inserted app_settings key 'audit_canary'='present' (not in the bundle), deleted every equipment profile, then restored the bundle. Result: the profile came back AND audit_canary was still 'present' -- merge semantics. So the Backup & Sync sentence is the false one, and it is the more dangerous direction to be wrong in: a user who believes 'replaces local configuration' will restore expecting a clean slate and instead get a hybrid of two configurations with no way to tell which settings came from where.",
   "expected": "One sentence, true for both the local and the remote path, and a restore that states exactly what it will do to each category (settings overwritten by key, library items added, nothing removed).",
   "repro": "Settings > Backup & Restore. Read the Backup & Sync paragraph. Click the restore icon on any Recent Backups row and read the dialog. Then: sqlite3 <data>/nightshade.db \"insert into app_settings values('audit_canary','present')\"; delete from equipment_profiles; restore the bundle; both the profile and audit_canary are present afterwards.",
   "evidence": "/tmp/ns-audit/shots/rss-58.png (the 'nothing is merged' paragraph) and /tmp/ns-audit/shots/rss-60.png (the 'without deleting your current data' dialog). SQL before/after quoted above.",
   "where": "packages/nightshade_app/lib/screens/settings/backup_screen.dart (Backup & Sync description vs the restore confirm dialog copy)",
   "proposed_fix": "Make the sync paragraph describe the actual merge semantics, or make the remote restore genuinely replace and say so in both places.",
   "verdict": "REFUTED",
   "verdict_reasoning": "Both sentences are true of their own operation; the finding compared copy from two different restore paths. The 'Sync is bundle-based ... nothing is merged' paragraph belongs to CloudSyncCard (cloud_sync_settings.dart:377-378, rendered on the Backup screen via backup_screen.dart:485) and describes the WebDAV/S3 pull-and-restore, which really does replace -- cloud_sync_settings.dart:324 calls service.pullAndRestore(..., replaceExisting: true) and its own confirm dialog (:313-314) repeats the replace wording. The 'without deleting your current data' dialog is ConfirmDialog.restore (utils/confirm_dialog.dart:88-100), used by backup_screen.dart:236 for a LOCAL Recent-Backups restore, which calls backupService.restoreBackup(replaceExisting: false) at :265-268 -- merge semantics, exactly as the dialog says. The verifier's experiment (canary survives, profile returns) is the documented behaviour of the local path and does not show the sync sentence is false. A mild UX ambiguity remains -- two restore semantics on one screen -- but the reported defect does not hold.",
   "corrected_severity": "P4",
   "location_wrong": "Cited packages/nightshade_app/lib/screens/settings/backup_screen.dart, which contains neither string. They live in packages/nightshade_app/lib/screens/settings/widgets/cloud_sync_settings.dart:313-314,377-378 and packages/nightshade_app/lib/utils/confirm_dialog.dart:96-98."
  },
  {
   "title": "Restore silently drops a backed-up equipment profile when its row id is already taken, and still reports 'Restored 123 items'",
   "kind": "defect",
   "severity": "P1",
   "unit": "settings:backup / screen:settings/backup_screen.dart",
   "what_happened": "The bundle nightshade_backup_2026-08-02T10-43-32.295333.nsbackup contains exactly one equipment profile: {\"id\": 3, \"name\": \"Backup Test Rig\"} (metadata: settingsCount 122, profilesCount 1 -> 123 items). Locally, row id 3 existed but had been renamed to 'Local Work Do Not Lose'. I restored that bundle. The toast said 'Restored 123 items. Restart Nightshade before the next run.' The DB afterwards: 3|Local Work Do Not Lose|1 -- one profile, the local one, unchanged. 'Backup Test Rig' was neither inserted nor applied over row 3; it was dropped on the floor. The same restore into a database where id 3 was free DID bring the profile back (I verified that first, after deleting every row), so the discriminator is the id collision, not the bundle. The count in the message is the bundle's item count, not the number of items actually applied, so it is a claim the app has not checked.",
   "expected": "Restore must not key library items on the backup's row ids -- ids are local and get reused as soon as you delete the highest row. Re-key on content (name) or insert with a fresh id, and report what was actually written ('122 settings updated, 1 profile added, 0 skipped'), never the bundle's own count.",
   "repro": "Create a profile, Create Backup, rename that profile (or delete it and create another that takes the same rowid), then restore the bundle from Recent Backups. Toast claims all items restored; sqlite3 <data>/nightshade.db 'select id,name from equipment_profiles' shows the backed-up profile is absent.",
   "evidence": "Bundle JSON equipmentProfiles[0] = id 3 / 'Backup Test Rig'; DB after restore = 3|Local Work Do Not Lose|1; tree after restore = 'panel: Restored 123 items. Restart Nightshade before the next run.' Control case: with equipment_profiles emptied, the same bundle restored the profile back as id 3.",
   "where": "the restore path in the backup service (row-id-keyed upsert/ignore for equipmentProfiles) called from packages/nightshade_app/lib/screens/settings/backup_screen.dart",
   "proposed_fix": "Strip ids on restore and insert as new rows (matching existing rows by name to update), and build the success message from the write result rather than from bundle metadata.",
   "verdict": "CONFIRMED",
   "verdict_reasoning": "Reproduced with my own bundle plus a control case. Bundle nightshade_backup_2026-08-02T12-13-00 contained equipmentProfiles[0] = {id:1,'VSS Renamed B'}, metadata settingsCount 122 + profilesCount 1 = 123. I renamed local row id 1 to 'VSS Local Keep' and restored: toast read verbatim 'Restored 123 items. Restart Nightshade before the next run.' while sqlite3 afterwards showed 1|VSS Local Keep -- the backed-up profile neither inserted nor applied. CONTROL: with equipment_profiles emptied, the SAME bundle restored it back as 1|VSS Renamed B, so the id collision is the discriminator. Mechanism confirmed in code: _importProfiles inserts with the backup's row id under InsertMode.insertOrIgnore and increments count unconditionally, so itemsRestored is rows ATTEMPTED, not rows written. One nuance in the app's favour that the finding omits: the confirm dialog hedges with 'library items from the backup may be added' -- but the toast then states a definite count it never checked. P1 retained.",
   "corrected_severity": "P1",
   "location_wrong": "cited only as 'the restore path in the backup service ... called from backup_screen.dart'; the defect lines are sequence_codec.dart:50-52 (id taken from the bundle), :114 (InsertMode.insertOrIgnore) and :116 (unconditional count++), with backup_screen.dart:267 forcing replaceExisting:false and :275 printing the count"
  },
  {
   "title": "'Restore' asks you to restart the app afterwards but does not offer to, and nothing on the screen stays to remind you",
   "kind": "ux",
   "severity": "P3",
   "unit": "settings:backup",
   "what_happened": "The restore result is a transient snackbar: 'Restored 123 items. Restart Nightshade before the next run.' Once it fades there is no indication anywhere that the running app is now showing stale in-memory state that disagrees with the database -- and the whole point of restoring is usually that you are about to observe. There is no Restart button.",
   "expected": "Either reload the providers after a restore (the app already rebuilds them on profile switch) or show a persistent banner with a 'Restart now' action.",
   "repro": "Settings > Backup & Restore > restore icon on any bundle > Restore. Wait 5 seconds; the message is gone and the screen looks normal.",
   "evidence": "tree immediately after restore contains the message; a tree read a few seconds later does not.",
   "where": "packages/nightshade_app/lib/screens/settings/backup_screen.dart restore result handling",
   "proposed_fix": "Persistent banner + 'Restart now', or invalidate the affected providers in place."
  },
  {
   "title": "Every Settings snackbar renders underneath the persistent bottom status bar, so all success and failure feedback in Settings is invisible",
   "kind": "defect",
   "severity": "P1",
   "unit": "settings (all sections) / screen:settings/settings_screen.dart",
   "what_happened": "Four separate Settings actions produce a SnackBar that exists in the widget tree but never appears on screen, because the Settings scaffold's snackbar is drawn at the bottom of the body while the app's always-on status bar (Idle | profile | Camera Disconnected | ... | LST) is painted on top of it. Confirmed cases: (1) a rejected profile Save -- tree contains 'Focal length must be a positive number (leave blank if unknown)' while the full-window capture /tmp/ns-audit/shots/rss-67.png shows nothing; (2) a failed catalog download -- tree contains 'Download failed: Exception: Annotation catalog download failed' while a full-resolution capture of the bottom 200px at the same moment (/tmp/ns-audit/shots/rss-75.png) shows only the status bar; (3) 'Failed to save profile: Exception: Profile not found'; (4) 'Restored 123 items. Restart Nightshade before the next run.' In every case I captured the screen between the click and a tree read that still contained the message, so the message was live and not yet dismissed. The user's experience is a button that does nothing, for both failures and successes.",
   "expected": "Feedback must be visible. Either give the SnackBar a bottom margin equal to the status-bar height (Flutter: SnackBarBehavior.floating with margin), or host it above the status bar in the shell.",
   "repro": "Settings > Equipment Profiles > New Profile > Edit > rename > Save (rejected). Or Settings > Catalogs > Download Standard (~50 MB) (VizieR TAP currently 503). Take a screenshot immediately; nothing is shown. Then run drive_linux.py tree and find the message text in the accessibility tree.",
   "evidence": "/tmp/ns-audit/shots/rss-67.png, /tmp/ns-audit/shots/rss-75.png, /tmp/ns-audit/shots/rss-snack-1.png -- all captured while the corresponding message was present in the accessibility tree.",
   "where": "the ScaffoldMessenger used by packages/nightshade_app/lib/screens/settings/* vs the persistent status bar in the desktop shell",
   "proposed_fix": "Set SnackBarBehavior.floating with a bottom margin >= status-bar height for the settings scaffold, or route settings feedback through the shell's own notification surface.",
   "verdict": "REFUTED",
   "verdict_reasoning": "The snackbars are visible; I measured rather than eyeballed. Method: xdotool click, then `import -window root` every 250-300 ms, then numpy pixel-diff each frame against a pre-click baseline. Case 1 (rejected profile Save): frames 1-2 change ONLY the status-bar clock digits (y1027-1036, x1671-1677); from frame 3 a full-width bar occupies y=970..1039, x=175..1744 (109,872 changed px). The crop is a red SnackBar with both validation messages, fully legible, persisting through its 4 s duration. Case 2 (restore toast): same geometry, green bar reading 'Restored 123 items...'. The stated cause is backwards -- window is 160,150..1760,1050 and the status bar is ~y1019..1050, so the snackbar overlaps it and is painted ON TOP, covering it. Structurally that is expected: app_shell.dart:633-816 makes StatusBar(compact:false) the last child of the shell Scaffold's body Column with no bottomNavigationBar, and Scaffold paints _ScaffoldSlot.snackBar after the body. What produced the original observation is a ~0.5 s gap between click and first painted snackbar frame on the softpipe renderer: any capture inside that window shows nothing while a `tree` read a second later still finds the message. I reproduced that same false negative myself before switching to burst capture -- harness artefact, not defect. Residual real issue worth P3 only: while shown the snackbar hides the status bar, and TransientBottomInset (snackbar_helper.dart:20-27), the mechanism built to prevent exactly that, is published by one screen in the whole app (imaging_screen.dart:153).",
   "corrected_severity": "P3"
  },
  {
   "title": "GLADE+ catalog download is broken end to end: the VizieR TAP endpoint it targets returns 503, and the app's only report of that is a snackbar you cannot see",
   "kind": "defect",
   "severity": "P2",
   "unit": "settings:catalogs / screen:settings/catalog_settings_screen.dart",
   "what_happened": "Settings > Catalogs > GLADE+ Galaxy Catalog > 'Download Standard (~50 MB)': the button flips to 'Downloading...' for about 3 seconds, then silently returns to its idle label. glade_plus_galaxies.csv stays 0 bytes and no .partial appears. Only the accessibility tree carries the reason -- 'Download failed: Exception: Annotation catalog download failed' -- and that snackbar is hidden behind the status bar (see the snackbar finding), so on screen the button simply does nothing. The cause is upstream: the URL built by buildGladePlusUrl() targets https://tapvizier.cds.unistra.fr/TAPVizieR/tap/sync, and I reproduced the exact query with curl -- HTTP 503 'No server is available to handle this request' in 0.36 s, while https://vizier.cds.unistra.fr/ itself answers 200. So the one deep-sky catalog the app offers to fetch on demand cannot be fetched at all, on any tier.",
   "expected": "A download that cannot succeed should say so in plain language and name the host ('VizieR TAP is not responding (HTTP 503) - try again later, or use Import from File'), and the app should not depend on a single TAP mirror for a 2 GB optional catalog.",
   "repro": "Settings > Catalogs > scroll to GLADE+ > Download Standard (~50 MB). Watch: 'Downloading...' -> idle. ls ~/.local/share/com.example.nightshade_desktop/catalogs/glade_plus_galaxies.csv -> still 0 bytes. curl the TAP URL from packages/nightshade_planetarium/lib/src/catalogs/catalog_manager/source_models.dart:337-355 -> 503.",
   "evidence": "curl output: code=503 size=108 time=0.36 for the Standard-tier query; tree at t4-t10 after the click: 'Download failed: Exception: Annotation catalog download failed'; file listing showing 0 bytes throughout.",
   "where": "packages/nightshade_planetarium/lib/src/catalogs/catalog_manager/source_models.dart:337-355 (buildGladePlusUrl) and the download handler in packages/nightshade_app/lib/screens/settings/catalog_settings_screen.dart",
   "proposed_fix": "Surface the HTTP status and host in the error; add a fallback mirror or a pre-built bundle (the app already ships a catalogs-v1 GitHub release for HYG/NGC); keep 'Import from File (CSV)' prominent as the documented workaround.",
   "verdict": "REFUTED",
   "verdict_reasoning": "The download works. I reproduced the exact query buildGladePlusUrl() emits for the Standard tier (source_models.dart:340-356, still targeting tapvizier.cds.unistra.fr/TAPVizieR/tap/sync) with curl: HTTP 200, 4,874,018 bytes of CSV in 9.2 s. Then I drove the UI: Settings > Catalogs > GLADE+ > 'Download Standard (~50 MB)' went to 'Downloading...' with a live 'Downloading: GLADE+ Galaxy Catalog' progress panel, the .partial grew, and it finished with glade_plus_galaxies.csv at 15,635,359 bytes -- no error, no failure snackbar. The 503 the finding measured was a transient VizieR outage, not an app defect, so 'cannot be fetched at all, on any tier' does not hold. Two residuals for a separate ticket: the tier is labelled '~50 MB' but delivers ~15.6 MB, and the download bypasses NIGHTSHADE_DATA_DIR -- it wrote into the developer's real ~/.local/share/com.example.nightshade_desktop/catalogs/ from a sandboxed instance.",
   "corrected_severity": "P4"
  },
  {
   "title": "A 426 MB abandoned GLADE download sat in the catalog directory for six weeks with nothing in the UI acknowledging it",
   "kind": "ux",
   "severity": "P3",
   "unit": "settings:catalogs",
   "what_happened": "~/.local/share/com.example.nightshade_desktop/catalogs contained glade_plus_galaxies.csv.partial at 426,030,763 bytes dated Jun 17, alongside a 0-byte glade_plus_galaxies.csv. The Catalogs page showed GLADE+ as not installed and offered a fresh download, with no mention that 407 MiB of a previous attempt was still on disk and no way to reclaim it from the UI. (It is deleted when a new download starts -- I watched it vanish the moment I pressed Download -- so the space is recovered only by attempting another download, which currently cannot succeed.)",
   "expected": "Show 'Partial download: 407 MB - Resume / Discard' on the card, or clean up partials on launch. Deep-sky users on small SSDs care about half a gigabyte.",
   "repro": "ls -la ~/.local/share/com.example.nightshade_desktop/catalogs/ before pressing Download.",
   "evidence": "Directory listing: glade_plus_galaxies.csv.partial 426030763 bytes, Jun 17 13:13; glade_plus_galaxies.csv 0 bytes, Jun 11.",
   "where": "the annotation catalog download service's partial-file handling",
   "proposed_fix": "Surface and offer to resume or delete partials; sweep stale .partial files at startup."
  },
  {
   "title": "The Deep-Star Tier card tells an astrophotographer to build and host their own tileset with a repo tool",
   "kind": "ux",
   "severity": "P3",
   "unit": "settings:catalogs / screen:settings/deep_star_catalog_card.dart",
   "what_happened": "Settings > Catalogs > 'Deep-Star Tier (Tycho-2 / Gaia)' reads: 'Streams faint stars below the bundled HYG floor (~mag 11.5) as view-culled tiles when zoomed in. Host a tileset built with tools/catalog_prep and point the URL below at it.' That is an instruction to a developer with a checkout of this repository, shipped verbatim in a consumer settings page. There is no default host, no 'get one from Nightshade' option, and no explanation of what happens if you do not.",
   "expected": "Ship or host a tileset and make this one toggle. If self-hosting really is the only path, say so honestly ('No public tileset is available yet; advanced users can host their own') and link documentation rather than a source path.",
   "repro": "Settings > Catalogs > scroll to Deep-Star Tier.",
   "evidence": "/tmp/ns-audit/shots/rss-74.png (card text visible at the bottom of the page).",
   "where": "packages/nightshade_app/lib/screens/settings/widgets/deep_star_catalog_card.dart",
   "proposed_fix": "Provide a default tileset URL or reword for a non-developer audience."
  },
  {
   "title": "Appliance Catalogs on the desktop app talks about 'the phone's own planetarium catalogs' and offers no way to connect anything",
   "kind": "defect",
   "severity": "P3",
   "unit": "settings:rig-catalogs",
   "what_happened": "Settings > Appliance Catalogs on the Linux desktop build renders a title, the subtitle 'Download and verify the rig's plate-solve / star catalogs', and one info panel: 'Connect to a remote appliance to manage its catalogs. This is separate from the phone's own planetarium catalogs.' There is no phone here -- this is the desktop app -- and the page carries no connect button, no appliance picker and no link to Remote Access, so it is an entirely inert settings leaf whose only sentence is about a different platform.",
   "expected": "Say 'This machine's own planetarium catalogs' (or 'this device's'), and either give the page an action that reaches the appliance-connection flow or hide the section on builds that are not acting as a remote client.",
   "repro": "Settings > search 'catalog' > Appliance Catalogs (desktop Linux build, no appliance paired).",
   "evidence": "/tmp/ns-audit/shots/rss-76.png",
   "where": "the appliance-catalogs settings section copy",
   "proposed_fix": "Platform-neutral wording plus a 'Connect an appliance' action that routes to Remote Access, or gate the section on an active appliance connection.",
   "verdict": "CONFIRMED",
   "verdict_reasoning": "Reproduced live on the Linux desktop build with no appliance connected. Settings > Appliance Catalogs renders exactly three strings and nothing else: 'Appliance Catalogs', 'Download and verify the rig's plate-solve / star catalogs', and one info panel 'Connect to a remote appliance to manage its catalogs. This is separate from the phone's own planetarium catalogs.' The tree shows no button, picker or link anywhere on the page. Source confirms: rig_catalog_settings.dart:281-289, the `if (backend == null)` branch renders a bare _Info with that text and no action, and the desktop build hard-codes 'the phone's'.",
   "corrected_severity": "P3",
   "location_wrong": "The finding gave no file. Precise location: packages/nightshade_app/lib/screens/settings/widgets/rig_catalog_settings.dart:276-289."
  },
  {
   "title": "The desktop app can never tell you a new version exists: the only 'Updates' page in Settings is for a remote appliance, and the built-in update manager is wired to a server URL nothing ever sets",
   "kind": "defect",
   "severity": "P2",
   "unit": "settings:updates",
   "what_happened": "Searching Settings for 'update' returns exactly two entries: About and 'Appliance Updates'. Appliance Updates says 'Connect to a remote appliance to manage its updates. The local desktop app updates itself through its own installer.' and renders nothing else off-network. There is no local update check anywhere in Settings, and About (which the settings catalog even tags with the keyword 'updates') shows only a version string and three web links. The app does ship an in-app updater -- app.dart:204-206 wraps the desktop shell in UpdateManagerWidget, which fires _checkForUpdates() 3 s after launch -- but that check is gated on state.updateServerUrl being non-empty (update_manager_widget.dart:40-43) and the only caller of UpdateNotifier.configure(serverUrl:) in the whole repo is apps/desktop/lib/headless_api/update_wiring.dart, i.e. the appliance server, never the GUI. So on the shipped desktop build the updater is inert: I ran the app for ~40 minutes with working internet and it made no version request, showed no update UI, and logged nothing about updates. The sentence 'updates itself through its own installer' is also not true of the Linux build, which is distributed as a tarball with no installer.",
   "expected": "A 'Check for updates' row in About or a Software Update section: current version + build, channel (stable/beta), last checked, and a manual check. NINA and SGP both check at launch and show release notes; an astrophotographer needs to know a fix landed before the night, not after.",
   "repro": "Settings > search 'update' -> About, Appliance Updates. Open Appliance Updates on the desktop build: one explanatory sentence, no controls. Open About: version 6.1.0, three links, System Information. No update control anywhere. drive_linux.py log over the whole session contains no update/version request.",
   "evidence": "tree of Settings > Appliance Updates (title, subtitle 'Check for, stage, and apply updates to the connected rig', single info panel). Source: packages/nightshade_app/lib/app.dart:204-206, packages/nightshade_updater/lib/src/widgets/update_manager_widget.dart:40-52, packages/nightshade_updater/lib/src/services/update_service.dart:236-239 ('Update server URL not configured'), and grep showing update_wiring.dart:79 as the sole configure() caller.",
   "where": "packages/nightshade_app/lib/screens/settings/widgets/update_settings.dart (appliance-only page) and the unconfigured desktop path above",
   "proposed_fix": "Configure the update service with the public release feed on desktop startup and give Settings > About a 'Software Update' block (version+build, channel, Check now, last checked). Until that exists, drop the 'updates itself through its own installer' claim.",
   "verdict": "CONFIRMED",
   "verdict_reasoning": "Live: Settings search 'update' returns exactly About and Appliance Updates. Appliance Updates renders only a title, the subtitle 'Check for, stage, and apply updates to the connected rig' and one info panel 'Connect to a remote appliance to manage its updates. The local desktop app updates itself through its own installer.' -- no controls. About renders version 6.1.0, tagline, three links and System Information; no update control, no build number. Code confirms the dead path at the cited lines: app.dart:204-206 wraps the desktop shell in UpdateManagerWidget; update_manager_widget.dart:39-43 _isUpdateConfigured() and :163-168 _checkForUpdates return early when updateProvider.state.updateServerUrl is empty; the only configure() caller outside the updater package is headless_api/update_wiring.dart:79, reached from desktop_app_bootstrap.dart:377, which builds a SEPARATE UpdateService for the headless API server and needs NIGHTSHADE_UPDATE_SERVER -- it never touches the Riverpod updateProvider the GUI widget reads.",
   "corrected_severity": "P2"
  },
  {
   "title": "The in-app Logs viewer records nothing about the failures the user just watched happen -- five failed catalog downloads and every rejected save are absent from it and from the 18.8 MB log export",
   "kind": "defect",
   "severity": "P2",
   "unit": "settings:replay-debug / screen:settings/log_viewer.dart",
   "what_happened": "Settings > Logs is the surface a user is told to use ('View application logs with filtering and export'). In a session where I triggered the GLADE+ download five times and watched it fail five times, searching the viewer for 'download' gives '0 of 139 entries / No entries match current filters' and searching for 'glade' gives 0 as well. The level filter reports the whole session as one error: Error+ = '1 of 139 entries', the single [ProfileActivationWriteThrough] 'Strict activation aborted: target profile 1 does not exist'. Not logged anywhere: the catalog download failure, the two rejected profile imports, the rejected profile saves, or the two later unhandled 'Cannot activate profile' exceptions that Flutter printed to stderr. I then pressed Export, which produced /home/scdouglas/Documents/nightshade_logs_2026-08-02T11-08-20.txt -- 18,829,614 bytes, 142,921 lines, concatenating the rotated on-disk logs back to 2026-07-27 -- and grepped it: 0 occurrences of 'glade' and 0 of 'annotation catalog download'. The failure genuinely has no persistent record.",
   "expected": "Every user-visible failure should leave a log line at WARN/ERROR with the cause (here: the VizieR TAP 503). Otherwise the log viewer gives a user and a support engineer false confidence -- 'only 1 error all night' -- about a session that failed repeatedly.",
   "repro": "Settings > Catalogs > Download Standard (~50 MB), which fails. Then Settings > Logs > search 'download' -> 0 of N entries. Set the level filter to Error+ -> 1 of N. Press Export and grep the resulting file for 'glade' -> 0 hits.",
   "evidence": "Tree readings '0 of 139 entries / No entries match current filters' for both 'download' and 'glade'; '1 of 139 entries' at Error+ with the single ProfileActivationWriteThrough row; grep -ic glade on the 18.8 MB export -> 0.",
   "where": "the catalog download handler in packages/nightshade_app/lib/screens/settings/catalog_settings_screen.dart (catches and snackbars without logging) and the equivalent swallow in the profile save/import paths",
   "proposed_fix": "Log every caught user-facing exception at ERROR with the underlying cause before showing the snackbar.",
   "verdict": "CONFIRMED",
   "verdict_reasoning": "Confirmed on this session's own failures, with one correction to the specifics. This run produced three user-visible 'Duplicate profile failed: Bad state: ...' snackbars, two rejected profile saves, one unhandled 'Cannot activate profile 6' exception and one successful 15.6 MB GLADE+ download. Settings > Logs then reported: 'duplicat' -> 0 of 124 entries; 'download' -> 0 of 124; 'glade' -> 0 of 124; 'catalog' -> 1; 'profile' -> 2. The only error-level line for the whole session is '[ProfileActivationWriteThrough] Strict activation aborted: target profile 6 does not exist' -- exactly the shape reported. I then pressed Export and grepped the 19,489,798-byte / 147,939-line result: 0 'glade', 0 'duplicat', 0 'annotation catalog'. Mechanism matches the cited location: catalog_settings_screen.dart:1229-1230 catches and calls _showError, which is only context.showErrorSnackBar (:414-418) with no logger call; the duplicate handler does the same at screen_shell.dart:394. Correction: the catalog download SUCCEEDED this time and still produced no log entry, so the gap is 'no catalog-download logging at all, success or failure', not only the failure path.",
   "corrected_severity": "P2"
  },
  {
   "title": "Log Export dumps 18.8 MB into the home Documents folder without warning, ignoring both the configured data directory and the app's own exports/ folder",
   "kind": "defect",
   "severity": "P3",
   "unit": "settings:replay-debug / screen:settings/log_viewer.dart",
   "what_happened": "Settings > Logs > Export wrote /home/scdouglas/Documents/nightshade_logs_2026-08-02T11-08-20.txt: 18,829,614 bytes, 142,921 lines, containing every rotated log file back to 2026-07-27 -- from a sandboxed instance launched with NIGHTSHADE_DATA_DIR=/tmp/ns-audit/r-settings-system/data. There is no file picker, no size estimate, and no confirmation; the only notice is the (invisible, see the snackbar finding) message 'Logs exported to: <path>'. It also does not use ~/Documents/Nightshade/exports/, which is where the app puts its other exports.",
   "expected": "Ask where to save (the app already uses a native save dialog for profile export), state the size, and default to the same exports/ directory as everything else. A user clicking Export to attach a log to a bug report does not expect an 18 MB file in the root of Documents.",
   "repro": "Settings > Logs > Export. ls -la ~/Documents/nightshade_logs_*.txt.",
   "evidence": "File listing above; head of the file shows '=== /home/scdouglas/.local/share/com.example.nightshade_desktop/logs/nightshade.log.2026-07-27 ==='; ~/Documents/Nightshade/exports/ contains only unrelated CSVs.",
   "where": "the export handler in packages/nightshade_app/lib/screens/settings/widgets/log_viewer.dart",
   "proposed_fix": "Use the same FilePicker save flow as profile export, default to the exports/ directory under the resolved data dir, and offer 'visible entries only' vs 'full history'.",
   "verdict": "CONFIRMED",
   "verdict_reasoning": "Reproduced exactly. Settings > Logs > Export, from an instance launched with NIGHTSHADE_DATA_DIR=/tmp/ns-audit/v2-settings-system/data, wrote /home/scdouglas/Documents/nightshade_logs_2026-08-03T17-11-30.txt -- 19,489,798 bytes, 147,939 lines. No file picker, no size estimate, no confirmation; the only notice is the line 'Logs exported to: <path>'. The file concatenates every rotated on-disk log back to 2026-07-28 (section headers '=== /home/scdouglas/.local/share/com.example.nightshade_desktop/logs/nightshade.log.2026-07-28 ===' through 2026-08-03, then '=== In-memory Dart log entries ==='), so it also reads the real user log dir rather than the sandbox. ~/Documents/Nightshade/exports/ holds only unrelated CSV/HTML session exports. Location is right: log_viewer.dart:241-276 _exportLogs, path built at :262 as '${docsDir.path}/nightshade_logs_$timestamp.txt' with no picker.",
   "corrected_severity": "P3"
  },
  {
   "title": "Replay & Debug is written for the developer who built it: a lowercase title sentence and two user-facing dialogs that name a database table",
   "kind": "ux",
   "severity": "P3",
   "unit": "settings:replay-debug / screen:settings/replay_debug_settings.dart",
   "what_happened": "The page header reads 'Replay Debug' with the subtitle 'decision-log retention + cleanup for the retrospective Replay screen.' -- lowercase first word, a '+' used as a conjunction. The logging toggle's help text says decisions are 'persisted to the sequence_decisions table', and the destructive confirmation says 'This deletes every row in the sequence_decisions table.' The confirm dialog also does not say how many rows it is about to delete (there were 3), and its 'Clear' action is rendered as plain blue text identical to 'Cancel', whereas the equivalent profile-delete dialog uses a filled red 'Delete' button.",
   "expected": "Sentence-cased prose that never names a SQL table; 'This will delete 3 recorded decisions' in the confirmation; and one consistent destructive-button style across dialogs.",
   "repro": "Settings > search 'replay' > Replay & Debug. Read the header. Press 'Clear all replay history'.",
   "evidence": "/tmp/ns-audit/shots/rss-77.png (page), /tmp/ns-audit/shots/rss-82.png (dialog footer: 'Cancel' and 'Clear' identically styled).",
   "where": "packages/nightshade_app/lib/screens/settings/widgets/replay_debug_settings.dart",
   "proposed_fix": "Rewrite the copy for users, add the row count to the confirmation, and use the shared destructive-button variant."
  },
  {
   "title": "Duplicate reports 'Duplicate profile failed' for a duplicate that succeeded, and leaks an internal invariant message to the user",
   "kind": "defect",
   "severity": "P2",
   "unit": "settings:equipment-profiles / screen:settings/profile_controls.dart",
   "what_happened": "Settings > Equipment Profiles > (profile) > Duplicate > Duplicate, accepting the pre-filled name. The app reported: 'Duplicate profile failed: Bad state: Profile duplication completed, but profile 5 was absent from the refreshed profile list.' The duplicate had in fact succeeded -- equipment_profiles gained row 5 and the profile list in the same screen shows it (three rows: 'Local Work Do Not Lose' [Active], 'Local Work Do Not Lose (Copy)', 'Local Work Do Not Lose (Copy)'). So the message is false in both halves: the operation did not fail, and profile 5 is not absent from the list. It reads like a post-write assertion racing the provider refresh, and the assertion text -- which names a row id and an internal list -- is shown verbatim to the user.",
   "expected": "Report success, or if the refresh really has not landed, retry/await it rather than declaring failure. A user who believes this message will press Duplicate again and end up with a third copy.",
   "repro": "Settings > Equipment Profiles > select a profile > overflow > Duplicate > press Duplicate. (I hit it on the second duplicate of the same source profile, i.e. when the proposed name collided with an existing profile.) sqlite3 <data>/nightshade.db 'select id,name from equipment_profiles' shows the new row; the list in the UI shows it too.",
   "evidence": "tree: 'panel: Duplicate profile failed: Bad state: Profile duplication completed, but profile 5 was absent from the refreshed profile list.' alongside DB rows 3,4,5 and a list rendering all three.",
   "where": "the duplicate handler in packages/nightshade_app/lib/screens/settings/equipment_profiles_screen_parts/profile_controls.dart and the profiles notifier's post-duplicate lookup",
   "proposed_fix": "Await the refreshed state (or look the row up by id in the DAO result) before asserting; never surface a Bad state message as user copy.",
   "verdict": "CONFIRMED",
   "verdict_reasoning": "Reproduced live, 3 times out of 5 duplicates of the same source profile. Duplicates 1 and 2 succeeded silently; 3, 4 and 5 each raised the snackbar 'Duplicate profile failed: Bad state: Profile duplication completed, but profile 4 / 5 / 6 was absent from the refreshed profile list.' while sqlite3 showed rows 4, 5 and 6 present and the list in the same screen rendered all of them (/tmp/ns-audit/shots/v2-31.png). Both halves of the message are false exactly as claimed. It is a post-write refresh race: screen_shell.dart:311-324 _awaitMutatedProfile awaits ref.read(equipmentProfilesProvider.future) after duplicateProfile's ref.invalidateSelf() and throws a StateError when the id is not yet in the returned list; that assertion text goes straight to showErrorSnackBar at :394.",
   "corrected_severity": "P2",
   "location_wrong": "Cited 'the duplicate handler in .../profile_controls.dart'. Both the handler and the message live in .../equipment_profiles_screen_parts/screen_shell.dart -- _duplicateProfile at :362-396, the throw in _awaitMutatedProfile at :311-324, the snackbar at :394. profile_controls.dart holds unrelated widgets (_EditableFilterChip, _DeviceChip)."
  },
  {
   "title": "Duplicate happily creates two profiles with byte-identical names, while Import de-duplicates -- so the profile list can show rows nothing can tell apart",
   "kind": "defect",
   "severity": "P3",
   "unit": "settings:equipment-profiles",
   "what_happened": "Duplicating the same profile twice and accepting the offered name both times produced ids 4 and 5, both named 'Local Work Do Not Lose (Copy)'. The list renders two identical rows (same title, same 'No description', same icon) with nothing to distinguish them, and the status-bar profile chip shows only a name, so once one of them is active you cannot tell which. The importer, by contrast, does resolve collisions -- importing a profile named 'Audit Rig Alpha' over an existing one produced 'Audit Rig Alpha (1)'. Two code paths, two different rules.",
   "expected": "One rule: reject or auto-suffix a colliding name everywhere. Duplicate should offer '(Copy 2)' the second time, exactly as Import offers '(1)'.",
   "repro": "Settings > Equipment Profiles > overflow > Duplicate > Duplicate (accept the pre-filled name). Repeat. sqlite3 'select id,name from equipment_profiles' -> two rows with the same name.",
   "evidence": "DB: 4|Local Work Do Not Lose (Copy) and 5|Local Work Do Not Lose (Copy); tree shows two identical list rows.",
   "where": "the duplicate handler's default name generation vs the importer's collision resolution",
   "proposed_fix": "Route both through one uniqueName(base) helper, and add a UNIQUE index on equipment_profiles.name if names are meant to be identifiers.",
   "verdict": "CONFIRMED",
   "verdict_reasoning": "Reproduced live. Duplicating 'Adv Rig One' five times, accepting the pre-filled name each time, produced ids 2,3,4,5,6 all named exactly 'Adv Rig One (Copy)' (sqlite3), rendered as five visually identical rows with the same icon and no subtitle (/tmp/ns-audit/shots/v2-31.png). The offered name is a fixed '${profile.name} (Copy)' built at screen_shell.dart:368 with no collision check, whereas the importer resolves collisions with a '(n)' suffix. P3 is fair -- usability/consistency, not data loss.",
   "corrected_severity": "P3"
  },
  {
   "title": "Enter does not submit the profile dialogs, so the fastest path through them is mouse-only",
   "kind": "ux",
   "severity": "P3",
   "unit": "settings:equipment-profiles",
   "what_happened": "In the Duplicate Profile dialog the name field is focused and pre-filled; pressing Enter does nothing -- the dialog stays open and no profile is created (DB unchanged). You must move to the mouse and click Duplicate.",
   "expected": "Enter submits, Escape cancels. That is the convention in every dialog of this shape, and these dialogs are one-field.",
   "repro": "Settings > Equipment Profiles > overflow > Duplicate > press Return. Nothing happens; the dialog is still up.",
   "evidence": "After xdotool key Return the tree still shows 'panel: Duplicate Profile / button: Cancel / button: Duplicate' and equipment_profiles is unchanged.",
   "where": "packages/nightshade_app/lib/screens/settings/equipment_profiles_screen_parts/profile_controls.dart dialog builders",
   "proposed_fix": "Give the TextField an onSubmitted that fires the primary action."
  },
  {
   "title": "The 'default startup profile' that Settings > General promises has no UI anywhere -- it is pinned to whichever profile you created first",
   "kind": "defect",
   "severity": "P2",
   "unit": "settings:equipment-profiles",
   "what_happened": "Settings > General > Auto-connect equipment reads 'Connects the default startup profile on launch, falling back to the last active profile when no default is set'. Equipment Profiles has no notion of a default: the badge says 'Active', the header action says 'Set Active', and the overflow menu is Edit / Duplicate / Export / Delete. Nothing shows or sets a default. In the database equipment_profiles.is_default is set on whichever profile was created first and never moves: after I created a profile (is_default=1), duplicated it twice (both is_default=0) and pressed Set Active on a duplicate, the rows read 3|...|is_active=0|is_default=1, 4|...|is_active=1|is_default=0. So the rig the app says it will connect on launch is not the rig the operator just made active, and there is no screen where that can be seen, let alone changed.",
   "expected": "Either show and let the user set the default (a 'Set as default startup profile' item in the overflow, and a 'Default' badge in the list), or drop the concept and make auto-connect use the active profile -- which is what the operator will assume.",
   "repro": "Settings > Equipment Profiles > create a profile, duplicate it, select the copy and press Set Active. sqlite3 <data>/nightshade.db 'select id,name,is_active,is_default from equipment_profiles' -> is_default is still on the original. Search all of Settings for 'default profile': the only hit is the General page's description text.",
   "evidence": "DB rows quoted above; Equipment Profiles tree showing only 'Active' / 'Set Active' and the four-item overflow menu; General settings copy captured in the tree.",
   "where": "packages/nightshade_app/lib/screens/settings/equipment_profiles_screen_parts/ (no default affordance) vs the auto-connect description in the general settings section",
   "proposed_fix": "Add a 'Set as default' action and a Default badge, and show which profile auto-connect will use on the General page.",
   "verdict": "CONFIRMED",
   "verdict_reasoning": "All three parts reproduced live. (1) Settings > General still reads 'Auto-connect equipment / Connects the default startup profile on launch, falling back to the last active profile when no default is set'. (2) Equipment Profiles exposes no default affordance: badge 'Active', header action 'Set Active', overflow exactly Edit / Duplicate / Export / Delete (/tmp/ns-audit/shots/v2-21.png). (3) The flag does not follow activation: after creating profile 1 (is_active=1, is_default=1), duplicating it five times (all is_default=0) and pressing Set Active on profile 2, sqlite3 shows 1|is_active=0|is_default=1 and 2|is_active=1|is_default=0. So the rig the copy says will be connected on launch is not the one just activated, and no screen can see or change it. (Separate observation, not part of this claim: grep finds no consumer of autoConnectEquipment in any startup connect path at all.)",
   "corrected_severity": "P2"
  }
 ]
}