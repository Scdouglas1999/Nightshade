{
  "key": "w4-widgets",
  "shared_file_suites": "Both packages I touched were run whole, with the golden tag excluded exactly as melos.yaml:318 (`flutter test --exclude-tags golden`) does — a plain `flutter test` also runs the host-specific pixel baselines, which fail on Linux by design (imaging_landscape_capture_test.dart's own header says 'Baselines are host-specific'). nightshade_core: +5391 ~4 -8; the 8 are weather_safety_nothing_to_safe_test.dart (2) and nina_sequence_fidelity_test.dart (6). nightshade_app: +2763 -3; the 3 are pre_session_simulation_settings_test.dart, settings_section_index_test.dart and weather_safety_sources_disclosure_test.dart. None of the 11 import first_light, connection_diagnostic or the imaging abort path. They are concurrent-agent churn: during this session nightshade_core repeatedly failed to COMPILE from other agents' in-flight edits (sessions_dao.dart 'capturedImages' undefined, weather_safety_provider.dart '_hasAnythingToSafe' undefined, sequence_executor.dart 'targetsDaoProvider' undefined), and the core failure set changed between two consecutive full runs (node_duration_provider_test and transient_alert_persistence_test failed in the first and passed in the second with no edit from me).",
  "fixes": [
    {
      "title": "Connection troubleshooter said it 'couldn't pin down the exact cause' while holding an error that named the exact cause, then gave four irrelevant hardware steps",
      "outcome": "fixed",
      "summary": "Confirmed the verifier's location correction: the dialog only renders; the classifier and knowledge base are packages/nightshade_core/lib/src/models/troubleshooter/connection_diagnostic.dart. The built-in guider's preflight messages (all six start 'Built-in guider requires ...', native/nightshade_native/bridge/src/builtin_guider.rs:1265-1330) matched no marker table, so _classify fell through to DiagnosticCategory.unknown -> headline \"The connection didn't complete\", body 'we couldn't pin down the exact cause', and _unknownSteps' four hardware steps: reseat the cable, confirm power, close other programs, restart Nightshade. The built-in guider is SOFTWARE - it reuses the imaging camera and pulses the mount - so it has no cable and no power switch; those steps cannot work and the operator concludes the app is broken. Added a first-party preflight marker list checked BEFORE every driver-shaped marker (our own setup checks are never a cable, a COM registration or a firewall), routing to DiagnosticCategory.configuration; threaded the normalized error into _headlineFor/_plainLanguageFor/_stepsFor so the configuration branch can name the actual value. New copy: headline 'Your equipment profile is missing a value', body explaining the guider is software with no cable to check, and steps keyed on which value the check named - focal length -> 'Open Equipment -> your active profile -> Edit' + 'Enter the Focal Length of the scope you are guiding with' (mm) + 'Save the profile, then connect the guider again'; pixel size -> the imaging camera reported 0; binning -> guide binning; connected camera/mount -> assign and connect both in the active profile.",
      "files_changed": [
        "packages/nightshade_core/lib/src/models/troubleshooter/connection_diagnostic.dart",
        "packages/nightshade_core/test/models/connection_diagnostic_test.dart",
        "packages/nightshade_app/test/widgets/troubleshooter/connection_troubleshooter_dialog_test.dart"
      ],
      "test": "Two levels. RENDERED: packages/nightshade_app/test/widgets/troubleshooter/connection_troubleshooter_dialog_test.dart - 'the built-in guider preflight renders the setting, not a cable' opens the real dialog with the verbatim focal_length_mm=0 message and asserts the screen shows no \"couldn't pin down\", no 'Reseat the cable', no 'Restart Nightshade', and does show 'Focal Length' (7/7 pass). MODEL: packages/nightshade_core/test/models/connection_diagnostic_test.dart - new group 'built-in guider preflight', 5 tests: the focal_length_mm=0 message classifies as configuration and the body no longer contains \"couldn't pin down\"; the STEPS contain 'focal length' and contain neither 'cable' nor 'power' nor 'restart nightshade' (asserted against a new _stepsText helper, because the explanation deliberately says there is no cable); the sibling 'requires an active profile with a guide focal length' variant classifies the same; the 'requires a connected camera in the active profile' variant names the profile and no cable; and the anti-inversion case - a real ASCOM guider 0x800706ba still gets the driver playbook, so the new rule cannot swallow genuine hardware failures on the same device type. 44/44 pass.",
      "revert_check": "Deleted the three-line `if (_containsAny(error, _firstPartySetupMarkers)) return DiagnosticCategory.configuration;` hook from _classify (the production call site; the copy branches stay compiled but unreachable). nightshade_core flutter test test/models/connection_diagnostic_test.dart -> +40 -4, all four behavioural tests failed; nightshade_app flutter test test/widgets/troubleshooter/connection_troubleshooter_dialog_test.dart -> +6 -1, the rendered-dialog test failed. Restored -> 44/44 and 7/7 pass."
    },
    {
      "title": "Closing the First Light dialog mid-capture orphaned the exposure, and there was no cancel inside the dialog",
      "outcome": "fixed",
      "summary": "Four parts, because the defect had four. (1) There was NO cancel path at all: FirstLightOrchestrator.run and firstLightControllerProvider had only reset(), which clears the UI state while the sensor keeps exposing. Added FirstLightOrchestrator.cancel() -> imagingServiceProvider.cancelExposure() (the same seam the exposure was started through; it resolves the pending exposure completer AND dispatches the driver abort) plus a per-run _cancelRequested latch checked after each stage, so a cancel during stretch/solve/annotate stops there too. A cancel unwinds via a dedicated _FirstLightCancelled to IDLE, not to `failed` — an operator-requested stop is not a pipeline failure, and the capture path would otherwise have reported 'Capture failed: the camera returned no image'. The latch is cleared at the top of run() so it cannot leak into the next run. (2) FirstLightController.cancel() delegates, guarded on the in-flight flag. (3) The dialog's running phases now render a 'Stop capture' footer action wired to that cancel; previously _actionsFor returned null for all four. (4) The header ✕ was the one hole PopScope did not cover — NightshadeDialog calls Navigator.pop directly — so the dialog now passes closeEnabled: !state.isRunning. Separately, the verifier proved the Imaging viewer's 'Abort capture' X was genuinely dead for this path: imaging_preview_toolbar decides to SHOW it from the GLOBAL exposureProgressProvider, while _abortCapture early-returned on `if (!_isSingleCapture && !_isLooping) return;` — screen-local flags a first-light (or centering, or sequencer) exposure never sets. The abort now always targets the shared imaging service; the local _isStoppingCapture latch is set only when this screen owns the capture, because that flag is cleared in the capture methods' finally, which never runs for a foreign exposure and would have hidden the button for the rest of the session (relocating the defect).",
      "files_changed": [
        "packages/nightshade_core/lib/src/services/first_light/first_light_orchestrator.dart",
        "packages/nightshade_core/lib/src/providers/first_light_provider.dart",
        "packages/nightshade_app/lib/widgets/first_light/first_light_flow_dialog.dart",
        "packages/nightshade_app/lib/screens/imaging/imaging_screen/imaging_screen_actions.dart",
        "packages/nightshade_core/test/services/first_light_cancel_test.dart",
        "packages/nightshade_app/test/widgets/first_light/first_light_flow_dialog_test.dart",
        "packages/nightshade_app/test/screens/imaging/imaging_abort_foreign_exposure_test.dart"
      ],
      "test": "Three files. (a) nightshade_core/test/services/first_light_cancel_test.dart — 3 tests against a stalled imaging service: cancel raises cancelExposure exactly once AND the flow ends at FirstLightPhase.idle with no errorMessage; a cancel with no run in flight aborts nothing; and a cancel does not leak into the next run (the second run must still reach `failed`, which a stale latch would swallow). (b) nightshade_app/test/widgets/first_light/first_light_flow_dialog_test.dart — 3 tests: the running phase's NightshadeDialog has closeEnabled false, its ✕ IconButton has onPressed null, and TAPPING it leaves FirstLightFlowDialog mounted; the running phase's 'Stop capture' button calls controller.cancel(); and a terminal phase is still closable with no Stop button (anti-inversion — the gate must not strand the operator in a finished dialog). (c) nightshade_app/test/screens/imaging/imaging_abort_foreign_exposure_test.dart — pumps the real ImagingScreen, arms exposureProgressProvider WITHOUT the screen starting a capture, asserts the abort button is offered, that tapping it reaches cancelExposure, that the button is still offered afterwards (no permanent latch), and that it disappears when the progress really ends. 16/16 first_light + 2/2 imaging-abort pass.",
      "caveats": "Two honest limits. (1) The cancel is immediate for the EXPOSURE stage (cancelExposure resolves the pending completer and dispatches the driver abort). During stretch/solve/annotate the latch only takes effect at the next checkpoint, so a hung plate solve keeps the dialog up until it returns or times out. That is not a new stuck-ness: PopScope(canPop: !state.isRunning) already blocked Escape and the barrier during those stages before this change, and there is now an exit where there was none. (2) The imaging-toolbar abort now genuinely stops a sequencer or centering exposure too, not just a first-light one. That is a real behavioural expansion — the button previously did nothing for those — and it is what the button's own label and tooltip promise, but it was verified in a widget test with a fake service, not against a live sequencer run.",
      "revert_check": "Three separate reverts, each at the production call site. (1) Deleted `_ref.read(imagingServiceProvider).cancelExposure();` from FirstLightOrchestrator.cancel — flutter test test/services/first_light_cancel_test.dart -> +1 -2 ('cancel aborts the exposure at the camera and unwinds to idle' and 'a cancel does not leak into the next run' both failed). (2) Deleted `closeEnabled: !state.isRunning,` from the dialog -> 'running phase disables the header close button' failed (+14 -1). (3) Replaced the running-phase `return [NightshadeButton(label: 'Stop capture' ...)]` with `return null` -> 'running phase offers a stop that cancels at the camera' failed (+14 -1). (4) Re-inserted `if (!_isSingleCapture && !_isLooping) return;` at the top of _abortCapture -> 'the abort reaches the camera for an exposure this screen did not start' failed. All restored; 16/16 + 2/2 green."
    },
    {
      "title": "Readiness panel reported a green 'Profile devices — every device is connected' over an empty profile with nothing connected",
      "outcome": "already-correct",
      "summary": "Closed by wave 3 (reports/coverage/fixes/w3-widgets.json) AFTER the verifier's run; re-verified independently in the current tree rather than taken on trust. packages/nightshade_core/lib/src/models/readiness/readiness_models.dart:_buildProfileDevices now opens with three explicit empty-set branches before the offlineProfileDevices.isEmpty pass: !hasActiveProfile -> 'No equipment profile is active, so there are no device assignments to check.'; assignedProfileDeviceCount == 0 -> ReadinessLevel.caution 'No devices are assigned to this profile, so nothing connects automatically.' with a Fix route to /equipment. The inputs are genuinely computed, not defaulted — readiness_provider.dart:133-135 passes hasActiveProfile: ref.watch(activeEquipmentProfileProvider) != null, assignedProfileDeviceCount: assignedProfileSlots.length, otherAssignedProfileDeviceCount: otherAssignedProfileSlots.length, and the three parameters are required so a future caller cannot silently reintroduce the vacuous pass.",
      "files_changed": [],
      "test": "packages/nightshade_app/test/widgets/readiness/ (equipment_readiness_panel_test.dart, readiness_dismissal_count_test.dart, readiness_status_chip_test.dart) — green in this session as part of a 51-test verification run.",
      "revert_check": "n/a — no change made this wave; verified by reading the production branch and the provider that feeds it."
    },
    {
      "title": "Dismissing a blocking readiness item hid it while the summary kept counting it",
      "outcome": "already-correct",
      "summary": "Closed by wave 3; re-verified. Both halves the verifier named are present. readiness_panel.dart:175-185 — the dismissal filter keeps `i.level == ReadinessLevel.blocked || !dismissed.contains(i.id)`, so a blocker is listed even if its id is already in the session set, and the ✕ is not rendered on a blocked row. equipment_readiness_panel.dart:_subtitle subtracts the dismissed caution rows from the number it quotes and appends ' (N dismissed)'. Count and visible rows now agree.",
      "files_changed": [],
      "test": "packages/nightshade_app/test/widgets/readiness/readiness_dismissal_count_test.dart — green.",
      "revert_check": "n/a — no change made this wave."
    },
    {
      "title": "'Disconnect All' stayed enabled and silently no-opped when nothing was connected",
      "outcome": "already-correct",
      "summary": "Closed by wave 3; re-verified. equipment_status_indicator.dart's Disconnect All PopupMenuItem now carries `enabled: canDisconnect` and greys its own icon/label, and canDisconnect reads the SAME target list the sweep walks — equipmentDisconnectTargets(ref).any((t) => !equipmentDisconnectShouldSkip(t.connectionState)) — rather than the chip's six-slot counts, so a connected dome cannot grey out a button that still has work to do.",
      "files_changed": [],
      "test": "packages/nightshade_app/test/widgets/equipment_status_indicator_test.dart — green (includes the dome-only anti-inversion case).",
      "revert_check": "n/a — no change made this wave."
    },
    {
      "title": "Guiding-control buttons were invisible to assistive tech (role 'panel', never DISABLED)",
      "outcome": "already-correct",
      "summary": "Closed by wave 3; re-verified. guide_controls_panel.dart:_buildControlButton wraps the control in Semantics(container: true, button: true, enabled: !isDisabled, label: label, onTap: ..., excludeSemantics: true), which covers all six controls the audit named plus Stop/Resume because they share the one builder.",
      "files_changed": [],
      "test": "packages/nightshade_app/test/widgets/phd2/guide_controls_panel_semantics_test.dart — green.",
      "revert_check": "n/a — no change made this wave."
    },
    {
      "title": "Weather status card rendered an epoch-zero timestamp as 'Updated 20667 days ago'",
      "outcome": "already-correct",
      "summary": "Closed by wave 3; re-verified at the cause the verifier identified rather than in the card. weather_providers.dart:584 is now `lastUpdate: lastFetchedAt` with the sentinel `?? DateTime.fromMillisecondsSinceEpoch(0)` gone and WeatherStatus.lastUpdate made nullable, so 'no fetch has landed' is representable and the card's existing null guard can actually fire.",
      "files_changed": [],
      "test": "packages/nightshade_app/test/widgets/weather/weather_status_card_last_update_test.dart — green.",
      "revert_check": "n/a — no change made this wave."
    },
    {
      "title": "The Slew split-button was invisible to assistive tech — no node at all",
      "outcome": "already-correct",
      "summary": "Closed by wave 3; re-verified. slew_dropdown_button.dart:124 wraps the PopupMenuButton in Semantics(button: true, enabled: enabled, label: visibleLabel, hint: 'Opens slew options'); the annotations merge into the PopupMenuButton's existing InkWell node, so the result is one named, enabled, tappable node rather than the previous unnamed one. The inner ExcludeSemantics stays, which is correct — removing it would publish a second, unactionable 'Slew' node.",
      "files_changed": [],
      "test": "packages/nightshade_app/test/widgets/slew_dropdown_button_test.dart — green.",
      "revert_check": "n/a — no change made this wave."
    },
    {
      "title": "Dashboard Glance-mode toggle reported itself as DISABLED to assistive tech while working normally",
      "outcome": "already-correct",
      "summary": "Closed by wave 3; re-verified. glance_mode_toggle.dart's Semantics node now carries `enabled: !_saving` alongside `toggled: enabled`, so the node publishes ENABLED as well as CHECKABLE and only the in-flight save reports it disabled.",
      "files_changed": [],
      "test": "packages/nightshade_app/test/screens/dashboard/widgets/glance_mode_toggle_test.dart — green.",
      "revert_check": "n/a — no change made this wave."
    },
    {
      "title": "Four widgets exported from the app barrel with zero call sites — dead UI shipping in the binary",
      "outcome": "already-correct",
      "summary": "Closed by wave 3; re-verified. widgets/object_info_panel.dart, widgets/tour_selection_sheet.dart and widgets/sequence_controls.dart no longer exist on disk and their barrel exports are gone. The fourth, sequence/variable_picker.dart, was WIRED rather than deleted: VariablePickerButton is now the suffixIcon of the live-stacking Watermark template field, the one field whose value is interpolated at render time.",
      "files_changed": [],
      "test": "packages/nightshade_app/test/widgets/sequence/variable_picker_wiring_test.dart — green.",
      "revert_check": "n/a — no change made this wave."
    },
    {
      "title": "The NIGHT STORY timeline was hidden whenever the session had no science products, even when narrator events exist",
      "outcome": "already-correct",
      "summary": "Closed by wave 3 (reports/coverage/fixes/w3-s-app-shared.json) AFTER this verifier's run, which is why it still carried prior_attempt 'blocked'. Re-verified: science_analytics_tab.dart's allEmpty early-return branch now renders the _SectionHeading('NIGHT STORY') + NightStoryTimeline(sessionId: activeSessionId) between the solve-rate card and the ladder, unconditionally. Rendered unconditionally rather than by widening the allEmpty predicate with the narrator feed — the narrator is a session-narrative surface, not a science product, and the timeline owns its own empty state.",
      "files_changed": [],
      "test": "packages/nightshade_app/test/screens/analytics/widgets/science_tab_night_story_test.dart — green (pumps the real tab with every science provider empty and one narrator event).",
      "revert_check": "n/a — no change made this wave."
    }
  ]
}
