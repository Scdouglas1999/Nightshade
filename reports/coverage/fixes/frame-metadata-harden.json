{
  "fix": "frame-metadata-harden",
  "title": "Close the adversarial verifier's gaps in the frame-metadata fix: sever-proof tests for every hop, an explicit AIRMASS fallback, a deliberate exposure-duration decision, and a bounded residual second source",
  "status": "done",
  "date": "2026-08-02",
  "outcome": "closed",
  "premise": "The verifier confirmed the fix WORKS but found three of its four hops had no test at all (severing each left the whole suite green), plus two unflagged behaviour risks. Everything below was re-measured here, not taken on faith.",

  "rule": "A test counts only if it FAILS when the named PRODUCTION line is severed. Every drill below was run: sever -> run -> observe failure -> restore -> observe pass.",

  "sever_drills": [
    {
      "id": "A1",
      "hop": "typed FRB payload -> event data map (Dart)",
      "line_severed": "packages/nightshade_core/lib/src/backend/ffi_backend/event_mapping.dart:558 and :576 — deleted `...frameCaptureToEventData(sequencerEvent.capture),` from BOTH the FrameAccepted and FrameRejected arms",
      "test": "packages/nightshade_core/test/backend/ffi_frame_event_capture_wiring_test.dart (2 tests)",
      "failed_when_severed": true,
      "failure": "both tests: `the frame event lost the \"gain\" key, so captured_images.gain goes back to NULL for every sequenced frame`",
      "seam": "Production code UNCHANGED. The mapper is a private extension method on a private class, so a `@visibleForTesting sequencerEventInfoForTesting` seam was added to FfiBackend that calls the real `_extractSequencerEventInfo` — same pattern as the existing `profileFromBridgeForTesting`. The test drives the actual mapper, not a copy.",
      "anti_tautology": "The expected key/value table is written out literally in the test rather than derived from `frameCaptureToEventData`, so the mapper cannot be compared against itself."
    },
    {
      "id": "A2",
      "hop": "FITS writer's FrameContext -> the frame event (Rust sequencer)",
      "line_severed": "native/nightshade_native/sequencer/src/instructions.rs:2536 — replaced the `&frame_ctx` argument to `emit_grade_progress` with `&crate::scheduling::FrameContext::default()`",
      "test": "native/nightshade_native/sequencer/src/instructions.rs::tests::frame_event_is_stamped_from_the_context_save_fits_received",
      "failed_when_severed": true,
      "failure": "`frame 1 event carries a different capture than the FITS writer got` — left all-None/0, right the real telemetry (gain 139, RA 5.5h, focuser 31705, ...)",
      "note": "CONFIRMED the verifier's complaint in passing: the pre-existing `frame_event_carries_the_fits_writers_own_capture_context` stayed GREEN under this same sever, because it calls `emit_grade_progress` directly with a hand-made context and derives both sides of its comparison from that one literal.",
      "method": "Drives the REAL call site. `execute_exposure` runs a 2-frame Dark burst against a ScriptedDomeRotatorOps extended to RECORD every FrameContext handed to `save_fits`, plus scripted telemetry (mount coords, pier side, cooler power, focuser position/temp, gain/offset/sensor temp). The test compares what the FITS writer received against what the event carried, frame by frame, then pins the individual values so an all-default context cannot satisfy the equality if both hops were severed together."
    },
    {
      "id": "A3",
      "hop": "ProgressDetail -> typed SequencerEvent (Rust bridge)",
      "line_severed": "native/nightshade_native/bridge/src/api/sequencer.rs:976 and :1026 — replaced `capture: capture.into()` with `capture: Default::default()` in BOTH arms",
      "test": "native/nightshade_native/bridge/src/api/sequencer.rs::frame_event_capture_tests (2 tests)",
      "failed_when_severed": true,
      "failure": "both tests: left all-None/0 FrameCaptureMetadata, right the populated payload"
    },
    {
      "id": "A3b",
      "hop": "save_fits's own frame_ctx -> the FITS header (the fourth hop, flagged as 'same shape')",
      "line_severed": "native/nightshade_native/bridge/src/sequencer_ops.rs:988 — `build_rich_header(image_data, frame_ctx, pointing)` with the frame_ctx argument replaced by `&FrameContext::default()`",
      "test": "native/nightshade_native/bridge/src/sequencer_ops.rs::pointing_tests::save_fits_writes_the_header_from_its_own_frame_context",
      "failed_when_severed": true,
      "failure": "GAIN card read back off disk was Some(100) (the ImageData fallback) instead of Some(139) (the context)",
      "method": "Drives `DeviceOps::save_fits` itself on a real BridgeDeviceOps (AppState::new() is in-memory, no DB needed), writes a FITS file and reads it back. Every prior test in this module called `build_rich_header` directly, which is exactly why this hop was invisible."
    },
    {
      "id": "B",
      "hop": "AIRMASS fallback when the sequencer has no site but app settings does",
      "line_severed": "native/nightshade_native/bridge/src/sequencer_ops.rs — (1) the `context_altitude_pointing(frame_ctx, self.get_observer_location(), Utc::now())` call in save_fits replaced with `None`; (2) separately, `header.altitude = frame_ctx.mount_altitude_deg.or_else(|| pointing.and_then(|p| p.altitude_deg))` reduced back to `frame_ctx.mount_altitude_deg`",
      "test": "pointing_tests::save_fits_writes_the_header_from_its_own_frame_context + context_pointing_without_altitude_resolves_it_from_app_settings + context_altitude_wins_and_no_site_stays_absent",
      "failed_when_severed": true,
      "failure": "sever (1): `a sequenced frame must keep AIRMASS when app settings knows the site, even though the sequencer's own location seed did not`. sever (2): the same, plus `left: None, right: Some(55.47)` on the altitude assertion."
    },
    {
      "id": "C",
      "hop": "driver-reported exposure vs the commanded exposure",
      "line_severed": "native/nightshade_native/sequencer/src/instructions.rs — both directions: (1) dropped the upper bound (`reported_secs > 0.0 && reported_secs <= longest_believable` -> `reported_secs > 0.0`); (2) never adopted the driver value at all (`if false`)",
      "test": "instructions.rs::tests::nonsense_driver_exposure_report_cannot_inflate_integration_totals + plausible_driver_exposure_report_wins_over_the_commanded_value",
      "failed_when_severed": true,
      "failure": "sever (1): `an impossible exposure report must not reach the FITS header` — left 3600.0, right 60.0. sever (2): left 60.0, right 60.4.",
      "note": "Both directions are pinned, so neither the trust nor the bound can be silently removed."
    },
    {
      "id": "D",
      "hop": "the residual `capture.X ?? attribution.X` second source",
      "line_severed": "packages/nightshade_core/lib/src/providers/sequence/sequence_executor/event_operations.dart:692,759,765-768 — both directions: (1) dropped the capture side (`attribution.X` only); (2) dropped the attribution side (`capture.X` only)",
      "test": "packages/nightshade_core/test/providers/sequence/sequence_frame_capture_metadata_test.dart — 'capture payload beats a loaded sequence that disagrees' + 'legacy host with no capture payload falls back to the tree'",
      "failed_when_severed": true,
      "failure": "sever (1): row.gain was 0 (the node config) instead of 139 (the camera). sever (2): row.gain was null instead of the tree's 0.",
      "note": "The two tests bracket the fallback: it may never contribute when a payload is present, and it must contribute when one is absent."
    }
  ],

  "airmass": {
    "verdict": "REAL regression, now closed and tested",
    "cause": "save_fits skips read_mount_pointing whenever frame_ctx.mount_ra_hours is Some — correct, that read is the second source the fix removed — but header.altitude then came only from frame_ctx.mount_altitude_deg. The sequencer derives that alt/az ONLY when its own ExecutionContext.latitude/longitude were seeded, while the FITS writer's site comes from app settings' get_observer_location(). A run whose executor never received a location therefore had pointing but no altitude, and silently lost AIRMASS.",
    "fix": "New `context_altitude_pointing(frame_ctx, observer, when)` fills in the altitude from the CONTEXT'S OWN ra/dec plus the app-settings site. Not a second mount read — only the derived geometry is added, so the header and the row still describe one instant. `build_rich_header` now takes the altitude as `frame_ctx.mount_altitude_deg.or_else(pointing altitude)`. Returns None (AIRMASS stays absent) when the context already has an altitude, has no pointing, or no site is configured anywhere — never computed from a guessed site.",
    "refactor": "`calculate_altitude` (a DeviceOps trait method that read Utc::now() internally) now delegates to a free `altitude_degrees(..., when)`, so the fallback is testable without a clock or a device and both paths use one piece of geometry."
  },

  "exposure_duration_decision": {
    "decision": "KEEP the driver's self-reported exposure, but BOUND it.",
    "why_driver": "FITS EXPTIME means the exposure that happened, not the one that was asked for, and `build_rich_header` has ALWAYS read it straight off ImageData — so switching to the commanded value would change what lands in every FITS file, a larger unannounced change than the one flagged. On a camera with a mechanical shutter the two genuinely differ, and the measured value is what a later calibration match wants.",
    "why_bounded": "The same field is now also the NOT NULL captured_images.exposure_duration that every integration total sums. One lying driver could corrupt a season of totals with nothing downstream able to tell.",
    "bound": "Accept when `0 < reported <= commanded * 1.05 + 1.0`. The shutter cannot have been open materially longer than the sequencer waited for it, so an over-report is not a measurement but a fault; the commanded value is kept and a WARN names the driver's number and the inflation factor. The 1.0s + 5% grace covers shutter/readout latency and drivers that quantise their exposure clock coarsely on long subs.",
    "one_sided_on_purpose": "A report SHORTER than commanded is physically possible (an aborted or truncated exposure really did end early) and can only under-count integration — the safe direction, and visible as an obviously short sub rather than as a night claiming hours it never collected. Only the impossible direction is rejected.",
    "surface": "Applied in build_frame_context_for_save, so the bounded value is the single one both the FITS header and the DB row are stamped from. Splitting them would have re-created the two-sources defect this whole path removed."
  },

  "second_source": {
    "verdict": "CAN still fire, but on exactly one path — kept, with the case named in a comment.",
    "ffi_path": "DEAD BY CONSTRUCTION. Native fills FrameContext.gain from `image_data.gain.or(config.gain)`, and `config.gain` IS the producing node's configured gain — the same value resolveFrameAttribution reads from the tree. A null capture gain therefore implies a null node gain, so the fallback can never supply a value the capture did not already have. Same for offset. binX/binY/frameType/exposureSecs are non-Option in the Rust struct, so they are always present on FFI.",
    "live_path": "A paired client talking to an appliance older than the capture payload (a phone that auto-updated against a Pi that did not). NetworkBackend decodes the event's `data` map verbatim from JSON, and SequenceExecutor's handler is backend-agnostic, so that host's frame event arrives with none of the capture keys and the tree walk is all there is. Losing it would put NULL gain/offset and a 1x1 binning on 4x4 frames, breaking master-dark matching for that whole session.",
    "also_noted_in_comment": "Attribution is the WEAKER source even where it overlaps: for a SmartExposure burst the tree walk picks a plan by matching filter NAME, which can select the wrong plan, while the native value came off the exposure that actually ran. targetId is deliberately NOT one of these fallbacks — attribution is its only source, because it is the targets.id primary key only the tree knows."
  },

  "verification": {
    "cargo_test": "nightshade_sequencer + nightshade_bridge: 440 + 706 + 5 + 4 + 12 passed, 0 failed (was 435 + 703; +5 new)",
    "flutter_test_core": "5208 passed, 0 failed, 4 skipped (was 5204; +4 new)",
    "cargo_fmt": "clean across the whole native workspace",
    "dart_analyze": "nightshade_core lib+test: 0 errors (7 pre-existing infos/warnings, none in touched files). nightshade_app lib: No issues found.",
    "dart_format": "clean on all four touched Dart files"
  },

  "files_changed": [
    "native/nightshade_native/sequencer/src/instructions.rs",
    "native/nightshade_native/bridge/src/sequencer_ops.rs",
    "native/nightshade_native/bridge/src/api/sequencer.rs",
    "packages/nightshade_core/lib/src/backend/ffi_backend.dart",
    "packages/nightshade_core/lib/src/providers/sequence/sequence_executor/event_operations.dart",
    "packages/nightshade_core/test/backend/ffi_frame_event_capture_wiring_test.dart",
    "packages/nightshade_core/test/providers/sequence/sequence_frame_capture_metadata_test.dart"
  ],

  "still_open": [
    "Not validated on hardware. Every telemetry read is exercised against scripted/Null device doubles; the mount/pier/cooler reads and a real driver's LastExposureDuration have not been driven against a physical rig.",
    "The exposure-duration grace (5% + 1.0s) is a judgement call, not a measurement. No driver in the wild has been sampled to confirm the band is wide enough for a slow mechanical shutter; a real over-report now surfaces as a WARN rather than silently, which is how it would be discovered.",
    "The AIRMASS fallback recomputes the altitude at SAVE time rather than capture time. For a sequenced frame that is at most a couple of seconds late, and the sequencer's own capture-time value still wins whenever it exists."
  ]
}
