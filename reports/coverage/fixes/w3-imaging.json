{
  "key": "imaging",
  "area": "Imaging / Framing / Guiding screens (packages/nightshade_app/lib/screens/{imaging,framing,guiding}/**)",
  "input_triage": {
    "items_in_findings_file": 8,
    "fixed_here": 7,
    "already_fixed_by_another_agent": 1,
    "note": "Item 8 (FITS omits XPIXSZ/YPIXSZ) was already closed by the s-imaging-service agent (see reports/coverage/fixes/w3-s-imaging-service.json, persistence.dart now reads the pitch from equipmentCameraCapabilitiesProvider); re-verified present, not re-done. Item 7 (onboarding subfolder promise) named no file in my ownership list, but a scan of every other fix3 findings file shows NO other agent holds it, so I fixed it rather than leave it. The 12 pre-existing golden failures in these four suites are NOT mine: with every one of my visual changes mechanically neutralised the pixel diffs are byte-identical (e.g. onboarding_android_landscape_932x430 = 33.71% / 135107px either way) - this is the known Windows-captured-goldens-on-Linux debt."
  },
  "incident": {
    "what": "I ran `git stash` by accident in a throwaway bash command, which reset the whole repo's uncommitted campaign work (460 files) to HEAD while other agents were mid-write. `git stash pop` did NOT restore it (only 10 files came back).",
    "recovery": "Backed up the 10 files other agents had touched during the ~2 min window, checked those back to HEAD, ran `git stash apply` (restoring all 460), then 3-way merged (`git merge-file` with HEAD as base) each of the 10 so both the campaign work and the concurrent agents' newest edits survive. Verified: the working tree now differs from stash@{0} in exactly the 4 files that gained newer agent content, 0 conflict markers, `dart analyze` clean on all merged files.",
    "residual": "stash@{0} is deliberately LEFT IN PLACE as a safety net and a full patch is saved at /tmp/claude-1000/-home-scdouglas-Documents-Nightshade2/224b7868-cbfc-40e1-8fb0-99c71d23f174/scratchpad/rescue/stash-full-backup.patch. Nobody should pop or drop it without re-checking; the working tree already supersedes it.",
    "at_risk": "Any edit another agent made between the stash and the apply landed on a HEAD-reverted file. The 3-way merge recovered the 10 files git flagged, but an agent that wrote a file in that window and has not re-read it since may hold a stale view."
  },
  "fixes": [
    {
      "title": "Overlays menu label wraps mid-word to \"Rea\" / \"dou\"",
      "source": "reports/coverage/fix3/imaging--framing--guiding.json item 4 (P3, reproduced verbatim by the verifier)",
      "outcome": "fixed",
      "summary": "_OverlayMenuRow laid the row's optional description out BESIDE the label as a non-flexible Text, so the description claimed its full intrinsic width first and the Expanded label got the remainder. Inside the popup's 296 px cap that left the label ~29 px and 'Readouts' broke across lines. Measured under test on the pre-fix code the label box was 152 px tall (eight wrapped lines) against a 19 px single line. The description now stacks UNDER the label in a Column inside the Expanded, so no description can squeeze a control's name; the label is maxLines:1 + softWrap:false + ellipsis as a backstop, and the fixed SizedBox(height: 40) became a minHeight so the two-line row is not clipped.",
      "files_changed": [
        "packages/nightshade_app/lib/screens/imaging/widgets/imaging_preview_toolbar.dart",
        "packages/nightshade_app/test/screens/imaging/imaging_preview_toolbar_test.dart"
      ],
      "test": "packages/nightshade_app/test/screens/imaging/imaging_preview_toolbar_test.dart -> 'overlays_menu_label_is_not_squeezed_by_its_description'. Drives the real ImagingScreen at 1600x900, taps the Overlays popover, asserts the rendered height of the 'Readouts' Text equals that of the sibling single-line 'Crosshair' label (identical style, no description) and that its RenderParagraph did not exceed maxLines (so it is not ellipsised to 'Rea...' either). Command: cd packages/nightshade_app && flutter test test/screens/imaging/imaging_preview_toolbar_test.dart -> 3/3 pass.",
      "revert_check": "Replaced the new Expanded(Column(label, subtitle)) block in _OverlayMenuRow.build with the original `Expanded(child: Text(label, ...))` followed by `if (subtitle != null) ...[SizedBox(width: spaceSm), Text(subtitle!, ...)]`. Result: FAILS with 'Expected: a numeric value within <0.5> of <19.0> / Actual: <152.0>'. Restored -> 3/3 pass.",
      "caveats": "Readouts is the only row that currently carries a description; the fix is structural so any future described row is safe."
    },
    {
      "title": "Annotations empty state tells the user to \"Capture an image\" after they have captured 12",
      "source": "reports/coverage/fix3/imaging--framing--guiding.json item 3 (P3; verifier corrected the location to annotation_panel_parts/tab_panel.dart:911-923, NOT objects_panel.dart as filed)",
      "outcome": "fixed",
      "summary": "The empty state branched on ONE variable, `annotation == null`, and always printed 'No image annotated' + 'Capture an image to see detected objects' - while the status card two rows above already said 'No plate solver installed'. It now branches on the real blocker via a new library-level _annotationEmptyStateCopy(hasImage, annotationsEnabled, state) covering: annotations disabled, no frame yet, solver missing (reusing AnnotationStatusIndicator._isSolverMissing so the two surfaces cannot disagree), catalogsNotInstalled, plateSolveFailed for another reason, error, in-flight, and 'this frame is not annotated'. Both object lists (tab_panel.dart AND objects_panel.dart, which carried a duplicate of the same branch) now render one shared AnnotationEmptyState widget so the copy cannot drift.",
      "files_changed": [
        "packages/nightshade_app/lib/screens/imaging/widgets/annotation_panel_parts/status_widgets.dart",
        "packages/nightshade_app/lib/screens/imaging/widgets/annotation_panel_parts/tab_panel.dart",
        "packages/nightshade_app/lib/screens/imaging/widgets/annotation_panel_parts/objects_panel.dart",
        "packages/nightshade_app/test/screens/imaging/annotation_panel_blocker_copy_test.dart"
      ],
      "test": "NEW packages/nightshade_app/test/screens/imaging/annotation_panel_blocker_copy_test.dart, 3 copy tests: with a frame in currentImageProvider + the real 'no supported external plate solver is configured' AnnotationState the panel must NOT say 'Capture an image to see detected objects' and MUST say 'Cannot annotate: no plate solver' / 'Install ASTAP or set its path in Settings to label objects.'; with catalogsNotInstalled it must say 'Cannot annotate: no catalog'; and with NO frame it must STILL say 'Capture an image to see detected objects' (guards against inverting the fix). Command: flutter test test/screens/imaging/annotation_panel_blocker_copy_test.dart -> 4/4 pass.",
      "revert_check": "In AnnotationEmptyState.build, replaced the `_annotationEmptyStateCopy(...)` call with the original hard-coded `title = 'No image annotated'; hint = 'Capture an image to see detected objects';`. Result: 2 of 4 fail - 'Found 1 widget with text \"Capture an image to see detected objects\"' where none was expected, and 'Found 0 widgets with text \"Cannot annotate: no catalog\"'. Restored -> 4/4 pass.",
      "caveats": "The page banner that names the catalog blocker is unchanged; this only stops the object list contradicting it."
    },
    {
      "title": "Annotation panel's plate-solver warning is hard-clipped at the panel edge with no ellipsis",
      "source": "reports/coverage/fix3/imaging--framing--guiding.json item 5 (P3, verified from the evidence screenshot plus the layout)",
      "outcome": "fixed",
      "summary": "AnnotationStatusIndicator built Row(mainAxisSize.min) -> icon -> Column(Texts) with no Flexible anywhere, so the text column was laid out at its unconstrained INTRINSIC width. Its maxLines:2 + ellipsis could never fire because the text was never asked to wrap; the Row simply overflowed and the panel clipped the tail flat mid-word ('...in Settings to label ob'). Measured under test: the hint laid out 680 px wide inside a 320 px panel. Wrapped the column in Flexible (plus crossAxisAlignment.start and maxLines on the headline). SECOND call site fixed too: in live_preview_area.dart the same indicator sits in a Positioned with `left` but no `right` - genuinely unbounded width, where Flexible alone cannot help - so it now gets the same Responsive.previewOverlayMaxWidth ConstrainedBox its sibling ReAnnotateSuggestionBanner already had.",
      "files_changed": [
        "packages/nightshade_app/lib/screens/imaging/widgets/annotation_panel_parts/status_widgets.dart",
        "packages/nightshade_app/lib/screens/imaging/widgets/live_preview_area.dart",
        "packages/nightshade_app/test/screens/imaging/annotation_panel_blocker_copy_test.dart"
      ],
      "test": "packages/nightshade_app/test/screens/imaging/annotation_panel_blocker_copy_test.dart -> 'solver_warning_wraps_inside_the_panel': pumps AnnotationTabPanel at a 320 px panel width in the solver-missing state and asserts the rendered width of the hint Text is <= the panel width.",
      "revert_check": "Removed the Flexible wrapper around the status card's text Column (restoring the bare `Column(crossAxisAlignment.start, mainAxisSize.min, ...)` child of the Row and the headline's maxLines). Result: FAILS with 'Expected: a value less than or equal to <320.0> / Actual: <680.0>'; the resulting overflow additionally trips the three copy tests. Restored -> 4/4 pass.",
      "caveats": "The live_preview_area call site is bounded by construction now but has no dedicated test; the panel call site is the one the finding reproduced against."
    },
    {
      "title": "Imaging Guiding panel: Dither/Stop are disabled but drawn as live controls, so clicking Dither is a silent dead click",
      "source": "reports/coverage/fix3/imaging--framing--guiding.json item 1 (filed P2 -> corrected P3). The filed root cause was WRONG and the verifier said so: Dither and Stop ARE already gated (guiding_panel.dart:299/:283 `isEnabled: isConnected && isGuiding`), so no dither is ever actually skipped. The real defect is the disabled affordance.",
      "outcome": "fixed",
      "summary": "The shared SmallButton's OUTLINE variant rendered its disabled state as a FULL-STRENGTH `textMuted` border and label - the exact colour ordinary secondary text and borders use everywhere else - so a disabled outline button was indistinguishable from an enabled one. Only the FILLED variant (used by Start) had a visible disabled treatment, which is why the report saw a greyed Start beside a live-looking Stop and Dither inside one card. Disabled outline controls now dim to NightshadeTokens.opacityDisabled on border, icon and label together, and MouseRegion no longer offers the click cursor for a control that cannot be pressed. Icon and label colour were also collapsed into one `contentColor` so they can never drift apart again.",
      "files_changed": [
        "packages/nightshade_app/lib/screens/imaging/widgets/panel_widgets.dart",
        "packages/nightshade_app/test/screens/imaging/guiding_panel_disabled_affordance_test.dart"
      ],
      "test": "NEW packages/nightshade_app/test/screens/imaging/guiding_panel_disabled_affordance_test.dart, 2 tests. (1) Pumps the real GuidingPanel in its default DISCONNECTED state, asserts 'No guider connected' is on screen, then for both 'Stop' and 'Dither' reads the painted BoxDecoration border colour and Text style colour and asserts they are NOT full-strength textMuted, that alpha < 1.0, and that label and border dim together. (2) An ENABLED outline SmallButton must still paint the full-strength primary border - so the fix cannot be 'grey everything out'. Command: flutter test test/screens/imaging/guiding_panel_disabled_affordance_test.dart -> 2/2 pass.",
      "revert_check": "Reverted panel_widgets.dart to `final outlineColor = isEnabled ? widget.colors.primary : widget.colors.textMuted;`. Result: test 1 FAILS with 'Expected: not Color:<Color(alpha: 1.0000, red: 0.5647, green: 0.6000, blue: 0.6510 ...)> / Actual: <same>' - the literal full-strength textMuted. Restored -> 2/2 pass; the neighbouring imaging_panels_no_measurement_test.dart (6 tests, also drives GuidingPanel) stays green.",
      "caveats": "SmallButton is used across the imaging panels, so every disabled outline control in that family now dims - which is the point. Verified against the four Android/phone golden captures: my change does not move a single pixel in them (identical diffs with the change neutralised), because those goldens fail wholesale for an unrelated pre-existing reason."
    },
    {
      "title": "Guiding \"Brain Settings\" expands into skeleton placeholders that never resolve and never error",
      "source": "reports/coverage/fix3/imaging--framing--guiding.json item 2 (filed P2 -> corrected P3), verifier located the cause exactly",
      "outcome": "fixed",
      "summary": "BrainParamsNotifier (nightshade_core guiding_provider/brain_and_calibration.dart:16-53) starts in AsyncValue.loading() and only ever calls fetch() while the guider is connected AND deviceId == kPhd2CanonicalId; its disconnect listener resets it to loading again. So with PHD2 absent the provider can NEVER leave `loading`, and desktop_sections.dart rendered `loading:` as six ShimmerLoading rows unconditionally - a permanent claim that data is on the way. (The panel is not missing an error surface, as the report implied: a full error state with the real message and a Retry button already exists at :891-926, it is simply unreachable because nothing ever throws.) The shimmer is now gated on the same predicate the notifier uses to decide whether to fetch; when no fetch can run the panel says 'Brain settings need PHD2 / These are PHD2's own guiding parameters. Connect PHD2 to read and edit them.' Also fixed while there: the shimmer placeholder needs ~320 px in a flex-3 slot and overflowed the column by 47 px on a 900 px-tall window, so it is now scrollable.",
      "files_changed": [
        "packages/nightshade_app/lib/screens/guiding/guiding_screen_parts/desktop_sections.dart",
        "packages/nightshade_app/test/screens/guiding/guiding_brain_settings_disconnected_test.dart"
      ],
      "test": "NEW packages/nightshade_app/test/screens/guiding/guiding_brain_settings_disconnected_test.dart, 3 tests. (1) Drives the real GuidingScreen at 1280x900 with the guider disconnected, taps 'Brain Settings', pumps a further 4 s, and asserts findsNothing for ShimmerLoading plus the two explanatory strings. (2) With PHD2 connected and phd2GetAlgoParamNames stubbed to a never-completing Future, ShimmerLoading must STILL render and 'Brain settings need PHD2' must not - so the fix does not swallow a genuine in-flight fetch. (3) With PHD2 connected and the call throwing, the real error branch + Retry must appear. Command: flutter test test/screens/guiding/guiding_brain_settings_disconnected_test.dart -> 3/3 pass.",
      "revert_check": "Changed `loading: () => canFetchBrainParams` back to `loading: () => true` (i.e. always shimmer). Result: test 1 FAILS with 'Found 6 widgets with type \"ShimmerLoading\"' where none were expected - the exact six rows the report saw. Restored -> 3/3 pass; the whole test/screens/guiding suite is green except three pre-existing golden captures.",
      "caveats": "The gate mirrors the notifier's own precondition (connected && deviceId == kPhd2CanonicalId) rather than inferring it, so the two cannot disagree; if that precondition ever loosens in core, this panel needs the same edit. The underlying provider design (loading doubling as 'never started') is unchanged - fixing that means giving BrainParamsNotifier an explicit 'unavailable' state, which is a core file outside this ownership."
    },
    {
      "title": "Framing sidebar \"Equipment\" section shows an indefinite spinner",
      "source": "reports/coverage/fix3/imaging--framing--guiding.json item 6 (filed P2 -> corrected P3). The verifier corrected the shape: it is not one 21-minute hang, it FLICKERS - eight of ten samples over 30 s were a bare spinner, two were the resolved rig.",
      "outcome": "fixed",
      "summary": "framingFOVProvider watches cameraStateProvider, so every telemetry tick (cooler power, sensor temperature, exposure progress) re-runs its async backend.getCameraStatus round-trip. The Equipment card rendered that with `.when(...)`, whose skipLoadingOnReload defaults to FALSE, so on every one of those reloads it THREW AWAY the equipment it had already resolved and painted a bare spinner for the duration of the round-trip - which is what read as 'stuck'. The FOV/Resolution/Sensor rows immediately below never flickered because controls_and_status.dart:20 reads `equipmentAsync.valueOrNull` and keeps the retained value. Both `.when` call sites in the card (the status badge and the content) now pass skipLoadingOnReload: true, so the card behaves like the rows below it; a genuine FIRST load still shows the spinner. The status badge's `loading: () => const SizedBox()` - which made a stuck first load invisible - now renders a muted 'Loading...'.",
      "files_changed": [
        "packages/nightshade_app/lib/screens/framing/widgets/framing_sidebar/target_and_equipment.dart",
        "packages/nightshade_app/test/screens/framing/framing_equipment_reload_flicker_test.dart"
      ],
      "test": "NEW packages/nightshade_app/test/screens/framing/framing_equipment_reload_flicker_test.dart, 2 tests. (1) Renders the real FramingEquipmentSection, resolves it to 'My First Rig / Simulated Camera', then pushes the exact state Riverpod produces for a WATCHED-DEPENDENCY change - AsyncLoading.copyWithPrevious(data, isRefresh: FALSE) - and asserts no CircularProgressIndicator appears and both the rig name and camera stay on screen. The isRefresh flag is load-bearing: `when` skips loading on a refresh by default but not on a reload, and my first draft passed without the fix because it used the default (refresh) form. (2) On a genuine first load the badge must read 'Loading...'. Command: flutter test test/screens/framing/framing_equipment_reload_flicker_test.dart -> 2/2 pass.",
      "revert_check": "Removed both `skipLoadingOnReload: true,` lines. Result: test 1 FAILS with 'Found 1 widget with type \"CircularProgressIndicator\"' where none was expected. Restored; then separately reverted the badge to `loading: () => const SizedBox()` -> test 2 FAILS with 'Found 0 widgets with text \"Loading...\"'. Restored -> 2/2 pass.",
      "caveats": "This fixes the RENDERING lie. The churn itself is still there: framingFOVProvider watches the whole cameraStateProvider when it needs only connectionState + deviceName, so a driver round-trip still fires on every telemetry tick. That provider is packages/nightshade_core/lib/src/providers/framing_provider/support.dart:40-104, outside this ownership - worth a separate item. framing_actions_panel.dart also watches framingFOVProvider but already uses valueOrNull, so it was never affected."
    },
    {
      "title": "Onboarding promises target/date subfolders; every frame lands flat in the chosen directory",
      "source": "reports/coverage/fix3/imaging--framing--guiding.json item 7 (P3, both halves verified). Filed `where` names files in nightshade_core and onboarding, neither in this ownership list.",
      "outcome": "fixed",
      "summary": "The step read 'Sessions will be organized into target/date subfolders under this directory.' Nothing does that: the shipped default file_naming_pattern (database/database/default_settings.dart:21) is `$TARGET_$FILTER_$DATE_$SEQ`, which has no separator, so every frame lands flat. The subfolder machinery IS real - imaging_service/file_paths.dart treats the pattern as a `/`-separated path and creates the directories, and the NamingPattern model's own default is `$TARGET/$FRAMETYPE/...` - the shipped settings default just overrides it flat. I fixed the COPY rather than the default, because changing the shipped default silently rearranges where a fresh install's frames land (a product decision) and default_settings.dart was concurrently modified by another agent. The step now says 'Every capture is saved here. File names - and any subfolders - come from the naming pattern in Settings -> Imaging.', which is true for both flat and nested patterns and points at the control that decides.",
      "files_changed": [
        "packages/nightshade_app/lib/screens/onboarding/steps/capture_dir_step.dart",
        "packages/nightshade_app/test/screens/onboarding/capture_dir_subfolder_promise_test.dart"
      ],
      "test": "NEW packages/nightshade_app/test/screens/onboarding/capture_dir_subfolder_promise_test.dart. Binds the copy to the behaviour instead of pinning a string: it pumps the real OnboardingCaptureDirStep, reads the shipped default fileNamingPattern out of the harness's seeded database (so it is the pattern a fresh install actually gets), asserts that pattern is flat, and then asserts no rendered text matches /(will be|are)\\s+organi[sz]ed.*subfolder/ while some rendered text does mention the naming pattern. Command: flutter test test/screens/onboarding/capture_dir_subfolder_promise_test.dart -> 1/1 pass.",
      "revert_check": "Restored the literal old sentence 'Sessions will be organized into target/date subfolders under this directory.' Result: FAILS with 'Expected: empty / Actual: WhereIterable<String>:[...]'. Restored -> 1/1 pass; the three existing onboarding capture-dir suites (authority, remote, reverify) stay green at 8/8.",
      "caveats": "OWNERSHIP: capture_dir_step.dart is not in my declared list. I took it because a scan of every fix3 findings file shows this item exists ONLY in mine, so nobody else would have fixed it; the edit is one string plus a comment, in a file no concurrent agent had touched today. If the owner would rather HONOUR the promise than scope the copy, the one-line alternative is to change default_settings.dart:21 (and the matching fallback in app_settings_state.dart:485) to a `/`-separated pattern - that is a behaviour change for fresh installs and needs a product decision, which is why I did not make it."
    },
    {
      "title": "FITS omits XPIXSZ/YPIXSZ although onboarding collects pixel size",
      "source": "reports/coverage/fix3/imaging--framing--guiding.json item 8 (P3)",
      "outcome": "already-correct",
      "summary": "Already fixed by the s-imaging-service agent before I started. persistence.dart no longer passes `pixelSizeX: null, // Pixel size not stored in profile yet`; it reads the pitch from equipmentCameraCapabilitiesProvider and writes it per axis only when the driver reports a positive value. Recorded in reports/coverage/fixes/w3-s-imaging-service.json with its own revert check (reverting persistence.dart:92-93 fails 2 of 4 tests with 'Expected: <3.76> Actual: <null>'). Re-verified present; not re-done, and the file is outside this ownership.",
      "files_changed": [],
      "test": "packages/nightshade_core/test/services/imaging_service_fits_pixel_size_test.dart (owned by the s-imaging-service agent), 4/4 pass.",
      "revert_check": "Not re-run here - the owning agent's entry documents an independent re-run this session.",
      "caveats": "The report's supporting claim that 'the comment is no longer true' was itself wrong, as the verifier noted: equipment_profiles has no pixel-size column and CameraStateSnapshot carries no pitch. The value is only reachable via the camera's capability report, which is what the landed fix uses. The missing OBJECT card is NOT a bug for a manual capture: persistence.dart:47 already passes objectName: targetName, which is legitimately null with no session target."
    }
  ]
}
