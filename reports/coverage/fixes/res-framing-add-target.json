{
  "key": "framing-add-target",
  "outcome": "fixed",
  "summary": "addTargetHeader's bare `if (rootNodeId == null) return;` / `if (root == null) return;` (was tree_editing.dart:205-210) is gone. Both it and addNode now call a new _ensureRootNode(newNodes), which resolves / repairs / creates the container the insert hangs from and writes the id back onto the sequence's rootNodeId. Framing -> Add to Sequence -> Add target now actually adds the target on a rootless sequence instead of firing the disclosure snackbar that sent the user to the Sequencer, where the same insert failed the same way.",
  "files_changed": [
    "packages/nightshade_core/lib/src/providers/sequence/sequence_editor/tree_editing.dart",
    "packages/nightshade_core/test/providers/sequence/add_to_rootless_sequence_test.dart",
    "packages/nightshade_app/test/screens/framing/add_target_to_rootless_sequence_test.dart"
  ],
  "test": [
    "packages/nightshade_app/test/screens/framing/add_target_to_rootless_sequence_test.dart :: 'a saved sequence with no root node still accepts the target' — drives the real production call site addFramedTargetToExistingSequence (the function framing_screen.dart:493 calls) through the destination picker and the 'Add target' button, on a savedSequencesProvider row whose rootNodeId is null. Asserts NO 'Could not add' snackbar, that the flow returns true, and that the header is reachable from the root via descendantsOf.",
    "packages/nightshade_core/test/providers/sequence/add_to_rootless_sequence_test.dart :: 5 tests — rootless library row, dangling root pointer kept under its own id, import that only lost the pointer keeps its own root (no extra wrapper), existing tree not stranded, and the addNode half."
  ],
  "revert_check": "Two separate reverts, both actually applied and run.\n(1) In addTargetHeader, restored `final rootNodeId = _currentSequence!.rootNodeId; if (rootNodeId == null) return; final root = newNodes[rootNodeId]; if (root == null) return;` in place of `final rootNodeId = _ensureRootNode(newNodes); final root = newNodes[rootNodeId]!;`. Result: 4/5 core tests FAILED (`Expected: not null Actual: <null>`, `Expected: 'set' Actual: <null>`) and the app-level wiring test FAILED with the exact user-visible symptom — `Found 1 widget with text containing Could not add`. Restored; all pass.\n(2) In addNode, restored `} else if (_currentSequence!.rootNodeId != null) {` in place of `} else {`. Result: the addNode core test FAILED (`Expected: '<uuid>' Actual: <null>` on the new node's parentId). Restored; all pass.",
  "verification": [
    "flutter test (whole nightshade_core package): 5217 passed, 4 skipped, 0 failed.",
    "packages/nightshade_core: flutter test test/providers/sequence/ -> 428 passed, 0 failed.",
    "packages/nightshade_app: framing + sequencer + planetarium/send_to_sequencer + utils. 8 failures, all pre-existing and unrelated: 6 Windows-captured goldens/layout captures (framing_hips_layer_wiring, framing_registration, 2x captures_landscape under framing, 2x under sequencer) and 2 calendar-dependent SmartNightService failures in plan_tonight_sequencer_helper_no_filter_wheel_test ('Target \"M31\" has no usable imaging window between 2026-08-03T21:58 and 2026-08-04T04:14 at min altitude 30' thrown from smart_night_service.dart:706 — nothing to do with tree_editing).",
    "dart format --set-exit-if-changed clean on all 3 files; dart analyze clean on sequence_editor/ and both test files."
  ],
  "needs_shared_change": "packages/nightshade_core/lib/src/providers/sequence/sequence_editor/snippet_editing.dart has the same class of defect and is NOT in my ownership list. It is a `part` of the same library, so _ensureRootNode is directly callable from it. (a) line 49-50: `mergeParentId ??= _currentSequence!.rootNodeId; if (mergeParentId == null) return;` — merging a template snippet into a rootless sequence returns having written nothing, exactly as addTargetHeader used to. Should be `mergeParentId ??= _ensureRootNode(newNodes);`. (b) lines 154-160: when rootNodeId is null it mints an `InstructionSetNode(name: 'Sequence Root')`, uses it as the insert parent, but never writes its id onto the sequence's rootNodeId — so the snippet lands under a container nothing points at, invisible to rootNode/targetHeaders/the executor. Should be `insertParentId ??= _ensureRootNode(newNodes);` with `rootNodeId:` set on the closing copyWith.",
  "caveats": "Reachability of the rootless state was confirmed, not assumed: sequences.rootNodeId is `text().nullable()` (database/tables/sequences.dart:17), sequence_file_service.dart:241-247 only validates rootNodeId 'if present', sequences_dao.dart:267-268 falls back to the source id when the id-remap misses, and loadCopyForEditing (sequence_editor.dart:463) copies source.rootNodeId through verbatim — so a library row with a null root reaches the editor unchanged and Framing's picker hands it straight to addTargetHeader. The disclosure-only layer in add_target_to_sequence_flow.dart (read-back + 'Could not add' snackbar) was deliberately left in place: it still guards every other way the write could fail to land, and its test now proves it stays silent on this path."
}
