{
  "key": "s-imaging-service",
  "area": "ImagingService: capture pipeline, loop capture, cancel latch, persistence gating",
  "owned_files": [
    "packages/nightshade_core/lib/src/services/imaging_service.dart",
    "packages/nightshade_core/lib/src/services/imaging_service/**"
  ],
  "input_triage": {
    "blocked_from_wave2_items_scanned": 42,
    "items_whose_needs_shared_change_lands_in_my_files": 0,
    "note": "Every one of the 42 needs_shared_change fields was resolved to a concrete file list; none names imaging_service.dart or imaging_service/**. Item 25 (p2-imaging, abort does not stop the exposure) explicitly clears the Dart side: 'ImagingService.cancelExposure resolves the pending completer' \u2014 the remaining half is native/nightshade_native/bridge/src/api/imaging.rs api_camera_cancel_exposure sim branch, not mine. Work below comes from the KNOWN CROSS-CUTTING list and from reports/coverage/fix3/imaging--framing--guiding.json, which contains two verified items whose 'where' IS in my files. RE-VERIFIED independently on re-run: keyword scan of all 42 needs_shared_change fields for imaging_service/captureImage/startLoopCapture/persistence.dart/quality_processing/file_paths/cancelExposure returns exactly one hit (item 25), and that item's own text places the remaining work in native api_camera_cancel_exposure. Of the 4 KNOWN CROSS-CUTTING items: #1 (headless sequencer validation) and #2 (saturation_limit in Rust) are not in my files; #3 (capture_settings_panel.dart) is in nightshade_app; #4 (centering plate-solve frames written as keeper lights) needed captureUtilityFrame in MY file and is DONE \u2014 centering_service.dart:707 and :1066 both call imagingService.captureUtilityFrame today and centering_service_test.dart asserts the keeper path is never used. One further report item names one of my files: reports/coverage/fix3/imaging--framing--guiding.json item 6 ('onboarding promises target/date subfolders', where=file_paths.dart) \u2014 location is wrong. file_paths.dart already implements `/`-separated subfolder patterns and buildImageFilePath creates the directories; the flat result comes from the shipped default value `$TARGET_$FILTER_$DATE_$SEQ` at database/database/default_settings.dart:21 and app_settings_stored_snapshot_mapping.dart:76 (the NamingPattern model's own default IS `$TARGET/$FRAMETYPE/...`). The fix is that default or the onboarding copy \u2014 neither is mine."
  },
  "fixes": [
    {
      "title": "Every FITS this app writes omits XPIXSZ/YPIXSZ, so no downstream tool can derive the plate scale from the file",
      "source": "reports/coverage/fix3/imaging--framing--guiding.json item 7 (P3, verified, where=persistence.dart:73-74)",
      "outcome": "fixed",
      "summary": "_saveFitsFile passed `pixelSizeX: null, // Pixel size not stored in profile yet` \u2014 true of the equipment_profiles table (no pixel-size column) and the wrong reason to write nothing, because the connected camera reports the pitch. It now comes from equipmentCameraCapabilitiesProvider (the same cached capability read the readout-mode step already does, so no extra driver round-trip) and is written per axis only when the driver reports a positive value. The Rust writer emits XPIXSZ = pixel_size_x * XBINNING (bridge/src/api/imaging.rs:3708/3845/4147), so the UNBINNED pitch is what is passed.",
      "files_changed": [
        "packages/nightshade_core/lib/src/services/imaging_service/persistence.dart",
        "packages/nightshade_core/test/services/imaging_service_fits_pixel_size_test.dart"
      ],
      "test": "packages/nightshade_core/test/services/imaging_service_fits_pixel_size_test.dart (4 tests: pitch reaches the header; the value is unbinned with binning=2; a 0.0/unreported axis gets no fabricated card; a null capability report still saves the frame). Command: cd packages/nightshade_core && flutter test test/services/imaging_service_fits_pixel_size_test.dart -> 4/4 pass.",
      "revert_check": "Reverted persistence.dart:92-93 back to `pixelSizeX: null, // Pixel size not stored in profile yet` / `pixelSizeY: null, ...`. Result: 2 of 4 fail with `Expected: <3.76> Actual: <null>` ('the FITS carries the camera-reported pixel pitch', 'the pitch is the UNBINNED sensor value'). Restored -> 4/4 pass. RE-RUN INDEPENDENTLY THIS SESSION: reverted persistence.dart:92-93 to `pixelSizeX: null` / `pixelSizeY: null` -> 2 of 4 fail with Expected <3.76> Actual <null>; restored -> 4/4 pass.",
      "caveats": "Only the Dart manual/loop capture path is touched. The native sequencer writes its own headers and was not in scope. A driver that reports no pixel size still yields no card \u2014 that is the honest outcome; the onboarding-collected pixel size has nowhere to persist (no equipment_profiles column), which is a separate defect owned by whoever owns onboarding."
    },
    {
      "title": "Live-view and plate-solve exposures renumber the operator's keepers: a 3-frame framing loop makes the next Snapshot _0004 with two keepers on disk",
      "source": "Same defect class as KNOWN CROSS-CUTTING items 3 and 4 (live-view/utility frames leaking into the acquisition record); found by reading the persistence gating I own, not reported by a verifier.",
      "outcome": "fixed",
      "summary": "_capture set the shared `_frameNumber` before it knew whether the frame was a keeper, so every non-persisted frame moved the counter that `$SEQ`/`$FRAMENUM` renders into saved filenames. startLoopCapture passes frameNumber:1..N, so a SHORT loop pushed the counter backwards onto a number already on disk (the next keeper then landed as `..._0003_001.fits` via _ensureUniqueFilePath) and a long loop pushed it into the hundreds. Utility frames (centering/verification, which pass no frameNumber) advanced it by one per plate-solve iteration. The keeper counter now only advances for persisted frames; non-persisted frames get a local number for the progress ring only.",
      "files_changed": [
        "packages/nightshade_core/lib/src/services/imaging_service.dart",
        "packages/nightshade_core/test/services/imaging_service_keeper_numbering_test.dart"
      ],
      "test": "packages/nightshade_core/test/services/imaging_service_keeper_numbering_test.dart (3 tests: 2 snapshots + 3-frame loop + snapshot -> 0001/0002/0003; snapshot + 2 captureUtilityFrame + snapshot -> 0001/0002; an explicit caller frameNumber:7 still wins and continues 0008). Command: cd packages/nightshade_core && flutter test test/services/imaging_service_keeper_numbering_test.dart -> 3/3 pass.",
      "revert_check": "Collapsed the new branch at imaging_service.dart:180-186 back to the single line `_frameNumber = frameNumber ?? (_frameNumber + 1); final int captureFrameNumber = _frameNumber;`. Result: 2 of 3 fail \u2014 Expected ['0001','0002','0003'] Actual ['0001','0002','0004'], and Expected ['0001','0002'] Actual ['0001','0004']. Restored -> 3/3 pass. RE-VERIFIED THIS SESSION by re-running the suite (6/6 pass, including the two original keeper-numbering assertions) after every subsequent edit.",
      "caveats": "No data was ever lost to this: _ensureUniqueFilePath already prevented an overwrite. The visible damage was a sequence number that counted throwaway exposures plus the ugly `_001` collision suffix. Regression check: imaging_service_test.dart (73 tests incl. the exposure-progress frameNumber assertions), imaging_service_loop_retention_test.dart, imaging_service_filter_provenance_test.dart and centering_service_test.dart all green."
    },
    {
      "title": "Keeper sequence numbers restart at 0001 every time the imaging service is rebuilt, so the frame collides with one already on disk and lands as `..._0001_001.fits`",
      "source": "Same defect family as the keeper-numbering fix above; found by reading the ownership area (persistence gating + `$SEQ`), not reported by a verifier. Confirmed by test before fixing.",
      "outcome": "fixed",
      "summary": "`_frameNumber` counts only the frames of ONE ImagingService instance, and `imagingServiceProvider` does `ref.watch(backendProvider)` \u2014 so the counter returns to 0 on every app start AND on every host switch/reconnect, several times a night. The shipped default pattern `$TARGET_$FILTER_$DATE_$SEQ` still resolves to the same folder and the same UTC date, so the next Snapshot asked for `$SEQ` = 0001 with 0001 already written. No data was lost (_ensureUniqueFilePath appends `_001`), but the operator was left with two files whose sequence number both read 0001 plus a `_001` suffix no pattern asked for, which sorts wrong in every stacker. The auto-assigned keeper number is now the lowest free one: _nextKeeperFrameNumber renders the pattern for successive numbers and walks past the frames already on disk. Probing the folder (rather than remembering a high-water mark) is also what makes it restart correctly \u2014 tomorrow's `$DATE` renders a path that does not exist, so tomorrow's first frame is 0001 again.",
      "files_changed": [
        "packages/nightshade_core/lib/src/services/imaging_service/file_paths.dart",
        "packages/nightshade_core/lib/src/services/imaging_service.dart",
        "packages/nightshade_core/test/services/imaging_service_keeper_numbering_test.dart"
      ],
      "test": "packages/nightshade_core/test/services/imaging_service_keeper_numbering_test.dart, 2 new tests: 'a rebuilt service continues the numbering already on disk' (2 keepers on one container, a 3rd on a freshly built one -> 0001/0002/0003 and no `_001` path) and 'numbering restarts when the folder is empty again' (delete the folder contents between containers -> back to 0001, proving it follows the folder rather than a high-water mark). Command: cd packages/nightshade_core && flutter test test/services/imaging_service_keeper_numbering_test.dart -> 6/6 pass.",
      "revert_check": "Replaced imaging_service.dart:187-193 (`_frameNumber = frameNumber ?? await _nextKeeperFrameNumber(exposureSettings: settings, targetName: targetName);`) with the old `_frameNumber = frameNumber ?? (_frameNumber + 1);`. Result: 'a rebuilt service continues the numbering already on disk' fails with Expected ['0001','0002','0003'] Actual ['0001','0002','001'] \u2014 the literal `_0001_001.fits` collision. Restored -> 6/6 pass.",
      "caveats": "The probe runs BEFORE the exposure so the progress ring and the filename carry the same number; it therefore resolves `$DATE`/`$TIME` from clockProvider.now() while the saved path resolves them from the camera's own timestamp minus the exposure time. Those are the same instant to within the microseconds between the probe and cameraStartExposure, so they can only disagree if an exposure is started inside a sub-second window around UTC midnight \u2014 and _ensureUniqueFilePath is still the backstop if they do. Patterns that render no frame number (e.g. `$TARGET_$DATETIME`) are detected and skipped rather than probed forever. The helper never throws: an unusable pattern must still fail the capture at save time, where the operator gets the real message. Regression: imaging_service_test.dart + loop_retention + filter_provenance + fits_pixel_size + centering_service_test.dart = 120 tests green."
    },
    {
      "title": "Losing the host mid-exposure leaves the app narrating a frame that will never arrive: the ring freezes mid-count and the camera panel keeps saying 'exposing' for the rest of the session",
      "source": "Found by reading the ownership area (capture pipeline + cancel latch). Same cry-wolf class as the campaign's other 'the app states something untrue' items. Confirmed by test before fixing.",
      "outcome": "fixed",
      "summary": "`imagingServiceProvider` watches `backendProvider`, so a host switch \u2014 or a remote client dropping back to DisconnectedBackend when the network goes \u2014 disposes the service and calls `retire()` with a frame in flight. From that instant `_hasBackendAuthority` is false, and every release of the shared exposure state was gated on it: the cancel branch, the success path and the outer finally all skipped `cameraStateProvider.setExposing(false)` and `exposureProgressProvider.reset()`. Neither provider is per-backend and neither is rebuilt on a backend change, so nothing else ever cleared them \u2014 measured in the test: the ring stayed at 42% / 174 s remaining and `isExposing` stayed true, permanently. The authority check was the wrong guard: authority is lost precisely when a frame is abandoned. It is replaced by `_ownsSharedExposureState`, the narrower property it was standing in for \u2014 release exactly once, only from the capture that armed the state, so a later capture's state can never be stomped. `retire()` now performs that release immediately (a 300 s sub would otherwise keep the UI lying for five more minutes) using notifier references captured at exposure start, because `retire` runs inside `ref.onDispose` where this service's own `Ref` is already gone.",
      "files_changed": [
        "packages/nightshade_core/lib/src/services/imaging_service.dart",
        "packages/nightshade_core/test/services/imaging_service_retire_progress_test.dart"
      ],
      "test": "packages/nightshade_core/test/services/imaging_service_retire_progress_test.dart \u2014 'retiring mid-exposure clears the exposure it was narrating'. It drives the real wiring: a 300 s capture is started against a backend that emits ExposureProgress and no completion, the test asserts the exposure is genuinely running, then swaps backendProvider's state (what a host switch does), which rebuilds imagingServiceProvider and fires `ref.onDispose(service.retire)`. Asserts exposureProgressProvider == ExposureProgress.idle() and cameraStateProvider.isExposing == false. Command: cd packages/nightshade_core && flutter test test/services/imaging_service_retire_progress_test.dart -> 1/1 pass.",
      "revert_check": "Restored all three original release sites (cancel branch `if (_hasBackendAuthority(backend)) { cameraNotifier.setExposing(false); progressNotifier.reset(); }`, the same pair in the outer finally under `hasAuthority`, and dropped the `_releaseSharedExposureState()` call from `retire()`). Result: the test fails with Expected ExposureProgress(0.0, 0.0, 0.0, 1, null, false) Actual ExposureProgress(126.0, 174.0, 42.0, 1, null, false) \u2014 the frozen ring, verbatim. Restored -> 1/1 pass.",
      "caveats": "Two notes on scope. (1) `retire()` is only reached when something rebuilds imagingServiceProvider; a read-only consumer can defer that, so the prompt clearing happens on the next read \u2014 but the unconditional release in `_capture`'s finally now closes the abandoned frame either way, which is the part that used to be skipped forever. (2) The related native gap is NOT fixed here and is not mine: reports/coverage/fix3/_blocked_from_wave2.json item 25 shows api_camera_cancel_exposure's `sim_` branch only flips the state to Idle while the simulated exposure future sleeps to completion, so on the simulator the sensor is not really stopped. The Dart side (cancelExposure resolving the pending completer, and now the state release) is complete. Regression: 121 tests across imaging_service_test.dart + keeper_numbering + loop_retention + filter_provenance + fits_pixel_size + retire_progress + centering_service_test.dart green; dart analyze clean."
    },
    {
      "title": "`$SEQUENCE` in the naming pattern renders the TARGET name on the Dart capture path, so one pattern produces two different folder trees",
      "source": "Found by diffing my file's substitution table against native/nightshade_native/imaging/src/naming.rs, which the file's own doc comment says it mirrors. Confirmed by test before fixing.",
      "outcome": "fixed",
      "summary": "buildTimestampSubstitutions mapped `$SEQUENCE` to `targetName ?? 'Sequence'`. The Rust FilenameGenerator maps it to `ctx.sequence`, falling back to the literal \"Sequence\" (naming.rs:328-332), and ImagingService._patternVariables is documented as 'honoured here so that patterns shared between the Dart and Rust capture paths behave identically'. With `$SEQUENCE/$TARGET_$SEQ` the sequencer filed frames under the plan's name while a manual Snapshot of the same object filed them under the object's name \u2014 in a directory the pattern says is a sequence. Nothing on the Dart path (Snapshot, Loop, plate-solve frame) belongs to a sequence, so the honest value is the same no-sequence literal Rust uses.",
      "files_changed": [
        "packages/nightshade_core/lib/src/services/imaging_service.dart",
        "packages/nightshade_core/test/services/imaging_service_naming_tokens_test.dart"
      ],
      "test": "packages/nightshade_core/test/services/imaging_service_naming_tokens_test.dart \u2014 'an ad-hoc capture does not file itself under a fake sequence'. Goes through the real pipeline (appSettings.fileNamingPattern = `$SEQUENCE/$TARGET_$SEQ`, captureImage with targetName 'M31') and asserts the directory the FITS was actually written into, not the pure helper \u2014 so deleting the _buildPatternSubstitutions wiring would fail it too. Command: cd packages/nightshade_core && flutter test test/services/imaging_service_naming_tokens_test.dart -> 1/1 pass.",
      "revert_check": "Restored `r'$SEQUENCE': targetName ?? 'Sequence',` at imaging_service.dart:1455. Result: the test fails with Expected 'Sequence' Actual 'M31' (the folder the frame landed in). Restored -> 1/1 pass.",
      "caveats": "`$SESSION` remains a knowing divergence and is left alone: Dart renders the UTC date (documented at file_paths.dart:20-24 so the folder matches the FITS DATE-OBS), Rust falls back to the LOCAL date. Changing either is a cross-language decision with on-disk consequences for existing libraries, and the Rust half is not my file. Regression: 122 tests across the imaging + centering suites green."
    }
  ]
}