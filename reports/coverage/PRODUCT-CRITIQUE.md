# Nightshade sweep — product critique

Not defects. These are places the app works as built and the design is wrong: a default everyone must change, a flow that costs more clicks than it should, something a user reaches for and does not find. Refuted findings are excluded.

Generated by `tools/production/findings_report.dart` from `reports/coverage/swept`.

- **431** distinct findings across **17** areas (431 raw, 0 duplicates merged)
- **2** independently reproduced by an adversarial verifier; **26** were refuted and are excluded
- **70** units recorded as unreached, with reasons


## P2

### Fail-closed is the shipped default and the Weather Safety page never tells a sensorless rig what that means

`default` · settings (sequencer, science, phd2-guiding, plate-solving, weather-safety, notifications + all transports, integrations/plugins, annotations, observation log, auto-save, cloud-sync) · **CONFIRMED** _(reported P1, corrected by verifier)_

**What happens:** safety_fail_mode defaults to failClosed (verified in the fresh DB and at app_settings.dart:82). 'Enable weather safety' correctly defaults OFF -- but the moment a user turns it on, which reads like the responsible thing to do, a rig with no weather device and no safety monitor is treated as unsafe: the app raises a CRITICAL 'Safe the rig', and per the codebase's own WeatherSafetyNoSourceRule 'the run will park and abort shortly after it starts'. The Weather Safety page itself contains NO fail-mode control, NO row showing which weather sources are connected or what they currently read, and NO hint that zero sources are attached. The fail-mode dropdown lives on a different page (Sequencer). The rule's resolution hint literally says 'switch the safety fail mode to fail open in Settings > Automation & Safety' -- a string the user only meets at run-start, not on the page where they made the choice.

**Expected:** The page that owns the master switch should show live source state and the fail-mode consequence. Concretely: a status row at the top ('Weather sources: none connected -- with Fail Closed, runs will park and abort'), the fail-mode selector moved next to the master switch, and turning the master switch on with zero sources should either default that rig to Warn Only or ask.

**Repro:** Fresh profile, nothing connected. Settings > AUTOMATION & SAFETY > Weather Safety. Read the whole page (3 toggles, 3 thresholds, 2 alert-timing numbers) -- nothing mentions sources or fail mode. Toggle 'Enable weather safety' on. Then go to Sequencer to find 'Safety fail mode: Fail Closed (Park)'.

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/weather_safety_settings.dart:120-175; default at packages/nightshade_core/lib/src/models/settings/app_settings.dart:82; consequence documented at packages/nightshade_core/lib/src/providers/sequence/rules/weather_safety_rules.dart:9-70`

**Proposed fix:** Add a source-status SettingRow driven by weatherStateProvider/safetyMonitorStateProvider at the top of WeatherSafetySettings, move the SafetyFailMode dropdown onto that page (mirroring it in Sequencer the way auto-park is already mirrored), and when weatherSafetyEnabled flips true with no live source, present a one-time choice rather than a CRITICAL toast.

### 3D Surface Explorer has no axes, no colour scale and no numbers, so the plot cannot be read

`ux` · analytics (Session, History, Projects, Equipment Stats, Science, Diagnostics) + science screen · **not yet verified**

**What happens:** With a 5x5 uniformity grid seeded (values 0.10 at tile (0,0) rising to 0.32 at (4,4)) the card renders 25 coloured dots on an otherwise empty ~450px-tall panel. There is no x/y axis, no indication which corner of the sensor is which, no colour bar, no min/max value, no units, and no hover/tap readout. The only text is the title, the layer dropdown, 'Z Exaggeration' and 'Contours'.

**Expected:** A field-quality map's entire purpose is to say WHERE on the sensor the problem is and HOW BAD. Without an orientation cue (top-left of frame, etc.) and a value scale the user cannot act on it - they cannot tell a 2% uniformity spread from a 40% one, and cannot map a red corner onto a physical tilt adjustment. NINA's Hocus Focus and ASTAP's tilt map both label corners and print numbers.

**Repro:** Analytics > Science with science_tile_metrics seeded; look at the 3D Surface Explorer card.

**Where:** `packages/nightshade_app/lib/screens/analytics/widgets/science_surface_explorer.dart:60-118 (card contents) and the _SurfacePainter at :300-332 which draws only circles and optional stems`

**Proposed fix:** Add corner labels (TL/TR/BL/BR), a colour bar with min/median/max in the layer's units, and a tap-to-read tooltip giving tile (row,col) and value. Shrink the fixed 240-480px plot box to the space actually used.

### Building the standard night by hand takes ~21 actions before configuring anything, and the empty Builder points at none of the faster paths

`ux` · sequencer · **not yet verified**

**What happens:** I built cool -> unpark -> slew -> autofocus -> N lights (dither is built into Take Exposures at ditherEvery:3) -> park -> warm by hand. Each node costs three actions with the palette search (click the field, type, click the row's '+') or a scroll-and-click without it, so seven nodes is ~21 actions - and that is before entering a single temperature, exposure or coordinate. Three of my searches added the wrong node (see the ranking defect). Meanwhile the app already has two much better paths: the Quick-Start Wizard produces the same spine plus Plate Solve & Center, Start/Stop Guiding, a HFR refocus trigger, a Weather Safety recovery, a Park-on-Error recovery and a Meridian Flip in about ten actions; and the Starters tab has six curated, well-described sequences. The empty Builder shows neither - just 'Drop instructions here' and a red error badge.

**Expected:** The empty-state canvas should be the entry point: 'Start from a wizard / Start from a starter / Build manually', the way NINA's template gallery and SGP's sequence wizard do. Also add insert-after-selected keyboard entry, since the palette + button already inserts relative to the selection.

**Repro:** Sequencer > Builder > New Sequence, then build the canonical sequence via the palette and count the interactions; compare with Templates > Quick-Start Wizard.

**Where:** `packages/nightshade_app/lib/screens/sequencer/sequencer_screen_parts/builder_layout.dart`

**Proposed fix:** Replace the bare 'Drop instructions here' with three primary actions. Keep the drop zone below them.

### 'Audible alert on critical events' is OFF by default inside the section titled 'Critical events (unattended imaging)'

`default` · settings (sequencer, science, phd2-guiding, plate-solving, weather-safety, notifications + all transports, integrations/plugins, annotations, observation log, auto-save, cloud-sync) · **not yet verified**

**What happens:** Under the heading 'Critical events (unattended imaging)', 'Audible alert on critical events -- Play the system bell when a critical error fires' ships OFF. The sibling 'Push critical alerts to mobile' ships ON but honestly discloses that it delivers nothing until remote access is configured. So on a fresh install the section whose entire purpose is reaching an absent operator has zero working channels, and the one that needs no setup at all is the one turned off.

**Expected:** Default the system bell ON. It costs nothing, needs no configuration, and is the only channel that works out of the box on a laptop sitting next to the rig. If the concern is nuisance during attended imaging, gate it on 'a sequence is running' rather than defaulting it off.

**Repro:** Fresh profile. Settings > NOTIFICATIONS & REMOTE > Notifications > 'Critical events (unattended imaging)'.

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/notification_settings.dart (critical-events section defaults)`

**Proposed fix:** Flip the shipped default for the audible critical alert to true.

### 'Use TNS sandbox' defaults OFF -- a first-time submission goes straight to the production Transient Name Server

`default` · settings (sequencer, science, phd2-guiding, plate-solving, weather-safety, notifications + all transports, integrations/plugins, annotations, observation log, auto-save, cloud-sync) · **not yet verified**

**What happens:** Settings > Science > Transient Name Server (TNS) provides Bot id, Bot name, Reporting group id, Data source id, Reporter name, Bot API key, and 'Use TNS sandbox -- Submit to the TNS sandbox instead of production (no public record)', which is OFF on a fresh profile. A user who fills in their bot credentials and submits a discovery therefore posts a public, permanent AT record to the real TNS on their very first attempt, with no confirmation step and no dry-run.

**Expected:** Sandbox ON by default. Require an explicit opt-out (ideally after one successful sandbox submission) before the app can write to production TNS. Submitting a bogus transient to the public registry is not something you can quietly undo, and the cost of the safe default is one toggle.

**Repro:** Fresh profile. Settings > SCIENCE > Science > scroll to 'Transient Name Server (TNS)'.

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/science_settings.dart:866-895 (_TnsSandboxRow) and ScienceSettings.tnsUseSandbox default`

**Proposed fix:** Default tnsUseSandbox to true and add a confirmation dialog on the first switch to production.

### The Notifications page is one ~3000px scroll with transport config 2500px below where you select it, and three duplicate ways to say the same thing

`ux` · settings (sequencer, science, phd2-guiding, plate-solving, weather-safety, notifications + all transports, integrations/plugins, annotations, observation log, auto-save, cloud-sync) · **not yet verified**

**What happens:** Top-to-bottom the page is: General, Notification Events, Critical events, Push to Mobile (+Test), Discord (legacy), Pushover (legacy), Routing master switch, ~25 per-event routing rows, Email (SMTP), Generic webhook, Pushover (routing matrix), Telegram, Discord (routing matrix), MQTT broker, Home Assistant. Selecting 'Email (SMTP)' as a routing transport near the top gives no link to the SMTP fields far below. Meanwhile 'Sequence complete' and 'Errors' under Notification Events duplicate 'Sequence completed' and 'Sequence failed' under Push to Mobile, which duplicate the 'Sequence Completed'/'Sequence Failed' rows in Per-event routing -- three separate controls for one intent, with no explanation of precedence. Reaching MQTT from the top took nine wheel-scroll batches.

**Expected:** Split transports onto their own settings leaf (or tabs: Events | Routing | Transports), and make the transport chips in the routing dialog link to their config. Collapse the three duplicate event lists into the routing matrix, which is the general case.

**Repro:** Settings > NOTIFICATIONS & REMOTE > Notifications and scroll from top to bottom.

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/notification_settings.dart:684 (embeds NotificationRoutingSettings, which itself appends _TransportsSection)`

**Proposed fix:** Register 'Notification transports' as its own settings leaf in settings_catalog.dart and have _CategoryEditorDialog's chips deep-link into it.

### Getting a target into the scheduler is a three-deep dead end; every empty state points at another empty state

`ux` · planner, scheduler, tonight, suggestions, weather, transients · **not yet verified**

**What happens:** Following the app's own instructions from a fresh profile: (1) Schedule > Target queue: 'Add a target to your catalog, then set how many frames you want in each filter' with a button labelled 'Open target catalog' — which navigates to Plan Tonight > Projects showing 'No projects yet'. (2) New Project > 'No targets yet — Add catalog targets to this project' > 'Add Target'. (3) The Add Target sheet says 'No targets in the catalog. Add targets to your library first, then attach them to this project' — with no button, link or hint for how. Meanwhile 'Add to observing list' on a recommendation card creates an observing_lists row that is a completely separate concept and never reaches the catalog (verified: observing_list_items got a row, targets stayed at 0). The only writer of the targets table in the whole app is framing_provider.saveTarget() — i.e. the real path is Recommendation > Send to Framing > save, which nothing tells you.

**Expected:** The button labelled 'Open target catalog' should open a target catalog with an 'Add target' action. Any of these three screens should be able to add a catalog target directly.

**Repro:** Fresh profile with a location: Plan Tonight > Schedule > 'Open target catalog' > New Project > Add Target.

**Where:** `packages/nightshade_app/lib/screens/planner/widgets/scheduler_tab_content/empty_state.dart (button label/route), packages/nightshade_app/lib/screens/planner/widgets/projects_tab_content/ (Add Target sheet); sole targets writer at packages/nightshade_core/lib/src/providers/framing_provider.dart:799`

**Proposed fix:** Put a 'Save to catalog' action on the recommendation card next to 'Add to observing list' (it already has the RA/Dec/type/mag/size it needs), and give the Add Target sheet a 'New target...' row that opens the same form. Also reconcile the three overlapping concepts — observing lists, catalog targets, project targets — or at least explain them in the empty states.

### PHD2 cannot be configured during onboarding unless PHD2 is already running

`ux` · imaging, framing, guiding · **not yet verified**

**What happens:** The onboarding guider step offers native guiders ('Sim (0)' - there is no simulated guider device) plus a PHD2 block with Host=localhost and Port=4400 prefilled and a Test button. Test correctly reports 'No response on localhost:4400. Is PHD2 running with "Enable Server" turned on?'. But there is no way to SELECT PHD2 as the guider - pressing Next with the prefilled host/port silently discards it, and the review step shows 'Guider - not set -'. So a user setting up indoors before their PHD2 install/first launch cannot record their guider choice at all, and the Guiding screen then greets them with a whole panel of dead-looking controls.

**Expected:** Let the user commit host/port without a successful test (Test stays available as a verification), and record the guider as PHD2 in the profile.

**Repro:** 1. Do not run PHD2. 2. Onboarding -> Guider step -> press Test (honest error) -> press Next. 3. Review step shows 'Guider - not set -'.

**Where:** `onboarding guider step + packages/nightshade_app/lib/screens/guiding/guiding_screen_parts/desktop_sections.dart:230-245`

**Proposed fix:** Add an explicit 'Use PHD2' selection on the onboarding step that persists host/port regardless of Test result. Separately: discovery reports 'Guider: 1 devices' in the log but the onboarding guider list shows 'Sim (0)' - worth reconciling.

### 'Focus Model' settings page is fully built but is not mounted anywhere - the temp-compensation model viewer is unreachable, and the settings index is out of lock-step

`missing` · settings (imaging / calibration / calibration-library / dark-library / captured-images / image-grading / adaptive-exposure / adaptive-conditions / preflight / autofocus / focus model) · **not yet verified**

**What happens:** focus_model_settings.dart (310 lines: pulls the appliance's temperature-compensation model - slope, R2, reliability - over REST and offers a Clear action) has no SettingsSectionDef. buildSettingsGroups' Advanced group builds logs/backup/updates/rig-catalogs/captured-images/replay-debug only. The alias map sends 'focus-model' deep links to 'autofocus', whose page (driven end-to-end) contains only AUTOFOCUS + PREDICTIVE AUTOFOCUS. Separately, _structuralGroups still lists 'focus-model' as an Advanced section key, so the derived kSettingsSectionIndex assigns 'replay-debug' an index one higher than its real catalog position.

**Expected:** Either surface the page (Advanced > Focus Model, gated on a remote backend the way Captured Images is) or delete it and drop the alias and the phantom _structuralGroups entry. Also: searching 'focus model' in Settings matches the Autofocus section (its keywords include 'focus model' and 'temperature compensation'), landing the user on a page that has neither - and app_settings still stores temp_compensation and temp_coefficient (-12.0) with no UI anywhere in the Autofocus page to edit them.

**Repro:** Settings > Equipment > Autofocus; scroll the entire page (two sections: AUTOFOCUS, PREDICTIVE AUTOFOCUS, ending at 'Learned models'). No focus-model / temperature-compensation controls. Settings search 'gain' likewise returns Imaging, which has no gain control.

**Where:** `packages/nightshade_app/lib/screens/settings/settings_catalog.dart:591-678 (Advanced group), :686-695 (aliases), :786-817 (_structuralGroups lists 'focus-model'); packages/nightshade_app/lib/screens/settings/widgets/focus_model_settings.dart:19`

**Proposed fix:** Add the section to the Advanced group next to captured-images, remove 'focus-model' from kMergedSectionAliases, and delete the phantom key from _structuralGroups so kSettingsSectionIndex matches the built catalog.

### Settings > Imaging is called 'Default capture settings' and is indexed for gain/offset/binning/exposure, but contains none of them

`ux` · settings (imaging / calibration / calibration-library / dark-library / captured-images / image-grading / adaptive-exposure / adaptive-conditions / preflight / autofocus / focus model) · **not yet verified**

**What happens:** The Imaging section renders exactly two rows: a read-only 'Archive format: FITS - 16-bit' and 'File naming pattern'. Its catalog keywords are ['exposure','gain','offset','binning','file format','fits','frame naming'], so searching 'gain' in Settings returns Imaging (plus Help & Tutorials, Image Grading, Science) - and none of those pages has a gain control. Default gain/offset/binning/cooling actually live in the onboarding 'Camera defaults' step / equipment profile, which the search never points at.

**Expected:** Either surface the profile's camera defaults on this page (or a deep link to Equipment Profiles), or strip the false keywords so search does not promise a control that isn't there.

**Repro:** Settings > search 'gain' -> Imaging is offered; open Imaging -> two rows, no gain.

**Where:** `packages/nightshade_app/lib/screens/settings/settings_catalog.dart:335-349 (keywords); packages/nightshade_app/lib/screens/settings/widgets/imaging_settings.dart:42-87 (two rows only)`

**Proposed fix:** Add a 'Capture defaults' section that surfaces the active profile's gain/offset/binning/cooling read-only with an 'Edit in Equipment Profiles' action, and keep the keywords.

### Backlash IN ships at 350 steps with method 'Overshoot' - an unmeasured, asymmetric mechanical default applied to every focuser

`default` · settings (imaging / calibration / calibration-library / dark-library / captured-images / image-grading / adaptive-exposure / adaptive-conditions / preflight / autofocus / focus model) · **not yet verified**

**What happens:** A freshly seeded profile has af_backlash_comp_method='Overshoot', af_backlash_in='350', af_backlash_out='0'. Backlash is a per-focuser mechanical property the app cannot know; 350 steps is comparable to the entire default AF sweep (step size 50 x 2 x initial offset 4 = 400 steps) and can exceed the usable travel of small stepper/Crayford focusers, so every AF move is perturbed - or clamped/errored - out of the box. The asymmetry (IN 350, OUT 0) has no physical justification either.

**Expected:** Ship 0/0 (no compensation) and provide a backlash-measurement helper, the way NINA does. A non-zero default silently changes where the focuser lands on the first night on unknown hardware.

**Repro:** Fresh profile (drive_linux.py start --fresh), Skip onboarding, Settings > Equipment > Autofocus. Also sqlite3 .../nightshade.db "select key,value from app_settings where key like 'af_backlash%'".

**Where:** `packages/nightshade_core/lib/src/database/database/default_settings.dart:90-92`

**Proposed fix:** Default af_backlash_in and af_backlash_out to 0, and add a 'Measure backlash' action next to the Backlash compensation dropdown.

### Autofocus and Predictive AF express three key thresholds in raw focuser steps the app cannot interpret

`ux` · settings (imaging / calibration / calibration-library / dark-library / captured-images / image-grading / adaptive-exposure / adaptive-conditions / preflight / autofocus / focus model) · **not yet verified**

**What happens:** Step size (50), Backlash IN (350) and Predictive AF 'Drift threshold (steps)' (200) are all raw focuser-step counts with no relation to the critical focus zone, steps-per-micron, or the focuser's travel range - none of which the page shows. On a focuser whose CFZ is ~30 steps the 200-step drift gate never warns; on one with 500 steps of total travel it always warns. Range maxima are equally uninformed (step size up to 10000, backlash up to 10000).

**Expected:** Show the connected focuser's max position/step size next to these fields and express drift as a fraction of the measured V-curve half-width (or in microns via a steps-per-micron input), so the numbers mean something. NINA and SGP both derive step size from a measured curve rather than a blind default.

**Repro:** Settings > Equipment > Autofocus (right column) and scroll to PREDICTIVE AUTOFOCUS > 'Drift threshold (steps)'.

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/autofocus_settings.dart:306-312 (step size 1..10000), :395-413 (backlash 0..10000); packages/nightshade_core/lib/src/database/database/default_settings.dart:78, and predictive_af.drift_threshold_steps default 200`

**Proposed fix:** Add the connected focuser's position range and a steps-per-micron field to the page, and label the drift threshold in both steps and the equivalent fraction of the fitted curve.

### Adaptive Exposure lengthens subs when the sky gets brighter - the opposite of the sub-exposure rule every other tool applies

`ux` · settings (imaging / calibration / calibration-library / dark-library / captured-images / image-grading / adaptive-exposure / adaptive-conditions / preflight / autofocus / focus model) · **not yet verified**

**What happens:** The page states 'Lengthens or shortens each frame automatically when the sky is brighter or darker than your reference, so signal-to-noise stays roughly constant'. The implementation is ratio = 10^((mag_ref - mag_live)/2.5), so a brighter sky (lower mag) multiplies the exposure up. The module's own worked example admits the outcome: 60 s nominal at ref 21.5 becomes 499 s under a Bortle-6 19.2 sky, 'far too long for backyard tracking', and is then clamped. With shipped defaults (ref 21.5, max 600 s) any sky more than ~1 mag brighter than reference pins every sub at the 600 s ceiling all night. SharpCap/NINA/Dr. Glover's rule is the inverse: t proportional to 1/sky-rate, i.e. shorter subs under a brighter sky, to keep the sky from filling the well.

**Expected:** Either invert the scaling to the standard 'swamp read noise / fixed sky ADU' rule, or rename the feature and say plainly that it maximises per-frame SNR at the cost of longer, more loss-prone subs, and default it to a target sky-ADU rather than a magnitude reference.

**Repro:** Settings > Imaging > Adaptive Exposure, read the description and 'How it works' card; compare with the formula at the named line.

**Where:** `native/nightshade_native/sequencer/src/scheduling/adaptive_exposure.rs:332-336 and the module doc at :39-59`

**Proposed fix:** Add a 'target sky background (ADU or e-)' mode that scales t inversely with sky rate, make it the default, and keep the constant-SNR mode as an explicit opt-in.

### Image Grading thresholds cannot be edited until the master toggle is armed, so enabling it arms scale-blind defaults immediately

`ux` · settings (imaging / calibration / calibration-library / dark-library / captured-images / image-grading / adaptive-exposure / adaptive-conditions / preflight / autofocus / focus model) · **not yet verified**

**What happens:** With the shipped default (grading OFF) all five threshold fields render at 0.5 opacity inside an IgnorePointer, so they cannot be edited. The only way to tune them is to switch grading ON first - which immediately arms HFR absolute 3.50 px, eccentricity 0.70, star-count min 10 and 'pause after 3 consecutive rejects'. HFR in pixels is pixel-scale dependent: at ~0.4"/px (2000 mm, 3.76 um) 2.5" seeing gives HFR ~6 px, so every frame is rejected and the sequence pauses after three - a lost night from one switch flip.

**Expected:** Allow the thresholds to be edited while the master toggle is off (grey the section header, not the inputs), and express the HFR gate in arcseconds (or as a multiple of the measured baseline) so it is scale-independent. Keeping only the relative 'HFR baseline percent' check armed by default would be safer.

**Repro:** Fresh profile, Settings > Imaging > Image Grading: try to click 'HFR absolute (pixels)' with the master toggle off - the field does not take focus.

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/image_grading_settings.dart:553-558 (Opacity + IgnorePointer keyed on `enabled`), defaults at packages/nightshade_core/lib/src/providers/settings_sections/app_settings_state.dart:611-616`

**Proposed fix:** Drop the IgnorePointer, keep only the visual dimming, and switch the absolute HFR gate to arcseconds computed from the optical train.

### Default file naming pattern puts every frame of every target and every night in one flat directory, and the field documents 5 of 18 variables

`default` · settings (imaging / calibration / calibration-library / dark-library / captured-images / image-grading / adaptive-exposure / adaptive-conditions / preflight / autofocus / focus model) · **not yet verified**

**What happens:** The shipped default is '$TARGET_$FILTER_$DATE_$SEQ' with no '/' - so lights, and every target and every session, land side by side in the capture root. The row's subtitle advertises only '$TARGET, $FILTER, $DATE, $SEQ, $EXPOSURE'; the pipeline supports 18 tokens including $FRAMETYPE, $FRAMENUM, $EXPTIME, $TIME, $DATETIME, $GAIN, $OFFSET, $TEMP, $BINNING, $CAMERA, $TELESCOPE, $SEQUENCE, $SESSION. Nothing tells the user that '/' creates subdirectories - the single most-used feature of a naming pattern - even though the code's own default constant is '$TARGET/$FRAMETYPE/$TARGET_$FILTER_$EXPTIME_$FRAMENUM'.

**Expected:** Ship '$TARGET/$FRAMETYPE/$TARGET_$FILTER_$EXPTIME_$FRAMENUM' (the constant already in NamingPattern), list all supported variables in a popover, and state that '/' creates folders. NINA and SGP both default to target/date/filter folders.

**Repro:** Fresh profile, Settings > Imaging: pattern reads '$TARGET_$FILTER_$DATE_$SEQ', subtitle lists 5 variables.

**Where:** `packages/nightshade_core/lib/src/database/database/default_settings.dart:21 and packages/nightshade_core/lib/src/providers/settings_sections/app_settings_state.dart:485 (default); packages/nightshade_app/lib/screens/settings/widgets/imaging_settings.dart:66-67 (subtitle); packages/nightshade_core/lib/src/models/imaging/imaging_models/naming_and_autofocus.dart:10,34-51 (the richer default and full variable list)`

**Proposed fix:** Change the seeded default to the subdirectory pattern and replace the 5-variable subtitle with a full variable reference plus a live filename preview.

### Dark Library has no way to add or import dark frames

`missing` · settings (imaging / calibration / calibration-library / dark-library / captured-images / image-grading / adaptive-exposure / adaptive-conditions / preflight / autofocus / focus model) · **not yet verified**

**What happens:** The page offers Auto dark subtraction, temperature tolerance, statistics, Clean Orphans, Clear Library, Frame Groups and All Entries. The empty state says 'Capture dark or bias frames to populate the library' but there is no button anywhere on the page to start a dark-capture run, and no way to import an existing library of masters from disk. Everything on the page is destructive or read-only.

**Expected:** An 'Import darks...' file/folder picker and a 'Build dark library' action that seeds a dark-capture sequence, next to Clean Orphans / Clear Library. Every imager arriving from NINA/SGP already has a dark library on disk.

**Repro:** Settings > Equipment > Dark Library, scroll the whole page.

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/dark_library_settings.dart (Library Management section: only Clean Orphans + Clear Library)`

**Proposed fix:** Add Import and Build actions to the Library Management row.

### The Night Outlook ranks by geometry alone: a mag-13.2, 1.9-arcmin galaxy outranks M31 and M27

`ux` · planner, scheduler, tonight, suggestions, weather, transients · **not yet verified**

**What happens:** On 2026-08-01 at 40N/-75W with 1114 candidates, the ranking was NGC6015 (mag 11.2, 5.8') score 76 and IC1231 (mag 13.2, 1.9') score 76 at the top, with M31 at 64 and M27 at 68. Filtering to Type=Nebula put Cat's Eye (54 arcsec) at 75 and NGC6210 (30 arcsec) at 74 in the top two. The score is peak altitude + moon distance + window hours + transit proximity; nothing in it accounts for whether the object is worth imaging or fits the user's sensor. Notably the app HAS the information: the equipment profile gives a 0.69° x 0.39° FOV, and the Tonight one-tap ranker does use it ('Well-framed at 62% of FOV' for M92). So the two 'best target tonight' rankers on the same product disagree — Plan Tonight says NGC6015/M92 depending on state, Tonight says M92 with a framing term.

**Expected:** The default ranking should surface targets a user can actually image with their rig. A 30-arcsec planetary nebula at 1.29 arcsec/px is 23 pixels across.

**Repro:** Site 40/-75, no equipment profile: Plan Tonight > Recommendation > scroll the candidate list. Then 'Type: any' > Nebula > Apply.

**Where:** `packages/nightshade_planetarium/lib/src/planning/target_scoring.dart:447-460 (altScore, moonScore, transitScore, darknessScore — no size or magnitude term)`

**Proposed fix:** Add a framing term to the composite score: fraction of the short sensor axis the target subtends, peaking around 0.2-0.7 and falling off hard below ~0.05 (unresolvable) and above 1.0 (needs a mosaic), plus a mild surface-brightness/magnitude term. Reuse the same computation the Tonight screen already does so the two rankers agree. Failing that, default the 'Size' filter to the FOV-fitting range instead of 'any' — the app knows the FOV the moment a profile exists.

### PolarAlignmentNode cannot be added to a sequence - it is absent from the palette

`missing` · sequencer · **not yet verified**

**What happens:** PolarAlignmentNode is a full instruction node (model at instruction_nodes/alignment_and_dome.dart:319 with copyWith/serialization) but there is no palette entry for it in any category. Searching 'polar' in the node palette returns zero results, so a user cannot put polar alignment into an automated startup sequence; the only route is the standalone /polar-alignment screen the toolbar compass icon navigates to.

**Expected:** Either a 'Polar Alignment' entry under Mount, or - if the node is deliberately retired - remove it from the model so a sequence file containing one does not deserialize into an unreachable node.

**Repro:** Sequencer > Builder > Nodes tab > type 'polar' in the search field. Zero results. Scroll the full uncollapsed palette: categories are Target, Imaging, Science, Guiding, Mount, Dome, Flat Panel, Focus, Camera, Logic, Timing, Utilities - none contains it.

**Where:** `packages/nightshade_core/lib/src/providers/sequence/node_palette.dart:288-322 (Mount category)`

**Proposed fix:** Add the entry to the Mount category next to Meridian Flip.

### Scheduler moon avoidance is illumination-only; the app has moon separation everywhere else

`missing` · planner, scheduler, tonight, suggestions, weather, transients · **not yet verified**

**What happens:** The Add-constraint wizard's Moon avoidance step offers exactly one parameter: 'max illumination', default 30%, described as 'Skip this target when the moon's illumination is above a set percentage.' The saved payload is {"max":0.3} with kind moonIlluminationMax. Meanwhile the planner filter bar has 'Minimum moon separation' in degrees, every recommendation card prints a separation ('Moon 93°', 'Moon 46°'), and the score already uses _scoreMoonDistance(moonDist, moonIllumination).

**Expected:** The hard constraint a user actually wants is separation-based, ideally illumination-scaled (NINA's moon-avoidance lorentzian; SGP's moon distance). Illumination alone rejects a 90%-moon target 150 degrees away and accepts a 40%-moon target 10 degrees away — exactly backwards in both cases.

**Repro:** Plan Tonight > Schedule > click a queued target > Add constraint > Moon avoidance > Next.

**Where:** `packages/nightshade_app/lib/screens/scheduler/widgets/target_constraints_editor/wizard_kind_step.dart and constraint_fields.dart; TargetConstraintKind in nightshade_core`

**Proposed fix:** Add a moonSeparationMin constraint kind (degrees) and, better, a combined 'avoid when separation < f(illumination)' with the standard width/decay parameters. The wizard also has no minimum-altitude / maximum-airmass / meridian-side constraint, which are the three most-used per-target constraints in NINA and SGP.

### 'Best Targets Tonight' ranks purely by transit altitude, so it recommends obscure galaxies that transit in daylight and pass through the zenith

`ux` · planetarium + your_sky + constellation · **not yet verified**

**What happens:** The five 'Best Targets Tonight' on 2026-08-02 at 40N/105W were IC311, NGC5311, NGC5313, IC4155 and NGC6685 - all listed at '90 deg', with transit times 09:24, 19:58, 19:59, 19:13 and 00:48. The same panel, six rows above, states Sunset 22:16 and Sunrise 07:56 and Astro Dusk 00:00 / Astro Dawn 06:12. So four of the five 'best targets tonight' reach their best moment in broad daylight, and the panel says so on its own face. The tooltip is honest about why - 'Objects sorted by transit altitude (>30 deg)' - but that sort has two further consequences: (a) it is maximised at declination = latitude, so the whole list is objects that pass through the ZENITH, which is the one place a German equatorial cannot track through and an alt-az mount cannot slew through; (b) it ignores the Moon entirely, and the panel three rows above says the Moon is 84% Waning Gibbous, so five faint galaxies are the worst possible choice for this night. The app grades these targets itself and disagrees: selecting NGC6685 and opening the sidebar's Info tab shows a score badge of '20 - Poor', for an object the Tonight tab has just called one of the five best targets tonight. It is a magnitude 14.6 galaxy 0.9' x 0.7' across.

**Expected:** Rank by integration hours available inside astronomical darkness above a user altitude floor, then break ties with moon separation and object brightness/size. NINA's Sky Atlas and the ASIAIR both filter to the dark window by default; SGP's framing/target list at minimum shows the visible-hours bar. A minimum of: drop anything whose transit is outside the dark window, and show 'X.Xh above 30 deg in darkness' instead of a peak altitude that every dec-40 object shares.

**Repro:** Plan Tonight > Planetarium > right sidebar > Tonight tab > scroll to 'Best Targets Tonight' and compare the transit times against the Evening/Morning Twilight table in the same panel.

**Where:** `packages/nightshade_planetarium/lib/src/providers/planetarium_providers/mosaic_targets.dart:190`

**Proposed fix:** Score on darkness-window integration time with a moon-separation penalty (the repo already has _scoreMoonDistance in planning/target_scoring.dart - reuse it), and surface hours-in-darkness rather than peak altitude.

### With no data at all, Analytics > Session invents a session card called "Quick Capture" instead of an empty state

`ux` · analytics (Session, History, Projects, Equipment Stats, Science, Diagnostics) + science screen · **not yet verified**

**What happens:** With imaging_sessions and captured_images both emptied (a brand-new user's state), Analytics opens on Session showing a card headed 'Quick Capture / Standalone snapshots taken outside sequences' with DURATION '—', EXPOSURES 0, INTEGRATION 'No data', MEDIAN HFR 'No data', a 'Captured Images' block reading 'No images captured in this session', and four empty charts. There is no session-picker bar and nothing that says the user has not imaged yet. The History tab in the same state does it properly: 'No session history / Complete an imaging session to see history here'.

**Expected:** The first tab of Analytics is the first thing a new user sees, and it should say 'No sessions yet - your first night will appear here' (the Dashboard already uses that wording) rather than render a fabricated session with four blank charts. Presenting a named card with a duration and an exposure count for something that does not exist is the same class of claim as the rest of this report.

**Repro:** Empty imaging_sessions and captured_images (or use a fresh profile), restart, open Analytics.

**Where:** `packages/nightshade_app/lib/screens/analytics/analytics_screen/session_tab.dart (the synthetic standalone/quick-capture session is used as the fallback when no real session exists)`

**Proposed fix:** Only show the Quick Capture bucket when it actually contains standalone frames; otherwise render the same empty state History uses.

### No single 'safe the rig' action: at 2am with clouds coming in, stopping the run and parking the mount are on two different tabs with no shared shortcut

`ux` · mobile operation - companion dashboard shell, mount/sequencer/log/settings tabs, session picker and session replay, driven disconnected on the ns_test_api35 emulator · **not yet verified**

**What happens:** The companion is the surface you hold when you are in bed and the sky turns. The two things you need in that moment - stop the sequence and park the mount - live in two separate bottom-tab destinations with no common entry point. Stopping means: Sequencer tab, then press-and-hold the Stop button. Parking means: switch to the Mount tab, then reach _ControlsRow, which is the FOURTH child of that tab's ListView (after _PositionCard, _SlewRateSelector and the _Dpad), so on a phone it is below the fold and needs a scroll first. That is a minimum of tab-switch + scroll + tap, with a tab switch in the middle, and nothing anywhere in the persistent chrome offers 'stop and park'. The AppBar has room - it currently carries only a battery percentage, a network dot and a 2-item overflow menu whose contents (Saved servers, Session history) are both cold-path.

**Expected:** A single persistent 'Safe the rig' affordance reachable from every tab - press-and-hold, destructive styling, one gesture - that stops the sequence and parks the mount in the correct order. Put it in the AppBar action slot or as a FAB. The press-and-hold pattern the Stop button already uses is the right interaction for this (it survives a pocket tap); it just needs to be reachable without first navigating to the right tab.

**Repro:** 1. Companion UI enabled; Skip Connection. 2. Note the AppBar actions on any tab: battery '100%' at [678,165][828,238] and 'More' at [954,139][1080,265]. 3. Open More: only 'Saved servers' [471,170][1059,296] and 'Session history / replay' [471,296][1059,422]. 4. Sequencer tab: the Stop control is `Button: Press and hold to stop the sequence / Stop [DISABLED] [551,2012][1038,2127]` - present only on this tab. 5. Mount tab with no mount: only the empty state renders.

**Where:** `apps/mobile/lib/screens/dashboard/mobile_dashboard_screen.dart:129-193 (AppBar actions + overflow); apps/mobile/lib/screens/dashboard/tabs/mount_tab.dart:258-279, 805; apps/mobile/lib/screens/dashboard/tabs/sequencer_tab.dart (Stop)`

**Proposed fix:** Add a third AppBar action - a red shield/park glyph - wired to a press-and-hold that calls sequencer stop then mount park, with a confirmation toast naming both outcomes. Failing that, promote Park into _PositionCard so it is above the fold on the Mount tab and costs one tab-switch plus one tap.

### Science tab defaults to the newest session even when it has no science data

`ux` · analytics (Session, History, Projects, Equipment Stats, Science, Diagnostics) + science screen · **not yet verified**

**What happens:** After adding a 2-frame session (Night E - M13, no photometry, no solves) that started later than the 120-frame photometry night, the Science tab opened on Night E and showed '0% - 0 of 2 solved', 'Warming up', and the empty-state 'Turn pretty pictures into real measurements'. The 120-frame calibrated night was two clicks away in a dropdown.

**Expected:** The default should be the most recent session that actually has science products. Otherwise a user who took two test frames after a long photometry run comes back to an empty Science tab.

**Repro:** Analytics > Science with sessions 4 (120 frames, solved+photometry) and 5 (2 frames, nothing) where 5 starts later.

**Where:** `packages/nightshade_app/lib/screens/analytics/widgets/science_analytics_tab.dart session selection`

**Proposed fix:** Default to the newest session with >0 photometry measurements or >0 solved frames; fall back to newest overall.

### The stack result can only leave the app as 8-bit PNG/JPEG -- there is no linear FITS or 16-bit TIFF export

`missing` · science / session_review / stack_result / first_light · **not yet verified**

**What happens:** The stack result screen offers exactly four actions: Export PNG, Export JPEG, Share Card, AstroBin. Export PNG writes an 8-bit RGBA PNG (verified: 512x512, bit depth 8, colour type 6) of the STF-stretched view; Export JPEG writes an 8-bit JPEG. The integrated data itself -- which the engine holds as u16 and which the app already persists as a raw buffer for the viewer -- can never be taken out of Nightshade.

**Expected:** A 'Save FITS' (32-bit float or 16-bit, unstretched, with the acquisition header) or 16-bit TIFF action. Every user of NINA, SGP or the ASIAIR takes the integration into PixInsight, Siril, APP or Photoshop for the actual processing; an 8-bit screen-stretched PNG is a preview, not a deliverable. As shipped, Stack & Share is a dead end: the app is the only place the stack can live.

**Repro:** 1. Complete a Stack & Share run. 2. Read the four buttons in the header; there is no other export surface. 3. `drive_linux.py tree` on the result screen lists exactly: Export PNG, Export JPEG, Share Card, AstroBin, and the Stretch dropdown.

**Where:** `packages/nightshade_app/lib/screens/stack_result/stack_result_screen.dart (action row)`

**Proposed fix:** Add 'Save FITS' writing the unstretched integration with EXPTIME = total integration, STACKCNT, FILTER and the acquisition keywords; the native side already writes FITS elsewhere.

### The 'AstroBin' export produces JSON + Markdown, not the acquisition CSV AstroBin imports, and omits the observation date entirely

`ux` · science / session_review / stack_result / first_light · **not yet verified**

**What happens:** Pressing 'AstroBin' asks for one filename and then writes two sibling files with its own extensions -- rsr-astrobin.json and rsr-astrobin.md -- ignoring the .csv extension I typed. The JSON is {subjectType, integrationSecs, integrationHms, frames, perFrameExposureSecs, title, filter} and the Markdown is a four-line summary. AstroBin's acquisition importer takes a CSV whose columns are date, number, duration, filter, binning, gain, sensorCooling, darks, flats, bias, bortle, meanSqm, meanFwhm, temperature. Neither exported file can be imported; the user has to retype the numbers into AstroBin's form. The export also has no `date` field at all, and AstroBin requires a date on every acquisition row.

**Expected:** Emit AstroBin's acquisition CSV (long-exposure format) with the session date filled in from the stack's frames, so the button does what its name promises: one file the user drops into AstroBin's importer. Keep the Markdown as a secondary 'copy for a forum post' output if desired, and honour the extension the user chose in the save dialog.

**Repro:** 1. Stack result > 'AstroBin'. 2. Save as /tmp/ns-audit/rsr-astrobin.csv. 3. ls -> rsr-astrobin.json and rsr-astrobin.md exist; the .csv does not.

**Where:** `packages/nightshade_core/lib/src/services/stack_share_export_service.dart:367 buildAstroBinMetadata (no date field on AstroBinExportMetadata); the two-file write in the same service.`

**Proposed fix:** Add `date` (from the earliest/latest frame captured_at) and emit CSV as the primary format.

### 'Recover Sequence?' modal has no way out except Discard (destructive) or Resume (acts)

`ux` · analytics (Session, History, Projects, Equipment Stats, Science, Diagnostics) + science screen · **not yet verified**

**What happens:** The recovery dialog offers only Discard and Resume; Escape does not dismiss it and there is no barrier dismiss. The Incomplete Sessions dialog on the same startup path does offer 'Decide Later'.

**Expected:** A user who wants to look at the sequence before deciding is forced to destroy the checkpoint or start a run. Match the sibling dialog and offer 'Decide later'.

**Repro:** Launch with a checkpoint present; press Escape - dialog remains.

**Where:** `packages/nightshade_app/lib/screens/shell/app_shell.dart startup checkpoint flow`

**Proposed fix:** Add a third action that leaves the checkpoint on disk and closes the dialog, as the Incomplete Sessions dialog does.

### Equipment Stats: half of the tab is permanently "Not tracked"

`ux` · analytics (Session, History, Projects, Equipment Stats, Science, Diagnostics) + science screen · **not yet verified**

**What happens:** With a full session history seeded, Mount shows Total Slews / Total Tracking Time / Meridian Flips = Not tracked, Focuser shows Total Movements = Not tracked, Guider shows Total Guide Time and Star Lost Events = Not tracked. Six of the twelve figures are hardcoded strings.

**Expected:** An "Equipment Stats" tab whose Mount card contains no numbers at all is not worth a tab. Several of these are already derivable from data the app stores: meridian flips are recorded as sequencer decisions/narrator events, guide time and star-lost from guide_rms_history, focuser movements from focus_models/autofocus runs.

**Repro:** Analytics > Equipment Stats with 3 sessions and 27 frames seeded.

**Where:** `packages/nightshade_app/lib/screens/analytics/analytics_screen/equipment_stats.dart:137-139,150,156,162`

**Proposed fix:** Either wire the six to the tables that already hold the data, or drop the Mount card and rename the tab to what it measures (camera + focus + guiding quality).

### "Claim all pending" locks every remaining panel of a club mosaic for 12 hours in one click, with no per-panel claim in the UI

`ux` · dashboard, mosaic, polar_alignment, flat_wizard, collaborative_sky · **not yet verified**

**What happens:** The only claim affordance on the mosaic project screen is 'Claim all pending'. One click took all six panels (verified: 6 of 6 rows in the hub's collaborative_mosaic_panels now carry a claim_token, all assigned to AuditRig) and held them for the hub's full 12-hour claimTtl (mosaic_broker_service.dart:55). The panel list on the collaborative detail screen shows each panel's claim state but offers no per-panel claim/release control, so a member with one clear night has no way to take just the two panels they can actually shoot, and no way to hand back the rest short of releasing the whole project.

**Expected:** Claim per panel from the panel card (with a 'claim N panels' bulk option), show a running 'you hold X of Y', and offer Release on each claimed panel. A 12-hour blanket claim by the first rig to open the project defeats the point of splitting the mosaic across a club.

**Repro:** Publish a mosaic to a hub -> Claim all pending -> sqlite3 /tmp/ns-audit/hub/hub.db 'select count(*), sum(claim_token is not null) from collaborative_mosaic_panels' -> 6|6.

**Where:** `packages/nightshade_app/lib/screens/mosaic/mosaic_project_screen.dart (collaborative section) and packages/nightshade_app/lib/screens/collaborative_sky/collab_mosaic_detail_screen.dart (panel rows)`

**Proposed fix:** Add per-panel claim/release buttons to the panel rows and change the bulk action to 'Claim N panels...' with a count picker.

### Captured Images thumbnails are inert - tapping a frame does nothing anywhere in Analytics

`ux` · analytics (Session, History, Projects, Equipment Stats, Science, Diagnostics) + science screen · **not yet verified**

**What happens:** Clicking a thumbnail in the Session tab and in the session detail dialog produces no reaction: no preview, no grade action, no metadata. ImageThumbnailStrip takes an onImageTap callback and no caller in the app passes one.

**Expected:** A gallery of the night frames is the obvious place to enlarge a frame, read its FITS header, or accept/reject it. NINA and the ASIAIR both open the frame from the thumbnail. The ImageGraderDialog that would do this exists but is reachable only from the Science tab.

**Repro:** Analytics > Session > Night A - M31; click any thumbnail. Nothing happens. Same in History > session detail.

**Where:** `packages/nightshade_app/lib/screens/analytics/widgets/image_thumbnail_strip.dart:42/189, session_tab.dart:307, session_detail_dialog.dart:266`

**Proposed fix:** Pass onImageTap that opens ImageGraderDialog (or a frame inspector) from both call sites.

### The Captured Images thumbnail rail cannot be scrolled with an ordinary mouse wheel and has no scrollbar

`ux` · analytics (Session, History, Projects, Equipment Stats, Science, Diagnostics) + science screen · **not yet verified**

**What happens:** With 16 frames in the session only 12 thumbnails fit. Pointing at the rail and turning the wheel scrolls the PAGE, not the rail; the remaining 4 frames stay off-screen. Only a horizontal-wheel / shift+wheel gesture moved it. There is no scrollbar and no left/right affordance.

**Expected:** On desktop, a horizontal rail must be reachable with the wheel a user actually has - either map vertical wheel to the rail, or show a Scrollbar and arrow affordances. A 200-frame night is unusable otherwise.

**Repro:** Analytics > Session > Night A - M31; hover the Captured Images rail; wheel down (page scrolls). Shift+wheel to reveal the trailing frames.

**Where:** `packages/nightshade_app/lib/screens/analytics/widgets/image_thumbnail_strip.dart:178 ListView.builder(scrollDirection: Axis.horizontal) with no Scrollbar and default desktop ScrollBehavior`

**Proposed fix:** Wrap in a Scrollbar(thumbVisibility: true) and add a Listener that maps PointerScrollEvent.dy onto the horizontal controller, as the app already does for the sequencer node rail.

### Starting a co-imaging session makes you type RA and Dec by hand with no target lookup

`ux` · dashboard, mosaic, polar_alignment, flat_wizard, collaborative_sky · **not yet verified**

**What happens:** 'Start co-imaging session' asks for Target name, Center RA (degrees), Center Dec (degrees) and a radius as four free-text fields, with placeholder examples ('e.g. NGC 7000', 'e.g. 314.75 or 20:58:47'). There is no catalog search, no 'use the mount's current position', and no 'use the target I already have in Plan Tonight / the sequencer', even though the app has a 1082-target catalog search on the adjacent Recommendation tab and a connected mount reporting RA/Dec.

**Expected:** A target field that searches the installed catalogs and fills RA/Dec (as Plan Tonight and Framing already do), plus a 'from mount' and 'from current sequence target' shortcut.

**Repro:** Plan Tonight -> Discover -> Collaborate -> Start session.

**Where:** `packages/nightshade_app/lib/screens/collaborative_sky/ (start-session dialog)`

**Proposed fix:** Reuse the catalog autocomplete widget from the planner in this dialog.

### Sky Flats has no twilight timing, no sky-brightness gate and no anti-solar pointing - the three things sky flats are actually about

`ux` · dashboard, mosaic, polar_alignment, flat_wizard, collaborative_sky · **not yet verified**

**What happens:** The Sky Flats tab offers exactly Dawn/Dusk, a filter checklist with Auto-Order, a histogram target, a tolerance and a frame count. There is no twilight clock (the app already computes civil/nautical/astronomical twilight for the site - the Tonight card shows it), no 'start when the sky reaches N ADU' trigger, no window countdown, and no slew to the anti-solar point at ~75 deg altitude, which is where sky flats have to be taken. The user is left to eyeball when to press Start and to aim the scope themselves.

**Expected:** Show 'Dusk flats: civil twilight 20:31, usable window 20:35-20:58 (23 min)' with a countdown and an arm-at-time option; offer 'slew to flat spot' (anti-solar azimuth, ~75 deg alt) as NINA's Sky Flat wizard does; auto-start when the measured sky ADU enters the target band.

**Repro:** Flat Wizard -> Sky Flats. Observing location set to 40N/-105E; nothing on the tab refers to the sun, twilight or time.

**Where:** `packages/nightshade_app/lib/screens/flat_wizard/ (Sky Flats tab)`

**Proposed fix:** Bind the tab to the existing twilight service and add an arm/countdown plus a flat-spot slew action.

### Flat wizard Frame Count can only be changed one click at a time - 27 clicks to go from the default 30 to 3

`ux` · dashboard, mosaic, polar_alignment, flat_wizard, collaborative_sky · **not yet verified**

**What happens:** Frame Count is a -/+ stepper with a step of 1 and the number itself is not editable: I clicked the number, pressed Ctrl+A, typed '3' and pressed Enter, and it stayed at 30. Getting from the default 30 to 5 took 25 clicks on '-'. There is no keyboard entry, no long-press acceleration, and no presets.

**Expected:** Make the number a text field (as the Exposure and Gain fields elsewhere in the app are), or add hold-to-repeat and 5/10-step presets. NINA and SGP both take a typed count.

**Repro:** Flat Wizard -> Quick Capture -> click the Frames number, Ctrl+A, type 3, Enter -> it stays 30. Then click '-' and watch it fall by 1 per click.

**Where:** `packages/nightshade_app/lib/screens/flat_wizard/tuning_controls.dart`

**Proposed fix:** Replace the read-only number with a NightshadeNumberField; keep the steppers for fine adjustment.

### All-Sky alignment shows a drift-derived error with no indication of how long the drift baseline is

`ux` · dashboard, mosaic, polar_alignment, flat_wizard, collaborative_sky · **not yet verified**

**What happens:** All-Sky measures polar error from the drift between a baseline solve and the current solve (native all_sky_polar/mod.rs keeps the baseline fixed and re-solves every 3 s). Its precision is therefore entirely a function of elapsed baseline, but the screen never shows elapsed time, iteration count, or a confidence indicator. In my run the headline number went 0.0\" (green, 'Within acceptance') at ~8 s, then 40104\" (11 degrees) at the next sample, then settled near 1376\". A user watching only the reticle would have believed all three.

**Expected:** Show elapsed baseline time and iteration count next to the error, and withhold the acceptance verdict until the baseline is long enough for the number to mean anything (SharpCap's drift routine does exactly this).

**Repro:** Polar Alignment -> All-Sky -> Start Alignment; watch the right-rail Azimuth/Altitude/Total tiles from the first solve onward.

**Where:** `packages/nightshade_app/lib/screens/polar_alignment/polar_alignment_screen_parts/_right_panel.dart; native/nightshade_native/sequencer/src/all_sky_polar/mod.rs`

**Proposed fix:** Publish elapsed-baseline seconds and sample count in the progress event and render them; gate the green 'Within acceptance' caption on a minimum baseline.

### Location has no place search — you must know your decimal latitude and longitude

`ux` · settings (general / appearance / location / files-storage / connection / settings shell) · **not yet verified**

**What happens:** Settings > Location offers three bare numeric fields (Latitude, Longitude, Elevation) plus 'Sync from Server' and 'Use Device Location'. There is no city/place lookup, no DMS entry, and no map picker. A new user who knows they observe from 'Bend, Oregon' has to leave the app to find 44.0582 / -121.3153 / 1100 m.

**Expected:** ASIAIR and SkySafari let you pick a city; NINA accepts DMS and can pull from the mount. At minimum accept DMS input ('47 36 22 N') in the same field, and offer an offline city list — the app already ships large catalogs.

**Repro:** Settings > Location, top card.

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/location_settings.dart`

**Proposed fix:** Add a DMS-tolerant parser to the lat/long fields and a bundled city picker; optionally 'read from connected mount'.

### The five-rung Science guide is a set of paragraphs, not a ladder you can climb -- every rung opens an explanation and a 'Got it' button with no action

`ux` · science / session_review / stack_result / first_light · **not yet verified**

**What happens:** Analytics > Science shows a 'Science guide' with rungs 1-5 (Measure your sky / Track a star / Build a light curve / Find the period / Contribute it) and a single page-level 'Run calibration' button. Opening rung 2 gives 'Point at a target and let the photometry follow it frame to frame...' plus the gate 'Calibrate your sky first so brightness reads true.' and one button: 'Got it'. Rung 5 gives 'Your measurements are good enough to share...' plus 'Find a period - or build out the curve - before exporting.' and again only 'Got it'. Neither sheet offers the action it is describing, or a link to where that action lives. The only actionable rung is rung 1, whose button is on the page rather than in the sheet.

**Expected:** Each rung sheet should carry the action for that rung -- 'Pick a target' (rung 2), 'Open the light curve' (rung 3), 'Run period search' (rung 4), 'Open the export hub' (rung 5) -- enabled or disabled with the stated prerequisite. As built, a user who reads the ladder has to go find each surface themselves, which is exactly the discoverability problem the ladder exists to solve.

**Repro:** 1. Analytics > Science. 2. Click rung 2 'Track a star' -> sheet with text and 'Got it'. 3. Escape, click rung 5 'Contribute it' -> sheet with text and 'Got it'.

**Where:** `packages/nightshade_app/lib/screens/analytics/widgets/science_rung_sheet.dart, hosted by packages/nightshade_app/lib/screens/science/science_screen.dart`

**Proposed fix:** Give ScienceRungSheet a primary action per rung that routes to that rung's surface, disabled with the prerequisite as its tooltip when the gate is unmet.

### The Submit-discovery dialog defaults to TNS -- the live, irreversible, public submission -- rather than a local file export

`default` · science / session_review / stack_result / first_light · **not yet verified**

**What happens:** The dialog opens with the TNS chip selected and the primary action reading 'Submit to TNS', with the footnote 'Submits live to the TNS via your bot API key -- creates a public AT record.' AAVSO and MPC, which write a file to disk that the user uploads themselves, are the non-default options.

**Expected:** Default to the reversible action. The first-listed and pre-selected network should be a local export (AAVSO), with TNS an explicit opt-in -- creating a public Astronomical Transient record on a mis-classified residual is not undoable, and a first-time user reaching this dialog has by definition never submitted anything before.

**Repro:** 1. Analytics > Science > First Light > Submit on any confirmed candidate. 2. Read which chip is selected and what the primary button says.

**Where:** `packages/nightshade_app/lib/screens/analytics/widgets/transient_report_panel.dart (_format initial value)`

**Proposed fix:** Initialise _format to the first ENABLED format, preferring aavso > mpc > tns; if none is enabled, select aavso and show its (cheapest) blocker.

### Timezone list is 18 hardcoded cities — most of the world cannot select their site's timezone

`ux` · settings (general / appearance / location / files-storage / connection / settings shell) · **not yet verified**

**What happens:** The Timezone dropdown offers exactly: UTC, America/{New_York,Chicago,Denver,Los_Angeles,Phoenix,Anchorage}, Pacific/Honolulu, Europe/{London,Paris,Berlin,Moscow}, Asia/{Tokyo,Shanghai,Kolkata}, Australia/{Sydney,Perth}, Pacific/Auckland. There is no free-text entry and no search. All of South America, Africa, the Middle East, Scandinavia, Spain/Portugal, Canada, Mexico and most of Asia are unrepresented — including Chile and Namibia, where a large fraction of remote-hosted amateur rigs actually sit.

**Expected:** A searchable IANA zone list (as NINA and every OS settings panel provide), or — better for an astro app — derive the site timezone from the lat/long that is already entered three rows above, with an override.

**Repro:** Settings > Location > Time > Timezone dropdown.

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/location_settings.dart (timezone items list)`

**Proposed fix:** Replace with a searchable IANA list, plus a 'from location' button that resolves lat/long -> tz.

### Radar timeline opens on the OLDEST frame, so the Weather screen shows two-hour-stale cloud by default

`default` · shared-widgets · **not yet verified**

**What happens:** Navigating to Weather leaves the scrubber at 'Frame 1 of 13' / 08:20 with the playhead pinned to the far left, while the wall clock was 10:26 and the scrubber's own green NOW marker sits at the far right over frame 13. The map you are looking at is therefore 2 hours out of date until you press play or drag.

**Expected:** A cloud radar should open on the newest frame (frame 13 / NOW) and let you scrub BACK to see where the cloud came from. That is what RainViewer's own viewer, Clear Outside and the ASIAIR's sky view all do. Opening two hours in the past is the one state that can make you decide to keep imaging under cloud that has already arrived.

**Repro:** Sidebar -> Weather. Read 'Frame N of 13' and the time chip next to the scrubber against the clock in the status bar. Observed on first paint with no interaction.

**Where:** `packages/nightshade_app/lib/widgets/weather/radar_timeline_scrubber.dart (currentFrameIndex initialised by screens/weather/weather_screen.dart:339)`

**Proposed fix:** Initialise the frame index to frames.length - 1 (the NOW frame) instead of 0, and have the skip-next button jump to NOW rather than stepping one frame.

### Pre-flight never checks whether the devices a sequence's nodes need are present

`missing` · sequencer (the 37 seq-node:* instruction types) · **not yet verified**

**What happens:** A sequence of [Open Cover, Calibrator On] with zero cover calibrators discovered passed pre-flight as 'Ready with Warnings' — the only warning was 'No Exposures'. The run then aborted 1 s in on 'No cover calibrator (flat panel) connected'. The same held for the dome nodes. Pre-flight does check filters for Smart Exposure and structure for Target Scheduler, so the device-requirement check simply does not cover the dome/cover family.

**Expected:** Pre-flight should map each node type to the device it needs and error when that device is absent — that is the whole point of running it before dark.

**Repro:** Add Open Cover + Calibrator On with no cover calibrator connected > Start.

**Where:** `packages/nightshade_core/lib/src/providers/sequence/rules/`

### Stop Guiding fails the whole run when nothing is guiding

`ux` · sequencer (the 37 seq-node:* instruction types) · **not yet verified**

**What happens:** A sequence [Stop Guiding, Start Guiding] with no guider connected: 'Stop Guiding failed: Failed to stop guiding: No active guider configured' and the run aborted before Start Guiding ever ran. A Stop Guiding node is normally used in an end-of-night or pre-flip block where guiding may or may not be active.

**Expected:** Stopping something that is already stopped is a no-op success (this is what NINA and SGP do). Reserve failure for a guider that is connected and refuses to stop.

**Repro:** New sequence > Stop Guiding > Start.

**Where:** `native/nightshade_native/sequencer/src/instructions.rs (stop guiding instruction)`

### Palette search ranks description matches above exact name matches: typing 'Dither' and pressing + adds Smart Exposure

`ux` · sequencer (the 37 seq-node:* instruction types) · **not yet verified**

**What happens:** The palette filters on name OR description and keeps the original category order. 'Smart Exposure' has the description 'One row per filter; handles rotation + dither', so searching 'Dither' lists Smart Exposure first and the actual Dither node second. Building a sequence by search-then-click-the-first-result silently gave me two Smart Exposure nodes and zero Dither nodes; I only noticed by reading the accessibility tree afterwards.

**Expected:** Exact/prefix name matches must sort above description matches.

**Repro:** Sequencer > Nodes > type 'Dither' in Search nodes. Smart Exposure is the first result.

**Where:** `packages/nightshade_app/lib/screens/sequencer/widgets/node_palette.dart:53-67`

### 'Add Filter Plan' in Smart Exposure adds a duplicate of the same filter, with different defaults from the row above it

`ux` · sequencer (the 37 seq-node:* instruction types) · **not yet verified**

**What happens:** The node's own subtitle is 'One row per filter'. Row 1 was seeded from the sequencer defaults (L, 3 x 2.0 s). Clicking '+ Add Filter Plan' produced a second row also set to L, with 10 x 60.0 s — neither a copy of the row above nor a value the user chose, and a duplicate of an existing filter, which the 'one row per filter' contract says is impossible. Nothing warns about the duplicate.

**Expected:** Pick the next unused filter from the profile, and copy count/duration/gain/offset/binning from the previous row (this is what NINA's Smart Exposure does). Block or warn on duplicate filters.

**Repro:** Configure profile filters, add Smart Exposure, click '+ Add Filter Plan'.

**Where:** `packages/nightshade_app/lib/screens/sequencer/widgets/smart_exposure_properties.dart`

### Three entry points, three different vocabularies for the same failure - one of them raw Dart exception text

`ux` · mobile first run and connection - main.dart connection screen, mobile_discovery_ops, qr_scanner_screen, saved_servers_screen, tailscale_setup_sheet, first_run_setup_screen, checkpoint_resume_dialog · **not yet verified**

**What happens:** The same underlying condition - 'the address you gave me does not answer' - is reported three different ways depending on which button the user pressed. Enter IP: 'Cannot reach http://192.0.2.77:8080/api/info from this device.' followed by two well-written paragraphs of what to check on the PC and on the phone. Relay: 'Could not reach the relay: SocketException: Failed host lookup: \'relay.tail1a2b3.invalid\' (OS Error: No address associated with hostname, errno = 7)'. Tailscale: the wrong message entirely (see the P1 above). The relay text leaks a Dart exception class, an OS error string and an errno onto a consumer screen at 2am.

**Expected:** One shared reachability-failure renderer used by every entry point: what was tried (scheme://host:port/api/info), the plain-language cause (name not found / connection refused / timed out), and the next step. The Enter-IP copy is already the right answer - promote it and delete the other two paths' bespoke strings.

**Repro:** Compare Enter IP -> 192.0.2.77 -> Connect, Connect via Relay -> https://relay.tail1a2b3.invalid + abcd-efgh-ijkl -> Connect, and Connect over Tailscale -> nightshade-rig.tail1a2b3.ts.net -> Connect.

**Where:** `apps/mobile/lib/main_parts/mobile_reconnect_ops.dart and mobile_discovery_ops.dart`

### Polar alignment reports error in raw arcseconds ("1800.0 arcseconds", "Up 2287.9\""), which no one adjusting a wedge can read

`ux` · dashboard, mosaic, polar_alignment, flat_wizard, collaborative_sky · **not yet verified**

**What happens:** Every error readout in the wizard is a raw arcsecond count with one decimal: the Adjust Mount card said 'Azimuth Left 427.6"', 'Altitude Up 2287.9"', 'Total Error 2327.5"', and the completion screen said 'Final error: 1800.0 arcseconds'. A user turning altitude/azimuth bolts thinks in arcminutes and degrees; converting 2287.9 arcsec to 38' in their head, in the dark, while the number updates every 3 s, is not reasonable.

**Expected:** Format adaptively as NINA/SharpCap do: <60" -> arcsec, <60' -> arcmin+arcsec (38' 08"), above that degrees+arcmin (0deg 38' 08"). Keep the bare arcsecond figure only near the threshold where it is the meaningful unit.

**Repro:** Run TPPA to the adjust phase and read the Adjust Mount card and the completion headline.

**Where:** `packages/nightshade_app/lib/screens/polar_alignment/polar_alignment_screen_parts/_measurement_panel.dart and the completion panel`

**Proposed fix:** One shared formatter (formatAngleError) used by the adjust card, the summary, the right-rail totals and the history rows.

### The one layer that makes imaging-scale fields usable - DSS survey imagery - is off by default and buried in a panel

`default` · planetarium + your_sky + constellation · **not yet verified**

**What happens:** At a 2.0 deg field centred on M57 the chart drew about 25 stars across the whole 3.8 x 2.0 deg canvas - roughly 3 stars per square degree, the bundled HYG catalog's depth. Turning on Layers > 'Survey imagery (DSS, below 8 deg)', which ships OFF, filled the same field with real Digitized Sky Survey imagery in about 6 seconds and turned an empty grey rectangle into something you could actually frame a target against. The toggle is three clicks deep (command bar > Layers > scroll) and its label does not hint that it is the difference between a usable and an unusable imaging field.

**Expected:** For an app whose planetarium exists to plan and frame astrophotography, DSS should be ON by default at the FOVs where the star catalog runs out (the layer already self-limits to below 8 deg, so it costs nothing at naked-eye zooms), or the app should offer it the first time the user zooms past the catalog's depth. NINA's framing assistant and Telescopius both default to survey imagery at framing scale for exactly this reason.

**Repro:** Plan Tonight > Planetarium > zoom to 2 deg > count the stars > command bar > Layers > toggle 'Survey imagery (DSS, below 8 deg)'.

**Where:** `packages/nightshade_app/lib/screens/planetarium/widgets/redesign/layers_panel.dart`

**Proposed fix:** Default the survey layer on (it is already gated to FOV < 8 deg), and keep a one-line 'uses network' note in the panel.

### Switching the active profile is only possible by changing which profile launches at startup

`ux` · equipment, diagnostics, shell, onboarding, first_light, tutorial · **not yet verified**

**What happens:** Clicking a non-active profile in the sidebar only highlights it -- the header, the connected devices and the status bar all stay on the previous profile, and the sidebar's footer collapses to just 'Edit Profile'. There is no 'Activate' / 'Use this profile' control anywhere. The only way I found to switch was right-click > 'Set as Default', which does switch is_active but also permanently changes what loads at startup. Profile management (Set as Default, Edit Profile, Duplicate, Delete) is entirely behind right-click; the profile card carries no overflow button or any other affordance advertising that a menu exists.

**Expected:** A visible primary action to switch the active profile for this session, kept separate from 'set as startup default', plus a visible '...' overflow on each profile card exposing the same menu that right-click does.

**Repro:** 1. Create two profiles. 2. Click the non-active one in the sidebar -- header and devices do not change; footer shows only 'Edit Profile'. 3. Right-click it -> the only way to make it active is 'Set as Default'.

**Where:** `packages/nightshade_app/lib/screens/equipment/profile_sidebar.dart`

**Proposed fix:** Add an 'Activate' button to the selected profile card and a '...' overflow menu; keep the star strictly for 'launch with this profile'.

### Three separate tutorial systems fire at once on a first run, and the second one tells the user to do what they just did

`ux` · equipment, diagnostics, shell, onboarding, first_light, tutorial · **not yet verified**

**What happens:** Finishing the 13-step onboarding wizard and choosing 'Take the first-night walkthrough' opens a 7-step modal wizard. Its 'Show me on the Sequencer screen' deep-link navigates correctly, but on arrival two more tutorials appear simultaneously: a coach-mark tour 'Step 1 of 7 - Welcome to Nightshade / Nightshade is your imaging assistant. Three things to set up first: equipment, a profile, and a sequence.' plus a bottom-right toast 'Sequencer Tour - Learn how to create and run automated imaging sequences. [Maybe Later] [Start Tour]'. The coach-mark tour's copy tells the user to set up equipment and a profile, which the 13-step wizard they just completed already did. Every screen then has its own persistent tour toast (Dashboard Tour, Equipment Tour, Imaging Tour, Settings Tour, Analytics Tour) that stays until individually dismissed and occludes the bottom-right of the content area.

**Expected:** One onboarding path at a time. The coach-mark tour should either not fire when a profile already exists, or be reworded to reflect completed state. Per-screen tour toasts should be suppressed while any other guided flow is active, and should be dismissible once globally.

**Repro:** 1. Fresh profile, complete onboarding, click 'Take the first-night walkthrough'. 2. Advance to step 7, click 'Show me on the Sequencer screen'. 3. Observe the Welcome coach-mark tour and the Sequencer Tour toast both appear.

**Where:** `packages/nightshade_app/lib/screens/tutorial/first_night_wizard.dart + the per-screen tour toast host`

**Proposed fix:** Introduce a single 'guided flow active' flag; suppress the coach-mark tour and all per-screen toasts while a wizard is open, and skip the 'set up equipment and a profile' steps when a profile already exists.


## P3

### PHD2 Guiding settings has no way to test the connection -- the one integration you cannot verify

`missing` · settings (sequencer, science, phd2-guiding, plate-solving, weather-safety, notifications + all transports, integrations/plugins, annotations, observation log, auto-save, cloud-sync) · **CONFIRMED** _(reported P1, corrected by verifier)_

**What happens:** The entire PHD2 Guiding page is Host, Port, 'PHD2 executable path (Auto-detect, optional)' and a paragraph of prose. There is no Connect/Test button, no live PHD2 connection state, no PHD2 version readout, no PHD2 profile selector, no 'launch PHD2 if not running', and no 'connect equipment in PHD2 on start'. Every other integration in this app can be verified from its settings page -- SMTP/Discord/Pushover/Telegram/generic-webhook all have 'Send test', cloud sync has 'Test Connection', both plate solvers have 'Verify', and the plate solving page even shows 'Auto-detect -- not found' inline. PHD2 is the most failure-prone dependency in the whole stack and is the only one with no verification affordance.

**Expected:** A 'Test connection' button that opens the PHD2 event socket and reports version + current profile + connected state, and an inline auto-detect status for the executable path matching the Plate Solving page's 'Auto-detect -- not found' pattern. NINA shows PHD2's state and version inline on its guider page.

**Repro:** Settings > search 'PHD2' > PHD2 Guiding. Read the whole page.

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/phd2_guiding_settings.dart`

**Proposed fix:** Add a Test row that calls the existing PHD2 client's connect/get-version RPC against the configured host:port and renders success (version, active profile) or the socket error, plus an auto-detect status subtitle on the executable path row reusing the plate-solver detection pattern.

### "Alt now" is the wrong axis for a tonight planner

`default` · planner, scheduler, tonight, suggestions, weather, transients · **not yet verified**

**What happens:** The filter chip is 'Minimum altitude right now'. Driven at 10:12 local — 12 hours before darkness — it filtered 1114 candidates down to 147 by the daytime altitude of objects that will be nowhere near that altitude tonight. The night-outlook chips right next to it are all whole-night quantities (peak in dark, hours visible).

**Expected:** Filter on altitude within the imaging window (peak altitude in dark, or altitude at a chosen time), which is what the cards already compute and display.

**Repro:** Plan Tonight > Recommendation in the daytime > 'Alt now: any' > drag to 45 > Apply. Top result IC1805 at 53° now, an object that will be at a different altitude all night.

**Where:** `packages/nightshade_app/lib/screens/planner/planner_screen_parts/_filter_controls.dart (alt-now dialog) and packages/nightshade_core/lib/src/providers/suggestion_filter_provider.dart`

**Proposed fix:** Rename to 'Minimum peak altitude tonight' and filter on visibility.peakAltitude (already on every suggestion), keeping 'right now' as a secondary option for people at the eyepiece.

### The Rationale block never names the target it is explaining

`ux` · planner, scheduler, tonight, suggestions, weather, transients · **not yet verified**

**What happens:** The Recommendation tab ends with 'Rationale — Why this plan was chosen' containing 'Excellent peak altitude (62°), far from the 87% moon (102°). 5.0 hours above minimum altitude...'. Those are NGC6015's numbers. With a search active the block sits directly under an M31 card whose own chips read 'Peak in dark 79°, Moon 46°, 4.8h visible', so it reads as an explanation of the card above it and contradicts every number on it. The block also asserts 'Large unfinished dataset makes this an efficient completion target' for an object with zero captured frames (the same block says 'No data collected yet').

**Expected:** Name the target ('Why NGC6015 was chosen'), and do not describe a target with no frames as a 'large unfinished dataset'.

**Repro:** Plan Tonight > Recommendation > type 'M31' in the search box > scroll to the bottom.

**Where:** `packages/nightshade_app/lib/screens/planner/planner_screen_parts/_recommendation_tab.dart (rationale section rendered from plan, after the candidate list)`

**Proposed fix:** Title the section with the plan's primary target name, and move it directly under the hero card instead of below the candidate list. Drop the 'large unfinished dataset' phrasing when capturedFrames == 0.

### Constellation filter is an unsearchable list of 3-letter abbreviations

`ux` · planner, scheduler, tonight, suggestions, weather, transients · **not yet verified**

**What happens:** 'Constellation: any' opens a plain dropdown listing 'Constellation: And', 'Constellation: Aql', 'Constellation: Aqr', 'Constellation: Ari', 'Constellation: Aur', 'Constellation: Boo', 'Constellation: CVn', 'Constellation: Cam', 'Constellation: Cas', 'Constellation: Cep', ... — 88 entries, abbreviations only, no type-ahead, and the redundant 'Constellation: ' prefix repeated on every row eats the width.

**Expected:** Full names (or 'Andromeda (And)'), with a search field, and only the constellations that actually have candidates tonight.

**Repro:** Plan Tonight > Recommendation > 'Constellation: any'.

**Where:** `packages/nightshade_app/lib/screens/planner/planner_screen_parts/_filter_controls.dart`

**Proposed fix:** Drop the repeated prefix, show full names, add a filter field, and sort/limit to constellations present in the current candidate set.

### "Run unattended all night" arms all-night automation on one click with no confirmation or readiness summary

`default` · planner, scheduler, tonight, suggestions, weather, transients · **not yet verified**

**What happens:** One click on 'Run unattended all night' put the autopilot into Running, evaluating every 60s and ready to dispatch a sequence to the mount — with weather safety off, the guider disconnected, no dark/flat library, and no pre-flight summary shown. There is no confirmation dialog and no summary of what will happen. (Stop and Pause do work correctly, and the Idle/Running/countdown states were truthful.)

**Expected:** A single click that will slew a telescope and run unattended for 8 hours should first show what it is about to do: chosen target, filters, weather-safety state, guiding state, park-at-dawn state — and require confirmation. NINA and SGP both make you build and start a sequence explicitly.

**Repro:** Plan Tonight > Schedule > 'Run unattended all night'.

**Where:** `packages/nightshade_app/lib/screens/planner/widgets/scheduler_tab_content/decision_panel.dart`

**Proposed fix:** Gate the start behind the existing readiness checklist (the onboarding 'What's next' screen already links to one) rendered as a confirm sheet: green/amber rows for weather safety, guider, park-at-dawn, disk space and the first target, plus a 'Start anyway' for the amber cases.

### The onboarding step list marks a step complete just for visiting it

`ux` · equipment, diagnostics, shell, onboarding, first_light, tutorial · **not yet verified**

**What happens:** On the guider step I ran 'Test' against PHD2 with nothing listening; it correctly reported 'No response on localhost:4400...' and I selected no native guider. Pressing Next still put a filled blue tick next to 'Guider' in the left step list, identical to the ticks on Camera and Mount which really were configured. The Review screen is honest ('Guider - not set -'), so only the step list is misleading. Step 1's 'Back' button is also rendered enabled and is a silent no-op.

**Expected:** Tick a step only when it produced a value; show visited-but-skipped steps in a distinct state. Disable Back on step 1.

**Repro:** 1. Onboarding step 7, click 'Test' (fails), select nothing, click Next. 2. Sidebar shows 'Guider' with a filled tick. 3. Continue to step 12 -- Review says 'Guider - not set -'.

**Where:** `packages/nightshade_app/lib/screens/onboarding/onboarding_screen.dart (step completion model)`

**Proposed fix:** Track per-step 'has value' separately from 'visited' and render skipped optional steps with a dash rather than a tick.

### Two different score scales for "how good is this target tonight" on the same screen

`ux` · planner, scheduler, tonight, suggestions, weather, transients · **not yet verified**

**What happens:** Plan Tonight > Recommendation shows scores on a 0-100 badge (M92 77, M31 63, NGC6015 76). Plan Tonight > Schedule > Target queue shows a SCORE column reading 1.655, 1.500, 1.300 for the same objects. Nothing on either screen says what either number means or that they are different scales.

**Expected:** One scale, or at least a unit/tooltip. A user comparing the two tabs cannot tell whether 1.655 is good.

**Repro:** Plan Tonight > Recommendation (badges), then Plan Tonight > Schedule (SCORE column) with the same targets.

**Where:** `packages/nightshade_app/lib/screens/scheduler/widgets/target_score_row.dart vs packages/nightshade_planetarium/lib/src/planning/target_scoring.dart (totalScore 0-100)`

**Proposed fix:** Normalise the scheduler score to 0-100 for display, or label the column 'weight' and add a tooltip breaking it down (the decision panel already has the per-factor weights in 'Scoring weights').

### The optical-train step asks the user to look up a pixel size the driver already reports

`ux` · equipment, diagnostics, shell, onboarding, first_light, tutorial · **not yet verified**

**What happens:** Onboarding step 8 has a 'Camera pixel size' field whose placeholder is literally 'Look this up in your camera spec sheet', even though a camera was already chosen in step 3 and the driver reports pixel size. Step 9 then makes the user pick their camera a second time from a separate 'Camera library' to seed gain/offset. The rest of this step is excellent -- the telescope library is well stocked, and the computed values are exactly right (550 mm x 0.80 = 440.0 mm, f/4.40, 206.265 x 3.76 / 440 = 1.76 arcsec/px, all verified against the displayed numbers).

**Expected:** Prefill pixel size from the selected camera (with an override), and reuse the step-3 camera identity to seed the step-9 defaults instead of asking the user to identify their camera twice.

**Repro:** 1. Onboarding step 3: pick the camera. 2. Step 8: 'Camera pixel size' is empty with the spec-sheet placeholder. 3. Step 9: 'Choose from camera library' asks for the camera again.

**Where:** `packages/nightshade_app/lib/screens/onboarding/optical_train_step.dart`

**Proposed fix:** Query the chosen camera driver (or the camera-library preset) for pixel size and prefill it, keeping the field editable.

### The capture-folder step offers no default and no way to type a path

`ux` · equipment, diagnostics, shell, onboarding, first_light, tutorial · **not yet verified**

**What happens:** Step 10 shows 'No folder selected yet' with a single 'Browse' button that opens the native GTK chooser. There is no suggested default (~/Pictures/Nightshade or similar) and no text field to type or paste a path -- which matters on a headless/appliance install driven over VNC or a small screen. Once chosen it does verify writability ('Folder is writable.'), which is genuinely good, and Next is correctly blocked with 'Pick a capture folder.' until one is set.

**Expected:** Prefill a sensible per-platform default that the user can accept with one click, and accept a typed/pasted path alongside Browse.

**Repro:** 1. Onboarding step 10. 2. Only 'Browse' is offered; Next is blocked until a folder is chosen.

**Where:** `packages/nightshade_app/lib/screens/onboarding/onboarding_screen.dart (capture folder step)`

**Proposed fix:** Default to a platform pictures/Nightshade path, create it on accept, and add an editable path field.

### Equipment Settings mixes auto-save and Apply-required sections and repeats its own title

`ux` · equipment, diagnostics, shell, onboarding, first_light, tutorial · **not yet verified**

**What happens:** The gear at the top-right of the Equipment screen opens 'Equipment Settings', whose dialog title bar and first content heading both read 'Equipment Settings'. Camera Settings (gain/offset), Mount Settings (meridian flip, flip offset) and Focuser Settings (temp comp, coefficient, backlash) have no Apply and appear to save on edit, while the Built-in Guider block at the bottom has its own Reset/Apply pair plus a 'Last applied: exp 1.0s, gain 100, bin 1, cal 250ms' line. There is nothing telling the user which sections need Apply. The dialog is also short relative to the window, so the two-column masonry content scrolls in a cramped viewport with the right column running far past the left.

**Expected:** One save model per dialog. If the guider genuinely needs an explicit apply (it pushes to a running guider), say so on the section; otherwise make everything consistent.

**Repro:** 1. Equipment > gear (top right). 2. Note the duplicated 'Equipment Settings' heading. 3. Scroll: only Built-in Guider has Reset/Apply.

**Where:** `packages/nightshade_app/lib/screens/equipment/settings_tab.dart / dialogs_and_settings.dart`

**Proposed fix:** Drop the duplicated in-content heading, and either give every section an explicit Apply or annotate the guider section with why it alone needs one.

### INDI and Alpaca startup discovery default to off after the wizard offered them as scan backends

`default` · equipment, diagnostics, shell, onboarding, first_light, tutorial · **not yet verified**

**What happens:** Onboarding step 2 presents Native, Alpaca and INDI as pre-checked scan backends (only Sim is off). Settings > Equipment > Connection then shows 'Query INDI on startup' and 'Query Alpaca on startup' both defaulting to OFF, with an INDI server address of localhost:7624. So a Linux user who accepts the wizard's defaults gets INDI listed as a backend but never actually queried at startup, and the two surfaces disagree about whether INDI is in play.

**Expected:** The wizard's backend selection should drive these toggles, or the toggles should default on when a server address is configured. At minimum the two screens should not contradict each other.

**Repro:** 1. Onboarding step 2: INDI and Alpaca are checked by default. 2. Finish onboarding. 3. Settings > Equipment > Connection: 'Query INDI on startup' and 'Query Alpaca on startup' are both off.

**Where:** `packages/nightshade_app/lib/screens/settings/connection_settings.dart + onboarding drivers step`

**Proposed fix:** Persist the wizard's backend choices into the startup-query toggles.

### Discovery says only that no switches or cover calibrators were found, without saying why or what to do

`ux` · equipment, diagnostics, shell, onboarding, first_light, tutorial · **not yet verified**

**What happens:** For the two device classes with no simulator, the expanded Discovery panel ends with two bare italic lines, 'No cover / calibrators found' and 'No switches found', each with a refresh icon -- while every other class gets a titled section with a '1 found' count. There is no indication of which backends were searched, whether an error occurred, or how to add one manually (e.g. an Alpaca or INDI address). The app log does record 'Discovery complete for Switch: 0 devices, 0 backend errors', so the information exists.

**Expected:** Match the other sections: 'SWITCHES - 0 found', with a subtitle naming the backends scanned and a hint such as 'Switches are usually reached over Alpaca or INDI - configure a server in Settings > Connection.'

**Repro:** 1. Equipment > Discovery > Expand. 2. Scroll to the bottom of the device list.

**Where:** `packages/nightshade_app/lib/screens/equipment/discovery_panel.dart (empty-class rows)`

**Proposed fix:** Render zero-result classes with the same section chrome and a one-line reason plus a route to configure a remote server.

### The whole screens/suggestions/ UI ships but is unreachable from any screen

`missing` · planner, scheduler, tonight, suggestions, weather, transients · **not yet verified**

**What happens:** packages/nightshade_app/lib/screens/suggestions/ contains SuggestionFilters (with chip_controls, slider_controls, sort_and_reset_controls), ScoreBreakdown and TransientAlertsPanel. Repo-wide grep finds no import of screens/suggestions outside that directory except one test (test/screens/suggestions/suggestion_filters_remote_test.dart:54). ScoreBreakdown and TransientAlertsPanel have zero references anywhere, including tests. The controls the running app actually uses are the planner's own _SortDropdown/_ResetChip in screens/planner/planner_screen_parts/_filter_controls.dart:598-680.

**Expected:** Either wire these surfaces into a screen or delete them. In particular TransientAlertsPanel looks like the intended 'transients in your planner' integration that today does not exist — the Observing Alerts view is only reachable under Analytics > Science.

**Repro:** Navigate every Plan Tonight tab (Recommendation, Projects, Schedule, Framing, Planetarium, Discover) and Analytics > Science; none render these widgets. Then `grep -rn 'SuggestionFilters\|ScoreBreakdown\|TransientAlertsPanel' packages/ --include=*.dart`.

**Where:** `packages/nightshade_app/lib/screens/suggestions/widgets/suggestion_filters.dart:30, .../widgets/suggestion_filters/sort_and_reset_controls.dart, .../widgets/suggestion_filters/slider_controls.dart, .../widgets/score_breakdown.dart, .../widgets/transient_alerts_panel.dart:29`

**Proposed fix:** Delete the directory, or land TransientAlertsPanel on the Plan Tonight Recommendation tab (which is where an observer would expect tonight's transient alerts to appear). Also retire suggestion_filters_remote_test.dart — a green test over an unshipped widget is worse than no test.

### The Object types dialog reflows when a chip is selected, sliding Apply down under the pointer

`ux` · planner, scheduler, tonight, suggestions, weather, transients · **not yet verified**

**What happens:** The 'Object types' dialog lays its 7 chips out in a Wrap. Selecting 'Nebula' adds a checkmark, which widens the chip, pushes the layout from 2 rows to 3, and moves the Clear/Apply row down ~20px. My click at the previous Apply position landed on empty space; I had to look at a screenshot to find the button again.

**Expected:** The dialog's action row should not move when a chip is toggled.

**Repro:** Plan Tonight > Recommendation > 'Type: any' > click 'Nebula' > note Apply has moved from y=427 to y=447.

**Where:** `packages/nightshade_app/lib/screens/planner/planner_screen_parts/_filter_controls.dart (object-types dialog)`

**Proposed fix:** Reserve the checkmark's width in the unselected chip (fixed-width leading slot) or give the chip area a fixed height.

### "Unattended Autopilot" panel title is permanently truncated

`ux` · planner, scheduler, tonight, suggestions, weather, transients · **not yet verified**

**What happens:** The Schedule tab's left panel header renders as 'Unattended Aut…' (or 'Unattended …' when the status badge is wider), in every state I saw — Idle, Running, and with no location set. The panel is ~265px wide with plenty of vertical room below.

**Expected:** The full name 'Unattended Autopilot' should be readable; wrap to two lines or move the status badge below the title.

**Repro:** Plan Tonight > Schedule, look at the left panel header in any state.

**Where:** `packages/nightshade_app/lib/screens/planner/widgets/scheduler_tab_content/decision_panel.dart`

**Proposed fix:** Wrap the title (maxLines: 2) or put the Idle/Running badge on its own row.

### Selecting a node and pressing Delete does nothing; deletion requires hovering the row and hitting a 3-state hover toolbar

`ux` · sequencer (the 37 seq-node:* instruction types) · **not yet verified**

**What happens:** I selected the Slew to Target node (its properties panel loaded, the row showed the selected outline) and pressed Delete. Nothing happened — the header still read '4 nodes'. Deletion only worked by hovering the row until an icon strip appears and clicking the trash icon, then confirming a dialog. There is a sequence_tree_shortcuts.dart in the codebase, so shortcuts exist for other actions.

**Expected:** Delete/Backspace on a selected node should delete it (with the same confirm dialog).

**Repro:** Select any node in the tree, press Delete.

**Where:** `packages/nightshade_app/lib/screens/sequencer/widgets/sequence_tree_shortcuts.dart`

### Calibrator On hardcodes a 0-255 brightness scale and states it as fact

`ux` · sequencer (the 37 seq-node:* instruction types) · **not yet verified**

**What happens:** The Calibrator On properties show a slider fixed to 0-255 with the caption '0 = off, 255 = maximum brightness'. ASCOM/Alpaca cover calibrators report their own MaxBrightness (commonly 100, 1023 or 65535); the node has no way to read it and the caption asserts a value that will be wrong for most panels. (The node itself is also unreachable at runtime — see the dome/cover finding.)

**Expected:** Read MaxBrightness from the connected calibrator and scale the slider and caption to it; if none is connected, say the range is the device's.

**Repro:** Add Calibrator On > read Properties.

**Where:** `packages/nightshade_app/lib/screens/sequencer/widgets/node_properties_panel_parts (calibrator properties)`

### Smart Exposure claims 'Round-robin: RGRGRG…' regardless of what filters the node actually contains

`ux` · sequencer (the 37 seq-node:* instruction types) · **not yet verified**

**What happens:** With a single filter plan (L), the Rotate Filters switch's caption reads 'Round-robin: RGRGRG...' — naming R and G filters that are not in the node and describing a rotation that cannot happen with one row. It reads as a description of this node's behaviour rather than a generic illustration.

**Expected:** Build the preview string from the node's own plans, e.g. 'L L L' for one row or 'L G L G L' for the configured rows.

**Repro:** Add Smart Exposure with one filter plan > scroll Properties to Rotate Filters.

**Where:** `packages/nightshade_app/lib/screens/sequencer/widgets/smart_exposure_properties.dart`

### Target node has two different name fields ('Name' and 'Target Name') that disagree by default

`ux` · sequencer (the 37 seq-node:* instruction types) · **not yet verified**

**What happens:** A freshly added Target node shows 'Name: Target' at the top of the properties panel and 'Target Settings > Target Name: New Target' below it. The tree row and every run log use 'New Target'; the node label uses 'Target'. Nothing on screen explains which one matters.

**Expected:** One field. If a node-label vs astronomical-target distinction is really needed, label them 'Step label' and 'Target', and default them to the same value.

**Repro:** Sequencer > add Target > read Properties.

**Where:** `packages/nightshade_app/lib/screens/sequencer/widgets/node_properties_panel_parts/target_node_properties`

### The Tonight screen hedges about a fact it knows: "may be below your horizon or your location is not set"

`ux` · planner, scheduler, tonight, suggestions, weather, transients · **not yet verified**

**What happens:** With no observing location configured, the Tonight screen shows 'Nothing imageable right now — The sky may be below your horizon or your location is not set. Check Settings, then refresh.' with a Refresh button as the only action. The app knows unambiguously that the location is unset — every other surface says so plainly: Plan Tonight shows 'Location not configured / Set your observing latitude and longitude in Settings before using the planner' with an 'Open Settings' button, the Weather screen shows 'Location Not Configured' with 'Open Location Settings', and the dashboard shows 'Set an observing location for twilight times' with a 'Set location' button.

**Expected:** Same treatment as the other three surfaces: state the actual cause and offer a one-click 'Open Settings'. The Refresh button offered here can never succeed.

**Repro:** Fresh profile, skip onboarding, Dashboard > 'Image tonight'.

**Where:** `packages/nightshade_app/lib/screens/tonight/tonight_screen.dart:273-279 (and the same hedged string at one_tap_tonight_controller.dart:94)`

**Proposed fix:** Branch on settings.latitude==0 && settings.longitude==0 before rendering the generic no-target card and show the 'Location not configured' state with an Open Settings action, exactly as the planner does.

### Framing sidebar only scrolls when the wheel is over certain sub-regions, and resets to the top on every navigation

`ux` · imaging, framing, guiding · **not yet verified**

**What happens:** The Framing sidebar is roughly three viewport-heights tall (Target, Equipment, Frame, Preview FOV, FOV Overlay, Survey Source, Coordinates, Altitude, Mosaic, Guided Framing steps 1-4, Utilities). Mouse-wheel over y=700 did nothing; over y=950/990 it scrolled. Every navigation away and back resets the scroll to the top, so reaching Utilities (Add to Sequence / Auto-build / Save Target / Cache Image) takes 40+ wheel notches from a cold arrival, and clicks land on the wrong control if the layout shifts mid-scroll (twice a click intended for 'Slew to Target' hit the split-button chevron instead).

**Expected:** The whole sidebar is one scrollable, the wheel works anywhere over it, and the scroll offset survives an in-app navigation.

**Repro:** 1. Plan Tonight -> Framing. 2. Wheel over the middle of the sidebar (no movement) then over the bottom (moves). 3. Navigate to Imaging and back - offset resets to top.

**Where:** `packages/nightshade_app/lib/screens/framing/widgets/framing_sidebar/ (nested scrollables inside the sidebar column)`

**Proposed fix:** Single outer scrollable with a PageStorageKey so the offset persists; collapse Guided Framing to a compact stepper so Utilities is not below three screens of content.

### 'Captured Images' is a remote-only image browser filed under Settings > Advanced, where it is permanently dead on desktop

`ux` · settings (imaging / calibration / calibration-library / dark-library / captured-images / image-grading / adaptive-exposure / adaptive-conditions / preflight / autofocus / focus model) · **not yet verified**

**What happens:** On a local desktop backend the section renders one line: 'Connect to a remote appliance to browse its captured frames.' It is honest, but a thumbnail gallery is not a setting, and the row occupies a permanent slot in the Advanced nav for every desktop user who will never see content in it.

**Expected:** Hide the section when the backend is not a NetworkBackend (the app already knows), or move the browser into the Imaging/Session Review surface where a gallery belongs and leave a deep link in Settings.

**Repro:** Settings > Advanced > Captured Images on a local desktop instance.

**Where:** `packages/nightshade_app/lib/screens/settings/settings_catalog.dart:650-664; packages/nightshade_app/lib/screens/settings/widgets/captured_images_settings.dart:15-21 (doc comment already says 'Remote-only; the local desktop has its own image browser')`

**Proposed fix:** Filter the section out of the catalog when backendProvider is not a NetworkBackend, the same way the page already gates its body.

### The Slew split-button's primary half does nothing - every slew costs two clicks through a menu

`ux` · shared-widgets · **not yet verified**

**What happens:** The control renders as a classic split button: a filled 'Slew' button with a chevron attached. But the whole visual child is wrapped in IgnorePointer, so clicking the 'Slew' half does not slew - it opens the same dropdown as the chevron. You must then click 'Slew' a second time, in the menu.

**Expected:** A split button's primary half performs the default action; only the chevron opens the alternatives. That is how NINA's 'Slew'/'Slew and center' and SGP's target actions behave. As built, the most common action (plain slew) costs two clicks and a menu traversal, and the affordance actively lies about what the left half does.

**Repro:** Imaging > Mount > GoTo/Sync with valid coordinates and the mount unparked. Click the middle of the 'Slew' label (not the chevron). The dropdown opens instead of the mount moving.

**Where:** `packages/nightshade_app/lib/widgets/slew_dropdown_button.dart:154-171 (IgnorePointer + onPressed: enabled ? () {} : null)`

**Proposed fix:** Split the hit area: the label runs _handleSlewMode(SlewMode.slew) directly, the chevron opens the popup.

### Pinning a narrator event is invisible on the timeline - the PINNED marker only exists inside the detail sheet

`ux` · shared-widgets · **not yet verified**

**What happens:** Event 2 was stored with pinned=1. On the NIGHT STORY timeline its row is visually identical to the two unpinned rows - same dot, same layout, no pin glyph, no ordering change. The 'PINNED' label appears only after you open that specific event's detail sheet.

**Expected:** Pinning exists so an operator can mark the two or three things that mattered in an eight-hour night. A pin you can only see by opening each item one at a time is not a pin. Show a pin glyph on the timeline row (and ideally offer a 'Pinned only' chip next to the existing All/Discoveries/Milestones/Conditions/Equipment/Quality filters).

**Repro:** Set pinned=1 on one narrator_events row, open Analytics > Science > NIGHT STORY, compare the three rows, then open each one.

**Where:** `packages/nightshade_app/lib/screens/analytics/widgets/night_story_timeline.dart (row builder) - the PINNED treatment lives only in widgets/narrator/narrator_detail_sheet.dart:84-93.`

**Proposed fix:** Render the pin state on the timeline row and add a pinned filter chip.

### UI scale dropdown truncates its own selected value to 'Extra Lar...' at large UI scale

`ux` · settings (general / appearance / location / files-storage / connection / settings shell) · **not yet verified**

**What happens:** With UI scale = Extra Large (1.4x) the dropdown that sets it renders 'Extra Lar...' — the control cannot display the value it currently holds because SettingsDropdown is given a fixed 150 px width that does not scale.

**Expected:** The dropdown width should scale with ui_scale/font size, or the labels should be short enough ('1.4x') to survive.

**Repro:** Settings > Appearance > UI scale > Extra Large (1.4x).

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/settings_widgets/settings_switch_and_dropdown.dart (fixed `width:` on SettingsDropdown)`

**Proposed fix:** Make the dropdown width intrinsic (IntrinsicWidth / min-width) instead of a hardcoded 150.

### 'Clear Library' is enabled and destructive when the library is empty, and its confirm dialog names no count and no path

`ux` · settings (imaging / calibration / calibration-library / dark-library / captured-images / image-grading / adaptive-exposure / adaptive-conditions / preflight / autofocus / focus model) · **not yet verified**

**What happens:** With 0 dark frames, 0 bias frames and 0 masters, the red 'Clear Library' button is still enabled. Its dialog reads 'Remove all entries from the dark library?' with an 'Also delete files from disk' checkbox - it does not say how many entries, or which directory would be deleted from. Confirming produced the (correct and honest) result banner 'Library cleared (0 entries removed).'

**Expected:** Disable the button at 0 entries, and put the count and the on-disk root in the dialog ('Remove 47 entries? 47 files under /path will also be deleted').

**Repro:** Settings > Equipment > Dark Library (empty) > Clear Library.

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/dark_library_settings.dart:342-372 (confirm dialog)`

**Proposed fix:** Gate the button on _stats.total > 0 and interpolate the count + resolved path into the dialog body.

### Reference sky brightness range 14-24 mag/arcsec2 admits values no real sky can reach, which pin the feature at its clamp forever

`default` · settings (imaging / calibration / calibration-library / dark-library / captured-images / image-grading / adaptive-exposure / adaptive-conditions / preflight / autofocus / focus model) · **not yet verified**

**What happens:** The 'Reference sky brightness' input clamps to [14, 24]. The darkest natural sky is ~22.0 mag/arcsec2. I typed 99 and it silently became 24.00 with no message. At ref = 24 against a typical 21.5 sky the ratio is 10^(2.5/2.5) = 10x, so every exposure is driven to the 'Longest exposure' clamp for the whole session; at ref = 14 every exposure sits on the 'Shortest exposure' clamp.

**Expected:** Restrict to a physically meaningful band (roughly 16-22.2) and tell the user when a typed value was clamped instead of silently rewriting it.

**Repro:** Settings > Imaging > Adaptive Exposure (enabled) > 'Reference sky brightness': click, Ctrl+A, type 99, Enter -> shows 24, no message.

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/adaptive_exposure_settings.dart:152-153 (min 14, max 24)`

**Proposed fix:** Narrow the range and surface a 'clamped to N' hint on commit.

### 'Reset All to 0 deg' wipes an imported horizon with no confirmation, one click from the Import button

`ux` · settings (general / appearance / location / files-storage / connection / settings shell) · **not yet verified**

**What happens:** 'Import .hor / CSV' and 'Reset All to 0 deg' sit side by side on the same row, ~120 px apart. Clicking Reset immediately set horizon_profile_json to all zeros. No confirm dialog, no undo, and the imported file is not remembered, so recovering means finding the .hor again.

**Expected:** Confirm before discarding a non-zero horizon, or offer undo in the snackbar.

**Repro:** Import a horizon, then click 'Reset All to 0 deg' on the same row.

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/location_settings.dart (Reset All action)`

**Proposed fix:** Add a confirm when any value is non-zero, or an 'Undo' action on the snackbar.

### Storage path fields look like editable text inputs but silently ignore typing

`ux` · settings (general / appearance / location / files-storage / connection / settings shell) · **not yet verified**

**What happens:** Settings > Files & Storage > Storage renders 'Image output' and 'Sequences' as bordered input boxes with the placeholder 'Not set' and a small folder button beside them. Clicking into the box and typing '/tmp/ns-audit/rsg-files/lights' then pressing Enter or Tab does nothing at all: the box still reads 'Not set' and image_output_path stays empty in app_settings. The value can only be set through the folder button (which then works and persists correctly).

**Expected:** Either make the field editable — pasting a UNC/NAS path is how people configure this on a real rig, and a directory picker cannot reach a path that is not mounted yet — or render it as a plain label instead of a text input so it does not invite typing.

**Repro:** Settings > Files & Storage > click the 'Not set' box on Image output > type any path > Enter. Field unchanged; sqlite3 app_settings image_output_path still ''.

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/settings_widgets/settings_color_and_path_inputs.dart:136-230; used from file_path_settings.dart:160-190`

**Proposed fix:** Make SettingsPathInput a real TextField that commits on submit/blur with a directory-exists check, keeping the browse button as a shortcut.

### At a ~900px window the status bar shows three identical 'Disconnected' chips with no device names

`ux` · settings (general / appearance / location / files-storage / connection / settings shell) · **not yet verified**

**What happens:** Resizing the window to 900x800 collapses the bottom status bar so the device labels are dropped: it reads 'Disconnected  Disconnected  Disconnected', distinguished only by three small monochrome glyphs. At 1600px the same chips read 'Camera Disconnected / Mount Disconnected / Guider Disconnected'. The Focus chip and the right-hand run-state chip are dropped entirely.

**Expected:** When width is short, drop the WORD 'Disconnected' (the colour and dot already carry it) and keep the device name, or collapse the three into one 'No devices connected' chip. Three identical words tell the operator nothing.

**Repro:** Resize the desktop window to 900px wide and read the bottom status bar.

**Where:** `app shell bottom status bar (packages/nightshade_app/lib/screens/shell/)`

**Proposed fix:** Priority-drop the status word before the device name.

### Files & Storage refuses to name the application data folder, so a user cannot find their own database, logs or backups

`ux` · settings (general / appearance / location / files-storage / connection / settings shell) · **not yet verified**

**What happens:** The Application Data card has one row, 'Database and logs', whose entire value is the sentence 'Managed automatically in Nightshade's application data folder'. There is no path, no copy button and no folder button - the two rows above it (Image output, Sequences) each get both a path field and a folder-picker button. In this run the database was actually at /tmp/ns-audit/r-settings-general/data/nightshade.db and nothing in the UI says so.

**Expected:** Show the resolved absolute path (read-only is fine) with a 'Reveal in file manager' / copy button, the way Image output does. Users need this to attach a log to a bug report, to back the DB up manually, or to check they are pointed at the right profile - NINA and SGP both put the working folder one click away.

**Repro:** Settings > Files & Storage > Application Data > 'Database and logs'.

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/file_path_settings.dart (Application Data section)`

**Proposed fix:** Render the resolved data directory as a selectable path with copy + reveal actions, and state that it moves with NIGHTSHADE_DATA_DIR when that is set.

### Settings search returns section names only - it never shows or scrolls to the row that matched

`ux` · settings (general / appearance / location / files-storage / connection / settings shell) · **not yet verified**

**What happens:** Typing 'Alpaca' filters the sidebar down to a single entry, 'Connection', and nothing else changes: the detail pane keeps showing whatever section was already open, the result gives no hint which row matched, and clicking through drops you at the top of a long Connection page where you still have to hunt for the Alpaca row yourself. Same for 'Red night' (-> 'Appearance') and 'Elevation' (-> 'Location').

**Expected:** A settings search should show the matching ROW ('Query Alpaca on startup - Connection'), and clicking it should open that section scrolled to and briefly highlighting that row. The index already stores the row titles that matched, so the information is there and is being thrown away.

**Repro:** Settings > search box > type 'Alpaca'. Sidebar shows 'Connection' with no row context. Click it; the Connection page opens at the top with no indication of where the match is.

**Where:** `packages/nightshade_app/lib/screens/settings/settings_screen.dart + settings_search_index.g.dart`

**Proposed fix:** Return (section, rowTitle) pairs from the matcher, render them as result rows under their section, and pass the row key through navigation so the target section can Scrollable.ensureVisible + flash it.

### Pre-flight prints the same guider problem twice and both times tells you to connect PHD2, in a build that ships its own native guider

`ux` · sequencer run-time (executing sequences end to end) · **not yet verified**

**What happens:** The General section lists 'No Guider Connected - This sequence includes guiding or dithering operations that require PHD2' immediately followed by 'Guider Not Connected - Dither requires a guider (PHD2) but none is connected.' Both carry the identical hint 'Connect to PHD2 in the Guiding panel.' Discovery in this same build offers a 'Built-in Multi-Star Guider (native)' with no PHD2 involved.

**Expected:** One warning per distinct problem, and advice that matches the guider backends this build actually offers.

**Repro:** Build any sequence containing a Dither node with no guider connected, press Start, scroll the Pre-Flight dialog.

**Where:** `Sequencer > Builder > Start > Pre-Flight Validation > General`

**Proposed fix:** De-duplicate the two rules and make the remediation text backend-aware ('Connect a guider in Equipment, or PHD2 in the Guiding panel').

### Node palette search ranks Smart Exposure above the node literally named 'Dither' when you search 'Dither'

`ux` · sequencer run-time (executing sequences end to end) · **not yet verified**

**What happens:** Typing 'Dither' in the node palette search returns 'Smart Exposure' first (its description contains 'handles rotation + dither') and the node actually named 'Dither' second. Clicking the first result's + - the natural action after typing an exact node name - adds a Smart Exposure node instead. I hit this myself: the sequence silently gained an unwanted Smart Exposure node (visible only as the planned-frame count jumping from 4 to 8).

**Expected:** An exact name match ranks first. NINA's instruction search puts a literal name match at the top.

**Repro:** Sequencer > Builder > Nodes > search 'Dither'.

**Where:** `Sequencer > Builder > Nodes palette`

**Proposed fix:** Score name matches above description matches; exact/prefix name match first.

### Continue Session reports '0 minutes integration' for a session that has 12s of integration on the card behind it

`ux` · sequencer run-time (executing sequences end to end) · **not yet verified**

**What happens:** The dialog's Progress row reads '4/4 frames captured, 0 minutes integration'. The dashboard card for the same run reads '12s integration'. Integration time is rounded to whole minutes, so any sub-minute (and any 90s) session is reported as 0 or as a truncated value.

**Expected:** Match the rest of the app: '12s integration'. Reporting real captured photons as '0 minutes' is the sort of thing that makes a user think the run did nothing.

**Repro:** Run a short sequence (4 x 3s), relaunch, read the Continue Session Progress row and compare with the dashboard's last-run card.

**Where:** `App launch > Continue Session > Progress row`

**Proposed fix:** Use the same duration formatter as the dashboard card (h/m/s), not integer minutes.

### A node can only be reordered by drag; the node context menu offers just Duplicate and Delete

`ux` · sequencer run-time (executing sequences end to end) · **not yet verified**

**What happens:** After adding Unpark Mount late (it appended to the end, after Take Exposures) the only way to move it earlier is drag-and-drop: right-clicking the node yields a popup menu containing exactly two items, 'Duplicate' and 'Delete' (accessibility tree: 'panel: Popup menu / button: Duplicate / button: Delete'). There is no Move Up / Move Down / Cut / Paste, and no keyboard equivalent. My drag attempt (press, 9 intermediate moves, release over the target row) left the order unchanged.

**Expected:** Move up / move down (or cut+paste) in the context menu, as NINA and SGP both provide. Order is semantically load-bearing here - unpark must precede slew - and a sequence with 30 instructions is not comfortably reordered by dragging in a scrolling tree.

**Repro:** Sequencer > Builder > add any node > right-click it > read the menu.

**Where:** `Sequencer > Builder > node context menu`

**Proposed fix:** Add Move Up / Move Down / Indent / Outdent to the context menu and bind them to Alt+Arrow.

### 'Sensor W' field label is ellipsized to 'Sensor...' in Calibration Library's matching preview

`ux` · settings (imaging / calibration / calibration-library / dark-library / captured-images / image-grading / adaptive-exposure / adaptive-conditions / preflight / autofocus / focus model) · **not yet verified**

**What happens:** In Settings > Imaging > Calibration Library > Matching Preview, the empty 'Sensor W' input renders its resting label as 'Sensor...' at width 90. The neighbouring 'Sensor H' fits, so the two fields cannot be told apart until one is filled (the floating label then shows in full).

**Expected:** Either widen the field or shorten the label to 'Sensor W (px)'/'W px' so it is legible when empty.

**Repro:** Settings > Imaging > Calibration Library, look at the Matching Preview field row.

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/calibration_library_settings.dart:973 (_field(_sensorWidth, 'Sensor W', 90))`

**Proposed fix:** Bump the width to 110 (as used for Exposure/Filter) or relabel.

### Required-field error text is truncated to an unreadable stub

`ux` · settings (imaging / calibration / calibration-library / dark-library / captured-images / image-grading / adaptive-exposure / adaptive-conditions / preflight / autofocus / focus model) · **not yet verified**

**What happens:** Clearing 'Max consecutive rejects' (allowClear:false) restores 3 and renders errorText 'A value is required.' inside a 140 px-wide field, where it clips to 'A value is requir...'. The user is told there is an error but not what it is.

**Expected:** Error text should render below the row at full width, not inside the fixed-width input.

**Repro:** Settings > Imaging > Image Grading (master toggle ON) > 'Max consecutive rejects': click, Ctrl+A, Delete, Enter.

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/image_grading_settings.dart:553 (effectiveWidth 140) and :575 (errorText inside the SizedBox-constrained TextField)`

**Proposed fix:** Move the error string out of InputDecoration.errorText into a full-width Text under the SettingRow, as the adaptive-exposure page already does.

### Night Narrator ticker produced zero insights across a 65-minute session with 12 frames, an autofocus run and a cooldown

`ux` · imaging, framing, guiding · **not yet verified**

**What happens:** NarratorTicker is mounted unconditionally at the top-right of the preview (live_preview_area.dart:629). Over a 65-minute driven session - 12 captures across two filters, a 9-point autofocus that moved best HFR from 2.46 to 2.10, a cooldown from +20 C to -10 C, a filter change, an aborted exposure - the ticker never rendered a single line in any of the ~40 Imaging screenshots. The Transient Alerts bell was likewise empty ('No active alerts'). Either the detectors need a sequencer run to fire, or the feature is invisible on the surface where it is advertised.

**Expected:** At minimum, something like 'Cooling reached -10.0 C' or 'Autofocus improved HFR 2.46 -> 2.10' should surface, since those are exactly the events the narrator is documented to detect.

**Repro:** 1. Imaging screen. 2. Run autofocus, cool the camera, change filter, take frames. 3. Watch the top-right of the preview.

**Where:** `packages/nightshade_app/lib/screens/imaging/widgets/narrator_ticker.dart, mounted at packages/nightshade_app/lib/screens/imaging/widgets/live_preview_area.dart:629`

**Proposed fix:** Confirm whether the narrator requires an active sequencer session; if so, either wire manual-capture events into it or do not mount the ticker on a surface where it can never speak.

### Default snapshot exposure is 55.0 s

`default` · imaging, framing, guiding · **not yet verified**

**What happens:** On first arrival at the Imaging screen with a fresh profile the Exposure field reads 55.0 sec and the bottom banner reads 'Dur 55 s'. The first thing anyone does on the Imaging screen is take a framing/focus test shot, and 55 s is a minute of waiting before you see anything.

**Expected:** A short default (1-5 s) for the manual Imaging screen, or last-used-value persistence. NINA defaults its manual capture to a short exposure for exactly this reason; long subs belong in the sequencer where the value is set per instruction.

**Repro:** 1. Fresh profile, complete onboarding (camera defaults step asks only for gain/offset, never exposure). 2. Nav -> Imaging. Read the Exposure field.

**Where:** `packages/nightshade_core/lib/src/providers/imaging_provider.dart:16 (exposureSettingsProvider default) / sequencer_defaults`

**Proposed fix:** Default the Imaging-screen exposure to ~2 s and persist the user's last manual value per profile.

### Rotator relative-move strip clips +15 deg off the card with no scroll affordance

`ux` · imaging, framing, guiding · **not yet verified**

**What happens:** Imaging -> Rotator tab, Relative Move renders -15 / -5 / -1 | +1 / +5 and the +5 button runs into the card border; +15 is entirely off-panel. The row is a horizontal SingleChildScrollView so the button is technically reachable by scrolling, but there is no fade, arrow or partial-button hint - unlike the filter strip in the bottom banner, which has a ShaderMask fade for exactly this reason.

**Expected:** Either the row fits at the default panel width, or it gets the same trailing-fade affordance the filter strip has.

**Repro:** 1. Imaging -> Rotator tab. 2. Look at Relative Move.

**Where:** `packages/nightshade_app/lib/screens/imaging/widgets/rotator_panel.dart:472-518 (horizontal SingleChildScrollView, no fade); compare imaging_bottom_banner.dart:540-547 which does fade`

**Proposed fix:** Wrap in the same ShaderMask fade, or drop to two rows at narrow widths.

### Science report timestamps are printed without a timezone, so a shared report is ambiguous by up to a day

`ux` · analytics (Session, History, Projects, Equipment Stats, Science, Diagnostics) + science screen · **not yet verified**

**What happens:** The exported report's Session block reads '**Started:** 2026-07-31T23:00:00.000'. The DB stores that session's start as 1785574800 = 2026-08-01T03:00:00Z. The report prints local wall time in ISO-8601 shape but with no offset and no 'Z', while the footer prints '_Generated by Nightshade 6.1.0 on 2026-08-02T14:42:11.518312Z._' with a Z.

**Expected:** One convention, stated. Photometry reports are shared with AAVSO/TNS and read by people in other timezones; an unlabelled local ISO timestamp next to a labelled UTC one invites a whole-night error.

**Repro:** Analytics > Science > Export report; read the Session block and the footer of the .md.

**Where:** `packages/nightshade_core/lib/src/services/science/science_report_exporter.dart:131 (Session block)`

**Proposed fix:** Emit UTC with the Z suffix (and optionally the local time in parentheses with its offset), as the footer already does.

### Every science rung sheet is a dead end: it names the blocker but gives no button to clear it

`ux` · analytics (Session, History, Projects, Equipment Stats, Science, Diagnostics) + science screen · **not yet verified**

**What happens:** Rung 3's sheet says 'Turn on live photometry and track a star to start collecting points' and offers only Close and 'Got it'. Rung 5's says 'Find a period - or build out the curve - before exporting' and offers only Close and 'Got it'. Neither has a control that does the named thing; the user has to close the sheet and go hunt for the Settings > Science toggle or the target picker themselves.

**Expected:** A guided-onboarding sheet that states a precondition should offer the action that satisfies it, the way the ladder card itself does for rung 1 ('Run calibration'). NINA's and the ASIAIR's equivalents deep-link.

**Repro:** Analytics > Science > click any locked rung in the Science guide.

**Where:** `packages/nightshade_app/lib/screens/analytics/widgets/science_onboarding/science_rung_sheet.dart`

**Proposed fix:** Give each rung an optional primary action (rung 2: open the photometry target picker; rung 3: open Settings > Science live photometry; rung 4: scroll to and run Period Analysis; rung 5: open the AAVSO/MPC export) and render it beside 'Got it'.

### FocusModelPanel is orphaned - the machine that builds the temperature-compensation model cannot display it

`missing` · imaging, framing, guiding · **not yet verified**

**What happens:** Imaging -> Focus tab contains only Manual Focus and Autofocus; there is no temperature-compensation model anywhere on the screen, and the 'TC' chip in the status bar has no drill-in (clicking it produced nothing). FocusModelPanel exists (per-filter tabs, scatter plot, slope/R2 prediction readout) but `grep -rn FocusModelPanel packages/` returns only its own definition plus a comment in settings/widgets/focus_model_settings.dart:16 that literally calls it 'the orphaned local FocusModelPanel'. So the only surface that shows the model is FocusModelSettings, which is documented 'Remote-only' and pulls the model over REST from a paired appliance. A desktop user running autofocus locally has no way to see the model their own runs are building.

**Expected:** The Focus tab shows the model built from the local autofocus history, with slope/R2/reliability and the per-filter tabs the widget already implements.

**Repro:** 1. Imaging -> Focus tab; run an autofocus. 2. Scroll the Focus tab - only Manual Focus and Autofocus exist. 3. Click the 'TC' chip in the status bar - nothing opens. 4. `grep -rn 'FocusModelPanel' packages/`.

**Where:** `packages/nightshade_app/lib/screens/imaging/widgets/focus_model_panel.dart:12 (defined, never constructed); packages/nightshade_app/lib/screens/settings/widgets/focus_model_settings.dart:16 (comment naming it orphaned)`

**Proposed fix:** Mount FocusModelPanel under the Focus tab (below Autofocus) and make the status-bar TC chip scroll to it.

### Period Analysis states the same period in three different units in one card (min, days, hours)

`ux` · analytics (Session, History, Projects, Equipment Stats, Science, Diagnostics) + science screen · **not yet verified**

**What happens:** On one scroll of the Period Analysis card the same detected period is written as '20.0 min' (BLS headline and detail table), 'P=0.014d' (the marker on the Lomb-Scargle periodogram), and 'P=0.33h' (the marker on the BLS spectrum). The two input fields are labelled 'Min period (d)' and 'Max period (d)', and the BLS spectrum's x axis is in hours (0.2h, 0.4h, 0.7h, 1.2h, 2.0h) while the LS periodogram's x axis is in 1/day.

**Expected:** One unit per card, or at least one unit for the markers that annotate the headline result. A user cross-checking 20.0 min against 0.33 h against 0.014 d has to do arithmetic to confirm the app is talking about the same peak.

**Repro:** Analytics > Science > Period Analysis > Run Period Search; compare the headline, the two chart markers and the two x axes.

**Where:** `packages/nightshade_app/lib/screens/analytics/widgets/period_analysis_panel/periodogram_painter.dart (marker label in days) vs bls_spectrum_painter.dart (marker label in hours) vs period_analysis_panel.dart _formatPeriod (minutes)`

**Proposed fix:** Use _formatPeriod for both chart markers so they read '20.0 min' like the headline; keep the axes in their natural units but label them explicitly.

### Image grader shows the same value 'off' with the slider thumb at opposite ends of two adjacent tracks

`ux` · analytics (Session, History, Projects, Equipment Stats, Science, Diagnostics) + science screen · **not yet verified**

**What happens:** In the Image grader, 'Max FWHM' reads 'off' with its thumb pinned at the far LEFT of the track, while the next row 'Max eccentricity' also reads 'off' with its thumb pinned at the far RIGHT. The two disabled rules look like opposite settings.

**Expected:** One visual convention for a disabled rule - e.g. thumb at the permissive end for every rule, or grey the whole row out.

**Repro:** Analytics > Science > 'Grade N frames' on a session whose frames have NULL fwhm and NULL eccentricity.

**Where:** `packages/nightshade_app/lib/screens/analytics/widgets/image_grader_dialog.dart (per-rule slider construction)`

**Proposed fix:** Render an off rule as a dimmed track with the thumb at a single fixed end, and put the enable/disable affordance in one place instead of a +/- glyph that differs per row.

### MPC export only offers the legacy 80-column format while the app's own transient panel documents ADES PSV

`ux` · analytics (Session, History, Projects, Equipment Stats, Science, Diagnostics) + science screen · **not yet verified**

**What happens:** The MPC Report Export card is subtitled 'Select observations to include in MPC 80-column format report' and produces only that format. The sibling TransientReportPanel's own header comment states 'MPC - generates an ADES PSV astrometry report you submit via the MPC web form.' Two paths in the same product target the same recipient in two different formats, and the one wired to the moving-object detector is the legacy one.

**Expected:** The MPC has been migrating submitters to ADES (PSV/XML) and the 80-column format is deprecated for new programmes. The moving-object export should emit ADES PSV (with the observatory code, telescope aperture/design and observer/measurer names ADES requires), or at least offer it as a choice, and the two panels should agree.

**Repro:** Analytics > Science > ANOMALIES > MPC Report Export; compare the subtitle with the class doc of transient_report_panel.dart.

**Where:** `packages/nightshade_app/lib/screens/analytics/widgets/mpc_export_panel.dart and packages/nightshade_core/lib/src/services/science/mpc_export_service.dart`

**Proposed fix:** Add an ADES PSV writer alongside the 80-column one and a format selector defaulting to ADES; carry the observer/measurer/telescope fields from the equipment profile.

### Photometric Calibration Wizard's frame picker has no select-all / filter-by-altitude, so building the airmass spread it asks for means clicking through 120 identical rows

`ux` · analytics (Session, History, Projects, Equipment Stats, Science, Diagnostics) + science screen · **not yet verified**

**What happens:** Analytics > Science > Photometric Transforms > Calibrate > step 1. The instructions read 'selecting frames of the same field at different altitudes adds airmass spread, which lets the fit also recover the atmospheric extinction coefficient.' For Night D the list is 120 rows, every one reading 'vt_0NN.fits / V | 120.0s | Stars: 1500 | RA: 120.0000 Dec: 25.0000'. There is no Select All, no Clear, no range select, no altitude or airmass column, and no way to sort. The only filter is a free-text Filter box for the filter NAME.

**Expected:** If the wizard wants an airmass spread it should show altitude/airmass per frame and offer 'pick N spread across altitude' or at least Select All / shift-click ranges. As built the user must find high- and low-altitude frames by eye from rows that do not show altitude at all.

**Repro:** Analytics > Science > Calibrate > choose a session with many solved frames.

**Where:** `packages/nightshade_app/lib/screens/analytics/widgets/photometric_calibration_wizard/frame_selection.dart:250-262 (row subtitle) - no bulk-selection controls anywhere in the file`

**Proposed fix:** Add an Airmass / Altitude column derived from mount_altitude (already stored on captured_images), sort by it, and add Select All / Select spread (lowest, median, highest airmass).

### Calibration wizard prints solved RA/Dec as bare decimals with no unit

`ux` · analytics (Session, History, Projects, Equipment Stats, Science, Diagnostics) + science screen · **not yet verified**

**What happens:** Every frame row in the calibration wizard reads 'RA: 120.0000 Dec: 25.0000'. No degree sign, no h/m/s, nothing to say the value is degrees rather than hours - and 'RA' is conventionally quoted in hours.

**Expected:** 'RA 08h00m00s  Dec +25 00 00' or at minimum 'RA 120.0000 deg'. The app elsewhere (MPC export) formats RA sexagesimally, so the two disagree.

**Repro:** Analytics > Science > Calibrate > pick a session with plate-solved frames; read any row.

**Where:** `packages/nightshade_app/lib/screens/analytics/widgets/photometric_calibration_wizard/frame_selection.dart:258-259`

**Proposed fix:** Reuse the sexagesimal formatter the MPC export already has, or append the unit.

### A CRITICAL 'Safe the rig' event has no history anywhere in the app

`ux` · settings (sequencer, science, phd2-guiding, plate-solving, weather-safety, notifications + all transports, integrations/plugins, annotations, observation log, auto-save, cloud-sync) · **not yet verified**

**What happens:** Seconds after the red CRITICAL 'Safe the rig' toast fired, I opened the bell in the title bar. It is a 'Transient Alerts' popover (astronomy transients) and it read 'No active alerts' with a 'View all alerts' link into the transients screen. There is no notification centre, no event history and no log surface in the UI where a rig-safing or other critical event can be reviewed after the toast disappears -- an operator who was asleep has no way to find out what happened without opening the raw text log.

**Expected:** A notification/event history reachable from the same bell (or a second bell), retaining critical and warning notifications for the session with timestamps. The 'Cry wolf' class of defects in this project is precisely about statements the app makes and then discards.

**Repro:** Trigger the CRITICAL toast (Weather Safety > enable, with fail-closed). Immediately click the bell icon in the title bar.

**Where:** `title-bar bell wires to the transient-alert popover, not to uiNotificationProvider history`

**Proposed fix:** Persist uiNotificationProvider entries for the session and add a history list to the bell popover, separated from transient astronomy alerts.

### Empty-chart messages splice the axis label into the sentence, producing "No °C recorded for these frames"

`ux` · analytics (Session, History, Projects, Equipment Stats, Science, Diagnostics) + science screen · **not yet verified**

**What happens:** The four Session-tab charts build their empty text from the axis label, giving 'No HFR (px) recorded for these frames', 'No RMS (arcsec) recorded for these frames', 'No Focuser position (steps) recorded for these frames' and - worst - 'No °C recorded for these frames'.

**Expected:** Use the chart's title, not its axis label: 'No sensor temperature recorded for these frames'. The unit-in-parentheses reads as a typo and the bare degree sign is meaningless as a noun.

**Repro:** Analytics > Session with a session that has no frames (or a fresh profile).

**Where:** `packages/nightshade_app/lib/screens/analytics/widgets/session_chart.dart (empty-state text composed from the y-axis label)`

**Proposed fix:** Pass a separate humanLabel ('sensor temperature', 'guiding RMS', 'focuser position', 'half-flux radius') for the empty message.

### About shows 6.1.0 but drops the build number, and System Information omits everything a support ticket needs

`ux` · settings (remote-access, updates, about, help, replay-debug, backup, equipment-profiles, catalogs, rig-catalogs) · **not yet verified**

**What happens:** About reports 'Version 6.1.0'. The shipped desktop version is 6.1.0+25 (apps/desktop/pubspec.yaml:4) -- the build number, the one field that distinguishes two builds of the same release, is not shown. 'System Information' lists only Platform / OS Version / Dart Version. It does not list the Rust bridge version, the database schema version, the data directory, the active backend, or the release channel, all of which are the first things asked for on a bug report.

**Expected:** NINA's About shows version+build+commit and a one-click 'copy system info'. Show '6.1.0 (build 25)', add schema version / data dir / bridge version, and give the card a Copy button.

**Repro:** Settings > About.

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/about_settings.dart:73,159-166`

**Proposed fix:** Render '${appVersion.version} (build ${appVersion.buildNumber})' and extend the System Information card; add a copy-to-clipboard action.

### Seven paired devices are all called 'Android companion' with no way to tell them apart before revoking

`ux` · settings (remote-access, updates, about, help, replay-debug, backup, equipment-profiles, catalogs, rig-catalogs) · **not yet verified**

**What happens:** The Paired Devices list shows 7 identical rows: title 'Android companion', subtitle 'Android phone or tablet', 'Not seen yet', 'No connection recorded yet', differing only by a paired-on date. There is no IP, no last-seen, no device model, no scope, and no rename. Revoking the phone you sold is guesswork.

**Expected:** Show last-seen, last IP, granted scope (the DB already stores auth_grant_spec -- one of these devices holds 'admin' and the UI never says so) and allow renaming. NINA/ASIAIR-class tools name the client and show when it last talked.

**Repro:** Settings > Remote Access > Manage Pairing.

**Where:** `packages/nightshade_app/lib/screens/settings/pairing_screen.dart:543-620 (_buildDeviceRow)`

**Proposed fix:** Render auth_grant_spec as a badge, add last-seen/IP, and add a rename action.

### Import failures are reported as raw Dart exception text ('Import failed: FormatException: Unexpected character (at character 1)')

`ux` · settings (remote-access, updates, about, help, replay-debug, backup, equipment-profiles, catalogs, rig-catalogs) · **not yet verified**

**What happens:** Equipment Profiles > import (the download-arrow button next to New Profile) accepts any file. Choosing a non-JSON file shows the snackbar 'Import failed: FormatException: Unexpected character (at character 1)'; choosing a syntactically valid but unrelated JSON shows 'Import failed: FormatException: Profile name must be a non-empty string'. Both are the raw toString() of the thrown exception.

**Expected:** 'That file is not a Nightshade profile export' plus the file name. The exported JSON also carries no format marker or version field at all (I exported one and read it: it is a bare map of profile fields), so the importer has nothing to check against and cannot warn about profiles exported by a newer build.

**Repro:** Settings > Equipment Profiles > import icon > pick any .txt. Then pick a JSON that is not a profile.

**Where:** `packages/nightshade_app/lib/screens/settings/equipment_profiles_screen_parts/profile_controls.dart (import handler) and the profile toJson used by Export`

**Proposed fix:** Add {"format":"nightshade.equipment-profile","version":1} to the export and validate it on import; map failures to plain-language messages.

### 'Restore' asks you to restart the app afterwards but does not offer to, and nothing on the screen stays to remind you

`ux` · settings (remote-access, updates, about, help, replay-debug, backup, equipment-profiles, catalogs, rig-catalogs) · **not yet verified**

**What happens:** The restore result is a transient snackbar: 'Restored 123 items. Restart Nightshade before the next run.' Once it fades there is no indication anywhere that the running app is now showing stale in-memory state that disagrees with the database -- and the whole point of restoring is usually that you are about to observe. There is no Restart button.

**Expected:** Either reload the providers after a restore (the app already rebuilds them on profile switch) or show a persistent banner with a 'Restart now' action.

**Repro:** Settings > Backup & Restore > restore icon on any bundle > Restore. Wait 5 seconds; the message is gone and the screen looks normal.

**Where:** `packages/nightshade_app/lib/screens/settings/backup_screen.dart restore result handling`

**Proposed fix:** Persistent banner + 'Restart now', or invalidate the affected providers in place.

### A 426 MB abandoned GLADE download sat in the catalog directory for six weeks with nothing in the UI acknowledging it

`ux` · settings (remote-access, updates, about, help, replay-debug, backup, equipment-profiles, catalogs, rig-catalogs) · **not yet verified**

**What happens:** ~/.local/share/com.example.nightshade_desktop/catalogs contained glade_plus_galaxies.csv.partial at 426,030,763 bytes dated Jun 17, alongside a 0-byte glade_plus_galaxies.csv. The Catalogs page showed GLADE+ as not installed and offered a fresh download, with no mention that 407 MiB of a previous attempt was still on disk and no way to reclaim it from the UI. (It is deleted when a new download starts -- I watched it vanish the moment I pressed Download -- so the space is recovered only by attempting another download, which currently cannot succeed.)

**Expected:** Show 'Partial download: 407 MB - Resume / Discard' on the card, or clean up partials on launch. Deep-sky users on small SSDs care about half a gigabyte.

**Repro:** ls -la ~/.local/share/com.example.nightshade_desktop/catalogs/ before pressing Download.

**Where:** `the annotation catalog download service's partial-file handling`

**Proposed fix:** Surface and offer to resume or delete partials; sweep stale .partial files at startup.

### The Deep-Star Tier card tells an astrophotographer to build and host their own tileset with a repo tool

`ux` · settings (remote-access, updates, about, help, replay-debug, backup, equipment-profiles, catalogs, rig-catalogs) · **not yet verified**

**What happens:** Settings > Catalogs > 'Deep-Star Tier (Tycho-2 / Gaia)' reads: 'Streams faint stars below the bundled HYG floor (~mag 11.5) as view-culled tiles when zoomed in. Host a tileset built with tools/catalog_prep and point the URL below at it.' That is an instruction to a developer with a checkout of this repository, shipped verbatim in a consumer settings page. There is no default host, no 'get one from Nightshade' option, and no explanation of what happens if you do not.

**Expected:** Ship or host a tileset and make this one toggle. If self-hosting really is the only path, say so honestly ('No public tileset is available yet; advanced users can host their own') and link documentation rather than a source path.

**Repro:** Settings > Catalogs > scroll to Deep-Star Tier.

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/deep_star_catalog_card.dart`

**Proposed fix:** Provide a default tileset URL or reword for a non-developer audience.

### Replay & Debug is written for the developer who built it: a lowercase title sentence and two user-facing dialogs that name a database table

`ux` · settings (remote-access, updates, about, help, replay-debug, backup, equipment-profiles, catalogs, rig-catalogs) · **not yet verified**

**What happens:** The page header reads 'Replay Debug' with the subtitle 'decision-log retention + cleanup for the retrospective Replay screen.' -- lowercase first word, a '+' used as a conjunction. The logging toggle's help text says decisions are 'persisted to the sequence_decisions table', and the destructive confirmation says 'This deletes every row in the sequence_decisions table.' The confirm dialog also does not say how many rows it is about to delete (there were 3), and its 'Clear' action is rendered as plain blue text identical to 'Cancel', whereas the equivalent profile-delete dialog uses a filled red 'Delete' button.

**Expected:** Sentence-cased prose that never names a SQL table; 'This will delete 3 recorded decisions' in the confirmation; and one consistent destructive-button style across dialogs.

**Repro:** Settings > search 'replay' > Replay & Debug. Read the header. Press 'Clear all replay history'.

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/replay_debug_settings.dart`

**Proposed fix:** Rewrite the copy for users, add the row count to the confirmation, and use the shared destructive-button variant.

### Enter does not submit the profile dialogs, so the fastest path through them is mouse-only

`ux` · settings (remote-access, updates, about, help, replay-debug, backup, equipment-profiles, catalogs, rig-catalogs) · **not yet verified**

**What happens:** In the Duplicate Profile dialog the name field is focused and pre-filled; pressing Enter does nothing -- the dialog stays open and no profile is created (DB unchanged). You must move to the mouse and click Duplicate.

**Expected:** Enter submits, Escape cancels. That is the convention in every dialog of this shape, and these dialogs are one-field.

**Repro:** Settings > Equipment Profiles > overflow > Duplicate > press Return. Nothing happens; the dialog is still up.

**Where:** `packages/nightshade_app/lib/screens/settings/equipment_profiles_screen_parts/profile_controls.dart dialog builders`

**Proposed fix:** Give the TextField an onSubmitted that fires the primary action.

### The 'No weather data sources available' warning recurs on a completely idle app

`ux` · settings (sequencer, science, phd2-guiding, plate-solving, weather-safety, notifications + all transports, integrations/plugins, annotations, observation log, auto-save, cloud-sync) · **not yet verified**

**What happens:** With weather safety enabled and fail mode Warn Only, the amber 'Weather Safety / No weather data sources available' toast reappeared at 10:00:59, 10:09:20, 10:24:19 and 10:30:22 local while the app sat idle on Settings pages with no run, no equipment and no state change. It is correct information, but as a recurring nag with nothing the user can act on from that toast it trains people to swipe away weather toasts -- which is precisely the class of alert you need them to read.

**Expected:** Emit the warning once per condition transition (or once per run start), and surface the standing condition as a persistent inline row on the Weather Safety page instead of a repeating toast.

**Repro:** Settings > Weather Safety > enable weather safety. Settings > Sequencer > Safety fail mode > Warn Only. Leave the app idle and watch for ~30 min.

**Where:** `packages/nightshade_core/lib/src/providers/weather_safety_provider.dart:762-776 (shouldShowFailModeWarning -> uiNotificationProvider.showWarning on every evaluation that resolves to preserve)`

**Proposed fix:** Track the last-warned condition in the notifier state and only re-emit when the condition string changes or a run starts.

### Search hint badge reads '⌘K' on Linux and Windows, where the shortcut is Ctrl+K

`ux` · planetarium + your_sky + constellation · **not yet verified**

**What happens:** The planetarium command bar renders a hardcoded '⌘K' chip next to Search on every platform. On the Linux desktop build this is the Apple Command glyph on a machine that has no Command key; the handler itself accepts either Meta or Control. The same chip is drawn in the plan panel's search field.

**Expected:** Show 'Ctrl+K' on Linux/Windows and '⌘K' only on macOS.

**Repro:** Plan Tonight > Planetarium on Linux; look at the Search control in the command bar and at the search field in the right panel.

**Where:** `packages/nightshade_app/lib/screens/planetarium/widgets/redesign/command_bar.dart:193`

**Proposed fix:** Switch the literal on defaultTargetPlatform.

### Object search has no exact-designation path: typing a catalogue number returns 175 fuzzy matches

`ux` · planetarium + your_sky + constellation · **not yet verified**

**What happens:** Typing '6720' - an unambiguous NGC designation - returns '175 results'. M57/NGC6720 is ranked first, which is right, but positions 2-6 are NGC7107, IC728, NGC7112, IC5118 and IC59, none of which contain the string '6720' in any designation, plus 169 stars. Selecting the wrong one silently moves the whole chart. There is no way to say 'exact match only', and no visible ordering rule that would let a user predict what they are about to click.

**Expected:** Exact designation matches first with a visible 'exact' section, then prefix matches, then fuzzy - and cap the fuzzy tail. NINA's object search and the ASIAIR both resolve an exact catalogue ID with a single hit. Also worth noting: the empty-state hint says 'Try "M42", "Andromeda Galaxy", or "Sirius"' which promises name search, but reaching the field at all needs three clicks (see the Search-button finding) and typing letters into it is broken by the view hotkeys (see the hotkey finding), so in practice the digits-only path is the only one that works.

**Repro:** Plan Tonight > Planetarium > right panel > Search tab > click the field > type 6720.

**Where:** `packages/nightshade_app/lib/screens/planetarium/widgets/mobile_widgets/search_sheet.dart`

**Proposed fix:** Rank exact designation > prefix > substring > fuzzy, section the list by match quality, and stop counting the fuzzy tail in the headline number.

### Weather Safety carries three hardware thresholds that are inert without a sensor, and no debounce/hysteresis anywhere

`ux` · settings (sequencer, science, phd2-guiding, plate-solving, weather-safety, notifications + all transports, integrations/plugins, annotations, observation log, auto-save, cloud-sync) · **not yet verified**

**What happens:** The Hardware Thresholds section offers Max humidity 90%, Max wind speed 30 km/h and Max cloud cover 80% with subtitles like 'Pause when sensor humidity exceeds this value'. On a rig with no weather device -- the exact rig the fail-closed default punishes -- all three are inert, and the page never says so. Separately, the page has no debounce, no 'N consecutive unsafe readings', and no 'wait N minutes after conditions clear before resuming'; the only timing controls are radar-alert distance (30 km) and lead time (15 min), which are about the cloud-radar API rather than the hardware gate. A single spurious humidity spike therefore trips the whole gate.

**Expected:** Grey out or annotate the threshold rows when no weather device is connected, and add an unsafe-debounce (consecutive readings or seconds) plus a resume-delay. NINA and SGP both expose these; without them the thresholds are all-or-nothing.

**Repro:** Settings > AUTOMATION & SAFETY > Weather Safety, full page.

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/weather_safety_settings.dart:170-268`

**Proposed fix:** Add a connected-source guard on the Hardware Thresholds section and add unsafeDebounceSeconds / resumeDelayMinutes to WeatherSettings with rows on this page.

### Region detail opens on a viewport-filling empty preview with no empty state; everything informative is below the fold

`ux` · planetarium + your_sky + constellation · **not yet verified**

**What happens:** Opening a newly created region shows a title bar ('Ring Region') and then a single card that fills the entire 1600x900 window with flat background and one small grey glyph in the middle - no text, no 'nothing imaged here yet', no action. Only after scrolling does the screen reveal the coordinates line '18h 54m . +33 02' . r 0.50', the hint 'Image this region across more nights to scrub it through time.', a 'Contributing frames' section reading 'No folds at this point in time.', and a 'Deepening over time' chart. The parent Your Sky screen gets this right ('Your sky is dark - for now' plus an explanation and a button); the detail screen does not.

**Expected:** Put the empty-state message inside the preview card, and size the preview so at least the coordinate line and the contributing-frames section are visible without scrolling on a standard desktop window.

**Repro:** Plan Tonight > Discover > Your Sky > create a region > click its card.

**Where:** `packages/nightshade_app/lib/screens/your_sky/region_detail_screen.dart`

**Proposed fix:** Give the preview a max height and an empty-state label.

### Enabling Morning Report in the widget picker adds nothing and gives no explanation

`ux` · dashboard, mosaic, polar_alignment, flat_wizard, collaborative_sky · **not yet verified**

**What happens:** Checking 'Morning Report' in the Dashboard Widgets dialog produced no visible tile anywhere on the dashboard (both columns scrolled to the end). The widget returns SizedBox.shrink() when there are no completed runs, so on a fresh profile the toggle reads as broken.

**Expected:** Either render an empty state ('No completed runs yet - last night's accepted/rejected counts will appear here'), consistent with how Storage and Last Run already do it, or mark the row in the picker as unavailable with the reason.

**Repro:** Dashboard -> Edit Dashboard -> Widgets -> check Morning Report -> Done -> scroll both columns.

**Where:** `packages/nightshade_app/lib/screens/dashboard/widgets/cockpit_morning_report.dart:30`

**Proposed fix:** Render the tile with an empty state rather than shrinking it, so the picker toggle and the dashboard agree.

### Science 'Show advanced overlay controls' (off) and 'Science overlays' (on) both promise PSF/residual layers with no disclosure that one gates the other

`ux` · settings (sequencer, science, phd2-guiding, plate-solving, weather-safety, notifications + all transports, integrations/plugins, annotations, observation log, auto-save, cloud-sync) · **not yet verified**

**What happens:** Two adjacent switches in the Mode section: 'Show advanced overlay controls -- Adds PSF, residual, and tile-map layers to the Imaging overlays menu' (ships OFF) and 'Science overlays -- Enable overlays such as PSF map, residual vectors, and tracks' (ships ON). They are independent booleans (advancedModeEnabled / overlayEnabled) whose subtitles name the same three artefacts. The shipped state therefore reads 'Science overlays: ON' while the control that exposes them is hidden, and neither row mentions the other.

**Expected:** This app already solved this exact problem one page away: the Weather Safety rows append '-- inactive until "Enable weather safety" is on' when the master switch is off. Do the same here, or collapse the pair into one switch plus a sub-option.

**Repro:** Fresh profile. Settings > SCIENCE > Science > Mode.

**Where:** `packages/nightshade_app/lib/screens/settings/widgets/science_settings.dart:50-73`

**Proposed fix:** Reuse the _withPrerequisite() helper pattern from weather_safety_settings.dart:73-77 for the dependent row.

### The History button in the polar alignment header appears to do nothing - the panel it opens is off-screen below the config column

`ux` · dashboard, mosaic, polar_alignment, flat_wizard, collaborative_sky · **not yet verified**

**What happens:** Clicking 'History' in the top-right of the Polar Alignment screen highlights the button (primary variant) but produces no visible change anywhere on screen: the Alignment History card is appended to the BOTTOM of the left configuration column, below Essential/Common/Advanced, and with Common+Advanced expanded it sits past the bottom of the viewport. I had to scroll the left column ~10 wheel clicks to discover it existed.

**Expected:** Toggling History should scroll/animate the panel into view, or open it as an overlay/right-rail panel. A header toggle whose only effect is off-screen is indistinguishable from a dead control.

**Repro:** Polar Alignment -> expand Common and Advanced -> click History (top right). Compare /tmp/ns-audit/shots/rw-116.png (before) and rw-117.png (after) - identical apart from the button tint. Then scroll the left column: /tmp/ns-audit/shots/rw-118.png shows the panel.

**Where:** `packages/nightshade_app/lib/screens/polar_alignment/polar_alignment_screen_parts/_configuration_panel.dart:54-57 (history appended after the collapsible sections) and _screen_shell.dart:31-36 (the toggle)`

**Proposed fix:** On toggleHistoryPanel(true), animate the config ScrollController to the panel; or move history to the top of the column / into the right rail where the empty space already is.

### Palette search with no matches renders a completely blank panel

`ux` · sequencer · **not yet verified**

**What happens:** Typing a term with no matches ('polar') empties the whole palette body - no 'no results' text, no suggestion, just black space under the search field. Nothing tells the user whether the search failed or the node does not exist.

**Expected:** 'No nodes match "polar"' with a clear-search affordance.

**Repro:** Sequencer > Builder > Nodes tab > type 'polar'.

**Where:** `packages/nightshade_app/lib/screens/sequencer/sequencer_screen_parts/node_palette.dart:120-140`

**Proposed fix:** Add an empty state when filteredCategories is empty.

### Polar alignment config controls stay visually enabled during a run but silently ignore clicks

`ux` · dashboard, mosaic, polar_alignment, flat_wizard, collaborative_sky · **not yet verified**

**What happens:** With an All-Sky run in progress I clicked Hemisphere 'South' and Binning '4x4'. Neither changed (they correctly stayed North / 2x2 - the run is not corrupted), but nothing indicated why: no dimming, no tooltip, no snackbar. The whole Essential/Common/Advanced column renders at full contrast during a run.

**Expected:** Dim/disable the configuration column while a run is active, with a tooltip such as 'Stop the alignment to change settings'.

**Repro:** Start an alignment, then click South and 4x4 in the left column. Compare /tmp/ns-audit/shots/rw-128.png (during run, controls at full contrast) with /tmp/ns-audit/shots/rw-129.png (after the clicks - unchanged).

**Where:** `packages/nightshade_app/lib/screens/polar_alignment/polar_alignment_screen_parts/_configuration_panel.dart`

**Proposed fix:** Wrap the column in AbsorbPointer + Opacity keyed on phase != idle, with an explanatory tooltip.

### Re-opening Polar Alignment shows the previous run's "Alignment Complete" screen instead of a fresh start

`ux` · dashboard, mosaic, polar_alignment, flat_wizard, collaborative_sky · **not yet verified**

**What happens:** After finishing a TPPA run and pressing Done (which navigates back to Imaging), re-entering Imaging -> Mount -> Three-Point Polar Alignment reopens straight onto the old completion screen: green tick, 'Alignment Complete', 'Final error: 1800.0 arcseconds', Before/After summary. The only way to get the instructions back is to press Restart.

**Expected:** Entering the wizard should show the idle/instructions state; the previous result belongs in the History panel, which already exists.

**Repro:** Run TPPA to completion -> Done -> Imaging -> Mount -> Three-Point Polar Alignment.

**Where:** `packages/nightshade_app/lib/screens/polar_alignment/polar_alignment_screen_parts/_screen_shell.dart / polarAlignmentStateProvider lifetime`

**Proposed fix:** Reset the alignment state to idle on screen dispose (or on entry) once the result has been persisted to history.

### Starters tab says 'copy one into your current sequence' but the action replaces the whole sequence

`ux` · sequencer · **not yet verified**

**What happens:** The Starters section is captioned 'Bundled, ready-to-run sample sequences. Tap a card to copy one into your current sequence.' Clicking 'Use this starter' discards the current sequence entirely and loads the starter (correctly prompting 'Discard unsaved changes?' first when the sequence is dirty - see verified_correct). 'Copy into' reads as append/merge.

**Expected:** 'Replace your current sequence with this starter.'

**Repro:** Sequencer > Templates > read the Starters caption, then click 'Use this starter' on any card with a non-empty sequence loaded.

**Where:** `packages/nightshade_app/lib/screens/sequencer/tabs/template_tab/_starters_section.dart`

**Proposed fix:** Reword the caption and the button ('Replace with this starter').

### Multi-Filter Batch and Sky Flats show bare unlabelled numbers under "Global Settings" (11% / +-10%)

`ux` · dashboard, mosaic, polar_alignment, flat_wizard, collaborative_sky · **not yet verified**

**What happens:** In Quick Capture the two sliders are captioned 'Histogram Target' and 'Tolerance'. On the Multi-Filter Batch and Sky Flats tabs the same two sliders appear under a single 'Global Settings' heading with no captions at all - just '11%' and '+-10%' stacked above 'Frames:'. A user who has not visited Quick Capture cannot tell which is the target and which is the tolerance.

**Expected:** Keep the same captions on every tab.

**Repro:** Flat Wizard -> Multi-Filter Batch (or Sky Flats) -> scroll to Global Settings.

**Where:** `packages/nightshade_app/lib/screens/flat_wizard/tuning_controls.dart`

**Proposed fix:** Reuse the captioned slider widget from Quick Capture on all three tabs.

### No move up / move down in the node context menu - reordering is drag-only

`ux` · sequencer · **not yet verified**

**What happens:** The right-click menu offers Skip to here, Insert Above, Insert Below, Duplicate, Group into Sequential Container, Group into Parallel Container, Disable, Delete - but no way to move the node one slot. On a 28-node tree that scrolls, moving a node past the viewport edge means a drag with autoscroll, which is the least reliable interaction in the whole builder. Separately, 'Group into Sequential Container' is enabled with a single node selected while 'Group into Parallel Container' is greyed out for the same selection, with no explanation for the asymmetry.

**Expected:** Move Up / Move Down entries (and Alt+Up/Alt+Down), plus consistent enablement between the two Group actions.

**Repro:** Sequencer > Builder > right-click any node row and read the menu.

**Where:** `packages/nightshade_app/lib/screens/sequencer/widgets/sequence_tree_context_menu.dart`

**Proposed fix:** Add the two entries backed by the same reorder mutation the drag handler calls.

### Quick-Start Wizard shows both a per-filter Count and a Loop Iterations field and never states the resulting frame total

`ux` · sequencer · **not yet verified**

**What happens:** Step 2 presents an inline table (Filter | Exposure | Count | Binning) with Count 10 and, directly under it, 'Loop Settings: Fixed Count / Iterations 10'. It is not stated whether those multiply. Step 5 (Review) lists 'Light 10x 120s (1x1)' and 'Loop: Type 10 iterations' and gives no total frame count and no total time. The built sequence turns out to be Loop x10 containing a 1x120s exposure = 10 frames, so the two 10s are the same number expressed twice.

**Expected:** One count field, and a running total on both step 2 and the review step: '10 frames, 20 min of integration, ~24 min with overhead'. That total is the single number a user needs to decide whether tonight's plan fits the window.

**Repro:** Sequencer > Templates > Quick-Start Wizard > step 2 and step 5.

**Where:** `packages/nightshade_app/lib/screens/sequencer/widgets/quick_start_wizard_dialog/_filter_step.dart and _review_step.dart`

**Proposed fix:** Collapse the duplicate count into one control and add a totals line derived from the same estimator the Builder header uses.

### The collaborative mosaic detail screen offers the mosaic's OWNER a "Join mosaic" button

`ux` · dashboard, mosaic, polar_alignment, flat_wizard, collaborative_sky · **not yet verified**

**What happens:** Opening the mosaic I had just published (collab_role='owner' in my own DB, and the detail header lists me as the only contributor) shows a card titled 'Image with this club' reading 'Join this mosaic to mirror its panel grid into a local project, then claim open panels and capture them on your rig' with a 'Join mosaic' button. Pressing it correctly navigates to my existing local project and does not duplicate it (mosaic_projects still has exactly 1 row), but the wording tells the owner to join something they own.

**Expected:** For collab_role='owner' the card should read 'Open the local project' / 'Manage panels'. Reserve 'Join mosaic' for mosaics owned by someone else.

**Repro:** Publish a mosaic to a hub -> Plan Tonight -> Discover -> Collaborate -> Refresh -> tap the mosaic row.

**Where:** `packages/nightshade_app/lib/screens/collaborative_sky/collab_mosaic_detail_screen.dart`

**Proposed fix:** Branch the CTA on the local project's collab_role.

### Loop defaults to 1 iteration and Conditional defaults to 'Always Execute' - both are no-ops out of the box

`default` · sequencer · **not yet verified**

**What happens:** A freshly added Loop node has Condition Type 'Fixed Count' and Repeat Count 1, i.e. it wraps its children and changes nothing. A freshly added Conditional has Condition Type 'Always Execute', i.e. it branches on nothing. Both silently do the harmless thing rather than prompting for the value that gives them a purpose.

**Expected:** Loop should default to a meaningful count (or open with the count field focused and empty so it must be set); Conditional should default to a condition that reads as unconfigured and surface a validation warning until it is set.

**Repro:** Sequencer > Builder > add 'Loop', read Properties. Add 'Conditional', read Properties.

**Where:** `packages/nightshade_core/lib/src/models/sequence/sequence_models/logic nodes (LoopNode.repeatCount, ConditionalNode.conditionType defaults)`

**Proposed fix:** Add a live-validation info rule 'Loop repeats once - it will have no effect' and 'Conditional always executes - no condition set', so the no-op state is at least visible.

### The Mosaic projects list has no way to create a mosaic and no back affordance

`ux` · dashboard, mosaic, polar_alignment, flat_wizard, collaborative_sky · **not yet verified**

**What happens:** Analytics -> Projects -> 'Mosaic projects' opens a full-page list titled 'Multi-panel mosaics: capture, integrate, stitch' with rows and nothing else: no 'New mosaic' button, no back arrow, no hint that mosaics are created from the Framing sidebar (which itself only exposes the mosaic panel once camera pixel specs are configured - with a Simulated Camera connected the sidebar still said 'Camera Not Configured' and never showed a mosaic section).

**Expected:** A 'New mosaic' action on the list (opening the same wizard Framing uses) and a back control. Otherwise the empty list is a dead end.

**Repro:** Analytics -> Projects -> Mosaic projects.

**Where:** `packages/nightshade_app/lib/screens/mosaic/mosaic_projects_list_screen.dart`

**Proposed fix:** Add a primary 'New mosaic' action that opens the mosaic wizard, and a leading back button.

### Settings search jumps to the section but not to the setting you searched for

`ux` · dashboard, mosaic, polar_alignment, flat_wizard, collaborative_sky · **not yet verified**

**What happens:** Typing 'Smart Night' into the Settings search returned one result, 'Sequencer'. Clicking it opened the Sequencer settings page scrolled to the top, at 'Auto focus on filter change' - the Smart Night Defaults group was about 30 wheel-clicks further down, with no highlight and no anchor.

**Expected:** Deep-link to the matched row and flash it, or list the matching rows rather than just the section.

**Repro:** Settings -> search 'Smart Night' -> click the Sequencer result.

**Where:** `packages/nightshade_app/lib/screens/settings/settings_search_index.g.dart consumers`

**Proposed fix:** Carry the matched row key through the navigation and scroll/highlight it on arrival.

### Narrowband mixer opens with every channel weight at 0.00, i.e. a palette that renders black

`default` · science / session_review / stack_result / first_light · **not yet verified**

**What happens:** With Ha, OIII and SII masters present the mixer opens labelled 'Custom mix' with all nine R/G/B weights at 0.00. Applying that state would map every channel to zero.

**Expected:** When the resolved channel set is Ha+OIII+SII, default to SHO; when it is Ha+OIII only, default to HOO; when it is a single narrowband master, default that channel to a luminance mapping. A user should never have to make three clicks to get out of a state that cannot produce an image.

**Repro:** 1. Seed Ha/OIII/SII masters. 2. Session Review > Workbench > Narrowband mixer. 3. Read the nine weight values and the 'Custom mix' label.

**Where:** `packages/nightshade_app/lib/screens/session_review/widgets/narrowband_mixer_panel.dart:92 (_palette = 'custom') and the zero-initialised _weights`

**Proposed fix:** Initialise _palette/_weights from the best-matching preset for widget.channels in initState/didUpdateWidget instead of 'custom' + zeros.

### Filter is a free-text field with no binding to the wheel; a typo becomes a 2am run failure

`ux` · sequencer · **not yet verified**

**What happens:** Change Filter's Properties offers a plain text box pre-filled with 'L' plus an 'Edit filters...' link. Take Exposures shows 'No filters in profile' in the same style. With no wheel connected there is no dropdown, no validation and no autocomplete, so 'Ha' vs 'H-alpha' vs 'HA' is a silent mismatch discovered mid-run.

**Expected:** Bind to the active equipment profile's filter list (a dropdown), with free text only as an explicit override, and validate the name against the profile at build time rather than at run time. NINA and SGP both bind this to the configured wheel.

**Repro:** Sequencer > Builder > add 'Change Filter' > read Properties > Filter Settings.

**Where:** `packages/nightshade_app/lib/screens/sequencer/widgets/node_properties_panel_parts (filter settings section)`

**Proposed fix:** Render a DropdownButton over profile.filterNames when available; keep the text field only when the profile has none, and add a validation warning when the typed name is not in the profile.

### Two different things are called Save, two different things are called Templates, and the tab names collide

`ux` · sequencer · **not yet verified**

**What happens:** The toolbar floppy icon is 'Save Sequence' and writes a file via the OS chooser; saving your work into the app lives on a different tab as 'Sequences > Save Current'. Separately, the Builder's left panel has a tab named 'Snippets' whose panel header reads 'Templates' and whose search box reads 'Search templates...', while a top-level tab is also called 'Templates' and holds something different (starters + saved sequence templates). A new user cannot tell which Save persists their work or which Templates they are in.

**Expected:** 'Export sequence file...' for the toolbar action and 'Save' for the library action; and one of the two Templates surfaces renamed (the left panel is snippets - fragments - so its header should say 'Snippets').

**Repro:** Compare the toolbar floppy tooltip with Sequences > Save Current; then compare the Snippets tab's panel header with the Templates top-level tab.

**Where:** `packages/nightshade_app/lib/screens/sequencer/sequencer_screen_parts/snippet_palette.dart; widgets/sequence_toolbar.dart:487-494`

**Proposed fix:** Rename as above. Low cost, removes a real confusion.

### Mosaic preview shows only 5 of 9 panel numbers for a 3x3 grid and hides the centre panel's number under the drag handle

`ux` · sequencer · **not yet verified**

**What happens:** With Grid 3x3 and 'Active panels: 9' in the summary, the sky preview draws panel labels 8, 9, 6, 2, 3 and puts the drag-centre handle over the middle cell. Panels 1, 4, 5 and 7 have no visible number, so the user cannot map a panel in the picture to a panel in the plan or know which one they are toggling off.

**Expected:** Every active panel legible and numbered; the drag handle should not sit on top of a label.

**Repro:** Sequencer > Builder > toolbar grid icon (Plan Mosaic) > leave the 3x3 default > look at the sky preview.

**Where:** `packages/nightshade_app/lib/screens/sequencer/widgets/mosaic_wizard_dialog/painters.dart and visual_planner.dart`

**Proposed fix:** Draw labels after the overlap fills so they are not overpainted, and offset the centre drag handle (or move the centre label).

### An unrecognised transient-kind token is presented to the user as a definite 'Dipole' artefact classification, which also silently blocks submission

`ux` · science / session_review / stack_result / first_light · **not yet verified**

**What happens:** TransientKind.fromWire falls back to TransientKind.dipole for any token it does not recognise. The candidate card then renders a 'Dipole' chip -- a specific, factual claim about the residual's morphology -- and Submit refuses with 'Dipole artefacts are not real transients - they cannot be submitted.' I hit this with a row whose kind was 'new_source' instead of the wire token 'newSource': the card asserted the detection was a dipole artefact and would not let it be reported. The fallback is deliberate and documented ('the least-exciting, artefact-leaning class'), but the UI presents the fallback as a measurement.

**Expected:** Keep the conservative gating, but label it honestly: an unknown token should render as 'Unclassified' and the refusal message should read 'This detection has an unrecognised classification and cannot be submitted', not assert a dipole morphology the differencing code never reported.

**Repro:** 1. Insert a transient_detections row with kind='new_source'. 2. Analytics > Science > First Light. 3. The card shows a 'Dipole' chip; pressing Submit toasts 'Dipole artefacts are not real transients - they cannot be submitted.' 4. Change kind to 'newSource' and restart -> the same row now reads 'New source' and Submit opens the dialog.

**Where:** `packages/nightshade_core/lib/src/services/transients/transient_candidate.dart:45 fromWire; rendered at packages/nightshade_app/lib/screens/first_light/widgets/transient_candidate_card.dart:383-405`

**Proposed fix:** Add an `unknown` member to TransientKind, make fromWire return it, label it 'Unclassified', and gate submission on `kind == newSource || kind == pointBrightening || kind == movingStreak` rather than on `!= dipole`.

### Live Stacking's explanatory helper text is truncated to one ellipsised line

`ux` · sequencer · **not yet verified**

**What happens:** Under 'Broadcast mode' the helper reads 'Broadcast only keeps memory clean; Recor...' and under 'Stack combine method' it reads 'Average is fastest; Median+Rej and Sigma r...'. These sentences exist precisely to help the user choose between the dropdown options, and the half that names the alternative is cut off with no tooltip or expansion.

**Expected:** Wrap to as many lines as needed - the panel scrolls anyway.

**Repro:** Sequencer > Builder > add 'Live Stacking' > read the two helper lines in Properties.

**Where:** `packages/nightshade_app/lib/screens/sequencer/widgets/live_stacking_properties.dart`

**Proposed fix:** Remove maxLines/TextOverflow.ellipsis from the helper Text widgets.

### Validation badges on the sequence header are not clickable, so the only way to read '1 error' is to press Start

`ux` · sequencer · **not yet verified**

**What happens:** A newly created empty sequence immediately displays a red '1' and an amber '1' in the header. Clicking either does nothing (they are plain containers). There is no issues panel in the Builder. The only way to learn what they are is to press Start and read the Pre-Flight dialog - which for the empty case turns out to be 'Empty Sequence' and 'No Exposures', i.e. 'you have not built anything yet', shown as a red error on a screen the user just opened.

**Expected:** Clicking the badge opens the same issue list the Pre-Flight dialog shows. And 'you just created an empty sequence' should be an info/hint, not a red error.

**Repro:** Sequencer > Builder > toolbar 'New Sequence'. Click the red '1' badge: nothing. Press Start to discover what it means.

**Where:** `packages/nightshade_app/lib/screens/sequencer/widgets/sequence_tree/support_widgets.dart:595-676 (_ValidationBadges / _MiniCountBadge have no gesture handler)`

**Proposed fix:** Wrap the badges in an InkWell that opens the validation issue list read-only.

### After discovery has already failed, the biggest button on screen re-runs the thing that just failed

`ux` · mobile first run and connection - main.dart connection screen, mobile_discovery_ops, qr_scanner_screen, saved_servers_screen, tailscale_setup_sheet, first_run_setup_screen, checkpoint_resume_dialog · **not yet verified**

**What happens:** The connection screen offers seven actions. Six are equally-weighted low-contrast text buttons (Enter IP 209x126px, Scan QR 216x126px, Connect over Tailscale, Connect via Relay, Saved servers, Skip Connection) and exactly one is a filled primary button: 'Search for Server' (996x131px), which repeats the automatic discovery that ran on launch and failed 4 seconds ago. The action with the highest success probability for someone standing next to their rig - Scan QR, because the desktop puts a QR on screen when you start pairing - is a small text button, and the error card that lists 'Scanning the QR code from the desktop' as the first thing to try offers no button to do it.

**Expected:** Once discovery has failed at least once, promote 'Scan QR' to the primary filled button and demote 'Search for Server' to a text action ('Scan again'). Better still, make the three bullets in the failure card tappable so 'Scanning the QR code from the desktop' opens the scanner directly - that removes a full step from the most likely recovery path.

**Repro:** Launch with no appliance, wait for the failure card, count the actions and their weights.

**Where:** `apps/mobile/lib/main.dart (connection screen action list)`

### The Tailscale sheet tells the user to go install an app but will not take them there, and never checks whether it is already installed

`ux` · mobile first run and connection - main.dart connection screen, mobile_discovery_ops, qr_scanner_screen, saved_servers_screen, tailscale_setup_sheet, first_run_setup_screen, checkpoint_resume_dialog · **not yet verified**

**What happens:** Step 1 of the sheet reads 'Install Tailscale on this phone - Get it from the App Store / Play Store and sign in with the same account as your observatory PC.' It is plain text: nothing is tappable, so the user must leave Nightshade, find the store, search for Tailscale, install, sign in, and come back and retype everything (which the sheet then discards on the next failure). The sheet also never checks whether Tailscale is present or up on this phone, even though that is trivially detectable on Android and is the single most common reason the connect will fail.

**Expected:** On a phone this should be a button that deep-links to the Play Store listing (market://details?id=com.tailscale.ipn), and step 1 should collapse to a green tick when the package is already installed. Detecting 'Tailscale is not installed' before the connect attempt turns a mystery timeout into a one-line answer.

**Repro:** Connection screen -> Connect over Tailscale -> read step 1; nothing in it is a control.

**Where:** `apps/mobile/lib/widgets/tailscale_setup_sheet.dart (step list)`

### Manual entry asks for an 'Access Token' that a first-time user cannot have, and never mentions the port it will use

`ux` · mobile first run and connection - main.dart connection screen, mobile_discovery_ops, qr_scanner_screen, saved_servers_screen, tailscale_setup_sheet, first_run_setup_screen, checkpoint_resume_dialog · **not yet verified**

**What happens:** The Enter-IP form has exactly two fields: 'Server address' and 'Access Token' (with a show/hide eye). A first-run user has no access token - the real first-connect credential is the pairing CODE, which the app asks for afterwards in its own dialog. There is no helper text saying the token is optional, and no hint that the address may include a port or that 8080 is assumed; the user only learns the port from the failure message afterwards ('Cannot reach http://192.0.2.77:8080/api/info').

**Expected:** Label it 'Access token (optional - leave blank to pair with a code)', exactly the phrasing the Tailscale sheet already uses for the same field ('Leave blank to pair with a code'), and give the address field the hint '192.168.1.20 or 192.168.1.20:8080 (default 8080)'.

**Repro:** Connection screen -> Enter IP -> inspect the two fields.

**Where:** `apps/mobile/lib/main.dart (manual entry form)`

### Session History's 'Refresh history' toolbar action is enabled while disconnected and produces no perceptible change, unlike every other dead action in the app

`ux` · mobile operation - companion dashboard shell, mount/sequencer/log/settings tabs, session picker and session replay, driven disconnected on the ns_test_api35 emulator · **not yet verified**

**What happens:** With no server paired, Session History correctly shows 'Connect to a server / Session replay requires the phone to be paired with a Nightshade server.' But the AppBar's 'Refresh history' button stays enabled. Tapping it runs _refresh -> _loadMore, which immediately hits `if (backend is! NetworkBackend)` and re-sets the same _LocalBackendNotSupported error, re-rendering an identical screen. No spinner, no SnackBar, no change of any kind. I tapped it and both the a11y tree and the screen were byte-identical before and after. The app disables dead controls correctly everywhere else on this dashboard - the Sequencer's Start and Stop both dump as [DISABLED], and the Log tab's 'Clear log' dumps as [DISABLED] - so this one enabled-but-inert action is inconsistent with the app's own established convention.

**Expected:** Disable the refresh action when the backend is not a NetworkBackend, exactly as Start/Stop/Clear log already do, so `tree` reports [DISABLED] and a gloved 2am tap does not go unanswered.

**Repro:** 1. Companion UI enabled; Skip Connection. 2. AppBar overflow 'More' -> 'Session history / replay'. 3. `drive_android.py tree` and note the screen. 4. `drive_android.py tap 'Refresh history'`. 5. `drive_android.py tree` again.

**Where:** `apps/mobile/lib/screens/replay/session_picker_screen.dart:182-183 (action), 60-71 (_loadMore early return)`

**Proposed fix:** `onPressed: (_loading || ref.watch(backendProvider) is! NetworkBackend) ? null : () => unawaited(_refresh())`.

### The validation error/warning badges in the sequence header are not clickable

`ux` · sequencer (the 37 seq-node:* instruction types) · **not yet verified**

**What happens:** The sequence header shows chips like '9 errors / 5 warnings / 1 info'. Clicking the red error chip does nothing — no popover, no jump to the offending node, no panel. The only way to read the issues is to press Start and open the Pre-Flight dialog, which is a strange thing to have to do while editing.

**Expected:** Clicking a badge should open the issue list (the pre-flight content already exists) and clicking an issue should select the offending node.

**Repro:** Build a sequence with errors, click the red '1' chip next to the sequence name.

**Where:** `packages/nightshade_app/lib/screens/sequencer/widgets (sequence header validation chips)`

