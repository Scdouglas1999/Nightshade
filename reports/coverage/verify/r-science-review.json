[
 {
  "title": "'Integrate now' silently threw away 8 of 9 subs and presented the 1-frame result as a finished master, with no error, warning or count anywhere in the UI",
  "severity": "P0",
  "kind": "defect",
  "unit": "screen:session_review/workbench_view.dart",
  "what_happened": "With 9 accepted 300s subs, clicking 'Integrate now' produced a master card reading 'M51 - L / 1 frame - 00:05:00 integration - L' and no other feedback: no toast, no error banner, no 'x of y subs rejected' line. The database shows integrated_master_frames has 1 accepted row and 8 rows with rejection_reason 'registration failed: too few stars to register: reference=0, frame=N, need >= 3', and integrated_masters.stats_json is {\"framesIntegrated\":1,\"framesRejected\":8,...}. Two distinct failures compound: (a) the integrator picked sub 1 as the alignment reference, detected 0 stars in it, and then failed every other sub against that empty reference instead of choosing a different reference or aborting; (b) the count of dropped subs exists in the outcome and in the DB but is never rendered for a manual integration.",
  "expected": "Two things. (1) A reference frame with 0 detected stars must not be accepted as the reference -- fall back to the next-best frame (frames 4-9 each detected 29-30 stars) or abort with 'could not find a usable reference frame'. (2) Integration must report its own result: 'Integrated 1 of 9 subs - 8 failed to register' with the reason, exactly as the automatic-integration path already does (auto_integration_service.dart:378 builds precisely that sentence). Losing 89% of a night's data must never be silent.",
  "repro": "1. Seed 10 lights for one session with real FITS star fields (star sigma 1.0-2.25 px). 2. Analytics > History > Audit Night A > sparkles. 3. Workbench > 'Reject HFR above' at 3.5 (leaves 9 accepted). 4. Scroll the right column to Integration settings, click 'Integrate now'. 5. Wait ~40 s. 6. Read the Masters card and look for any error surface.",
  "evidence": "UI after the run: Masters card 'M51 - L / 1 frame - 00:05:00 integration - L' (/tmp/ns-audit/shots/rsr-24.png), header still '9 accepted - 1 rejected', no toast (/tmp/ns-audit/shots/rsr-21.png). DB: sqlite3 nightshade.db \"select image_id,accepted,rejection_reason from integrated_master_frames where master_id=2\" -> 1|1|(null) then 8 rows 0|'registration failed: too few stars to register: reference=0, frame=4..30, need >= 3'. stats_json {\"framesIntegrated\":1,\"framesRejected\":8}. App log: '[STAR_DETECT] Detected 0 stars after filtering' on the reference frame.",
  "where": "packages/nightshade_app/lib/screens/session_review/session_review_controller.dart:710 integrate() -- the returned outcome is stored in state.lastOutcome but workbench_view.dart:190 uses it only for a preview path and a button label; framesIntegrated/framesRejected are never rendered. Reference selection is in the native post-session integration path."
 },
 {
  "title": "Project-growth panel groups subs by UTC calendar day, so one continuous evening is reported as two nights and every night is dated a day late",
  "severity": "P1",
  "kind": "defect",
  "unit": "screen:session_review/workbench_view.dart",
  "what_happened": "Session Review (narrative) 'This project over time' reports '2.0h integrated - 24 frames across 4 nights' with x-axis labels Jul 26 / Jul 28 / Jul 29 / Jul 30, and 'Best night: Jul 28 - 0.5h'. The database contains exactly THREE observing sessions (Audit Night A on the evening of Jul 25 local, B on Jul 27 local, C on Jul 29 local). Audit Night C is one continuous 50-minute run (19:30-20:20 America/New_York) that straddles 00:00 UTC, and the panel splits it into two separate 'nights' (Jul 29 with 4 frames, Jul 30 with 4 frames). Every other night is also labelled with the following calendar day because 21:00 EDT is 01:00 UTC.",
  "expected": "Nights should be grouped by OBSERVING night in the site's local time (the conventional noon-to-noon boundary), so three sessions produce three points labelled Jul 25, Jul 27, Jul 29 and the panel says '3 nights'. Never a UTC calendar day: for any observer west of Greenwich the label is a day late, and for any observer east of Greenwich (all of Europe/Asia) the 00:00 UTC boundary falls in the MIDDLE of the observing night, splitting every single night in two.",
  "repro": "1. Seed three sessions of M51 lights, the third spanning 19:30-20:20 America/New_York on 2026-07-29 (crosses 00:00 UTC). 2. Analytics > History > Audit Night A > sparkles (Review & Integrate). 3. Scroll the narrative view to 'This project over time'.",
  "evidence": "UI: '2.0h integrated - 24 frames across 4 nights', axis Jul 26 / Jul 28 / Jul 29 / Jul 30, 'Best night: Jul 28 - 0.5h' (/tmp/ns-audit/shots/rsr-08.png). DB: sqlite3 nightshade.db \"select strftime('%Y-%m-%d',captured_at,'unixepoch') utc, strftime('%Y-%m-%d',captured_at,'unixepoch','localtime') local, count(*) from captured_images group by 1\" -> 2026-07-26|2026-07-25|10, 2026-07-28|2026-07-27|6, 2026-07-29|2026-07-29|4, 2026-07-30|2026-07-29|4. Three local nights, four UTC days.",
  "where": "packages/nightshade_core/lib/src/services/smart_project_service.dart:679 _dayOf() -> DateTime.utc(dt.year, dt.month, dt.day); used by growthCurve(), bestNight() and reReferenceAdvice()"
 },
 {
  "title": "Narrowband mixer 'Apply palette' is a dead control: no composite, no file, no error, no log line",
  "severity": "P1",
  "kind": "defect",
  "unit": "screen:session_review/narrowband_mixer_panel.dart",
  "what_happened": "With three finalized single-filter masters for the target (Ha 20 frames, OIII 15, SII 10), the Narrowband mixer renders correctly, SHO/HOO presets populate the weight matrix correctly, and 'Apply palette' is enabled. Pressing it five times (three via click-img, twice via a raw xdotool click on the hovered button) produced nothing at all: narrowband_composites stayed at 0 rows, no FITS was written anywhere on disk, no toast appeared in the accessibility tree on six consecutive polls, and the app log recorded no combine activity. The screen's toast path is demonstrably alive -- the same session surfaced 'Curve is out of date - re-integrate to refresh before culling' from the cull rail -- and the neighbouring 'Run both' A/B button responded immediately with a 'Preview...' state and native STAR_DETECT log lines, so neither the toast machinery nor the click mapping explains the silence.",
  "expected": "Apply palette should either produce a narrowband_composites row plus an on-disk composite FITS and render it in the composite card, or fail loudly with the reason ('Need at least two finalized narrowband masters to combine.' is already written for that case). Silently doing nothing on the mixer's only action makes the whole SHO/HOO feature untestable by the user.",
  "repro": "1. Seed three finalized integrated_masters for the target with filter Ha / OIII / SII and real master_fits_path files. 2. Session Review > Workbench > Refresh. 3. Scroll to 'Narrowband mixer', press SHO (matrix fills SII->R, Ha->G, OIII->B correctly). 4. Press 'Apply palette'. 5. sqlite3 nightshade.db 'select count(*) from narrowband_composites' -> 0; no new .fits under the masters output dir; no toast.",
  "evidence": "/tmp/ns-audit/shots/rsr-30.png (mixer with 3 channels), rsr-33.png (SHO matrix + enabled Apply palette), rsr-35.png (cursor hovering the button, confirming the hit target). AT-SPI polls immediately after the click contain only 'panel: Combine matrix' -- no error node. DB: narrowband_composites count 0 after 5 presses. Log tail contains nothing between the click and the next discovery sweep.",
  "where": "packages/nightshade_app/lib/screens/session_review/widgets/narrowband_mixer_panel.dart:195-202 (onApply) -> workbench_view.dart:304 -> session_review_controller.dart:1233 runNarrowband() -> post_session_seam.dart:327 combineChannels()"
 },
 {
  "title": "Stack & Share byte-swaps every pixel when 'Apply calibration' is on (the default), so it reports 'Reference frame has only 0 stars' and cannot stack anything",
  "severity": "P0",
  "kind": "defect",
  "unit": "screen:stack_result/stack_and_share_dialog.dart",
  "what_happened": "With 8 accepted 300s L subs whose FITS provably contain 40 well-separated stars each (peak 40614 ADU over a 440 ADU background, sigma 22 ADU -> SNR ~1800), pressing Start in Stack & Share fails instantly with 'Failed to initialize stacker: Reference frame has only 0 stars, need at least 5 for alignment'. Turning OFF the 'Apply calibration' switch and pressing Start again on the SAME frames initialises fine -- the log reads 'Live stacker initialized: 512x512 reference with 34 stars'. The two paths differ only in how the pixels reach the stacker: calibration-off uses the native file loader (stacking_start_from_file), calibration-on loads the frame into Dart via apiReadFitsLinearData and hands back a u16 buffer (stacking_start_from_data). The bridge's image_data_to_linear_f64 decodes the ImageData buffer with u16::from_be_bytes, but fits.rs wrote that buffer with to_le_bytes (as does every other reader in the imaging crate -- processing.rs, calibration.rs, registration.rs, stretch.rs, star_reduction.rs, background_extraction.rs and lib.rs all use from_le_bytes). Every pixel therefore comes back byte-swapped: a 440 ADU background becomes 0xB801 = 47105 and a 40614 ADU star becomes 0xA69E = 42654, so the background spans ~42000-52000 with ~10000 ADU of fake noise and the stars are buried. Hence zero detections. 'Apply calibration' is ON by default, so out of the box Stack & Share fails 100% of the time on 16-bit FITS -- the format every camera writes.",
  "expected": "apiReadFitsLinearData must decode ImageData with the same endianness fits.rs encoded it with (little-endian). Stack & Share with the default options must stack the session's frames.",
  "repro": "1. Have a session with 8 accepted 16-bit (BITPIX=16, BZERO=32768) light FITS. 2. Imaging > Stack > Stack & Share. 3. Leave all options at their defaults and press Start -> 'Reference frame has only 0 stars'. 4. Toggle 'Apply calibration' OFF and press Start -> stacker initialises with 34 stars (it then fails later for an unrelated reason, see the residual finding). 5. Verify the frames really have stars: python3 peak-finder over audit_L_017..024.fits reports 40 local maxima each, top fluxes ~37000-40000 over a 440 median with sigma 22.",
  "evidence": "UI: /tmp/ns-audit/shots/rsr2-09.png shows 'Apply calibration' ON with 'Stack & Share failed / NightshadeError.imageError(field0: Failed to initialize stacker: Reference frame has only 0 stars, need at least 5 for alignment)'; /tmp/ns-audit/shots/rsr2-10.png shows the same dialog with the toggle OFF. App log (/tmp/ns-audit/r-science-review/app.log): three 'Starting live stacking from data: 512x512 (262144 pixels)' entries that each ended in the 0-stars error, then '14:53:10 Starting live stacking with reference: .../audit_L_024.fits' followed by 'Live stacker initialized: 512x512 reference with 34 stars'. Independent pixel check of audit_L_017.fits: median 440, MAD-sigma 22.2, 40 local maxima, brightest 40614.",
  "where": "native/nightshade_native/bridge/src/api/imaging.rs:1537-1560 image_data_to_linear_f64 (from_be_bytes for U16/U32/F32/F64) vs native/nightshade_native/imaging/src/fits.rs:322-365 (to_le_bytes). Reached from packages/nightshade_core/lib/src/services/stack_and_share_service.dart:724 _loadRawU16 -> stacking_engine_seam.dart:172 apiReadFitsLinearData."
 },
 {
  "title": "A single sub that fails alignment aborts the entire Stack & Share run and deletes the partial result, even though the code counts 'rejected' frames as a normal outcome",
  "severity": "P1",
  "kind": "defect",
  "unit": "screen:stack_result/stack_and_share_dialog.dart",
  "what_happened": "With 'Apply calibration' off the stacker initialised on audit_L_024.fits with 34 stars, then the very first follower frame (audit_L_018.fits) was rejected for alignment residual and the whole run died: the dialog showed 'Stack & Share failed', no stacked_results row was written, and the log shows 'Live stacker stopped and resources released' one millisecond after the rejection -- frames 019-024 were never attempted. The native layer treats this as a per-frame rejection (it increments stats.rejected_alignment_failures and returns Err), and the Dart loop even has a framesRejected counter for exactly this case, but the counter is dead code: _addStackFrame is awaited without a try/catch, so the Err becomes an exception that escapes the for-loop into the run-level catch, which deletes the incomplete stacked_results row and rethrows.",
  "expected": "A frame that fails to register must be counted and skipped, and the run must continue with the remaining subs -- exactly what the framesRejected counter was written for. Losing an entire night's stack to one satellite-trailed or wind-shaken sub is the single most expensive failure mode this feature has. The dialog should end with 'Stacked 6 of 8 subs - 2 rejected (alignment)'.",
  "repro": "1. Imaging > Stack > Stack & Share on an 8-sub session, turn 'Apply calibration' off, press Start. 2. Watch the log: 'Adding frame to stack: .../audit_L_018.fits' then 'Frame rejected: alignment residual 10.34px exceeds max 10.00px (28 matches)' then 'Live stacker stopped'. 3. Dialog reads 'Stack & Share failed'. 4. sqlite3 nightshade.db 'select count(*) from stacked_results' -> unchanged (1, the pre-seeded row); no new row for this run.",
  "evidence": "App log 2026-08-02T14:53:10: 'Live stacker initialized: 512x512 reference with 34 stars' / 'Adding frame to stack: audit_L_018.fits' / 'WARN Frame rejected: alignment residual 10.34px exceeds max 10.00px (28 matches)' / 'Live stacker stopped and resources released'. UI: 'Stack & Share failed - NightshadeError.imageError(field0: Alignment residual too high: 10.34px (max 10.00px))'. DB: stacked_results count 1 before and after.",
  "where": "packages/nightshade_core/lib/src/services/stack_and_share_service.dart:328 (`final result = await _addStackFrame(...)` inside the followers loop, no try/catch) and the now-unreachable framesRejected accounting at :335-340; run-level catch that deletes the row at :484."
 },
 {
  "title": "Registration reports a 10.34px alignment residual for a pure 2.2px translation of the same 40-star field, so even the working path cannot stack; match_radius_px defaults to 50px",
  "severity": "P1",
  "kind": "defect",
  "unit": "screen:stack_result/stack_and_share_dialog.dart",
  "what_happened": "audit_L_018.fits and audit_L_024.fits contain the SAME 40 stars offset by a rigid ~2.2px translation -- every star in 024 has a counterpart in 018 at distance 2.0-3.16px (median 2.24px), verified independently with a peak-finder and a nearest-neighbour match. The stacker matched 28 pairs and then computed an alignment residual of 10.34px, which exceeded its own 10.00px ceiling, so the frame was rejected. A correct affine fit on that geometry gives a sub-pixel residual. With match_radius_px defaulting to 50.0 and match_flux_tolerance to 0.7 in a 512x512 frame whose mean star separation is ~81px, the matcher is free to pair a star with a neighbour 50px away, and a handful of such wrong pairs is enough to wreck the least-squares fit and inflate the residual.",
  "expected": "Registering two frames that differ by a 2.2px shift must produce a residual well under 1px and the frame must be accepted. Separately, max_alignment_residual_px = 10.0 is not a meaningful quality gate -- a 10px misalignment folded into the stack is a visibly smeared image; NINA/DSS-class tolerances are a fraction of a pixel.",
  "repro": "1. Stack & Share on the 8-sub session with 'Apply calibration' off. 2. Log: 'Frame rejected: alignment residual 10.34px exceeds max 10.00px (28 matches)'. 3. Independently measure the true offset: peak-find both FITS (40 peaks each) and nearest-neighbour match -> min 2.00px, median 2.24px, max 3.16px.",
  "evidence": "App log 'WARN Frame rejected: alignment residual 10.34px exceeds max 10.00px (28 matches)'. Measured geometry: n024=40, n018=40, match distances min 2.0 / median 2.24 / max 3.16 px. Config defaults at native/nightshade_native/imaging/src/stacking.rs:85-104.",
  "where": "native/nightshade_native/imaging/src/stacking.rs:91 match_radius_px: 50.0, :92 match_flux_tolerance: 0.7, :94 max_alignment_residual_px: 10.0; residual computed at :487 compute_alignment_residual and the fit at :485 compute_affine_transform."
 },
 {
  "title": "First Light's 'Could not load candidates' Retry button does not re-query -- the error survives Retry and a tab round-trip, and only a full app restart clears it",
  "severity": "P1",
  "kind": "defect",
  "unit": "screen:first_light/transient_candidate_card.dart",
  "what_happened": "The First Light candidate list threw while decoding one row and rendered 'Could not load candidates / FormatException: Invalid radix-10 number (at character 1) / tile-042' with a Retry button. I then fixed the offending value in the database (tile_id 'tile-042' -> 261982, confirmed typeof=integer) and pressed Retry twice, and switched to the Science sub-tab and back: the identical error text -- including the stale 'tile-042' fragment -- was still on screen. Only killing and restarting the app made the list load. The same staleness affects the success path: after the list HAS loaded, editing a detection's row (kind) outside the app and switching sub-tabs away and back still rendered the old value, so the candidate list is fetched once per app launch and is never refetched.",
  "expected": "Retry must invalidate the provider and re-run the query -- that is the entire contract of a Retry button. And the candidate list must refresh as new detections arrive: the panel's own header promises 'Every solved frame is differenced against your deep sky atlas ... Anything that appeared, moved, or brightened on a deep patch lands here to review', which is false if the list is a one-shot read taken at app launch.",
  "repro": "1. Put a row in transient_detections whose tile_id is not parseable (SQLite type affinity permits a TEXT value in the INTEGER column). 2. Analytics > Science > First Light -> 'Could not load candidates'. 3. Fix the row: update transient_detections set tile_id=261982. 4. Press Retry -> same error. 5. Switch to Science and back to First Light -> same error. 6. Restart the app -> the candidate renders correctly.",
  "evidence": "/tmp/ns-audit/shots/rsr2-15.png (error state with Retry). AT-SPI tree after two Retry presses and a tab round-trip still contains 'panel: Could not load candidates' / 'FormatException: Invalid radix-10 number (at character 1) tile-042'. DB at that moment: select id,tile_id,typeof(tile_id) -> 1|261982|integer. After a restart the same tab shows 'Possible unknown transient / 13h 29m 52.7s +47 11 43.1'.",
  "where": "packages/nightshade_app/lib/screens/first_light/first_light_view.dart (candidate list provider + the Retry action on its error state)"
 },
 {
  "title": "Observing Alerts reports a First Light detection's brightness CHANGE as its apparent magnitude, so a self-discovered transient is announced as 'mag 2.4  NAKED EYE'",
  "severity": "P1",
  "kind": "defect",
  "unit": "screen:science/science_screen.dart",
  "what_happened": "Analytics > Science > Observing Alerts renders the First Light detection as 'NS J13.50+47.2 / Supernova - Unconfirmed - possible new transient / 13h 29m 52s +47 11' 43\" / mag 2.4 [NAKED EYE]'. The detection row has delta_mag = -2.35 (a brightness CHANGE in magnitudes relative to the atlas template) and no photometric zero point anywhere in the pipeline -- there is no apparent magnitude for this object at all. transient_alert_mapper.dart:20 sets `magnitude = detection.deltaMag?.abs()`, and the card at transient_card.dart:581 then buckets `magnitude <= 6.0` as NAKED EYE, <= 10 BINOCULAR, <= 14 SMALL SCOPE. Because a difference-image delta magnitude of more than ~6 is essentially impossible, EVERY First Light self-discovery that has a measured delta is announced as a naked-eye or binocular object. The app is telling the user they have found a magnitude-2.4 supernova -- brighter than Polaris, and the brightest supernova in recorded history.",
  "expected": "Do not populate the alert's apparent-magnitude field from a delta magnitude. Either leave magnitude null (the card already handles a null magnitude by omitting the chip) and show the delta separately as '\u03942.35 mag', or compute a real apparent magnitude from a photometric zero point when one exists. The NAKED EYE / BINOCULAR / SMALL SCOPE bucketing must never run on a delta.",
  "repro": "1. Insert a transient_detections row with kind='newSource', delta_mag=-2.35, snr=18.4, reviewed=1. 2. Restart the app. 3. Analytics > Science > Observing Alerts. 4. Read the chip on the alert card.",
  "evidence": "/tmp/ns-audit/shots/rsr2-21.png: green chip 'mag 2.4  NAKED EYE' next to a card titled 'NS J13.50+47.2 / Supernova'. DB: select id,kind,delta_mag,snr,fwhm from transient_detections -> 1|newSource|-2.35|18.4|2.41. There is no apparent-magnitude column on the table at all and photometric_transforms is empty, so 2.4 cannot be an apparent magnitude by construction.",
  "where": "packages/nightshade_core/lib/src/services/transients/transient_alert_mapper.dart:20 (`final magnitude = detection.deltaMag?.abs();`) consumed by packages/nightshade_app/lib/screens/transients/widgets/transient_card.dart:553 and :580-588 _getBrightnessInfo"
 },
 {
  "title": "The photometry 'Uncertainty' column mixes two units: magnitudes for target/check rows and raw ADU flux noise for every comparison-star row, ~3 million times larger",
  "severity": "P1",
  "kind": "defect",
  "unit": "screen:science/science_screen.dart",
  "what_happened": "Analytics > Science > (export hub) > Photometry Measurements > CSV exports 495 rows with columns ... SNR, Uncertainty ... For target and check rows Uncertainty is a magnitude sigma (0.00054-0.00071 across all 198 such rows). For every one of the 297 comparison rows it is instead the flux noise in ADU (1912.7-2211.8). The identity is exact, not approximate: `select max(abs(uncertainty - flux/snr)) from photometry_measurements where role='comparison'` returns 0.0. So one column, in the database and in the CSV a user hands to AAVSO or plots error bars from, carries two physical quantities that differ by a factor of about 3.4 million, with no unit column to tell them apart.",
  "expected": "Uncertainty must be a magnitude sigma for every row. A comparison star's uncertainty is 1.0857/SNR (or the same variance-propagated sigma the check star already gets); the ADU flux noise, if it is wanted, belongs in a separate `flux_uncertainty` column with its own header.",
  "repro": "1. Run a science session so photometry_measurements is populated (495 rows here). 2. Analytics > Science > toolbar database icon > Photometry Measurements > CSV. 3. Open the CSV: target/check rows have Uncertainty ~6e-4, comparison rows ~2.1e3. 4. Confirm in the DB: select role,count(*),min(uncertainty),max(uncertainty),max(abs(uncertainty-flux/snr)) from photometry_measurements group by role -> check|99|0.00056|0.00071|2354.4, comparison|297|1912.7|2211.8|0.0, target|99|0.00054|0.00068|2478.3.",
  "evidence": "Exported file /home/scdouglas/Documents/Nightshade/exports/photometry_2026-08-02T11-16-14.csv, e.g. image 100: target_primary flux 4629522 snr 2119.58 unc 0.000597 / comparison_2 flux 4506308 snr 2090.34 unc 2155.777 where flux/snr = 2155.777. Source: packages/nightshade_core/lib/src/services/science/science_processing_service/private_helpers.dart:465-471 `uncertainty: drift.Value<double?>(checkUncertainty ?? star.flux / (star.snr...))`.",
  "where": "packages/nightshade_core/lib/src/services/science/science_processing_service/private_helpers.dart:465-471"
 },
 {
  "title": "The cull rail advertises 'Keep best 1 (+0%)' from a curve it already knows is stale; pressing it always refuses, and its advice ('re-integrate to refresh') reproduces the same stale curve",
  "severity": "P2",
  "kind": "defect",
  "unit": "screen:session_review/sub_cull_rail.dart",
  "what_happened": "The improvement curve is derived from the master that was just produced. Because that master contained only 1 registered frame (see the P0 above), the persisted curve has one point and a recommendation of {keepN: 1, predictedSnrGainPct: 0.0, reason: 'Keep all 1 subs: already optimal (+0.0% SNR vs full stack)'}. The cull rail then renders an enabled primary action 'Keep best 1 (+0%)' whose tooltip is 'Drop to the optimizer-recommended best 1 subs (+0% SNR)'. Pressing it three times changed nothing (still 9 accepted in the DB) and toasted 'Curve is out of date - re-integrate to refresh before culling'. So the control knows the curve does not match the 9 accepted subs, yet still renders a specific, quantified, actionable label -- and its remedy is to re-run the same integration that produced the stale curve.",
  "expected": "The rail must decide staleness at render time, not at press time. When the curve's population does not cover the currently-accepted subs, or when predictedSnrGainPct <= 0, show a disabled/absent action (or an explicit 'analysis out of date' chip) rather than a confident 'Keep best 1 (+0%)'. A control should not offer an operation it will unconditionally refuse.",
  "repro": "1. Do the Integrate-now repro above (produces a 1-frame master). 2. A new button 'Keep best 1 (+0%)' appears beside Blink mode / Select-lasso. 3. Click it. 4. sqlite3 nightshade.db \"select count(*) from captured_images where session_id=1 and is_accepted=1\" -> still 9; toast reads 'Curve is out of date - re-integrate to refresh before culling'.",
  "evidence": "/tmp/ns-audit/shots/rsr-21.png, rsr-22.png, rsr-25.png show 'Keep best 1 (+0%)' while the header reads '9 accepted - 1 rejected'; AT-SPI tree after the click contains 'panel: Curve is out of date - re-integrate to refresh before culling'. DB improvement_curve_json for master 2: {\"points\":[{\"n\":1,...}],\"recommendation\":{\"keepN\":1,\"keptIndices\":[0],\"predictedSnrGainPct\":0.0,\"reason\":\"Keep all 1 subs: already optimal (+0.0% SNR vs full stack)\"},\"population\":[\".../audit_L_001.fits\"]} -- population length 1 vs 9 accepted, which is exactly the staleCurve guard in session_review_controller.dart:1377.",
  "where": "packages/nightshade_app/lib/screens/session_review/widgets/sub_cull_rail.dart:473 (tooltip) and :475 (label 'Keep best $keepN (+${gainPct}%)'); guard in session_review_controller.dart:1354 cullToRecommended()"
 },
 {
  "title": "sub_gallery_panel.dart (601 lines) is built by no screen -- only by a widget test -- while the code claims the narrative view uses it",
  "severity": "P3",
  "kind": "defect",
  "unit": "screen:session_review/sub_gallery_panel.dart",
  "what_happened": "Grepping every .dart file under packages/ and apps/ finds exactly one construction of SubGalleryPanel, in packages/nightshade_app/test/screens/session_review/sub_cull_rail_dynamic_subs_test.dart:102. Neither narrative_view.dart nor workbench_view.dart references it, and driving the app confirms it: the narrative view renders hero preview, Night Doctor, improvement curve, findings, project growth and the calibration cards, and contains no sub grid at all. sub_cull_rail.dart:24 states 'One controller, two renderings: the narrative screen uses SubGalleryPanel', which is not true of the shipped build.",
  "expected": "Either the narrative view shows the sub gallery (as the comment says and as the two-renderings design implies), or the dead panel and its comment are deleted. As shipped, a user reviewing a night in Narrative mode has no way to see, blink, accept or reject individual subs without switching to Workbench.",
  "repro": "1. Open Session Review for any session. 2. Stay in Narrative and scroll top to bottom -- no sub grid appears. 3. grep -rn 'SubGalleryPanel(' packages apps --include='*.dart' -> one hit, in a test.",
  "evidence": "/tmp/ns-audit/shots/rsr-07.png, rsr-09.png, rsr-10.png cover the entire narrative scroll: sections are 'How deep is deep enough?', 'What I fixed and found', 'This project over time', 'Calibrated & annotated'. grep output as above.",
  "where": "packages/nightshade_app/lib/screens/session_review/widgets/sub_gallery_panel.dart (whole file); false claim at widgets/sub_cull_rail.dart:24"
 },
 {
  "title": "The 'Maximum Quality' integration profile is a placebo -- it produces byte-identical settings to Balanced",
  "severity": "P2",
  "kind": "defect",
  "unit": "screen:session_review/integration_settings_panel.dart",
  "what_happened": "Selecting each of the five integration profiles in turn and diffing the whole ADVANCED block of the accessibility tree: Fast changes the resampler to Bilinear, Few Subs changes rejection to Percentile clip with 0.2/0.1, Long Night / Gradient changes normalization Mode from Global to Local grid -- but Balanced and Maximum Quality produce an IDENTICAL 30-line block (`diff bal.txt maxq.txt` is empty) and an identical summary sentence. The source confirms it: IntegrationSettings.preset(maximumQuality) sets resampler=lanczos3, weighting=snrSquared, generateRejectionMap=true, cosmeticCorrection=true, outputBitDepth=f32 -- every one of which is already the constructor default that balanced inherits. The only field that differs between the two presets is sourcePreset, which is a label.",
  "expected": "A profile named 'Maximum Quality' must actually cost something in exchange for quality -- e.g. a tighter RANSAC threshold, more reference stars, local normalization, linear-fit/percentile rejection tuned for the sub count, or drizzle. As shipped, a user who deliberately picks Maximum Quality over Balanced for their best data gets exactly the same stack and is misled about it.",
  "repro": "1. Session Review > Workbench > Integration settings > expand Advanced settings. 2. Click 'Balanced', capture the ALIGNMENT..DRIZZLE block of `drive_linux.py tree`. 3. Click 'Maximum Quality', capture again. 4. diff -> no differences. 5. Repeat for 'Long Night / Gradient' -> exactly one line differs (Global -> Local grid), proving the capture method detects real changes.",
  "evidence": "/tmp/ns-audit/bal.txt vs /tmp/ns-audit/maxq.txt -> identical (30 lines each); /tmp/ns-audit/bal.txt vs /tmp/ns-audit/ln.txt -> 1 line differs. Summary line unchanged: 'Will integrate 9 accepted subs using Winsorized sigma. Recommends culling the worst 10%.' for both. Source: packages/nightshade_core/lib/src/models/imaging/integration_settings.dart:737-745 vs the constructor defaults at :672,:676,:690,:691,:692.",
  "where": "packages/nightshade_core/lib/src/models/imaging/integration_settings.dart:737 IntegrationSettings.preset(IntegrationPreset.maximumQuality)"
 },
 {
  "title": "Running an A/B recipe preview leaves a full 'Preview... 100%' progress bar pinned under the header forever, so the screen permanently claims a job is running",
  "severity": "P2",
  "kind": "defect",
  "unit": "screen:session_review/workbench_view.dart",
  "what_happened": "Pressing 'Run both' in the A/B recipe compare panel puts a green progress strip labelled 'Preview... 100%' between the Narrative/Workbench toggle and the content. The two previews completed (the panel filled in with 'Frames kept tie 1 vs 1', 'Residual tie 0.00 vs 0.00 px', 'Integration time tie 00:05:00 vs 00:05:00'), but the strip was still on screen 13 minutes later, survived switching to Narrative and back, and is still present in the AT-SPI tree as 'panel: Preview...'.",
  "expected": "The strip must clear when the action that raised it finishes. The controller's own comment says a terminal 1.0 fraction 'is left on screen until the action's own completion handler clears it' -- integrate() does clear it (clearProgress/clearLiveProgress) but reIntegratePreview(), which is what the A/B panel calls, has no such handler, so the strip is orphaned.",
  "repro": "1. Session Review > Workbench > A / B compare > 'Run both'. 2. Wait for both preview cards to fill in. 3. The strip under the header still reads 'Preview... 100%'. 4. Toggle to Narrative and back -- still there. 5. `drive_linux.py tree | grep Preview` still returns 'panel: Preview...'.",
  "evidence": "/tmp/ns-audit/shots/rsr-36.png (strip at 100% with the A/B results already rendered above it), rsr-37.png / rsr-38.png (same strip 13 min later, in Narrative and in Workbench). AT-SPI: 'panel: Preview...' persists across polls.",
  "where": "packages/nightshade_app/lib/screens/session_review/session_review_controller.dart:515 _bindProgress() and :976 reIntegratePreview() (no clearProgress on completion); rendered by session_review_screen.dart:119 _ProgressStrip"
 },
 {
  "title": "A/B recipe compare declared 'tie 1 vs 1' when both recipes had silently dropped 8 of 9 subs",
  "severity": "P2",
  "kind": "defect",
  "unit": "screen:session_review/workbench_view.dart",
  "what_happened": "After the failed 9-sub integration, 'Run both' in A/B compare produced 'Frames kept  tie  1 vs 1', 'Residual  tie  0.00 vs 0.00 px', 'Integration time  tie  00:05:00 vs 00:05:00'. Both recipes registered only 1 of the 9 accepted subs, and the panel presents that as a neutral tie between two recipes rather than as two failed runs. A user comparing recipes would conclude the two are equivalent, when in fact neither produced a usable stack.",
  "expected": "When framesKept is far below the accepted-sub count the comparison is meaningless and must say so -- e.g. 'Recipe A registered 1 of 9 subs; recipe B registered 1 of 9. Neither result is comparable.' A 'tie' verdict on two degenerate stacks is a false statement about the data.",
  "repro": "1. With 9 accepted subs whose reference frame yields no detectable stars, Session Review > Workbench > A / B compare > 'Run both'. 2. Read the verdict row.",
  "evidence": "/tmp/ns-audit/shots/rsr-36.png: 'Frames kept tie 1 vs 1 | Residual tie 0.00 vs 0.00 px | Integration time tie 00:05:00 vs 00:05:00', while the header reads '9 accepted - 1 rejected'.",
  "where": "packages/nightshade_app/lib/screens/session_review/widgets/ab_compare_panel.dart:561 (_cmp on framesIntegrated)"
 },
 {
  "title": "Stack & Share surfaces the raw Rust error enum to the user: 'NightshadeError.imageError(field0: ...)'",
  "severity": "P2",
  "kind": "defect",
  "unit": "screen:stack_result/stack_and_share_dialog.dart",
  "what_happened": "Both failures rendered in the dialog's error card verbatim as 'NightshadeError.imageError(field0: Failed to initialize stacker: Reference frame has only 0 stars, need at least 5 for alignment)' and 'NightshadeError.imageError(field0: Alignment residual too high: 10.34px (max 10.00px))'. The wrapper type name and the flutter_rust_bridge positional-field label 'field0:' are shown to the operator.",
  "expected": "Show the message only, plus an actionable next step ('No stars were detected in the reference frame. Check focus, or turn off Apply calibration.'), with the raw type available behind a 'technical details' disclosure like ErrorDialog already provides elsewhere in this app.",
  "repro": "1. Imaging > Stack > Stack & Share > Start on any 16-bit FITS session. 2. Read the red card.",
  "evidence": "/tmp/ns-audit/shots/rsr2-09.png (verbatim text in the dialog).",
  "where": "packages/nightshade_app/lib/screens/stack_result/stack_and_share_dialog.dart error rendering; the message is the toString() of the bridge NightshadeError."
 },
 {
  "title": "After session recovery the imaging Session card says 'Captured 0 frames' while the line under it says 'Integration 40m 0s'",
  "severity": "P2",
  "kind": "defect",
  "unit": "screen:stack_result/stack_and_share_dialog.dart",
  "what_happened": "Resuming an interrupted session (Incomplete Sessions Found > Resume, on a session with 8 successful exposures and 2400s integration) leaves the Imaging screen's Session card reading 'Captured  0 frames' directly above 'Integration  40m 0s', 'Status Active', 'Duration 87:14:28'. Zero frames and forty minutes of integration cannot both be true. The card reads the frame count from sessionImages.length -- the list of frames captured in THIS app run, which is empty after a restart -- while the integration line reads the restored sessionState.totalIntegrationSecs.",
  "expected": "Both numbers must come from the same source. After recovery the card should read 'Captured 8 frames / Integration 40m 0s' (SessionStateNotifier.recoverSession already restores completedExposures = 8 from the DB row; the same field is used correctly by the detail panel's 'Images Captured:' line at capture_panel.dart:427).",
  "repro": "1. sqlite3 nightshade.db \"update imaging_sessions set status='active', end_time=null where id=3\". 2. Restart the app. 3. 'Incomplete Sessions Found' > Resume. 4. Imaging > read the Session card.",
  "evidence": "/tmp/ns-audit/shots/rsr2-07.png: 'Captured 0 frames', 'Integration 40m 0s', 'Status Active'. Recovery dialog itself correctly reported '8 exposures / 40m total' (/tmp/ns-audit/shots/rsr2-06.png). DB: imaging_sessions id 3 successful_exposures=8, total_integration_secs=2400.",
  "where": "packages/nightshade_app/lib/screens/imaging/widgets/capture_panel.dart:201-208 (Captured = sessionImages.length) vs :221-227 (Integration = sessionState.totalIntegrationSecs)"
 },
 {
  "title": "The Share Card's metadata strip is clipped: it renders 'M51  Integration 00:35:00  Frames 7  Fil\u2026' and the filter is cut off the right edge",
  "severity": "P2",
  "kind": "defect",
  "unit": "screen:stack_result/stack_result_screen.dart",
  "what_happened": "Stack result > Share Card exports a 512x512 RGB PNG whose bottom caption bar reads 'M51   Integration  00:35:00   Frames  7   Fil' and then hits the right edge mid-word. The caption is laid out at a font size derived from the image HEIGHT (ShareCardSpec.fontScale defaults to ShareCardFontScale.scaleToHeight) with no measurement against the available WIDTH, so any card narrower than the caption needs simply truncates. This is the artefact the feature exists to produce -- the thing a user posts publicly -- and it ships with the text running off the edge.",
  "expected": "Either measure the caption and shrink the font / wrap to a second line until it fits the card width, or render the card on a fixed-size canvas (e.g. 1200x1200 or 1200x675) with the image letterboxed, so the caption geometry is independent of the stack's pixel dimensions.",
  "repro": "1. Produce any stack result whose image is 512px wide. 2. Stack result screen > 'Share Card' > save to a .png. 3. Open the PNG and read the bottom strip.",
  "evidence": "/tmp/ns-audit/rsr-sharecard.png (copied to /tmp/ns-audit/shots/rsr2-12.png): 512x512, bit depth 8, colour type 2; bottom bar reads 'M51 | Integration 00:35:00 | Frames 7 | Fil'. The result screen and the AstroBin sidecar both carry 'Filter L', so the data is present -- only the rendering truncates.",
  "where": "packages/nightshade_core/lib/src/models/imaging/stack_and_share_models.dart:644-676 ShareCardSpec (layout bottomBar, fontScale scaleToHeight); composition in packages/nightshade_core/lib/src/services/stack_share_export_service.dart"
 },
 {
  "title": "In the Submit-discovery dialog all three network chips are dead on a default install, and only the currently-selected network's blocker is explained",
  "severity": "P2",
  "kind": "defect",
  "unit": "screen:first_light/transient_submit_dialog.dart",
  "what_happened": "Submit discovery opens with Network = TNS and an amber line 'TNS submission needs your bot id/name, reporting group, data source, reporter, and api key -- configure them in Settings > Science.' Clicking the AAVSO chip does nothing (verified twice, once with coordinates measured from a cropped region shot so the hit point was unambiguous); TNS stays selected. Preview and 'Submit to TNS' are equally inert -- no dialog, no snackbar, no log line, no file. All three chips are disabled because _formatEnabled is false for each: AAVSO needs an observer code AND a magnitude zero point, MPC needs an observatory code AND a mover-class detection, TNS needs the five bot fields. None of the chips render a disabled affordance, and the panel prints a reason only for the format that happens to be selected, so a user who wants the AAVSO export -- the cheapest one to enable, it needs a single settings field -- gets a chip that silently ignores them and a message about TNS credentials that has nothing to do with their problem.",
  "expected": "A disabled chip must look disabled and must say why on hover/press. Better: list all three networks with their individual status ('AAVSO - needs observer code', 'MPC - not a moving object', 'TNS - needs bot key') so the user can see at a glance which one is reachable and what it costs.",
  "repro": "1. Seed a confirmed newSource detection. 2. Analytics > Science > First Light > Submit. 3. Click AAVSO -> nothing; TNS stays selected (/tmp/ns-audit/shots/rsr2-19.png vs rsr2-20.png are identical). 4. Click Preview -> nothing, no snackbar within 1s, nothing in the log.",
  "evidence": "/tmp/ns-audit/shots/rsr2-18.png (whole dialog), rsr2-19.png and rsr2-20.png (network row before and after an AAVSO click, byte-for-byte the same state). Source: transient_report_panel.dart:127-140 (`onTap: enabled ? ... : null`) and :142-148 (the reason block only renders _disabledReason for `_format`).",
  "where": "packages/nightshade_app/lib/screens/analytics/widgets/transient_report_panel.dart:127-148 and :296-320 (Preview/Copy), surfaced by packages/nightshade_app/lib/screens/first_light/widgets/transient_submit_dialog.dart"
 },
 {
  "title": "Confirming a First Light candidate does not update the Observing Alerts tab beside it: the same detection reads 'Confirmed' on one sub-tab and 'New' on the next",
  "severity": "P2",
  "kind": "defect",
  "unit": "screen:science/science_screen.dart",
  "what_happened": "On Analytics > Science > First Light I pressed Confirm on the candidate; the card's badge changed to 'Confirmed', the toast read 'Marked as confirmed.' and the database row updated (reviewed=1). Switching one tab across to Observing Alerts -- the same screen, the same detection, mapped through transientAlertFromDetection -- the card still carries a blue 'New' badge and sits under the 'New' filter. transientAlertFromDetection maps reviewed -> TransientAlertState.acknowledged, so the alert list is being served from a snapshot taken at app launch and is not rebuilt when the detection changes.",
  "expected": "Two views of one row, on two tabs of one screen, must not disagree. Confirming in First Light should immediately move the alert out of 'New'.",
  "repro": "1. Seed a confirmed-able detection and restart the app. 2. Analytics > Science > First Light > Confirm (badge becomes 'Confirmed', toast 'Marked as confirmed.'). 3. sqlite3 -> reviewed=1. 4. Click 'Observing Alerts'. 5. The card shows the 'New' badge.",
  "evidence": "AT-SPI at 11:10 on First Light: 'Confirmed' twice plus 'panel: Marked as confirmed.'; DB at that instant: 1|newSource|1|0 (reviewed=1). /tmp/ns-audit/shots/rsr2-21.png at 11:14 on Observing Alerts: 'New' badge on the same detection. Mapping that should have produced 'acknowledged': packages/nightshade_core/lib/src/services/transients/transient_alert_mapper.dart:41-46.",
  "where": "packages/nightshade_core/lib/src/providers/transient_alert_provider.dart (the alert list is not invalidated when transient_detections changes)"
 },
 {
  "title": "Science CSV exports never ask where to save and write to a hard-coded ~/Documents/Nightshade/exports, ignoring the app's configured data directory",
  "severity": "P2",
  "kind": "defect",
  "unit": "screen:science/science_screen.dart",
  "what_happened": "Pressing CSV on 'Photometry Measurements' produced no save dialog at all; the toast read 'Exported 495 rows to /home/scdouglas/Documents/Nightshade/exports/photometry_2026-08-02T11-16-14.csv'. This instance was launched with NIGHTSHADE_DATABASE_DIR and NIGHTSHADE_DATA_DIR pointed at /tmp/ns-audit/r-science-review/data, and the export ignored both. Every other export I drove in this sweep (stack PNG/JPEG, Share Card, AstroBin) opens a GTK save dialog and honours the chosen path, so the science hub is the odd one out. The shared hard-coded directory is also a collision point: it already contained an mpc_report written at the same minute by a different concurrently-running instance.",
  "expected": "Either open the same save dialog the other exports use, or write into the app's configured data directory and say so. A user who has moved their Nightshade data to another disk will not find these files, and a headless/appliance install may not have a Documents directory at all.",
  "repro": "1. Launch with NIGHTSHADE_DATA_DIR set somewhere other than the default. 2. Analytics > Science > toolbar database icon > any 'CSV' button. 3. No file picker appears; read the toast path.",
  "evidence": "AT-SPI: 'Exported 495 rows to /home/scdouglas/Documents/Nightshade/exports/photometry_2026-08-02T11-16-14.csv'. `ls /home/scdouglas/Documents/Nightshade/exports/` shows the file plus artefacts from other audit profiles. The sandbox dir /tmp/ns-audit/r-science-review/ received nothing.",
  "where": "packages/nightshade_app/lib/screens/analytics/widgets/science_export_hub.dart (export path resolution)"
 },
 {
  "title": "The stack result's 'Avg HFR' statistic never renders because the service hard-codes avgHfr: null, even though every selected sub carries an HFR",
  "severity": "P3",
  "kind": "defect",
  "unit": "screen:stack_result/stack_result_screen.dart",
  "what_happened": "stack_result_screen.dart:522 renders an 'Avg HFR ... px' row only `if (result.avgHfr != null)`, and stack_and_share_service.dart:425 persists `avgHfr: null` unconditionally. So the row is dead: two real Stack & Share runs in this session wrote stacked_results rows 2 and 3 with avg_hfr NULL, while every one of their source frames has hfr = 2.4 in captured_images. The only row in the table with an avg_hfr is one I seeded by hand. The result panel therefore shows Integration / Frames stacked / Rejected / Avg residual / Filter / Dimensions and silently omits the one number an imager actually judges a stack by.",
  "expected": "Average the HFR of the frames that were integrated (they are already in hand as StackedFrameSelection rows) and persist it, so the panel shows 'Avg HFR 2.40 px'.",
  "repro": "1. Run Stack & Share to completion. 2. The result panel has no Avg HFR row. 3. sqlite3 nightshade.db 'select id,frames_stacked,avg_hfr from stacked_results' -> 2|7|(null), 3|8|(null); 'select distinct hfr from captured_images where session_id=3' -> 2.4.",
  "evidence": "/tmp/ns-audit/shots/rsr2-11.png (Integration panel with no HFR row). DB as above. Source: packages/nightshade_core/lib/src/services/stack_and_share_service.dart:425 `avgHfr: null,`.",
  "where": "packages/nightshade_core/lib/src/services/stack_and_share_service.dart:425; consumed at packages/nightshade_app/lib/screens/stack_result/stack_result_screen.dart:522"
 },
 {
  "title": "The standalone /science route can never build ScienceScreen -- its redirect always fires, so the screen class and its Scaffold are unreachable code",
  "severity": "P3",
  "kind": "defect",
  "unit": "screen:science/science_screen.dart",
  "what_happened": "app_router.dart declares the /science GoRoute with BOTH a `redirect` that unconditionally returns '/analytics?tab=science...' and a `pageBuilder` that constructs ScienceScreen. In go_router a non-null redirect short-circuits the route, so the pageBuilder never runs. ScienceScreen is constructed in exactly one place in the whole repo -- that dead pageBuilder -- so the class, its Scaffold and its initialTabQuery plumbing ship but can never appear. Driving the app confirms it: every route into science lands on the Analytics shell with the Science tab selected, never on a standalone science screen.",
  "expected": "Delete the dead pageBuilder (and ScienceScreen with it) or drop the redirect if a standalone screen is actually wanted. Leaving both is a trap: a future reader editing ScienceScreen will change nothing that ships.",
  "repro": "1. Reach science by every available path -- Dashboard cockpit narrator, imaging science HUD, sequencer light-curve panel, transients screen. 2. All land on Analytics with the Science tab active (header reads 'Analytics', sub-tabs Science / First Light / Observing Alerts). 3. grep -rn 'ScienceScreen' packages apps --include='*.dart' -> only the definition and app_router.dart:232.",
  "evidence": "/tmp/ns-audit/shots/rsr2-23.png (the only rendering of the science workspace is inside the Analytics shell). packages/nightshade_app/lib/router/app_router.dart:222-236: `redirect: (context, state) { ... return '/analytics?tab=science...'; }` followed by `pageBuilder: ... child: ScienceScreen(initialTabQuery: tabQuery)`.",
  "where": "packages/nightshade_app/lib/router/app_router.dart:222-236; packages/nightshade_app/lib/screens/science/science_screen.dart:246-258 ScienceScreen"
 }
]