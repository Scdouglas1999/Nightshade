# Live-rig findings — 2026-08-09

First hardware validation in this campaign. Rig: ZWO **ASI1600MM-Cool** and **ASI178MM** on
`Sean-Laptop` (192.168.1.50), current tree built on the machine (6.1.0 build 25) and driven
headless through an SSH tunnel. Daytime, no slews.

Every item below was **reproduced on the running app before being written down**, per the owner's
rule. Where the app's answer is compared against ground truth, the ground truth comes from talking
to the ASCOM driver directly with a standalone PowerShell COM probe (`C:\src\ascomprobe.ps1`), not
from reading Nightshade's source.

---

## L1 — `?deviceType=switch` reports zero switches on a rig with six *(fixed)*

`GET /api/devices?deviceType=switch` → `{"devices":[]}`.
`GET /api/devices?deviceType=switch_` → six devices: PegasusAstro Switch 1/2/3, both JustAHub
switches, and Simulated Power Switch.

`DeviceType.switch_` carries a trailing underscore only because `switch` is a Dart reserved word,
and that artefact had leaked into the wire vocabulary. No client guesses `switch_`.

**The general case is worse than the instance.** `?deviceType=totalNonsense` also returned
**HTTP 200 `{"devices":[]}`** — an unparseable type fell through to an empty list, so the endpoint
told the caller authoritatively that the rig had no such hardware when the truth was that it had
not understood the word. Every sibling handler in this API already rejects an unparseable type with
`400 invalid_request`.

Fixed in `device_type_parser.dart` (accept the natural spelling for reserved-word members) and
`device_discovery_handlers.dart` (400 with the valid list instead of a silent empty result).
`apps/desktop/test/headless_api/device_type_parser_test.dart` covers it, including a guard that the
vocabulary cannot become ambiguous if a `foo` is ever added beside a `foo_`.

Note this would have reproduced against the **simulator** too — `Simulated Power Switch` is in that
list of six. It survived a year of audits because nobody had asked that endpoint for a switch by
its obvious name, which is what a denominator catches and exploration does not. Those 577 API
routes only entered the coverage ledger today.

---

## L2 — the app never shows the camera's real model *(open)*

| | value |
|---|---|
| ASCOM driver `Name` (ground truth) | **`ZWO ASI1600MM-Cool`** |
| Nightshade discovery list | `ASI Camera (1)` |
| Nightshade connected-device list | `ASICamera2 Camera` |

Discovery showing `ASI Camera (1)` is defensible — pre-connection, all the app has is the ASCOM
registry entry, and `discovery.rs:617` correctly uses `ascom_dev.name`. But **after** a successful
connect the driver hands over `Name = "ZWO ASI1600MM-Cool"`, and the app still reports a generic
ProgID-derived string.

Why it matters here specifically: this rig has **two** ZWO cameras attached (ASI1600MM-Cool and
ASI178MM). They appear as `ASI Camera (1)` and `ASI Camera (2)` — the operator has to remember
which ASCOM slot is which sensor, when the driver knows and will say.

Not fixed yet: the right fix is to refresh the stored display name from the driver's `Name` on
connect, and that touches the shared device-info path, so it wants its own change plus a test.

---

## L3 — `readoutModes` is empty when the driver reports one *(open, low severity)*

`GET /api/camera/readout-modes?deviceId=ascom:ASCOM.ASICamera2.Camera` → `{"readoutModes":[]}`,
while the driver reports `ReadoutModes.Count == 1` (`ReadoutMode = 0`, `CanFastReadout = False`).

A camera with a single readout mode and no fast-readout support arguably has nothing worth
presenting, so an empty list may be a deliberate "nothing to choose here". But the app is
discarding information the driver supplied, and the endpoint's own contract does not say so.
Needs a decision on intent before it is called a defect.

---

## Not findings — checked and dismissed

* **`recommended-settings` all null.** `unityGain`, `hcgGain`, `defaultOffset` and
  `recommendedCoolingSetpointC` are all null for the ASI1600MM. This is **correct**:
  `native/src/camera.rs:197` states that these come from the **vendor SDK** and that returning all
  `None` "is the honest answer for cameras whose SDK genuinely doesn't expose unity gain — callers
  MUST NOT fabricate a value". The connection here is ASCOM, which exposes none of them. Worth a
  separate test via the native ZWO SDK path, since `ASICamera2.dll`, `EFW_filter.dll` and
  `EAF_focuser.dll` are all in the bundle yet camera discovery returned no `native` entries.
* **`0x80020009` on connect.** Reproduced before the hardware was plugged in, and it was honest —
  the driver was registered but no camera was attached. See the environment note below.
* **The device-type filter is ignored.** My own harness sent `?type=` instead of `?deviceType=`
  and got all 67 devices back for every type. The API was right; the harness was wrong.

---

## Environment notes worth keeping

* **ASCOM discovery enumerates registered *drivers*, not present *hardware*.** With nothing plugged
  in, the rig still advertised 5 cameras, 7 rotators and 6 switches. Any conclusion drawn from a
  discovery list needs that caveat.
* **A reimaged Windows box keeps its ASCOM registrations but loses vendor USB drivers.** Before the
  cameras were re-seated, the ASI1600 appeared as `WestBridge`, `USB\VID_04B4&PID_00F3`, problem
  code **28** (no driver) — a Cypress FX3 in bootloader mode, i.e. a ZWO camera that never got its
  firmware loaded. The app's connect error at that moment was
  `SDK error: Failed to set property Connected: Exception occurred. (0x80020009)`, which is
  accurate and useless. Windows already knew the device had problem code 28; an app that checked
  could say so. **Candidate product finding, not yet written up** because the right message depends
  on distinguishing not-plugged-in from in-use-by-another-app from driver-broken.
* **Win32-OpenSSH kills the process tree when the session closes**, so an appliance started with
  `Start-Process` over SSH dies immediately. Run it in the foreground of a long-lived SSH session.
* **Windows Firewall blocks 8080 inbound**; an SSH `-L` tunnel avoids needing a firewall rule.
* **`flutter build windows` does not stage the native DLLs.** `scripts/stage_windows_release.ps1`
  exists for exactly that and I skipped it, which is why the first launch died with the bridge's own
  (excellent, specific) "bridge library older than the Dart build" error. Vendor SDK DLLs need
  `-IncludeVendorSdks`, and on this machine they came from the shipped 6.0.0 bundle.

---

## L4 — the app reports a phantom device as "connected" *(P0 for unattended operation; fix written, not yet validated on hardware)*

**The single most dangerous thing found so far.** The owner does not own an ASI mount — only the
driver is installed, because ASCOM ships drivers independently of hardware. Connecting to it:

```
POST /api/devices/connect {"deviceId":"ascom:ASCOM.ASIMount.Telescope","deviceType":"mount"}
  -> {"status":"connected", ...}

GET  /api/mount/status?deviceId=ascom:ASCOM.ASIMount.Telescope
  -> {"connected":true,"tracking":false,"slewing":true,"parked":false,
      "rightAscension":0.0,"declination":0.0,"altitude":0.0,"azimuth":0.0,
      "siderealTime":-1.0,"canPark":true,"canSlew":true,"canSync":true, ...}
```

Ground truth, from a standalone PowerShell COM probe against the same driver with Nightshade
disconnected (`C:\src\mountprobe.ps1`):

```
Name: ASI Mount
Connected=true accepted      <- no exception
Connected        = False     <- the driver disagrees
Slewing          = True
AtPark           = False
SiderealTime     = -1
RightAscension/Declination/Altitude/Azimuth = 0
```

So the garbage telemetry is the DRIVER's — Nightshade does not fabricate the `-1`. The defect is
that Nightshade **believed the connect**. `AscomDeviceConnection::connect`
(`native/nightshade_native/ascom/src/windows/connection.rs`) set `Connected = true`, saw no COM
exception, set `self.connected = true` and returned `Ok(())`. It never read the property back —
even though `is_connected()` sits eleven lines below and does exactly that.

**Why this is a P0 for tonight.** `slewing: true` is latched on for ever. Any consumer that waits
for a slew to finish — Slew & Center, meridian flip, the sequencer's own move steps — waits all
night on a mount that does not exist, while the UI insists everything is connected and fine. The
operator's only mistake is picking the wrong entry from a device list that, by ASCOM convention,
advertises installed DRIVERS rather than present HARDWARE.

**Fix written** (`connection.rs`): after setting `Connected = true`, poll `Connected` back for up to
5 s; on failure, set `Connected = false` again so no half-open handle is left behind, and return a
message that names the real possibilities — not powered, not plugged in, or already open in another
application. One function, so it covers every ASCOM device type: mount, camera, focuser, filter
wheel, rotator, dome, switch, safety monitor, cover calibrator, observing conditions.

**Not yet validated on hardware** — this is Windows-only code that cannot compile on the Linux
workstation. It must be rebuilt on the laptop and re-tested against BOTH the phantom ASI mount
(must now fail with the new message) and the five genuinely-attached devices (must still connect).

### Related, still open

`siderealTime: -1.0` reaches the API as a plausible-looking number. Sidereal time has no valid
negative value; a consumer plotting or comparing it gets nonsense rather than a gap. Whether the
transport should carry `null` instead is a contract question, not a driver question.

---

## Live device inventory (2026-08-09, all connected simultaneously)

| type | device id | driver-reported name |
|---|---|---|
| camera | `ascom:ASCOM.ASICamera2.Camera` | ZWO ASI1600MM-Cool |
| camera | `ascom:ASCOM.ASICamera2_2.Camera` | (ASI178MM, second slot) |
| focuser | `ascom:ASCOM.EAF.Focuser` | EAF Focuser |
| filter wheel | `ascom:ASCOM.EFW2.FilterWheel` | EFW2 FilterWheel |
| rotator | `ascom:ASCOM.ASICAA.Rotator` | ASICAA Rotator |
| mount | **PegasusAstro NYX101** | *not yet connected — the ASI Mount entry is a phantom* |

Note every connected name above is generic (see L2): the app shows `EAF Focuser`, `EFW2
FilterWheel`, `ASICAA Rotator` rather than the models the drivers report. L2 is therefore not a
camera-specific defect — it affects all five device types.

---

# Session 2 — live rig, full hardware (2026-08-09 evening)

Real devices connected simultaneously and exercised: **ASI1600MM-Cool** camera, **EAF** focuser,
**EFW2** filter wheel, **ASICAA** rotator, **Pegasus NYX101** mount (ASCOM). Mount parked, daylight.

## What works, verified on hardware

* **Mount telemetry is real and sane.** NYX101 via `ascom:ASCOM.PegasusAstroNYX101.Telescope`:
  parked `true`, slewing `false`, RA 20.0236 h, Dec +87.156°, Alt 39.950°, Az 3.710°, sidereal time
  14.1014 h, side of pier west, plus a per-field `availability` map that honestly marks
  `at_home: unsupported`. This is the contrast case that makes L4 diagnosable.
* **Cooling ramps correctly.** Setpoint 0 °C accepted; sensor 26.7 → 26.0 → 24.5 °C over 36 s with
  cooler power 2% → 8% → 16%.
* **Filter wheel physically moves.** Slot 0 → 1, reported name `1` → `2`, `isMoving` false after.
* **A real 2 s exposure completes** and reports 4656×3520 — the ASI1600MM's true sensor geometry.
* **The oversized-image guard is honest.** `/api/camera/last-image` refuses a 65 MB frame with
  `legacy_image_too_large`, giving `width`, `height`, `displayBytes`, `maxLegacyDisplayBytes` and
  the endpoint to use instead. No truncation, no silent downscale.
* **`filter-wheel/set-by-name` with a nonexistent filter is exemplary.** Asking for `Ha` on a wheel
  reporting `1..8` returns `HTTP 400`, names the filter and the device, lists the available filters
  in both `expected` and `message`, and **does not move the wheel**. This is the opposite of the
  cry-wolf pattern and is worth preserving as the reference shape for the rest of the API.

## L5 — the same mount appears three times, and one of the three cannot connect *(open)*

`GET /api/devices?deviceType=mount` lists the NYX101 as **three separate devices**:

| driverType | id |
|---|---|
| ascom | `ascom:ASCOM.PegasusAstroNYX101.Telescope` |
| alpaca | `alpaca:http://192.168.1.33:80:telescope:0` |
| native | `native:onstep:COM4:115200` |

Nothing tells the operator these are one physical mount. Two of them work; the **native OnStep path
fails**:

```
POST /api/devices/connect {"deviceId":"native:onstep:COM4:115200","deviceType":"mount"}
  -> "Operation timeout: LX200 command response timed out"   (reproduced twice)
```

Discovery itself had already talked to that port successfully — the app's own log line reads
`Found OnStep mount on COM4 at 115200 baud: Pegasus NYX-101#`. So identification over COM4 works and
connection over COM4 does not. A COM4 handle leak in the discovery path was recorded on this same
rig on 2026-06-17, which makes a leaked handle the leading hypothesis, but it is **not yet proven**:
the decisive experiment is a fresh app start with a native connect attempted BEFORE any discovery
runs. Not run yet.

Risk for tonight is low (ASCOM works and is the configured path) but the three-way duplication is a
real trap: connecting two entries for one mount would have two drivers fighting over one serial
port.

## Pre-flight note — filter names are numeric

The EFW reports `["1","2","3","4","5","6","7","8"]`. Any sequence step that references a filter by
name (`Ha`, `L`) will be rejected — correctly, per the excellent error above, but rejected. Filter
names need assigning in the profile before an unattended run, or every filter-change step must use
slot indices.

## Open question — a bare exposure writes no file

`POST /api/camera/expose` completed and the frame is retrievable through the image endpoints, but no
`.fit`/`.fits` appeared anywhere under the data directory or the user profile in the following 15
minutes. This is *probably* correct — a manual snapshot is not a saved sub, and persistence is the
sequencer's job — but it is unverified either way, and "did tonight's frames actually reach the
disk" is the single most important question of the run. Resolve it with a real sequence, not by
reading the capture code.

---

# Session 3 — first real sequencer run on live hardware

Sequence: 1× Loop → TargetHeader (Polaris, RA 2.5303 h, Dec +89.264°) → two 2 s `TakeExposure`
nodes on filter slots 0 and 1, `save_to = C:\src\rigframes`. Camera, focuser, wheel, rotator and
the NYX101 all connected and verified live beforehand.

## Good — a pre-flight guard that fails closed correctly

First start attempt was refused:

> `This sequence captures frames but no image save path is configured. Set the sequencer save path
> before starting — every frame would otherwise be captured and discarded.`

That is exactly right: it names the consequence, not just the missing field. It is also proof the
pre-flight layer *can* block a start — which is what makes L6 below a gap rather than an absence.

## L6 — a run starts with NO equipment profile and hangs in "recovering" for ever *(P0 for tonight)*

`GET /api/profiles` → `{"profiles":[]}`. There is no equipment profile on this appliance and no
camera assigned to one. The sequencer started anyway:

```
POST /api/sequencer/start -> {"status":"started"}
GET  /api/sequencer/status ->
  {"state":"recovering","currentNodeId":"e1","currentNodeName":"Sub e1",
   "progress":0.0,"message":"Recovering: Device disconnected"}
```

…and stayed in `recovering / Device disconnected` at `progress 0.0` for the entire observation
window, retrying silently. **No frames were written.**

Meanwhile the device manager disagreed with the executor. Queried during the hang:

```
GET /api/devices/connected -> camera ascom:ASCOM.ASICamera2.Camera,
                              rotator ascom:ASCOM.ASICAA.Rotator,
                              filterWheel ascom:ASCOM.EFW2.FilterWheel, ...
```

So the camera the executor calls "disconnected" is connected, live, and had taken a real 4656×3520
frame minutes earlier. The leading hypothesis is that the executor resolves hardware through the
**profile assignment** rather than the session's connected devices, and an empty profile reads as a
disconnected device — the same shape recorded on 2026-08-05 as "Discovery Connect without Assign
leaves `profile.cameraId` empty". **Not yet proven**; proving it means assigning a profile and
re-running, which is the next test.

Why this is a tonight problem regardless of root cause: the failure is **silent and unbounded**.
The pre-flight layer that correctly refuses a missing save path has no equivalent check for "no
profile / no camera assigned", so the run begins, reports a healthy `started`, and then loops in
recovery until someone notices. On an unattended night that is the whole session lost with no
error surfaced.

## L7 — Stop reports failure after successfully stopping *(P0-adjacent; cry-wolf, opposite polarity)*

```
POST /api/sequencer/stop ->
  {"error":"internal_error",
   "message":"TimeoutException after 0:00:30.000000: The native executor accepted the stop command
              but never reported a terminal state within 30s. The hardware was NOT confirmed
              stopped, so nothing has been torn down."}

GET /api/sequencer/status -> {"state":"failed","message":"Cancelled: Preflight"}
```

The run **did** reach a terminal state — the stop worked. The API waited 30 s, gave up, and told the
operator the hardware was *not* confirmed stopped and nothing had been torn down. At 2am that
sentence means "your mount may still be slewing"; it is false, and it is the kind of false alarm
that makes an operator intervene destructively.

A second `stop` against the already-terminal run **also blocks for 30 s** and returns the same
message, rather than returning immediately for a run that is already finished.

The wording itself is admirable — it refuses to claim a teardown it cannot verify. The defect is
that the terminal state arrived and the waiter did not see it.

## L8 — the Dart wire-validation test fixtures do not match the Rust parser

Getting a sequence to load took four rejections, each naming a field the Dart test fixture in
`apps/desktop/test/headless_api/sequencer_wire_validation_test.dart` does not supply or spells
differently:

| Rust demands | fixture has |
|---|---|
| `TargetHeaderConfig.priority` (required) | absent |
| `ExposureConfig.duration_secs` | `duration` |
| `Binning` as a bare enum string (`"One"`) | `{"x":1,"y":1}` (my guess, also wrong) |
| `SequenceDefinition.id` and `name` | absent |

`sequence_wire_validation.dart`'s own doc comment says the point of validating the wire artifact is
that "there is no serialization gap between what is checked and what executes". There is a gap: the
validator's tests exercise JSON the executor would reject outright. The validator may still be
correct — but nothing proves it, because no test feeds it a string the Rust side would accept.

## L9 — a profile cannot be created through the headless API, because the error says nothing *(open)*

`POST /api/profiles` with a body carrying `name`, all five real device ids, focal length, aperture,
binning and cooling flags returns:

```
{"error":"Malformed profile payload","code":"invalid_request",
 "field":"profile","expected":"equipment_profile","message":"Malformed profile payload"}
```

No field name, no expected shape, no hint. `profile_handlers.dart:86-92` is the cause:

```dart
try {
  profile = EquipmentProfile.fromJson(profileJson);
} on Object {
  throw BadRequestError(field: 'profile', expected: 'equipment_profile',
                        message: 'Malformed profile payload');
}
```

The intent is good and the comment says so — *"a malformed shape becomes a structured 400 instead of
letting a TypeError/FormatException collapse into an opaque 500"*. But `on Object` swallows the
exception object entirely, and that exception is the only thing that knows **which** field was wrong.
The caller is left to reverse-engineer `EquipmentProfile.fromJson` from source, which is exactly what
I had to start doing.

Compare `filter-wheel/set-by-name`, which for a bad filter returns the device, the requested value,
and the full list of valid ones. Same API, same release. That is the standard this should meet:
include the underlying error's message (it is a local type error, not sensitive), or validate
field-by-field.

Consequence for tonight: **there is no working path to create an equipment profile remotely.** If
L6's root cause is the empty profile, then a headless-only operator cannot fix L6 either.

### Checked: this is not the known profile-store schism

An earlier campaign recorded a schism where the GUI's SQLite store and the remote `/api/profiles`
store were different databases. Not the cause here: the only databases on the laptop are the
scratch ones this session created (`C:\src\rigdata\nightshade.db`, 987 KB, and `pairing.db`).
Nothing exists under `%APPDATA%\com.example\nightshade_desktop` or `%LOCALAPPDATA%`. The machine was
reimaged, so this build's GUI has never run here and there are genuinely zero profiles. `L6` is a
real "started with no profile at all", not a store mismatch.

## L10 — the ZWO CAA rotator is a SECOND phantom, and it silently swallowed a move *(confirms L4; P0 for tonight)*

The earlier PnP scan found only the two cameras — no CAA, EAF or EFW by name — so the rotator was
suspect. Nightshade nonetheless:

```
POST /api/devices/connect  {"deviceId":"ascom:ASCOM.ASICAA.Rotator","deviceType":"rotator"}
  -> {"status":"connected"}
GET  /api/rotator/status   -> {"position":0.0}
POST /api/rotator/move-to  {"angle":30.0}
  -> {"commandId":"708a29ef-...","status":"moving"}
```

`status: moving`. Position then read **0.0 at t+12 s, t+22 s, t+32 s and t+42 s.** Nothing moved,
nothing errored, and the app never stopped claiming it was moving.

Independent COM probe of all three ZWO auxiliaries — the control group that makes this conclusive:

| driver | `Connected` reads back | `Name` | hardware attached |
|---|---|---|---|
| `ASCOM.ASICAA.Rotator` | **False** | ZWO CAA | **no — phantom** |
| `ASCOM.EAF.Focuser` | True | ZWO Focuser | yes |
| `ASCOM.EFW2.FilterWheel` | True | ZWO FilterWheel | yes |

Same rig, same session, same vendor's driver family: the read-back cleanly separates the two real
devices from the phantom, and it is the exact check `AscomDeviceConnection::connect` was missing.
That is now two independent phantoms (ASI Mount, ZWO CAA) caught by one test on one afternoon.

**Why this is worse than the mount case.** A phantom mount at least announces itself through
`slewing: true` forever. A phantom rotator reports a plausible `position: 0.0`, accepts every move
with `status: moving`, and stays silent. A sequence that rotates to frame a target would run all
night at the wrong angle with every frame quietly mis-rotated and no error anywhere. There is no
symptom until the data is reviewed.

This raises L4 from "the fix is written" to "the fix is required before tonight", and it gives the
fix a ready-made acceptance test: after rebuilding, `ASCOM.ASICAA.Rotator` must FAIL to connect with
the new message, while `ASCOM.EAF.Focuser` and `ASCOM.EFW2.FilterWheel` must still connect.

## API ergonomics — four field names guessed wrong in a row

Working the device endpoints by hand, every one of these was rejected before the right spelling was
found: `/api/camera/expose` wants `exposureTime` (not `exposureSeconds`), `/api/focuser/move-relative`
wants `delta` (not `steps`), `/api/rotator/move-to` wants `angle` (not `position` — even though
`/api/rotator/status` *returns* `position`), and `/api/rotator/move-absolute` does not exist at all.
Each rejection was a clean, correct 400 naming the missing field, which is why this is an ergonomics
note rather than a defect — but `status` returning `position` while `move-to` demands `angle` is a
genuine inconsistency inside one device's own endpoint pair.

## L11 — one failed run wedges the camera for the rest of the night *(P0 for tonight)*

After the L6 run failed and was stopped, **every exposure through Nightshade timed out**, while the
camera itself stayed perfectly healthy. Sequence of measurements, all on the live ASI1600MM-Cool:

```
1. Before the run:  POST /api/camera/expose 2s  -> complete, 4656x3520 frame
2. Sequencer run enters "recovering", is stopped (L6, L7)
3. Autofocus:  "Autofocus exposure failed: Exposure ... did not complete within 62.0s timeout
                (2.0s requested exposure plus safety margin);
                CRITICAL: failed to restore original focuser position 22850: halt failed:
                Halt failed: Operation timeout: Focuser halt timed out after 10s"
4. Plain expose 2s        -> same 62 s timeout
5. GET /api/camera/cooling -> {"coolerOn":true,"targetTemp":0.0,"sensorTemp":0.1,"coolerPower":43.0}
                              (telemetry perfectly responsive; the cooler had reached its setpoint)
6. POST /api/camera/abort  -> {"status":"aborted","wasRunning":false,
                               "message":"No exposure was in progress ...; nothing to abort."}
7. Plain expose 2s        -> STILL times out
```

Ground truth, Nightshade disconnected, independent COM client:

```
Connected reads back = True
CameraState = cameraIdle
ImageReady after 2.9s = True
```

**The camera and driver are fine.** The wedge is in Nightshade's own per-connection camera state.
Confirmed by the cure: `POST /api/devices/disconnect` then `/connect`, and the very next exposure
returned `{"status":"complete"}`.

Why this is the worst finding of the session for an unattended night:

* **It is silent in exactly the way that matters.** The dashboard keeps showing a cooled, connected,
  healthy camera — `coolerOn`, setpoint reached, 43% power — while nothing can be captured.
* **`abort` actively misleads.** It reports there is nothing to abort, which is true of the app's
  own bookkeeping and false about the situation, so the one recovery an operator would reach for
  reports success and changes nothing.
* **Nothing self-heals.** The app never reconnects the camera on repeated exposure timeouts, even
  though disconnect/reconnect is a complete and instant cure that it is perfectly capable of
  performing.
* **One bad run poisons the night.** The trigger was a run that failed for an unrelated reason (L6).

### Second defect inside the same failure: autofocus abandons the focuser

The autofocus error is worth reading twice:

> `CRITICAL: failed to restore original focuser position 22850: halt failed: Halt failed:
> Operation timeout: Focuser halt timed out after 10s`

Autofocus moved the focuser, could not expose, tried to put it back, and **failed** — then failed
again trying to halt it. The focuser is left at an unknown position. The app knows this and says so
in capitals, which is genuinely good reporting, but there is no recovery: no retry of the restore,
no re-home, and the run would carry on out of focus. On an unattended night the remaining hours are
captured at whatever focus position the failure happened to leave behind.

The 10 s halt timeout is also worth questioning against a real EAF, which is not a fast device.

## L12 — the safety subsystem's first answer is "unsafe", and it can auto-park *(observed once; needs a restart to re-prove)*

Two consecutive `GET /api/safety/status` calls, seconds apart, no configuration touched in between:

| field | first call | second call |
|---|---|---|
| `isSafe` | **false** | true |
| `safetyStatus` | **unsafe** | safe |
| `hardwareWeatherSafe` / `safetyMonitorSafe` / `apiWeatherSafe` | false / false / false | true / true / true |
| `hardwareWeatherReading` / `safetyMonitorReading` | **unknown** | absent |
| `monitoringEnabled` | **true** | false |

Then ten consecutive polls at 3 s intervals: `isSafe=True` every time. So this is not ongoing
flapping — it is the **first evaluation** after the subsystem comes up, before settings load, where
`monitoringEnabled` is still true and every reading is `unknown` rather than `absent`. Fail-closed
turns "unknown" into "unsafe", correctly, on data that has not arrived yet.

**Observed once.** I did not restart the appliance to reproduce, because a restart would take the
rig offline while other work was using it. Re-proving it needs a cold start with a poll running from
the first second — that test should be run before tonight.

Why it matters tonight, from this rig's own settings
(`GET /api/safety/settings`): `failMode: fail_closed`, **`autoParkOnUnsafe: true`**,
`autoStopOnUnsafe: false`, `warningDelaySeconds: 60`, `requiredSafeDurationSeconds: 300`,
`enabledMonitors: []`.

So a transient "unsafe" is wired to **park the mount**. The 60 s warning delay is the thing standing
between a first-read transient and an automatic park, and whether it covers this window is exactly
what the restart test would establish. Note also `enabledMonitors: []` with `fail_closed` — the
configuration this rig will actually run tonight has no monitors at all, which is the same shape as
the 6.0.0 blocker where fail-closed weather safety aborted every run on a rig with no weather sensor.

The steady-state wording is honest and good: *"Weather safety is off — conditions are not being
checked"*, with `failModeWarning: "No weather data sources available"`. The problem is only the
first answer, and that it is wired to an actuator.

## L13 — every ASCOM *write* discards the driver's explanation; every *read* keeps it *(open, confined, high value)*

Enabling tracking on the parked NYX101 — which the mount correctly refused — produced:

```
POST /api/mount/tracking {"enabled":true}
 -> "Failed to set ASCOM mount ... tracking=true:
     SDK error: Failed to set property Tracking: Exception occurred. (0x80020009)"
```

The behaviour is right: the mount stayed `parked=true, tracking=false, slewing=false`. The message
is a bare HRESULT. ASCOM drivers put a human sentence in the `EXCEPINFO` for exactly this case —
something on the order of *"Cannot set Tracking while the mount is parked"* — and it never reaches
the operator.

Root cause, and it is a one-line asymmetry in `connection.rs`:

* **Property reads** pass a real `EXCEPINFO` and decode it: `excepinfo_to_string` is called at lines
  876, 918, 956, 994 and 1036.
* **Property writes** pass `None`: `invoke_with_retry(dispid, DISPATCH_PROPERTYPUT, &params, None, None)`
  at lines **708, 752 and 795**. The description is never even requested, so nothing downstream can
  recover it.

Consequence: `0x80020009` on *any* write is unexplainable by construction. That covers connect,
tracking, gain, offset, cooling setpoint and filter position — and a previous live-rig session on
this same hardware recorded exactly this pain as "gain/offset + FW-move fault 0x80020009". Every one
of those investigations was chasing a message the driver had already written and the app had thrown
away.

Fix is confined: pass an `EXCEPINFO` from the three `DISPATCH_PROPERTYPUT` sites and feed it through
the same `excepinfo_to_string` path the getters already use. The retry loop needs care — `EXCEPINFO`
carries `BSTR`s that must be freed between attempts — which is presumably why the setters were
written this way.

This also explains why L4's phantom-device message had to be written from first principles rather
than surfaced from the driver: on the connect path there is no description to surface.

## L14 — the executor and the device manager disagree about what is connected *(P0; this is L6's root cause)*

Re-running the same sequence with a healthy camera changed the symptom from "hangs in recovering"
to "fails immediately", and the second failure names the cause:

```
GET /api/run-watch/snapshot
  ...progress.message:
  "Sequence failed: Exposure: Cannot capture with requested filter position 0
   because no filter wheel is connected"
```

At that exact moment:

```
GET /api/devices/connected           -> includes filterWheel ascom:ASCOM.EFW2.FilterWheel
GET /api/filter-wheel/position       -> {"position":0,"name":"1","isMoving":false}
```

The wheel is connected, responsive, and had been **physically rotated** through the API twenty
minutes earlier. The executor cannot see it.

That makes L6 and this the same defect wearing two masks: the first run said "Device disconnected"
about a working camera, this one says "no filter wheel is connected" about a working wheel. The
executor resolves hardware through a channel that does not include devices connected via
`POST /api/devices/connect` — almost certainly the equipment-profile assignment, which on this
appliance is empty (L9 documents why it cannot even be populated remotely).

**For tonight this is the central risk.** Any device attached ad hoc rather than through an assigned
profile is invisible to the sequencer, and the way that surfaces is either a silent unbounded
"recovering" loop or a bare "Failed" — not "your filter wheel is not assigned to the active
profile", which is what the operator needs to read at 1am.

## L15 — the failure reason is missing from the endpoint that reports failure

`GET /api/sequencer/status` — the obvious place to look, and the one a client polls — reported only:

```
{"state":"failed","currentNodeId":"root","currentNodeName":"Preflight",
 "progress":0.0,"message":"Failed: Preflight"}
```

"Failed: Preflight" is the node's name, not a reason. The actual sentence — *"Cannot capture with
requested filter position 0 because no filter wheel is connected"* — lives in
`GET /api/run-watch/snapshot`, nested at `sequencer.progress.message`, an endpoint a client has to
know to ask for.

The good message already exists and is well written. It simply is not where anyone looks. Any
remote client, mobile app or dashboard polling `/api/sequencer/status` shows a run that failed for
no stated reason.

### L14 refined — the executor HAS the device list and still says the device is missing

`GET /api/run-watch/snapshot` carries a `devices` array, and it is complete:

```
camera      ascom:ASCOM.ASICamera2.Camera
rotator     ascom:ASCOM.ASICAA.Rotator
filterWheel ascom:ASCOM.EFW2.FilterWheel
focuser     ascom:ASCOM.EAF.Focuser
mount       ascom:ASCOM.PegasusAstroNYX101.Telescope
```

The same payload whose `sequencer.progress.message` reads *"Cannot capture with requested filter
position 0 because no filter wheel is connected"* **lists the filter wheel two fields away.** So this
is not the Dart device manager failing to tell the Rust executor; the executor's own snapshot has the
wheel. The exposure instruction's filter lookup is consulting some other registry — a profile
assignment or an executor-local device map populated at run start — rather than the connected-device
list it already publishes.

That narrows the fix considerably and rules out the "profile assignment is the whole story" reading
I gave in L6: the profile is empty, but the executor demonstrably has the devices anyway.

### L6 refined — "hangs for ever" is really "retries silently for 90 minutes"

The snapshot's `recovery` block gives the exact numbers:

```
{"cause":"DeviceDisconnected","attempt_count":1,"max_attempts":9,
 "retry_interval_secs":600.0, ...}
```

**Nine attempts, ten minutes apart.** The loop is bounded — at ninety minutes. For the whole of that
time `/api/sequencer/status` reports `state: "recovering"`, `progress: 0.0`, and the single word
"Recovering: Device disconnected", with no attempt counter, no next-retry time, and no indication
that a ninety-minute clock is running.

Ten minutes is a sensible interval for a genuine transient — a USB dropout, a driver restarting.
It is the wrong interval for a device that is present and simply not visible to the executor, which
is the case here: the retry can never succeed, and it burns ninety minutes of dark sky discovering
that. The operator sees a run that looks alive.

## L16 — the pre-flight layer is good, and its gaps are exactly the P0s *(this is the fix for L6 and L14)*

Three start attempts, three different pre-flight outcomes, and the pattern is unmistakable:

| sequence contains | pre-flight verdict |
|---|---|
| exposures, no save path configured | **BLOCKED** — *"This sequence captures frames but no image save path is configured. Set the sequencer save path before starting — every frame would otherwise be captured and discarded."* |
| a CenterTarget step, no plate solver installed | **BLOCKED** — *"This sequence centers on a target but no plate solver (ASTAP or solve-field) was found on this system. Install and configure a plate solver before running — centering would fail on every target otherwise."* |
| an exposure on filter position 0, wheel not visible to the executor | **ALLOWED TO START**, then failed mid-run (L14) |
| exposures with no equipment profile at all | **ALLOWED TO START**, then 90 minutes of silent recovery (L6) |

Both existing guards are excellent and share a shape worth copying verbatim: they name the missing
thing, name the consequence in the operator's terms, and refuse before anything moves. Neither says
merely "invalid configuration".

So L6 and L14 are not a missing capability — they are **two missing entries in a guard list that
already exists and already works**. The sequence declares which devices it needs (a filter index
implies a wheel, an exposure implies a camera, a slew implies a mount); pre-flight already walks the
tree for save paths and centering. The same walk should refuse to start when a required device is
not resolvable by the executor, in the same voice:

> *"This sequence changes filters but no filter wheel is available to the sequencer. Assign one to
> the active equipment profile before running — every exposure that requests a filter would fail."*

That is a far smaller change than fixing the executor/device-manager split (L14), and it converts
the two worst unattended failure modes — a 90-minute silent retry and a bare "Failed" — into a
refusal the operator reads before going to bed. **Both should be done: the guard tonight, the split
properly afterwards.**

## Rig environment — ASTAP is not installed *(pre-flight blocker for tonight, owner's call)*

`GET /api/plate-solver/config` → `{"astapPath":"","astrometryPath":"","catalogPath":"","solverChoice":"auto"}`
`GET /api/plate-solver/detect` → all nulls.

Confirmed on the machine: no `astap.exe` in any standard location, no `.290`/`.1476` star database,
nothing in the uninstall registry. The reimage took it with everything else.

Consequence: **no centering, no framing, no meridian-flip recentre tonight** unless it is installed.
The app is honest about this and refuses to start such a sequence (above), which is the correct
behaviour — but it means any sequence with a CenterTarget step cannot run.

## L17 — an ASTAP-only rig cannot configure its solver remotely *(live confirmation of a closeout P1)*

Staged portable ASTAP at `C:\src\astap\bin\astap.exe` with all 1476 D05 database files beside it,
then tried to tell the appliance about it:

```
POST /api/plate-solver/config {"astapPath":"C:\\src\\astap\\bin\\astap.exe",
                               "catalogPath":"C:\\src\\astap\\bin","solverChoice":"astap"}
  -> {"error":"astrometryPath is required","field":"astrometryPath","expected":"string"}

POST ... same, plus "astrometryPath":""
  -> {"error":"Value must not be empty","field":"astrometryPath"}
```

`GET /api/plate-solver/config` still reads `{"astapPath":"","astrometryPath":"","catalogPath":"","solverChoice":"auto"}`.

So configuring **ASTAP alone is impossible over the API** — the handler demands a non-empty
`astrometryPath` for a solver the operator does not have and did not choose. `solverChoice: "astap"`
is explicit in the request and ignored.

The closeout wave independently found this and marked it fixed, so the repair is already in the tree.
What this adds is that it was reproduced **on the real rig, in the exact situation it exists for** —
a fresh machine, ASTAP just staged, preparing an unattended night — and that it is the last thing
standing between this rig and working centering. Note also the sharp edge: the app's own pre-flight
message tells the operator to *"Install and configure a plate solver"*, and then the configure step
refuses them.

The running appliance binary was built before the closeout wave's repairs, which is why it still
fails here. **It needs rebuilding and redeploying before tonight** — that also delivers L4's
phantom-device fix, without which the ZWO CAA and the ASI Mount both still report themselves
connected.

---

# Session 4 — mount control and rig preparation

## Working on real hardware (NYX101, no slews)

The full park/track cycle behaves correctly and leaves the rig where it started:

| step | result |
|---|---|
| `POST /api/mount/tracking {"enabled":true}` while **parked** | correctly refused; mount stayed `parked=true, tracking=false, slewing=false` (message quality is L13) |
| `POST /api/mount/unpark` | `{"status":"unparked"}` → `parked=false, tracking=true`, alt/az unchanged at 39.95 / 3.71 — no unwanted motion |
| `POST /api/mount/tracking {"enabled":false}` | `tracking=false` |
| `POST /api/mount/tracking {"enabled":true}` | `tracking=true, trackingRate=sidereal` |
| `POST /api/mount/park` | `{"status":"parking"}` → `parked=true, tracking=false`, alt/az 39.95 / 3.70 |

Two things worth noting from this. Unpark **starts tracking by itself** — correct OnStep/NYX
behaviour, but it means the mount begins moving the moment it is unparked, which an unattended
sequence should expect rather than assume. And the park/unpark round trip left altitude and azimuth
identical to two decimal places, so nothing drifted.

## Rig prepared for tonight

* **Portable ASTAP staged** at `C:\src\astap\bin\astap.exe` with all **1476** D05 star-database
  files beside it. Nothing was installed: the ZIP was extracted into `C:\src\astap` and the star
  database was transferred from the workstation (99 MB) because SourceForge returns 403 to direct
  downloads. Removing that one directory undoes all of it.
* The Windows ZIP ships **only the GUI `astap.exe`**, not `astap_cli.exe`. That is the binary other
  imaging apps drive too, so it is the right target for `astapPath` — but note it opens a window if
  invoked with `-h`, which is how I briefly put an ASTAP window on the owner's screen.
* **The app cannot yet be told about it** — see L17. That needs the rebuild.

## Still to do before tonight

1. **Rebuild and redeploy the Windows binary.** The running appliance was built at 17:12 and
   predates every fix from today: L4's phantom-device detection (this rig has **two** phantoms — the
   ASI Mount and the ZWO CAA rotator, both of which currently report themselves connected), L1's
   device-type parsing, L17's plate-solver config, and whatever the tonight-critical fix wave lands.
2. **Re-run the phantom test after redeploying**: `ASCOM.ASICAA.Rotator` and
   `ASCOM.ASIMount.Telescope` must both now FAIL to connect with the new message, while
   `ASCOM.EAF.Focuser`, `ASCOM.EFW2.FilterWheel`, `ASCOM.ASICamera2.Camera` and
   `ASCOM.PegasusAstroNYX101.Telescope` must still connect.
3. **Configure the solver** and prove a real solve end to end.
4. **Re-run the sequence** and confirm frames actually reach disk — still unproven (L6/L14 blocked
   every attempt so far).

---

## CORRECTION to L11 — the camera "wedge" was probably the camera leaving the bus

Later evidence overturns L11's conclusion. A PnP scan now shows **only the ASI178MM**:

```
OK  Image  ZWO ASI178MM Camera
(no ZWO ASI1600MM COOL; no devices in an error state)
```

And both ASCOM slots resolve to it:

```
ASCOM.ASICamera2.Camera    -> Name = ZWO ASI178MM, 3096 x 2080, CanSetCCDTemperature = False
ASCOM.ASICamera2_2.Camera  -> Name = ZWO ASI178MM, 3096 x 2080, CanSetCCDTemperature = False
```

Earlier in the same session, `ASCOM.ASICamera2.Camera` reported `ZWO ASI1600MM-Cool`, produced a
4656x3520 frame, and cooled from 26.7 °C to 0.1 °C. **The ASI1600MM has dropped off USB.**

That is a simpler explanation for L11 than app state: the 1600 disappeared, Nightshade's live handle
to it hung on every exposure (62 s timeout), and the disconnect/reconnect "cure" worked because the
reconnect bound to the surviving 178MM — a *different camera*, not a recovered one.

**What this does and does not overturn:**

* The claim "Nightshade's per-connection camera state was wedged" is **not established**. Withdrawn.
* My "ground truth" COM probe that exposed in 2.9 s does **not** refute it either — with the 1600
  gone, that probe was talking to the 178MM. It proved *a* camera worked, not *the* camera.
* What still stands, and is arguably worse: **the app never noticed the camera had changed.** It
  kept reporting a connected, cooled camera; `abort` still said "no exposure in progress"; and the
  reconnect silently bound a different sensor with different geometry and no cooler, under the same
  device id and the same generic display name (`ASICamera2 Camera` — see L2). Nothing surfaced
  "the camera you were using is gone" or "this is now a different camera".

That last point is the real finding, and it is a genuine unattended hazard: a USB dropout mid-night
followed by any reconnect can silently swap which sensor is being used, with cached capabilities
from the old one.

## L18 — the ASCOM slot to physical camera binding is not stable *(P1)*

With two ZWO cameras attached, `ASCOM.ASICamera2.Camera` was the ASI1600MM-Cool. With one attached,
the same ProgID is the ASI178MM — and so is `ASCOM.ASICamera2_2.Camera`. The ProgID identifies a
*driver slot*, not a camera.

Nightshade treats the ProgID as the device identity (`deviceId: ascom:ASCOM.ASICamera2.Camera`),
reads capabilities once at connect, and shows a name derived from the ProgID rather than the
driver's `Name` (L2). So all three of the things that would reveal a swap are absent: the id does
not change, the displayed name does not change, and the cached capabilities do not change.

For tonight: if a camera browns out and re-enumerates, the run can continue against the wrong sensor
with the wrong pixel scale, the wrong frame size, and a cooler that does not exist. Reading `Name`
on connect (L2) and re-reading capabilities after any reconnect would both close it.

## L19 — turning the cooler OFF sends a fabricated -10 °C setpoint, and fails

```
POST /api/camera/cooling {"deviceId":"...","enabled":false}     <- no targetTemp sent
  -> "Failed to set ASCOM camera ... cooler (enabled=false, target=-10C):
      SDK error: Failed to set cooler: Failed to set property SetCCDTemperature..."
```

`camera_handlers.dart` correctly forwards `targetTemp: null`. The `-10` is applied two layers down at
`device_manager/ops/camera.rs:2251` and `:2327` — `target_temp.unwrap_or(-10.0)` — and the module
header documents it as "the historical Nightshade default target when the caller does not specify".

A default target is defensible when *enabling* cooling. Applying it while *disabling* is not: no
setpoint is needed to turn a cooler off, and here writing `SetCCDTemperature` is precisely what threw
(the 178MM has no TEC, so `CanSetCCDTemperature` is False). The result is that **the cooler cannot be
turned off at all** on a camera that reports no cooling capability — the request fails as a whole.

End-of-night warm-up is exactly when this is called. It should skip the setpoint write when
`enabled == false`, and skip it when the camera reports `CanSetCCDTemperature == false`.

## L20 — a device lost mid-exposure fails the run outright; a device missing at start retries for 90 minutes *(P1; the asymmetry is backwards)*

Unplanned but clean experiment: the owner physically reconnected the rig's USB while a run was
mid-exposure. Observed behaviour:

```
t+09s..t+15s  running   "AdaptiveExposure: 2.0s (nominal 2.0s, disabled) (0%)"
t+30s         failed    "Failed: DiskProof3"
reason:       "Exposure: Exposure failed: Exposure on ascom:ASCOM.ASICamera2.Camera
               did not complete within 62.0s timeout (2.0s requested exposure plus safety margin)"
```

The run sat at `progress 0.0` for the full 62 s timeout, then **failed outright**. It did **not**
enter recovery — the `recovery` block in the snapshot was still the stale one from 22:17, untouched.

Set against L6, the two behaviours are the wrong way round:

| situation | what happens | what should happen |
|---|---|---|
| device not resolvable **at start** (present but invisible to the executor) | 9 retries, 10 min apart — **90 minutes** | refuse at pre-flight; it can never succeed |
| device lost **mid-exposure** (a real USB dropout) | hard failure after 62 s, no retry | retry — this is precisely the transient recovery exists for |

A momentary USB brownout at 2am is the single most common unattended fault on a real rig, and it is
the case that gets no retry at all. Meanwhile the case that cannot possibly recover gets an hour and
a half of them.

Also worth noting: for the ~75 s the exposure was hanging, `/api/sequencer/status` reported
`state: "running"` with the cheerful `"AdaptiveExposure: 2.0s (nominal 2.0s, disabled) (0%)"` — no
indication that a 2 s exposure was 30× overdue. A stalled exposure is invisible until it fails.

## L21 — the executor keeps addressing a camera that was disconnected and replaced *(P0; cleanest reproduction of L14)*

The native ZWO SDK path appeared once the rig was re-plugged, exposing both cameras with their real
names and **stable per-camera ids**:

```
native:zwo:1  ZWO ASI1600MM-Cool
native:zwo:0  ZWO ASI178MM
```

Steps, all through the API:

1. `POST /api/devices/disconnect {"deviceId":"ascom:ASCOM.ASICamera2.Camera"}` → disconnected
2. `POST /api/devices/connect    {"deviceId":"native:zwo:1"}` → connected
3. Confirmed live on the new device: `GET /api/camera/cooling?deviceId=native:zwo:1` →
   `{"coolerOn":false,"targetTemp":-10.0,"sensorTemp":26.2,"canCool":true}`, and setting a 0 °C
   setpoint returned ok
4. Loaded a **fresh** sequence and started it

```
Sequence failed: Exposure: Exposure failed: Exposure failed:
  ASCOM camera ascom:ASCOM.ASICamera2.Camera not found
```

The executor addressed the **disconnected ASCOM device**, not the connected native one. A newly
loaded sequence did not clear it, so the binding is app-level state captured at some earlier point,
not something carried in the sequence.

This is L14 with the ambiguity removed. Previously the executor "could not see" a device that was
connected; here it actively reaches for a device that is **gone**, while a working camera of the
same type sits connected. Any of these tonight would hit it: swapping cameras, reconnecting after a
USB dropout, or simply choosing a different driver path than the one used earlier in the session.

**Frames on disk after four separate runs: 0.** The question "do captured frames actually reach the
disk" is still unanswered, and every attempt has been blocked by a different link in this same chain.

## The native SDK path is markedly better than ASCOM on this rig

Worth recording as a recommendation rather than a defect, because it resolves three findings at once:

| | ASCOM path | native ZWO path |
|---|---|---|
| device id | `ascom:ASCOM.ASICamera2.Camera` — a **driver slot** that re-binds to whatever camera is present (L18) | `native:zwo:1` — stable per camera |
| displayed name | `ASICamera2 Camera` (L2) | **`ZWO ASI1600MM-Cool`** |
| recommended settings | all null (L2's "not a finding" note) | `unityGain: 0`, `defaultOffset: 10`, and an honest note: *"ZWO SDK reports default gain = 0 (ElecPerADU at default = 0.496); ZWO SDK reports default offset = 10"* |

This also **confirms the earlier judgement call was right**: the all-null recommended settings under
ASCOM were correct behaviour, not a defect, exactly as `native/src/camera.rs` claims — the same
endpoint returns real values the moment the vendor SDK is the transport. For tonight, the native
path is the one to use.

---

## L22 — THE headline defect: the sequencer cannot capture at all *(P0; supersedes L6/L14/L21 as the clean statement)*

Reproduced on a **freshly restarted appliance** — no stale state, no prior connections, nothing
carried over:

1. Restarted `nightshade_desktop.exe --headless` (new process, PID confirmed)
2. `POST /api/devices/connect {"deviceId":"native:zwo:1","deviceType":"camera"}` → connected
   (this is the ZWO ASI1600MM-Cool over the native SDK — real name, real cooling, verified live)
3. `POST /api/devices/connect {"deviceId":"ascom:ASCOM.EFW2.FilterWheel"}` → connected
4. `POST /api/sequencer/save-path {"path":"C:\\src\\rigframes"}` → `{"status":"ok"}`
5. Loaded a two-exposure sequence with no filter references, no centering, no slew
6. `POST /api/sequencer/start` → `{"status":"started"}`

Result — and these three readings are from **one `GET /api/run-watch/snapshot` response**:

```
devices:  [("camera", "native:zwo:1"), ("filterWheel", "ascom:ASCOM.EFW2.FilterWheel")]
reason:   "Error: Exposure: No camera connected"
recovery: {"cause":"DeviceDisconnected","attempt_count":1,"max_attempts":9,
           "retry_interval_secs":600.0,"max_duration_secs":5400.0}
```

`GET /api/devices/connected` returns the identical two devices at the same moment.

**The executor's own snapshot lists the camera on the line above the message saying no camera is
connected.** This is not stale binding (L21), not an empty profile (L6), and not a device-manager
sync gap (L14) — those were three views of this. The exposure instruction's camera lookup does not
consult the device list the executor already holds and already publishes.

`max_duration_secs: 5400` confirms the 90-minute ceiling from L6.

### What this means for tonight

**On this appliance, in headless mode, a sequence cannot capture a single frame.** Five runs, five
different failure paths, zero frames on disk:

| run | camera path | outcome |
|---|---|---|
| 1 | ASCOM, no profile | 90-min silent recovery loop |
| 2 | ASCOM | "no filter wheel is connected" (wheel was connected) |
| 3 | ASCOM, no filters in sequence | FITS save failed — bad `save_to` template (my payload) |
| 4 | native, after camera swap | addressed the disconnected ASCOM camera (L21) |
| 5 | native, clean process, save path set | **"No camera connected"** while the snapshot lists it |

This is the one thing that must be fixed before an unattended run is possible. It is already the
subject of the `no-profile-hang` agent in the tonight-critical fix wave, and this entry is the
evidence that should drive it: the fix is not "populate the profile", it is "make the exposure
instruction resolve the camera from the executor's live device list".

---

# Session 5 — rebuilt appliance, fixes validated on hardware

Rebuilt on the laptop from commit `b8f96227` (both halves confirmed fresh: `data\app.so` 20:03,
`nightshade_bridge.dll` 20:00) and relaunched.

## L4 / L10 CLOSED — phantom detection validated on real hardware

The same driver that returned `{"status":"connected"}` an hour earlier now returns:

> `ASCOM.ASICAA.Rotator accepted the connect request but did not come online and still reports
> Connected = false. The driver is installed, but the device is not responding - check that it is
> powered, plugged in, and not already open in another application.`

Control group, same appliance, same minute:

| device | result |
|---|---|
| `ascom:ASCOM.ASICAA.Rotator` (phantom) | **refused** |
| `ascom:ASCOM.ASIMount.Telescope` (phantom) | **refused** |
| `native:zwo:1` camera | connected |
| `ascom:ASCOM.EAF.Focuser` | connected |
| `ascom:ASCOM.EFW2.FilterWheel` | connected |
| `ascom:ASCOM.PegasusAstroNYX101.Telescope` | connected |

Two phantoms rejected, four real devices unaffected.

## L22 CLOSED — the sequencer captures, and frames reach disk

First completed run of the session: `state: completed, progress: 1.0`, and on disk:

```
C:\src\rigframes\Polaris_1_0001.fits      12,882,240 bytes
C:\src\rigframes\Polaris_1_0001_001.fits  12,882,240 bytes
```

The fix was the `sequencerSetDevices` wiring on the headless `load -> start` path. Everything I
recorded as L6, L14, L21 and L22 was that one defect seen from four angles.

## FITS header — what is right, and what is missing

```
NAXIS1/2 3096 x 2080   EXPTIME 2.0        DATE-OBS 2026-08-10T00:06:02
IMAGETYP 'Light'       OBJECT 'Polaris'   FILTER '1'   FILTPOS 0
GAIN 265  OFFSET 10    CCD-TEMP 29.2      XBINNING/YBINNING 1
PIXSIZE1/2 2.4         SITELAT 39.97190   SITELONG -75.35760
RA 37.95450  OBJCTRA '02 31 49.08'        DEC 89.264  OBJCTDEC '+89 15 50.40'
FOCUSPOS 22850         FOCTEMP 30.53      NS-SESID/NS-FIDX/NS-NPLN
```

Genuinely good: real site coordinates, focuser position **and** temperature, sexagesimal and decimal
pointing that agree, and a session id tying frames to a run.

**Missing, and it matters:**

* **No `ALTITUDE`, no `AIRMASS`.** This is S5 in `simulator-fidelity-backlog.md` — recorded there
  from source inspection, now **confirmed on real hardware**. Every frame written tonight will be
  missing the two keywords photometry depends on.
* **No `INSTRUME`, `TELESCOP` or `FOCALLEN`.** Nothing in the file says which camera or scope took
  it. With two cameras on this rig and `PIXSIZE 2.4` the only clue, that is a real archival gap —
  and most processing tools read `INSTRUME` to group frames.

### Resolution *(2026-08-09)* — one code defect, one configuration gap

Both reproduced against the Linux simulator build before anything was changed; a camera-only run
with no equipment profile wrote a header of exactly the shape above.

**`OBJCTALT`/`AIRMASS` — code defect, fixed.** Not the `altitude: None` on file in
`simulator-fidelity-backlog.md` S5 (already fixed); the live gate is in `instructions.rs`, which
derives alt/az only inside `if let Some(mount_id)`. No mount, no altitude — even though `RA`/`DEC`
were written from the target and altitude is pure geometry from that same pointing plus the site and
the timestamp, all three already in the frame context. Now derived from
`FrameContext::recorded_altitude_deg`, so the altitude and the pointing cards cannot describe
different directions. Re-run after the fix: `OBJCTALT 39.371`, `AIRMASS 1.5736`.

**`INSTRUME` — code defect, fixed.** It was built only from the observer profile, which is a
cross-product of app settings and the ACTIVE EQUIPMENT PROFILE. This rig has no profile, so the
keyword was absent although the camera was connected and named. It now falls back to the driver's
own answer for the connected camera (`DeviceOps::camera_get_model`). A profile that names a camera
still wins.

Caveat worth stating plainly: on this rig the fallback yields the app's current generic label
(`ASICamera2 Camera`), not `ZWO ASI1600MM-Cool`, because of **L2** above. So `INSTRUME` will be
present but will not by itself distinguish the two ZWO cameras until L2 is fixed — at which point it
improves with no further change here. The fallback prefers `display_name`, which carries the serial
when the driver reports one, precisely so it can tell two identical bodies apart.

**`TELESCOP`/`FOCALLEN` — not a code defect.** The wiring is sound and was verified end to end:
`POST /api/sequencer/update-observer-profile` with a telescope name and focal length produced
`FOCALLEN`, `APTDIA`, `TELESCOP`, `INSTRUME` and `OBSERVER` on the very next frame. They are absent
on the rig because no equipment profile exists there (finding **L6**), and no driver can report what
scope the camera is bolted to. **Action for the owner, no rebuild required:** create an equipment
profile, or push one over the API before tonight's run — the values are the operator's to state.

### Independent verification *(2026-08-09, second pass)*

Re-reproduced from scratch rather than taken on trust. Rig ground truth re-read over `ssh nslaptop`:
`C:\src\rigframes\Polaris_1_0001.fits` has `SITELAT 39.97190`, `RA 37.95450`, `DEC 89.264` and no
`OBJCTALT`, `AIRMASS`, `INSTRUME`, `TELESCOP` or `FOCALLEN`. A camera-only run on the Linux bundle
carrying the **pre-fix** `libnightshade_bridge.so` wrote the same header shape; the same run against
the **rebuilt** library wrote `INSTRUME 'Simulator camera 1'`, `OBJCTALT 39.9719` and
`AIRMASS 1.55390`. Dec 90° makes the expected altitude the observer's latitude exactly, and 1.55390
is just under the plane-parallel `sec z` of 1.55694 — Pickering (2002) to four places. With the
mount connected the header is unchanged in kind before and after (`OBJCTALT -9.548` / `-9.611` from
the parked pointing, `AIRMASS` correctly omitted below the horizon), so the fix adds the mountless
case without touching the mount case.

One test was added on top of the fixer's: the fixer's `mountless_frame_still_records_altitude_and_
airmass` exercises `FitsWriteHeaderRich::from_frame_context`, one layer below the function that
actually wrote the rig frame. `sequencer_ops::pointing_tests::mountless_sequencer_frame_keeps_its_
altitude_through_the_save_path` pins `build_rich_header` itself, which is where a later assignment
could silently undo the altitude. Severing `altitude: ctx.recorded_altitude_deg()` fails it with
"a mountless sequencer frame must still record its altitude".

Two things the owner needs before tonight's run:

* The payload keys are **`telescopeFocalLengthMm`** and **`telescopeApertureMm`**, not
  `focalLengthMm`/`apertureMm`. The endpoint answers `200 {"status":"ok"}` to the wrong spelling and
  silently drops the values — verified: the frame came out with `TELESCOP`, `INSTRUME` and
  `OBSERVER` but no `FOCALLEN` or `APTDIA`. With the documented keys all five appear.
* The frames this rig wrote at 20:45 and 20:51, **with the Pegasus mount connected and listed by
  `/api/devices/connected`**, still carry `RA`/`DEC` equal to the target and no `OBJCTALT` — and
  `SITELAT`/`SITELONG` of `0.0`. So the mount branch is not firing on this rig even now, which is
  what makes the mountless path the one that will supply the altitude here once the appliance is
  rebuilt. The `0.0` site is most likely a concurrent probe run that never pushed a location, but it
  is worth stating that the fix derives the altitude from whatever site the executor carries: a
  wrong site yields a confidently wrong `AIRMASS` rather than an absent one. That property is not
  new — the mount branch has always derived alt/az from `ctx.latitude` unchecked — but it is only
  worth anything if the site is right.

## L23 — the native camera index is not stable either; my earlier recommendation was wrong *(P1)*

I recommended the native ZWO path over ASCOM because its ids looked stable. They are not.

| | before the replug | after the replug |
|---|---|---|
| `native:zwo:0` | ZWO ASI178MM | **ZWO ASI1600MM-Cool** |
| `native:zwo:1` | ZWO ASI1600MM-Cool | **ZWO ASI178MM** |

They swapped. I connected `native:zwo:1` believing it was the 1600 and captured the whole run on the
178MM — proven by the header: `NAXIS1/2 3096 x 2080` and `PIXSIZE 2.4`, which are the 178's geometry
and pixel size, not the 1600's 4656 x 3520 at 3.8 µm.

So the native id is an **enumeration index**, not an identity, and it re-orders across a replug
exactly as the ASCOM driver slot does (L18). Neither transport gives a stable camera identity. The
ZWO SDK exposes a serial number; that is what an id should be built from.

Correcting the record: the native path is still better for **names** and **recommended settings**,
but it is **not** the fix for identity that I claimed it was.

## L24 — frame numbering is per-node, so sibling nodes collide *(moderate)*

Two single-frame `TakeExposure` nodes on one target produced `Polaris_1_0001.fits` and
`Polaris_1_0001_001.fits`, both with `NS-FIDX = 1`. A single node with `count: 3` produces clean
`CountTest_1_0001/0002/0003`.

So the counter is per-node, and identical sibling nodes collide on the filename. Credit where due:
the collision suffix means **no frame is ever overwritten**, which is the right priority. But a
target built as several same-filter nodes yields `_0001`, `_0001_001`, `_0001_002` …, so sorting by
name no longer gives capture order, and `NS-FIDX` repeats.

## Autofocus fails correctly, and the earlier stranding did not recur

On the rebuilt appliance, autofocus against the real EAF + camera in daylight:

```
POST /api/focuser/autofocus/start {"exposureTime":1.0,"stepSize":100,"stepsOut":3}
  -> {"jobId":"5c5a3bbf-...","status":"queued"}
GET /api/jobs (t+20s)
  -> state: failed
     "Autofocus failed: Insufficient stars detected. Only 0 stars found (minimum: 10).
      This may indicate clouds, poor seeing, or incorrect camera settings."
```

That is the right answer, arrived at in under 20 s, and the message names the three real causes in
the operator's language. A capped scope in daylight is functionally identical to heavy cloud, so
this is a genuine test of the cloud path and it passes.

Notably **the CRITICAL focuser-stranding from the earlier session did not recur**. That failure read:

> `CRITICAL: failed to restore original focuser position 22850: halt failed: Halt failed:
> Operation timeout: Focuser halt timed out after 10s`

It happened while the camera had silently left the USB bus, so the restore had no working camera and
the halt hit a dead device. With healthy hardware, autofocus aborts and returns cleanly. The
underlying weakness stands — there is no retry of the position restore, and a 10 s halt timeout is
short for a real EAF — but it is a hardware-fault-path concern, not an everyday one.

---

# Session 6 — a realistic multi-filter run on the ASI1600MM-Cool

Sequence shaped like tonight's: target M51, filter slot 0 x2 Light, slot 1 x2 Light, slot 2 x1 Light
at bin 2x2, slot 0 x1 Dark. Camera `native:zwo:0` (the 1600), cooling to -5 C, dither and
meridian-flip config set through the API first.

Result: **`completed`, 6/6 exposures, six files on disk.** Frame geometry and binning are right —
32,783,040 bytes for 4656x3520 at bin 1, and 8,199,360 for 2328x1760 at bin 2 — and `XBINNING`,
`FILTPOS` and `IMAGETYP` are all recorded correctly in the headers.

## The meridian fix works on hardware

```
t+010s running  Light f1  "Meridian flip is overdue by 184 min and has not fired (hour angle +3.1...)"
t+020s running  Dark f0   "AdaptiveExposure: 1.0s (nominal 1.0s, disabled) (0%)"
```

The run **carried on**. Before the fix this is precisely where it stalled for ever — the closeout
wave's P1 reproduced it as a freeze at `1/12 8%` with the flip "firing in ~0s" at hour angle +8.93h.
Now it states the overdue condition and proceeds.

## L25 — frame type is invisible in the filename, so darks land among lights *(P1, data integrity)*

Files written, in this order:

| file | IMAGETYP | filter | binning |
|---|---|---|---|
| `M51_1_0001.fits` | Light | 0 | 1 |
| `M51_1_0001_001.fits` | **Dark** | 0 | 1 |
| `M51_1_0002.fits` | Light | 0 | 1 |
| `M51_2_0001.fits` | Light | 1 | 1 |
| `M51_2_0002.fits` | Light | 1 | 1 |
| `M51_3_0001.fits` | Light | 2 | **2** |

The dark is named `M51_1_0001_001.fits` — the same stem as the light beside it, distinguished only
by the **collision suffix**, which exists for de-duplication and carries no meaning about content.
Nothing in the name says `Dark`.

Consequences for an unattended night that writes lights and darks to one folder:

* A calibration script globbing `M51_1_*.fits` for lights silently pulls a dark into the stack.
* Sorting by name interleaves frame types.
* `_0001_001` is indistinguishable from a re-taken light that collided with an existing name.

`IMAGETYP` is correct inside every file, so nothing is lost — but the filename is what humans and
most scripts sort on. The template needs the frame type (and ideally the binning, given
`M51_3_0001` is a 2x2 frame with nothing in its name to say so).

## L26 — a run reports `completed` while its status message is an error

Final state was `state: completed`, `completedExposures: 6/6` — and the message shown alongside it:

```
Error: Autofocus failed: Insufficient stars detected. Only 0 stars found (minimum: 10).
```

An autofocus fired that the sequence never asked for (no Autofocus node; presumably a filter-change
refocus trigger), it failed in daylight as it should, and the run correctly carried on to finish all
six exposures. Continuing is the right call — a failed autofocus should not end the night.

But the operator-facing result is a completed run whose one-line summary is an error, so the
dashboard cannot be read at a glance: is this a good night's data or a failed run? The terminal
status should say the run completed and carry the autofocus failure as a warning alongside, not
overwrite the summary with the last error seen.

Related, from the same run: `currentNodeName` read **`Unknown`** for the whole autofocus phase, and
the progress line stuck at `Autofocus: step 0/15 (47%)` — the step counter never advanced past 0
while the percentage climbed.

---

# Session 7 — guiding, stop, and checkpoint/resume on the rig

## L7 CLOSED — stop is fast, honest and preserves the checkpoint

Stopping a live run mid-exposure:

```
POST /api/sequencer/stop
  -> {"commandId":"33263b0a-...","status":"stopped","preserveCheckpoint":true,"wasRunning":true}
     returned in 0.9 s
GET /api/sequencer/status -> {"state":"cancelled","progress":0.75,"message":"Cancelled: ResumeTest"}
GET /api/sequencer/checkpoint/has -> {"hasCheckpoint":true}
```

Previously this took the full 30 s and returned *"the hardware was NOT confirmed stopped, so nothing
has been torn down"* on a run that had in fact stopped. Now it is sub-second, states what it did,
and reports that the checkpoint survived.

## Guiding — checked, and NOT a defect

`POST /api/phd2/connect` returned `{"status":"connected"}`, which after L4 looked like another
phantom. It is not: PHD2 is genuinely running on the laptop (1 process, listening on 4400), so the
connect was honest. Recorded because the instinct to distrust it was right and the check took a
minute.

The unattended failure paths are clean:

| call | result |
|---|---|
| `guider/dither` on a disconnected guider | `Device not connected: Built-in multi-star guider` |
| `guider/start-guiding` on a disconnected guider | same, correctly refused |
| `phd2/dither` while connected but NOT guiding | `Failed to dither: PHD2 error: Dither error` |

The last one is honest but thin — it forwards PHD2's own string. "Cannot dither: PHD2 is connected
but not guiding" would be the version an operator can act on at 2am.

## Checkpoint `has` vs `info` — checked, NOT a defect

`checkpoint/has` returned `false` while `checkpoint/info` returned a populated record, which looked
like two endpoints disagreeing. It isn't: after a **completed** run there is a checkpoint record but
nothing to resume, and `has` answers "is there something resumable". Mid-run it correctly flips to
`true`. The naming invites the misreading — `hasCheckpoint: false` beside an `info` payload
describing a checkpoint — but the logic is right. My first reading was wrong.

## L27 — resume re-shoots the whole burst instead of continuing *(P1 for unattended nights)*

An 8-frame burst was interrupted after 5 frames and resumed from its checkpoint:

```
checkpoint/info -> {"sequenceName":"ResumeTest","completedExposures":5,
                    "completedIntegrationSecs":0.0,"canResume":true,"ageSeconds":5}
POST /api/sequencer/checkpoint/resume -> {"status":"resumed"}
status t+09s -> "Exposure: Frame 1/8 (3.0s) (12%)"
```

**It restarted at frame 1 of 8.** The checkpoint knew five were done and the resumed run shot all
eight again. The files confirm it — every frame from the first pass now has a `_001` twin:

```
ResumeTest_1_0003.fits   ResumeTest_1_0003_001.fits
ResumeTest_1_0004.fits   ResumeTest_1_0004_001.fits
ResumeTest_1_0005.fits   ResumeTest_1_0005_001.fits
ResumeTest_1_0006.fits   ResumeTest_1_0006_001.fits
ResumeTest_1_0007.fits   (no twin - beyond the interruption point)
ResumeTest_1_0008.fits
```

Frames 3-6 exist twice; 7 and 8 once. Exactly the signature of a full re-shoot layered over a
partial first pass.

On an 8-frame test this is a curiosity. On a real night it is hours: a 100-frame sequence
interrupted at frame 90 — by cloud, a dropout, or the operator stopping to check something —
restarts at frame 1 and re-shoots 90 frames it already has. The resume also silently doubles the
disk footprint of everything already captured.

Note also `completedIntegrationSecs: 0.0` in the same payload that reports `completedExposures: 5`.
Five 3-second frames is 15 seconds of integration; the field is not being accumulated, which is
probably why nothing downstream can tell how much of the burst was already done.

## Web dashboard — first exercise ever, and it mostly holds up

The dashboard (`GET /dashboard`, 80 KB) had never been opened by any audit; it entered the coverage
denominator only today. Every endpoint it depends on, tested against the live rig:

| endpoint | result |
|---|---|
| `/api/images/recent` | 200, and it lists the frames this session actually captured |
| `/api/logs/tail` | 200, live SSE stream |
| `/api/weather/current` | 200 |
| `/api/targets/search` | 200 `{"targets":[]}` — **correct**, see below |
| `/api/focus-model/filter-offsets` | 400 `No active equipment profile. Load a profile first.` |

The last one is honest, but combined with **L9** (a profile cannot be created through the API) it
means that dashboard panel is permanently broken on a headless-only rig — there is no path to the
profile it demands.

### Checked and dismissed: target search returning nothing

`/api/targets/search?query=M51` returns `{"targets":[]}`, as do M31, Vega, NGC7000 and Andromeda —
while `/api/catalog/status` reports HYG v4.4 **installed** with **119,614 objects** and OpenNGC DSO
installed. That looks damning and is not a defect:

* `handleSearchTargets` queries `database.targetsDao` — the operator's **saved target library**,
  which is empty on a fresh appliance.
* The dashboard's own source comment says so: *"The autocomplete dropdown pulls from
  /api/targets/search so saved targets are reachable by name."*
* Sky-catalog search is a different endpoint and it works, with accurate data:
  `/api/planetarium/catalog/search?query=M51` →
  `{"name":"M51","catalogId":"NGC5194","raHours":13.498,"dec":47.195,"type":"Galaxy","magnitude":8.36,"constellation":"CVn"}`.
  M51 is NGC 5194 at 13h29m/+47°12′, mag 8.4, in Canes Venatici — every field correct.

Recorded because "the app can't find M51" is exactly the conclusion a hurried audit would ship, and
it would have been wrong. Twice I suspected a defect here and twice the app was right.

## L28 — the silent-empty-on-unknown-parameter pattern is not confined to device types

L1 fixed one instance: `?deviceType=totalNonsense` used to return `200 {"devices":[]}` and now
returns a 400. The same shape survives elsewhere:

```
/api/planetarium/catalog/search?query=M51 -> {"results":[{"name":"M51", ...}]}
/api/planetarium/catalog/search?q=M51     -> {"results":[]}
/api/planetarium/catalog/search?name=M51  -> {"results":[]}
```

An unrecognised parameter name produces an empty result set with HTTP 200, indistinguishable from
"the catalogue has no such object". A client author who guesses `q` — the more common spelling, and
the one I guessed first — gets a silent, permanent empty list with nothing pointing at the mistake.

The fix pattern already exists in this codebase and reads well: reject the request naming the
expected parameter, as `/api/camera/cooling` does with *"Missing 'deviceId' query parameter"*. This
wants an audit across every GET handler that takes an optional query parameter, not another one-off.

## L29 — most captured frames are never registered, so the app cannot see its own data *(P0 for tonight)*

After six completed runs on one appliance process:

```
FITS files on disk (C:\src\rigframes)        : 30
Images registered (GET /api/images?limit=200):  8   — all from one run, "ResumeTest"
Sessions (GET /api/sessions)                 :  1   — "ResumeTest", totalExposures 8
```

Runs that captured, completed, and wrote FITS to disk but produced **no session and no registered
frames**: `DiskProof4`, `TonightShape` (the 6-frame M51 run), `CountTest` (twice) and `GainTest`.
The last of those was run specifically as a control after noticing the gap — it completed
(`state: completed, progress: 1.0`), wrote its files, and left `sessions` and `images` unchanged at
1 and 8.

The correlation is exact: the only run with a session record is the only run whose frames are
registered. Image registration appears to be gated on a session existing, and sessions are not being
created for ordinary runs. `ResumeTest` was the one run that was checkpointed, stopped and resumed,
which is the obvious suspect for what created its session — but the trigger is not established and
should not be guessed at.

**Why this is a P0 for an unattended night.** The frames are safe: they are on disk with correct
headers, which is the thing that matters most. But everything the app offers *for reviewing* a
night reads the database, not the directory — the image library, session review, analytics, the
frame grader, the dashboard's recent-images panel, integration totals, and the run report. An
operator who images all night would wake to a full disk and an app that shows one session's worth of
data, or none.

It also silently invalidates any decision the app makes from history: adaptive exposure, filter
integration budgets, and `stop_on_budget_met` all count frames the database knows about.

### L29 root cause — the headless start path re-implements a subset of the Dart executor

`sequencer_handlers.dart` `handleSequencerStart` has two branches:

```dart
final hasInEditorSequence = container.read(currentSequenceProvider) != null;
if (hasInEditorSequence) {
  final executor = container.read(sequenceExecutorProvider);
  await executor.start();                       // <- the full Dart lifecycle
} else {
  // headless load -> start
  await backend.sequencerSetSimulationMode(false);
  ...
  await _wireConnectedDevicesIntoNativeExecutor(backend);
  await backend.sequencerStart();                // <- straight to native
}
```

The Dart branch's `executor.start()` reaches `_startNativeExecution`, which does two host-side
things the bare branch does not. The **first is the one that actually writes the rows**, and my
initial reading of this finding had it wrong — it is worth stating the correction plainly, because
the wrong version would have produced a fix that did not work:

* `runtime_config_operations.dart:161` subscribes `_handleSequencerEvent` to `backend.eventStream`.
  That listener is what turns a native frame event into a `captured_images` row, advances the
  session counters, feeds progress and the run vitals, and drives auto-grading. **Nothing else
  writes those rows.** The headless branch never subscribed, so on the appliance no one was
  listening when the frames came back.
* `sequence_executor.dart:769` `_startSessionRow(sequence)` opens the `imaging_sessions` row. The
  frame insert treats `session_id` as nullable (`event_operations.dart:662-665` deliberately writes
  NULL when no session is open), so this is not what suppressed registration — but without it the
  rows that *do* land are unattributed and the session counters never move. A night as a pile of
  orphan rows is barely better than no rows.

That also explains the one exception exactly. `resumeFromCheckpoint` (`sequence_executor.dart:2194`)
is the **only other place in the codebase** that installs the same subscription, and `ResumeTest` is
the only run that went through it.

An earlier fix had already tripped over this same fact without naming it as the cause: the long
comment at `sequence_executor.dart:1248-1258` documents that "the headless `POST
/api/sequencer/load` -> `/api/sequencer/start` path starts the sequence on the NATIVE executor
without ever calling `start`, so this executor never installs `_nativeEventSubscription` and
`_onTerminalEvent` can never fire" — and worked around the symptom (a Stop that waited 30 s for an
event that could not arrive) rather than the missing subscription.

**This is the same seam that produced L22.** The headless path re-implements a subset of what the
Dart executor does at start, and every omitted step is a separate silent failure:

| step the Dart executor performs at start | headless path |
|---|---|
| wire connected devices into the native executor | **fixed today** (L22) |
| subscribe to the native event stream | **fixed today** — the registration half of L29 |
| open the `imaging_sessions` row | **fixed today** — the attribution half of L29 |
| `startRun` + persist the sequence snapshot | **still missing** — no run-history diff |
| `_bindCatalogTargets(sequence)` | **still missing** — unverified consequences |

The durable fix is not another patch to the `else` branch; it is to make the headless path share one
start routine with the editor path, so a step added for one is present in both. Until then, every
future addition to `executor.start()` silently skips the appliance — which is the configuration that
runs unattended all night with nobody watching.

### L29 fix as landed

`SequenceExecutor.attachHostListenersForNativeRun()` (public, idempotent, safe to call before every
native start — a previous run's teardown cancels the subscription, so run two would otherwise be as
blind as run one) plus `_openSessionRowForNativeRun`, both called from the bare branch before
`sequencerStart()`. The session summary comes from a `_WireSequenceSummary` parsed at
`POST /api/sequencer/load`, because the native executor exposes no way to ask it back for the
sequence name or its targets.

Two details that are not obvious and are pinned by tests:

* **Ordering.** The session row must exist before the first frame event, or that frame is stamped
  `session_id` NULL — the row is opened before the listener is attached and both before the start.
* **The second run of the night.** `SessionService.startSession` refuses to open a session while one
  is active, so a stale row from the previous target would have filed the next target's frames under
  the wrong night. The handler closes an open session first — but only when the native executor
  reports a non-running state, since a busy executor refuses the start with a 409 and ending a live
  run's session on the way to that refusal would corrupt the run in progress.

Tests: `apps/desktop/test/headless_api/sequencer_start_session_row_test.dart` (8 cases). 7 of the 8
fail when the two calls are removed from the start path, including the loop-multiplier denominator
(expected 10, got 0) and the subscription assertion.

## L30 — the disk the app writes to is not the disk it monitors *(P1 for an overnight run)*

```
POST /api/sequencer/save-path {"path":"C:\\src\\rigframes"} -> {"status":"ok","path":"C:\\src\\rigframes"}
   ... 30 FITS subsequently written there ...
GET  /api/system/disk-space -> {"configured": false}
```

Two different settings, no link between them:

* `handleSequencerSetSavePath` validates the directory and then calls
  `backend.sequencerSetSavePath(requested)` — the **native executor's** save path. It never writes
  `appSettings.imageOutputPath`.
* `handleDiskSpace` reads `settings.imageOutputPath`, finds it empty, and returns
  `{"configured": false}` — which the handler's own comment correctly calls "a valid state, not an
  error".

Each is right on its own terms, and together they mean the free-space guard watches a volume the
frames are not going to. On this appliance it is inert: 30 frames written, disk monitoring reporting
"not configured". If both were set but to different volumes it would be worse than inert — it would
report healthy space on the wrong disk while the real one filled.

An overnight run is precisely when this matters. A 100-frame night from the ASI1600 is 3.3 GB; from
a modern full-frame sensor, several times that.

**Credit where it is due, because I had this wrong.** I earlier recorded that pre-flight validates
the save path's *presence* but not its *writability*. It does validate writability, properly:
`handleSequencerSetSavePath` creates the directory, writes a `.nightshade-write-probe-<ts>` file,
deletes it, and on failure returns *"Save path ... is not usable: <reason>. Choose a directory this
host can write to."* That is a real check with a real error. The gap is only that the checked path
and the monitored path are different settings.

### L30 fix as landed

`handleSequencerSetSavePath` now also calls `setImageOutputPath(requested)` after the native call
succeeds, so the one endpoint a headless-only operator has for choosing where frames go points both
the executor and the free-space guard at the same directory. Non-fatal if the settings write fails —
the frames go where the caller asked either way, and refusing a valid save path over a monitoring
setting would trade a working night for a bookkeeping one.

Tests: `apps/desktop/test/headless_api/sequencer_save_path_disk_monitor_test.dart` — the link, that a
later change moves *both* (a guard left on the previous directory is the "healthy space on the wrong
volume" case), and that a rejected path changes neither.

## The event stream is healthy — and carries a naming trap

`GET /api/run-watch/events` (SSE) during a live 3-frame run produced 23 KB and full coverage:

```
16 Progress          15 ExposureProgress   9 InstructionProgressStructured
 9 InstructionProgress  6 ExposureStarted   3 NodeStarted
 3 FrameAccepted        3 ExposureCompleted 3 ExposureComplete   2 HostStateChanged
```

`GET /api/ws/ticket` also works and is well designed: a single-use ticket with
`expiresInSeconds: 60`, so a WebSocket can authenticate without putting the bearer token in a URL.

`FrameAccepted` firing three times for three frames is a useful corroboration of **L29** — the
grading path runs and announces each frame, and the database write is the only thing missing.

### L31 — two different events one letter apart *(minor, but a real trap)*

```
{"category":"sequencer","eventType":"ExposureCompleted","data":{"frame":1,"total":3,"duration_secs":1.0}}
{"category":"imaging",  "eventType":"ExposureComplete", "data":{"success":true}}
```

Not a duplicate emission — two different subsystems reporting different things. But a client author
filtering on `ExposureComplete` silently receives the imaging layer's bare `{"success":true}` and
never sees the sequencer's frame counter, or vice versa. The difference is one character and is
invisible when scanning a log. The `category` field distinguishes them, so this is workable rather
than broken; the names should still say what each means.

## L32 — a wrong-verb request is answered by a parameterised route, not a 405 *(moderate; costs client authors real time)*

```
GET /api/calibration/darks/find-match?...  -> 400 {"error":"Path segment is not a valid integer",
                                                   "field":"id","expected":"integer"}
GET /api/calibration/darks/clear           -> 400  (identical error)
```

Neither path takes an `id`. `find-match` and `clear` are registered as **POST**
(`calibration_routes.dart:52` and `:83`), and the literal routes are correctly declared *before* the
parameterised `GET /api/calibration/darks/<id>` at `:92` — so route ordering is fine. What happens is
that a GET finds no literal GET route, falls through to the `<id>` route, and that handler dutifully
tries to parse `find-match` as an integer.

The result is an error that names a field the caller never sent, about a value they never supplied,
for a path that exists. A `405 Method Not Allowed` naming the accepted verb would have taken seconds
instead of a detour into the route table.

Recorded as a finding rather than as my mistake — though it was also my mistake, and that is the
point: I sent the wrong verb, and the app's answer actively pointed away from the cause. With
`/api/calibration/darks/<id>` and `<id>/download` registered alongside eleven literal sibling paths,
every one of those siblings has the same trap for anyone who guesses GET.

With the correct verb the endpoint behaves properly:
`POST /api/calibration/darks/find-match` → `{"error":"exposureDuration is required","field":"exposureDuration","expected":"number"}` —
a clean, named-field 400.

### L32 fix as landed — fixed for the whole API, not for `find-match`

The headless server registers every route through one declarative table
(`routes/headless_route.dart` `registerRoutes`), which makes the table the single source of truth for
what exists. `withMethodNotAllowedFallbacks` now expands that table: for every **literal** path, each
verb the path does not implement gets a synthesized `405` carrying an RFC 9110 `Allow` header and a
body naming the verbs that do work. A path added tomorrow gets the correct answer without anyone
remembering to ask for it.

Three deliberate limits:

* **Parameterised paths are skipped.** `<id>` matches whatever the caller sent, so "this path exists
  but not with that verb" is not a claim the expander can make about them.
* **`HEAD` and `OPTIONS` are never synthesized.** HEAD is a legitimate probe of a GET route and
  OPTIONS is CORS preflight; answering either 405 would break working clients.
* **Ordering is load-bearing.** Each stub is inserted immediately after the LAST real registration of
  its path — behind the handlers that implement it, ahead of any parameterised route declared later.
  That is the same ordering rule the literal routes themselves already depend on, and a test asserts
  it rather than trusting it.

An unregistered path is still a 404: 405 asserts the path exists, and claiming it for a path nobody
registered would be a new lie in place of the old one.

Tests: `apps/desktop/test/headless_api/method_not_allowed_test.dart` (8 cases, including the verbatim
rig request). The whole 947-test headless suite is green with the expansion active, which is the
regression check that matters — every existing route now passes through it.

---

# Wave-2 residuals and the findings they produced

The 2026-08-09 fix wave came back with five verdicts (two HOLDS, three INCOMPLETE, all five with a
real repair landed). The INCOMPLETE verdicts are not failed fixes — they are fixes whose *claims*
outran their evidence, plus adjacent gaps the verifier found while checking. Those adjacent gaps are
recorded here as findings in their own right.

## L33 — a settings endpoint answered "ok" to fields it did not understand *(fixed)*

```
POST /api/sequencer/update-observer-profile {"focalLengthMm":1000,"apertureMm":200}
  -> 200 {"status":"ok"}
   ... next frame: TELESCOP, INSTRUME, OBSERVER present; FOCALLEN and APTDIA absent ...
```

The real keys are `telescopeFocalLengthMm` / `telescopeApertureMm`. The obvious spelling was accepted
and dropped, and the only way to discover it was to read a FITS header later.

**Fixed** by `fieldReport()` in `validation.dart`: the response now names the keys it applied and the
keys it ignored, with the accepted set spelled out in the warning. Reporting rather than rejecting is
deliberate — a 400 on an unknown key is the stricter contract but turns a forward-compatible client
(a phone that auto-updated ahead of its appliance) into a hard failure. Naming the ignored keys costs
nothing and is enough: the response stops claiming it did something it did not.

## L34 — a capability the camera does not have was reported as an appliance fault *(fixed)*

```
POST /api/camera/cooling {"deviceId":"native:zwo:1","enabled":false}
  -> 501 {"error":"internal_error","message":"Operation not supported"}
```

Status right, code a lie. `internal_error` says the host broke; the truth is the ASI178MM has no
cooler, which no retry and no restart will change. The wire code was derived from the status alone
(`statusCode >= 500 ? 'internal_error' : 'device_error'`), so 502 and 504 carried the same mislabel —
a driver that is unreachable or slow is not a host fault either.

**Fixed**: `not_supported` / `device_unreachable` / `device_timeout`, with `internal_error` reserved
for a genuine 500.

## L35 — three device roles had no pre-flight at all *(fixed)*

`sequencerSetDevices` carries five ids, so the Rust `collect_required_devices` walk can refuse a run
that needs a camera, mount, focuser, filter wheel or rotator it was never given. The guider, dome and
cover calibrator are reached through `device_ops` with no id, so that walk structurally cannot see
them — its own doc comment says so about `Dither`. Confirmed live: `Dither`, `StartGuiding` and
`CalibratorOn` each answered `200 {"status":"started"}` and then failed mid-run, with the recovery
loop waiting for hardware nobody had connected.

**Fixed host-side**, which is the only place with the answer, because it is the only place that knows
what is connected. The wire tree parsed at load names the roles it needs; start refuses with the same
`preflight_rejected` 400 the native refusals use. Disabled nodes contribute nothing, `StopGuiding`
alone is not a reason to refuse, and a device-listing failure lets the run proceed rather than
becoming a new way to lose a night.

## L36 — no ASCOM device ever reports its model *(open)*

Live-confirmed tonight: `/api/devices/connected` reports `EFW2 FilterWheel` and `EAF Focuser` — the
ProgID, not the model. The camera identity work added a ZWO-native name repair, but the ASCOM branch
contributes geometry only, so the four other ASCOM device types still show their driver name. The
close is a `Name` read after `Connected = true` in the Windows wrapper. Cosmetic on the equipment
screen; not cosmetic in `INSTRUME`, which is what a stacking program matches calibration against.

## L37 — the frames written tonight carry SITELAT/SITELONG 0.0 *(needs a live re-check)*

`GET /api/settings/location` correctly reports 39.9719 / -75.3576, and the same rig's frames from
20:45 and 20:51 carry `SITELAT 0.0`. Most likely a probe run that never pushed a location rather than
a product defect — but it matters more now than it did: the OBJCTALT/AIRMASS fix derives the altitude
from whatever site the executor is carrying, so a wrong site turns "no keyword" into a confidently
wrong one. Re-check after the redeploy with a known-good location pushed first.

## L38 — the mount branch was not firing *(expected closed by the L22 fix; must be re-verified)*

Those same frames carry `RA`/`DEC` equal to the target and no `OBJCTALT`, with the NYX101 connected
and listed by `/api/devices/connected`. The alt/az derivation is gated on `if let Some(mount_id)`, so
this says the executor had no mount id — which is exactly **L22**, and the binary those frames came
from (bridge DLL dated 20:00) predates the L22 fix. The prediction is therefore that a redeployed
build both wires the mount and writes the horizon keywords. Stated as a prediction on purpose: it has
not been observed yet, and the observation is the point.

## Residuals carried forward, not yet closed

* **Two ZWO bodies of the same model are indistinguishable.** No serial (`ASIGetSerialNumber` answers
  error 16 on the ASI1600MM-Cool) and identical geometry. A genuine SDK limit.
* **`observed_identities` is in-memory**, so picking `native:zwo:1` after an app restart believing it
  is the 1600 is caught by nothing. Persisting the baseline is the real close for L23.
* **`ZwoCamera::load_camera_info`'s comment is false** — it claims the loop re-resolves a stable
  CameraID across USB re-enumeration; a P/Invoke probe showed `CameraID == index`.
* **`Mosaic` and `MeridianFlip`-with-auto-center** need devices the pre-flight walk does not map, and
  neither could be reproduced without forcing the condition. Left alone rather than fixed from
  reading code.
* **A capability probe that ERRORS is treated as "do not write" and the call reports success.** Right
  for a cooler-off; for a cooler-on with a setpoint it means the setpoint is silently dropped while
  the API returns 200. Deserves at least a warn-level log.

---

# Live validation of the redeployed build — 2026-08-09 21:53 local

Rig: Pegasus NYX101 (ASCOM), ZWO ASI178MM (native SDK), ZWO EFW (native), ZWO EAF (ASCOM).
Appliance: the audit build on port 8081, database `C:\src\rigdata`, frames `C:\src\rigframes2`.

**Harness note worth keeping.** The `NightshadeHeadless` scheduled task on that laptop points at
`C:\src\Nightshade\...`, which is the owner's own install — **not** the audit tree at
`C:\src\ns-audit`. Testing through it would have validated the wrong binary, and the token mismatch
is the only reason that was caught. The audit build now runs under a separate `NightshadeAudit` task
on `--port=8081` so the two can coexist.

| finding | before | after, on hardware |
|---|---|---|
| **L29** frames registered | 30 on disk / 8 in DB | 3 captured → **3 registered**, session id 2, `totalExposures 3`, `successfulExposures 3`, `integration 6.0s`, `status completed` |
| **L29** second run | one session per process | run two opened **session id 4**, its own three frames |
| **L30** disk guard | `{"configured": false}` after writing 30 frames | `{"configured": true, "path": "C:\\", "freeBytes": 413857234944}` immediately after `save-path` |
| **L32** wrong verb | `400 "Path segment is not a valid integer", field: id` | `405`, `allow: POST`, `{"code":"method_not_allowed","error":"GET is not supported for /api/calibration/darks/find-match. This endpoint accepts: POST."}` |
| **L33** unknown fields | `200 {"status":"ok"}`, values dropped | `applied:["telescopeName"]`, `ignored:["apertureMm","focalLengthMm"]`, warning naming all seven accepted keys |
| **L35** guider pre-flight | `200 {"status":"started"}` then mid-run death | `400 preflight_rejected`, `missingDevices:["guider"]`, message naming the node |
| **L38** horizon keywords | absent with the mount connected | `OBJCTALT = 39.956`, `AIRMASS = 1.554` |
| **L37** site | `SITELAT 0.0` | `SITELAT 39.9719`, `SITELONG -75.3576` |
| **L22** mount wiring | executor had no mount id | proven by L38: the alt/az derivation only runs with a mount id |

The header is self-consistent, which is the property that matters more than the presence of the
keywords. `RA 0.660h / DEC +87.16` is the mount's **actual** pointing (parked near the pole), not the
target's; altitude 39.956° from latitude 39.9719° is what a dec of +87° gives, and `AIRMASS 1.5544`
sits just below the plane-parallel `sec z` of 1.5569 as a refracting atmosphere requires. The file
describes one direction, and it is the direction the telescope was pointing.

`INSTRUME = 'ZWO ASI178MM (3520810329000900)'` — the camera identity work, with a real serial, in the
keyword a stacking program matches calibration against.

## L39 — a refused start left a phantom night *(mine, found by running the fix; fixed)*

The L29 fix introduced it and the live drive caught it. A `load` that failed on a missing field was
followed by a `start`:

```
POST /api/sequencer/load  -> 400  "missing field `auto_select_star`"
POST /api/sequencer/start -> 409  {"error":"no_sequence_loaded"}
GET  /api/sessions        -> id=3  name="L29 Registration Check"
                                   successfulExposures=0  status=completed
```

A night that never happened, named after a sequence from twenty minutes earlier. Two defects in one:

1. The session row is opened **before** the native executor can refuse the start, and nothing closed
   it on refusal.
2. A **failed** load left the previous load's wire summary in place, so the next start labelled
   itself with a sequence that was no longer loaded.

Both fixed: the row is tracked and closed in both error paths (and released to the run once
`sequencerStart()` returns), and the summary is dropped *before* validation rather than set after it.
Three tests pin it, including the validator-rejected-load variant.

This is the shape the earlier campaign named *relocating the defect* — a fix that moves the untruth
rather than removing it. It was only visible because the fix was run, not read.

## L40 — the first connect to a present camera can fail "no longer present" *(open)*

```
GET  /api/devices?deviceType=camera -> native:zwo:0 | ZWO ASI178MM
POST /api/devices/connect {"deviceType":"camera","deviceId":"native:zwo:0"}
  -> {"error":"device_connect_failed",
      "message":"... Invalid device: ZWO camera ID 0 is no longer present in the SDK enumeration"}
```

Windows disagreed: `Get-PnpDevice -PresentOnly` listed **both** `ZWO ASI178MM Camera` and
`ZWO ASI1600MM COOL Camera` with `Status OK`, and no other process held them (one
`nightshade_desktop` running, the owner's instance stopped). A rescan and a second connect succeeded
immediately.

So the SDK enumeration the connect path checks against can be stale relative to both discovery and
the operating system. On an attended rig it costs one retry. On an unattended one — auto-connect at
dusk — it is the whole night. The fix shape is a single re-enumeration before declaring a device
absent. Not yet reproduced deterministically, so not yet fixed: it needs a repeatable trigger first.

## L41 — an appliance restart loses the capture folder *(fixed)*

Reproduced immediately after restarting the appliance with the folder already configured:

```
GET  /api/system/disk-space -> {"configured": true, "path": "C:\\", "freeBytes": 413782007808}
POST /api/sequencer/start   -> 400 preflight_rejected
       "This sequence captures frames but no image save path is configured.
        Set the sequencer save path before starting — every frame would
        otherwise be captured and discarded."
```

The appliance knew the folder and refused the run anyway. The native executor's save path is
per-process state; `imageOutputPath` is durable; nothing bridged the two at start. On an unattended
rig any restart — a power blip, the updater, a crash-and-relaunch — costs the whole night, and the
refusal names a setting the operator has already set.

`_startNativeExecution` has always pushed it for the editor path. Same step, missing from the same
seam as L22, L29 and L35. That seam has now yielded five separate defects; the durable answer
remains one shared start routine, and every one of these is an argument for it.

Fixed by pushing `imageOutputPath` into the native executor at start, keeping the editor path's
empty-path guard (pushing null would clobber a checkpoint-restored path with "don't save"). Two
tests.

Worth stating plainly, because it is the same class and is **not** fixed: a restart also leaves every
device disconnected. Nothing auto-connects on appliance startup, so a reboot mid-night ends the night
whatever the save path says. That may be deliberate — connecting hardware without a human present is
a real decision — but for a rig described as unattended it should be a decision someone made on
purpose, not a gap.

## L42 — the ASI1600MM is invisible to the ZWO SDK while Windows says it is fine *(rig condition, owner action)*

Three views of the same bus, taken within a minute of each other at 22:20 local:

```
Windows          Get-PnpDevice -PresentOnly -> ZWO ASI178MM Camera        Status OK
                                               ZWO ASI1600MM COOL Camera  Status OK
ZWO SDK (probe)  ASIGetNumOfConnectedCameras = 1
                 index=0  Name='ZWO ASI178MM'
Nightshade       native:zwo:0 -> ZWO ASI178MM
                 native:zwo:1 -> ZWO ASI178MM
```

The standalone probe runs the SDK outside the app, so it is ground truth: **the ASI1600MM is not
visible to the ASI SDK**, even though Windows has it enumerated and healthy. That is a
driver/USB-link condition on the rig, not something the app can fix — the classic shape is a USB3
link that has renegotiated badly or a hub that cannot hold the camera's power draw. It needs a
physical intervention.

It also explains the whole evening's camera churn: `native:zwo:1` present at 20:52, gone by 21:25,
`native:zwo:0` failing its first connect at 21:44 and succeeding on retry, and the connected camera
disappearing from `/api/devices/connected` at 22:19 after a clean connect at 22:16.

### L43 — the app shows a phantom camera slot left over from a previous enumeration *(open, app defect)*

Separable from L42 and genuinely ours. With the SDK offering exactly one camera, the app offered
**two** — `native:zwo:0` and `native:zwo:1` — and labelled both with the surviving camera's name. An
operator would see two cameras, pick either, and get the same body; a sequence pinned to
`native:zwo:1` would open the 178 believing it had the 1600.

This is [L23] made concrete: the id is an enumeration index, and the registry keeps entries the SDK
no longer offers. The close is to drop discovery entries a re-enumeration no longer reports, and to
refuse a connect to an index whose identity does not match the one recorded for it — the second half
of which is already built and unverified for exactly this case.

Note on my own reading: I first called this "the app labels the 1600 as a 178", which was wrong, and
the probe is what corrected it. Windows listing a device says nothing about whether the vendor SDK
can see it, and the app can only ever see what the SDK offers.

## L44 — force-killing the appliance leaves the ZWO camera held *(observed; owner action to clear)*

After `Stop-Process -Force` on the appliance and a clean relaunch, the ASI178MM could not be opened
by anything:

```
ZWO SDK probe   ASIGetNumOfConnectedCameras = 1
                index=0 Name='ZWO ASI178MM'
                ASIOpenCamera rc=5          <- still held
Nightshade      POST /api/devices/connect {"deviceId":"native:zwo:0"}
                  -> "NightshadeError.connectionFailed(... reason: Device disconnected)"
Windows         one nightshade_desktop process, started after the kill, no devices connected
```

Nothing in user space holds it and the SDK still refuses to open it, so the handle survived the
process that owned it. The camera needs a replug to clear.

Two things follow. For tonight: the ASI178MM is now in the same state as the ASI1600MM (L42), so
**both** cameras want a physical replug before the run. For the product: the appliance's own shutdown
path (`HeadlessShutdown`) closes cameras properly, but nothing protects a hard kill or a crash — and
a crash-and-relaunch is exactly the case an unattended rig has to survive. Whether that is fixable
above the SDK is an open question; ZWO offers no "force close by index".

## L45 — the owner install had no capture folder configured

`GET /api/system/disk-space` on the owner's own instance answered `{"configured": false}`, i.e. the
durable `imageOutputPath` was never set in that database. With the L41 fix in place that is no longer
silent — the run is refused at Start with the reason — but it is worth knowing before dark rather
than at the first exposure. Set for tonight to `C:\src\rigframes`, the folder already in use.

## L40 fix as landed

A connect that fails because the vendor SDK no longer enumerates the device now invalidates the
discovery cache, so the next list tells the truth instead of continuing to offer hardware that is
gone. The 60-second `DISCOVERY_CACHE_TTL` was the window: a client that retried — or an unattended
rig auto-connecting at dusk — kept failing against a list nobody had refreshed. A manual rescan
cleared it and the very next connect succeeded, which is the manual form of exactly this call.

The predicate is deliberately narrow. A device that IS on the bus and merely refuses to open (in use,
permissions, a driver fault) leaves the cache alone: the list is accurate, and dropping it would make
every busy-camera retry re-run a full multi-driver scan. Two tests, one of them the verbatim rig
message.

## Harness notes from this session

* **The appliance started by a scheduled task cannot be killed with `Stop-Process` over SSH** —
  "Access is denied", because it runs in another session's context. `Stop-ScheduledTask -TaskName
  NightshadeHeadless` is the control that works, and a deploy script that skips it will fail later at
  the DLL-copy step with a lock instead of at the stop.
* **`Binning` is an externally-tagged enum on the wire** — `"binning": "One"`, not `{"x":1,"y":1}`.
  A `{x,y}` map fails deserialization with `expected map with a single key`, which reads like a
  structural error rather than a wrong-shaped field.
* **`StartGuidingConfig` needs `auto_select_star` and `max_calibration_axis_error_deg`** — both
  non-`#[serde(default)]`, so a minimal guiding node is rejected for a missing field.

## Test-suite note: one ordering-dependent flake, not a regression

`packages/nightshade_core/test/framing/framing_pan_refetch_test.dart` — *"RA recenter step shrinks
near the pole (cos(dec) scaling)"* — failed once in a full-suite run and passed both in isolation and
on a full-suite re-run (5,556 tests, all green). The trace carries Riverpod's
`_debugAssertCanDependOn` warning, i.e. a provider read against a container another test file owns:
cross-file leakage surfaced by test ordering, the same class as the shared-database hazard closed
earlier in this campaign. Recorded rather than chased, because it is not caused by tonight's changes
and the suite is green.

# The build the owner will actually run — installed and verified 2026-08-09 22:36

`C:\src\Nightshade\apps\desktop\build\windows\x64\runner\Release` now holds the validated build
(exe 22:34, `nightshade_bridge.dll` 22:33, vendor SDK DLLs preserved), so the owner's normal
`NightshadeHeadless` task and the desktop GUI both pick it up. The previous install is backed up
whole at `C:\src\Nightshade-Release-backup-20260809` (878 files).

Verified against that instance, on its own database:

```
GET  /api/system/disk-space              -> {"configured":true,"path":"C:\\","freeBytes":413584617472}
GET  /api/calibration/darks/find-match   -> 405, allow: POST, code method_not_allowed
POST /api/sequencer/update-observer-profile {"focalLengthMm":1000}
                                         -> applied:[], ignored:["focalLengthMm"], warning naming the seven real keys
POST /api/sequencer/start  (no save-path call this process, fresh after restart)
                                         -> refused on the CAMERA, not on the save path   <- L41 closed
POST /api/sequencer/start  (sequence with StartGuiding, no guider connected)
                                         -> 400 preflight_rejected, missingDevices:["guider"]   <- L35 closed
```

The camera refusal is correct and expected: both ZWO bodies are currently unavailable (L42, L44) and
need a physical replug. Everything the software owns is in place.

## L46 — a paused run reports itself as running *(P0 for an unattended night; fixed)*

Found by running the full multi-target rehearsal on the Linux appliance rather than by reading code.
The run stopped dead and the API said it was healthy:

```
02:38:41  log: Trigger fired: HFR Degradation - action: Autofocus
          log: Autofocus failed — HFR baseline reset ...
          log: Execution paused at boundary, waiting for resume...
02:45:22  GET /api/sequencer/status
          -> {"state":"running","currentNodeId":"d1","currentNodeName":"Settle",
              "progress":0.333,"message":"Delay: 2s remaining (0%)"}
```

Seven minutes parked, reported as `running`, with a two-second delay that had lasted seven minutes.
Nobody asked for a pause: an always-armed HFR-degradation trigger force-fired after a filter change,
its autofocus action failed, and `executor/mod.rs` took the documented "pause for operator
intervention" branch. That policy is defensible for an attended run. What is not defensible is that
the operator is never told.

**Not confined to triggers.** The same probe against an explicit operator pause:

```
POST /api/sequencer/pause  -> {"status":"paused"}
GET  /api/sequencer/status -> {"state":"running", ...}
```

So **every** pause was misreported, however it arose.

### Root cause

`SequencerState` — the body of `GET /api/sequencer/status` — is built from the `SequenceProgress`
snapshot (`bridge/src/api/sequencer.rs`, `impl From<SequenceProgress> for SequencerState`), **not**
from the executor's `state` lock. Every pause path wrote the lock and left the snapshot alone:

* the `ExecutorCommand::Pause` arm of the command handler (the operator pause), and
* **eighteen** trigger/safety pause sites in the executor's trigger monitor.

The recovery driver is the one place that got it right — it writes both
(`*recovery_driver_state.write() = Running` **and** `prog.state = Running`), which is exactly the
pattern the other nineteen were missing.

### Fix

Every site that parks the executor now stamps the progress snapshot too, and `Resume` does the
mirror-image update so a resumed run cannot report as paused — swapping one untruth for the other
would have been the classic *relocated defect*.

Verified live on the rebuilt appliance:

```
POST /api/sequencer/pause  -> GET status -> {"state":"paused", ...}
POST /api/sequencer/resume -> GET status -> {"state":"running", ...}
```

776 Rust sequencer tests green.

### The rehearsal that found it, completing cleanly afterwards

Two targets, a nested `Count` loop, two filter changes, a delay node, and a pause/resume in the
middle:

```
planned    2 frames (Target A, Filter 2) + 2x2 frames (Target B, Filter 1) = 6
registered 6      by filter {'Filter 1': 4, 'Filter 2': 2}
session    total=6  ok=6  integration=9.0s (6 x 1.5s)  status=completed
```

The `totalExposures` denominator applied the loop multiplier correctly end to end — the thing the
unit test asserts, now observed against a running appliance.

### Left open, deliberately

The status `message` still reads `"Delay: 2s remaining"` while paused. The state is now truthful,
which is the load-bearing half; the per-site pause reason would have to be threaded through
nineteen call sites, each with its own wording, and inventing one would be guessing. Recorded rather
than half-done.

Also unresolved as a *policy* question, not a defect: an always-armed HFR trigger whose autofocus
fails will park an unattended run until morning. Whether that should instead log-and-continue on a
rig with nobody watching is the owner's call.

## L47 — pausing an idle executor answers "paused" *(minor)*

```
GET  /api/sequencer/status -> {"state":"idle", ...}
POST /api/sequencer/pause  -> {"status":"paused"}
GET  /api/sequencer/status -> {"state":"idle", ...}
```

Nothing was paused and nothing was harmed — the executor's command channel only exists inside a run,
so the command is accepted by the API and dropped. The status endpoint stays truthful, which is why
this is minor rather than a repeat of L46. But the *reply* claims an action that did not happen,
which is the same shape as L33: an endpoint saying "ok" to something it did not do. `409` with
"nothing is running" would be the honest answer, matching what the Dart executor's own `pause()`
raises.

Checked deliberately, because L46's fix stamps the progress snapshot on every Pause command and a
pause-while-idle could have written `paused` over a legitimately idle state. It does not: verified on
the rig after the deploy.

## L48 — a completed database restore keeps telling the user their data is gone *(P1, fixed)*

Found on the owner's own install, not the audit tree. `C:\Users\scdou\OneDrive\Documentos\Nightshade\`
held all of this at once:

```
.recovered-on-1786325956702.txt                          174   8/9  9:39 PM
nightshade-corrupt-1786325956700-nightshade.db     1,265,664   8/9  4:04 PM
nightshade-corrupt-1786325956700-nightshade.db-journal 12,824   8/9  4:04 PM
nightshade.db                                        987,136   8/9 10:36 PM

marker: reason=open-time error: attempt to write a readonly database
```

Two separate things, one already fixed and one not.

**The quarantine itself was the old build.** The reason string is `SQLITE_READONLY` — a *hot rollback
journal* (note the quarantined `-journal`) on a file inside a OneDrive-synced folder, which is the
Windows default location for `Documents`. SQLite must write to replay a hot journal; OneDrive had the
file unwritable; the old build read that as corruption and set the database aside. Pulled both files
down and checked: the "corrupt" one returns `PRAGMA integrity_check = ok` and contains 1 equipment
profile, 5 sequences and 50 captured frames. The one the owner was running had 0, 0, 0.

The already-shipped `_isCorruption` / `_needsWritableRetry` split in `integrity_check.dart` prevents
a repeat, and this incident (21:39) predates the 22:58 redeploy. Confirmed the split actually works
rather than assuming it: `SqliteException.resultCode` reports the **primary** code, verified by
forcing a real `SQLITE_READONLY` against the real binding — `resultCode=8 extendedResultCode=8`. Had
it reported the extended code (264/520/1032), `_needsWritableRetry`'s `== 8` would never have matched
and the retry it exists to perform would have been dead code.

**The residual, which was live and is the actual defect.** `applyPendingRestore` swaps the database
back but never clears `.recovered-on-*.txt`. Replayed the owner's exact state locally — their marker,
their quarantined file. Restore works: profile, 5 sequences and 50 frames all come back, and the
displaced database is kept as `nightshade-replaced-*` so the restore is itself reversible. But every
launch afterwards still announces:

> Nightshade could not open your database when it last started and recreated a fresh one so the app
> could start. Your existing settings, profiles, sessions and captures are not in the new database.

about a database that now contains all of them — permanently, on every launch (verified across two
relaunches with `profiles=1` in the file at the same time). Worse than stale: the quarantined file is
now named `nightshade-replaced-*`, so `_findMostRecentBackup` finds nothing to offer and the notice
degrades from an actionable dialog into an alarm with no button. Same shape as the cry-wolf class —
the app stating something untrue about itself — at the single most alarming moment it has.

Fixed by clearing the markers after a *successful* swap only; a failed restore leaves the user on the
recreated database, where the warning is still true and must survive. Both directions pinned in
`restore_clears_recovery_marker_test.dart`, and the revert-check discriminates: with the fix removed
the success case fails on exactly that assertion while the failure case still passes.

Verified in the running app, not just in tests: replayed the owner's state on a release build, hit
**Restore my database**, relaunched, and got `profiles=1 images=50 sequences=5`, zero markers left,
zero false notices. The first attempt at this verification was invalid and is worth recording — the
harness prefers `build/linux/x64/release/bundle` and I had rebuilt only `debug`, so the "fix didn't
work" reading was a stale binary, not a bad fix.

**Not fixed, and the owner's to decide:** their live database and eight-megabyte FITS files are being
written directly into a OneDrive-synced folder, which is what produced the unwritable file in the
first place. That is a user-configuration matter with a real cost during a ten-hour unattended run,
so it is flagged rather than changed underneath them.

## L49 — first `/api/devices` call costs 82 seconds *(P3)*

```
call 0  HTTP:200  total:82.348s
call 1  HTTP:200  total:0.023s
call 2  HTTP:200  total:0.031s
```

The cold ASCOM registry scan across the ~40 drivers installed on the rig; cached thereafter, so it
costs nothing during a run. Recorded because a user opening Equipment for the first time after launch
waits 82 seconds, and nothing in the response says a scan is in progress.

## L50 — an interrupted run's checkpoint records zero integration *(P2, fixed)*

Found by aborting a run in the GUI and relaunching: the startup "Recover Sequence?" modal offered to
resume, and the checkpoint behind it read, on disk:

```json
{ "completed_exposures": 3, "completed_integration_secs": 0.0,
  "executor_state": "Paused", "is_active": true,
  "timestamp": "2026-08-10T06:37:41Z" }
```

Three frames of an 8-second exposure node had completed — 24 seconds of sky — and the checkpoint
recorded none of it.

**Cause.** The executor credited integration in one lump from the exposure node's closing progress
event (`final_upd.completed_exposure_secs = Some(total_exposure_time)`), gated on
`result.status == NodeStatus::Success`; per-frame events deliberately carried `None` to avoid
double-counting. So a node that was paused, aborted or failed contributed exactly zero seconds no
matter how much of it had run. This matters most where it hurts most: a real unattended night is one
long exposure node per filter, so a crash at 04:00 after six hours of imaging would have offered a
resume dialog reading "0m integration".

**Fix.** Credit each frame as it lands, on the same monotonic `current > *last` sighting that already
makes `completed_exposures` exactly-once under retries and batched bursts — demonstrably correct,
since the same checkpoint held an accurate `completed_exposures: 3`. The closing one-shot now
contributes only the remainder (`(total - already_credited).max(0.0)`), so a producer with no
per-frame duration — smart exposure is not in `exposure_node_metadata` — still gets its whole burst
counted, and a completed TakeExposure node never adds twice.

**Verified in the running app**, by repeating the measurement that found it:

| | before | after |
|---|---|---|
| paused mid-node, 3 frames × 8s | `completed_integration_secs = 0.0` | **24.0** |
| same run resumed to completion | — | `framesCaptured 10, integrationSecs 80.0` (= 10 × 8, no double count) |

776 `nightshade_sequencer` tests pass.

**Scope correction, established rather than assumed.** `sequence_runs.stats_json` for the *earlier*,
pre-fix interrupted run already read `framesCaptured 5, integrationSecs 40.0`. The Dart-side session
report was therefore never wrong; only the Rust executor's counter — the one feeding the checkpoint
and the resume dialog — was. Worth stating plainly, because the first framing of this finding was
broader than the evidence supported.

**Two related things left open, both observed and neither fixed here.** After a deliberate Stop the
checkpoint still holds `executor_state: "Paused"` and `is_active: true`, so the next launch offers to
resume a run the operator chose to end; and the checkpoint's frame count lags the run (it read 3
while the UI showed 6), so a resume re-shoots frames already on disk. Conservative rather than lossy
— it redoes work instead of skipping it — but both are real and want a decision about when a
terminal state should clear the checkpoint.

**Build trap worth recording:** `flutter build linux --release` does NOT rebuild the Rust. The bundle
kept a stale `libnightshade_bridge.so` and the first verification attempt was measuring the old
binary. Run `cargo build --release -p nightshade_bridge` and copy it into `bundle/lib/` before
believing any native-side result.

**Also withdrawn:** the resume modal appeared to report a status of "Completed" for an interrupted
run. It does not — `Completed` is the *label* of the row whose value is "N frames (Mm integration)".
The accessibility tree flattened label and value into sibling nodes and I assembled them wrongly.
