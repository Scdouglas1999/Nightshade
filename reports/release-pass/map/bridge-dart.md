# Release-pass map — `nightshade_bridge` (Dart)

Subsystem: `packages/nightshade_bridge/`
Mapper: read-only. No source file was edited.
Date: 2026-08-11

---

## 0. Orientation — what is generated and what is not

This matters more than usual here, because the two files named in the task brief
(`lib/src/api/imaging.dart` 2495, `lib/src/api/sequencer.dart` 1322) are **both
flutter_rust_bridge output**, not hand-written.

`packages/nightshade_bridge/lib` totals 92,618 lines of Dart. Of that,
**87,131 lines are generated** and 5,487 are hand-written.

Generated (exempt from the oversize rule, but see §1 for the Rust-side split):

* Everything under `lib/src/api/` — all 23 files carry
  `// This file is automatically generated ... @generated by flutter_rust_bridge@ 2.11.1`
  on line 1–2. Verified with `grep -rl "automatically generated" lib/`.
* `lib/src/frb_generated.dart` (27,838), `frb_generated.io.dart` (24,285),
  `frb_generated.web.dart` (4,226).
* `lib/src/event.dart` (1,260), `device.dart` (710), `device_capabilities.dart`
  (1,107), `error.dart`, `state.dart`, `storage.dart`, `lib.dart` — all FRB mirror files.
* All `*.freezed.dart` (`event.freezed.dart` 9,024, `error.freezed.dart` 2,709, …).

Generation contract (`native/nightshade_native/flutter_rust_bridge.yaml`):
`rust_input: crate::api`, `rust_root: bridge/`,
`dart_output: ../../packages/nightshade_bridge/lib/src/`. The mapping is 1:1 —
one Rust module under `bridge/src/api/` produces one Dart file under
`lib/src/api/`. **You cannot split a generated Dart file directly; you split the
Rust module and re-run codegen via `scripts/dev.sh`.**

Hand-written files, by size:

| Lines | File |
|------:|------|
| 1057 | `lib/src/bridge_stub/sequencer_operations.dart` |
|  970 | `lib/src/phd2_client.dart` |
|  874 | `lib/src/bridge_stub/discovery_operations.dart` |
|  799 | `lib/src/bridge_stub/guiding_operations.dart` |
|  761 | `lib/src/bridge_stub/equipment_operations.dart` |
|  723 | `lib/src/event_display.dart` |
|  619 | `lib/src/alpaca_client.dart` |
|  587 | `lib/src/bridge_stub/native_bridge_facade.dart` |
|  512 | `lib/src/bridge_stub/connection_operations.dart` |
|  508 | `lib/src/ascom_client.dart` |
|  483 | `lib/src/bridge_stub/runtime_operations.dart` |
|  294 | `lib/src/utils/circuit_breaker.dart` |
|  284 | `lib/src/bridge_stub.dart` |
|  243 | `lib/src/bridge_stub/storage_and_image_operations.dart` |
| 1400 | `test/fakes/fake_native_bridge.dart` (test-only) |

Note the **`bridge_stub` library is one compilation unit of 5,638 lines**
(`bridge_stub.dart` + 9 `part` files). It is already split by domain; the
problem is not the split, it is the volume (§2.1).

### The three-layer call topology (needed to read the rest of this report)

```
nightshade_core / apps
        │
        ├─(A)─► bridge_api.apiX(...)          top-level, generated  ──► Rust
        │
        └─(B)─► NativeBridge.x(...)   static facade
                        │  native_bridge_facade.dart  (1-line delegation × 153)
                        ▼
                _NativeBridgeImplementation.x()   bridge_stub/*_operations.dart
                        │  guard + try/catch/log/rethrow
                        ▼
                gen_api.apiX(...)                 ──► Rust
```

Both paths are live and **mixed inside the same files**. Counts of
`bridge_api.` vs `NativeBridge.` per file in `nightshade_core/lib/src/backend/ffi_backend/`:

| File | `bridge_api.` | `NativeBridge.` |
|---|---:|---:|
| `mount_guiding_operations.dart` | 27 | 52 |
| `sequencer_recovery_operations.dart` | 14 | 34 |
| `status_profile_operations.dart` | 9 | 13 |
| `discovery_camera_operations.dart` | 10 | 9 |
| `image_polar_operations.dart` | 7 | 0 |
| `session_heartbeat_operations.dart` | 4 | 0 |

---

## 1. Oversized files

### 1.1 `lib/src/api/imaging.dart` — 2495 lines — GENERATED — risk: low, but split upstream

**Why it is big.** It is the Dart mirror of
`native/nightshade_native/bridge/src/api/imaging.rs` (**7406 lines**), the
largest Rust API module. 56 top-level `pub fn` + 28 generated classes land in
one file.

**Split plan (Rust-side; Dart follows automatically).**
`bridge/src/api/` already demonstrates the pattern with `api/devices/` and
`api/connection/` subdirectories that produce `lib/src/api/devices/*.dart`.
Apply the same to imaging. Convert `bridge/src/api/imaging.rs` into
`bridge/src/api/imaging/mod.rs` + six submodules; the generator will emit six
Dart files under `lib/src/api/imaging/`.

| New Rust module → new Dart file | Functions that move |
|---|---|
| `imaging/exposure.rs` → `api/imaging/exposure.dart` | `api_camera_start_exposure`, `api_camera_start_exposure_configured`, `api_camera_cancel_exposure`, `api_get_last_image`, `api_get_last_raw_image_data`, `api_clear_device_image`, `api_get_star_crops_from_last_image` |
| `imaging/autofocus.rs` → `api/imaging/autofocus.dart` | `api_run_autofocus`, `api_cancel_autofocus`, `api_run_indi_autofocus`, `api_calculate_hfr`, `api_detect_stars_in_file` |
| `imaging/file_io.rs` → `api/imaging/file_io.dart` | `api_read_fits_file`, `api_read_fits_linear_data`, `api_read_xisf_file`, `api_save_fits_file`, `save_fits_file_rich`, `api_save_fits_from_last_capture`, `api_save_xisf_file`, `api_save_jpeg_file`, `api_save_png_file`, `api_save_tiff_file`, `api_save_rgba_jpeg_file`, `api_save_rgba_png_file`, `api_update_fits_keywords`, `api_generate_filename`, `api_get_next_frame_number`, `api_generate_fits_thumbnail` |
| `imaging/display.rs` → `api/imaging/display.dart` | `api_apply_stretch`, `api_auto_stretch_image`, `api_auto_stretch_color_image`, `api_calculate_auto_stretch`, `api_calculate_histogram`, `api_debayer_image`, `api_debayer_fits_file`, `api_get_image_stats` |
| `imaging/calibration.rs` → `api/imaging/calibration.dart` | `api_calibrate_image_data`, `api_calibrate_image_file`, `api_combine_master_frames`, `api_defect_map_build`, `api_defect_map_apply`, `api_defect_map_clear`, `api_defect_map_get_status`, `api_sequencer_apply_defect_map`, `api_compute_fits_quality_maps`, `api_compute_last_capture_quality_maps` |
| `imaging/live_stack.rs` → `api/imaging/live_stack.dart` | `api_stacking_start`, `api_stacking_start_from_data`, `api_stacking_add_frame`, `api_stacking_add_frame_from_data`, `api_stacking_get_result`, `api_stacking_get_stats`, `api_stacking_frame_count`, `api_stacking_is_active`, `api_stacking_reset`, `api_stacking_stop` |

**Stays put:** the private helpers already listed as ignored in the generated
header (`percentile`, `median`, `mad`, `display_data_to_rgba`,
`store_captured_image_atomically`, `defect_maps_root`, …) move with whichever
submodule uses them; put shared ones in `imaging/mod.rs` as `pub(crate)`.

**Behaviour preservation:** `lib/src/api_barrel.dart` is the hand-written
aggregate re-export (`export 'api/imaging.dart';`). Replace that single line
with six `export 'api/imaging/*.dart';` lines and **no downstream import
changes at all** — every consumer already goes through
`package:nightshade_bridge/nightshade_bridge.dart`. Verify with
`grep -rn "src/api/imaging.dart" packages apps server` → currently only
`api_barrel.dart` references it.

**Cost/benefit:** this is a codegen + Rust-refactor task with a mandatory
`flutter_rust_bridge_codegen` re-run under the `CPATH` documented in
`flutter_rust_bridge.yaml`. Do it only if the Rust `imaging.rs` is being split
anyway. Otherwise **leave it**: it is machine-written, nobody reads it, and a
bad codegen run is a runtime kernel-transform failure that passes
`flutter analyze` (documented in the yaml comments).

### 1.2 `lib/src/api/sequencer.dart` — 1322 lines — GENERATED — risk: low

Mirror of `bridge/src/api/sequencer.rs` (3729 lines). 77 top-level functions,
7 classes/enums. Same rule as above. Natural Rust submodules:

| New module → Dart file | Functions |
|---|---|
| `sequencer/nodes.rs` → `api/sequencer/nodes.dart` | the 18 `api_create_*_node` fns + `api_build_sequence` |
| `sequencer/planning.rs` → `api/sequencer/planning.dart` | `api_calculate_mosaic_area`, `api_calculate_mosaic_panels`, `api_estimate_mosaic_time`, `api_calculate_altitude` |
| `sequencer/control.rs` → `api/sequencer/control.dart` | `api_sequencer_{load,load_json,start,pause,resume,stop,skip,skip_to_node,reset,get_state,subscribe_events,plugin_node_finished,set_devices,set_save_path,set_simulation_mode}` |
| `sequencer/config.rs` → `api/sequencer/config.dart` | the 15 `api_sequencer_update_*` fns + `set_safety_*` |
| `sequencer/checkpoint.rs` → `api/sequencer/checkpoint.dart` | `api_sequencer_{save,clear,has,get}_checkpoint*`, `resume_from_checkpoint`, `api_perform_meridian_flip` |
| `sequencer/recovery.rs` → `api/sequencer/recovery.dart` | `api_sequencer_recovery_*`, `api_sequencer_get_*_json`, `api_broadcast_*`, decision-logging fns |

Same barrel edit, same zero-downstream-change property.

### 1.3 `lib/src/bridge_stub/sequencer_operations.dart` — 1057 lines — HAND-WRITTEN — risk: medium

**Why it is big.** 38 methods, and **35 of them are the identical 20-line
boilerplate**:

```dart
Future<void> sequencerX(args) async {
  if (!_nativeAvailable) { _nativeBridgeRequired('sequencerX'); }
  try {
    await gen_api.apiSequencerX(args);
    developer.log('[Bridge] …', name: 'NativeBridge', level: 800);
  } catch (e) {
    developer.log('[Bridge] Error …: $e', name: 'NativeBridge', level: 1000);
    rethrow;
  }
}
```

Measured across `bridge_stub/`: 102 `_nativeBridgeRequired(` call sites,
69 `rethrow;`, 133 `developer.log(`. In this file alone: 38 / 38 / 59.

The pattern adds **nothing** over calling `gen_api.apiSequencerX` directly
except (a) a fail-closed guard and (b) a log line — both of which can be
factored into one helper.

**Split plan.** Do the de-boilerplating first, then the file does not need a
split at all (it drops to ~350 lines).

1. Add `lib/src/bridge_stub/native_call.dart` (new `part`):

   ```dart
   part of '../bridge_stub.dart';

   extension _NativeCall on _NativeBridgeImplementation {
     Future<T> _native<T>(String op, Future<T> Function() body, {String? okLog}) async {
       if (!_nativeAvailable) _nativeBridgeRequired(op);
       try {
         final r = await body();
         if (okLog != null) developer.log('[Bridge] $okLog', name: 'NativeBridge', level: 800);
         return r;
       } catch (e) {
         developer.log('[Bridge] Error in $op: $e', name: 'NativeBridge', level: 1000);
         rethrow;
       }
     }
   }
   ```

2. Rewrite each of the 35 pass-throughs as a one-liner:

   ```dart
   Future<void> sequencerSetSavePath({String? path}) => _native(
     'sequencerSetSavePath',
     () => gen_api.apiSequencerSetSavePath(path: path),
     okLog: 'Set sequencer save path: ${path ?? "<none>"}',
   );
   ```

3. **What must NOT be collapsed** (these have real bodies — keep them verbatim):
   * `sequencerSubscribeEvents` (lines 26–49) — idempotence flag `_sequencerEventsSubscribed`.
   * `sequencerStart` (647–666) — calls `sequencerSubscribeEvents()` first, then sets `_sequencerState`.
   * `sequencerPause/Resume/Stop/Reset/ResumeFromCheckpoint` (669–724, 796–813, 1004–1020) — mutate `_sequencerState` / `_loadedSequenceJson`.
   * `sequencerLoadJson` (52–68) — caches `_loadedSequenceJson`.
   * `sequencerGetStatus` (862–889) — computes `calculateSequencerProgressFraction` and maps to `SequencerStatus`.
   * `sequencerGetCheckpointInfo` (932–957) — FRB→local type mapping.
   * `sequencerUpdateDefaultAdaptiveExposure` (578–619) — flattens 3 maps into 6 parallel lists.
   * `sequencerEventStream` (820–832), `getSequencerState`, `isSimulationMode` — no try/catch.

4. Then split what remains into two parts:
   * `bridge_stub/sequencer_control.dart` — lifecycle + state (`subscribe`, `load`, `start/pause/resume/stop/skip/skipToNode/reset`, `getStatus`, `getSequencerState`, `eventStream`, `pluginNodeFinished`, `setSimulationMode`, `performMeridianFlip`).
   * `bridge_stub/sequencer_config.dart` — every `sequencerSet*` / `sequencerUpdate*` / `sequencerClear*` + the checkpoint five.
   * `calculateSequencerProgressFraction` (top-level, lines 3–11) stays in `sequencer_control.dart` — it is public and covered by `test/sequencer_progress_test.dart`.

**Effort:** medium. Behaviour-preserving as long as step 3 is respected. The
error-message text changes from `'Error <verb> …'` to `'Error in <op>'`; grep
first for tests asserting on those strings (`grep -rn "Error starting sequence" packages` → none today).

### 1.4 `test/fakes/fake_native_bridge.dart` — 1400 lines — HAND-WRITTEN — risk: **delete, do not split**

See §3.1 — it has no consumer other than its own 305-line smoke test, and it is
structurally impossible to inject. Split plan = deletion, not reorganisation.

### 1.5 Nothing else in `lib/` exceeds 1000 hand-written lines

`phd2_client.dart` at 970 is the next largest and is under the bar; it is
covered under §2.2 (duplication) rather than as an oversize item.

---

## 2. Duplication

### 2.1 The `NativeBridge` facade is a third layer over an already-thin wrapper — CANONICAL: the generated `api/*.dart`

**Evidence.** `native_bridge_facade.dart` is 587 lines containing exactly 153
static members, every one of which is a pure one-line delegation
(`static Future<void> sequencerPause() => _nativeBridge.sequencerPause();`,
line 449). The implementation it delegates to is itself, in ~110 of those cases,
a pure `gen_api.apiX(...)` pass-through (see §1.3 and
`storage_and_image_operations.dart:10–233`, where 20 of 21 methods are
`guard; return gen_api.apiX(...)` with no logging at all).

Consumers already bypass it half the time — see the `bridge_api.` vs
`NativeBridge.` table in §0. `image_polar_operations.dart` and
`session_heartbeat_operations.dart` use the generated API exclusively and lose
nothing.

**Canonical survivor:** the generated `api/*.dart` (re-exported via
`lib/src/api_barrel.dart`).

**What merges into it:** for every facade method whose implementation is a pure
pass-through, delete the facade entry *and* the impl method, and change the
`nightshade_core` call site from `NativeBridge.x(...)` to `bridge_api.apiX(...)`.
That is ~110 of 153 members, ≈700 lines across `native_bridge_facade.dart`,
`storage_and_image_operations.dart`, `equipment_operations.dart`, and
`sequencer_operations.dart`.

**What must stay in `NativeBridge`** (these hold Dart-side state or logic the
generated API does not have):
`init`, `isNativeAvailable`, `getNativeVersion`, `nativeLibrary`,
`eventStream` (fallback re-mapping), `discoverDevices` +
`invalidateDiscoveryCache` (the 60 s cache + single-sweep completer),
`connectDevice` / `disconnectDevice` / `isDeviceConnected` /
`getConnectedDevices` (the `_connectedDevices` bookkeeping and Alpaca
fallback), all `phd2*` and `guider*` (the `_phd2Client` fallback and
`_canonicalGuiderDeviceId` normalisation), `sequencerStart/Pause/Resume/Stop/
Reset/LoadJson/GetStatus/GetSequencerState` (`_sequencerState`),
`sequencerSubscribeEvents`, `sequencerGetCheckpointInfo`,
`sequencerUpdateDefaultAdaptiveExposure`, `apiGetImageStats`.

**Effort:** large, but mechanical and highly parallelisable (one call site at a
time). Do it *after* §3.2 removes the dead facade members, which cuts the work
by ~40 entries for free.

### 2.2 A complete second device stack in Dart, only reachable when the native library fails to load — CANONICAL: Rust

Three hand-written Dart client implementations exist solely for the
`!_nativeAvailable` branch:

| Dart | Lines | Rust counterpart | Lines |
|---|---:|---|---:|
| `lib/src/phd2_client.dart` | 970 | `native/nightshade_native/imaging/src/phd2.rs` | 2201 |
| `lib/src/alpaca_client.dart` | 619 | `native/nightshade_native/alpaca/src/{client,discovery}.rs` | 2443 |
| `lib/src/ascom_client.dart` | 508 | `native/nightshade_native/ascom/src/` | 6179 |
| supporting: `utils/retry.dart` (193), `utils/circuit_breaker.dart` (294), `rolling_rms_calculator.dart` (72) | 559 | (Rust has its own retry/backoff) | — |

Gating evidence:
* `guiding_operations.dart:24` — `if (_nativeAvailable) { … gen_api.apiPhd2Connect(…); return; }` before any `_phd2Client` use; the same shape repeats at lines 142, 169, 200, 321, 343, 363, 389, 402, 418, 438, 449, 465, 556, 568.
* `discovery_operations.dart:297` — ASCOM registry discovery is inside `if (!_nativeAvailable && Platform.isWindows)`.
* `discovery_operations.dart:334` — Alpaca UDP + PHD2 subnet scan are inside `if (!_nativeAvailable)`.
* `connection_operations.dart:98/154` — Alpaca is the *only* family that can reach the Dart client with native loaded, and only after a native connect failure.
* `bridge_stub.dart:12–15` (the library docstring) states the policy: *"this bridge will NOT fall back to simulator implementations … Use INDI/ASCOM/Alpaca external simulators for testing instead."*

The doc says fail-closed; the code carries 2,656 lines of a second
implementation that fires when the DLL is missing. Those two facts contradict
each other.

**Canonical survivor:** Rust. **Recommendation:** decide the policy explicitly.
Either (a) delete the Dart fallback stack and let `!_nativeAvailable` fail closed
everywhere — consistent with the stated policy and with
`_nativeBridgeRequired`'s existence — or (b) keep it and write down when it is
supposed to run. Do not ship both with an undocumented split. If (a): delete
`phd2_client.dart`, `alpaca_client.dart`, `utils/retry.dart`,
`utils/circuit_breaker.dart`, `rolling_rms_calculator.dart`, the `!_nativeAvailable`
tails of `guiding_operations.dart`, `_connectAlpacaDevice` /
`_connectAscomDevice`, and the fallback sections of `_runFullDiscoverySweep`;
keep `ascom_client.dart`'s `discoverAscomDrivers` only if the Windows registry
fallback is still wanted. Also delete the four tests that exist only to cover the
fallback (`alpaca_client_test.dart`, `phd2_event_parsing_test.dart`,
`phd2_rpc_test.dart`, `rolling_rms_calculator_test.dart`,
`connection_resilience_test.dart`). **Effort: large.**

### 2.3 Two `apiDiscoverIndiAtAddress` — one Rust, one Dart, and production calls the Rust one

* Dart: `NativeBridge.apiDiscoverIndiAtAddress` →
  `discovery_operations.dart:9–90` (TCP + `<getProperties>` + XML) and
  `_parseIndiDevices` at `discovery_operations.dart:93–193`. ~185 lines.
* Rust: generated top-level `apiDiscoverIndiAtAddress` at
  `lib/src/api/discovery.dart:31`, backed by
  `native/nightshade_native/indi/src/discovery.rs:313 infer_device_type`.

`lib/nightshade_bridge.dart` exports **both** (`export 'src/bridge_stub.dart' …;`
line 8 and `export 'src/api_barrel.dart';` line 38). They differ only by the
`NativeBridge.` receiver prefix.

Production resolves to the **Rust** one:
`packages/nightshade_core/lib/src/backend/ffi_backend/discovery_camera_operations.dart:44`
calls `bridge_api.apiDiscoverIndiAtAddress(host: host, port: port)` — a
top-level call, so it binds to the generated function.

The Dart one's only callers are
`packages/nightshade_bridge/test/indi_discovery_parsing_test.dart:56` and `:171`.

The two classifiers have **drifted**: the Dart table
(`discovery_operations.dart:139–163`) recognises 7 types (camera, mount,
focuser, filterWheel, rotator, dome, weather); the Rust
`infer_device_type` recognises 10 (adds `Guider`, `SafetyMonitor`,
`CoverCalibrator`). A maintainer fixing "INDI discovery misses my safety
monitor" has a 50 % chance of patching the file that never runs.

**Canonical survivor:** the generated/Rust `apiDiscoverIndiAtAddress`.
**Merge:** delete `NativeBridge.apiDiscoverIndiAtAddress` (facade line 16–19),
the impl (`discovery_operations.dart:9–90`), `_parseIndiDevices` (93–193), and
`test/indi_discovery_parsing_test.dart` (203 lines). Port the test's XML fixtures
to a Rust `#[test]` against `infer_device_type` if that coverage is wanted.
**Effort: small.** Highest value-per-line in this report.

### 2.4 Identity type-converters — `_toGenDeviceType` / `_fromGenDeviceType` / `_fromGenDriverType`

`bridge_stub.dart:91–92`:

```dart
typedef DeviceType = gen_device.DeviceType;
typedef DriverType = gen_device.DriverType;
```

`discovery_operations.dart:776–845` then defines three exhaustive switch
functions that map each enum value to itself, ~70 lines:

```dart
gen_device.DeviceType _toGenDeviceType(DeviceType deviceType) {
  switch (deviceType) {
    case DeviceType.camera:  return gen_device.DeviceType.camera;   // identity
    …
```

Call sites: `discovery_operations.dart:260`, `connection_operations.dart:105`,
`:382`, `:463`, `:489`, `:491`. All can be replaced with the argument itself.

**Merge:** delete all three functions and inline the argument. **Effort: small.**
Adjacent but *not* identical: `_deviceTypeToAscomType` (434–460) and
`_alpacaTypeMatches` (848–873) are real mappings — keep them (subject to §2.2).

### 2.5 `phd2*` and `guider*` are the same eight operations twice

`guiding_operations.dart` defines a matched pair for every guider operation:
`phd2StartGuiding`/`guiderStartGuiding` (164 / 314), `phd2StopGuiding`/`guiderStop`
(199 / 341), `phd2Dither`/`guiderDither` (—/354), `phd2Loop`/`guiderLoop`
(555 / 387), `phd2FindStar`/`guiderFindStar` (—/400),
`phd2SetLockPosition`/`guiderSetLockPosition` (—/411),
`phd2GetLockPosition`/`guiderGetLockPosition` (—/434),
`phd2DeselectStar`/`guiderDeselectStar` (—/447),
`phd2GetStarImage`/`guiderGetStarImage` (567 / 460).

The `guider*` variants are the unified, device-id-parameterised form; each one's
non-native branch is literally
`if (normalizedDeviceId == 'phd2_guider') { await phd2X(...); return; }`
(e.g. lines 330–337, 347–350, 374–383). The `phd2*` variants are the legacy
PHD2-only form. `_canonicalGuiderDeviceId` (`bridge_stub.dart:79`) exists purely
to bridge the two naming schemes.

**Canonical survivor:** `guider*`. **Merge:** the `phd2*` methods become private
helpers of the `guider*` ones (or vanish entirely if §2.2(a) is chosen). Facade
entries `phd2StartGuiding`, `phd2StopGuiding`, `phd2Dither`, `phd2Loop`,
`phd2FindStar`, `phd2SetLockPosition`, `phd2GetLockPosition`,
`phd2DeselectStar`, `phd2GetStarImage` should go once `nightshade_core`'s
`mount_guiding_operations.dart` is switched to the `guider*` forms.
**Effort: medium** (requires touching the core call sites).

### 2.6 PHD2 install-detection duplicated across three platform methods

`discovery_operations.dart:642–773`: `_isPhd2InstalledWindows`,
`_isPhd2InstalledMacOS`, `_isPhd2InstalledLinux` are 130 lines of the same
shape — a path list, then `which`, then `pgrep`/`tasklist`, then a package
manager. The Windows PHD2 path list at 643–648 is byte-identical to the one at
`guiding_operations.dart:53–58`.

**Merge:** one `_isPhd2Installed({required List<String> paths, List<List<String>> probes})`
plus a per-platform const path table; hoist the Windows path list to a single
`const _phd2WindowsPaths`. **Effort: small.** Subsumed by §2.2(a).

---

## 3. Dead code

### 3.1 `test/fakes/fake_native_bridge.dart` (1400) + `test/fakes/fakes.dart` (8) + `test/fakes/fake_native_bridge_test.dart` (305)

**Evidence.**
* `grep -rln "FakeNativeBridge\|fake_native_bridge" packages apps server` returns
  exactly four files: the fake, its barrel, its own smoke test, and
  `packages/nightshade_app/test/harness/mock_backend.dart` — where the only
  mention is a comment (lines 13–21) explaining **why it is not used**:
  *"Why NOT delegate to `FakeNativeBridge` … a test that wants to exercise the
  `NightshadeBackend` contract directly needs a fake at the backend layer, not
  the bridge layer."*
* The escape hatch that comment offers — *"An `FfiBackend(bridge: FakeNativeBridge(...))`
  construction is also available"* — **does not exist**:
  `packages/nightshade_core/lib/src/backend/ffi_backend.dart:137` is
  `FfiBackend({super.database});`. There is no `bridge` parameter, and the real
  bridge is a `static`-only class over a private singleton
  (`native_bridge_state.dart:5`, `final _nativeBridge = _NativeBridgeImplementation();`),
  so it is structurally un-injectable. The fake's own header (lines 7–12) admits
  this.
* `test/fakes/fakes.dart` documents its import path as
  `package:nightshade_bridge/test/fakes/fakes.dart` — not a resolvable URI
  (`test/` is not under `lib/`). `grep -rn "fakes.dart" packages` shows the only
  `../fakes/fakes.dart` importers are in **`nightshade_core/test/`**, importing
  `nightshade_core`'s own, different barrel.

**Recommendation:** delete all three files (1713 lines). If a bridge-layer fake
is genuinely wanted later, the prerequisite is an injectable seam on
`FfiBackend`, which is a separate design decision.

### 3.2 40 of 153 `NativeBridge` facade members have zero callers repo-wide

Method: extract every `static` member from `native_bridge_facade.dart`, then
`grep -rho "NativeBridge\.<name>\b" --include=*.dart packages apps server` for
each. Zero hits for:

```
apiAutoStretchImage   apiCancelAutofocus   apiDebayerImage   apiGetImageStats
apiRunAutofocus       cameraStatus         dispose           endSession
filterWheelSetPosition filterWheelStatus   focuserStatus     getLastImage
getNativeVersion      getPlateSolverPath   getSequencerState getSessionState
invalidateDiscoveryCache isDeviceConnected isPlateSolverAvailable isSimulationMode
loadedSequenceJson    mountAbort           mountFindHome     mountGetTrackingRate
mountMoveAxis         mountSetTrackingRate mountSlewAltAz    nativeLibrary
phd2AutoSelectStar    phd2GetStarImage     phd2PauseGuiding  phd2ResumeGuiding
sequencerClearDefaultAdaptiveExposure      sequencerEventStream
sequencerSubscribeEvents                   sequencerUpdateDefaultAdaptiveExposure
sequencerUpdateSkyBrightness               setCameraBinning  startExposure
startSession
```

Caveat honoured: for the ones that *look* like they must be live, I traced the
real path. `mountMoveAxis` is called as `backend.mountMoveAxis(...)`
(`nightshade_app/lib/screens/dashboard/widgets/mount_control_card.dart:628`),
which lands on
`nightshade_core/lib/src/backend/ffi_backend/mount_guiding_operations.dart:43–44`
→ `bridge_api.mountMoveAxis(...)` — the **generated** function, never the facade.
Same for `mountAbort` (`mount_guiding_operations.dart:75–76`). So these facade
entries are shadowed duplicates, not load-bearing.

Three of them are internally used and must only lose the *facade* entry, not the
impl: `sequencerSubscribeEvents` (called by `sequencerStart`,
`sequencer_operations.dart:653`), `invalidateDiscoveryCache`
(`runtime_operations.dart:407`, referenced by the `discoverDevices` doc), and
`isNativeAvailable` (which *does* have a caller). Everything else can lose both
layers.

**Recommendation:** delete the 40 facade entries and, for the 37 with no internal
use, their `_NativeBridgeImplementation` methods too. ~350 lines.

### 3.3 `_connectAscomDevice` is unreachable

`connection_operations.dart:93–96` computes
`shouldUseNativeOnly = deviceId.startsWith('ascom:') || 'native:' || 'indi:'`.
For an `ascom:` id there are exactly two outcomes:

* `_nativeAvailable == true` → native connect succeeds and returns (line 129), or
  fails and **throws** at line 141–145 because `shouldUseNativeOnly` is true.
* `_nativeAvailable == false` → **throws** at line 154–158.

Line 172 (`if (deviceId.startsWith('ascom:')) { await _connectAscomDevice(...); }`)
is therefore never reached. Consequently:

* `_connectAscomDevice` (lines 184–239, 56 lines) is dead.
* `ascom.AscomDeviceClient` has no live constructor call
  (`grep -rn "AscomDeviceClient" packages apps server` → only the class
  definition at `ascom_client.dart:282`, the dead call at
  `connection_operations.dart:200`, and the field declaration).
  `ascom_client.dart:282–466` (`connect`, `disconnect`, `getProperty`,
  `setProperty`, `callMethod`, `dispose`) is dead — ~185 lines.
* `_ascomClients` (`native_bridge_state.dart:31`) is written only in the dead
  branch, so the disconnect cleanup at `connection_operations.dart:426–441`
  always finds `null`.

Also dead in the same file: `showAscomChooser` (`ascom_client.dart:469`) —
`grep -rn "showAscomChooser" packages apps server` → definition only.
And `discoverAllAscomDrivers` (`ascom_client.dart:92`) — definition only.

### 3.4 `_initializeDefaultStates` fabricates hardware status that nothing reads

`runtime_operations.dart:88–149` (62 lines) assigns invented values:
`CameraStatus(sensorWidth: 4144, sensorHeight: 2822, pixelSizeX: 3.76, gain: 100…)`,
`FilterWheelStatus(filterCount: 7, filterNames: ['L','R','G','B','Ha','OIII','SII'])`,
`FocuserStatus(position: 25000, maxPosition: 50000)`, `MountStatus(...)`.

Full read/write trace (`grep -rn "_cameraStatus\|_mountStatus\|_focuserStatus\|_filterWheelStatus" bridge_stub*`):

* `_mountStatus` — **written once (runtime_operations.dart:110), never read.**
  Pure dead field.
* `_cameraStatus`, `_focuserStatus`, `_filterWheelStatus` — written once, read
  only by the getters at `sequencer_operations.dart:19–21`, which are exposed
  only as `NativeBridge.cameraStatus/focuserStatus/filterWheelStatus` — all three
  in the zero-caller list of §3.2.

So the whole block is dead **and** hazardous if revived: the getters can never
return live hardware state (nothing else ever writes these fields), so any future
consumer would silently render a 4144×2822 camera and a 7-slot LRGB+SHO wheel
that do not exist. This is the shape recorded in the `cry-wolf-defect-class`
memory. **Delete the four fields and `_initializeDefaultStates()` together with
the three getters.**

### 3.5 `utils/safe_cast.dart` doc claim is false

`lib/nightshade_bridge.dart:56–57`: *"Re-exported from nightshade_core for
discoverability."* There is no `safe_cast` in `nightshade_core`
(`find packages -name "safe_cast*.dart"` → only the bridge's own file and its
test). The file is the original, not a re-export. Not dead — `safelyCast*` has 8
live uses in `guiding_operations.dart:587–612, 736–739` — but the comment is
wrong and should be corrected or dropped.

---

## 4. Performance risks

### 4.1 `apiAutoStretchImage` / `apiDebayerImage` / `apiCalibrateImageData` / `apiGenerateFitsThumbnail` are generated **sync** and block the calling isolate — impact: HIGH

**Evidence.** 60 of the generated top-level API functions have non-`Future`
return types, i.e. FRB `sync` mode. The heavy ones:

* `lib/src/api/imaging.dart:456` — `Uint8List apiAutoStretchImage({...})`
* `lib/src/api/imaging.dart:485` — `Uint8List apiDebayerImage({...})`
* `lib/src/api/imaging.dart:501` — `Uint8List apiGenerateFitsThumbnail({...})`
* `lib/src/api/imaging.dart:557` — `Uint16List apiCalibrateImageData({...})`
* `lib/src/api/imaging.dart:445` — `ImageStatsResult apiGetImageStats({...})`
* `lib/src/api/plate_solve.dart:48` — `PlateSolverDetection apiPlatesolveDetect()`

A sync FRB call runs the Rust body on the Dart thread. For a full-frame stretch
or debayer of a 24 MP sensor that is hundreds of milliseconds of jank per call.

Live call sites (cross-package, listed so the implementer can measure):
`nightshade_core/lib/src/providers/auto_stretch_provider.dart:232`,
`nightshade_core/lib/src/services/stacking_engine_seam.dart:190`,
`nightshade_core/lib/src/services/calibration_service.dart:373`,
`nightshade_core/lib/src/services/thumbnail_sidecar_service.dart:48`,
`nightshade_core/lib/src/backend/ffi_backend/session_heartbeat_operations.dart:84`,
`nightshade_core/lib/src/backend/ffi_backend/image_polar_operations.dart:58`,
`nightshade_core/lib/src/backend/ffi_backend/mount_guiding_operations.dart:669`.

`auto_stretch_provider.dart` is the sharpest case: the doc comment at line 221
says *"This is compute-intensive, so it runs in an isolate"*, and the Dart
fallback at line 240 does use `compute(...)` — but the Rust path at line 232 is
a bare synchronous call on whatever isolate the provider runs on.

**Fix (bridge side):** drop `#[frb(sync)]` from these Rust functions so they
generate as `Future` and run on the FRB worker pool. That is a signature change
(callers must `await`), so it is a coordinated bridge+core change. Measure first
with a full-res frame before committing to it.

### 4.2 `rawData.toList()` before the sync stretch — impact: HIGH (cross-package, at our boundary)

`nightshade_core/lib/src/providers/auto_stretch_provider.dart:234` passes
`data: rawData.toList()` where `rawData` is a `Uint16List`. `.toList()` allocates
a fresh `List<int>` and copies element-by-element — for a 24 MP frame that is
24 M boxed-int slots and a full O(n) copy, immediately before the *synchronous*
Rust call of §4.1. The generated signature
(`imaging.dart:456`, `required List<int> data`) is what forces the copy.

**Fix:** change the Rust parameter to `ZeroCopyBuffer<Vec<u16>>` (or accept
`Uint16List` directly) so FRB generates a typed-data parameter and the copy
disappears. Bridge-side change, core-side call-site update.

### 4.3 PHD2 subnet scan opens up to 254 TCP sockets per interface — impact: MEDIUM (fallback path only)

`discovery_operations.dart:542–578` (`_scanSubnetForPhd2`) iterates
`for (int i = 1; i <= 254; i++)`, creating a `Socket.connect` with a 500 ms
timeout for each, in batches of 50. `_getLocalNetworkAddresses`
(`discovery_operations.dart:507–538`) enumerates **every** IPv4 interface and
derives a /24 per address — on a dev box with Docker, a VPN and Wi-Fi that is 3+
subnets, i.e. 750+ connect attempts, ≈8 s of the discovery sweep.

Reached only from `_runFullDiscoverySweep`'s `if (!_nativeAvailable)` branch
(line 334) → `_discoverPhd2Instances` (line 373). Goes away entirely under §2.2(a).

### 4.4 Discovery cache is a full sweep of all 11 device types on any single-type miss — impact: LOW (intentional)

`discovery_operations.dart:207–240`: a request for one `DeviceType` that misses
the 60 s cache runs `_runFullDiscoverySweep()` across `DeviceType.values`. This
is deliberate and documented (lines 203–206: *"A single sweep discovers ALL
device types so that concurrent callers … share one network scan"*), and the
`_discoverySweepInProgress` completer correctly coalesces concurrent callers.
Recorded as understood behaviour, not a defect.

### 4.5 No provider/widget perf risks in this package

`nightshade_bridge` contains no `Widget`, no `ConsumerWidget`, and no Riverpod
provider. `grep -rn "ref.watch\|StatelessWidget\|ConsumerWidget" lib/` → no hits.
Rebuild-storm / missing-`const` / non-builder-list findings do not apply here;
they belong to the app-package mappers.

---

## 5. Reliability risks

### 5.1 `Phd2Client` loses every JSON message split across TCP packets — HIGH confidence, fallback path

`phd2_client.dart:457–462`:

```dart
void _handleData(List<int> data) {
  _lastDataReceived = DateTime.now();
  final buffer = StringBuffer();          // <-- LOCAL, recreated per packet
  buffer.write(String.fromCharCodes(data));
  _processBuffer(buffer);
}
```

`_processBuffer` (465–493) is written to carry a partial trailing line forward —
line 476–479: *"If this is the last line and doesn't end with newline, keep it in
buffer"* → `buffer.write(line)`. But that buffer is the function-local one, which
is garbage as soon as `_handleData` returns. `grep -n "StringBuffer\|_buffer" phd2_client.dart`
returns only lines 459 and 465 — **there is no instance-level buffer field**.

Failure: any PHD2 JSON-RPC frame that arrives split across two TCP reads (routine
for `get_star_image`, whose base64 payload is kilobytes) is dropped. The first
fragment fails `jsonDecode` and is logged at level 900; the second fragment fails
too. The matching `Completer` in `_pendingRequests` never completes and the
caller hangs until the 30 s timeout at `phd2_client.dart:692–698`.

**Fix:** promote the buffer to a field (`final StringBuffer _rxBuffer = StringBuffer();`),
never clear it wholesale, and use `utf8.decode(data, allowMalformed: true)` with
a running decoder instead of `String.fromCharCodes` (which also mangles any
non-ASCII byte — line 460).

### 5.2 `phd2AutoSelectStar` has an inverted native guard — HIGH confidence

`guiding_operations.dart:544–552`:

```dart
Future<void> phd2AutoSelectStar() async {
  if (_nativeAvailable) {
    _nativeBridgeRequired('phd2AutoSelectStar');   // throws "requires the native bridge"
  }
  if (_phd2Client == null || !_phd2Client!.isConnected) { … }
```

Every other guard in the package is `if (!_nativeAvailable)`. Verified unique:
`grep -n -B1 "_nativeBridgeRequired(" bridge_stub/*.dart | grep "if (_nativeAvailable)"`
matches only `guiding_operations.dart:545`.

`_nativeBridgeRequired` returns `Never`, so on any build where the native library
loaded — i.e. every shipping build — this method throws
`UnsupportedError('Operation "phd2AutoSelectStar" requires the native bridge.')`,
which is the exact opposite of the truth. There is no
`apiPhd2AutoSelectStar` in the generated API (`grep -rn "AutoSelect" lib/src/api/`
→ no hits), so "unsupported with native" may be the intent — but the message
lies. Latent today: `NativeBridge.phd2AutoSelectStar` has no callers (§3.2).

**Fix:** either implement the native path, or throw
`UnsupportedError('phd2AutoSelectStar is only available on the Dart PHD2 fallback client')`.

### 5.3 `ImageStats.mad` is populated with the standard deviation

`storage_and_image_operations.dart:189–196`:

```dart
return ImageStats(
  …
  stdDev: native.stdDev,
  mad: native.stdDev,     // <-- MAD set from stdDev
);
```

The `ImageStats` class (`bridge_stub.dart:228–244`) declares `mad` and `stdDev`
as distinct fields. For a star field these differ by roughly 1.48×; any threshold
tuned on MAD would be wrong. Currently latent — `NativeBridge.apiGetImageStats`
is in the zero-caller list (§3.2) — but if the wrapper survives the §2.1 cleanup
this must be fixed or `mad` removed.

### 5.4 `discoverAlpacaServers` mutates its result list after returning it

`alpaca_client.dart:127` fires `unawaited(_tryLocalPorts(servers));` and then
`await Future<void>.delayed(timeout);` (line 130) before `return servers;` (133).
`_tryLocalPorts` (148–173) probes three ports with a 500 ms timeout each and
appends to the *same* list.

`discoverAllAlpacaDevices` (219–231) then does
`for (final server in servers) { await getAlpacaDevices(server); }` — an
`await` inside a `for-in` over a list another future may still be appending to.
If any localhost probe outlives the caller's timeout (2 s from
`discovery_operations.dart:339–341`, vs a worst case of ~1.5 s + scheduling for
`_tryLocalPorts`), the iteration throws `ConcurrentModificationError`.

Timing-dependent, not deterministic — flagged as a race, not a guaranteed bug.
**Fix:** `await _tryLocalPorts(servers)` (it is already bounded by three 500 ms
timeouts) or have it return its own list and merge after the delay.

### 5.5 `NativeBridge.dispose()` permanently breaks the process-wide singleton

`storage_and_image_operations.dart:239–242` is `void dispose() { _eventController.close(); }`.
`_eventController` is a `final` field on the process-wide singleton
(`native_bridge_state.dart:5, 13–14`) and is never reassigned. After `dispose()`,
every `_eventController.add(...)` — `runtime_operations.dart:60`,
`connection_operations.dart:119/220/338/446`, `guiding_operations.dart:129/152`,
`equipment_operations.dart:637/650` — throws `StateError: Cannot add event after closing`,
and `init()` cannot recover because `_initialized` is also never reset.

Latent: `NativeBridge.dispose` has no callers (§3.2). **Fix:** delete `dispose()`
along with the rest of §3.2, or make it reset `_initialized` and recreate the
controller.

### 5.6 Fire-and-forget async in `Phd2Client`

* `phd2_client.dart:365` — `_reconnectWithBackoff();` called from the synchronous
  `_handleConnectionLost()` with the future discarded. The body is fully wrapped
  in `try/catch/finally` (377–436) so nothing escapes, but the reconnect is
  unobservable and untestable.
* `phd2_client.dart:305` — `Timer.periodic(heartbeatInterval, (_) => _sendHeartbeat())`
  discards the future. `_sendHeartbeat` catches `on Exception` only (line 341);
  a non-`Exception` `Error` (e.g. a `TypeError` from a malformed PHD2 reply
  inside `_sendRequest`) escapes as an unhandled async error in the root zone.
  Widen to `catch (e)` or `on Object`.
* `guiding_operations.dart:149` — `unawaited(_phd2Client?.disconnect() ?? …)`.
  Explicit and intentional; noted only for completeness.

### 5.7 Swallowed errors worth knowing about

* `discovery_operations.dart:82–89` — `apiDiscoverIndiAtAddress` returns `[]` on
  any failure, so a wrong host/port and an unreachable server are
  indistinguishable from "server has no devices". (Dead per §2.3.)
* `discovery_operations.dart:180–190` — `_parseIndiDevices` returns whatever it
  parsed on XML failure, with a comment noting the regex fallback deliberately
  adds nothing. Silent partial results.
* `discovery_operations.dart:560–562` — `.catchError((e) {})` per host in the
  subnet scan. Intentional.
* `alpaca_client.dart:111–113`, `169–171`, `206–213` — malformed discovery
  responses and dead ports are swallowed silently.
* `connection_operations.dart:466–475` — `isDeviceConnected` falls back to the
  Dart `_connectedDevices` map when the native query throws, so a native failure
  is reported as the last-known state rather than as an error. Deliberate
  (comment at 472) but worth a second look given the cry-wolf history.

No `unwrap`/`expect`/`panic` applies — this subsystem is Dart-only.

---

## 6. Suspected cross-package duplication (for the cross-cutting agent)

1. `lib/src/phd2_client.dart` (970) duplicates `native/nightshade_native/imaging/src/phd2.rs` (2201) — full PHD2 JSON-RPC client twice.
2. `lib/src/alpaca_client.dart` (619) duplicates `native/nightshade_native/alpaca/src/client.rs` (1767) + `discovery.rs` (676).
3. `lib/src/ascom_client.dart` (508) duplicates `native/nightshade_native/ascom/src/` (6179) — registry discovery + COM property access.
4. `discovery_operations.dart:93–193 _parseIndiDevices` duplicates `native/nightshade_native/indi/src/discovery.rs:313 infer_device_type`, and has drifted (7 device types vs 10).
5. `bridge_stub/native_bridge_facade.dart` (153 statics) is a near-1:1 shadow of the generated `lib/src/api/*.dart` surface, and `nightshade_core/lib/src/backend/ffi_backend/*` is a *third* 1:1 layer on top of both.
6. `nightshade_core/lib/src/backend/ffi_backend.dart:8–12` imports `package:nightshade_bridge/nightshade_bridge.dart` **five times** under five aliases (`bridge`, `bridge_api`, `bridge_caps`, `bridge_device`, `bridge_error`) — the same library. Cosmetic, but it hides which layer a call targets and directly enabled the §2.3 shadowing.
7. `utils/circuit_breaker.dart` + `utils/retry.dart` (487) vs whatever retry/backoff the Rust device layer uses — two resilience policies with independent thresholds.
8. `bridge_stub/sequencer_operations.dart:3 calculateSequencerProgressFraction` vs `nightshade_core/lib/src/models/sequence/sequence_models/sequence_state.dart:600` (`completedExposures / totalExposures`) vs `apps/mobile/lib/services/foreground_service.dart:141` — three progress formulas; the bridge one special-cases `state == 'completed'` → 1.0 and clamps, the other two do not.
9. `lib/src/device_capabilities.dart` (generated, 1107) vs `nightshade_core/lib/src/models/backend/device_capabilities.dart` — mapped field-by-field in `ffi_backend/bridge_model_mappers.dart` (329 lines of pure conversion).
10. `lib/src/event_display.dart` (723) is bridge-owned but is a pure presentation concern re-exported through `nightshade_core/lib/nightshade_core_events.dart` — worth checking whether the app also formats these events anywhere.

---

## 7. Ordered work list

1. **§2.3** Delete the shadow Dart INDI discovery (`NativeBridge.apiDiscoverIndiAtAddress`,
   `_parseIndiDevices`, `indi_discovery_parsing_test.dart`). ~390 lines, small, removes a
   drifted classifier that would mislead the next person who touches INDI discovery.
2. **§3.1** Delete `test/fakes/` (fake + barrel + self-test, 1713 lines). No consumer,
   structurally un-injectable, and its own doc points at an `FfiBackend(bridge:)`
   constructor that does not exist.
3. **§3.2 + §3.4 + §3.3** Delete the 40 zero-caller facade members, the four
   write-only status fields with `_initializeDefaultStates`, and the unreachable
   `_connectAscomDevice` / `AscomDeviceClient` / `showAscomChooser` /
   `discoverAllAscomDrivers`. ~650 lines, and it removes the fabricated
   4144×2822-camera / 7-filter-wheel state before anything starts reading it.
4. **§5.1 + §5.2** Fix the two live-path defects in the Dart fallback: the PHD2
   receive buffer that drops every split frame, and the inverted
   `phd2AutoSelectStar` guard. Small, and cheap insurance if §2.2 is answered (b).
5. **§2.2 decision** Rule on the Dart fallback device stack: delete it (matching
   the stated fail-closed policy) or document when it runs. 2,656 lines hang on
   this answer, as do items 6 and 8 below.
6. **§1.3** De-boilerplate `sequencer_operations.dart` with a `_native<T>()` helper,
   then split into `sequencer_control.dart` + `sequencer_config.dart`. 1057 → ~350.
7. **§2.4** Delete the three identity enum converters and inline the argument. ~70 lines, trivial.
8. **§4.1 + §4.2** Measure `apiAutoStretchImage` on a full-res frame; if it janks,
   drop `#[frb(sync)]` and switch the `List<int>` parameter to a zero-copy typed
   buffer. Coordinated bridge+core change — schedule after the deletions.

Deliberately **not** recommended for this pass: splitting `api/imaging.dart` and
`api/sequencer.dart` (§1.1, §1.2). They are generated, nobody reads them, and the
split requires a Rust refactor plus a codegen run whose failure mode passes
`flutter analyze` and breaks at runtime. Do it only if `imaging.rs` /
`sequencer.rs` are being split on the Rust side anyway; the plans above are
written so they can be picked up at that moment.
