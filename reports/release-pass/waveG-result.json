{
 "checkers": 3,
 "verified": 16,
 "failed": [
  {
   "role": "driver-sequencer",
   "id": "WF-STOP-N1",
   "evidence": "BLOCKED, not refuted. The DB oracle passes: all 24 light frames across 4 runs read exposure_duration 15.0 (min=max=15.0 per node), so the 5.0s first-light-frame symptom did not recur. But the second half of the oracle failed - no meridian flip could be made to fire, so the camera-claim mechanism was never exercised. Run 15 (mount parked at RA 0h, target 10min past meridian) logs a NEW honest warning before every frame: 'Not holding the next 15s exposure for a meridian flip: the target is +0.17h past the meridian but the mount reports hour angle -2.30h, so the flip trigger cannot fire.' - proving the gate keys on the MOUNT's HA, not the target's (Wave F's flip fired only because its mount sat at RA 0h with LST ~07h). Run 17, with the mount slewed to and TRACKING the target 19-24 min past the meridian and enable_meridian_flip=true / minutes_past=5.0 / max_retries=3, captured 8/8 frames back-to-back with zero flips, zero waits, and no warning after frame 1 ('the mount has not reported a position'); its Session Report reads 'Meridian flips 0'. Cannot separate app defect from a simulator that never reports a pier side needing a flip."
  },
  {
   "role": "driver-sequencer",
   "id": "WF-N4/WF-STOP-N3",
   "evidence": "One operator Stop at 01:54:26 raised TWO toasts, not one, and the second is a regression: a RED 'Sequence Error / Sequence cancelled' error toast beside the info 'Sequence stopped / Sequence stopped by request at 01:54.' (shots/waveG-sequencer/g15-toasts.png, cropped from g15-stopclick.png ~2s after the click). Wave F recorded the red error toast as GONE ('no red, no Critical, no failed, no aborted'); it is back. The router's per-transport content dedupe evidently collapsed the three identical INFO notices into one, but a differently-worded error-classified producer now shows through beside it. RECENT EVENTS still triplicates: three 'Sequence stopped' rows all at 01:54:26 plus 'Decision logged - system_event #15: Sequence cancelled' (g19-events.png) - the feed count is unchanged from Wave F. All feed rows are INFO level so the feed itself does not cry wolf."
  },
  {
   "role": "driver-sequencer",
   "id": "WF-STOP-N4",
   "evidence": "NOT REACHABLE, skipped as the charter permits. The stall string 'no progress for ' is present in the shipped lib/libapp.so and the retry-surfacing synthetic node id 'trigger:meridian_flip' is present in lib/libnightshade_bridge.so, so both halves shipped. But the simulator produced no meridian flip at all in any of four runs (see WF-STOP-N1), so no run ever stalled in a retry ladder and nothing could be observed claiming Running with a past ETA."
  },
  {
   "role": "driver-chrome",
   "id": "WD-EQ-3",
   "evidence": "Equipment -> Discovery -> Connect 'Built-in Multi-Star Guider' (one click) still raises TWO 'Guider Error' toasts that differ only by a trailing full stop, with no (x2) count: '...requires an active profile with a guide focal length' and '...guide focal length.' Reproduced twice ~14 min apart (shots waveG-01/02/03). The router's normalized-key dedupe (strip trailing punctuation) is not collapsing the exact pair it was written for. Trap: a screenshot 3 s after the click shows only ONE card because the second producer lands between 3 s and 5 s -- a short-wait check reads as fixed."
  },
  {
   "role": "driver-chrome",
   "id": "WF-EQ-N1",
   "evidence": "resize 1000 800 -> Sequencer. Sequences IS fixed: 'Sequence Library' renders in full and the search box is full width (waveG-08). Templates is NOT: still 'Sequence T...' at the same rendered font size as the Sequences title beside it, so the shrink-to-16px-floor never reaches this caller, and BOTH subtitles are still cut -- 'Start with a temp...' and 'Browse and load your s...' (waveG-07). The a11y names do carry the full strings ('panel: Sequence Templates\\nStart with a template or save your sequences for reuse.'), so the mitigation is live but the visible CON-52 sentence is still destroyed at a supported desktop width."
  },
  {
   "role": "driver-chrome",
   "id": "WE-SP-5",
   "evidence": "Not verifiable as specified: the Moon card and the Smart Night prompt never coexist on this build, so the CockpitStandby reserve was never exercised. Standby briefing (0 devices): tree grep -ci moonrise = 1, grep -ci 'build smart night' = 0 (waveG-14). With devices connected the prompt appears ('Plan tonight automatically' / 'Skip this step' / 'Build Smart Night') and the dashboard switches to the connected layout: moonrise = 0, prompt = 2 -- checked at 1600x900 and again 20 s later, counts unchanged. smart_night_prompt_card.dart gates the prompt on equipmentReady && readyGraceElapsed && profile != null && location != null, and the Moon card lives in the no-equipment branch. RESIDUAL of the same class found instead: connected dashboard at 1000x800 scrolled to the hard bottom (75 notches; last 25 moved nothing, two captures pixel-identical bar the clock) -- the prompt card occupies x 372-851 / y 573-709 and paints over the RECENT EVENTS rows at y 588 and y 641, cutting 'Equipment Conne...' mid-word (waveG-13)."
  },
  {
   "role": "refuter",
   "id": "SEQ-18-provider-tally (ffix-exposure-integrity, \"fifth look\": node_exposure_tally.dart)",
   "evidence": "REFUTED by wire shape. The tally reads `final json = event.data['detail_json']; final map = json is Map ? json : const {};` (packages/nightshade_core/lib/src/providers/sequence/node_exposure_tally.dart:167-168). Production delivers a JSON *String*: native/nightshade_native/bridge/src/api/sequencer/event_translation.rs:174 `serde_json::to_string(detail)` -> bridge/src/event/sequencer.rs:128 `detail_json: String` -> event_mapping.dart:534 `'detail_json': sequencerEvent.detailJson`. Every other consumer decodes it first (event_operations.dart:316 `_decodeStructuredProgressJson` -> jsonDecode at :521). The shipped pin feeds a Map (exposure_card_wave_f_events_test.dart:66-68 `{'frame': frame, 'total': total}`), so it passes on a shape production never sends. Throwaway test (/tmp/.../scratchpad/refute_tally_test.dart, run in packages/nightshade_core): with detail_json = jsonEncode({'frame':N,'total':4}) the tally after four frames printed `WIRE-SHAPE tally after 4 frames: {}` (expected 4, got null); the same events with a Map printed `MAP-SHAPE tally: {node-one: NodeExposureTally(4/4)}`. Consequence, same file: because `ExposureCompleted` carries no node_id on the wire (event_mapping.dart:481-489), `_namedSourceSeen` never becomes true and the id-less fallback is used forever, so replaying the waveF boundary (NodeStarted node2 emitted before node1's last frame) printed `BOUNDARY tally: {node-one: 3/4, node-two: 4/4}` -- the finished node reads 3/4 and the NEXT node is credited with its frame, i.e. the join-by-position mistake the docstring says it avoids. At HEAD the card falls back to the old string parse, so SEQ-18 is unclosed on a real host."
  },
  {
   "role": "refuter",
   "id": "camera-claim-serialization incl. the must-NOT-wait list (ffix-exposure-integrity, WF-STOP-N1)",
   "evidence": "The LIST holds: `cargo test -p nightshade_sequencer every_camera_driving_trigger_action_waits_for_the_frame_in_flight` -> `test result: ok. 1 passed` at HEAD, and camera_driving_trigger_action (executor/trigger_context.rs:427-435) returns None for Pause/ParkAndAbort/Dither. The stated INVARIANT is refuted: 'The flip and recenter arms hand the camera back ... including on the paths where they do not run at all, so the capture loop never waits out the ten-minute expiry.' The claim is now taken unconditionally above the action match (executor/start.rs:3403-3414) for RecoveryAction::Autofocus, but the Autofocus arm releases it in exactly one place -- start.rs:3786, inside the `(Some(_), Some(_))` camera+focuser branch. The sibling `_ =>` branch (start.rs:3954-4014), reached when the camera or focuser id is absent, logs 'skipping the refocus and continuing the run' and falls through with no `clear_camera_busy`. There are only five release sites in the crate (grep clear_camera_busy: start.rs:3786/4106/4309/4463/4500, plus expose.rs:455/479) and none of them run on that path, so the hold expires only at TRIGGER_CAMERA_CLAIM_SECS = 600 s (trigger_context.rs), during which instructions/expose.rs:412-434 blocks every frame. The arm's own comment records this as a live-reproduced scenario ('a rig with the camera + wheel connected and no focuser ... backstop for a device that disappears mid-run'). Evidence is line-anchored static control flow, not executed: the leak is inside a private closure in start.rs and could not be exercised without a repo edit."
  },
  {
   "role": "refuter",
   "id": "WF-STOP-N2-integration-source-swap (ffix-exposure-integrity)",
   "evidence": "Holds for TakeExposure nodes; refuted as stated ('Every surface now sums the same number the row does' / 'this also fixes adaptive/smart exposure'). Only ONE producer stamps the new field: node/instructions/expose.rs:255 `upd.frame_exposure_secs = Some(recorded_secs)` (grep frame_exposure_secs across the sequencer crate returns node/progress.rs:708/750/773, expose.rs:255, start.rs:1029/1037 and nothing else). SmartExposure's own producer sets current_frame/total_frames but no recorded seconds (node/instructions/smart_exposure.rs:605-609), and it keeps a second, plan-based accumulator (`total_exposure_time = duration_secs * f64::from(total_count)` into context.completed_integration_secs, node/instructions/expose.rs:267-271; `secs_just_added = plan.duration_secs * frames_just_taken`, smart_exposure.rs:362). Worse for the 'every surface' claim: the ExposureStarted/ExposureCompleted pair is built inside `if let Some((planned_secs, filter)) = metadata` (executor/start.rs:1047-1088) and `exposure_node_metadata` is populated only from `NodeType::TakeExposure` (start.rs:353-367; the code comment at start.rs:868-870 says so outright). A Smart Exposure node therefore emits no ExposureCompleted at all, and the Dart run vitals are built from that event (sequence_executor/event_operations.dart:157-195 `_recordRunFrame` + `recordCompletedFrameIntegration`), so a smart-exposure run still reports frames/integration that the captured_images row sum contradicts. The checkpoint half of the claim does hold (executor/checkpoint.rs:149 copies progress.completed_integration_secs)."
  },
  {
   "role": "refuter",
   "id": "WF-STOP-N4-stall-detector (ffix-exposure-integrity, the ETA half)",
   "evidence": "REFUTED empirically. `_stalledFor` hashes `progress.elapsedSecs` into its progress signature (packages/nightshade_app/lib/screens/sequencer/widgets/sequence_progress_bar.dart:108-115), and SequenceExecutor._startPerRunTimers runs `Timer.periodic(const Duration(seconds: 1))` writing `elapsedSecs: elapsed` into that same SequenceProgress for the whole run whenever it is not paused (packages/nightshade_core/lib/src/providers/sequence/sequence_executor.dart:908-920). So the signature changes every second and `_lastProgressChangeAt` is reset every second; `quiet` can never reach the threshold. The shipped pin passes only because its `_FixedProgressNotifier` freezes elapsedSecs at 240 for the whole 150 s (sequence_progress_bar_stall_test.dart:26-45). Throwaway test (/tmp/.../scratchpad/refute_stall_test.dart, run in packages/nightshade_app): the identical waveF harness with the one production behaviour restored (elapsedSecs +1 per pumped second, nothing else moving) printed `AFTER 150s WITH A LIVE RUN CLOCK: finish-time widgets=1 stall-rows=0` -- the finish time is still promised and no 'no progress for' row ever appears. The RetryScheduled forwarding half of WF-STOP-N4 does hold (meridian_flip_executor.rs:1736-1760 forwards only the mid-ladder notice under the stable synthetic id)."
  }
 ],
 "results": [
  {
   "role": "driver-sequencer",
   "verified": [
    "SEQ-18",
    "WF-STOP-N2",
    "WF-STOP-N5-first-half"
   ],
   "failed": [
    {
     "id": "WF-STOP-N1",
     "evidence": "BLOCKED, not refuted. The DB oracle passes: all 24 light frames across 4 runs read exposure_duration 15.0 (min=max=15.0 per node), so the 5.0s first-light-frame symptom did not recur. But the second half of the oracle failed - no meridian flip could be made to fire, so the camera-claim mechanism was never exercised. Run 15 (mount parked at RA 0h, target 10min past meridian) logs a NEW honest warning before every frame: 'Not holding the next 15s exposure for a meridian flip: the target is +0.17h past the meridian but the mount reports hour angle -2.30h, so the flip trigger cannot fire.' - proving the gate keys on the MOUNT's HA, not the target's (Wave F's flip fired only because its mount sat at RA 0h with LST ~07h). Run 17, with the mount slewed to and TRACKING the target 19-24 min past the meridian and enable_meridian_flip=true / minutes_past=5.0 / max_retries=3, captured 8/8 frames back-to-back with zero flips, zero waits, and no warning after frame 1 ('the mount has not reported a position'); its Session Report reads 'Meridian flips 0'. Cannot separate app defect from a simulator that never reports a pier side needing a flip."
    },
    {
     "id": "WF-N4/WF-STOP-N3",
     "evidence": "One operator Stop at 01:54:26 raised TWO toasts, not one, and the second is a regression: a RED 'Sequence Error / Sequence cancelled' error toast beside the info 'Sequence stopped / Sequence stopped by request at 01:54.' (shots/waveG-sequencer/g15-toasts.png, cropped from g15-stopclick.png ~2s after the click). Wave F recorded the red error toast as GONE ('no red, no Critical, no failed, no aborted'); it is back. The router's per-transport content dedupe evidently collapsed the three identical INFO notices into one, but a differently-worded error-classified producer now shows through beside it. RECENT EVENTS still triplicates: three 'Sequence stopped' rows all at 01:54:26 plus 'Decision logged - system_event #15: Sequence cancelled' (g19-events.png) - the feed count is unchanged from Wave F. All feed rows are INFO level so the feed itself does not cry wolf."
    },
    {
     "id": "WF-STOP-N4",
     "evidence": "NOT REACHABLE, skipped as the charter permits. The stall string 'no progress for ' is present in the shipped lib/libapp.so and the retry-surfacing synthetic node id 'trigger:meridian_flip' is present in lib/libnightshade_bridge.so, so both halves shipped. But the simulator produced no meridian flip at all in any of four runs (see WF-STOP-N1), so no run ever stalled in a retry ladder and nothing could be observed claiming Running with a past ETA."
    }
   ],
   "notes": "Report: /home/scdouglas/Documents/Nightshade2/reports/release-pass/gui/waveG-sequencer.md; shots in reports/release-pass/gui/shots/waveG-sequencer/ (21 files). Nothing was fixed.\n\nBUNDLE FRESHNESS PROVEN: lib/libapp.so and lib/libnightshade_bridge.so both mtime Aug 14 01:44, after the three F-fix commits (df728a6ce/16bbe47c0/482f3e247, all 01:42). String-grep finds every string the F-fix introduced: 'Sequence stopped by request at ${time.clock}.', 'no progress for ', 'trigger:meridian_flip'.\n\nHARNESS: drive_linux.py (profile after subcommand), display :86, profile waveG-sequencer, seeded from waveE-stop-pipeline with runs/images/checkpoints cleared and the observer site moved to lat +30.0 / lon -85.3 so the drive ran in real astronomical night (Sun -45 deg, LST 21:36 at start). That defeats the daylight gate that blocked Wave F's second run - reusable recipe. Camera/Mount/Focuser connected, mount unparked and tracking. Four runs: 15 (stopped by operator, 7 frames), 16 and 17 (completed 8/8), 18 (completed 1/1).\n\nSEQ-18 detail: node 1 reads '4 / 4 frames' with four filled boxes while node 2 reads '0 / 4 frames' with four empty boxes on the SAME screen mid-run (g13/g14); after the Stop node 1 still reads 4/4 (g22); after a fully completed run both read 4/4 (g30/g33). Charter deviation: node 2 had captured 3 frames by the time the Stop landed, so the post-stop pair is 4/4 vs 3/4 - the 4/4 vs 0/4 pair is the mid-run screenshot.\n\nWF-STOP-N2 detail: agreement checked on FIVE surfaces for all four runs - Session Report, Session Report target row, Execution History row, stats_json.integrationSecs, and sum(captured_images.exposure_duration) - plus the Dashboard Last-night card and the Continue-Session dialog after a restart. Run 15: 1m 45s / 1m 45s / 1m 45s / 105.0 / 105.0. LIMITATION stated in the report: in the simulator the camera's reported exposure always equals the command, so planned == recorded and this drive cannot discriminate 'frames x plan' from 'sum of rows' - it only proves the surfaces agree.\n\nTWO NEW OBSERVATIONS worth adjudicating (recorded, not filed as verdicts):\n1. Run 18's target had 3 children; after the first the executor logged 'Skipping remaining children in node 092bc477 due to next-target request' and 'Child Target completed with status: Skipped', yet the Session Report title reads 'New Sequence - Completed' with nothing anywhere disclosing that two instructions were skipped. Same cry-wolf class this campaign exists to remove.\n2. The node exposure tally is stale between runs after a builder edit: changing a node's count to 1 leaves the previous run's '4 / 4 frames' and filled boxes on a card now configured for 1 frame, until the next run start clears it. Consistent with the deliberate 'cleared at run START' rule but the card then describes a plan it no longer matches.\n\nAlso confirmed working (Wave F did not have these): the meridian-gate warning, the guider dither warning, the FailOpen safety warning, and the pre-flight 'Mount Is Not Pointing At New Target' rule which correctly disappeared once a Slew to Target node was added."
  },
  {
   "role": "driver-chrome",
   "verified": [
    "WD-EQ-2a",
    "CON-56",
    "CON-62",
    "WE-EQ-N5",
    "WF-EQ-N2",
    "WF-EQ-N3",
    "D-2 / WF-SS-N3",
    "WF-SN-N4 / WF-SCI-N3",
    "WF-SCI-N1",
    "WF-SCI-N2",
    "WF-SCI-N4"
   ],
   "failed": [
    {
     "id": "WD-EQ-3",
     "evidence": "Equipment -> Discovery -> Connect 'Built-in Multi-Star Guider' (one click) still raises TWO 'Guider Error' toasts that differ only by a trailing full stop, with no (x2) count: '...requires an active profile with a guide focal length' and '...guide focal length.' Reproduced twice ~14 min apart (shots waveG-01/02/03). The router's normalized-key dedupe (strip trailing punctuation) is not collapsing the exact pair it was written for. Trap: a screenshot 3 s after the click shows only ONE card because the second producer lands between 3 s and 5 s -- a short-wait check reads as fixed."
    },
    {
     "id": "WF-EQ-N1",
     "evidence": "resize 1000 800 -> Sequencer. Sequences IS fixed: 'Sequence Library' renders in full and the search box is full width (waveG-08). Templates is NOT: still 'Sequence T...' at the same rendered font size as the Sequences title beside it, so the shrink-to-16px-floor never reaches this caller, and BOTH subtitles are still cut -- 'Start with a temp...' and 'Browse and load your s...' (waveG-07). The a11y names do carry the full strings ('panel: Sequence Templates\\nStart with a template or save your sequences for reuse.'), so the mitigation is live but the visible CON-52 sentence is still destroyed at a supported desktop width."
    },
    {
     "id": "WE-SP-5",
     "evidence": "Not verifiable as specified: the Moon card and the Smart Night prompt never coexist on this build, so the CockpitStandby reserve was never exercised. Standby briefing (0 devices): tree grep -ci moonrise = 1, grep -ci 'build smart night' = 0 (waveG-14). With devices connected the prompt appears ('Plan tonight automatically' / 'Skip this step' / 'Build Smart Night') and the dashboard switches to the connected layout: moonrise = 0, prompt = 2 -- checked at 1600x900 and again 20 s later, counts unchanged. smart_night_prompt_card.dart gates the prompt on equipmentReady && readyGraceElapsed && profile != null && location != null, and the Moon card lives in the no-equipment branch. RESIDUAL of the same class found instead: connected dashboard at 1000x800 scrolled to the hard bottom (75 notches; last 25 moved nothing, two captures pixel-identical bar the clock) -- the prompt card occupies x 372-851 / y 573-709 and paints over the RECENT EVENTS rows at y 588 and y 641, cutting 'Equipment Conne...' mid-word (waveG-13)."
    }
   ],
   "notes": "Report written to /home/scdouglas/Documents/Nightshade2/reports/release-pass/gui/waveG-chrome.md; 15 evidence shots at reports/release-pass/gui/shots/waveG-*.png. Nothing was fixed. Profile stopped cleanly.\n\nBundle driven was the F-fix build: lib/libapp.so and libnightshade_bridge.so both Aug 14 01:44, app launched 01:45. Setup: onboarding skipped, site 40.05/-75.1 (LST --:-- -> 22:17), captures dir set, 4 sim devices connected, TWO real sequencer runs driven (1s x3 then 1s x6) so 'most recent night' had a wrong answer available.\n\nHARNESS CORRECTION the charter's item 4 wording depends on: `tree` can NEVER print [DISABLED] for a Flutter button -- it prints the flag only when a node is interactive and carries neither `enabled` nor `sensitive`, and a disabled Flutter button publishes ['sensitive','showing','visible']. I therefore probed AT-SPI directly and, crucially, probed the ACTION list, which is what the F-fix changed. Disabled pre-flight Start: states=['sensitive','showing','visible'], actions=[], name='Start Sequence -- unavailable: fix the 1 pre-flight error above first'. Disabled Pause under the built-in guider: same signature, name carries the PHD2-only reason. Control experiments in the same dialogs: Re-check and (warnings-only) Start Anyway both give ['enabled','focusable','sensitive','showing','visible'] + actions=['Tap','Focus'], and the same Start button round-trips between the two signatures. Reusable probe left at /tmp/ns-audit/probe_actions.py (name substring + optional pid).\n\nD-2 settled by counting rather than by looking: `tree --all | grep -c 'Projection: Stereographic'` = 1 never-hovered, 1 during a 3 s hover, 1 with the pointer parked away for 8 s. The single occurrence is the BUTTON's accessible name (the fix put the name on the Icon and emptied the Material tooltip message, so no tooltip node is ever created) -- Wave F's 0-then-1-forever signature is gone in both directions.\n\nWF-SCI-N2 was settled against the database, not the screen: Refresh on session 1 wrote a NEW night_reports row (id 2, +47 s) where Wave F's decisive negative was that the table stayed at one row; and session 2 (6 subs) reads 70/100 'Rough night: every sub was graded poor.' while session 1 still reads 'Not graded -- only 3 light frames...'. Disclosed as NOT exercised: the 'stored report predates a sub it claims to judge' arm, because adding subs to an already-reported session is not reachable from the GUI (a re-run opens a new session).\n\nSAME-CLASS FINDING, different surface, not double-filed: Analytics -> History -> session card -- the icon-row tooltips 'Review & Integrate' and 'Session Report' stay in the a11y tree after the pointer leaves and accumulate as the pointer sweeps the row. That is D-2's shape on a surface D-2's fix did not touch.\n\nTwo more notes for whoever drives next: (1) wait 5 s before judging a toast stack (see WD-EQ-3); (2) 'the dashboard scrolled to the bottom' is ambiguous -- standby has two content branches (briefing with the Moon card when nothing is connected, connected-idle with Mount/Safety/Recent events otherwise) and a finding has to name which."
  },
  {
   "role": "refuter",
   "verified": [
    "router-dedupe-key-normalization (ffix-stop-toasts-scheduler, WF-N4/WF-STOP-N3/WD-EQ-3 recipe)",
    "nightshade-button-disabled-semantics (ffix-sky-science-polish, WF-SN-N4/WF-SCI-N3)"
   ],
   "failed": [
    {
     "id": "SEQ-18-provider-tally (ffix-exposure-integrity, \"fifth look\": node_exposure_tally.dart)",
     "evidence": "REFUTED by wire shape. The tally reads `final json = event.data['detail_json']; final map = json is Map ? json : const {};` (packages/nightshade_core/lib/src/providers/sequence/node_exposure_tally.dart:167-168). Production delivers a JSON *String*: native/nightshade_native/bridge/src/api/sequencer/event_translation.rs:174 `serde_json::to_string(detail)` -> bridge/src/event/sequencer.rs:128 `detail_json: String` -> event_mapping.dart:534 `'detail_json': sequencerEvent.detailJson`. Every other consumer decodes it first (event_operations.dart:316 `_decodeStructuredProgressJson` -> jsonDecode at :521). The shipped pin feeds a Map (exposure_card_wave_f_events_test.dart:66-68 `{'frame': frame, 'total': total}`), so it passes on a shape production never sends. Throwaway test (/tmp/.../scratchpad/refute_tally_test.dart, run in packages/nightshade_core): with detail_json = jsonEncode({'frame':N,'total':4}) the tally after four frames printed `WIRE-SHAPE tally after 4 frames: {}` (expected 4, got null); the same events with a Map printed `MAP-SHAPE tally: {node-one: NodeExposureTally(4/4)}`. Consequence, same file: because `ExposureCompleted` carries no node_id on the wire (event_mapping.dart:481-489), `_namedSourceSeen` never becomes true and the id-less fallback is used forever, so replaying the waveF boundary (NodeStarted node2 emitted before node1's last frame) printed `BOUNDARY tally: {node-one: 3/4, node-two: 4/4}` -- the finished node reads 3/4 and the NEXT node is credited with its frame, i.e. the join-by-position mistake the docstring says it avoids. At HEAD the card falls back to the old string parse, so SEQ-18 is unclosed on a real host."
    },
    {
     "id": "camera-claim-serialization incl. the must-NOT-wait list (ffix-exposure-integrity, WF-STOP-N1)",
     "evidence": "The LIST holds: `cargo test -p nightshade_sequencer every_camera_driving_trigger_action_waits_for_the_frame_in_flight` -> `test result: ok. 1 passed` at HEAD, and camera_driving_trigger_action (executor/trigger_context.rs:427-435) returns None for Pause/ParkAndAbort/Dither. The stated INVARIANT is refuted: 'The flip and recenter arms hand the camera back ... including on the paths where they do not run at all, so the capture loop never waits out the ten-minute expiry.' The claim is now taken unconditionally above the action match (executor/start.rs:3403-3414) for RecoveryAction::Autofocus, but the Autofocus arm releases it in exactly one place -- start.rs:3786, inside the `(Some(_), Some(_))` camera+focuser branch. The sibling `_ =>` branch (start.rs:3954-4014), reached when the camera or focuser id is absent, logs 'skipping the refocus and continuing the run' and falls through with no `clear_camera_busy`. There are only five release sites in the crate (grep clear_camera_busy: start.rs:3786/4106/4309/4463/4500, plus expose.rs:455/479) and none of them run on that path, so the hold expires only at TRIGGER_CAMERA_CLAIM_SECS = 600 s (trigger_context.rs), during which instructions/expose.rs:412-434 blocks every frame. The arm's own comment records this as a live-reproduced scenario ('a rig with the camera + wheel connected and no focuser ... backstop for a device that disappears mid-run'). Evidence is line-anchored static control flow, not executed: the leak is inside a private closure in start.rs and could not be exercised without a repo edit."
    },
    {
     "id": "WF-STOP-N2-integration-source-swap (ffix-exposure-integrity)",
     "evidence": "Holds for TakeExposure nodes; refuted as stated ('Every surface now sums the same number the row does' / 'this also fixes adaptive/smart exposure'). Only ONE producer stamps the new field: node/instructions/expose.rs:255 `upd.frame_exposure_secs = Some(recorded_secs)` (grep frame_exposure_secs across the sequencer crate returns node/progress.rs:708/750/773, expose.rs:255, start.rs:1029/1037 and nothing else). SmartExposure's own producer sets current_frame/total_frames but no recorded seconds (node/instructions/smart_exposure.rs:605-609), and it keeps a second, plan-based accumulator (`total_exposure_time = duration_secs * f64::from(total_count)` into context.completed_integration_secs, node/instructions/expose.rs:267-271; `secs_just_added = plan.duration_secs * frames_just_taken`, smart_exposure.rs:362). Worse for the 'every surface' claim: the ExposureStarted/ExposureCompleted pair is built inside `if let Some((planned_secs, filter)) = metadata` (executor/start.rs:1047-1088) and `exposure_node_metadata` is populated only from `NodeType::TakeExposure` (start.rs:353-367; the code comment at start.rs:868-870 says so outright). A Smart Exposure node therefore emits no ExposureCompleted at all, and the Dart run vitals are built from that event (sequence_executor/event_operations.dart:157-195 `_recordRunFrame` + `recordCompletedFrameIntegration`), so a smart-exposure run still reports frames/integration that the captured_images row sum contradicts. The checkpoint half of the claim does hold (executor/checkpoint.rs:149 copies progress.completed_integration_secs)."
    },
    {
     "id": "WF-STOP-N4-stall-detector (ffix-exposure-integrity, the ETA half)",
     "evidence": "REFUTED empirically. `_stalledFor` hashes `progress.elapsedSecs` into its progress signature (packages/nightshade_app/lib/screens/sequencer/widgets/sequence_progress_bar.dart:108-115), and SequenceExecutor._startPerRunTimers runs `Timer.periodic(const Duration(seconds: 1))` writing `elapsedSecs: elapsed` into that same SequenceProgress for the whole run whenever it is not paused (packages/nightshade_core/lib/src/providers/sequence/sequence_executor.dart:908-920). So the signature changes every second and `_lastProgressChangeAt` is reset every second; `quiet` can never reach the threshold. The shipped pin passes only because its `_FixedProgressNotifier` freezes elapsedSecs at 240 for the whole 150 s (sequence_progress_bar_stall_test.dart:26-45). Throwaway test (/tmp/.../scratchpad/refute_stall_test.dart, run in packages/nightshade_app): the identical waveF harness with the one production behaviour restored (elapsedSecs +1 per pumped second, nothing else moving) printed `AFTER 150s WITH A LIVE RUN CLOCK: finish-time widgets=1 stall-rows=0` -- the finish time is still promised and no 'no progress for' row ever appears. The RetryScheduled forwarding half of WF-STOP-N4 does hold (meridian_flip_executor.rs:1736-1760 forwards only the mid-ladder notice under the stable synthetic id)."
    }
   ],
   "notes": "Method: one graphify query, then targeted reads at HEAD (482f3e247, clean tree, no repo writes). Throwaway tests written only under the session scratchpad and run with `flutter test <abs path>` from inside the owning package; one warm `cargo test` filter run for the shipped camera-claim pin. Files: refute_button_test.dart, refute_tally_test.dart, refute_router_test.dart (all green/instructive), refute_stall_test.dart (refutation).\n\nWhat held, and what I threw at it:\n- Router dedupe key normalization: `?` preserved (`QUESTION-vs-STATEMENT sent=2`), per-transport independence with a trailing-`.` variant collapsed (`PER-TRANSPORT inApp=1 hook=1`), window edge sane (`WINDOW-EDGE(6s) sent=2`). Cost worth recording, not a refutation: the trailing-cosmetics strip also merges an in-progress notice with its completion \u2014 `Parking the mount\u2026` then `Parking the mount.` sent once (`ELLIPSIS-vs-PERIOD sent=[Parking the mount\u2026]`). I found no live producer pair of that shape, so I did not file it.\n- NightshadeButton: the pin only drives `onPressed: null`; the second disabled state (`isLoading: true` with a live onPressed) is equally clean \u2014 `LOADING actions=0 isEnabled=false hasEnabledState=true`, control `ENABLED actions=4194305`, tap fires once. `onTapDown` appears nowhere else in nightshade_ui except guide_star_view.dart, which already guards it.\n\nScope gap noticed while working the router item, offered as an observation rather than a refutation of the sampled claim: ffix-stop-toasts-scheduler frames WF-N4/WF-STOP-N3 as \"three identical toasts and three feed rows\" and root-causes both in the router. The router cannot reach the feed \u2014 InAppTransport writes only UiNotificationNotifier (transports/in_app_transport.dart), while RECENT EVENTS is built from eventHistoryProvider through its own collapseRepeatedEvents (run_dashboard_providers.dart:651-660). Only the `SequencerEvent_Error(\"Sequence cancelled\")` producer is canonicalised into one row (isOperatorStopNotice, :555-583); the `Stopped` lifecycle row and the \"Decision logged \u2026 Sequence cancelled\" row the same report discloses as out of scope are separate rows that collapseRepeatedEvents cannot merge (it requires equal title AND message). Someone should re-drive a Stop and count the rows before that half is called closed.\n\nTwo of the four refutations are the campaign's recurring shape \u2014 the test asserts a data shape production never produces (SEQ-18's Map-vs-String detail_json) or freezes a field production advances every second (WF-STOP-N4's elapsedSecs). Both fixes are invisible below the UI, so a G spot-check on a rebuilt bundle would not have caught either without reading the provider state directly."
  }
 ]
}