# Sequencer Ledger campaign — closeout (2026-09-13)

Branch `feature/sequencer-ledger`, based on `308d65c2a` (tip of `codex/observatory-finish`
at campaign start). Spec: `docs/design/sequencer_ledger_spec.md`. Direction chosen by the
owner from the "Sequencer Night Overview" design canvas: **A · Ledger + all of F (sticky
ancestors, run-length folding, collapse summaries + gutter map)**, plus a subtle animation
pass and any features worth adding along the way.

## What shipped

| Area | Delivered | Where |
|---|---|---|
| Density modes | Comfortable / Compact / Ledger, persisted, Ledger default on desktop, three-tier canvas-bar control, canvas-clamped fallback to Compact below the column floor | `widgets/sequencer_density.dart`, `sequence_toolbar.dart` |
| Ledger rows | 28 px rows, depth guides, aligned Filter/exp · Count · Duration · ETA columns with a header, 2 px progress bar, hover actions in a reserved slot, full parity with the card row (selection, context menu, drag, drop, shortcuts, tutorial keys, semantics) | `sequence_tree/ledger_row.dart`, `ledger_columns.dart` |
| ETA column | pre-run re-anchored to the minute; during a run anchored on the run start, lateness of the executing node carried into the pending tail, observed starts from `SequencerEvent_NodeStarted` shown for started nodes and kept until the next run | `ledger_columns.dart` |
| Rollup summaries | one-line description of a collapsed container (filter runs, targets, trigger groups, generic), shown in Ledger and Compact | `sequence_tree/rollup_summary.dart` |
| Inspector tabs | Settings / Activity / Notes; Activity hosts the progress panel, frame grid (with the frame-landed pop) and thumbnails; Notes edits the node comment (+ target notes); last-known activity persists across the session and clears on a new run | `node_properties_panel.dart`, `node_activity_tab.dart`, `node_notes_tab.dart`, `node_progress_panels/last_known_node_progress.dart` |
| Auto-collapse | on run start and on each node change, finished containers fold and the executing branch opens; hand-opened unfinished containers are left alone; off when Follow execution is off | `sequence_tree/auto_collapse.dart` |
| Sticky ancestors | up to three pinned ancestor rows, viewport inset so pins never occlude, tap-to-scroll, excluded from a11y, pinned as soon as the row top passes the stack (monotonic) | `sequence_tree/sticky_ancestors.dart` |
| Gutter map | always-on 34 px overview in Ledger with a draggable viewport rect, shared painter with the 80 px strip, slider semantics with page actions | `sequence_tree/gutter_map.dart`, `sequence_minimap.dart` |
| Run-length folding | pure model (`foldChildren`, stable group ids) + folded rows for filter runs and the acquire quartet; tap multi-selects members, drag moves the block in one undo step, delete-all with one prompt, keyboard treats a fold as one row, order/minimap/finder read the resolved density | `sequence_fold_model.dart`, `sequence_fold_state.dart`, `sequence_tree/ledger_fold_row.dart`, `fold_group_actions.dart` |
| Animations | expand/collapse (`SizeTransition` + fade-slide), density crossfade, running-row breath (gate inside a `RepaintBoundary`), hover, drop zones, pin slide/fade, selection colour, follow scroll, completion settle; every duration tokenised and zero under `disableAnimations` through one helper | `nightshade_ui/src/utils/animation_duration.dart`, tree parts |
| Inherited failures | eight base-branch test failures fixed (four product defects, four stale tests); the two framing goldens are Windows-captured by policy and stay owed to a Windows re-baseline | `reports/sequencer-ledger/w4-inherited-failures-report.md` |
| CI gates | format clean (eleven drifted files formatted), behavioral gate registered honestly with three product honesty fixes, placeholder and analyzer gates green | `w5-gates-report.md` |

## How it was built and verified

Seven workstreams, each with a written brief, an implementation agent, an adversarial
review by a separate agent, a fix pass, and an independent rerun of the sequencer suite on
the merged branch before the next wave started. Reviews caught, before merge: stale-run
snapshots reported as current, a build-phase provider write, hover actions missing from the
default density, an ETA column anchored at build time, a group-move algorithm that scattered
runs on same-parent moves, a sticky stack occluding the rows it pointed at, an auto-collapse
shield that Expand-all disabled for the night, and an ETA re-anchor that measured elapsed
time instead of lateness. Per-workstream reports are beside this file.

Live verification in the release bundle through `tools/ui_audit/drive_linux.py` with the
two fixtures now under `tools/ui_audit/fixtures/`: the 36-node two-target night renders in
one screen in Ledger, folds and rollups read as designed, sticky pins and the gutter track
scrolling, all three densities switch, the inspector tabs work, and a one-minute simulator
run showed auto-collapse, running chips on target / loop / folded row with per-member
progress, and every finished container folded at completion.

## Numbers (final, on the campaign branch)

| Check | Result |
|---|---|
| `flutter test test/screens/sequencer` | 721 pass (507 on the base commit) |
| `flutter test test/screens` | green apart from the two Windows-captured framing goldens (excluded by the CI tag) |
| `flutter test test/widgets` | 303 pass (299/4 on the base commit) |
| `melos run test` (all packages, golden tag excluded) | 8 of 10 packages green; `nightshade_app` and `nightshade_core` each had one test fail under the full parallel run (`log_export_target_test` "on-screen scope", `integrity_check_test` "hot rollback journal") — both pass in isolation on the base commit and on this commit, so they are order-sensitive, not regressions; see Owed |
| `dart analyze` (nightshade_app) | 890 issues, all pre-existing infos/warnings outside the campaign; 0 errors; 891 on the base commit |
| Format check (`packages lib`) | clean (eleven base-branch drift files formatted) |
| Placeholder gate | no new high-risk markers |
| Behavioral gate | 0 unregistered, 0 open (61 findings adjudicated: 29 files registered with file-specific reasons, 3 product fixes) |
| Analyzer rollup gate | 0 critical warnings |
| Live (release bundle, GUI harness) | full night in one screen; folding, rollups, sticky pins, gutter, three densities, inspector tabs; one-minute simulator run with auto-collapse, running chips, per-member fold progress, completion folding |

## Owed / not done here

- Two tests are order-sensitive under the full `melos run test` (they pass alone): `log_export_target_test` reads an empty file where it expected the on-screen entries, and `integrity_check_test`'s hot-journal replay case; both predate this branch and want an isolated temp path per test.

- Windows re-baseline of `framing_hips_layer_wiring` and `framing_registration` goldens.
- On-sky run on the owner's rig (the simulator run above is the closest stand-in).
- `target_header_card.dart` selection animation in Comfortable/Compact (flagged by W3).
- Mobile editor untouched by design.

## Addendum — same day, after the owner tried the build

Owner findings from the preview, each reproduced live before fixing (reports `w6-*`, `w7-*`, `w8-*`, `w9-*`, `w10-*`, `w11-*`, `w12-*`):

| Finding | Root cause | Fix |
|---|---|---|
| "Queue says empty" after loading a sequence | the panel was the planetarium wishlist only | tab renamed **Targets**, "In this sequence" section with tap-to-reveal, wishlist kept as "Saved for later" |
| Screen bounces left when switching inspector tabs | `Scrollable.ensureVisible` climbs into the screen's tab pager | shared tab bar scrolls its own position; all sequencer row reveals go through one tree-scoped helper |
| "Show the map" does nothing in Ledger | toggle governed the 80 px strip only | per-density overview preference; toggles the gutter in Ledger |
| Imaging text boxes not centred | digits' cap-height ink vs em box; suffix raised | standard visual density, suffix on the value baseline; measured to ±0.5 px |
| Imaging Mount sub-tab labels truncated | fixed two-column rows in a 244 px panel | measured `AdaptiveColumns`, sentence case, shared status chip; same fix in Camera/Focus/Guiding |
| Detect location needs GPS | deslop gated the internet tier off | one-click platform → Wi-Fi scan (beaconDB / optional Google key) → IP, with the accuracy radius shown; beaconDB has no coverage at the owner's site, hence the Google key field |
| App "chugging" | NVIDIA driver/kernel mismatch → Mesa llvmpipe software raster (34.7 of 40 idle CPU points) | reboot into kernel 7.2.4 is the fix; status bar and dashboard rebuild scoping landed anyway |
| 18 known failing tests in the owner's tree | greedy `NightshadeButton` width in start-aligned columns; text-field `LayoutBuilder` breaking dialog intrinsics; a DepthLock `toJson` emitting a Dart object; stale strings | 40 failures fixed at the root; gates, format, analyzer warnings to zero |

Final preview commit `e5fa164fe` (+ register docs `f279906a5`): every package suite green with the golden tag excluded (app 4387, core 6512, ui 530, planetarium 612, bridge 63, desktop 1274), format clean, placeholder / behavioral / analyzer gates green. Built on the imaging laptop (DESKTOP-MMAJGAK) and installed into `%LOCALAPPDATA%\Programs\Nightshade` at 19:39 local with the previous install backed up to `C:\src\nightshade-prev-20260913-193929`.

**Branch relationship for the owner.** `preview/ledger-plus-owner-tree` = a snapshot commit of the owner's uncommitted working tree (2026-09-13) + `feature/sequencer-ledger` + everything above. To adopt it: commit the working tree as-is (`git add -A && git commit`) and `git merge preview/ledger-plus-owner-tree` — both sides carry identical changes for the snapshot files, so the merge is clean.

Still owed: on-sky tonight (the first real run of all of this); reboot of the dev box for the GPU driver; RAM test for the btrfs fresh-write corruption (recurred today); Windows re-baseline of the two framing goldens and the planetarium perceptual baselines; `NightshadeDropdown` shares the text field's old `LayoutBuilder` trap (latent; fix written up in `w12-known-issues-report.md`).
