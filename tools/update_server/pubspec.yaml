name: nightshade_update_server
description: Self-hosted update server for Nightshade OTA updates
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  path: ^1.8.3
