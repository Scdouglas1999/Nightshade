// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.11.1.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';
import 'package:freezed_annotation/freezed_annotation.dart' hide protected;
part 'depthlock_events.freezed.dart';

@freezed
sealed class DepthLockEvent with _$DepthLockEvent {
  const DepthLockEvent._();

  /// A goal revision's committed analysis changed.
  const factory DepthLockEvent.goalUpdated({
    required String goalId,
    required BigInt revision,
    required String filterName,

    /// `insufficientEvidence` | `collecting` | `confirmationPending` |
    /// `achieved` | `unreliable`.
    required String state,
    double? score,
    double? conservativeScore,
    required double threshold,
    double? uncertaintyAdu,
    required double coverage,
    required int evidenceFrames,
    required int confirmationFrames,
    required String reason,
    required bool automaticCompletion,

    /// Exposures still needed (threshold plus confirmation), when the
    /// noise model can say; `None` before anything is measurable or
    /// when the goal is unreachable at its floor.
    int? framesRemaining,
    required bool reachable,
  }) = DepthLockEvent_GoalUpdated;

  /// A saved frame could not become evidence for a goal. The reason is
  /// the ingestion layer's own explanation, written for the operator.
  const factory DepthLockEvent.evidenceRejected({
    required String goalId,
    required BigInt revision,
    required String sourcePath,
    required String reason,
  }) = DepthLockEvent_EvidenceRejected;

  /// Analysis work was shed because the bounded queue was full. Raw
  /// saving and safety are unaffected; the frame stays on disk and can be
  /// re-ingested.
  const factory DepthLockEvent.analysisDropped({
    required String sourcePath,
    required String reason,
  }) = DepthLockEvent_AnalysisDropped;

  /// A goal was created, revised, removed or had its preferences changed
  /// (`change` names which).
  const factory DepthLockEvent.goalChanged({
    required String goalId,
    required BigInt revision,
    required String change,
  }) = DepthLockEvent_GoalChanged;
}
