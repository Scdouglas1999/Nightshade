/// Compatibility shim: the Rust `device_capabilities.rs` monolith became the
/// `device_capabilities/` module tree; the generated types live under it.
library;

export 'device_capabilities/types.dart';
