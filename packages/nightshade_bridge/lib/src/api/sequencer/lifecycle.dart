// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.11.1.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../../error.dart';
import '../../frb_generated.dart';
import '../sequencer.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These functions are ignored because they are not marked as `pub`: `seed_executor_site_from_settings`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `clone`, `fmt`

/// Load a sequence from JSON
Future<void> apiSequencerLoadJson({required String json}) => RustLib
    .instance
    .api
    .crateApiSequencerLifecycleApiSequencerLoadJson(json: json);

/// Load a sequence from a definition struct
Future<void> apiSequencerLoad({required SequenceDefinitionApi definition}) =>
    RustLib.instance.api.crateApiSequencerLifecycleApiSequencerLoad(
      definition: definition,
    );

/// Start the sequence executor
Future<void> apiSequencerStart() =>
    RustLib.instance.api.crateApiSequencerLifecycleApiSequencerStart();

/// Pause the sequence executor
Future<void> apiSequencerPause() =>
    RustLib.instance.api.crateApiSequencerLifecycleApiSequencerPause();

/// Resume the sequence executor
Future<void> apiSequencerResume() =>
    RustLib.instance.api.crateApiSequencerLifecycleApiSequencerResume();

/// Stop the sequence executor. `origin` names the CALLER — `None` /
/// `"operator"` for a human, `"scheduler"` for the autopilot — so the
/// executor can record an autopilot stop as the system event it is
/// instead of operator evidence.
Future<void> apiSequencerStop({String? origin}) => RustLib.instance.api
    .crateApiSequencerLifecycleApiSequencerStop(origin: origin);

/// Skip to the next instruction
Future<void> apiSequencerSkip() =>
    RustLib.instance.api.crateApiSequencerLifecycleApiSequencerSkip();

/// Jump execution to a specific node id, marking preceding siblings as
/// Skipped. Honoured on the next container's
/// tree-walk step; the currently-running instruction (e.g. an exposure burst)
/// completes before the jump takes effect. Returns an error if the executor
/// is not running (caller should gate the UI button on execution state).
Future<void> apiSequencerSkipToNode({required String nodeId}) => RustLib
    .instance
    .api
    .crateApiSequencerLifecycleApiSequencerSkipToNode(nodeId: nodeId);

/// Reset the sequence executor
Future<void> apiSequencerReset() =>
    RustLib.instance.api.crateApiSequencerLifecycleApiSequencerReset();

/// Dart side reports the verdict of a plugin-dispatched
/// node back to the Rust executor. Routes to
/// `ExecutorCommand::PluginNodeFinished`. The Rust instruction node
/// awaiting on the matching pending oneshot unblocks with Success or
/// Failure based on `success`.
///
/// `structured_detail_json` is an optional opaque JSON payload the plugin
/// author emits as the node's final progress event. Invalid JSON is
/// logged at warn and dropped (the verdict still applies).
///
/// Returns an error if the executor is not currently running — the
/// caller should treat that as a stale reply (the run was cancelled
/// between dispatch and reply) and drop the result.
Future<void> apiSequencerPluginNodeFinished({
  required String nodeId,
  required bool success,
  String? message,
  String? structuredDetailJson,
}) => RustLib.instance.api
    .crateApiSequencerLifecycleApiSequencerPluginNodeFinished(
      nodeId: nodeId,
      success: success,
      message: message,
      structuredDetailJson: structuredDetailJson,
    );

/// Get the current sequencer state
Future<SequencerState> apiSequencerGetState() =>
    RustLib.instance.api.crateApiSequencerLifecycleApiSequencerGetState();

/// Set the checkpoint directory for crash recovery
Future<void> apiSequencerSetCheckpointDir({required String path}) => RustLib
    .instance
    .api
    .crateApiSequencerLifecycleApiSequencerSetCheckpointDir(path: path);

/// Check if a recoverable checkpoint exists
Future<bool> apiSequencerHasCheckpoint() =>
    RustLib.instance.api.crateApiSequencerLifecycleApiSequencerHasCheckpoint();

/// Get info about the current checkpoint
Future<CheckpointInfoApi?> apiSequencerGetCheckpointInfo() => RustLib
    .instance
    .api
    .crateApiSequencerLifecycleApiSequencerGetCheckpointInfo();

/// Resume sequence from checkpoint
Future<void> apiSequencerResumeFromCheckpoint() => RustLib.instance.api
    .crateApiSequencerLifecycleApiSequencerResumeFromCheckpoint();

/// Standalone meridian flip — runs the canonical [`MeridianFlipExecutor`]
/// OUTSIDE any running sequence.
///
/// This is what the Dart-side standalone meridian monitor calls to flip an
/// attended non-sequencer session instead of only alerting while the mount
/// tracks into the pier. It runs the same engine with the same timeouts,
/// altitude check, re-center and refocus semantics as the in-sequence path.
///
/// Refuses while the sequence executor is Running/Paused/Stopping/
/// Recovering — two engines commanding one mount is how pier crashes
/// happen; the in-sequence trigger owns flips there.
Future<void> apiPerformMeridianFlip({
  required String mountId,
  String? cameraId,
  String? focuserId,
  String? coverCalibratorId,
  required String targetName,
  required double targetRaHours,
  required double targetDecDegrees,
  required bool pauseGuiding,
  required bool autoCenter,
  required bool refocusAfter,
  required bool resumeGuiding,
  required double settleTimeSecs,
}) => RustLib.instance.api.crateApiSequencerLifecycleApiPerformMeridianFlip(
  mountId: mountId,
  cameraId: cameraId,
  focuserId: focuserId,
  coverCalibratorId: coverCalibratorId,
  targetName: targetName,
  targetRaHours: targetRaHours,
  targetDecDegrees: targetDecDegrees,
  pauseGuiding: pauseGuiding,
  autoCenter: autoCenter,
  refocusAfter: refocusAfter,
  resumeGuiding: resumeGuiding,
  settleTimeSecs: settleTimeSecs,
);

/// Save current execution state as checkpoint
Future<void> apiSequencerSaveCheckpoint() =>
    RustLib.instance.api.crateApiSequencerLifecycleApiSequencerSaveCheckpoint();

/// Clear/discard checkpoint (call when sequence completes normally or user discards)
Future<void> apiSequencerClearCheckpoint() => RustLib.instance.api
    .crateApiSequencerLifecycleApiSequencerClearCheckpoint();

/// Checkpoint info returned to Dart
class CheckpointInfoApi {
  final String sequenceName;
  final String timestamp;
  final int completedExposures;
  final double completedIntegrationSecs;
  final bool canResume;
  final PlatformInt64 ageSeconds;

  const CheckpointInfoApi({
    required this.sequenceName,
    required this.timestamp,
    required this.completedExposures,
    required this.completedIntegrationSecs,
    required this.canResume,
    required this.ageSeconds,
  });

  @override
  int get hashCode =>
      sequenceName.hashCode ^
      timestamp.hashCode ^
      completedExposures.hashCode ^
      completedIntegrationSecs.hashCode ^
      canResume.hashCode ^
      ageSeconds.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is CheckpointInfoApi &&
          runtimeType == other.runtimeType &&
          sequenceName == other.sequenceName &&
          timestamp == other.timestamp &&
          completedExposures == other.completedExposures &&
          completedIntegrationSecs == other.completedIntegrationSecs &&
          canResume == other.canResume &&
          ageSeconds == other.ageSeconds;
}
