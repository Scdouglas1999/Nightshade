// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.11.1.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../error.dart';
import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These functions are ignored because they are not marked as `pub`: `failed`, `inspect_reference`, `new_goal_id`, `now_ms`, `service`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`

ApiDepthLockStatus apiDepthlockStatus() =>
    RustLib.instance.api.crateApiDepthlockApiDepthlockStatus();

List<ApiDepthGoal> apiDepthlockListGoals() =>
    RustLib.instance.api.crateApiDepthlockApiDepthlockListGoals();

ApiDepthGoal apiDepthlockGetGoal({required String goalId}) =>
    RustLib.instance.api.crateApiDepthlockApiDepthlockGetGoal(goalId: goalId);

/// Create a goal. `goal_id` may be empty to have one generated. The
/// selection timestamp is now: only exposures started after this call are
/// evidence, so the image the region was drawn on stays discovery data.
ApiDepthGoal apiDepthlockCreateGoal({
  required String goalId,
  required ApiDepthGoalDefinition definition,
}) => RustLib.instance.api.crateApiDepthlockApiDepthlockCreateGoal(
  goalId: goalId,
  definition: definition,
);

/// Replace a goal's definition. This is a new revision: the earlier one is
/// archived with references to its evidence, and evidence starts over.
ApiDepthGoal apiDepthlockReviseGoal({
  required String goalId,
  required BigInt expectedRevision,
  required ApiDepthGoalDefinition definition,
}) => RustLib.instance.api.crateApiDepthlockApiDepthlockReviseGoal(
  goalId: goalId,
  expectedRevision: expectedRevision,
  definition: definition,
);

/// Turn ingestion and automatic completion on or off without disturbing the
/// revision or its evidence.
ApiDepthGoal apiDepthlockSetGoalPreferences({
  required String goalId,
  required BigInt expectedRevision,
  required bool enabled,
  required bool automaticCompletion,
}) => RustLib.instance.api.crateApiDepthlockApiDepthlockSetGoalPreferences(
  goalId: goalId,
  expectedRevision: expectedRevision,
  enabled: enabled,
  automaticCompletion: automaticCompletion,
);

void apiDepthlockRemoveGoal({
  required String goalId,
  required BigInt expectedRevision,
}) => RustLib.instance.api.crateApiDepthlockApiDepthlockRemoveGoal(
  goalId: goalId,
  expectedRevision: expectedRevision,
);

/// Describe a candidate reference frame: its geometry from the header (when
/// it carries an undistorted TAN solution) and its acquisition settings.
Future<ApiDepthReferenceInfo> apiDepthlockInspectReference({
  required String path,
}) => RustLib.instance.api.crateApiDepthlockApiDepthlockInspectReference(
  path: path,
);

/// Suggest the systematic error floor for a goal from its reference light
/// and master frames at the given aperture scale. `pixel_scale_arcsec` is
/// the reference's native scale when the caller resolved the geometry from
/// its own records; omitted, it is read from the file's TAN header.
Future<ApiDepthFloorSuggestion> apiDepthlockSuggestFloor({
  required String referencePath,
  required String darkPath,
  required String flatPath,
  required double scaleArcsec,
  double? pixelScaleArcsec,
}) => RustLib.instance.api.crateApiDepthlockApiDepthlockSuggestFloor(
  referencePath: referencePath,
  darkPath: darkPath,
  flatPath: flatPath,
  scaleArcsec: scaleArcsec,
  pixelScaleArcsec: pixelScaleArcsec,
);

/// Convert a rectangle drawn on the reference image (0-based pixel corners,
/// any order) into the sky rectangle the goal stores. The rectangle keeps
/// the image's pixel axes: width along image x, height along image y,
/// rotation is the position angle of image +y.
ApiSkyRectangle apiDepthlockSkyRectangle({
  required ApiReferenceGeometry reference,
  required double x0,
  required double y0,
  required double x1,
  required double y1,
}) => RustLib.instance.api.crateApiDepthlockApiDepthlockSkyRectangle(
  reference: reference,
  x0: x0,
  y0: y0,
  x1: x1,
  y1: y1,
);

/// Validate a measurement definition without creating anything, so the
/// editor can explain a bad region, scale or background placement as the
/// user adjusts it.
int apiDepthlockCheckMeasurement({required ApiDepthMeasurement measurement}) =>
    RustLib.instance.api.crateApiDepthlockApiDepthlockCheckMeasurement(
      measurement: measurement,
    );

/// Offer one saved light to one goal and wait for the outcome. Archival
/// frames acquired before the goal's selection are reported as
/// `preSelection`, never counted.
Future<ApiDepthIngestOutcome> apiDepthlockIngestFrame({
  required String goalId,
  required String path,
}) => RustLib.instance.api.crateApiDepthlockApiDepthlockIngestFrame(
  goalId: goalId,
  path: path,
);

/// The goal's score after each prefix of its evidence (thinned to about
/// `max_points`), followed by the noise model's projection to the forecast
/// crossing. Display only; nothing here changes a verdict.
Future<List<ApiDepthCurvePoint>> apiDepthlockGoalCurve({
  required String goalId,
  required int maxPoints,
}) => RustLib.instance.api.crateApiDepthlockApiDepthlockGoalCurve(
  goalId: goalId,
  maxPoints: maxPoints,
);

/// Re-evaluate a goal over the evidence it already holds.
Future<ApiDepthGoal> apiDepthlockReplayGoal({required String goalId}) => RustLib
    .instance
    .api
    .crateApiDepthlockApiDepthlockReplayGoal(goalId: goalId);

/// The acquisition every contributing light must match.
class ApiAcquisitionSettings {
  final String instrument;
  final String filter;
  final double exposureSecs;
  final int? gain;
  final int? offset;
  final int binX;
  final int binY;
  final double? ccdTempC;

  const ApiAcquisitionSettings({
    required this.instrument,
    required this.filter,
    required this.exposureSecs,
    this.gain,
    this.offset,
    required this.binX,
    required this.binY,
    this.ccdTempC,
  });

  @override
  int get hashCode =>
      instrument.hashCode ^
      filter.hashCode ^
      exposureSecs.hashCode ^
      gain.hashCode ^
      offset.hashCode ^
      binX.hashCode ^
      binY.hashCode ^
      ccdTempC.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ApiAcquisitionSettings &&
          runtimeType == other.runtimeType &&
          instrument == other.instrument &&
          filter == other.filter &&
          exposureSecs == other.exposureSecs &&
          gain == other.gain &&
          offset == other.offset &&
          binX == other.binX &&
          binY == other.binY &&
          ccdTempC == other.ccdTempC;
}

/// One point of a goal's score history or projection.
class ApiDepthCurvePoint {
  final int frames;
  final double score;
  final double conservativeScore;
  final bool projected;

  const ApiDepthCurvePoint({
    required this.frames,
    required this.score,
    required this.conservativeScore,
    required this.projected,
  });

  @override
  int get hashCode =>
      frames.hashCode ^
      score.hashCode ^
      conservativeScore.hashCode ^
      projected.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ApiDepthCurvePoint &&
          runtimeType == other.runtimeType &&
          frames == other.frames &&
          score == other.score &&
          conservativeScore == other.conservativeScore &&
          projected == other.projected;
}

/// A calibration error floor derived from the goal's masters (see
/// `depthlock::input::suggest_floor`): the masters' own pixel noise,
/// averaged over the aperture. `source` is the derivation in words, ready
/// to be stored as the goal's `systematic_floor_source`.
class ApiDepthFloorSuggestion {
  final double floorAdu;
  final double darkNoiseAdu;
  final double flatRelativeNoise;
  final double skyAdu;
  final double aperturePixels;
  final String source;

  const ApiDepthFloorSuggestion({
    required this.floorAdu,
    required this.darkNoiseAdu,
    required this.flatRelativeNoise,
    required this.skyAdu,
    required this.aperturePixels,
    required this.source,
  });

  @override
  int get hashCode =>
      floorAdu.hashCode ^
      darkNoiseAdu.hashCode ^
      flatRelativeNoise.hashCode ^
      skyAdu.hashCode ^
      aperturePixels.hashCode ^
      source.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ApiDepthFloorSuggestion &&
          runtimeType == other.runtimeType &&
          floorAdu == other.floorAdu &&
          darkNoiseAdu == other.darkNoiseAdu &&
          flatRelativeNoise == other.flatRelativeNoise &&
          skyAdu == other.skyAdu &&
          aperturePixels == other.aperturePixels &&
          source == other.source;
}

/// What the goal is expected to cost, projected from the noise model (see
/// `depthlock::DepthForecast`). Frames, not hours: the host knows the
/// exposure length.
class ApiDepthForecast {
  final int framesToThreshold;
  final int framesToConfirm;
  final bool reachable;
  final double ceilingScore;
  final double perFrameNoiseAdu;
  final double recentFrameNoiseAdu;
  final double bestFrameNoiseAdu;

  const ApiDepthForecast({
    required this.framesToThreshold,
    required this.framesToConfirm,
    required this.reachable,
    required this.ceilingScore,
    required this.perFrameNoiseAdu,
    required this.recentFrameNoiseAdu,
    required this.bestFrameNoiseAdu,
  });

  @override
  int get hashCode =>
      framesToThreshold.hashCode ^
      framesToConfirm.hashCode ^
      reachable.hashCode ^
      ceilingScore.hashCode ^
      perFrameNoiseAdu.hashCode ^
      recentFrameNoiseAdu.hashCode ^
      bestFrameNoiseAdu.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ApiDepthForecast &&
          runtimeType == other.runtimeType &&
          framesToThreshold == other.framesToThreshold &&
          framesToConfirm == other.framesToConfirm &&
          reachable == other.reachable &&
          ceilingScore == other.ceilingScore &&
          perFrameNoiseAdu == other.perFrameNoiseAdu &&
          recentFrameNoiseAdu == other.recentFrameNoiseAdu &&
          bestFrameNoiseAdu == other.bestFrameNoiseAdu;
}

class ApiDepthGoal {
  final String id;
  final BigInt revision;
  final ApiDepthGoalDefinition definition;

  /// When this revision was selected (Unix ms); only later exposures count.
  final PlatformInt64 selectedAtMs;
  final int evidenceFrames;
  final BigInt evidenceRevision;

  /// True when `report` was computed over exactly the current evidence.
  final bool analysisCurrent;
  final ApiDepthReport? report;

  /// Exposures frozen in a provisional candidate awaiting confirmation.
  final int candidateFrames;
  final String? lastIssue;
  final int archivedRevisions;

  /// The estimator version this goal's measurement is defined against.
  final int estimatorVersion;

  const ApiDepthGoal({
    required this.id,
    required this.revision,
    required this.definition,
    required this.selectedAtMs,
    required this.evidenceFrames,
    required this.evidenceRevision,
    required this.analysisCurrent,
    this.report,
    required this.candidateFrames,
    this.lastIssue,
    required this.archivedRevisions,
    required this.estimatorVersion,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      revision.hashCode ^
      definition.hashCode ^
      selectedAtMs.hashCode ^
      evidenceFrames.hashCode ^
      evidenceRevision.hashCode ^
      analysisCurrent.hashCode ^
      report.hashCode ^
      candidateFrames.hashCode ^
      lastIssue.hashCode ^
      archivedRevisions.hashCode ^
      estimatorVersion.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ApiDepthGoal &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          revision == other.revision &&
          definition == other.definition &&
          selectedAtMs == other.selectedAtMs &&
          evidenceFrames == other.evidenceFrames &&
          evidenceRevision == other.evidenceRevision &&
          analysisCurrent == other.analysisCurrent &&
          report == other.report &&
          candidateFrames == other.candidateFrames &&
          lastIssue == other.lastIssue &&
          archivedRevisions == other.archivedRevisions &&
          estimatorVersion == other.estimatorVersion;
}

class ApiDepthGoalDefinition {
  final String label;
  final String projectId;
  final String targetId;
  final String profileId;
  final String filterName;

  /// 0-based filter wheel slot when known.
  final int? filterIndex;
  final String referencePath;
  final ApiReferenceGeometry reference;
  final ApiAcquisitionSettings acquisition;

  /// Accepted `CCD-TEMP` difference in °C when both sides carry it.
  final double temperatureToleranceC;
  final String darkPath;
  final String flatPath;
  final ApiDepthMeasurement measurement;
  final bool enabled;
  final bool automaticCompletion;

  const ApiDepthGoalDefinition({
    required this.label,
    required this.projectId,
    required this.targetId,
    required this.profileId,
    required this.filterName,
    this.filterIndex,
    required this.referencePath,
    required this.reference,
    required this.acquisition,
    required this.temperatureToleranceC,
    required this.darkPath,
    required this.flatPath,
    required this.measurement,
    required this.enabled,
    required this.automaticCompletion,
  });

  @override
  int get hashCode =>
      label.hashCode ^
      projectId.hashCode ^
      targetId.hashCode ^
      profileId.hashCode ^
      filterName.hashCode ^
      filterIndex.hashCode ^
      referencePath.hashCode ^
      reference.hashCode ^
      acquisition.hashCode ^
      temperatureToleranceC.hashCode ^
      darkPath.hashCode ^
      flatPath.hashCode ^
      measurement.hashCode ^
      enabled.hashCode ^
      automaticCompletion.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ApiDepthGoalDefinition &&
          runtimeType == other.runtimeType &&
          label == other.label &&
          projectId == other.projectId &&
          targetId == other.targetId &&
          profileId == other.profileId &&
          filterName == other.filterName &&
          filterIndex == other.filterIndex &&
          referencePath == other.referencePath &&
          reference == other.reference &&
          acquisition == other.acquisition &&
          temperatureToleranceC == other.temperatureToleranceC &&
          darkPath == other.darkPath &&
          flatPath == other.flatPath &&
          measurement == other.measurement &&
          enabled == other.enabled &&
          automaticCompletion == other.automaticCompletion;
}

/// `outcome` is one of `added`, `duplicate`, `alreadyAchieved`,
/// `preSelection`, `rejected`.
class ApiDepthIngestOutcome {
  final String outcome;
  final String? state;
  final String? reason;

  const ApiDepthIngestOutcome({required this.outcome, this.state, this.reason});

  @override
  int get hashCode => outcome.hashCode ^ state.hashCode ^ reason.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ApiDepthIngestOutcome &&
          runtimeType == other.runtimeType &&
          outcome == other.outcome &&
          state == other.state &&
          reason == other.reason;
}

class ApiDepthLockStatus {
  final bool available;
  final int goals;
  final int queueCapacity;
  final BigInt queued;
  final BigInt processed;
  final BigInt dropped;
  final BigInt evidenceAdded;
  final BigInt evidenceRejected;
  final BigInt lastFrameMs;
  final BigInt maxFrameMs;

  const ApiDepthLockStatus({
    required this.available,
    required this.goals,
    required this.queueCapacity,
    required this.queued,
    required this.processed,
    required this.dropped,
    required this.evidenceAdded,
    required this.evidenceRejected,
    required this.lastFrameMs,
    required this.maxFrameMs,
  });

  @override
  int get hashCode =>
      available.hashCode ^
      goals.hashCode ^
      queueCapacity.hashCode ^
      queued.hashCode ^
      processed.hashCode ^
      dropped.hashCode ^
      evidenceAdded.hashCode ^
      evidenceRejected.hashCode ^
      lastFrameMs.hashCode ^
      maxFrameMs.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ApiDepthLockStatus &&
          runtimeType == other.runtimeType &&
          available == other.available &&
          goals == other.goals &&
          queueCapacity == other.queueCapacity &&
          queued == other.queued &&
          processed == other.processed &&
          dropped == other.dropped &&
          evidenceAdded == other.evidenceAdded &&
          evidenceRejected == other.evidenceRejected &&
          lastFrameMs == other.lastFrameMs &&
          maxFrameMs == other.maxFrameMs;
}

/// The fixed measurement definition of a goal revision. `scale_arcsec` is
/// the aperture side; `threshold` the required lower-quartile depth score
/// (dimensionless, 3–100); `min_coverage` the fraction of apertures that must
/// be measurable (0.9–1.0); the systematic floor is the documented
/// calibration error in ADU that is never averaged down.
class ApiDepthMeasurement {
  final ApiSkyRectangle region;
  final ApiSkyRectangle background;
  final double scaleArcsec;
  final double threshold;
  final double minCoverage;
  final double systematicFloorAdu;
  final String systematicFloorSource;

  const ApiDepthMeasurement({
    required this.region,
    required this.background,
    required this.scaleArcsec,
    required this.threshold,
    required this.minCoverage,
    required this.systematicFloorAdu,
    required this.systematicFloorSource,
  });

  @override
  int get hashCode =>
      region.hashCode ^
      background.hashCode ^
      scaleArcsec.hashCode ^
      threshold.hashCode ^
      minCoverage.hashCode ^
      systematicFloorAdu.hashCode ^
      systematicFloorSource.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ApiDepthMeasurement &&
          runtimeType == other.runtimeType &&
          region == other.region &&
          background == other.background &&
          scaleArcsec == other.scaleArcsec &&
          threshold == other.threshold &&
          minCoverage == other.minCoverage &&
          systematicFloorAdu == other.systematicFloorAdu &&
          systematicFloorSource == other.systematicFloorSource;
}

/// What a candidate reference file offers. Geometry and acquisition are
/// reported separately with their own refusal reasons so the UI can say
/// exactly what is missing.
class ApiDepthReferenceInfo {
  final int width;
  final int height;
  final String pixelType;
  final bool monochrome;
  final ApiReferenceGeometry? geometry;
  final String? geometryIssue;
  final double? pixelScaleArcsec;
  final ApiAcquisitionSettings? acquisition;
  final String? acquisitionIssue;

  const ApiDepthReferenceInfo({
    required this.width,
    required this.height,
    required this.pixelType,
    required this.monochrome,
    this.geometry,
    this.geometryIssue,
    this.pixelScaleArcsec,
    this.acquisition,
    this.acquisitionIssue,
  });

  @override
  int get hashCode =>
      width.hashCode ^
      height.hashCode ^
      pixelType.hashCode ^
      monochrome.hashCode ^
      geometry.hashCode ^
      geometryIssue.hashCode ^
      pixelScaleArcsec.hashCode ^
      acquisition.hashCode ^
      acquisitionIssue.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ApiDepthReferenceInfo &&
          runtimeType == other.runtimeType &&
          width == other.width &&
          height == other.height &&
          pixelType == other.pixelType &&
          monochrome == other.monochrome &&
          geometry == other.geometry &&
          geometryIssue == other.geometryIssue &&
          pixelScaleArcsec == other.pixelScaleArcsec &&
          acquisition == other.acquisition &&
          acquisitionIssue == other.acquisitionIssue;
}

/// One committed evaluation. `state` is one of `insufficientEvidence`,
/// `collecting`, `confirmationPending`, `achieved`, `unreliable`.
class ApiDepthReport {
  final String state;
  final double? score;
  final double? conservativeScore;
  final double? uncertaintyAdu;
  final double coverage;
  final int evidenceFrames;
  final int confirmationFrames;
  final String reason;
  final ApiDepthForecast? forecast;

  const ApiDepthReport({
    required this.state,
    this.score,
    this.conservativeScore,
    this.uncertaintyAdu,
    required this.coverage,
    required this.evidenceFrames,
    required this.confirmationFrames,
    required this.reason,
    this.forecast,
  });

  @override
  int get hashCode =>
      state.hashCode ^
      score.hashCode ^
      conservativeScore.hashCode ^
      uncertaintyAdu.hashCode ^
      coverage.hashCode ^
      evidenceFrames.hashCode ^
      confirmationFrames.hashCode ^
      reason.hashCode ^
      forecast.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ApiDepthReport &&
          runtimeType == other.runtimeType &&
          state == other.state &&
          score == other.score &&
          conservativeScore == other.conservativeScore &&
          uncertaintyAdu == other.uncertaintyAdu &&
          coverage == other.coverage &&
          evidenceFrames == other.evidenceFrames &&
          confirmationFrames == other.confirmationFrames &&
          reason == other.reason &&
          forecast == other.forecast;
}

/// A reference frame's frozen TAN geometry (FITS 1-based CRPIX, CD in
/// degrees per pixel).
class ApiReferenceGeometry {
  final int width;
  final int height;
  final double crval1;
  final double crval2;
  final double crpix1;
  final double crpix2;
  final double cd11;
  final double cd12;
  final double cd21;
  final double cd22;

  const ApiReferenceGeometry({
    required this.width,
    required this.height,
    required this.crval1,
    required this.crval2,
    required this.crpix1,
    required this.crpix2,
    required this.cd11,
    required this.cd12,
    required this.cd21,
    required this.cd22,
  });

  @override
  int get hashCode =>
      width.hashCode ^
      height.hashCode ^
      crval1.hashCode ^
      crval2.hashCode ^
      crpix1.hashCode ^
      crpix2.hashCode ^
      cd11.hashCode ^
      cd12.hashCode ^
      cd21.hashCode ^
      cd22.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ApiReferenceGeometry &&
          runtimeType == other.runtimeType &&
          width == other.width &&
          height == other.height &&
          crval1 == other.crval1 &&
          crval2 == other.crval2 &&
          crpix1 == other.crpix1 &&
          crpix2 == other.crpix2 &&
          cd11 == other.cd11 &&
          cd12 == other.cd12 &&
          cd21 == other.cd21 &&
          cd22 == other.cd22;
}

/// A tangent-plane rectangle on the sky. Center in ICRS degrees, sides in
/// arcseconds, rotation in degrees (position angle of the rectangle's
/// height axis, east of north).
class ApiSkyRectangle {
  final double raDeg;
  final double decDeg;
  final double widthArcsec;
  final double heightArcsec;
  final double rotationDeg;

  const ApiSkyRectangle({
    required this.raDeg,
    required this.decDeg,
    required this.widthArcsec,
    required this.heightArcsec,
    required this.rotationDeg,
  });

  @override
  int get hashCode =>
      raDeg.hashCode ^
      decDeg.hashCode ^
      widthArcsec.hashCode ^
      heightArcsec.hashCode ^
      rotationDeg.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ApiSkyRectangle &&
          runtimeType == other.runtimeType &&
          raDeg == other.raDeg &&
          decDeg == other.decDeg &&
          widthArcsec == other.widthArcsec &&
          heightArcsec == other.heightArcsec &&
          rotationDeg == other.rotationDeg;
}
