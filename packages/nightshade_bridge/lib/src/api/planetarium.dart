// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.11.1.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';
import 'package:freezed_annotation/freezed_annotation.dart' hide protected;
part 'planetarium.freezed.dart';

// These functions are ignored because they are not marked as `pub`: `lookup`, `next_id`, `registry`, `with_planetarium`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`

/// Create a planetarium instance bound to the Flutter engine handle.
PlatformInt64 planetariumCreate({required PlatformInt64 engineHandle}) =>
    RustLib.instance.api
        .crateApiPlanetariumPlanetariumCreate(engineHandle: engineHandle);

/// Submit a resize and return the currently-published texture id (`0` while
/// the first allocation is still pending). **Non-blocking.**
///
/// We *cannot* block the FFI-sync caller waiting for the render thread to
/// publish a new texture id, because the texture-creation closure must run
/// on the Flutter platform thread (irondash requirement) — and on Windows
/// Flutter desktop, the Dart UI isolate that invokes this FRB-sync function
/// runs **on that same platform thread**. Blocking here freezes the Win32
/// message pump, which prevents `EngineContext::perform_on_main_thread` from
/// ever dispatching its closure → permanent self-deadlock with no texture
/// ever produced.
///
/// Instead: queue the `Resize` command, return whatever id is currently in
/// the atomic, and let the caller poll. Once this function returns, the
/// platform thread resumes its message pump, the closure runs, the render
/// thread publishes the new texture id, and the next poll picks it up.
PlatformInt64 planetariumResize(
        {required PlatformInt64 handle,
        required int w,
        required int h,
        required double dpr}) =>
    RustLib.instance.api.crateApiPlanetariumPlanetariumResize(
        handle: handle, w: w, h: h, dpr: dpr);

/// Read the currently-published Flutter texture id without queuing a resize.
/// Returns `0` if no successful allocation has happened yet, or a positive
/// integer once the render thread has published one.
PlatformInt64 planetariumTextureId({required PlatformInt64 handle}) =>
    RustLib.instance.api
        .crateApiPlanetariumPlanetariumTextureId(handle: handle);

/// Read the last surface-allocate error from the render thread, if any.
/// Returns `None` when allocation is still pending or has succeeded.
String? planetariumLastSurfaceError({required PlatformInt64 handle}) =>
    RustLib.instance.api
        .crateApiPlanetariumPlanetariumLastSurfaceError(handle: handle);

/// Update view pose (pan/zoom/roll/projection).
void planetariumSetPose(
        {required PlatformInt64 handle, required ViewPoseDto pose}) =>
    RustLib.instance.api
        .crateApiPlanetariumPlanetariumSetPose(handle: handle, pose: pose);

/// Update simulated/observed time.
void planetariumSetTime(
        {required PlatformInt64 handle, required AstroTimeDto time}) =>
    RustLib.instance.api
        .crateApiPlanetariumPlanetariumSetTime(handle: handle, time: time);

/// Update observer site and atmosphere inputs.
void planetariumSetObserver(
        {required PlatformInt64 handle, required ObserverDto observer}) =>
    RustLib.instance.api.crateApiPlanetariumPlanetariumSetObserver(
        handle: handle, observer: observer);

/// Update layer visibility and quality knobs.
void planetariumSetConfig(
        {required PlatformInt64 handle, required RenderConfigDto config}) =>
    RustLib.instance.api.crateApiPlanetariumPlanetariumSetConfig(
        handle: handle, config: config);

/// Forward a normalized gesture event; updates view pose on pan/zoom/rotate.
void planetariumPushGesture(
        {required PlatformInt64 handle, required GestureEventDto evt}) =>
    RustLib.instance.api
        .crateApiPlanetariumPlanetariumPushGesture(handle: handle, evt: evt);

/// Screen pick via projection inverse and registered catalog hit indexes.
SelectedObjectDto? planetariumHitTest(
        {required PlatformInt64 handle,
        required double x,
        required double y}) =>
    RustLib.instance.api
        .crateApiPlanetariumPlanetariumHitTest(handle: handle, x: x, y: y);

/// Register a star pack for catalog queries and hit testing (tests / pack-load path).
Future<void> planetariumRegisterStarPack(
        {required PlatformInt64 handle, required BoxStarPack pack}) =>
    RustLib.instance.api.crateApiPlanetariumPlanetariumRegisterStarPack(
        handle: handle, pack: pack);

/// Load a verified catalog pack from disk, register star tiles, and mark catalog dirty.
void planetariumLoadPack(
        {required PlatformInt64 handle, required String path}) =>
    RustLib.instance.api
        .crateApiPlanetariumPlanetariumLoadPack(handle: handle, path: path);

/// Set or clear the selected object (reprojects screen position each frame).
void planetariumSetSelection(
        {required PlatformInt64 handle, SelectedObjectDto? selected}) =>
    RustLib.instance.api.crateApiPlanetariumPlanetariumSetSelection(
        handle: handle, selected: selected);

/// Update the catalog tracking target used by [`PoseLockDto::LockedToTarget`].
///
/// Sent from Dart whenever the user picks a star/DSO to follow. Passing
/// `None` clears the target, which causes the renderer to raise a
/// `MissingTarget` pose error if the lock is still
/// [`PoseLockDto::LockedToTarget`] — silent fall-back to a stale target is
/// explicitly avoided per `CLAUDE.md` "errors are a feature".
void planetariumSetTrackingTarget(
        {required PlatformInt64 handle, TrackingTargetDto? target}) =>
    RustLib.instance.api.crateApiPlanetariumPlanetariumSetTrackingTarget(
        handle: handle, target: target);

/// Update the pose-lock mode (free / target / mount / body).
///
/// Sent from Dart when the user toggles tracking. The lock mode and its
/// reference data (mount position, tracking target) are decoupled so the
/// Dart side can refresh either without rewriting the other; the renderer
/// fails loud when the required reference is missing for the requested
/// mode.
void planetariumSetPoseLock(
        {required PlatformInt64 handle, required PoseLockDto lock}) =>
    RustLib.instance.api
        .crateApiPlanetariumPlanetariumSetPoseLock(handle: handle, lock: lock);

/// Update the mount's last-reported equatorial position used by
/// [`PoseLockDto::LockedToMount`].
///
/// Pushed from the equipment layer whenever a telescope driver reports a new
/// RA/Dec. Passing `None` clears the cached position; combined with the lock
/// being [`PoseLockDto::LockedToMount`] this surfaces a `MissingMount` pose
/// error rather than silently freezing the view at a stale position.
void planetariumSetMountPosition(
        {required PlatformInt64 handle, MountPositionDto? mount}) =>
    RustLib.instance.api.crateApiPlanetariumPlanetariumSetMountPosition(
        handle: handle, mount: mount);

/// Read the latest published scene snapshot for overlay layers.
SceneSnapshotDto planetariumSnapshot({required PlatformInt64 handle}) =>
    RustLib.instance.api.crateApiPlanetariumPlanetariumSnapshot(handle: handle);

/// Tear down the planetarium handle (stops render thread and releases surface).
void planetariumDispose({required PlatformInt64 handle}) =>
    RustLib.instance.api.crateApiPlanetariumPlanetariumDispose(handle: handle);

// Rust type: RustOpaqueNom<flutter_rust_bridge::for_generated::RustAutoOpaqueInner<Box < dyn StarPack >>>
abstract class BoxStarPack implements RustOpaqueInterface {}

/// Astronomical time (FFI).
class AstroTimeDto {
  final double jdUtc;
  final double jdUt1;
  final double jdTt;

  const AstroTimeDto({
    required this.jdUtc,
    required this.jdUt1,
    required this.jdTt,
  });

  @override
  int get hashCode => jdUtc.hashCode ^ jdUt1.hashCode ^ jdTt.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is AstroTimeDto &&
          runtimeType == other.runtimeType &&
          jdUtc == other.jdUtc &&
          jdUt1 == other.jdUt1 &&
          jdTt == other.jdTt;
}

/// Solar-system body identifier carried across the FFI for pose-lock control.
///
/// Mirrors [`nightshade_planetarium::scene::BodyId`]. Used by Dart when the user
/// chooses to track a planet (e.g. "follow Jupiter" in the planetarium UI):
/// the host wraps the selection in [`PoseLockDto::LockedToBody`] and sends it
/// down via [`planetarium_set_pose_lock`].
enum BodyIdDto {
  mercury,
  venus,
  mars,
  jupiter,
  saturn,
  uranus,
  neptune,
  moon,
  sun,
  ;
}

/// Screen-space placement for a single constellation art overlay figure,
/// carried across the FFI per published snapshot.
///
/// Mirrors [`nightshade_planetarium::scene::ConstellationArtPlacement`]; the
/// renderer reprojects every visible figure each frame and the Flutter side
/// draws them as a `CustomPainter` overlay on top of the GPU texture.
class ConstellationArtPlacementDto {
  /// IAU three-letter abbreviation (e.g. `"Ori"`, `"UMa"`).
  final String abbreviation;

  /// Normalized screen X in widget coordinates (0 = left).
  final double screenX;

  /// Normalized screen Y in widget coordinates (0 = top).
  final double screenY;

  /// Size multiplier relative to the catalog figure.
  final double scale;

  /// Fill/stroke alpha multiplier in [0, 1].
  final double opacity;

  const ConstellationArtPlacementDto({
    required this.abbreviation,
    required this.screenX,
    required this.screenY,
    required this.scale,
    required this.opacity,
  });

  @override
  int get hashCode =>
      abbreviation.hashCode ^
      screenX.hashCode ^
      screenY.hashCode ^
      scale.hashCode ^
      opacity.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ConstellationArtPlacementDto &&
          runtimeType == other.runtimeType &&
          abbreviation == other.abbreviation &&
          screenX == other.screenX &&
          screenY == other.screenY &&
          scale == other.scale &&
          opacity == other.opacity;
}

/// Normalized gesture event (FFI).
class GestureEventDto {
  final GestureKindDto kind;
  final double x;
  final double y;
  final double dx;
  final double dy;
  final double vx;
  final double vy;
  final double factor;
  final double radians;

  const GestureEventDto({
    required this.kind,
    required this.x,
    required this.y,
    required this.dx,
    required this.dy,
    required this.vx,
    required this.vy,
    required this.factor,
    required this.radians,
  });

  @override
  int get hashCode =>
      kind.hashCode ^
      x.hashCode ^
      y.hashCode ^
      dx.hashCode ^
      dy.hashCode ^
      vx.hashCode ^
      vy.hashCode ^
      factor.hashCode ^
      radians.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is GestureEventDto &&
          runtimeType == other.runtimeType &&
          kind == other.kind &&
          x == other.x &&
          y == other.y &&
          dx == other.dx &&
          dy == other.dy &&
          vx == other.vx &&
          vy == other.vy &&
          factor == other.factor &&
          radians == other.radians;
}

/// Gesture phase forwarded from Flutter (FFI).
enum GestureKindDto {
  panStart,
  panUpdate,
  panEnd,
  zoomStart,
  zoomUpdate,
  zoomEnd,
  rotateStart,
  rotateUpdate,
  rotateEnd,
  tap,
  doubleTap,
  cancel,
  ;
}

/// Label category for overlay styling (FFI).
enum LabelCategoryDto {
  star,
  dso,
  constellation,
  body,
  satellite,
  minorPlanet,
  ;
}

/// Screen-space label hint (FFI).
class LabelHintDto {
  final BigInt objectId;
  final double screenX;
  final double screenY;
  final double apparentMag;
  final int priority;
  final String text;
  final LabelCategoryDto category;

  const LabelHintDto({
    required this.objectId,
    required this.screenX,
    required this.screenY,
    required this.apparentMag,
    required this.priority,
    required this.text,
    required this.category,
  });

  @override
  int get hashCode =>
      objectId.hashCode ^
      screenX.hashCode ^
      screenY.hashCode ^
      apparentMag.hashCode ^
      priority.hashCode ^
      text.hashCode ^
      category.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LabelHintDto &&
          runtimeType == other.runtimeType &&
          objectId == other.objectId &&
          screenX == other.screenX &&
          screenY == other.screenY &&
          apparentMag == other.apparentMag &&
          priority == other.priority &&
          text == other.text &&
          category == other.category;
}

/// Mount-reported equatorial position carried across the FFI. Mirrors
/// [`nightshade_planetarium::scene::MountPosition`].
///
/// Pushed by [`planetarium_set_mount_position`] from mount state updates
/// (ASCOM/INDI/Alpaca telescope drivers). When `None` is sent and the lock
/// mode is [`PoseLockDto::LockedToMount`], the renderer raises a
/// `MissingMount` pose error instead of silently freezing on a stale value.
class MountPositionDto {
  /// Mount right ascension (radians, J2000).
  final double raRad;

  /// Mount declination (radians, J2000).
  final double decRad;

  const MountPositionDto({
    required this.raRad,
    required this.decRad,
  });

  @override
  int get hashCode => raRad.hashCode ^ decRad.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is MountPositionDto &&
          runtimeType == other.runtimeType &&
          raRad == other.raRad &&
          decRad == other.decRad;
}

/// Observer site (FFI).
class ObserverDto {
  final double latitudeRad;
  final double longitudeRad;
  final double elevationM;
  final double pressureHpa;
  final double temperatureC;

  const ObserverDto({
    required this.latitudeRad,
    required this.longitudeRad,
    required this.elevationM,
    required this.pressureHpa,
    required this.temperatureC,
  });

  @override
  int get hashCode =>
      latitudeRad.hashCode ^
      longitudeRad.hashCode ^
      elevationM.hashCode ^
      pressureHpa.hashCode ^
      temperatureC.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ObserverDto &&
          runtimeType == other.runtimeType &&
          latitudeRad == other.latitudeRad &&
          longitudeRad == other.longitudeRad &&
          elevationM == other.elevationM &&
          pressureHpa == other.pressureHpa &&
          temperatureC == other.temperatureC;
}

@freezed
sealed class PoseLockDto with _$PoseLockDto {
  const PoseLockDto._();

  /// User-controlled pose; gestures write the free pose directly.
  const factory PoseLockDto.free() = PoseLockDto_Free;

  /// Center on the catalog object identified by `object_id`.
  const factory PoseLockDto.lockedToTarget({
    required BigInt objectId,
  }) = PoseLockDto_LockedToTarget;

  /// Center on the mount's most-recently reported RA/Dec.
  const factory PoseLockDto.lockedToMount() = PoseLockDto_LockedToMount;

  /// Center on the named solar-system body at the current time.
  const factory PoseLockDto.lockedToBody({
    required BodyIdDto body,
  }) = PoseLockDto_LockedToBody;
}

/// Per-layer render configuration (FFI).
class RenderConfigDto {
  final bool showStars;
  final bool showConstellations;
  final bool showConstellationBoundaries;
  final bool showConstellationArt;
  final bool showEquatorialGrid;
  final bool showAltAzGrid;
  final bool showGalacticGrid;
  final bool showEcliptic;
  final bool showGalacticPlane;
  final bool showMilkyWay;
  final bool showHorizon;
  final bool showAtmosphere;
  final bool showDsos;
  final bool showSolarSystem;
  final bool showSatellites;
  final bool showMinorPlanets;
  final bool showVariableStars;
  final double magnitudeLimit;
  final int quality;
  final int bortleClass;
  final bool twinkle;

  const RenderConfigDto({
    required this.showStars,
    required this.showConstellations,
    required this.showConstellationBoundaries,
    required this.showConstellationArt,
    required this.showEquatorialGrid,
    required this.showAltAzGrid,
    required this.showGalacticGrid,
    required this.showEcliptic,
    required this.showGalacticPlane,
    required this.showMilkyWay,
    required this.showHorizon,
    required this.showAtmosphere,
    required this.showDsos,
    required this.showSolarSystem,
    required this.showSatellites,
    required this.showMinorPlanets,
    required this.showVariableStars,
    required this.magnitudeLimit,
    required this.quality,
    required this.bortleClass,
    required this.twinkle,
  });

  @override
  int get hashCode =>
      showStars.hashCode ^
      showConstellations.hashCode ^
      showConstellationBoundaries.hashCode ^
      showConstellationArt.hashCode ^
      showEquatorialGrid.hashCode ^
      showAltAzGrid.hashCode ^
      showGalacticGrid.hashCode ^
      showEcliptic.hashCode ^
      showGalacticPlane.hashCode ^
      showMilkyWay.hashCode ^
      showHorizon.hashCode ^
      showAtmosphere.hashCode ^
      showDsos.hashCode ^
      showSolarSystem.hashCode ^
      showSatellites.hashCode ^
      showMinorPlanets.hashCode ^
      showVariableStars.hashCode ^
      magnitudeLimit.hashCode ^
      quality.hashCode ^
      bortleClass.hashCode ^
      twinkle.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is RenderConfigDto &&
          runtimeType == other.runtimeType &&
          showStars == other.showStars &&
          showConstellations == other.showConstellations &&
          showConstellationBoundaries == other.showConstellationBoundaries &&
          showConstellationArt == other.showConstellationArt &&
          showEquatorialGrid == other.showEquatorialGrid &&
          showAltAzGrid == other.showAltAzGrid &&
          showGalacticGrid == other.showGalacticGrid &&
          showEcliptic == other.showEcliptic &&
          showGalacticPlane == other.showGalacticPlane &&
          showMilkyWay == other.showMilkyWay &&
          showHorizon == other.showHorizon &&
          showAtmosphere == other.showAtmosphere &&
          showDsos == other.showDsos &&
          showSolarSystem == other.showSolarSystem &&
          showSatellites == other.showSatellites &&
          showMinorPlanets == other.showMinorPlanets &&
          showVariableStars == other.showVariableStars &&
          magnitudeLimit == other.magnitudeLimit &&
          quality == other.quality &&
          bortleClass == other.bortleClass &&
          twinkle == other.twinkle;
}

/// Immutable per-frame scene snapshot for Dart overlays (FFI).
class SceneSnapshotDto {
  final BigInt frameId;
  final ViewPoseDto viewPose;
  final List<LabelHintDto> labels;
  final List<ConstellationArtPlacementDto> constellationArt;
  final SelectedObjectDto? selected;

  const SceneSnapshotDto({
    required this.frameId,
    required this.viewPose,
    required this.labels,
    required this.constellationArt,
    this.selected,
  });

  @override
  int get hashCode =>
      frameId.hashCode ^
      viewPose.hashCode ^
      labels.hashCode ^
      constellationArt.hashCode ^
      selected.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is SceneSnapshotDto &&
          runtimeType == other.runtimeType &&
          frameId == other.frameId &&
          viewPose == other.viewPose &&
          labels == other.labels &&
          constellationArt == other.constellationArt &&
          selected == other.selected;
}

/// Selected object projected to screen space (FFI).
class SelectedObjectDto {
  final BigInt objectId;
  final double screenX;
  final double screenY;
  final double raRad;
  final double decRad;
  final LabelCategoryDto category;
  final String displayName;

  const SelectedObjectDto({
    required this.objectId,
    required this.screenX,
    required this.screenY,
    required this.raRad,
    required this.decRad,
    required this.category,
    required this.displayName,
  });

  @override
  int get hashCode =>
      objectId.hashCode ^
      screenX.hashCode ^
      screenY.hashCode ^
      raRad.hashCode ^
      decRad.hashCode ^
      category.hashCode ^
      displayName.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is SelectedObjectDto &&
          runtimeType == other.runtimeType &&
          objectId == other.objectId &&
          screenX == other.screenX &&
          screenY == other.screenY &&
          raRad == other.raRad &&
          decRad == other.decRad &&
          category == other.category &&
          displayName == other.displayName;
}

/// Sky projection mode (FFI).
enum SkyProjectionDto {
  stereographic,
  orthographic,
  azimuthalEquidistant,
  ;
}

/// Catalog tracking target carried across the FFI. Mirrors
/// [`nightshade_planetarium::scene::TrackingTarget`].
///
/// Pushed by [`planetarium_set_tracking_target`] whenever the Dart side
/// selects a star/DSO to follow; the renderer holds the most-recent value and
/// uses it when [`PoseLockDto::LockedToTarget`] is active. The `object_id`
/// MUST match the id in the lock — the renderer fails loud (no silent reuse)
/// when the ids do not agree, mirroring the `MismatchedTarget` error in
/// `PoseError`.
class TrackingTargetDto {
  /// Catalog object id (must match the id in `PoseLockDto::LockedToTarget`).
  final BigInt objectId;

  /// Target right ascension (radians, J2000).
  final double raRad;

  /// Target declination (radians, J2000).
  final double decRad;

  const TrackingTargetDto({
    required this.objectId,
    required this.raRad,
    required this.decRad,
  });

  @override
  int get hashCode => objectId.hashCode ^ raRad.hashCode ^ decRad.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is TrackingTargetDto &&
          runtimeType == other.runtimeType &&
          objectId == other.objectId &&
          raRad == other.raRad &&
          decRad == other.decRad;
}

/// View pose crossing the FFI boundary.
class ViewPoseDto {
  final double raRad;
  final double decRad;
  final double fovRad;
  final double rollRad;
  final SkyProjectionDto projection;

  const ViewPoseDto({
    required this.raRad,
    required this.decRad,
    required this.fovRad,
    required this.rollRad,
    required this.projection,
  });

  @override
  int get hashCode =>
      raRad.hashCode ^
      decRad.hashCode ^
      fovRad.hashCode ^
      rollRad.hashCode ^
      projection.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ViewPoseDto &&
          runtimeType == other.runtimeType &&
          raRad == other.raRad &&
          decRad == other.decRad &&
          fovRad == other.fovRad &&
          rollRad == other.rollRad &&
          projection == other.projection;
}
