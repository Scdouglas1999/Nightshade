// The form primitives (text/number/toggle/dropdown inputs, property-field
// wrapper, quick-time and destructive-action buttons) live in the public
// `node_property_widgets.dart` library, which the panel imports directly.
part of '../node_properties_panel.dart';

// _NodeTypeBadge is gone: the node's name and type were a 64 px tinted
// hero at the top of a 300 px column that then had 40 px left for the
// settings the operator opened it for. The SectionTitle in
// node_properties_panel.dart names the node in one 20 px row.
